================================================================================
  SECURITY NETWORK DIAGRAM: Training Hub
  Type: Python Library (not a deployed service)
  Distribution: RHOAI
================================================================================

NOTE: training_hub is a Python library imported by user code, not a
standalone service. It does not expose any inbound network surfaces
(no HTTP endpoints, no gRPC services, no CRDs, no Kubernetes Services).
All network activity is outbound-only, initiated by backend implementations
at training time.

┌─────────────────────────────────────────────────────────────────────────┐
│  USER ENVIRONMENT (Notebook / Training Job Pod)                         │
│                                                                          │
│  [User Code]                                                             │
│  - Imports training_hub as Python library                                │
│  - Calls: sft(), osft(), lora_sft(), lora_grpo(), gepa()               │
│         │                                                                │
│         │ In-process Python call                                         │
│         │ No network boundary                                            │
│         ▼                                                                │
│  [Training Hub Library]                                                  │
│  - AlgorithmRegistry → Algorithm → Backend                               │
│  - No inbound authentication (library, not service)                      │
│  - 0 inbound network surfaces                                            │
│         │                                                                │
│         ├──── In-Process ────────────────────────────────────────┐       │
│         │     (instructlab-training, unsloth, gepa backends)     │       │
│         │     No additional network boundary                     │       │
│         │                                                        │       │
│         ├──── Subprocess ────────────────────────────────────────┤       │
│         │     (verl backend)                                     │       │
│         │     subprocess.run() → Ray/torchrun workers            │       │
│         │     Local IPC / cluster networking                     │       │
│         │                                                        │       │
│         └──── Co-located Engine ─────────────────────────────────┘       │
│               (ART backend)                                              │
│               Manages vLLM AsyncLLM engine in-process                    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  OUTBOUND CONNECTIONS (from backend implementations)                     │
│                                                                          │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  [1] LLM API (via litellm)                      │                    │
│  │  - Purpose: GRPO reward scoring                  │                    │
│  │  - Backends: verl, ART (LoRA GRPO)               │                    │
│  │  - Protocol: HTTPS                               │                    │
│  │  - Port: 443/TCP (typical)                       │                    │
│  │  - Encryption: TLS 1.2+                          │                    │
│  │  - Auth: OPENAI_API_KEY (env var)                │                    │
│  │  - Base URL: OPENAI_API_BASE (env var, optional) │                    │
│  │  - Provisioned by: User at runtime               │                    │
│  └─────────────────────────────────────────────────┘                    │
│                                                                          │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  [2] MLflow Tracking Server                      │                    │
│  │  - Purpose: Training metrics and parameter logging│                   │
│  │  - Protocol: HTTPS (typical)                     │                    │
│  │  - Port: Configured via URI                      │                    │
│  │  - Encryption: TLS (server-dependent)            │                    │
│  │  - Auth: MLFLOW_TRACKING_URI (env var)           │                    │
│  │  - Provisioned by: User at runtime               │                    │
│  └─────────────────────────────────────────────────┘                    │
│                                                                          │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  [3] Weights & Biases API                        │                    │
│  │  - Purpose: Training metrics logging              │                   │
│  │  - Protocol: HTTPS                               │                    │
│  │  - Port: 443/TCP                                 │                    │
│  │  - Encryption: TLS 1.2+                          │                    │
│  │  - Auth: WANDB_API_KEY (env var)                 │                    │
│  │  - Entity: WANDB_ENTITY (env var)                │                    │
│  │  - Provisioned by: User at runtime               │                    │
│  └─────────────────────────────────────────────────┘                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

CREDENTIAL INVENTORY:
┌──────────────────────┬─────────────────────┬───────────────────────────┐
│ Credential           │ Type                │ Purpose                   │
├──────────────────────┼─────────────────────┼───────────────────────────┤
│ OPENAI_API_KEY       │ Environment Variable│ LLM API authentication    │
│ OPENAI_API_BASE      │ Environment Variable│ LLM API base URL override │
│ MLFLOW_TRACKING_URI  │ Environment Variable│ MLflow server endpoint    │
│ WANDB_API_KEY        │ Environment Variable│ W&B API authentication    │
│ WANDB_ENTITY         │ Environment Variable│ W&B organization/entity   │
└──────────────────────┴─────────────────────┴───────────────────────────┘

  All credentials are user-provisioned at runtime via environment variables.
  The GEPA backend manages environment variable lifecycle with save/restore
  semantics (saves current values, sets required values, restores on exit).

RBAC Summary:
  No Kubernetes RBAC roles or bindings defined.
  training_hub is a Python library — it does not create ServiceAccounts,
  ClusterRoles, or RoleBindings.

Service Mesh Configuration:
  Not applicable — training_hub is not a deployed Kubernetes service.
  Network policies depend on the hosting environment (notebook server,
  training job pod, etc.).

Secrets:
  No Kubernetes Secrets defined.
  All credentials are passed as environment variables by the user.

SECURITY NOTES:
  - 0 inbound authentication rules (library, not a service)
  - 5 outbound credential references (all user-provisioned env vars)
  - GEPA backend has env var save/restore semantics to prevent credential leakage
  - verl backend spawns subprocesses that inherit the parent process environment
  - No service identity or mTLS — library runs within host process context
