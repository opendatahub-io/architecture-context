┌─────────────────────────────────────────────────────────────────────────┐
│  CLI EXECUTION ENVIRONMENT (CI/CD or Developer Workstation)             │
│                                                                          │
│  Note: model-metadata-collection is a CLI batch tool, NOT a deployed     │
│  Kubernetes service. It has no inbound network surfaces (no listeners,   │
│  no HTTP/gRPC endpoints, no CRDs). All network activity is outbound.     │
│                                                                          │
│  [model-extractor CLI]                                                   │
│  - Binary: model-extractor                                               │
│  - Concurrency: goroutines (max-concurrent=5, semaphore-throttled)       │
│  - Config: .env file (HF_TOKEN, GITHUB_TOKEN)                           │
│  - No listening ports                                                    │
│  - No Kubernetes RBAC (not deployed as workload)                         │
│         │                                                                │
│         │                                                                │
│         ├──────────────────────────────────────────────────────────┐      │
│         │                                                         │      │
│         │ 1. OCI Registry                                         │      │
│         │    Port: 443/TCP                                        │      │
│         │    Protocol: HTTPS                                      │      │
│         │    Encryption: TLS 1.2+                                 │      │
│         │    Auth: containers/image library defaults               │      │
│         │          (Docker config.json / registry auth)           │      │
│         │    Operations: GET manifests, GET blobs (layers)        │      │
│         │                                                         │      │
│         ├──────────────────────────────────────────────────────────┤      │
│         │                                                         │      │
│         │ 2. HuggingFace API                                     │      │
│         │    Port: 443/TCP                                        │      │
│         │    Protocol: HTTPS                                      │      │
│         │    Encryption: TLS 1.2+                                 │      │
│         │    Auth: Bearer Token (HF_TOKEN env var, optional)     │      │
│         │    Operations: GET /api/collections, GET /api/models    │      │
│         │                                                         │      │
│         ├──────────────────────────────────────────────────────────┤      │
│         │                                                         │      │
│         │ 3. GitHub API                                           │      │
│         │    Port: 443/TCP                                        │      │
│         │    Protocol: HTTPS                                      │      │
│         │    Encryption: TLS 1.2+                                 │      │
│         │    Auth: Bearer Token (GITHUB_TOKEN env var, optional)  │      │
│         │    Operations: GET repos/.../contents (agent.yaml,      │      │
│         │                README.md)                               │      │
│         │                                                         │      │
│         └──────────────────────────────────────────────────────────┘      │
│                                                                          │
│  [metadata-report CLI]                                                   │
│  - Binary: metadata-report                                               │
│  - No network activity (reads local files only)                          │
│  - No listening ports                                                    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Outbound Only)                                       │
│                                                                          │
│  [registry.redhat.io]                                                    │
│  - Purpose: OCI container image registry for Red Hat AI models           │
│  - Auth: Docker config / registry credentials                            │
│  - Encryption: TLS 1.2+                                                  │
│  - Data: Container manifests, model card layers (tar/gzip)              │
│                                                                          │
│  [huggingface.co]                                                        │
│  - Purpose: Model metadata enrichment (tags, license, README, tools)    │
│  - Auth: Bearer HF_TOKEN (optional, rate-limited without)               │
│  - Encryption: TLS 1.2+                                                  │
│  - Data: Model details, collection data, README content                 │
│                                                                          │
│  [api.github.com]                                                        │
│  - Purpose: Agent catalog data (agent.yaml, README.md from repos)       │
│  - Auth: Bearer GITHUB_TOKEN (optional, rate-limited without)           │
│  - Encryption: TLS 1.2+                                                  │
│  - Data: YAML config files, Markdown documentation                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC configured (CLI tool, not deployed as a workload)
- No ServiceAccounts
- No ClusterRoles or RoleBindings

Service Mesh Configuration:
- Not applicable (CLI tool, not part of service mesh)

Secrets / Credentials:
- HF_TOKEN (environment variable) - HuggingFace API token, optional, loaded from .env
- GITHUB_TOKEN (environment variable) - GitHub API token, optional, loaded from .env
- OCI registry credentials - Via containers/image library defaults (Docker config.json)
- Shell environment variables take precedence over .env file values

Network Attack Surface:
- No inbound attack surface (no listeners, no endpoints)
- Outbound-only connections to three external HTTPS services
- All outbound connections use TLS 1.2+
- Authentication tokens are optional (tool functions without them, with rate limits)
