┌─────────────────────────────────────────────────────────────────────────┐
│  LOCAL / OPERATOR WORKSTATION                                            │
│                                                                          │
│  [kubectl-rhaii_validate CLI]                                            │
│  - Runs locally or as a pod                                              │
│  - Auth: kubeconfig credential chain (admin-level)                       │
│         │                                                                │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS / WebSocket                                    │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: kubeconfig (user cert/token/OIDC)                        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 6443/TCP                                                        │
│  - Protocol: HTTPS + WebSocket (log streaming)                           │
│  - TLS: 1.2+                                                             │
│                                                                          │
│  Resource Operations (all via kube-apiserver):                            │
│  ┌───────────────────────────────────────────────┐                       │
│  │ /v1/Namespace          - create, delete       │                       │
│  │ /v1/ServiceAccount     - create               │                       │
│  │ /v1/ConfigMap          - create, get, update   │                       │
│  │ /v1/Pod                - get, list, logs       │                       │
│  │ /v1/Node               - list (platform detect)│                      │
│  │ batch/v1/Job           - create, watch         │                       │
│  │ rbac/v1/ClusterRole    - create, delete        │                       │
│  │ rbac/v1/ClusterRoleBinding - create, delete    │                       │
│  └───────────────────────────────────────────────┘                       │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  WORKLOAD PLANE (Ephemeral - Namespace: rhaii-validation)                │
│                                                                          │
│  [Per-Node Validation Job Pods]                                          │
│  - ServiceAccount: rhaii-validator                                       │
│  - SA token mount: DISABLED (automountServiceAccountToken: false)        │
│  - Images:                                                               │
│    - Validator image (self-reference for `run` subcommand)               │
│    - Tools image (iperf3, RDMA tools)                                    │
│  - Security Context: Privileged (OpenShift SCC)                          │
│                                                                          │
│  No inbound network services exposed                                     │
│  No ingress                                                              │
│                                                                          │
│  Inter-pod communication (multi-node tests only):                        │
│  ┌───────────────────────────────────────────────┐                       │
│  │ TCP bandwidth: iperf3 (ephemeral ports)       │                       │
│  │ RDMA connectivity: ibv_rc_pingpong            │                       │
│  │ RDMA bandwidth: ib_write_bw                   │                       │
│  │ Topology: ring or star (configurable)         │                       │
│  └───────────────────────────────────────────────┘                       │
│                                                                          │
│  Hardware access (host-level):                                           │
│  ┌───────────────────────────────────────────────┐                       │
│  │ GPU devices (NVIDIA/AMD)                      │                       │
│  │ Host sysfs (device topology, NIC status)      │                       │
│  │ RDMA devices (InfiniBand verbs)               │                       │
│  └───────────────────────────────────────────────┘                       │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None - CLI only communicates with Kubernetes API                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

Ephemeral ClusterRole: rhaii-validator
  - Created at runtime by controller
  - Rules: empty (no API permissions for workload pods)
  - Deleted on exit (deferred cleanup)

ClusterRoleBindings (ephemeral):
  1. rhaii-validator
     - Namespace: rhaii-validation
     - Role: rhaii-validator (ClusterRole)
     - ServiceAccount: rhaii-validator
     - Lifecycle: created/deleted by controller

  2. rhaii-validator-scc-{namespace} (OpenShift only)
     - Namespace: {namespace}
     - Role: system:openshift:scc:privileged (ClusterRole)
     - ServiceAccount: rhaii-validator
     - Purpose: Host sysfs access for GPU/RDMA checks
     - Lifecycle: created/deleted by controller

Controller Auth:
  - Auth: kubeconfig credential chain (admin-level permissions)
  - Permissions: Namespace, SA, ConfigMap, Node, Pod, Job,
    ClusterRole, ClusterRoleBinding CRUD operations
  - NOT scoped by RBAC - relies on invoking user's privileges

Workload Pod Auth:
  - ServiceAccount: rhaii-validator
  - Token mount: DISABLED (automountServiceAccountToken: false)
  - No API access from workload pods

Security Hardening (Recent):
  - ec99b2d: Deferred RBAC cleanup to prevent leftover cluster-scoped grants
  - db4dc72: Disabled SA token mount on workload pods
  - 5f907e6: Pinned image digests for supply chain integrity

Egress:
  - Destination: Kubernetes API (kube-apiserver)
  - Port: 6443/TCP
  - Protocol: HTTPS / WebSocket
  - Encryption: TLS 1.2+
  - Auth: kubeconfig credentials
  - No other external egress

Ingress:
  - None. No services exposed. No HTTP/gRPC endpoints.
