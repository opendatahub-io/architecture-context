┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [API Consumer]                                                          │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: TLS 1.2+ (optional, rustls + ring crypto)          │
│         │ Auth: Header passthrough (configured allowlist)                │
│         │       X-Forwarded-Access-Token → Bearer rewrite (optional)     │
│         │       Client certificate verification (optional mTLS)          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GUARDRAILS API SERVER (Port 8033)                                      │
│                                                                          │
│  [fms-guardrails-orchestr8]                                              │
│  - Runtime: Rust (axum HTTP + tonic gRPC client)                        │
│  - TLS: rustls with ring crypto provider                                │
│  - TLS Termination: Application-level (optional)                        │
│  - mTLS: Optional client certificate verification                       │
│  - Header Filtering: Configured passthrough_headers allowlist            │
│  - Token Rewrite: X-Forwarded-Access-Token → Authorization: Bearer      │
│                                                                          │
│  Endpoints:                                                              │
│    POST /api/v1/task/classification-with-text-generation                │
│    POST /api/v1/task/server-streaming-classification-with-text-gen      │
│    POST /api/v2/chat/completions-detection (conditional*)               │
│    POST /api/v2/text/completions-detection (conditional*)               │
│    POST /api/v2/text/detection/chat                                     │
│    POST /api/v2/text/detection/content                                  │
│    POST /api/v2/text/detection/context                                  │
│    POST /api/v2/text/detection/generated                                │
│    POST /api/v2/text/detection/stream-content                           │
│    POST /api/v2/text/generation-detection                               │
│  * Only registered when OpenAI-compatible generation is configured       │
│                                                                          │
│         │                     │                    │                      │
│         │ gRPC                │ gRPC               │ REST                 │
│         ▼                     ▼                    ▼                      │
├─────────────────────────────────────────────────────────────────────────┤
│  DOWNSTREAM SERVICES (Internal)                                         │
│                                                                          │
│  [TGIS Generation Service]                                               │
│  - Port: 8033/TCP                                                        │
│  - Protocol: gRPC (tonic)                                                │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Text generation via TGIS/NLP/caikit-nlp                    │
│                                                                          │
│  [Chunker Services]                                                      │
│  - Port: 8085/TCP                                                        │
│  - Protocol: gRPC (tonic)                                                │
│  - Encryption: TLS (optional, configurable via named TLS config)        │
│  - Auth: None                                                            │
│  - Purpose: Content chunking for detection pipelines                    │
│                                                                          │
│  [Detector Services]                                                     │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP/HTTPS (reqwest)                                        │
│  - Encryption: TLS (optional, configurable via named TLS config)        │
│  - Auth: Bearer token (optional, loaded from env var at config time)    │
│  - Concurrency: Configurable limit (default: 5 concurrent per task)     │
│  - Purpose: Content detection and classification                        │
│                                                                          │
│  [Detector Health Services]                                              │
│  - Port: 8081/TCP                                                        │
│  - Protocol: HTTP/HTTPS (reqwest)                                        │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Downstream health monitoring                                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  HEALTH SERVER (Port 8034) - Separate Listener                          │
│                                                                          │
│  [Health Server]                                                         │
│  - Port: 8034/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None                                                      │
│  - Auth: None (unauthenticated)                                         │
│  - Endpoints: GET /health, GET /info                                    │
│                                                                          │
│  [Health Monitor] ──── Port: 8034/TCP ──── [Health Server]              │
│                        Protocol: HTTP                                    │
│                        Encryption: None                                  │
│                        Auth: None                                        │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  No ClusterRoles or RoleBindings defined.
  (Application-level service - no Kubernetes API access required)

Secrets:
  - TLS server cert/key (kubernetes.io/tls)
    Purpose: Rust HTTP server TLS
    Provisioned by: Platform operator / cert-manager
    Auto-Rotate: Unknown

  - TLS client CA cert (Opaque)
    Purpose: Rust HTTP server mTLS client verification
    Provisioned by: Platform operator
    Auto-Rotate: Unknown

  - API tokens (Opaque, env var)
    Purpose: Downstream service authentication (Bearer tokens)
    Provisioned by: Platform operator
    Auto-Rotate: Unknown

  - Downstream TLS certs (Opaque)
    Purpose: Downstream service TLS client certificates
    Provisioned by: Platform operator
    Auto-Rotate: Unknown

Authentication & Authorization Summary:
  - /api/v1/*, /api/v2/*: Header passthrough with configured allowlist
  - /api/v1/*, /api/v2/*: X-Forwarded-Access-Token → Authorization: Bearer rewrite
  - All endpoints: Optional TLS (rustls) and mTLS (client cert verification)
  - /health, /info: Unauthenticated (no TLS, no auth)

TLS Configuration:
  - Inbound: rustls with ring crypto provider, TLS 1.2+
  - Named TLS configs: Reusable, per-service TLS configurations for downstream connections
  - Downstream gRPC (chunker): Optional TLS via named config
  - Downstream REST (detector): Optional TLS via named config
  - Downstream gRPC (TGIS): No TLS
  - Downstream REST (detector health): No TLS
