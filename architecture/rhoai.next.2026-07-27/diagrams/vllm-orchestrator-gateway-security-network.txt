┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client Application]                                                    │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (external TLS termination assumed)            │
│         │ Auth: Bearer Token (passthrough to orchestrator)               │
│         │ Path: POST /{route_name}/v1/chat/completions                  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY (Content Safety Interposition)                                 │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Binary: vllm-orchestrator-gateway (Rust/Axum/Tokio)                  │
│  - Listen Port: 8090/TCP                                                │
│  - Config: GATEWAY_CONFIG env → config/config.yaml                      │
│  - Function: Injects detector params, enforces fallback on detection    │
│  - Streaming: SSE (text/event-stream) + JSON responses                  │
│         │                                                                │
│         │ Port: Orchestrator port (configurable)                         │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: mTLS (when certs mounted) or plaintext             │
│         │   - Client cert: /etc/tls/private/tls.crt                     │
│         │   - Client key:  /etc/tls/private/tls.key                     │
│         │   - CA cert:     /etc/tls/ca/service-ca.crt                   │
│         │ Auth: Passthrough (Authorization, x-forwarded-* headers)       │
│         │ Path: POST /api/v2/chat/completions-detection                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INTERNAL SERVICE (Guardrails)                                          │
│                                                                          │
│  [TrustAI Guardrails Orchestrator]                                       │
│  - Endpoint: /api/v2/chat/completions-detection                         │
│  - Purpose: Content safety detection on inference requests              │
│  - Receives: Augmented payload with input/output detector maps          │
│  - Returns: Inference response + optional detection results             │
│         │                                                                │
│         │ Protocol: Component-specific                                   │
│         │ Purpose: Forward inference request                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INFERENCE BACKEND                                                       │
│                                                                          │
│  [vLLM Inference Endpoint]                                               │
│  - Purpose: LLM inference serving                                        │
│  - Auth: Managed by orchestrator                                         │
└─────────────────────────────────────────────────────────────────────────┘

Header Passthrough:
- Authorization: Forwarded from client to orchestrator (bearer token passthrough)
- x-forwarded-*: Forwarded from client to orchestrator (origin context)

mTLS Configuration (when enabled):
- Certificate mount: /etc/tls/private/tls.crt
- Private key mount: /etc/tls/private/tls.key
- CA certificate: /etc/tls/ca/service-ca.crt
- TLS library: OpenSSL (via openssl 0.10.73 + native-tls 0.2.12)
- Auto-upgrade: Gateway detects mounted certs and upgrades HTTP → HTTPS

Detection Behavior:
- When orchestrator response contains "detections" array
- AND route has "fallback_message" configured
- Gateway replaces response content with the fallback message
- Original inference output is suppressed

RBAC Summary:
- No Kubernetes RBAC roles or bindings defined in repository
- Component is likely deployed via external operator or Kustomize overlay

Secrets:
- TLS certificates mounted at /etc/tls/private/ (provisioned externally)
- CA certificate at /etc/tls/ca/service-ca.crt (provisioned by service CA)
- No secrets defined in repository manifests

Deployment Notes:
- No Kubernetes manifests (Deployments, Services, Ingress) found in repository
- Deployment is managed externally (operator or overlay)
- Configuration via GATEWAY_CONFIG environment variable
