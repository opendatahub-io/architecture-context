┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  No inbound runtime surfaces.                                            │
│  This component exposes NO HTTP, gRPC, or CRD-based APIs.               │
│  It operates as a batch workload invoked by external orchestration.      │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  WORKLOAD (Kubernetes Job / Pod)                                         │
│                                                                          │
│  [fms-hf-tuning]                                                         │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {job-service-account}                                 │
│  - Image: fms-hf-tuning:tag                                              │
│  - Runtime: Python ~=3.9                                                 │
│  - GPU: Optional (flash-attn, fms-acceleration, auto-gptq)              │
│  - No inbound ports exposed                                              │
│  - No readiness/liveness probes (batch workload)                         │
│         │                                                                │
│         │ Port: 443/TCP (configured by runtime)                          │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS                                                │
│         │ Auth: AWS IAM credentials (boto3)                              │
│         │   - S3_ACCESS_KEY_ID (env var)                                 │
│         │   - S3_SECRET_ACCESS_KEY (env var)                             │
│         │   - S3_ENDPOINT (env var)                                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3-Compatible Storage]                                                 │
│  - Purpose: Training dataset retrieval                                   │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS                                                       │
│  - Auth: AWS IAM credentials via boto3 SDK                               │
│  - SDK: boto3 >=1.34, <2.0                                               │
│                                                                          │
│  [Experiment Trackers] (Optional)                                        │
│  - Aim server (if configured)                                            │
│  - MLflow server (if configured)                                         │
│  - ClearML server (if configured)                                        │
│  - Protocol: Varies by tracker                                           │
│  - Encryption: Varies by deployment                                      │
│                                                                          │
│  [HuggingFace Hub] (Optional)                                            │
│  - Purpose: Model downloads via hf-transfer                              │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS                                                       │
│  - Auth: HF Token (if configured)                                        │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  No ClusterRoles or ClusterRoleBindings defined by this component.
  RBAC requirements are determined by the external orchestration layer
  (e.g., Kubernetes Job controller, Data Science Pipelines).

Service Mesh Configuration:
  No service mesh configuration defined.
  This component has no inbound services requiring mTLS.
  Outbound connections use standard TLS via boto3 SDK.

Secrets:
  - S3_ACCESS_KEY_ID (environment variable) - AWS IAM access key, provisioned by runtime environment
  - S3_SECRET_ACCESS_KEY (environment variable) - AWS IAM secret key, provisioned by runtime environment
  - S3_ENDPOINT (environment variable) - S3-compatible endpoint URL, provisioned by runtime environment

Security Notes:
  - No inbound attack surface: component exposes no network listeners
  - Outbound-only: connects to S3 storage and optionally experiment trackers
  - Credentials injected via environment variables (standard K8s Secret → env pattern)
  - CVE CWE-502 (unsafe pickle deserialization) fixed in commit b837608 (2026-07-21)
  - No webhook endpoints
  - No admission controllers
