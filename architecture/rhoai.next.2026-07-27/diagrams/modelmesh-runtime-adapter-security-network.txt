┌─────────────────────────────────────────────────────────────────────────────┐
│  MODELMESH POD BOUNDARY (Pod-local / Localhost)                             │
│                                                                             │
│  All adapter ↔ ModelMesh communication is pod-local gRPC.                  │
│  No application-level authentication interceptors configured.               │
│  Security relies entirely on external platform enforcement                  │
│  (Istio mTLS, Kubernetes NetworkPolicy).                                    │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │  ModelMesh Control Plane (mmesh.ModelMesh)                       │       │
│  │  - registerModel, unregisterModel                                │       │
│  │  - ensureLoaded, getModelStatus                                  │       │
│  │  - setVModel, getVModelStatus, deleteVModel                      │       │
│  │         │                                                        │       │
│  │         │ Protocol: gRPC (pod-local)                             │       │
│  │         │ Encryption: None (pod-local)                           │       │
│  │         │ Auth: None (no interceptors)                           │       │
│  │         ▼                                                        │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │  Runtime Adapter Sidecars (mmesh.ModelRuntime)                   │       │
│  │                                                                   │       │
│  │  [model-mesh-triton-adapter]                                      │       │
│  │  - Implements: loadModel, unloadModel, runtimeStatus,             │       │
│  │    modelSize, predictModelSize                                    │       │
│  │  - Protocol: gRPC                                                 │       │
│  │  - Encryption: None (pod-local)                                   │       │
│  │  - Auth: None                                                     │       │
│  │                                                                   │       │
│  │  [model-mesh-mlserver-adapter]                                    │       │
│  │  - Implements: loadModel, unloadModel, runtimeStatus,             │       │
│  │    modelSize, predictModelSize                                    │       │
│  │  - Protocol: gRPC                                                 │       │
│  │  - Encryption: None (pod-local)                                   │       │
│  │  - Auth: None                                                     │       │
│  │                                                                   │       │
│  │  [model-mesh-torchserve-adapter]                                  │       │
│  │  - Implements: loadModel, unloadModel, runtimeStatus,             │       │
│  │    modelSize, predictModelSize                                    │       │
│  │  - Protocol: gRPC                                                 │       │
│  │  - Encryption: None (pod-local)                                   │       │
│  │  - Auth: None                                                     │       │
│  │                                                                   │       │
│  │  [model-serving-puller]                                           │       │
│  │  - Implements: loadModel, unloadModel, runtimeStatus,             │       │
│  │    modelSize, predictModelSize                                    │       │
│  │  - Uses: pullman storage library                                  │       │
│  │  - Protocol: gRPC                                                 │       │
│  │  - Encryption: None (pod-local)                                   │       │
│  │  - Auth: None                                                     │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│         │                                              │                    │
│         │ Adapter → Backend (gRPC)                     │ Puller → Storage   │
│         │ Protocol: gRPC                               │ Protocol: HTTPS    │
│         │ Encryption: Configured by runtime            │ Encryption: TLS    │
│         │ Auth: None (app-level)                       │ Auth: Cloud SDK    │
│         ▼                                              ▼                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  BACKEND INFERENCE ENGINES (Pod-local or Adjacent)                          │
│                                                                             │
│  [NVIDIA Triton Inference Server]                                           │
│  - Service: inference.GRPCInferenceService                                  │
│  - RPCs: ModelInfer, ModelReady, ServerReady, ServerLive,                   │
│    ModelMetadata, ServerMetadata, ModelConfig, ModelStatistics,              │
│    ModelStreamInfer, RepositoryIndex, RepositoryModelLoad,                  │
│    RepositoryModelUnload, CudaSharedMemory*, SystemSharedMemory*            │
│  - Protocol: gRPC                                                           │
│  - Encryption: Unknown (runtime-configured)                                │
│  - Auth: Unknown (runtime-configured)                                       │
│                                                                             │
│  [Seldon MLServer]                                                          │
│  - Service: GRPCInferenceService (KFServing V2)                            │
│  - RPCs: ModelInfer, ModelReady, ModelMetadata,                             │
│    ServerReady, ServerLive, ServerMetadata                                   │
│  - Protocol: gRPC                                                           │
│  - Encryption: Unknown (runtime-configured)                                │
│  - Auth: Unknown (runtime-configured)                                       │
│                                                                             │
│  [PyTorch TorchServe]                                                       │
│  - Service: InferenceAPIsService (Ping, Predictions)                       │
│  - Service: ManagementAPIsService (RegisterModel, UnregisterModel,          │
│    DescribeModel, ListModels, ScaleWorker, SetDefault)                      │
│  - Protocol: gRPC                                                           │
│  - Encryption: Unknown (runtime-configured)                                │
│  - Auth: Unknown (runtime-configured)                                       │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Object Storage)                                         │
│                                                                             │
│  [Azure Blob Storage]                                                       │
│  - Purpose: Model artifact storage                                          │
│  - Protocol: HTTP/HTTPS                                                     │
│  - Encryption: Configured by runtime (TLS expected)                        │
│  - Auth: Azure SDK (azidentity v0.13.2)                                    │
│                                                                             │
│  [Google Cloud Storage]                                                     │
│  - Purpose: Model artifact storage                                          │
│  - Protocol: HTTP/HTTPS                                                     │
│  - Encryption: Configured by runtime (TLS expected)                        │
│  - Auth: GCS SDK (cloud.google.com/go/storage v1.28.1)                     │
│                                                                             │
│  [IBM Cloud Object Storage (S3-compatible)]                                │
│  - Purpose: Model artifact storage                                          │
│  - Protocol: HTTP/HTTPS                                                     │
│  - Encryption: Configured by runtime (TLS expected)                        │
│  - Auth: IBM COS SDK (ibm-cos-sdk-go v1.9.1)                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined (sidecar component, not an operator)
- No RoleBindings defined
- No ServiceAccounts defined (managed by ModelMesh parent deployment)

Service Mesh Configuration:
- No PeerAuthentication defined (relies on parent ModelMesh configuration)
- No AuthorizationPolicy defined
- Pod-local communication does not traverse service mesh
- External-facing traffic security delegated to ModelMesh + Istio

Secrets:
- No secrets directly referenced by modelmesh-runtime-adapter
- Object storage credentials expected to be injected via:
  - Environment variables (cloud SDK default credential chains)
  - Mounted secret volumes (ModelMesh storage-config)

Security Notes:
- 11 authentication rules confirm NO application-level auth across all gRPC services
- All 42 gRPC methods across 6 service definitions have no auth interceptors
- grpc.NewServer options contain only observability interceptors
- Security boundary is the ModelMesh pod; sidecar-to-sidecar is pod-local
- CVE fixes applied: golang.org/x/net (CVE-2024-45337),
  golang.org/x/oauth2 (CVE-2025-22868),
  k8s.io/apimachinery (SNYK-GOLANG-K8SIOAPIMACHINERYPKGUTILRUNTIME-8367153)
