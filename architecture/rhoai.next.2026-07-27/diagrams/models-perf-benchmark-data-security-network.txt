┌─────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME: API Client Data Ingestion                                   │
│                                                                          │
│  [API Client CLI]                                                        │
│  - Location: Build environment (not deployed at runtime)                 │
│  - Language: Python                                                      │
│  - Retry: 3 retries, exponential backoff, retry on 429/5xx              │
│         │                                                                │
│         │ Port: 443/TCP (assumed)                                        │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (configurable verify)                          │
│         │ Auth: Not specified in source (may be handled externally)      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL (Untrusted)                                                    │
│                                                                          │
│  [External Benchmark REST API]                                           │
│  - URL: Configured via API_BASE_URL env var                              │
│  - Endpoints:                                                            │
│    - GET /api/v1/providers                                               │
│    - GET /api/v1/models                                                  │
│    - GET /api/v1/evaluations                                             │
│    - GET /api/v1/performances                                            │
│    - GET /api/v1/validations                                             │
│    - GET /api/v1/variant-groups                                          │
│    - GET /api/v1/security                                                │
│  - All endpoints return paginated results                                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  RUNTIME: Init Container in Kubernetes Cluster                           │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER (Trusted)                                                       │
│                                                                          │
│  Namespace: opendatahub                                                  │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ [model-registry-operator]                                        │    │
│  │ - Manages model-catalog deployment lifecycle                     │    │
│  │ - Controls init container configuration                          │    │
│  └──────────────────────────┬──────────────────────────────────────┘    │
│                              │                                           │
│                              │ Manages                                   │
│                              ▼                                           │
│  Namespace: odh-model-registries                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ [model-catalog Deployment]                                       │    │
│  │                                                                  │    │
│  │  Init Container: models-perf-benchmark-data                      │    │
│  │  - Type: Data-only init container                                │    │
│  │  - Purpose: Mount benchmark data into pod filesystem             │    │
│  │  - No network listeners                                          │    │
│  │  - No outbound connections at runtime                            │    │
│  │  - Data format: NDJSON + JSON files                              │    │
│  │                                                                  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined by this component
- No RoleBindings defined by this component
- Component relies on model-registry-operator for deployment RBAC

Secrets:
- No secrets defined by this component
- API_BASE_URL configured via environment variable (not a secret store)
- Authentication to external API not specified in source

Network Exposure:
- No runtime network services (data-only init container)
- No ingress definitions
- No egress at runtime (data fetched at build time only)
- Build-time egress: HTTPS to external benchmark API (configurable URL)

Security Notes:
- Repository marked confidential; must not be made public
- TLS verification for external API is configurable (may be disabled)
- No authentication mechanism found in source for external API access
