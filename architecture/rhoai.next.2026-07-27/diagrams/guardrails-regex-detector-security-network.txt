┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL / UPSTREAM (Trust depends on deployment)                      │
│                                                                          │
│  [Client / Upstream Guardrails Service]                                  │
│         │                                                                │
│         │ Port: 8080/TCP (configurable via HTTP_PORT)                    │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None at application level                          │
│         │ Auth: None at application level                                │
│         │ Note: TLS and auth expected from infrastructure                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INFRASTRUCTURE LAYER (Expected - not configured in application)        │
│                                                                          │
│  [Service Mesh / Sidecar / Ingress Controller]                           │
│  - TLS Termination: Expected (TLS 1.2+)                                 │
│  - mTLS: Expected between mesh services                                 │
│  - Auth: Header passthrough to application                               │
│  - Note: NO authentication or TLS is configured in the application      │
│    source code. Security enforcement relies entirely on deployment       │
│    infrastructure (e.g., Istio sidecar, OpenShift Service Mesh).        │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP (plaintext within mesh)                         │
│         │ Encryption: mTLS (if service mesh present)                     │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (guardrails-regex-detector)                                 │
│                                                                          │
│  [regex-detector Binary]                                                 │
│  - Language: Rust (Axum 0.7.9 + Tokio 1.41.1)                          │
│  - Bind: 0.0.0.0:8080 (configurable)                                   │
│  - Single async listener, multi-threaded Tokio runtime                  │
│  - No background tasks or secondary listeners                           │
│                                                                          │
│  Endpoints:                                                              │
│  ┌────────────────────────────────────────────────────────────┐          │
│  │ POST /api/v1/text/contents                                 │          │
│  │ - Purpose: Regex-based text content detection              │          │
│  │ - Auth: None (passthrough headers only)                    │          │
│  │ - Input: JSON text content                                 │          │
│  │ - Output: Detection results (JSON)                         │          │
│  └────────────────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────────────────┐          │
│  │ GET /health                                                │          │
│  │ - Purpose: Health check                                    │          │
│  │ - Auth: None                                               │          │
│  │ - Output: "healthy" (string)                               │          │
│  └────────────────────────────────────────────────────────────┘          │
│                                                                          │
│  Middleware:                                                             │
│  - tower-http TraceLayer (structured logging, INFO level)               │
│  - No authentication middleware                                          │
│  - No TLS configuration                                                  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS                                                                  │
│                                                                          │
│  None - The service makes no outbound network connections.               │
│  All processing (regex matching) is performed in-process.                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount requirements defined in application source

Secrets:
- No secrets referenced in application source

Security Observations:
- CRITICAL: No authentication or authorization is implemented at the
  application level. The service accepts any request on port 8080.
- CRITICAL: No TLS is configured at the application level. All traffic
  is plaintext unless a service mesh sidecar or reverse proxy provides
  TLS termination.
- The service relies entirely on deployment infrastructure (OpenShift
  Service Mesh, Istio sidecars, NetworkPolicies) for security enforcement.
- Header passthrough is configured — upstream auth headers are forwarded
  but not validated by the application.
- No secret material is consumed or generated by the application.

Configuration:
- HTTP_PORT (env var, default: 8080) — listener port
- HOST (env var, default: 0.0.0.0) — bind address
