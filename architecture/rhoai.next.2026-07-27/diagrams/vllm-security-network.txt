┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client / Application]                                         │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.3                                            │
│         │ Auth: Platform-delegated (Bearer Token / mTLS)                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  PLATFORM LAYER (Service Mesh / Ingress)                                │
│                                                                          │
│  [Service Mesh Sidecar / Istio Gateway]                                  │
│  - TLS Termination: TLS 1.3 at mesh boundary                           │
│  - AuthN/AuthZ: Enforced by platform (not by vLLM container)           │
│  - PeerAuthentication: mTLS STRICT (if configured)                     │
│         │                                                                │
│         ├──────────────────────────────────────┐                        │
│         │                                      │                        │
│         │ Port: 8000/TCP                       │ Port: 8033/TCP         │
│         │ Protocol: HTTP                       │ Protocol: gRPC (h2c)   │
│         │ Encryption: mTLS (sidecar)           │ Encryption: mTLS       │
│         │ Auth: Service Identity               │ Auth: Service Identity  │
│         ▼                                      ▼                        │
├─────────────────────────────────────────────────────────────────────────┤
│  CONTAINER (UID 2000 - Non-root)                                        │
│                                                                          │
│  [odh-vllm-cuda-rhel9 Container]                                        │
│  - Base Image: registry.redhat.io/rhaiis/vllm-cuda-rhel9:3.2.2        │
│  - User: UID 2000 (non-root)                                            │
│  - No in-container TLS termination                                      │
│  - No in-container authentication                                       │
│  - Security delegated entirely to platform layer                        │
│                                                                          │
│  ┌─────────────────────────┐  ┌──────────────────────────┐             │
│  │ Uvicorn HTTP Server     │  │ vllm-tgis-adapter        │             │
│  │ Port: 8000/TCP          │  │ Port: 8033/TCP           │             │
│  │ OpenAI-compatible API   │  │ TGIS gRPC Protocol       │             │
│  │ Plaintext (no TLS)      │  │ Plaintext (no TLS)       │             │
│  └────────────┬────────────┘  └────────────┬─────────────┘             │
│               │                             │                           │
│               └──────────┬──────────────────┘                           │
│                          ▼                                               │
│  ┌───────────────────────────────────┐                                  │
│  │ vLLM Inference Engine             │                                  │
│  │ GPU-accelerated (CUDA)            │                                  │
│  │ PagedAttention memory management  │                                  │
│  └───────────────┬───────────────────┘                                  │
│                  │                                                       │
│                  │ Device: GPU (CUDA)                                    │
│                  │ Interface: CUDA Runtime API                           │
│                  ▼                                                       │
│  ┌───────────────────────────────────┐                                  │
│  │ NVIDIA GPU Device                 │                                  │
│  │ Allocated via device plugin       │                                  │
│  └───────────────────────────────────┘                                  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Model Storage - runtime-dependent)                  │
│                                                                          │
│  Note: No outbound connections configured in Dockerfile.                │
│  Model loading behavior is inherited from base image and                │
│  runtime configuration. Possible destinations:                          │
│                                                                          │
│  [S3-Compatible Storage]           [PVC / Local Storage]                │
│  - Port: 443/TCP                   - Mount: /mnt/models (typical)      │
│  - Protocol: HTTPS                 - No network egress                  │
│  - Encryption: TLS 1.2+                                                 │
│  - Auth: AWS IAM / Access Keys                                          │
└─────────────────────────────────────────────────────────────────────────┘

Security Posture Summary:
- Container runs as non-root (UID 2000)
- No in-container authentication or TLS termination
- All security enforcement delegated to platform layer (Service Mesh)
- No secrets managed within the container image
- Multi-architecture build (amd64, arm64) via Konflux/Tekton

RBAC Summary:
- No RBAC resources defined in this repository
- RBAC is managed by the deploying component (KServe / RHOAI model serving stack)

Secrets:
- No secrets defined in this repository
- Runtime secrets (model storage credentials) are injected by the platform

Build Security:
- Konflux-managed CI/CD pipeline
- Pipeline references: red-hat-data-services/konflux-central
- Base image from Red Hat registry (registry.redhat.io)
