┌─────────────────────────────────────────────────────────────────────────┐
│  CLIENTS (Internal - Platform Services)                                  │
│                                                                          │
│  [Data Science Pipelines]                                                │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: gRPC (HTTP/2)                                        │
│         │ Encryption: None by default (mTLS configurable via SSLConfig)  │
│         │ Auth: None (platform-delegated via network isolation)          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (ml-metadata Server)                                        │
│                                                                          │
│  [metadata_store_server]                                                 │
│  - Binary: C++ (Bazel-built, statically linked)                         │
│  - Base Image: ubi9-minimal                                              │
│  - User: 65534 (non-root)                                                │
│  - Namespace: {dspa-namespace}                                           │
│  - Listening Port: 8080/TCP (configurable via GRPC_PORT env var)        │
│  - Protocol: gRPC (HTTP/2)                                               │
│  - Service: MetadataStoreService (~40 RPCs)                              │
│  - TLS: Optional via MetadataStoreServerConfig.SSLConfig                │
│    - Server key/cert: configurable                                       │
│    - Custom CA: configurable                                             │
│    - Client cert verification: configurable (client_verify)             │
│  - Auth: None built-in                                                   │
│  - No HTTP endpoints exposed                                             │
│         │                                                                │
│         │ Port: 3306/TCP                                                 │
│         │ Protocol: MySQL wire protocol                                  │
│         │ Encryption: SSL configurable via MySQLDatabaseConfig.SSLOptions│
│         │ Auth: Database credentials                                     │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  DATA STORE                                                              │
│                                                                          │
│  [MariaDB/MySQL]                                                         │
│  - Port: 3306/TCP                                                        │
│  - Purpose: Persistent storage for ML metadata                           │
│  - Stores: artifacts, executions, contexts, types, events, lineage      │
│  - Encryption: SSL configurable                                          │
│  - Auth: Database user credentials                                       │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined in the component repository
- Access control delegated to platform-level network isolation
- No Kubernetes RBAC resources managed by this component

Authentication & Authorization:
- Endpoint: MetadataStoreService gRPC (all RPCs)
- Auth Mechanism: None built-in (mTLS configurable via SSLConfig)
- Enforcement: Application-level (optional)
- Policy: No built-in auth; platform-delegated via network isolation

Secrets:
- No secrets defined in the component repository
- Database credentials managed at platform level (Data Science Pipelines operator)

FIPS Compliance:
- C++ binary (not Go) - standard Go FIPS flags do not apply
- OpenSSL available via UBI9-minimal base image
- Optional TLS delegates to system OpenSSL at runtime
- No explicit FIPS mode configuration present
- TLS cipher selection delegated to gRPC/OpenSSL defaults

Network Isolation:
- No NetworkPolicies defined in the component
- Platform-level network isolation expected (service mesh, namespace policies)
- Server listens only on gRPC port (no management/metrics endpoints documented)
