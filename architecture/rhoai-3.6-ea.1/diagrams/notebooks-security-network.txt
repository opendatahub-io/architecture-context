┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Data Scientist / User]                                                 │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (OpenShift Router)                             │
│         │ Auth: OpenShift OAuth (browser redirect)                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (OpenShift Route)                                              │
│                                                                          │
│  [OpenShift Route]                                                       │
│  - TLS Termination: edge/reencrypt                                      │
│  - Target: oauth-proxy sidecar                                          │
│         │                                                                │
│         │ Port: 8443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS                                                │
│         │ Auth: OpenShift OAuth token validation                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  POD (StatefulSet - managed by odh-notebook-controller)                 │
│                                                                          │
│  [oauth-proxy sidecar]                                                   │
│  - Injected by: odh-notebook-controller                                  │
│  - AuthN: OpenShift OAuth                                                │
│  - Purpose: Gate access to notebook container                            │
│         │                                                                │
│         │ Port: 8888/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (pod-internal loopback)                       │
│         │ Auth: Trusted (oauth-proxy validated)                          │
│         ▼                                                                │
│  [JupyterLab / Code Server Container]                                   │
│  - Image: UBI9 + Python 3.12 notebook variant                           │
│  - Entrypoint: start-notebook.sh / run-code-server.sh                   │
│  - No built-in auth (delegated to oauth-proxy)                          │
│  - jupyter_server_config.py: no token/password/XSRF                     │
│  - Port configurable via NOTEBOOK_PORT (default 8888)                   │
│                                                                          │
│  User-initiated outbound connections (from notebook code cells):         │
│         │                                                                │
│         ├── Port: 443/TCP │ Protocol: HTTPS │ TLS │ K8s ServiceAccount   │
│         │   → Kubernetes API Server                                      │
│         │                                                                │
│         ├── Port: 443/TCP │ Protocol: HTTPS │ TLS │ Bearer/ServiceAcct   │
│         │   → KubeFlow Pipelines API                                     │
│         │                                                                │
│         ├── Port: 443/TCP │ Protocol: HTTPS │ TLS │ AWS IAM / AccessKey  │
│         │   → S3-compatible Object Storage                               │
│         │                                                                │
│         ├── Port: varies │ Protocol: HTTP/HTTPS │ Configurable │ Token   │
│         │   → MLflow Tracking Server                                     │
│         │                                                                │
│         └── Port: varies │ Protocol: HTTP/HTTPS │ Configurable │ Token   │
│             → CodeFlare / Distributed Computing                          │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SERVICES (Internal)                                            │
│                                                                          │
│  [odh-notebook-controller]                                               │
│  - Watches: Notebook CRs                                                 │
│  - Creates: StatefulSet pods with oauth-proxy sidecar                    │
│  - Manages: Notebook lifecycle                                           │
│                                                                          │
│  [RHOAI Dashboard]                                                       │
│  - Discovers: ImageStreams (component.opendatahub.io/name: notebooks)    │
│  - Creates: Notebook CRs on user request                                 │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  BUILD-TIME ONLY (not in runtime images)                                 │
│                                                                          │
│  [Konflux Pipeline]                                                      │
│  - Hermetic builds with Cachi2/Hermeto prefetching                       │
│  - Produces ImageStream-referenced container images                      │
│                                                                          │
│  [check-payload]                                                         │
│  - Go static binary (CGO_ENABLED=0)                                      │
│  - FIPS compliance validation                                            │
│  - Not shipped in notebook images                                        │
│                                                                          │
│  [buildinputs]                                                           │
│  - Go static binary (CGO_ENABLED=0)                                      │
│  - Dockerfile dependency graph analyzer                                  │
│  - Not shipped in notebook images                                        │
└─────────────────────────────────────────────────────────────────────────┘

Secrets:
- GITHUB_TOKEN (env var) - Provisioned by runtime environment
- GEMINI_API_KEY (env var) - Provisioned by runtime environment
- RENOVATE_TOKEN (env var) - Provisioned by runtime environment
- JIRA_API_TOKEN (env var) - Provisioned by runtime environment
- JIRA_TOKEN (env var) - Provisioned by runtime environment
- JIRA_OAUTH_CLIENT_SECRET (env var) - Provisioned by runtime environment

Authentication Summary:
- Notebook containers have NO built-in authentication
- All access gated by oauth-proxy sidecar (OpenShift OAuth)
- Platform-delegated authentication model
- User-initiated integrations use service-specific auth (IAM, Bearer, ServiceAccount)

FIPS Compliance:
- UBI9 base: OpenSSL in FIPS mode when host FIPS enabled
- cryptography==46.0.7: Uses system OpenSSL (FIPS-capable)
- pycryptodome==3.23.0: Self-contained, NOT FIPS-validated (potential non-FIPS path)
- compat-openssl11: Installed for check-payload FIPS scanner
- Go build tools: CGO_ENABLED=0, no GOEXPERIMENT=strictfipsruntime (out of scope)
