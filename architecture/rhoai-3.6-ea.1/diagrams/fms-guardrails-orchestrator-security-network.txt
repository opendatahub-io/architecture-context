┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [API Consumer]                                                          │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: TLS 1.2+ (optional, rustls + ring provider)        │
│         │ Auth: Passthrough headers (configurable)                       │
│         │       Optional: X-Forwarded-Access-Token → Bearer rewrite     │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (fms-guardrails-orchestr8)                                  │
│                                                                          │
│  [Guardrails Server - Port 8033]                                         │
│  - Binary: /app/bin/fms-guardrails-orchestr8                            │
│  - Framework: axum (HTTP) + tonic (gRPC client)                         │
│  - TLS: rustls with ring crypto provider (NOT FIPS-validated)           │
│  - mTLS: Optional client certificate verification                       │
│  - Auth: Header passthrough to downstream services                      │
│  - Endpoints: 10 POST routes (/api/v1/*, /api/v2/*)                    │
│  - Multi-arch: amd64, ppc64le, s390x                                   │
│                                                                          │
│  [Health Server - Port 8034]                                             │
│  - Endpoints: GET /health, GET /info                                    │
│  - Encryption: None                                                      │
│  - Auth: None (intentionally unauthenticated)                           │
│         │                                                                │
│         │                                                                │
│         ├──────────────────────┬────────────────────┬──────────────────┤ │
│         │                      │                    │                  │ │
│         ▼                      ▼                    ▼                  ▼ │
├─────────────────────────────────────────────────────────────────────────┤
│  DOWNSTREAM SERVICES                                                     │
│                                                                          │
│  [Chunker Services]                                                      │
│  - Port: 8085/TCP                                                        │
│  - Protocol: gRPC                                                        │
│  - Encryption: TLS (optional, configurable per-service)                 │
│  - Auth: None                                                            │
│  - Purpose: Text segmentation                                            │
│                                                                          │
│  [Detector Services]                                                     │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP/HTTPS (REST)                                           │
│  - Encryption: TLS (optional, configurable per-service)                 │
│  - Auth: Bearer token (optional, via header rewrite)                    │
│  - Purpose: Content analysis / detection                                 │
│                                                                          │
│  [Detector Health Services]                                              │
│  - Port: 8081/TCP                                                        │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Downstream health monitoring                                 │
│                                                                          │
│  [TGIS Generation Service]                                               │
│  - Port: 8033/TCP                                                        │
│  - Protocol: gRPC                                                        │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Text generation (LLM inference)                             │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY                                                           │
│                                                                          │
│  [OTLP Collector]                                                        │
│  - Protocol: gRPC or HTTP (configurable)                                │
│  - Purpose: OpenTelemetry traces and metrics export                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined in source
- Application runs as a standalone binary without Kubernetes operator patterns
- No CRDs defined

Secrets:
- TLS server cert/key (kubernetes.io/tls) - Rust HTTP server TLS, provisioned by platform operator / cert-manager, rotation unknown
- TLS client CA cert (Opaque) - Rust HTTP server mTLS client verification, provisioned by platform operator, rotation unknown
- API tokens (Opaque / env var) - Downstream service authentication, provisioned by platform operator, rotation unknown
- Downstream TLS certs (Opaque) - Downstream service TLS client certificates, provisioned by platform operator, rotation unknown

FIPS Compliance:
- CRITICAL: Application uses rustls with ring crypto provider
- ring is NOT a FIPS-validated cryptographic provider
- FIPS compliance is NOT achievable with current TLS configuration
- OpenSSL is NOT present in release image (removed in commit c5d0d1f)
- Build: `cargo build --release` without FIPS-specific flags
- All TLS deps (rustls, hyper-rustls, tokio-rustls, reqwest) use ring features

Authentication Flow:
- Inbound: Configurable header passthrough (application-level filtering)
- Optional: X-Forwarded-Access-Token → Authorization Bearer header rewrite
- Health endpoints (port 8034): Intentionally unauthenticated
- TLS/mTLS: Optional on inbound, configured via CLI args (cert, key, client CA paths)
- Outbound TLS: Per-service configurable via YAML config with optional client certs and insecure toggle
