============================================================================
  SECURITY NETWORK DIAGRAM: model-metadata-collection
============================================================================

  NOTE: This component is a DATA-ONLY container. It has NO network
  listeners, NO exposed ports, and NO runtime network traffic.
  All external network access occurs at BUILD TIME only (CI/CD).

============================================================================
  BUILD-TIME NETWORK FLOWS (CI/CD only — not in production image)
============================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  CI/CD ENVIRONMENT (Build-Time)                                         │
│                                                                          │
│  [model-extractor CLI]                                                   │
│  - Binary: cmd/model-extractor/main.go                                  │
│  - NOT shipped in production image                                       │
│         │              │              │                                   │
│         │              │              │                                   │
│    ─────┼──────────────┼──────────────┼──────────────────────────────    │
│         │              │              │                                   │
│         │ Port: 443    │ Port: 443    │ Port: 443                        │
│         │ Proto: HTTPS │ Proto: HTTPS │ Proto: HTTPS                     │
│         │ Enc: TLS     │ Enc: TLS     │ Enc: TLS                         │
│         │ Auth: Bearer │ Auth: Bearer │ Auth: System                     │
│         │ (HF_TOKEN,   │ (GITHUB_     │ cred chain                       │
│         │  optional)   │  TOKEN,      │ (containers/                     │
│         │              │  optional)   │  image/v5)                       │
│         ▼              ▼              ▼                                   │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Build-Time Only)                                     │
│                                                                          │
│  [HuggingFace API]         [GitHub API]        [Container Registries]    │
│  - Host: huggingface.co    - Host: api.github  - Host: quay.io, etc.    │
│  - Purpose: Fetch model      .com              - Purpose: Fetch image    │
│    collections & metadata  - Purpose: Fetch      architectures & OCI    │
│  - Auth: Bearer token        agent metadata      artifact metadata       │
│    (HF_TOKEN env var,        & README files    - Auth: System credential  │
│     optional)              - Auth: Bearer        chain                   │
│  - Encryption: TLS          token (GITHUB_     - Encryption: TLS         │
│                              TOKEN, optional)                            │
│                            - Encryption: TLS                             │
└─────────────────────────────────────────────────────────────────────────┘

============================================================================
  RUNTIME ARCHITECTURE (Production)
============================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  KUBERNETES CLUSTER (Runtime)                                            │
│                                                                          │
│  [model-metadata-collection Pod]                                         │
│  - Base image: ubi9-minimal                                              │
│  - CMD: ["sleep", "infinity"]                                            │
│  - NO network listeners                                                  │
│  - NO exposed ports                                                      │
│  - NO running processes (besides sleep)                                  │
│  - Data path: /app/data/*.yaml                                           │
│                                                                          │
│         ▲                                                                │
│         │ Volume Mount (filesystem only)                                 │
│         │ Path: /app/data                                                │
│         │ Protocol: N/A (local filesystem)                               │
│         │ Encryption: N/A                                                │
│         │ Auth: N/A                                                      │
│         │                                                                │
│  [RHOAI Platform Components]                                             │
│  - Read YAML catalogs from volume mount                                  │
│  - No network traffic to this container                                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccounts with elevated permissions
- Container requires no Kubernetes API access at runtime

Service Mesh Configuration:
- Not applicable — container has no network listeners
- No PeerAuthentication needed
- No AuthorizationPolicy needed

Secrets (Build-Time Only):
- HF_TOKEN (env var) — HuggingFace Bearer token, optional, for API rate limiting
- GITHUB_TOKEN (env var) — GitHub Bearer token, optional, for API rate limiting
- Container registry credentials — managed by containers/image/v5 system chain

Network Attack Surface:
- Runtime: NONE — no ports, no listeners, no network traffic
- Build-time: Outbound HTTPS only to trusted external APIs
- No ingress required at any phase
