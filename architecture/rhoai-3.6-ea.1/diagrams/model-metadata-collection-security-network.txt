┌─────────────────────────────────────────────────────────────────────────┐
│  RUNTIME CONTAINER (Minimal Attack Surface)                             │
│                                                                          │
│  [model-metadata-collection Container]                                   │
│  - Base Image: UBI9-micro                                                │
│  - Entrypoint: sleep infinity                                            │
│  - User: UID 1001 (non-root)                                             │
│  - No exposed ports                                                      │
│  - No network listeners                                                  │
│  - No application code                                                   │
│  - Data path: /app/data/ (read-only YAML catalogs)                      │
│                                                                          │
│  Network:                                                                │
│  - Ingress: NONE (no ports, no services, no ingress resources)           │
│  - Egress: NONE (sleep infinity has no outbound connections)             │
│                                                                          │
│  Volume Mounts:                                                          │
│  - /app/data/ → mounted by consuming RHOAI components                   │
│  - Access: filesystem only, no network protocol                          │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CI/CD BUILD-TIME (model-extractor CLI)                                 │
│                                                                          │
│  [model-extractor]                                                       │
│  - Language: Go 1.24                                                     │
│  - Runs during CI/CD pipeline only                                       │
│  - Does NOT ship in deployed container                                   │
│         │                                                                │
│         ├─── Port: 443/TCP ──────────────────────────────────────┐       │
│         │    Protocol: HTTPS                                      │       │
│         │    Encryption: TLS (Go default TLS stack)               │       │
│         │    Auth: Bearer Token (HF_TOKEN, optional)              │       │
│         │    Destination: huggingface.co                           │       │
│         │    Purpose: Fetch model collection metadata             │       │
│         │                                                          │       │
│         ├─── Port: 443/TCP ──────────────────────────────────────┤       │
│         │    Protocol: HTTPS                                      │       │
│         │    Encryption: TLS (Go default TLS stack)               │       │
│         │    Auth: Bearer Token (GITHUB_TOKEN, optional)          │       │
│         │    Destination: api.github.com                           │       │
│         │    Purpose: Fetch agent metadata and README content     │       │
│         │                                                          │       │
│         └─── Port: 443/TCP ──────────────────────────────────────┘       │
│              Protocol: HTTPS                                             │
│              Encryption: TLS (containers/image library)                  │
│              Auth: Default registry auth chain                           │
│              Destination: OCI container registries                        │
│              Purpose: Query image manifests for metadata                 │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (CI-time only)                                       │
│                                                                          │
│  [HuggingFace API - huggingface.co]                                      │
│  - Purpose: Model collection and metadata source                         │
│  - Auth: Optional Bearer HF_TOKEN                                        │
│  - Encryption: TLS                                                       │
│  - Rate limiting: Unauthenticated requests subject to rate limits        │
│                                                                          │
│  [GitHub API - api.github.com]                                           │
│  - Purpose: Agent metadata and README content                            │
│  - Auth: Optional Bearer GITHUB_TOKEN                                    │
│  - Encryption: TLS                                                       │
│  - Rate limiting: Unauthenticated requests subject to rate limits        │
│                                                                          │
│  [OCI Container Registries]                                              │
│  - Purpose: Image architecture and timestamp metadata                    │
│  - Auth: containers/image default authentication chain                   │
│  - Encryption: TLS                                                       │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccounts defined in repository
- Deployment RBAC managed externally

Service Mesh Configuration:
- No PeerAuthentication policies
- No AuthorizationPolicies
- No sidecar injection (data-only container)

Secrets:
- HF_TOKEN (environment variable) - HuggingFace API Bearer token, CI-time only, optional
- GITHUB_TOKEN (environment variable) - GitHub API Bearer token, CI-time only, optional
- OCI registry credentials via containers/image default auth chain, CI-time only

Security Notes:
- Runtime container has ZERO network attack surface (no ports, no listeners)
- All external API calls occur at CI/build time only, not at runtime
- Container runs as non-root (UID 1001)
- Base image: UBI9-micro (minimal package set)
- FIPS: Not applicable at runtime (no Go binaries or crypto operations in deployed image)
- Authentication tokens are optional; unauthenticated access works with rate limiting
