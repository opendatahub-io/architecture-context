┌─────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Platform-Controlled)                                  │
│                                                                          │
│  [Guardrails Orchestrator / Client]                                      │
│         │                                                                │
│         │ Port: 8080/TCP (configurable via HTTP_PORT)                    │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plain HTTP)                                   │
│         │ Auth: None (no application-level auth)                         │
│         │ Note: Platform network isolation expected                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE (Stateless Microservice)                                       │
│                                                                          │
│  [guardrails-regex-detector]                                             │
│  - Binary: /app/bin/regex-detector                                       │
│  - Image: UBI 9 Minimal + compat-openssl11                              │
│  - Rust 1.84.0 (Debian-compiled)                                         │
│  - Runtime: Tokio async + Axum web framework                             │
│                                                                          │
│  Endpoints:                                                              │
│  ┌─────────────────────────────────────────────────────────┐            │
│  │ POST /api/v1/text/contents                               │            │
│  │   Port: 8080/TCP                                         │            │
│  │   Protocol: HTTP (plain)                                 │            │
│  │   Auth: None                                             │            │
│  │   Purpose: Regex-based text content detection            │            │
│  │   Input: JSON {contents, detector_params.regex}          │            │
│  │   Output: JSON {detections with position, type, score}   │            │
│  └─────────────────────────────────────────────────────────┘            │
│  ┌─────────────────────────────────────────────────────────┐            │
│  │ GET /health                                               │            │
│  │   Port: 8080/TCP                                         │            │
│  │   Protocol: HTTP (plain)                                 │            │
│  │   Auth: None                                             │            │
│  │   Purpose: Liveness/readiness probe                      │            │
│  │   Output: "healthy" (static string)                      │            │
│  └─────────────────────────────────────────────────────────┘            │
│                                                                          │
│  No outbound connections                                                 │
│  No persistent state                                                     │
│  No secrets referenced                                                   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  (None — service makes no outbound calls)                                │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount-specific permissions
- Service does not interact with Kubernetes API

Service Mesh Configuration:
- No PeerAuthentication defined
- No AuthorizationPolicy defined
- mTLS: Optional (platform-level, not enforced by application)
- Network isolation: Delegated entirely to platform layer

Secrets:
- None — service does not reference or consume any secrets

Authentication & Authorization:
- POST /api/v1/text/contents: No application-level authentication;
  platform-level network isolation expected
- GET /health: Unauthenticated health check

FIPS Compliance:
- Application performs NO cryptographic operations
- No TLS termination, no certificate handling, no encryption, no hashing
- compat-openssl11 installed for glibc compatibility only (not for crypto)
- Regex crate dependency chain does not include cryptographic primitives

Security Notes:
- All security controls delegated to platform (NetworkPolicy, ServiceMesh)
- Service processes text payloads only — no access to secrets or cluster resources
- Horizontally scalable, side-effect-free, suitable for sidecar deployment
- Operators MUST ensure network-level isolation is configured
