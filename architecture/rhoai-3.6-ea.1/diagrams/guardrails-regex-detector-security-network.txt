┌─────────────────────────────────────────────────────────────────────────┐
│  CALLER (Guardrails Pipeline)                                           │
│                                                                          │
│  [Guardrails Orchestrator]                                               │
│         │                                                                │
│         │ Port: 8080/TCP (default, configurable via HTTP_PORT)           │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None at application layer                          │
│         │ Auth: None at application layer                                │
│         │ Note: TLS/mTLS delegated to platform (service mesh sidecar)   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE (Stateless Microservice)                                       │
│                                                                          │
│  [guardrails-regex-detector]                                             │
│  - Binary: /app/bin/regex-detector                                       │
│  - Base Image: UBI9-minimal                                              │
│  - Language: Rust (compiled, statically linked)                          │
│  - Framework: Axum 0.7.9 on Tokio 1.41.1                               │
│  - Middleware: tower-http tracing only                                   │
│                                                                          │
│  Endpoints:                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │ POST /api/v1/text/contents                                       │    │
│  │   Port: 8080/TCP                                                 │    │
│  │   Protocol: HTTP (plain TCP listener, no TLS)                    │    │
│  │   Auth: None (unauthenticated)                                   │    │
│  │   Purpose: PII detection (email, SSN, credit card) + custom regex│    │
│  │   Processing: Synchronous, in-memory, CPU-bound                  │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │ GET /health                                                      │    │
│  │   Port: 8080/TCP                                                 │    │
│  │   Protocol: HTTP (plain TCP listener, no TLS)                    │    │
│  │   Auth: None (unauthenticated)                                   │    │
│  │   Purpose: Liveness/readiness health check                       │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  No outbound network calls                                               │
│  No persistent state                                                     │
│  No external dependencies at runtime                                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK SUMMARY:
- Ingress: 8080/TCP HTTP only (single port, two routes)
- Egress: None — fully self-contained, no outbound connections
- No TLS at application layer — delegated to platform infrastructure
- No authentication at application layer — delegated to platform infrastructure

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount-specific permissions

Secrets:
- None — service requires no secrets or credentials

Service Mesh / Platform Security:
- TLS: Must be provided by service mesh sidecar (e.g., Istio) or ingress proxy
- mTLS: Optional, enforced at platform level
- Auth: Must be provided by API gateway or service mesh policy
- Network Policy: Should restrict ingress to guardrails orchestrator only

FIPS Compliance:
- No cryptographic operations performed by application
- No TLS/crypto crates in Cargo.toml (no rustls, openssl, native-tls)
- compat-openssl11 installed in container for system compatibility only
- FIPS posture delegated to platform infrastructure

Container Security:
- Base image: UBI9-minimal (Red Hat certified)
- Multi-stage build: separate test/lint/release stages
- Compiler: rust:1.84.0 (standard toolchain, no FIPS flags)
- Binary: /app/bin/regex-detector (statically compiled Rust)
