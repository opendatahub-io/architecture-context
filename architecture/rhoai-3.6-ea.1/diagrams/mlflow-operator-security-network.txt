┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [Prometheus Scraper]                                                       │
│         │                                                                   │
│         │ Port: 8443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (self-signed default; configurable)               │
│         │ Auth: TokenReview + SubjectAccessReview                           │
│         │       (controller-runtime authn/authz filter)                     │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (opendatahub / redhat-ods-applications)                 │
│                                                                             │
│  [mlflow-operator-controller-manager]                                       │
│  - Deployment: mlflow-operator-controller-manager                           │
│  - Image: controller:latest (FIPS: strictfipsruntime + UBI9)                │
│  - ServiceAccount: mlflow-operator-controller-manager                       │
│  - Leader Election: ConfigMaps + Leases (namespace: opendatahub)            │
│                                                                             │
│  Endpoints:                                                                 │
│  ┌──────────────────────────────────────────────────────────────┐            │
│  │ :8443/metrics  │ HTTPS │ TLS (self-signed) │ TokenReview+SAR │            │
│  │ :8081/healthz  │ HTTP  │ None              │ None (probe)    │            │
│  │ :8081/readyz   │ HTTP  │ None              │ None (probe)    │            │
│  └──────────────────────────────────────────────────────────────┘            │
│                                                                             │
│  Service: mlflow-operator-controller-manager-metrics-service                │
│  - Type: ClusterIP                                                          │
│  - Port: 8443/TCP → 8443                                                    │
│  - Exposure: Internal only                                                  │
│         │                                                                   │
│         │ Port: 6443/TCP                                                    │
│         │ Protocol: HTTPS/WSS                                               │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount token (in-cluster)                           │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE (Trusted)                                         │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 6443/TCP                                                           │
│  - Protocol: HTTPS + WebSocket                                              │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: ServiceAccount token, RBAC enforcement                             │
│                                                                             │
│  Resources watched:                                                         │
│  - MLflow (mlflow.opendatahub.io/v1) — cluster-scoped                      │
│  - MLflowOperator (components.platform.opendatahub.io/v1alpha1) — cluster   │
│  - MLflowConfig (mlflow.kubeflow.org/v1) — namespaced                       │
│  - Namespaces, Pods, Secrets, ConfigMaps                                    │
│  - Deployments, Services, ServiceAccounts, PVCs                             │
│  - Jobs, CronJobs, NetworkPolicies                                          │
│  - ClusterRoles, ClusterRoleBindings, RoleBindings                          │
│                                                                             │
│  Conditional CRD watches (registered only when CRD present):                │
│  - HTTPRoute (gateway.networking.k8s.io/v1) — Gateway API                  │
│  - ServiceMonitor (monitoring.coreos.com/v1) — prometheus-operator          │
│  - ConsoleLink (console.openshift.io/v1) — OpenShift Console               │
│                                                                             │
│  [apiservers.config.openshift.io/cluster]                                   │
│  - Watched for TLS profile changes                                          │
│  - Triggers graceful shutdown + reload on change                            │
│  - Default: Intermediate TLS profile                                        │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MANAGED RESOURCES (per MLflow instance namespace)                          │
│                                                                             │
│  [MLflow Tracking Server Deployment]                                        │
│  - Created by Helm chart rendering                                          │
│  - NetworkPolicy applied                                                    │
│  - ServiceAccount with scoped permissions                                   │
│  - PersistentVolumeClaim for data storage                                   │
│  - Migration Jobs/CronJobs for version upgrades                             │
│                                                                             │
│  [Kubelet]                                                                  │
│         │                                                                   │
│         │ Port: 8081/TCP                                                    │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: None                                                  │
│         │ Auth: None (health probes)                                        │
│         ▼                                                                   │
│  [mlflow-operator-controller-manager :8081]                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
─────────────
Primary Service Account: mlflow-operator-controller-manager

ClusterRole: mlflow-operator-manager-role
  - Core API: namespaces, pods, secrets → get, list, watch
  - Core API: configmaps, persistentvolumeclaims, secrets, serviceaccounts, services → full CRUD
  - apps: deployments → full CRUD
  - batch: cronjobs, jobs → full CRUD
  - networking.k8s.io: networkpolicies → full CRUD
  - monitoring.coreos.com: servicemonitors → full CRUD
  - mlflow.opendatahub.io: mlflows, mlflows/finalizers, mlflows/status → full CRUD
  - components.platform.opendatahub.io: mlflowoperators → get, list, patch, update, watch
  - config.openshift.io: apiservers → get, list, watch
  - console.openshift.io: consolelinks → full CRUD
  - gateway.networking.k8s.io: httproutes → full CRUD
  - rbac.authorization.k8s.io: clusterroles, clusterrolebindings, rolebindings → full CRUD + bind
  - services.platform.opendatahub.io: auths → get, list, watch

ClusterRole: mlflow-operator-metrics-auth-role
  - authentication.k8s.io: tokenreviews → create
  - authorization.k8s.io: subjectaccessreviews → create

ClusterRole: mlflow-operator-leader-election-role (namespace-scoped)
  - Core API: configmaps → full CRUD
  - coordination.k8s.io: leases → full CRUD
  - Core API: events → create, patch

User-facing Roles:
  - mlflow-operator-mlflow-view: Read-only access to MLflow resources
  - mlflow-operator-mlflow-edit: Full CRUD on MLflow resources
  - mlflow-operator-mlflow-integration: Integration access (datasets, experiments, models)

Secret Access:
─────────────
  - mlflow-artifact-connection Secret: resourceNames-scoped RBAC
  - Blast radius limited to single named secret
  - Both mlflow-operator-manager-role and manager-role restrict access

TLS Configuration:
──────────────────
  - Source: apiservers.config.openshift.io/cluster
  - Default profile: Intermediate (when API unavailable)
  - Dynamic reload: Watcher triggers graceful shutdown on change
  - Metrics TLS: Self-signed by controller-runtime (default)
  - Configurable via --metrics-cert-path flag

FIPS Compliance:
────────────────
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
  - Linking: Dynamic (UBI9 minimal base with OpenSSL)
  - Runtime: FIPS-validated crypto enforced via strictfipsruntime
  - TLS: Platform TLS profile with FIPS-compliant cipher suites
  - Risks: None detected
