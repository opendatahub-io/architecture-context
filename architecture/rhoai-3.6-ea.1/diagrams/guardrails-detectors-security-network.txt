┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│         │                                                                │
│         │ Port: 8080/TCP (built-in), 8000/TCP (HF, Judge)               │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plain HTTP; TLS via platform/mesh)           │
│         │ Auth: None (no app-level auth middleware)                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KSERVE SERVING INFRASTRUCTURE (Internal)                                │
│                                                                          │
│  [Built-in Detector Pod]                                                 │
│  - Image: AIPCC base (quay.io/aipcc/base-images/cpu:3.5.0)             │
│  - Entrypoint: /app/bin/regex-detector                                   │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP (plain)                                                │
│  - Auth: None                                                            │
│  - Metrics: GET /metrics (prometheus_client multi-process)               │
│  - Processing: Local (regex, file-type, custom detectors)                │
│  - FIPS: Inherited from AIPCC base image (OpenSSL, RHEL crypto)         │
│                                                                          │
│  [HuggingFace Detector Pod]                                              │
│  - ServingRuntime: guardrails-detector-runtime-guardian                   │
│  - Image: quay.io/rh-ee-mmisiura/guardrails-detector-huggingface:3d51741│
│  - Entrypoint: uvicorn app:app --workers 1 --port 8000                   │
│  - Port: 8000/TCP                                                        │
│  - Protocol: HTTP (plain)                                                │
│  - Auth: None                                                            │
│  - Models: AutoModelForSequenceClassification, GraniteForCausalLM        │
│  - Model source: MinIO via S3 data connection secret                     │
│         │                                                                │
│         │ Port: 9000/TCP                                                 │
│         │ Protocol: S3 (HTTP)                                            │
│         │ Encryption: Unknown                                            │
│         │ Auth: AWS credentials (aws-connection secret)                  │
│         ▼                                                                │
│  [MinIO - minio-guardrails-guardian]                                     │
│  - Type: ClusterIP                                                       │
│  - Port: 9000/TCP                                                        │
│  - Image: quay.io/trustyai/modelmesh-minio-examples:latest              │
│  - Purpose: Model artifact storage (S3-compatible)                       │
│                                                                          │
│  [LLM Judge Detector Pod]                                                │
│  - ServingRuntime: guardrails-detector-runtime-judge                     │
│  - Image: quay.io/trustyai/guardrails-detector-llm-judge:latest         │
│  - Entrypoint: uvicorn detectors.llm_judge.app:app --workers 4 --port   │
│    8000                                                                  │
│  - Port: 8000/TCP                                                        │
│  - Protocol: HTTP (plain)                                                │
│  - Auth: None                                                            │
│  - Metrics: GET /api/v1/metrics (prometheus-fastapi-instrumentator)      │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: Unknown (depends on deployment)                    │
│         │ Auth: None (app-level)                                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INFERENCE BACKEND (Internal)                                            │
│                                                                          │
│  [vLLM Inference Server - e.g. qwen2-predictor]                          │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Purpose: LLM evaluation backend for Judge detector                    │
│  - Configured via: VLLM_BASE_URL env var                                 │
│  - Auth: None (app-level)                                                │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- RoleBinding: user-one-view
  - Role: view (ClusterRole)
  - ServiceAccount: user-one
  - Note: Minimal RBAC; no custom ClusterRoles defined

Authentication & Authorization:
- NO application-level authentication on any detector endpoint
- All HTTP API endpoints (POST /api/v1/text/contents, POST /api/v1/text/generation,
  GET /metrics, GET /registry) are unprotected at the application layer
- Security is expected to be enforced at the platform/network level
  (Istio service mesh, KServe network policies)
- Risk: Network misconfiguration would expose unprotected content-analysis APIs

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Purpose: S3/MinIO credentials for HuggingFace model loading
  - Provisioned by: manifest
  - Auto-Rotate: Unknown
  - Contains: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY (expected)

FIPS Compliance:
- Built-in detector: FIPS-capable via AIPCC base image (OpenSSL, RHEL crypto policies)
- HuggingFace detector: Depends on build-time base image args (CUDA_BASE_IMAGE, CPU_BASE_IMAGE)
- LLM Judge detector: Not documented
- Application level: No direct crypto operations; uses system SSL via httpx/requests
- TLS: No application-level TLS; uvicorn serves plain HTTP
- Certificate handling: System trust store from base image

Network Exposure:
- All detector services are internal-only (no Ingress resources defined)
- MinIO exposed as ClusterIP service (internal only)
- No egress rules documented
