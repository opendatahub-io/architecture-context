┌─────────────────────────────────────────────────────────────────────────┐
│  UPSTREAM CALLER                                                        │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Role: Upstream caller for all detector variants                       │
│         │                                                                │
│         │ Port: 8080/TCP (Built-in), 8000/TCP (HF, Judge)               │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (configurable via ssl_ca_certs)               │
│         │ Auth: None (no auth middleware at application level)            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  DETECTOR PODS (Internal - Cluster Network)                             │
│                                                                          │
│  [Built-in Detector Pod]                                                 │
│  - Entrypoint: /app/bin/regex-detector                                   │
│  - Port: 8080/TCP                                                        │
│  - Endpoints: POST /api/v1/text/contents,                                │
│               POST /api/v1/text/generation,                              │
│               GET /metrics                                               │
│  - Auth: None                                                            │
│  - Encryption: None (TLS configurable but not enabled by default)        │
│  - Image: AIPCC CPU base (quay.io/aipcc/base-images/cpu:3.5.0)         │
│  - No external service calls (fully self-contained)                      │
│                                                                          │
│  [HuggingFace Detector Pod]                                              │
│  - Entrypoint: uvicorn app:app --workers 1 --port 8000                   │
│  - Port: 8000/TCP                                                        │
│  - Endpoints: POST /api/v1/text/contents,                                │
│               GET /metrics, GET /registry                                │
│  - Auth: None                                                            │
│  - Encryption: None (TLS configurable but not enabled by default)        │
│  - Image: AIPCC CPU base (quay.io/aipcc/base-images/cpu:3.5.0)         │
│  - ServingRuntime: guardrails-detector-runtime-guardian                   │
│  - Local PyTorch inference (no external HTTP calls for inference)         │
│         │                                                                │
│         │ Port: 9000/TCP                                                 │
│         │ Protocol: TCP                                                  │
│         │ Encryption: Unknown                                            │
│         │ Auth: AWS-style credentials (Opaque secret)                    │
│         ▼                                                                │
│  [MinIO - guardrails-container-deployment-guardian]                       │
│  - Service: minio-guardrails-guardian (ClusterIP)                        │
│  - Port: 9000/TCP                                                        │
│  - Image: quay.io/trustyai/modelmesh-minio-examples:latest              │
│  - Purpose: Model artifact storage                                       │
│                                                                          │
│  [LLM Judge Detector Pod]                                                │
│  - Entrypoint: uvicorn detectors.llm_judge.app:app --workers 4          │
│                --port 8000                                               │
│  - Port: 8000/TCP                                                        │
│  - Endpoints: POST /api/v1/text/contents,                                │
│               POST /api/v1/text/generation,                              │
│               GET /metrics, GET /registry                                │
│  - Auth: None                                                            │
│  - Encryption: None (TLS configurable but not enabled by default)        │
│  - Image: AIPCC CPU base (quay.io/aipcc/base-images/cpu:3.5.0)         │
│  - ServingRuntime: guardrails-detector-runtime-judge                     │
│         │                                                                │
│         │ Port: Unknown (configured via VLLM_BASE_URL)                   │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: Unknown                                            │
│         │ Auth: Unknown                                                  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [vLLM Inference Server]                                                 │
│  - Purpose: Remote LLM evaluation for Judge detector                     │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Auth: Unknown (configured via VLLM_BASE_URL env var)                  │
│  - Only accessed by LLM Judge variant                                    │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined by this component
- RoleBinding: user-one-view
  - Role: view (ClusterRole)
  - Service Account: user-one
  - Scope: Namespace-scoped

Authentication & Authorization:
- No auth middleware at application level (FastAPI/Starlette)
- All 5 HTTP endpoints accessible without credentials within pod network
- Authentication enforcement expected via platform layer (Istio/service mesh)

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Purpose: MinIO storage access credentials
  - Provisioned By: manifest
  - Auto-Rotate: Unknown

TLS/Encryption:
- Application-level TLS is configurable via ssl_ca_certs in config.yaml
- Not enabled by default in container entrypoints
- Base image: AIPCC CPU (UBI + OpenSSL) — crypto inherited from base
- No explicit FIPS mode opt-in at application level
- FIPS compliance depends on platform-level OpenSSL FIPS provider configuration

Network Services:
- guardrails-detectors: Internal (source server, no explicit Service type)
- minio-guardrails-guardian: ClusterIP, 9000/TCP, Internal
- No Ingress resources defined
- No explicit Egress rules defined
