┌─────────────────────────────────────────────────────────────────────────┐
│  COMPONENT: distributed-workloads                                        │
│  TYPE: Container Image Factory + Test Harness (NOT a service/operator)   │
│                                                                          │
│  NOTE: This component does NOT deploy controllers, services, or          │
│  admission webhooks. It produces container images consumed by platform   │
│  operators. All Services, Routes, and RBAC below are from EXAMPLE        │
│  manifests only.                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME (Konflux Pipeline)                                           │
│                                                                          │
│  [Konflux Build System]                                                  │
│         │                                                                │
│         │ Pulls base images:                                             │
│         │   - quay.io/aipcc/base-images/cuda-13.0-el9.6 (Runtime)       │
│         │   - quay.io/rhoai/odh-workbench-jupyter-minimal-* (Universal) │
│         │ Encryption: TLS (registry pull)                                │
│         │ Auth: Registry credentials                                     │
│         │ Build: Hermetic (hermeto/cachi2 prefetch)                      │
│         │                                                                │
│         │ Produces:                                                      │
│         │   - Runtime training images (CUDA/ROCm + OpenMPI + SSH)       │
│         │   - Universal training images (dual-mode entrypoint)           │
│         ▼                                                                │
│  [Container Registry - quay.io]                                          │
│  - Stores built images                                                   │
│  - Auth: Registry push credentials                                      │
│  - Encryption: TLS                                                       │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  RUNTIME (Platform-Managed Pods)                                         │
│                                                                          │
│  The produced images run as passive workloads. All network, security,   │
│  and lifecycle management is handled by the consuming platform           │
│  operators (Kubeflow Training Operator, KubeRay, Kueue, etc.)           │
│                                                                          │
│  [Training Pod - Runtime Image]                                          │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: Managed by operator                                   │
│  - Image: runtime training image (CUDA/ROCm + OpenMPI)                 │
│  - Network: SSH between pods (OpenMPI multi-node training)              │
│         │                                                                │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS/WSS                                            │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount token                                     │
│         ▼                                                                │
│  [Kubernetes API Server]                                                 │
│  - Purpose: Resource operations (if any)                                 │
│                                                                          │
│  [Universal Training Pod]                                                │
│  - Namespace: {user-namespace}                                           │
│  - Dual-mode entrypoint:                                                 │
│    - NOTEBOOK_ARGS set → Jupyter Notebook (8888/TCP)                    │
│    - Entrypoint overridden → Headless training                          │
│  - Network: Managed by platform operators                                │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  EXAMPLE MANIFESTS ONLY (Not Production)                                 │
│                                                                          │
│  The following services, routes, and RBAC are defined in example         │
│  manifests for workshops/demos. They are NOT part of the production      │
│  deployment surface.                                                     │
│                                                                          │
│  Services (example):                                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  minio-service (ClusterIP)                                      │    │
│  │  - 9000/TCP → MinIO API                                        │    │
│  │  - 9090/TCP → MinIO Console                                    │    │
│  │                                                                  │    │
│  │  minio (ClusterIP)                                              │    │
│  │  - 9000/TCP → MinIO API (http)                                  │    │
│  │  - 9001/TCP → MinIO Console                                    │    │
│  │                                                                  │    │
│  │  nfs-server (ClusterIP)                                         │    │
│  │  - 111/UDP  → Portmapper                                       │    │
│  │  - 2049/TCP → NFS                                               │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  Ingress/Routes (example):                                               │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  minio-api    (Route) → HTTPS/TLS                              │    │
│  │  minio-ui     (Route) → HTTPS/TLS                              │    │
│  │  minio-console(Route) → HTTPS/TLS                              │    │
│  │  minio        (Route) → HTTPS/TLS                              │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  Egress (example/test only):                                             │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  → Kubernetes API: 6443/TCP, HTTPS/WSS, TLS 1.2+              │    │
│  │    Auth: ServiceAccount token                                   │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary (Example Manifests Only):
- RoleBinding: edit
  - Namespace: distributed
  - Role: edit (ClusterRole)
  - ServiceAccount: stable-diffusion-nb

- RoleBinding: nfs-server-sa-anyuid
  - Namespace: nfs
  - Role: system:openshift:scc:anyuid (ClusterRole)
  - ServiceAccount: nfs-server-sa

- RoleBinding: nfs-server-sa-privileged
  - Namespace: nfs
  - Role: system:openshift:scc:privileged (ClusterRole)
  - ServiceAccount: nfs-server-sa

Secrets (Example Manifests Only):
- minio (Opaque) - MinIO credentials, provisioned by manifest
- minio-secret (Opaque) - MinIO credentials, provisioned by manifest

FIPS Compliance:
- No Go binaries shipped (Go code is test-only)
- Python images inherit OpenSSL from UBI9 base images (AIPCC / ODH workbench)
- cryptography==46.0.5 uses OpenSSL from base image
- FIPS mode opt-in delegated to base image and platform configuration

Security Dependencies:
- cryptography==46.0.5 (TLS/crypto library, uses base image OpenSSL)
- pyjwt==2.13.0 (JWT verification, transitive)
- kubernetes==30.1.0 (RBAC-capable client, test-only)
