┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [AI Agent / MCP Client]                                                 │
│         │                                                                │
│         │ Transport: stdio (direct process, no network)                  │
│         │        OR: SSE / streamable-http                               │
│         │ Port: 8000/TCP (HTTP transports)                               │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: Unknown (no TLS termination specified)             │
│         │ Auth: OIDC Bearer Token (JWT or TokenReview)                   │
│         │       when OIDC enabled; none when disabled                    │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION LAYER                                                       │
│                                                                          │
│  [OIDC Auth Middleware] (ASGI)                                           │
│  - Validates Bearer tokens on MCP protocol endpoints                    │
│  - Exempts: /health, /.well-known/oauth-protected-resource              │
│  - Validation modes: JWT verification or TokenReview                    │
│  - When OIDC disabled: all tools accessible (single-user mode)          │
│         │                                                                │
│         │ Internal (in-process)                                          │
│         ▼                                                                │
│  [FastMCP Server] (Starlette/uvicorn)                                    │
│  - Namespace: rhoai-mcp                                                  │
│  - ServiceAccount: rhoai-mcp                                             │
│  - Image: rhoai-mcp:latest                                               │
│  - Replicas: 1                                                           │
│  - Per-tool RBAC filtering via SubjectAccessReview                      │
│         │                                                                │
│         │ Internal (in-process, via pluggy plugin system)                │
│         ▼                                                                │
│  [Plugin Handlers]                                                       │
│  - Workbench Plugin (Kubeflow Notebooks)                                │
│  - Model Serving Plugin (KServe InferenceServices, ServingRuntimes)     │
│  - Training Plugin (TrainJobs, TrainingRuntimes)                        │
│  - Pipeline Plugin (DataSciencePipelinesApplications)                   │
│  - Cluster Mgmt Plugin (pods, nodes, PVs, namespaces, secrets)         │
│         │                                                                │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount token (default)                           │
│         │       OR forwarded user token / impersonation (OIDC mode)     │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES API SERVER                                                   │
│                                                                          │
│  [Kubernetes API - Port 6443/TCP HTTPS]                                 │
│  - TLS 1.2+                                                              │
│  - Auth: ServiceAccount token / user token / impersonation              │
│                                                                          │
│  Managed Resources:                                                      │
│  - core: pods, pods/log, services, namespaces, secrets, PVCs,           │
│          nodes, persistentvolumes, events                                │
│  - kubeflow.org: notebooks                                               │
│  - serving.kserve.io: inferenceservices, servingruntimes                │
│  - trainer.kubeflow.org: trainjobs, trainingruntimes,                   │
│                          clustertrainingruntimes                         │
│  - datasciencepipelinesapplications.opendatahub.io:                     │
│                          datasciencepipelinesapplications                │
│  - storage.k8s.io: storageclasses                                       │
└─────────────────────────────────────────────────────────────────────────┘

SERVICE TOPOLOGY:
┌────────────────────────────────────────────────────┐
│  Service: rhoai-mcp                                 │
│  Type: ClusterIP                                    │
│  Port: 8000/TCP → http (target)                    │
│  Protocol: TCP                                      │
│  Exposure: Internal (cluster-only)                  │
│  Namespace: rhoai-mcp                               │
└────────────────────────────────────────────────────┘

AUTHENTICATION & AUTHORIZATION:
┌────────────────────────────────────────────────────────────────────────┐
│  Endpoint                        │ Auth              │ Enforcement     │
│  ───────────────────────────────  ─────────────────── ───────────────  │
│  MCP API (SSE/streamable-http)  │ OIDC Bearer (JWT  │ OIDCAuth       │
│                                  │ or TokenReview)   │ Middleware     │
│                                  │ + per-tool RBAC   │ + SAR check    │
│  /health                        │ None              │ Excluded       │
│  /.well-known/oauth-protected-  │ None              │ Excluded       │
│      resource                   │                   │                 │
└────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: rhoai-mcp
  - Core API:
    - events, nodes, pods, pods/log, services: get, list, watch
    - persistentvolumes, pods: create
    - namespaces: create, delete, get, list, patch, watch
    - persistentvolumeclaims, secrets: create, delete, get, list, watch
  - kubeflow.org:
    - notebooks: create, delete, get, list, patch, watch
  - trainer.kubeflow.org:
    - trainjobs: create, delete, get, list, patch, update, watch
    - trainingruntimes: get, list, watch
    - clustertrainingruntimes: create, delete, get, list, watch
  - datasciencepipelinesapplications.opendatahub.io:
    - datasciencepipelinesapplications: create, delete, get, list, watch
  - serving.kserve.io:
    - inferenceservices: create, delete, get, list, watch
    - servingruntimes: create, get, list, watch
  - storage.k8s.io:
    - storageclasses: list

- ClusterRoleBinding: rhoai-mcp
  - Namespace: rhoai-mcp
  - ServiceAccount: rhoai-mcp
  - Scope: Cluster-wide (namespace isolation depends on OIDC/RBAC layer)

Security Evidence:
- TLS/Crypto: cryptography ==50.0.0 (wraps OpenSSL)
- Auth: PyJWT[crypto] >=2.12.1 (JWT operations)
- RBAC: kubernetes Python client (SubjectAccessReview capable)
- FIPS: Inherited from AIPCC base image OpenSSL; not explicitly configured at app level

Secrets:
- No application-specific secrets defined in deployment manifests
- ServiceAccount token auto-mounted for Kubernetes API access
