┌─────────────────────────────────────────────────────────────────────────┐
│  INIT CONTAINER (No Network — Filesystem Only)                          │
│                                                                          │
│  [Init Container: init_managed_pipelines]                                │
│  - Image: pipelines-components (UBI9/python-312)                         │
│  - Operation: One-shot filesystem copy                                   │
│  - Source: /managed-pipelines (baked into image at build)                │
│  - Destination: /config/managed-pipelines (shared volume)               │
│  - Network: NONE — no listening ports, no outbound connections          │
│  - Auth: NONE — local filesystem operation only                          │
│                                                                          │
│  NOTE: This is the primary deployable artifact. It runs as a             │
│  Kubernetes init container, copies files, then exits.                    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  KFP COMPONENT PODS (Ephemeral — Created per Pipeline Run)              │
│                                                                          │
│  [@dsl.component Pods]                                                   │
│  - Namespace: user pipeline namespace                                    │
│  - ServiceAccount: pipeline run service account                          │
│  - Image: component-specific (built from component Dockerfiles)         │
│  - Lifecycle: Ephemeral, created by KFP orchestrator per step           │
│         │                                                                │
│         │ Outbound connections (component-dependent):                    │
│         │                                                                │
│         ├── Port: 443/TCP                                                │
│         │   Protocol: HTTPS                                              │
│         │   Encryption: TLS (system OpenSSL via UBI9)                    │
│         │   Auth: AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY (env vars)  │
│         │   Destination: S3-compatible storage                           │
│         │                                                                │
│         ├── Port: 443/TCP                                                │
│         │   Protocol: HTTPS                                              │
│         │   Encryption: TLS (in-cluster CA)                              │
│         │   Auth: ServiceAccount token (in-cluster auto-mount)          │
│         │   Destination: Kubernetes API Server                           │
│         │                                                                │
│         ├── Port: Configured by runtime                                  │
│         │   Protocol: HTTP/HTTPS                                         │
│         │   Encryption: Varies (cluster-internal)                        │
│         │   Auth: CodeFlare SDK credentials                              │
│         │   Destination: Ray Cluster (via CodeFlare)                    │
│         │                                                                │
│         └── Port: 443/TCP                                                │
│             Protocol: HTTPS                                              │
│             Encryption: TLS                                              │
│             Auth: HF_TOKEN (env var, optional)                           │
│             Destination: HuggingFace Hub (dataset download)             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3-Compatible Storage]                                                 │
│  - Purpose: Dataset and model artifact storage                           │
│  - Auth: AWS IAM credentials (env vars)                                  │
│  - Encryption: TLS (boto3 verify=True default)                          │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Purpose: Resource CRUD operations                                     │
│  - Auth: ServiceAccount token (in-cluster)                               │
│  - Encryption: TLS (in-cluster CA)                                       │
│                                                                          │
│  [Ray Cluster via CodeFlare]                                             │
│  - Purpose: Distributed job execution (parse-chunk, training, eval)     │
│  - Auth: CodeFlare SDK credentials                                       │
│  - Encryption: Cluster-internal                                          │
│                                                                          │
│  [HuggingFace Hub]                                                       │
│  - Purpose: Dataset download                                             │
│  - Auth: HF_TOKEN (optional, for gated models/datasets)                 │
│  - Encryption: TLS                                                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined by this component
- Component pods inherit RBAC from the KFP pipeline run ServiceAccount
- Kubernetes API access depends on permissions granted to the pipeline SA

Secrets / Credentials (Environment Variables):
- AWS_SECRET_ACCESS_KEY — S3 storage credentials
- S3_SECRET_KEY — Alternative S3 credential key
- HF_TOKEN — HuggingFace Hub authentication (optional)
- LLM_API_KEY — LLM service API key
- OCI_PULL_SECRET_MODEL_DOWNLOAD — OCI registry pull secret for model downloads
- KUBERNETES_AUTH_TOKEN — Kubernetes API auth (fallback if not using in-cluster SA)
- CHUNK_MAX_TOKENS — Configuration (not a secret, but treated as env var)
- TOKENIZER — Configuration (not a secret, but treated as env var)

Supply Chain:
- Base image: registry.redhat.io/ubi9/python-312 (digest-pinned)
- Dependencies: Hash-pinned from RHAI curated PyPI index
- Index URL: console.redhat.com/api/pypi/public-rhai/rhoai/3.5/cpu-ubi9/simple
- Crypto: cryptography==49.0.0 (delegates to system OpenSSL on UBI9)
- FIPS: Platform-delegated via UBI9 — no explicit application-level FIPS opt-in

Network Attack Surface:
- MINIMAL — Init container has zero network exposure
- Component pods are ephemeral with OUTBOUND-ONLY connections
- No listening ports, no ingress, no services defined
- All outbound connections use TLS (boto3 verify=True, Kubernetes in-cluster CA)
