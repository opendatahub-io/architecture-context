┌─────────────────────────────────────────────────────────────────────────┐
│  POD STARTUP (Init Container Phase)                                     │
│                                                                          │
│  [Init Container: init_managed_pipelines]                                │
│  - Image: pipelines-components (UBI9/python-312)                         │
│  - Entrypoint: python -m scripts.init_managed_pipelines                  │
│  - No network listeners — batch-only                                     │
│         │                                                                │
│         │ Filesystem I/O only                                            │
│         │ Source: /app (pre-compiled YAMLs)                              │
│         │ Dest: /config/managed-pipelines (shared volume)                │
│         │                                                                │
│         │ When RELATED_IMAGE_* env vars present:                         │
│         │ Recompiles pipelines with substituted image refs               │
│         ▼                                                                │
│  [Shared Volume: /config/managed-pipelines]                              │
│  - Type: emptyDir or PVC (pod-local)                                     │
│  - Consumer: KFP API Server                                              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  PIPELINE EXECUTION (Step Containers)                                    │
│                                                                          │
│  [Pipeline Step Container]                                               │
│  - Namespace: {pipeline-namespace}                                       │
│  - ServiceAccount: {pipeline-sa}                                         │
│  - Runs as KFP @dsl.component decorated function                        │
│                                                                          │
│  OUTBOUND CONNECTIONS:                                                   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────┐            │
│  │  → Kubernetes API Server                                  │            │
│  │    Port: 443/TCP                                          │            │
│  │    Protocol: HTTPS                                        │            │
│  │    Encryption: TLS (in-cluster)                           │            │
│  │    Auth: ServiceAccount token (load_incluster_config)     │            │
│  │    Purpose: K8s resource operations, RayJob submission,   │            │
│  │             Model Registry registration                   │            │
│  └──────────────────────────────────────────────────────────┘            │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────┐            │
│  │  → S3-Compatible Storage                                  │            │
│  │    Port: 443/TCP                                          │            │
│  │    Protocol: HTTPS                                        │            │
│  │    Encryption: TLS 1.2+                                   │            │
│  │    Auth: AWS credentials (env vars)                       │            │
│  │      - AWS_SECRET_ACCESS_KEY                              │            │
│  │      - S3_SECRET_KEY                                      │            │
│  │    Purpose: Dataset download, model artifact storage      │            │
│  └──────────────────────────────────────────────────────────┘            │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────┐            │
│  │  → HuggingFace Hub (huggingface.co)                       │            │
│  │    Port: 443/TCP                                          │            │
│  │    Protocol: HTTPS                                        │            │
│  │    Encryption: TLS 1.2+                                   │            │
│  │    Auth: HF_TOKEN (env var, optional)                     │            │
│  │    Purpose: Model and dataset downloads                   │            │
│  └──────────────────────────────────────────────────────────┘            │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────┐            │
│  │  → KServe Inference Endpoint (cluster-internal)           │            │
│  │    Port: Runtime-configured                               │            │
│  │    Protocol: HTTPS / HTTP                                 │            │
│  │    Encryption: mTLS (service mesh) or TLS                 │            │
│  │    Auth: ServiceAccount / Bearer token                    │            │
│  │    Purpose: Model evaluation via Eval Hub                 │            │
│  └──────────────────────────────────────────────────────────┘            │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────┐            │
│  │  → Ray Cluster (cluster-internal, optional)               │            │
│  │    Port: Runtime-configured                               │            │
│  │    Protocol: HTTP/gRPC                                    │            │
│  │    Encryption: Cluster-internal (service mesh if present) │            │
│  │    Auth: In-cluster ServiceAccount                        │            │
│  │    Purpose: Distributed document processing (parse/chunk) │            │
│  └──────────────────────────────────────────────────────────┘            │
│                                                                          │
│  INBOUND CONNECTIONS: None                                               │
│  - No HTTP endpoints exposed                                             │
│  - No Kubernetes Services defined                                        │
│  - Batch-oriented: init container + pipeline step library                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  STORAGE                                                                 │
│                                                                          │
│  [Workspace PVC]                                                         │
│  - Purpose: Intermediate artifacts between pipeline steps                │
│  - Access: ReadWriteOnce per pipeline run                                │
│  - Data: Datasets, model artifacts, training outputs                     │
│                                                                          │
│  [Shared Volume: /config/managed-pipelines]                              │
│  - Purpose: Pre-compiled pipeline YAMLs for KFP API Server              │
│  - Access: Init container writes, API server reads                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined by this component
- Pipeline step containers inherit RBAC from the pipeline ServiceAccount
- Required permissions (inferred from code):
  - inferenceservices (serving.kserve.io): create, get, list
  - servingruntimes (serving.kserve.io): get, list
  - rayjobs (ray.io): create, get, list, watch (for CodeFlare SDK)
  - configmaps, secrets: get (for credential access)

Secrets / Credentials:
- AWS_SECRET_ACCESS_KEY (env var) — S3 storage access credentials
- TEST_DATA_AWS_SECRET_ACCESS_KEY (env var) — Test data S3 credentials
- INPUT_DATA_AWS_SECRET_ACCESS_KEY (env var) — Input data S3 credentials
- HF_TOKEN (env var) — HuggingFace Hub access token
- S3_SECRET_KEY (env var) — Alternative S3 credentials
- LLM_API_KEY (env var) — LLM service API key
- OCI_PULL_SECRET_MODEL_DOWNLOAD (env var) — OCI registry pull secret
- KUBERNETES_AUTH_TOKEN (env var) — Kubernetes API token
- CHUNK_MAX_TOKENS (env var) — Chunking configuration
- TOKENIZER (env var) — Tokenizer configuration

FIPS Compliance:
- Base image: UBI9 (registry.redhat.io/ubi9/python-312) — FIPS-validated OpenSSL
- Crypto library: cryptography==49.0.0 (delegates to OpenSSL)
- Package index: RHAI PyPI (cpu-ubi9 variant) — supply chain controlled
- FIPS mode: Inherited from host OS configuration
- Non-FIPS crypto risks: None detected

Network Exposure:
- NONE — This component exposes no network listeners
- All connections are OUTBOUND only (client-side)
- Init container performs filesystem I/O only (no network)
- Pipeline step containers make outbound calls to external services
