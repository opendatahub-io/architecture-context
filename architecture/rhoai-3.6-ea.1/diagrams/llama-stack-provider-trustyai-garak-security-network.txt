┌─────────────────────────────────────────────────────────────────────────┐
│  KUBERNETES JOB POD (No Inbound Connections)                            │
│                                                                          │
│  [llama-stack-provider-trustyai-garak]                                   │
│  - Deployment Type: Kubernetes Job (batch, not long-running)             │
│  - Namespace: {evaluation-namespace}                                     │
│  - Image: BASE_IMAGE (parameterized via Dockerfile.konflux)              │
│  - Exposed Services: NONE (io.openshift.expose-services="")             │
│  - Inbound Ports: NONE                                                   │
│                                                                          │
│  Credential Sources (Local Reads):                                       │
│  - /var/run/secrets/kubernetes.io/serviceaccount/token (SA Token)        │
│  - /mnt/model-auth/ (KFP-mounted model secrets)                         │
│  - /var/run/secrets/model/ (eval-hub model secrets)                      │
│  - Environment: API_KEY, OPENAICOMPATIBLE_API_KEY                        │
│  - Environment: KFP_AUTH_TOKEN                                           │
│  - Environment: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY                 │
│                                                                          │
│  Two Operational Modes:                                                  │
│  - Simple Mode: Garak runs in-pod                                        │
│  - KFP Mode: Garak runs in separate Kubeflow Pipelines pod              │
│    (AutomountServiceAccountToken=false with sidecar proxy in >= 0.4.4)  │
│         │                                                                │
│         │ OUTBOUND CONNECTIONS ONLY                                      │
│         │                                                                │
│         ├──────────────────────────────────────────────┐                  │
│         │                                              │                  │
│         ▼                                              ▼                  │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL SERVICES                                               │
│                                                                          │
│  [Kubeflow Pipelines API]                                                │
│  - Namespace: kubeflow / dspa-namespace                                  │
│  - Port: Configured at runtime                                           │
│  - Protocol: HTTPS                                                       │
│  - Auth: Bearer Token (ServiceAccount Token)                             │
│  - Auth Model 1: Direct SA token from filesystem                         │
│  - Auth Model 2: Sidecar proxy injection (eval-hub >= 0.4.4)           │
│  - Purpose: Submit pipeline runs, poll for completion                    │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 6443/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount Token                                            │
│  - Purpose: Read Secrets (AWS credentials for S3)                        │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [Target LLM Endpoint]                                                   │
│  - Port: Configured at runtime (model-dependent)                         │
│  - Protocol: HTTPS                                                       │
│  - Auth: Model API Key (per-model, from credential cascade)              │
│  - Purpose: Red-teaming probe execution (Garak)                          │
│  - Encryption: TLS (inherited from libraries)                            │
│                                                                          │
│  [S3-Compatible Storage]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: AWS IAM credentials (boto3)                                     │
│  - Credentials: AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY                │
│  - Source: Environment variables or K8s Secret (via K8s API)             │
│  - Purpose: Upload evaluation results and sanitized configurations       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined in component manifests
- Implicit RBAC: ServiceAccount requires read access to Secrets
  (for AWS credential resolution via in-cluster K8s API)

Secrets Summary:
- API_KEY (env var) - Generic LLM API key fallback
- KFP_AUTH_TOKEN (env var) - Kubeflow Pipelines authentication token
- OPENAICOMPATIBLE_API_KEY (env var) - OpenAI-compatible endpoint API key
- AWS_SECRET_ACCESS_KEY (env var) - S3 storage credentials
- AWS_ACCESS_KEY_ID (env var) - S3 storage credentials
- ServiceAccount Token (filesystem) - /var/run/secrets/kubernetes.io/serviceaccount/token

Credential Resolution Cascade (Model API Keys):
1. Role-specific environment variable (e.g., OPENAICOMPATIBLE_API_KEY)
2. KFP-mounted secrets (/mnt/model-auth/)
3. Eval-hub model secrets (/var/run/secrets/model/)
4. Generic API_KEY environment variable
5. "DUMMY" sentinel (unauthenticated local endpoints)

FIPS Compliance:
- No application-level FIPS configuration
- Cryptographic behavior inherited from container base image OpenSSL
- cryptography ==49.0.0 (transitive, OpenSSL backend)
- pyjwt ==2.13.0 (transitive)
- FIPS posture determined by BASE_IMAGE selected at build time
