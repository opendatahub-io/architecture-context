┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL CALLERS                                                            │
│                                                                              │
│  [llm-d Inference Gateway]                                                   │
│  - Role: Sends observed TTFT/TPOT latency samples for model training         │
│         │                                                                    │
│         │ Port: 8000/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (plain HTTP)                                      │
│         │ Auth: None (no auth middleware)                                     │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (No Service Mesh detected)                                 │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐   │
│  │  Training Server (1 replica)                                          │   │
│  │  - Deployment: training-server-deployment                             │   │
│  │  - Service: training-service (ClusterIP 8000:8000)                    │   │
│  │  - Image: placeholder-for-training-server-image                       │   │
│  │  - Base: quay.io/aipcc/base-images/cpu:3.5.0                         │   │
│  │  - Runtime: uvicorn (FastAPI)                                         │   │
│  │  - Volumes: emptyDir (model storage, ephemeral)                       │   │
│  │                                                                        │   │
│  │  Exposed Endpoints:                                                    │   │
│  │    POST /add_training_data_bulk  - Ingest latency samples             │   │
│  │    POST /flush                   - Flush training data                 │   │
│  │    GET  /data/status             - Training data status               │   │
│  │    GET  /debug/prefix_distribution - Debug info                       │   │
│  │    GET  /metrics                 - Prometheus metrics                  │   │
│  │    GET  /model/export            - Download all model artifacts        │   │
│  │    GET  /model/{name}/download   - Download specific model            │   │
│  │    GET  /model/{name}/info       - Model metadata                     │   │
│  │    GET  /model/download/info     - Download info                      │   │
│  │    GET  /models/list             - List available models              │   │
│  │    GET  /model/ttft/xgb/json     - XGBoost TTFT model (JSON)         │   │
│  │    GET  /model/ttft/lgb/txt      - LightGBM TTFT model (text)        │   │
│  │    GET  /model/ttft/lgb/importances - TTFT feature importances       │   │
│  │    GET  /model/tpot/xgb/json     - XGBoost TPOT model (JSON)         │   │
│  │    GET  /model/tpot/lgb/txt      - LightGBM TPOT model (text)        │   │
│  │    GET  /model/tpot/lgb/importances - TPOT feature importances       │   │
│  │                                                                        │   │
│  │  All endpoints: NO AUTHENTICATION                                      │   │
│  └────────────────────────────────────────────────────────────────────────┘   │
│         ▲                                                                    │
│         │ Port: 8000/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None                                                   │
│         │ Auth: None                                                         │
│         │ Polling every 10s (MODEL_SYNC_INTERVAL_SEC)                        │
│         │                                                                    │
│  ┌────────────────────────────────────────────────────────────────────────┐   │
│  │  Prediction Server (10 replicas)                                      │   │
│  │  - Deployment: prediction-server-deployment                           │   │
│  │  - Service: prediction-service (ClusterIP 80:8001)                    │   │
│  │  - Image: placeholder-for-prediction-server-image                     │   │
│  │  - Base: quay.io/aipcc/base-images/cpu:3.5.0                         │   │
│  │  - Runtime: uvicorn (FastAPI, $UVICORN_WORKERS workers)               │   │
│  │  - Volumes: emptyDir (model cache, ephemeral)                         │   │
│  │  - Env: TRAINING_SERVER_URL=http://training-service:8000              │   │
│  │                                                                        │   │
│  │  Exposed Endpoints:                                                    │   │
│  │    POST /predict               - Single prediction                    │   │
│  │    POST /predict/bulk          - Batch predictions                    │   │
│  │    POST /predict/bulk/strict   - Strict batch predictions             │   │
│  │    POST /reload                - Reload models                        │   │
│  │    GET  /status                - Server status                        │   │
│  │    GET  /healthz               - Liveness probe                       │   │
│  │    GET  /readyz                - Readiness probe                      │   │
│  │    GET  /                      - Root endpoint                        │   │
│  │                                                                        │   │
│  │  All endpoints: NO AUTHENTICATION                                      │   │
│  └────────────────────────────────────────────────────────────────────────┘   │
│         ▲                                                                    │
│         │ Port: 80/TCP → 8001/TCP                                            │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None                                                   │
│         │ Auth: None                                                         │
│         │                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  PREDICTION CLIENTS                                                          │
│                                                                              │
│  [llm-d Gateway / Internal Services]                                         │
│  - Role: Request latency predictions for routing decisions                   │
│  - Access: Via prediction-service ClusterIP                                  │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccounts explicitly configured
- Access control delegated entirely to network-level mechanisms

Service Mesh Configuration:
- No PeerAuthentication detected
- No AuthorizationPolicy detected
- No Istio/Service Mesh sidecar injection configured
- Network isolation relies on Kubernetes NetworkPolicy (if deployed)

Secrets / Environment Variables:
- LATENCY_ENABLE_TOKEN_IN_FLIGHT_FEATURES (env var) - Feature flag, runtime environment
- LATENCY_TPOT_ZERO_TOKEN_COUNT (env var) - Configuration, runtime environment
- TRAINING_SERVER_URL (env var) - Default: http://training-service:8000
- MODEL_SYNC_INTERVAL_SEC (env var) - Default: 10
- LATENCY_RETRAINING_INTERVAL_SEC (env var) - Default: 1800
- LATENCY_MAX_TRAINING_DATA_SIZE_PER_BUCKET (env var) - Default: 500
- UVICORN_WORKERS (env var) - Configurable worker count for prediction server

TLS / Encryption Notes:
- NO TLS termination at application level (uvicorn serves plain HTTP)
- TLS expected to be provided by upstream proxy, service mesh, or ingress
- AIPCC base image provides system OpenSSL for outbound HTTPS (requests library)
- hashlib.sha256 used for model file checksums only (not security-critical)

FIPS Compliance:
- Python application; FIPS relies on system OpenSSL from AIPCC base image
- No non-FIPS crypto libraries detected
- No custom cipher selection

Cold Start Risk:
- emptyDir volumes mean trained models are EPHEMERAL
- After pod restart, training server must re-accumulate data and retrain
- Prediction server will serve stale/no predictions until models sync
