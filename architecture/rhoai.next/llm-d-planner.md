# Component: llm-d-planner

## Metadata

- **Repository**: https://github.com/llm-d-incubation/llm-d-planner
- **Version**: v0.1.0-73-g4813387
- **Distribution**: RHOAI
- **Languages**: Python
- **Deployment Type**: Service (FastAPI Backend + Streamlit UI + vLLM Simulator)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/llm-d-incubation/llm-d-planner | -- | main | -- | local_analysis |

_This repository is the upstream origin. No midstream or downstream forks were detected. It lives in the `llm-d-incubation` GitHub organization, which hosts incubation-stage components for the llm-d project. No sync workflows found in `.github/workflows/`._

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

_No aliases detected._

## Purpose

**Short**: LLM deployment planning platform that guides users from business requirements to production-ready Kubernetes deployments through conversational intent extraction, SLO-driven GPU/model recommendations, and automated YAML generation.

**Detailed**: llm-d Planner is a full-stack web application that unifies three core capabilities for LLM deployment planning: (1) a **Planner** workflow that extracts deployment intent from natural language, maps it to traffic profiles and SLO targets, queries a benchmark database, and produces ranked model+GPU recommendations; (2) a **Capacity Planner** that estimates GPU memory requirements (model weights, KV cache for MHA/GQA/MQA/MLA attention, activation memory, CUDA graph overhead) for any HuggingFace model; and (3) a **GPU Recommender** that estimates inference performance (TTFT, ITL, throughput) across GPU types using BentoML's llm-optimizer roofline model.

The system is composed of a FastAPI backend exposing REST endpoints under `/api/v1/`, a Streamlit frontend for interactive use, a PostgreSQL database storing benchmark data, and an Ollama instance for LLM-powered intent extraction. It supports deploying generated KServe/vLLM configurations to Kubernetes clusters and includes a GPU-free vLLM simulator for development. The backend integrates with the RHOAI Model Catalog for benchmark data sourcing and supports multiple LLM providers (Ollama, Vertex AI, OpenAI-compatible) for intent extraction. It can generate deployment manifests for both standalone vLLM (KServe InferenceService) and the llm-d stack (kustomize overlay + Helm values).

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| Backend API | Python Service (FastAPI) | REST API for recommendations, capacity planning, GPU estimation, deployment, and database management |
| Streamlit UI | Python Frontend (Streamlit) | Chat-based web interface for interactive deployment planning, capacity analysis, and GPU comparison |
| vLLM Simulator | Python Service (FastAPI) | GPU-free mock vLLM server with OpenAI-compatible API for local development and testing |
| Capacity Planner | Python Library | GPU memory estimation engine supporting MHA/GQA/MQA/MLA attention, quantization, and parallelism |
| GPU Recommender | Python Library | Performance estimation across GPU types using BentoML llm-optimizer roofline model |
| Recommendation Workflow | Python Library | End-to-end orchestration: intent extraction → traffic profiling → SLO targeting → config scoring → ranking |
| Intent Extractor | Python Library | LLM-powered natural language understanding for deployment requirements |
| Configuration Generator | Python Library | Jinja2-based YAML generation for KServe InferenceService, HPA, ServiceMonitor, and llm-d stack |
| Cluster Manager | Python Library | Kubernetes deployment management via kubectl for InferenceService lifecycle |
| Knowledge Base | Python Library | Benchmark data loading, model catalog integration, SLO templates, and GPU catalog |

## Sub-Component Details

### Backend API

**Dockerfile**: `Dockerfile`
**Intent**: Primary service — the main FastAPI backend that exposes all REST endpoints. Always deployed.
**Image**: `quay.io/llm-d-planner/llm-d-planner-backend:latest`
**Language**: Python
**Port**: 8000/TCP

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|
| `/health` | GET | -- | None | Health check |
| `/api/v1/model-info` | POST | HuggingFace Hub API | HF_TOKEN (env) | Fetch model architecture metadata |
| `/api/v1/calculate` | POST | HuggingFace Hub API | HF_TOKEN (env) | Run capacity planning calculations |
| `/api/v1/estimate` | POST | HuggingFace Hub API | HF_TOKEN (env) | GPU performance estimation via roofline model |
| `/api/v1/extract` | POST | Ollama / Vertex AI / OpenAI | LLM provider credentials | Extract structured intent from natural language |
| `/api/v1/recommend` | POST | PostgreSQL, Ollama | DB + LLM credentials | Generate single best deployment recommendation |
| `/api/v1/ranked-recommend-from-spec` | POST | PostgreSQL | DB credentials | Generate 5 ranked recommendation lists from pre-built spec |
| `/api/v1/deploy` | POST | -- | None | Generate deployment YAML (inline response) |
| `/api/v1/deploy-to-cluster` | POST | Kubernetes API | kubectl kubeconfig | Generate and apply YAML to Kubernetes cluster |
| `/api/v1/cluster-status` | GET | Kubernetes API | kubectl kubeconfig | Check cluster accessibility and list InferenceServices |
| `/api/v1/deployments` | GET | Kubernetes API | kubectl kubeconfig | List all InferenceServices with status |
| `/api/v1/deployments/{id}/k8s-status` | GET | Kubernetes API | kubectl kubeconfig | Get real deployment status from cluster |
| `/api/v1/deployments/{id}/status` | GET | -- | None | Mock observability metrics (simulated) |
| `/api/v1/deployments/{id}` | DELETE | Kubernetes API | kubectl kubeconfig | Delete InferenceService from cluster |
| `/api/v1/deployment-mode` | GET | -- | None | Get current deployment mode (production/simulator) |
| `/api/v1/deployment-mode` | PUT | -- | None | Set deployment mode |
| `/api/v1/slo-defaults/{use_case}` | GET | -- | None | Get default SLO values for a use case |
| `/api/v1/workload-profile/{use_case}` | GET | -- | None | Get workload profile for capacity planning |
| `/api/v1/expected-rps/{use_case}` | GET | -- | None | Calculate expected RPS from workload patterns |
| `/api/v1/models` | GET | -- | None | List available models from catalog |
| `/api/v1/gpu-types` | GET | -- | None | List available GPU types from catalog |
| `/api/v1/use-cases` | GET | -- | None | List supported use cases with SLO templates |
| `/api/v1/priority-weights` | GET | -- | None | Get priority-to-weight mapping config |
| `/api/v1/weighted-scores/{use_case}` | GET | -- | None | Get use-case-specific accuracy scores |
| `/api/v1/db/status` | GET | PostgreSQL | None | Get benchmark database statistics |
| `/api/v1/db/upload-benchmarks` | POST | PostgreSQL | X-Admin-Password header | Upload benchmark JSON file |
| `/api/v1/db/reset` | POST | PostgreSQL | X-Admin-Password header | Reset benchmark database |
| `/api/v1/db/admin-required` | GET | -- | None | Check if admin password is configured |
| `/api/v1/db/verify-admin` | POST | -- | X-Admin-Password header | Verify admin password |

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| PostgreSQL | TCP (libpq) | 5432/TCP | Password (DATABASE_URL) | Benchmark data storage and retrieval |
| Ollama | HTTP | 11434/TCP | None | LLM inference for intent extraction (default provider) |
| Vertex AI | HTTPS | 443/TCP | GCP credentials (JSON) | LLM inference via Claude on Vertex AI (optional) |
| OpenAI-compatible API | HTTPS | 443/TCP | API key (Bearer) | LLM inference via any OpenAI-compatible endpoint (optional) |
| HuggingFace Hub API | HTTPS | 443/TCP | HF_TOKEN (Bearer, optional) | Model config, safetensors metadata, parameter counts |
| Kubernetes API | HTTPS | 6443/TCP | kubeconfig / in-cluster SA | Cluster management, InferenceService lifecycle, GPU detection |
| RHOAI Model Catalog | HTTPS | 8443/TCP | Bearer token / SA token | Benchmark data sourcing (optional, RHOAI deployments) |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `DATABASE_URL` | env | `postgresql://postgres:planner@localhost:5432/planner` | PostgreSQL connection string |
| `OLLAMA_HOST` | env | `http://localhost:11434` | Ollama server URL |
| `LLM_PROVIDER` | env | `ollama` | LLM provider selection: `ollama`, `vertex`, `openai` |
| `LLM_MODEL` | env | provider-specific default | Model name for intent extraction |
| `HF_TOKEN` | env | (empty) | HuggingFace token for gated model access |
| `PLANNER_BENCHMARK_SOURCE` | env | `postgresql` | Benchmark data source: `postgresql` or `model_catalog` |
| `MODEL_CATALOG_URL` | env | `https://model-catalog.rhoai-model-registries.svc:8443` | RHOAI Model Catalog endpoint |
| `MODEL_CATALOG_TOKEN` | env | (empty) | Bearer token for Model Catalog (or auto-read from SA) |
| `MODEL_CATALOG_VERIFY_SSL` | env | `true` | Whether to verify TLS for Model Catalog |
| `MODEL_CATALOG_CA_BUNDLE` | env | `/etc/pki/service-ca/service-ca.crt` | CA bundle path for Model Catalog TLS |
| `DB_ADMIN_PASSWORD` | env | (empty) | Admin password for DB management endpoints |
| `PLANNER_DETECT_CLUSTER_GPUS` | env | `true` | Enable/disable cluster GPU detection |
| `PLANNER_CLUSTER_GPU_CACHE_TTL` | env | `300` | GPU detection cache TTL in seconds |
| `PLANNER_DEBUG` | env | `false` | Enable debug logging |

### Streamlit UI

**Dockerfile**: `ui/Dockerfile`
**Intent**: Frontend — Streamlit-based web interface providing chat-driven interaction, recommendation display, capacity analysis pages, GPU recommender pages, and deployment management. Deployed alongside the backend.
**Image**: `quay.io/llm-d-planner/llm-d-planner-ui:latest`
**Language**: Python
**Port**: 8501/TCP

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Backend API | HTTP | 8000/TCP | None | All API calls proxied through backend |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `API_BASE_URL` | env | `http://backend:8000` | Backend API base URL |
| `STREAMLIT_SERVER_PORT` | env | `8501` | Streamlit listen port |
| `STREAMLIT_SERVER_ADDRESS` | env | `0.0.0.0` | Streamlit bind address |

### vLLM Simulator

**Dockerfile**: `simulator/Dockerfile`
**Intent**: Utility image — GPU-free mock vLLM server providing OpenAI-compatible `/v1/completions` and `/v1/chat/completions` endpoints with realistic latency simulation from benchmark data. Deployed only in development/testing mode (docker-compose `simulator` profile).
**Image**: (built locally)
**Language**: Python
**Port**: 8080/TCP

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `MODEL_NAME` | env | `meta-llama/Llama-3.1-8B-Instruct` | Simulated model name |
| `TENSOR_PARALLEL_SIZE` | env | `1` | Simulated TP configuration |
| `GPU_TYPE` | env | `H100` | Simulated GPU type |
| `PORT` | env | `8080` | Listen port |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_No CRDs defined — this is a service, not an operator._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/health` | GET | 8000/TCP | HTTP | None | None | Backend health check |
| `/api/v1/*` | GET/POST/PUT/DELETE | 8000/TCP | HTTP | None (TLS at Route) | HMAC (admin endpoints only) | Backend REST API (28 endpoints) |
| `/_stcore/health` | GET | 8501/TCP | HTTP | None (TLS at Route) | None | Streamlit UI health check |
| `/v1/completions` | POST | 8080/TCP | HTTP | None | None | Simulator vLLM-compatible API |
| `/v1/chat/completions` | POST | 8080/TCP | HTTP | None | None | Simulator vLLM-compatible chat API |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

_No gRPC services._

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| Python | >=3.11 | Yes | Runtime language |
| FastAPI | >=0.139.2 | Yes | Backend web framework |
| uvicorn | 0.51.0 | Yes | ASGI server |
| Pydantic | 2.13.4 | Yes | Data validation and schemas |
| PostgreSQL | 16 | Yes | Benchmark data storage |
| Ollama | latest | Yes (default LLM) | Local LLM inference for intent extraction |
| llm-optimizer (BentoML) | git HEAD | Yes | GPU performance estimation via roofline model |
| huggingface_hub | >=1.23.0 | Yes | Model metadata and safetensors API |
| transformers | >=5.14.1 | Yes | Model config parsing (AutoConfig) |
| Jinja2 | 3.1.6 | Yes | Deployment YAML template rendering |
| psycopg2-binary | 2.9.12 | Yes | PostgreSQL client |
| pandas | 3.0.3 | Yes | Data manipulation |
| requests | 2.34.2 | Yes | HTTP client |
| Streamlit | 1.60.0 | Yes (UI) | Frontend web framework |
| kubernetes (Python) | >=36.0.2 | No (optional) | Cluster GPU detection |
| anthropic (Vertex) | >=0.116.0 | No (optional) | Vertex AI LLM provider |
| openai | >=2.46.0 | No (optional) | OpenAI-compatible LLM provider |
| kubectl | any | No (runtime) | Kubernetes cluster deployment |
| KServe | v0.14.0 | No (target cluster) | InferenceService CRD for model serving |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| RHOAI Model Catalog | REST API (HTTPS) | Benchmark data sourcing when `PLANNER_BENCHMARK_SOURCE=model_catalog` |
| KServe (on target cluster) | CRD (InferenceService) | Deployment target for generated model serving configurations |
| vLLM (on target cluster) | Container image | Runtime for deployed LLM inference services |
| llm-d stack (on target cluster) | Kustomize + Helm | Alternative deployment target using llm-d base manifests |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| backend | ClusterIP | 8000/TCP | 8000 | HTTP | None (TLS at Route) | None | Internal (exposed via Route) |
| ui | ClusterIP | 8501/TCP | 8501 | HTTP | None (TLS at Route) | None | Internal (exposed via Route) |
| postgres | ClusterIP | 5432/TCP | 5432 | PostgreSQL wire protocol | None | Password | Internal |
| ollama | ClusterIP | 11434/TCP | 11434 | HTTP | None | None | Internal |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|
| planner | Route (OpenShift) | auto-generated | 443/TCP | HTTPS | TLS (edge termination) | Edge | External |
| planner-backend | Route (OpenShift) | auto-generated | 443/TCP | HTTPS | TLS (edge termination) | Edge | External |

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| HuggingFace Hub API (huggingface.co) | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (HF_TOKEN, optional) | Model config, safetensors metadata |
| Ollama server | 11434/TCP | HTTP | None | None | LLM inference for intent extraction |
| Vertex AI API (googleapis.com) | 443/TCP | HTTPS | TLS 1.2+ | GCP OAuth2 credentials | LLM inference (optional provider) |
| OpenAI-compatible API | 443/TCP | HTTPS | TLS 1.2+ | API key (Bearer) | LLM inference (optional provider) |
| RHOAI Model Catalog | 8443/TCP | HTTPS | TLS 1.2+ (custom CA) | Bearer Token / SA Token | Benchmark data sourcing (optional) |
| Kubernetes API server | 6443/TCP | HTTPS | TLS 1.2+ | kubeconfig / in-cluster SA | Cluster management, GPU detection |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| planner-gpu-reader | `""` (core) | nodes | list |

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| planner-gpu-reader | cluster-wide (ClusterRoleBinding) | planner-gpu-reader | planner-backend (planner namespace) |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| planner-secrets | Opaque | Database password, HF token, Model Catalog token, Vertex AI credentials, OpenAI API key, DB admin password | Manual | No |
| planner-service-ca | ConfigMap | OpenShift service-serving CA bundle for Model Catalog TLS | OpenShift service-ca operator (optional) | Yes |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| `/api/v1/db/upload-benchmarks` | POST | HMAC password (X-Admin-Password header) | Application (FastAPI) | Optional — only enforced when DB_ADMIN_PASSWORD env is set |
| `/api/v1/db/reset` | POST | HMAC password (X-Admin-Password header) | Application (FastAPI) | Optional — only enforced when DB_ADMIN_PASSWORD env is set |
| `/api/v1/db/verify-admin` | POST | HMAC password (X-Admin-Password header) | Application (FastAPI) | Password verification endpoint |
| All other endpoints | ALL | None | -- | No authentication — rely on network isolation (namespace, NetworkPolicy) |
| Backend → Model Catalog | HTTPS | Bearer Token / ServiceAccount Token | Application (httpx client) | Auto-reads SA token from `/var/run/secrets/kubernetes.io/serviceaccount/token` |
| Backend → HuggingFace | HTTPS | Bearer Token (optional) | Application (huggingface_hub) | HF_TOKEN env var, only needed for gated models |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A (Python — no compiled Go binary) | `Dockerfile` |
| **Linking** | N/A (Python interpreted) | `Dockerfile` |
| **OpenSSL in image** | Yes — `python:3.14-slim` base includes OpenSSL via Debian | `Dockerfile:2` |
| **OLM FIPS annotation** | Not applicable (not an OLM-managed operator) | -- |

_No Konflux Dockerfiles exist for this component. The standard Dockerfiles use `python:3.14-slim` (Debian-based), not UBI. For RHOAI product shipping, Konflux Dockerfiles with UBI base images would need to be created._

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | Relies on Python `requests` and `httpx` libraries with system defaults; custom CA bundle support via `MODEL_CATALOG_CA_BUNDLE` env | `src/planner/knowledge_base/model_catalog_client.py:49-56` |
| **Crypto libraries** | `requests`, `httpx` (both use system OpenSSL), `psycopg2-binary` (uses libpq/OpenSSL) | `pyproject.toml:7-27` |
| **Certificate handling** | System trust store by default; optional custom CA bundle via `MODEL_CATALOG_CA_BUNDLE` for Model Catalog TLS; OpenShift service-CA ConfigMap mounted at `/etc/pki/service-ca` | `deploy/kubernetes/backend.yaml:119-121`, `deploy/kubernetes/configmap.yaml:22` |
| **Non-FIPS crypto risks** | `hmac.compare_digest` used for admin password comparison (stdlib, FIPS-safe); no MD5/RC4/custom crypto detected | `src/planner/api/routes/database.py:10,37` |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A |
| **Language deps** | uv.lock | No (not committed) | uv | N/A — `pyproject.toml` specifies deps but no lock file committed |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | not present | No | Hermeto (formerly cachi2) | N/A |

_No lock files are committed on this branch (main). The `Dockerfile` uses `uv sync --frozen` which implies a `uv.lock` should exist at build time, but it is gitignored or ephemeral. No Konflux build pipeline is configured — this component has not yet been onboarded to Konflux for hermetic builds. Build hermeticity is a gap for RHOAI product shipping._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-namespace (Kubernetes deployment isolation) | `deploy/kubernetes/namespace.yaml`, `deploy/kubernetes/backend.yaml:2` |
| **Deployment model** | Single-tenant (one Planner instance per namespace) | `deploy/kubernetes/` manifests |
| **Tenant identifier** | Namespace name (`planner` by default) | `deploy/kubernetes/namespace.yaml` |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | Optional admin password for DB management; no auth on other endpoints | Application (HMAC) | Most API endpoints have no authentication — relies entirely on network isolation |
| Data storage | Dedicated PostgreSQL instance per deployment | Kubernetes (PVC) | No row-level or tenant-level data isolation within the database |
| Network traffic | NetworkPolicy for Model Catalog cross-namespace access; no intra-namespace policies | Kubernetes NetworkPolicy | No NetworkPolicy restricting access to backend/UI within the namespace |
| Compute & resources | Resource requests/limits on all containers | Kubernetes | No ResourceQuota at namespace level in manifests |
| Configuration & secrets | Per-namespace Secrets and ConfigMaps | Kubernetes | Secrets contain plaintext default values in manifest templates |
| API scoping | Namespace-scoped Kubernetes operations (InferenceService, pods) | Kubernetes API + kubectl | Backend CORS allows all origins (`allow_origins=["*"]`) — should be restricted in production |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| PostgreSQL | Single database per Planner instance | Dedicated PVC, password-protected connection |
| Ollama | Single instance per Planner deployment | No isolation — shared model inference |

## Data Flows

### Flow 1: Recommendation Workflow

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | User (browser) | Streamlit UI | 8501/TCP | HTTPS (via Route) | TLS (edge) | None |
| 2 | Streamlit UI | Backend API | 8000/TCP | HTTP | None (internal) | None |
| 3 | Backend API | Ollama | 11434/TCP | HTTP | None (internal) | None |
| 4 | Backend API | PostgreSQL | 5432/TCP | PostgreSQL | None (internal) | Password |
| 5 | Backend API | HuggingFace Hub | 443/TCP | HTTPS | TLS 1.2+ | Bearer (optional) |
| 6 | Backend API (response) | Streamlit UI | 8000/TCP | HTTP | None (internal) | None |
| 7 | Streamlit UI (response) | User (browser) | 8501/TCP | HTTPS (via Route) | TLS (edge) | None |

### Flow 2: Kubernetes Deployment

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | User (browser) | Streamlit UI | 8501/TCP | HTTPS (via Route) | TLS (edge) | None |
| 2 | Streamlit UI | Backend `/api/v1/deploy-to-cluster` | 8000/TCP | HTTP | None (internal) | None |
| 3 | Backend API | Kubernetes API | 6443/TCP | HTTPS | TLS 1.2+ | kubeconfig / SA token |
| 4 | Backend API (kubectl apply) | Kubernetes API | 6443/TCP | HTTPS | TLS 1.2+ | kubeconfig / SA token |

### Flow 3: Model Catalog Sync (RHOAI)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Backend (background thread) | RHOAI Model Catalog | 8443/TCP | HTTPS | TLS 1.2+ (custom CA) | Bearer / SA token |
| 2 | Backend (background thread) | PostgreSQL | 5432/TCP | PostgreSQL | None (internal) | Password |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| PostgreSQL | Database (libpq) | 5432/TCP | PostgreSQL wire | None (in-cluster) | Benchmark data storage, query, and management |
| Ollama | REST API | 11434/TCP | HTTP | None (in-cluster) | Default LLM provider for intent extraction |
| HuggingFace Hub | REST API | 443/TCP | HTTPS | TLS 1.2+ | Model config, safetensors metadata, parameter counts |
| RHOAI Model Catalog | REST API | 8443/TCP | HTTPS | TLS 1.2+ (custom CA) | Benchmark data sourcing (model_catalog mode) |
| Kubernetes API | REST API (kubectl) | 6443/TCP | HTTPS | TLS 1.2+ | InferenceService lifecycle, GPU node label detection |
| KServe (on target cluster) | CRD (InferenceService) | -- | -- | -- | Target for generated deployment YAML |
| vLLM (on target cluster) | Container runtime | 8000/TCP | HTTP | None | Runtime for deployed InferenceService pods |
| llm-d stack | Kustomize + Helm | -- | -- | -- | Alternative deployment target (kustomize overlay + Helm values) |
| BentoML llm-optimizer | Python library (in-process) | -- | -- | -- | Roofline performance estimation for GPU recommendation |
| Vertex AI | REST API | 443/TCP | HTTPS | TLS 1.2+ | Optional LLM provider for intent extraction |
| OpenAI-compatible endpoint | REST API | 443/TCP | HTTPS | TLS 1.2+ | Optional LLM provider for intent extraction |
| OpenShift service-CA | ConfigMap mount | -- | -- | -- | CA bundle for Model Catalog TLS verification |

## Architectural Analysis

llm-d Planner follows a clean **three-tier architecture** — Streamlit frontend, FastAPI backend, PostgreSQL database — with the backend acting as the sole integration point for all external services. The UI never communicates directly with Ollama, HuggingFace, or Kubernetes; everything is proxied through the backend API. This simplifies deployment security since only the backend pod needs network egress and RBAC permissions.

The **recommendation engine** is the system's intellectual core. It implements a multi-stage pipeline: (1) LLM-powered intent extraction converts natural language into structured `DeploymentIntent` schemas, (2) a traffic profile generator maps use cases to GuideLLM benchmark configurations with experience-class-driven SLO targets, (3) a `ConfigFinder` queries the benchmark database for exact matches on (model, GPU, traffic profile) tuples and fills gaps with BentoML roofline estimates, (4) a multi-criteria scorer evaluates each configuration on accuracy, price, latency, and complexity, and (5) an `Analyzer` produces five ranked views (balanced, best accuracy, lowest cost, lowest latency, simplest). This design makes the recommendation pipeline extensible — new scoring dimensions or estimation backends can be added without restructuring the workflow.

The **capacity planner** module is notably well-engineered, with accurate memory estimation that accounts for MHA/GQA/MQA/MLA attention types (including DeepSeek-V2/V3 compressed KV), quantization effects (GPTQ, FP8, MXFP4), multi-level parallelism (TP × PP × DP), and calibrated activation memory profiles from 49 empirical vLLM runs across 28 models. The constants are externalized in `vllm_memory_constants.json`, making recalibration possible without code changes.

The **LLM provider abstraction** uses Python's structural typing (`Protocol` class) to define a provider-agnostic interface, with factory-based selection via the `LLM_PROVIDER` environment variable. This allows swapping between Ollama (local, default), Vertex AI (Claude), and OpenAI-compatible endpoints without code changes — a pragmatic design for environments where GPU-based Ollama may not be available.

A significant **security gap** exists: the backend has no authentication on most endpoints and configures CORS to allow all origins (`allow_origins=["*"]`). In production, this relies entirely on OpenShift Route TLS termination and namespace-level NetworkPolicy for protection. The only authenticated endpoints are the database management routes (`/db/upload-benchmarks`, `/db/reset`), which use HMAC password comparison via the `X-Admin-Password` header. The RHOAI Model Catalog integration includes proper TLS handling with custom CA bundles and ServiceAccount token auto-discovery. Additionally, no Konflux Dockerfiles exist — the component uses standard `python:3.14-slim` base images rather than UBI, which is a prerequisite for RHOAI product shipping through Konflux pipelines.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| v0.1.0-73 | 2026-07-21 | Dependency updates (openai, transformers, huggingface-hub, matplotlib, streamlit) |
| v0.1.0-60 | 2026-07-07 | Fix accelerator name mismatch in GPU recommendation (#279) |
| v0.1.0-59 | 2026-07-03 | llm-d deployment support (#274) — added kustomize + Helm generation for llm-d stack |
| v0.1.0 | -- | Initial release — Planner, Capacity Planner, GPU Recommender core features |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| `pyproject.toml` | 1-160 | Metadata, Dependencies, Architecture Components |
| `README.md` | 1-352 | Purpose, Architecture Components, Dependencies |
| `Dockerfile` | 1-61 | Architecture Components, Sub-Component Details, FIPS Compliance, Build Hermeticity |
| `simulator/Dockerfile` | 1-33 | Sub-Component Details (Simulator) |
| `ui/Dockerfile` | 1-32 | Sub-Component Details (UI) |
| `src/planner/api/app.py` | 1-96 | Architecture Components, APIs Exposed |
| `src/planner/api/dependencies.py` | 1-206 | Architecture Components, Data Flows, Integration Points |
| `src/planner/api/routes/health.py` | 1-12 | APIs Exposed |
| `src/planner/api/routes/capacity_planner.py` | 1-187 | APIs Exposed, Sub-Component Details (API Routes) |
| `src/planner/api/routes/gpu_recommender.py` | 1-102 | APIs Exposed, Sub-Component Details (API Routes) |
| `src/planner/api/routes/intent.py` | 1-70 | APIs Exposed, Sub-Component Details (API Routes) |
| `src/planner/api/routes/recommendation.py` | 1-302 | APIs Exposed, Sub-Component Details (API Routes) |
| `src/planner/api/routes/specification.py` | 1-231 | APIs Exposed, Sub-Component Details (API Routes) |
| `src/planner/api/routes/configuration.py` | 1-444 | APIs Exposed, Sub-Component Details (API Routes), Data Flows |
| `src/planner/api/routes/database.py` | 1-183 | APIs Exposed, Security (Auth), Sub-Component Details (API Routes) |
| `src/planner/api/routes/reference_data.py` | 1-140 | APIs Exposed, Sub-Component Details (API Routes) |
| `src/planner/orchestration/workflow.py` | 1-467 | Architecture Components, Data Flows, Architectural Analysis |
| `src/planner/capacity_planner.py` | 1-1466 | Architecture Components, Architectural Analysis |
| `src/planner/gpu_recommender.py` | 1-593 | Architecture Components, Dependencies, Integration Points |
| `src/planner/llm/factory.py` | 1-43 | Architecture Components, Architectural Analysis |
| `src/planner/llm/client.py` | 1-71 | Architecture Components, Architectural Analysis |
| `src/planner/cluster/manager.py` | 1-357 | Architecture Components, Data Flows, Integration Points |
| `src/planner/cluster/gpu_detector.py` | 1-169 | Security (RBAC), Integration Points |
| `src/planner/configuration/generator.py` | 1-288 | Architecture Components, Sub-Component Details |
| `src/planner/configuration/llmd_generator.py` | 1-114 | Architecture Components, Sub-Component Details |
| `src/planner/shared/schemas/recommendation.py` | 1-158 | Architecture Components |
| `src/planner/intent_extraction/extractor.py` | 1-50 | Architecture Components |
| `src/planner/knowledge_base/model_catalog_client.py` | 1-60 | Integration Points, Security (Auth) |
| `scripts/schema.sql` | 1-105 | Dependencies (PostgreSQL schema) |
| `deploy/kubernetes/backend.yaml` | 1-164 | Network Architecture, Security, Deployment Manifests |
| `deploy/kubernetes/postgres.yaml` | 1-91 | Network Architecture, Dependencies |
| `deploy/kubernetes/ui.yaml` | 1-70 | Network Architecture, Sub-Component Details |
| `deploy/kubernetes/ollama.yaml` | 1-104 | Network Architecture, Dependencies |
| `deploy/kubernetes/route.yaml` | 1-36 | Network Architecture (Ingress) |
| `deploy/kubernetes/secrets.yaml` | 1-24 | Security (Secrets) |
| `deploy/kubernetes/gpu-reader-rbac.yaml` | 1-34 | Security (RBAC) |
| `deploy/kubernetes/configmap.yaml` | 1-23 | Sub-Component Details (Configuration) |
| `deploy/kubernetes/networkpolicy-model-catalog.yaml` | 1-30 | Network Architecture, Multi-Tenancy |
| `deploy/kubernetes/db-init-job.yaml` | 1-75 | Data Flows |
| `docker-compose.yml` | 1-166 | Architecture Components, Network Architecture |
| `simulator/requirements.txt` | 1-3 | Sub-Component Details (Simulator) |
| `ui/requirements.txt` | 1-6 | Sub-Component Details (UI) |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `find . -name "*.py" (src/planner)` | 56 Python files | Architecture Components |
| `find deploy -type f` | 14 deployment files | Network Architecture, Security, Deployment Manifests |
| `find . -name "Dockerfile*"` | 3 Dockerfiles | Architecture Components, Sub-Component Details |
| `git describe --tags --always` | -- | Metadata (Version) |
| `git log --oneline --no-merges -20` | 20 commits | Recent Changes |
| `git remote -v` | 1 remote | Provenance |
| `find . -name "*lock*"` | 0 lock files | Security (Build Hermeticity) |
| `find .github/workflows -name "sync*"` | 0 sync workflows | Provenance |

### Summary

- **Total files read**: 42
- **Total lines referenced**: ~7,500
- **Coverage**: All sections have direct source backing. Network Architecture derived from `deploy/kubernetes/` manifests. FIPS and Build Hermeticity sections document absence of Konflux infrastructure. Multi-Tenancy inferred from deployment manifests and application code patterns.

---
*Generated in 4m 56s (296s total)*
