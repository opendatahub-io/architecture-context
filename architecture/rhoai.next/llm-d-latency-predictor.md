# Component: llm-d-latency-predictor

## Metadata

- **Repository**: https://github.com/red-hat-data-services/llm-d-latency-predictor
- **Version**: v0.1.0+rhaiv.0-28-g10bfb8e
- **Distribution**: RHOAI
- **Languages**: Python
- **Deployment Type**: Service (dual-server ML prediction/training system)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/llm-d/llm-d-latency-predictor | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/llm-d-latency-predictor | manual | main | -- | local_analysis |

_The README references the upstream at `github.com/llm-d/llm-d-latency-predictor`. No sync workflows were found in `.github/workflows/`; the downstream appears to be manually synced. Git history shows commits related to "ODH sync" and downstream-specific modifications (Konflux pipelines, AIPCC base image migration, RHDS owner list updates)._

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

_No aliases detected._

## Purpose

**Short**: Latency prediction service for the llm-d ecosystem that trains and serves online ML models predicting TTFT (Time To First Token) and TPOT (Time Per Output Token) for LLM inference requests.

**Detailed**: The llm-d-latency-predictor is a dual-server architecture consisting of a training server and a prediction server. The training server collects request trace data (features like KV cache percentage, input token length, queue depth, prefix cache score, and tokens-in-flight), trains regression models (XGBoost, LightGBM, or Bayesian Ridge), and serves trained models via HTTP download endpoints. The prediction server periodically syncs trained models from the training server and serves low-latency TTFT/TPOT predictions via REST API, supporting both single and bulk (up to 10,000 requests) prediction modes.

The system supports configurable quantile regression (e.g., p90 predictions) or mean regression, a gated ensemble mode that uses separate sub-models for "noqueue" (idle) and "queued" request regimes, and disaggregated inference features via pod_type (prefill/decode) and tokens-in-flight tracking. Models are serialized as joblib files and downloaded over HTTP with checksum-based cache invalidation and atomic file replacement for safe multi-worker operation.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| prediction-server | Python FastAPI Service | Serves TTFT/TPOT latency predictions using periodically synced ML models |
| training-server | Python FastAPI Service | Collects training data, trains regression models, serves model files for download |
| test-harness | Python pytest Job | Integration test suite exercising the dual-server architecture end-to-end |
| common/types | Python Library | Shared data types (ModelType, ObjectiveType, QueueGatedModel, RandomDropDeque) |

## AIPCC Ecosystems Use

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| CPU | Dockerfile.konflux.prediction | `quay.io/aipcc/base-images/cpu:3.5.0@sha256:1459233907bdcfe4fdb4f9a8d39de861bacd868d7a1e5cee083ddd46f93c306d` | CPU | -- | aarch64, ppc64le, s390x, x86_64 | Active |
| CPU | Dockerfile.konflux.training | `quay.io/aipcc/base-images/cpu:3.5.0@sha256:1459233907bdcfe4fdb4f9a8d39de861bacd868d7a1e5cee083ddd46f93c306d` | CPU | -- | aarch64, ppc64le, s390x, x86_64 | Active |
| CPU | Dockerfile.konflux.test | `quay.io/aipcc/base-images/cpu:3.5.0@sha256:1459233907bdcfe4fdb4f9a8d39de861bacd868d7a1e5cee083ddd46f93c306d` | CPU | -- | aarch64, ppc64le, s390x, x86_64 | Active |

_All three Konflux Dockerfiles use the same AIPCC CPU base image (version 3.5.0, digest-pinned). The component does not require GPU/accelerator-specific libraries -- the ML models (XGBoost, LightGBM, scikit-learn) are CPU-only workloads performing regression inference, not deep learning._

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | Yes -- inherited from AIPCC base image; `requirements-konflux.txt` compiled against `https://packages.redhat.com/api/pypi/public-rhai/rhoai/3.5/cpu-ubi9-test/simple/` |
| **pip.conf / uv.toml configured** | Inherited from base image |
| **Additional packages installed** | fastapi, uvicorn, xgboost, lightgbm, scikit-learn, pandas, numpy, joblib, pydantic, requests, aiohttp, orjson, river, scipy (via `requirements-konflux.txt`, ~40 packages with hashes) |
| **Source** | `Dockerfile.konflux.prediction:3-4`, `Dockerfile.konflux.training:3-4`, `Dockerfile.konflux.test:7-8` |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|
| `/usr/libexec/rhaipcc/dnf` | Not used | -- |
| `/etc/rhaipcc/env` | Not used | -- |

_The Dockerfiles do not invoke AIPCC tooling scripts. The test Dockerfile installs `jq` via standard `dnf` rather than `/usr/libexec/rhaipcc/dnf`._

## Sub-Component Details

### prediction-server

**Dockerfile**: `Dockerfile.konflux.prediction`
**Intent**: Primary service that serves latency predictions. Deployed as a Deployment with 10 replicas (default) for high-throughput, low-latency prediction serving. Each worker periodically downloads trained models from the training server via HTTP.
**Image**: `odh-latency-predictor-prediction`
**Language**: Python (FastAPI + uvicorn)
**Port**: 8001/TCP

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|
| `/predict` | POST | -- | None | Single TTFT/TPOT prediction |
| `/predict/bulk/strict` | POST | -- | None | Bulk predictions (up to 10,000), strict mode (all fail on error) |
| `/predict/bulk` | POST | -- | None | Bulk predictions with per-request error handling |
| `/status` | GET | -- | None | Server readiness, model type, ensemble status |
| `/reload` | POST | training-service | None | Trigger manual model sync from training server |
| `/healthz` | GET | -- | None | Liveness probe |
| `/readyz` | GET | -- | None | Readiness probe (returns 503 until models loaded) |
| `/` | GET | -- | None | Root info endpoint |

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| training-service | HTTP | 8000/TCP | None | Download trained model files (joblib) and check model timestamps |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `TRAINING_SERVER_URL` | env | `http://training-service:8000` | URL of the training server for model downloads |
| `MODEL_SYNC_INTERVAL_SEC` | env | `10` | Interval (seconds) between model sync attempts |
| `LATENCY_MODEL_TYPE` | env | `xgboost` | ML model type: `xgboost`, `lightgbm`, or `bayesian_ridge` |
| `LATENCY_QUANTILE_ALPHA` | env | `0.9` | Quantile for quantile regression (e.g., 0.9 = p90) |
| `LATENCY_OBJECTIVE_TYPE` | env | `quantile` | Objective type: `quantile` or `mean` |
| `LATENCY_ENSEMBLE_MODE` | env | `true` | Enable gated ensemble (separate noqueue/queued models) |
| `LATENCY_ENABLE_TOKEN_IN_FLIGHT_FEATURES` | env | `true` | Include prefill/decode tokens-in-flight as features |
| `UVICORN_WORKERS` | env | `8` | Number of uvicorn worker processes |
| `PREDICT_HOST` | env | `0.0.0.0` | Bind address |
| `PREDICT_PORT` | env | `8001` | Listen port |
| `HTTP_TIMEOUT` | env | `30` | HTTP timeout for training server requests (seconds) |
| `DOWNLOAD_RETRIES` | env | `3` | Number of retries for model downloads |

### training-server

**Dockerfile**: `Dockerfile.konflux.training`
**Intent**: Stateful server that collects training data, periodically retrains regression models, and serves trained model files for download. Deployed as a single-replica Deployment with PersistentVolumeClaim for model storage.
**Image**: `odh-latency-predictor-training`
**Language**: Python (FastAPI + uvicorn)
**Port**: 8000/TCP

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|
| `/add_training_data_bulk` | POST | -- | None | Ingest training samples (actual TTFT/TPOT with features) |
| `/predict` | POST | -- | None | Direct prediction from training server models |
| `/model/{model_name}/info` | GET | -- | None | Model metadata (size, last modified) |
| `/model/{model_name}/download` | GET | -- | None | Download serialized model file (joblib) |
| `/models/list` | GET | -- | None | List all available models with metadata |
| `/model/download/info` | GET | -- | None | General model download info |
| `/model/ttft/xgb/json` | GET | -- | None | XGBoost TTFT tree structure (JSON) |
| `/model/tpot/xgb/json` | GET | -- | None | XGBoost TPOT tree structure (JSON) |
| `/model/ttft/lgb/txt` | GET | -- | None | LightGBM TTFT model (text format) |
| `/model/tpot/lgb/txt` | GET | -- | None | LightGBM TPOT model (text format) |
| `/model/ttft/lgb/importances` | GET | -- | None | LightGBM TTFT feature importances |
| `/model/tpot/lgb/importances` | GET | -- | None | LightGBM TPOT feature importances |
| `/metrics` | GET | -- | None | Prometheus-style metrics (model type, coefficients, training samples) |
| `/data/status` | GET | -- | None | Training/test data counts and ensemble status |
| `/flush` | POST | -- | None | Flush training data, test data, and/or metrics |
| `/debug/prefix_distribution` | GET | -- | None | Debug endpoint for prefix cache bucket distribution |
| `/healthz` | GET | -- | None | Liveness probe |
| `/readyz` | GET | -- | None | Readiness probe |
| `/` | GET | -- | None | Root info endpoint |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `LATENCY_RETRAINING_INTERVAL_SEC` | env | `1800` | Interval between retraining cycles (seconds) |
| `LATENCY_MIN_SAMPLES_FOR_RETRAIN` | env | `1000` | Minimum samples required for retraining |
| `LATENCY_MIN_SAMPLES_FOR_RETRAIN_FRESH` | env | `10` | Minimum samples for fresh model creation |
| `LATENCY_MAX_TRAINING_DATA_SIZE_PER_BUCKET` | env | `500` | Max samples per (queue, cache, prefix) bucket |
| `LATENCY_TEST_TRAIN_RATIO` | env | `0.1` | Fraction of incoming data held for test evaluation |
| `LATENCY_MAX_TEST_DATA_SIZE` | env | `1000` | Max test samples to keep |
| `LATENCY_MODEL_TYPE` | env | `xgboost` | ML model type |
| `LATENCY_QUANTILE_ALPHA` | env | `0.9` | Target quantile |
| `LATENCY_OBJECTIVE_TYPE` | env | `quantile` | Objective: `quantile` or `mean` |
| `LATENCY_ENSEMBLE_MODE` | env | `true` | Enable gated ensemble training |
| `LATENCY_MIN_SAMPLES_FOR_ENSEMBLE_SPLIT` | env | `200` | Min samples per queue regime for ensemble split |
| `LATENCY_SAMPLE_WEIGHTING_FOR_PREFIX_CACHE` | env | `false` | Enable sample weighting by prefix cache bucket |
| `LATENCY_TPOT_ZERO_TOKEN_COUNT` | env | `true` | Zero out num_tokens_generated for TPOT training |
| `LATENCY_ENABLE_TOKEN_IN_FLIGHT_FEATURES` | env | `true` | Include tokens-in-flight features |

### test-harness

**Dockerfile**: `Dockerfile.konflux.test`
**Intent**: Integration test image that exercises the dual-server architecture. Deployed as a Kubernetes Job that runs pytest against both the prediction and training servers, validating health checks, model training, model sync, single/bulk predictions, ensemble routing, and quantile regression calibration.
**Image**: `odh-latency-predictor-test`
**Language**: Python (pytest)
**Port**: N/A (client-only)

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `PREDICTION_SERVER_URL` | env | `http://<PREDICTION_IP>:80` | Prediction server URL |
| `TRAINING_SERVER_URL` | env | `http://<TRAINING_IP>:8080` | Training server URL |

## APIs Exposed

### Custom Resource Definitions (CRDs)

_No CRDs. This is a stateless service component._

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/predict` | POST | 8001/TCP | HTTP | None | None | Single TTFT/TPOT prediction |
| `/predict/bulk/strict` | POST | 8001/TCP | HTTP | None | None | Batch prediction (strict, up to 10k) |
| `/predict/bulk` | POST | 8001/TCP | HTTP | None | None | Batch prediction (lenient error handling) |
| `/status` | GET | 8001/TCP | HTTP | None | None | Prediction server status |
| `/reload` | POST | 8001/TCP | HTTP | None | None | Trigger model sync |
| `/healthz` | GET | 8001/TCP | HTTP | None | None | Liveness |
| `/readyz` | GET | 8001/TCP | HTTP | None | None | Readiness |
| `/add_training_data_bulk` | POST | 8000/TCP | HTTP | None | None | Ingest training samples |
| `/model/{name}/download` | GET | 8000/TCP | HTTP | None | None | Download trained model |
| `/model/{name}/info` | GET | 8000/TCP | HTTP | None | None | Model metadata |
| `/models/list` | GET | 8000/TCP | HTTP | None | None | List models |
| `/metrics` | GET | 8000/TCP | HTTP | None | None | Prometheus metrics |
| `/flush` | POST | 8000/TCP | HTTP | None | None | Flush training data |
| `/data/status` | GET | 8000/TCP | HTTP | None | None | Data status |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

_No gRPC services._

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| XGBoost | 3.3.0 | No (fallback to Bayesian Ridge) | Quantile/mean regression model for TTFT/TPOT prediction |
| LightGBM | 4.6.0 | No (fallback to Bayesian Ridge) | Alternative tree-based regression model |
| scikit-learn | 1.9.0 | Yes | BayesianRidge regression model, StandardScaler preprocessing |
| FastAPI | 0.138.1 | Yes | HTTP API framework |
| uvicorn | 0.49.0 | Yes | ASGI server |
| pandas | 3.0.3 | Yes | Feature engineering and DataFrame operations |
| numpy | 2.5.0 | Yes | Numerical computation |
| joblib | 1.5.3 | Yes | Model serialization/deserialization |
| pydantic | 2.13.4 | Yes | Request/response validation |
| scipy | 1.18.0 | Yes | Statistical functions (norm.ppf for quantile calculations) |
| orjson | 3.11.9 | No (optional) | Fast JSON serialization for bulk prediction responses |
| requests | 2.34.2 | Yes | HTTP client for model downloads (prediction server) |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| llm-d (inference engine) | Data source | Provides actual TTFT/TPOT latency observations used as training data |

## Deployment Manifests

### Kustomize Structure

| Base / Overlay | Path | Purpose |
|----------------|------|---------|
| base | `deploy/base/` | Root kustomization composing prediction, training, and test resources |
| base | `deploy/base/prediction/` | Prediction server Deployment, Service, ConfigMap |
| base | `deploy/base/training/` | Training server Deployment, Service, ConfigMap, PVC, StorageClass |
| base | `deploy/base/test/` | Integration test Job |

### Parameterization

| Parameter | Source | Default | Purpose |
|-----------|--------|---------|---------|
| `namespace` | kustomization.yaml | `default` | Target namespace for deployment |
| `TRAINING_SERVER_URL` | configmap (prediction) | `http://training-service:8000` | Training server URL for model sync |
| `MODEL_SYNC_INTERVAL_SEC` | configmap (prediction) | `10` | Model sync interval |
| `LATENCY_MODEL_TYPE` | configmap (both) | `xgboost` | ML model type |
| `LATENCY_RETRAINING_INTERVAL_SEC` | configmap (training) | `1` | Retraining interval |
| `LATENCY_MIN_SAMPLES_FOR_RETRAIN` | configmap (training) | `100` | Min samples for retraining |

### Distribution Variants

| Variant | Path | Differences |
|---------|------|-------------|
| base | `deploy/base/` | Only base manifests present; no ODH/RHOAI overlays found |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| prediction-service | LoadBalancer | 80/TCP | 8001 | HTTP | None | None | External |
| training-service | ClusterIP | 8000/TCP | 8000 | HTTP | None | None | Internal |
| training-service-external | LoadBalancer | 8080/TCP | 8000 | HTTP | None | None | External (optional) |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

_No ingress resources defined. Services are exposed via LoadBalancer type (designed for GKE; in RHOAI deployments, the platform operator would configure appropriate ingress)._

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| training-service | 8000/TCP | HTTP | None | None | Prediction server downloads models from training server |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_No RBAC resources defined. The component runs as a standard Deployment with no Kubernetes API interactions._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|

_No secrets used. Communication between prediction and training servers is unauthenticated HTTP._

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| All prediction endpoints | GET, POST | None | None | Open access |
| All training endpoints | GET, POST | None | None | Open access |

_**Security gap**: Neither server implements authentication or authorization. All endpoints are openly accessible. In a production RHOAI deployment, the platform operator (rhods-operator) would be expected to place these services behind appropriate network policies, kube-rbac-proxy sidecars, or service mesh mTLS._

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A (Python, not Go) | `Dockerfile.konflux.*` |
| **Linking** | N/A (Python, not compiled) | -- |
| **OpenSSL in image** | Yes (via AIPCC base image, RHEL 9.6) | `Dockerfile.konflux.prediction:1-2` |
| **OLM FIPS annotation** | Not present (no OLM bundle) | -- |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No TLS configured; HTTP only between services | `prediction/prediction_server.py:109-111`, `training/training_server.py` |
| **Crypto libraries** | hashlib.md5 used for model file checksum comparison (not cryptographic use) | `prediction/prediction_server.py:115-122` |
| **Certificate handling** | None; no HTTPS configured | -- |
| **Non-FIPS crypto risks** | MD5 used for file checksums (non-security-critical); no TLS between services | `prediction/prediction_server.py:117` |

_The component inherits OpenSSL from the AIPCC base image (RHEL 9.6), satisfying the check-payload gate. At the application level, it uses HTTP exclusively with no TLS. MD5 is used solely for file integrity checks (detecting model file changes), not for cryptographic purposes. The lack of TLS between prediction and training servers is a security consideration for production deployments._

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | `rpms.lock.yaml` | Yes | rpm-lockfile-prototype | `rpms.lock.yaml` |
| **Language deps** | `requirements-konflux.txt` (hashed, compiled) | Yes | uv pip compile | `requirements-konflux.txt` |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | Not present | No | Hermeto (formerly cachi2) | N/A |

_The branch has RPM lock files (`rpms.lock.yaml` for jq/oniguruma packages, scoped to aarch64 and x86_64) and hashed Python requirements (`requirements-konflux.txt` compiled via `uv pip compile` with `--generate-hashes`). No Hermeto artifact locks or cachi2 prefetch integration found. The `rpms.in.yaml` specifies the Containerfile context as `Dockerfile.konflux.test` (the only Dockerfile that installs RPMs via dnf)._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | N/A | No tenant concept; single shared instance |
| **Deployment model** | Single-tenant | `deploy/base/prediction/deployment.yaml`, `deploy/base/training/deployment.yaml` |
| **Tenant identifier** | N/A | No tenant isolation |

_The latency predictor is a shared infrastructure service. A single training server instance serves all models, and all prediction server replicas serve the same models. There is no per-tenant or per-namespace isolation of training data or models._

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | None | -- | No authentication; all endpoints open |
| Data storage | Single shared PVC for training models | Kubernetes | All training data mixed; no per-tenant partitioning |
| Network traffic | None | -- | No NetworkPolicy; services accept connections from any pod |
| Compute & resources | Resource requests/limits per pod | Kubernetes | Shared CPU/memory across all callers |
| Configuration & secrets | Shared ConfigMap | Kubernetes | Single configuration for all tenants |
| API scoping | None | -- | All API endpoints are global; no namespace or tenant filtering |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| Training data store (in-memory deques) | None | All incoming training data is mixed into shared buckets |
| Model storage (PVC) | None | Single PVC holds all trained models |

## Data Flows

### Flow 1: Training Data Ingestion and Model Training

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | llm-d inference engine (external) | training-service `/add_training_data_bulk` | 8000/TCP | HTTP | None | None |
| 2 | training-server (internal) | PVC `/models/*.joblib` | N/A | filesystem | N/A | N/A |
| 3 | training-server (retrain loop) | PVC `/models/*.joblib` | N/A | filesystem | N/A | N/A |

### Flow 2: Model Sync and Prediction Serving

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | prediction-server (sync thread) | training-service `/model/{name}/info` | 8000/TCP | HTTP | None | None |
| 2 | prediction-server (sync thread) | training-service `/model/{name}/download` | 8000/TCP | HTTP | None | None |
| 3 | prediction-server (local) | emptyDir `/local_models/*.joblib` | N/A | filesystem | N/A | N/A |
| 4 | Client (external) | prediction-service `/predict` | 80/TCP | HTTP | None | None |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| llm-d inference engine | REST (data source) | -- | HTTP | None | Provides actual TTFT/TPOT latency observations as training data via `/add_training_data_bulk` |
| llm-d routing layer | REST (consumer) | 80/TCP | HTTP | None | Consumes TTFT/TPOT predictions via `/predict` or `/predict/bulk/strict` to inform request routing decisions |
| training-service (self) | REST (model download) | 8000/TCP | HTTP | None | Prediction server downloads trained models from training server |

## Architectural Analysis

The llm-d-latency-predictor follows a clean **dual-server architecture** separating training (stateful, single-replica) from prediction (stateless, horizontally scalable). This separation is a strong design choice: the training server accumulates data and performs expensive model fitting on a configurable interval (default 30 minutes), while prediction servers can scale to 10+ replicas to handle high request volumes. Each prediction worker independently syncs models via HTTP, using checksum-based cache invalidation to avoid redundant reloads -- only one worker "wins" the download race per cycle, but all workers reload from the local filesystem copy.

The **gated ensemble model** (`QueueGatedModel`) is a notable architectural pattern. Rather than training a single model for all traffic conditions, the system splits training data by queue regime (num_request_waiting == 0 vs > 0) and trains separate sub-models for each. At prediction time, the request's queue depth determines which sub-model is used. This improves accuracy because the relationship between features and latency fundamentally differs between idle and queued states -- an insight that a single model must learn implicitly but the ensemble handles explicitly.

The **bucketed sampling strategy** for training data is designed to prevent distribution skew. Training samples are stored in a 3-dimensional bucket grid (queue bucket × cache bucket × prefix bucket = 5×20×4 = 400 buckets), each with a configurable max size (default 500). This ensures the model sees representative samples from the full feature space rather than being dominated by the most common operating conditions. The `RandomDropDeque` data structure implements reservoir-like sampling by randomly evicting existing items when a bucket is full.

A significant **security gap** exists: neither server implements any form of authentication or authorization, and all inter-service communication uses plaintext HTTP. The model download flow between prediction and training servers lacks integrity verification beyond the joblib deserialization check. In production RHOAI deployments, the platform operator would need to layer network policies, mTLS, or kube-rbac-proxy to secure these services. The current manifests include GKE-specific nodeSelector and StorageClass configurations, indicating the base manifests were designed for a GKE development environment rather than production OpenShift.

The **bulk prediction fast path** (`predict_batch_fast`) demonstrates thoughtful performance optimization: it extracts features as numpy arrays via `np.fromiter` with pre-allocated counts, builds DataFrames from dict-of-arrays (avoiding Pandas' slow per-row type inference), and returns results via `ORJSONResponse` to bypass Pydantic serialization overhead. This path handles up to 10,000 predictions per request.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 10bfb8e | Recent | Update Dockerfile.konflux COPY paths to match new directory structure |
| 720d12e | Recent | Restructuring source directories by component (#16) |
| f91b254 | Recent | Switch to AIPCC base images and RHAI GA index for Konflux builds |
| aab478e | Recent | Refactor: move manifests into overlays (#36) |
| 6602068 | Recent | Change pull pipelines to hermetic true |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| `README.md` | 1-100 | Metadata, Purpose, Architecture Components |
| `pyproject.toml` | 1-56 | Metadata, Dependencies, Languages |
| `Makefile` | 1-108 | Architecture Components, Deployment |
| `VERSION` | 1 | Metadata |
| `Dockerfile.konflux.prediction` | 1-28 | AIPCC Ecosystems Use, Sub-Component Details, Security |
| `Dockerfile.konflux.training` | 1-17 | AIPCC Ecosystems Use, Sub-Component Details |
| `Dockerfile.konflux.test` | 1-26 | AIPCC Ecosystems Use, Sub-Component Details |
| `common/types.py` | 1-64 | Architecture Components, Purpose |
| `common/__init__.py` | 1-3 | Metadata |
| `prediction/prediction_server.py` | 1-1181 | APIs Exposed, Sub-Component Details, Network Architecture, Security, Data Flows, Integration Points, Architectural Analysis |
| `training/training_server.py` | 1-999 | APIs Exposed, Sub-Component Details, Network Architecture, Data Flows, Architectural Analysis |
| `tests/test_dual_server_client.py` | 1-2099 | Sub-Component Details, Integration Points |
| `requirements-konflux.txt` | 1-174 | AIPCC Ecosystems Use, Dependencies |
| `deploy/base/kustomization.yaml` | 1-21 | Deployment Manifests |
| `deploy/base/prediction/deployment.yaml` | 1-67 | Network Architecture, Sub-Component Details, Multi-Tenancy |
| `deploy/base/prediction/service.yaml` | 1-18 | Network Architecture |
| `deploy/base/prediction/configmap.yaml` | 1-19 | Sub-Component Details, Configuration |
| `deploy/base/training/deployment.yaml` | 1-66 | Network Architecture, Sub-Component Details, Multi-Tenancy |
| `deploy/base/training/service.yaml` | 1-34 | Network Architecture |
| `deploy/base/training/configmap.yaml` | 1-14 | Sub-Component Details, Configuration |
| `deploy/base/training/pvc.yaml` | 1-14 | Data Flows, Multi-Tenancy |
| `deploy/base/test/job.yaml` | 1-54 | Sub-Component Details |
| `rpms.in.yaml` | 1-13 | Security (Build Hermeticity) |
| `rpms.lock.yaml` | 1-41 | Security (Build Hermeticity) |
| `OWNERS` | 1-14 | Metadata |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `Dockerfile*konflux*` (find) | 3 files | AIPCC Ecosystems Use, Architecture Components |
| `*.py` (find) | 8 files | Architecture Components |
| `kustomization.yaml` (find) | 4 files | Deployment Manifests |
| `@app.(get\|post\|put\|delete\|patch)` (grep) | `training/training_server.py` (20 matches) | APIs Exposed, Sub-Component Details |
| `sync*.yaml` (find in .github/workflows) | 0 files | Provenance |
| `upstream\|fork\|sync` (grep in .github/workflows) | 1 match (non-sync) | Provenance |
| `rpms.lock.yaml` (find) | 1 file | Security (Build Hermeticity) |

### Summary

- **Total files read**: 25
- **Total lines referenced**: ~4,800
- **Coverage**: All sections have direct source backing. Provenance lineage is inferred from README upstream URL, git remote, and commit history patterns (no provenance data or sync workflows found). Multi-tenancy assessment is inferred from deployment architecture (single shared instance, no tenant identifiers in code).

---
*Generated in 4m 29s (269s total)*
