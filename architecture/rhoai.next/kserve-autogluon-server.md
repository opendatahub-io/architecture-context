# Component: KServe AutoGluon Server

## Metadata

- **Repository**: https://github.com/red-hat-data-services/kserve-autogluon-server
- **Version**: 0.19.0
- **Distribution**: RHOAI
- **Languages**: Python
- **Deployment Type**: ML Inference Server (KServe runtime)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/kserve/kserve | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/kserve-autogluon-server | manual | main | -- | local_analysis |

_This repository is a downstream fork of the upstream KServe monorepo. The upstream repo contains the full KServe project (Go operator, Python serving runtimes, SDK). This downstream checkout extracts and ships only the AutoGluon server component as a standalone Konflux-built container image. No automated sync workflows were detected; synchronization appears to be manual._

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: KServe model serving runtime for AutoGluon TabularPredictor and TimeSeriesPredictor models, providing REST v1/v2 inference endpoints.

**Detailed**: The KServe AutoGluon Server is a Python-based inference runtime that serves models trained with the AutoGluon ML framework. It supports two predictor types: **TabularPredictor** (for classification and regression on structured/tabular data) and **TimeSeriesPredictor** (for time series forecasting). The server auto-detects the predictor type at startup by attempting to load the model directory with each predictor class.

For tabular models, the server supports both KServe inference protocol v1 (JSON) and v2 (tensor-based), including optional probability output via `predict_proba()` for classification tasks. For time series models, only REST v1 JSON is supported, with structured history and known-covariates inputs. The server integrates with KServe's model storage system (supporting S3, GCS, Azure Blob, and local filesystem) for model downloading.

The AutoGluon server ships as a single container image built by Konflux, using an AIPCC CPU base image. It is deployed as part of RHOAI's KServe serving infrastructure — when a user creates an `InferenceService` CR with `modelFormat.name: autogluon`, the KServe controller provisions a pod running this image, pointed at the user's saved predictor artifact.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| autogluonserver | Python Inference Server | Serves AutoGluon TabularPredictor and TimeSeriesPredictor models via KServe REST v1/v2 protocol |
| kserve (SDK) | Python Library (vendored) | KServe model serving framework providing ModelServer, Model base class, inference protocol, and storage |
| kserve-storage | Python Library (vendored) | KServe storage handler for downloading models from S3, GCS, Azure Blob, HuggingFace Hub, and local paths |

## AIPCC Ecosystems Use

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| CPU | Dockerfile.konflux.autogluon | registry.redhat.io/rhai/base-image-cpu-rhel9@sha256:118425714f88c5b24cd67e828e0bcc7a9bc634ab2a1448e8f0975dfd85aea45c | CPU | -- | aarch64, ppc64le, s390x, x86_64 | Active |

_The component uses the AIPCC CPU base image. Only a CPU variant is built — AutoGluon's tabular and time series backends (LightGBM, XGBoost, fastai/PyTorch) run on CPU. No GPU-accelerated variants (CUDA, ROCm, etc.) are provided._

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | Yes -- explicitly configured via `--index-url` in Dockerfile and requirements file |
| **pip.conf / uv.toml configured** | Overridden: Dockerfile uses explicit `--index-url https://console.redhat.com/api/pypi/public-rhai/rhoai/3.5-EA2/cpu-ubi9/simple/` |
| **Additional packages installed** | AutoGluon 1.5.0+rhaiv.3 (tabular with lightgbm, xgboost, tabm, fastai extras; timeseries), plus ~150 transitive dependencies including torch 2.11.0, scikit-learn, pandas, numpy, spacy, transformers |
| **Source** | Dockerfile.konflux.autogluon:21 |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|
| `/usr/libexec/rhaipcc/dnf` | Enable vendor repos for additional RPM installs | Not used (RPM installs are commented out in Dockerfile) |
| `/etc/rhaipcc/env` | Shell variables for variant, versions, repo info | Not used |

_The Dockerfile has commented-out `microdnf` install commands (lines 8, 45) suggesting RPM installs were previously used but removed in favor of pure Python/pip installs from the AIPCC package index._

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_The AutoGluon server does not define its own CRDs. It is deployed as a KServe runtime, managed by the KServe controller via `InferenceService` and `ClusterServingRuntime` CRDs._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/v1/models/{model_name}:predict` | POST | 8080/TCP | HTTP | None (plaintext) | None (delegated to platform) | v1 JSON inference for tabular and time series models |
| `/v2/models/{model_name}/infer` | POST | 8080/TCP | HTTP | None (plaintext) | None (delegated to platform) | v2 tensor-based inference for tabular models only |
| `/v2/models/{model_name}` | GET | 8080/TCP | HTTP | None (plaintext) | None (delegated to platform) | v2 model metadata (input/output tensor schemas) |
| `/v1/models/{model_name}` | GET | 8080/TCP | HTTP | None (plaintext) | None (delegated to platform) | v1 model readiness check |
| `/v2/health/ready` | GET | 8080/TCP | HTTP | None (plaintext) | None | Health/readiness probe |
| `/v2/health/live` | GET | 8080/TCP | HTTP | None (plaintext) | None | Health/liveness probe |

_Port 8080 is the KServe default HTTP port. In RHOAI deployments, TLS termination and authentication are handled externally by kube-rbac-proxy sidecar or platform ingress (Gateway API / HTTPRoute), not by the inference server itself._

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

_The AutoGluon server does not expose gRPC endpoints. KServe's model server supports gRPC via `--grpc_port` but the AutoGluon runtime uses HTTP-only inference._

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| autogluon.tabular | 1.5.0+rhaiv.3 | Yes | AutoGluon tabular prediction framework with LightGBM, XGBoost, TabM, fastai backends |
| autogluon.timeseries | 1.5.0+rhaiv.3 | Yes | AutoGluon time series forecasting with Chronos, statsforecast, mlforecast backends |
| kserve | 0.19.0 | Yes | KServe Python SDK providing ModelServer, Model base class, inference protocol handling |
| kserve-storage | 0.19.0 | Yes | Model download from S3, GCS, Azure Blob, HuggingFace Hub |
| torch | 2.11.0 | Yes | PyTorch backend for AutoGluon neural network models and Chronos time series |
| lightgbm | 4.6.0 | Yes | LightGBM backend for AutoGluon tabular gradient boosting |
| xgboost | 3.1.3 | Yes | XGBoost backend for AutoGluon tabular gradient boosting |
| fastai | 2.8.7 | Yes | fastai backend for AutoGluon tabular neural network models |
| scikit-learn | 1.7.2 | Yes | ML utilities used by AutoGluon for preprocessing and evaluation |
| pandas | 2.3.3 | Yes | DataFrame operations for tabular and time series data manipulation |
| numpy | 2.1.0 | Yes | Numerical computing for tensor operations and data conversion |
| uvicorn | 0.49.0 | Yes | ASGI server used by KServe ModelServer for HTTP serving |
| fastapi | 0.136.3 | Yes | Web framework used by KServe for REST endpoint routing |
| transformers | 4.57.6 | Yes | HuggingFace Transformers used by Chronos time series models |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| KServe Controller | CRD (InferenceService) | KServe controller creates pods running this image when InferenceService specifies `modelFormat: autogluon` |
| ClusterServingRuntime | CRD | Defines the autogluon runtime image and supported model format for KServe |
| Model Storage (S3/GCS/Azure/HF) | Storage API | Downloads saved predictor artifacts from remote storage at model load time |
| kube-rbac-proxy | Sidecar | Platform-injected auth sidecar fronting the inference server in RHOAI 3.x deployments |

## Deployment Manifests

_This component does not include kustomize manifests for deployment. The AutoGluon server is deployed dynamically by the KServe controller when an `InferenceService` CR references the `autogluon` model format. The container image reference is specified in a `ClusterServingRuntime` CR managed by the platform operator._

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| (dynamic per InferenceService) | ClusterIP | 8080/TCP | 8080 | HTTP | None | None (delegated to platform sidecar/ingress) | Internal |

_Services are created dynamically by the KServe controller for each InferenceService. The autogluon server container itself serves plain HTTP; TLS and auth are handled by the platform ingress stack (kube-rbac-proxy sidecar on 8443/TCP fronting the container on 8080/TCP)._

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

_Ingress is managed by the KServe controller and platform operator (HTTPRoute / Gateway API in RHOAI 3.x), not by this component._

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| S3-compatible storage | 443/TCP | HTTPS | TLS 1.2+ | AWS IAM credentials / Service Account | Download saved predictor artifacts from object storage |
| Google Cloud Storage | 443/TCP | HTTPS | TLS 1.2+ | GCP Service Account | Download saved predictor artifacts from GCS |
| Azure Blob Storage | 443/TCP | HTTPS | TLS 1.2+ | Azure credentials | Download saved predictor artifacts from Azure |
| HuggingFace Hub | 443/TCP | HTTPS | TLS 1.2+ | HF Token (optional) | Download model artifacts from HuggingFace Hub |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_The AutoGluon server does not define RBAC resources. It runs as a non-privileged container (UID 1001) and does not interact with the Kubernetes API._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|

_The server accesses storage credentials via environment variables injected by the KServe controller (e.g., `AWS_ACCESS_KEY_ID`, `AZURE_CLIENT_ID`, storage service account tokens). No secrets are defined in the component itself._

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| `/v1/models/*` | POST, GET | Bearer Token (delegated) | kube-rbac-proxy sidecar (RHOAI 3.x) | Platform RBAC policy |
| `/v2/models/*` | POST, GET | Bearer Token (delegated) | kube-rbac-proxy sidecar (RHOAI 3.x) | Platform RBAC policy |
| `/v2/health/*` | GET | None | None | Health probes are unauthenticated |

_The AutoGluon server itself does not implement authentication. In RHOAI 3.x deployments, auth is enforced by a kube-rbac-proxy sidecar injected by the KServe controller, which fronts the inference container on port 8443/TCP and proxies authenticated requests to 8080/TCP._

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A (Python application, no Go binary) | Dockerfile.konflux.autogluon |
| **Linking** | N/A (Python interpreted) | -- |
| **OpenSSL in image** | Yes (via AIPCC CPU base image, RHEL 9.6 UBI) | Dockerfile.konflux.autogluon:1 |
| **OLM FIPS annotation** | Not applicable (not an operator) | -- |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No direct TLS configuration; relies on Python `ssl` module defaults which use system OpenSSL | -- |
| **Crypto libraries** | cryptography 48.0.0 (transitive via kserve, kserve-storage); uses system OpenSSL | autogluon-all-requirements.txt:197 |
| **Certificate handling** | System trust store (RHEL CA bundle from base image) | -- |
| **Non-FIPS crypto risks** | None detected in autogluon server code; no direct crypto imports | -- |

_The AutoGluon server is a pure Python application. FIPS compliance is inherited from the AIPCC base image which provides RHEL 9.6 with OpenSSL configured for FIPS. The `cryptography` Python package (transitive dependency) uses OpenSSL as its backend. No non-FIPS crypto patterns were detected in the autogluon server source code._

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A |
| **Language deps** | autogluon-all-requirements.txt (pinned with SHA256 hashes) | Yes | pip (via uv export) | python/autogluonserver/autogluon-all-requirements.txt |
| **Language deps** | go.sum | Yes | go mod | go.sum (for Go components in parent repo; not used by autogluon server) |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | pip prefetch configured in Tekton PipelineRun | Yes | Hermeto (formerly cachi2) | .tekton/odh-kserve-autogluon-on-pull-request.yaml:54-63 |

_The build is configured as hermetic (`hermetic: "true"` in Tekton PipelineRun). Python dependencies are locked via `autogluon-all-requirements.txt` with SHA256 hashes (generated by `uv export`). Hermeto pip prefetch is configured in the Tekton pipeline to pre-fetch all Python packages. No RPM lock file is present on this branch (main); downstream release branches (e.g., `rhoai-3.5`) may add `rpms.lock.yaml`. The autogluon server Dockerfile has RPM installs commented out (lines 8, 45), so the RPM gap is mitigated by not installing additional RPMs beyond the base image._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-InferenceService (per-pod) | Inferred from KServe deployment model |
| **Deployment model** | Single-tenant per pod | Each InferenceService gets its own pod running autogluon server |
| **Tenant identifier** | InferenceService name + namespace | Inferred from KServe architecture |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | kube-rbac-proxy sidecar per pod | Platform (KServe controller) | Auth is platform-enforced, not application-level |
| Data storage | Per-InferenceService model storage URI | Application (KServe storage) | Models loaded from user-specified storageUri; no cross-tenant data access within the server |
| Network traffic | Pod-level network isolation | Kubernetes NetworkPolicy (platform) | No intra-pod network isolation between sidecar and server container |
| Compute & resources | Per-pod resource limits | Kubernetes (set via InferenceService spec) | Resource limits must be configured per InferenceService |
| Configuration & secrets | Per-pod env vars injected by KServe controller | Kubernetes / KServe | Storage credentials are per-InferenceService |
| API scoping | Single model per server instance | Application | Each pod serves exactly one model; no multi-model sharing within a pod |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|

_No shared services. Each AutoGluon server pod is an independent, single-tenant instance serving one model._

## Admission Webhooks

This component defines 19 webhook(s) (4 mutating, 11 validating, 4 conversion).

| Name | Type | Target Resources | Purpose |
|------|------|-----------------|---------|
| clusterservingruntime.kserve-webhook-server.validator | validating | clusterservingruntimes | Validates ServingRuntime and ClusterServingRuntime resources on create/update to ensure model format priority consistency (no duplicate priorities across runtimes for the same model format and protocol, and consistent priorities within a single runtime), and validates multi-node worker spec constraints including GPU resource types, parallel size values, blocked environment variables, and disallowing removal of an existing workerSpec. |
| conversion-unknown | conversion |  |  |
| conversion-unknown | conversion |  |  |
| conversion-unknown | conversion |  |  |
| conversion-unknown | conversion |  |  |
| inferencegraph.kserve-webhook-server.validator | validating | inferencegraphs |  |
| inferenceservice.kserve-webhook-server.defaulter | mutating | inferenceservices |  |
| inferenceservice.kserve-webhook-server.pod-mutator | mutating | pods | Mutates pods owned by KServe InferenceServices at creation time, injecting storage initializer init-containers (for model downloading with appropriate credentials), inference logger/batcher agent sidecars, metrics aggregator sidecars, GKE accelerator node selectors, Istio CNI security contexts, and optionally modelcar (OCI image source) volumes. It reads the InferenceService config and credential secrets to configure these injected containers. |
| inferenceservice.kserve-webhook-server.validator | validating | inferenceservices |  |
| llminferenceservice.kserve-webhook-server.v1alpha1.defaulter | mutating | llminferenceservices | Mutating defaulter that applies defaults to LLMInferenceService resources and, when local model caching is enabled in the inference service ConfigMap, lists cluster-scoped LocalModelCache and namespace-scoped LocalModelNamespaceCache resources to find one whose storage URI matches the service's model URI, then stamps matching local-model labels and PVC-name annotations onto the LLMInferenceService (namespace-scoped caches take precedence over cluster-scoped ones). |
| llminferenceservice.kserve-webhook-server.v1alpha1.validator | validating | llminferenceservices | Validating webhook for LLMInferenceService v1alpha1 resources on create and update. It enforces cross-field constraints on router/gateway/HTTPRoute configuration, ensures parallelism settings are consistent (e.g., worker requires parallelism, pipeline and data parallelism are mutually exclusive, data and dataLocal must be paired), validates scheduler config (replicas > 0, inline vs ref exclusivity), delegates scaling validation to the v1alpha2 hub type, and prevents mutation of the total parallelism size on updates. |
| llminferenceservice.kserve-webhook-server.v1alpha2.defaulter | mutating | llminferenceservices | Mutating defaulter that applies defaults to LLMInferenceService resources and, when local model caching is enabled in the inference service ConfigMap, lists cluster-scoped LocalModelCache and namespace-scoped LocalModelNamespaceCache resources to find one whose storage URI matches the service's model URI, then stamps matching local-model labels and PVC-name annotations onto the LLMInferenceService (namespace-scoped caches take precedence over cluster-scoped ones). |
| llminferenceservice.kserve-webhook-server.v1alpha2.validator | validating | llminferenceservices | Validating admission webhook for the LLMInferenceService v1alpha2 custom resource that enforces structural and cross-field constraints on create and update: router/gateway/HTTPRoute consistency, parallelism configuration (pipeline vs data, worker requirements, immutability of total parallelism size), scheduler config (replicas, inline vs ref), LoRA adapter naming (uniqueness, path-traversal prevention, base-model collision), and autoscaling rules (replicas vs scaling mutual exclusivity, HPA/KEDA actuator selection, WVA variantCost format, decode/prefill actuator consistency, KEDA idleReplicaCount bounds). |
| llminferenceserviceconfig.kserve-webhook-server.v1alpha1.validator | validating | llminferenceserviceconfigs | Validates LLMInferenceServiceConfig resources on create and update, ensuring baseRefs is not set (recursive refs are disabled), scheduler replicas are positive if specified, and running any injected controller-level validation. It also emits warnings when well-known configs are modified or deleted. |
| llminferenceserviceconfig.kserve-webhook-server.v1alpha2.validator | validating | llminferenceserviceconfigs | Validates LLMInferenceServiceConfig resources on create and update, rejecting configs that use the forbidden baseRefs field (recursive refs are disabled) or specify invalid scheduler replicas (must be >0), and emits warnings when well-known configs are modified or deleted. |
| localmodelcache.kserve-webhook-server.validator | validating | localmodelcaches |  |
| servingruntime.kserve-webhook-server.validator | validating | servingruntimes | Validates ServingRuntime and ClusterServingRuntime resources on create/update to ensure model format priority consistency (no duplicate priorities across runtimes for the same model format and protocol, and consistent priorities within a single runtime), and validates multi-node worker spec constraints including GPU resource types, parallel size values, blocked environment variables, and disallowing removal of an existing workerSpec. |
| trainedmodel.kserve-webhook-server.validator | validating | trainedmodels |  |
| localmodelnamespacecache.kserve-webhook-server.validator | validating | localmodelnamespacecaches | Validates LocalModelNamespaceCache resources on create, update, and delete. On create/update, it ensures all referenced node groups exist; on delete, it prevents removal if the cache is still referenced by any InferenceService or LLMInferenceService via matching local-model labels. |

### External Webhooks

The following webhooks from peer components intercept this component's resource types:

| Webhook | Defined By |
|---------|-----------|
| clusterservingruntime.kserve-webhook-server.validator | kserve |
| inferencegraph.kserve-webhook-server.validator | kserve |
| inferenceservice.kserve-webhook-server.defaulter | kserve |
| inferenceservice.kserve-webhook-server.pod-mutator | kserve |
| inferenceservice.kserve-webhook-server.validator | kserve |
| llminferenceservice.kserve-webhook-server.v1alpha1.defaulter | kserve |
| llminferenceservice.kserve-webhook-server.v1alpha1.validator | kserve |
| llminferenceservice.kserve-webhook-server.v1alpha2.defaulter | kserve |
| llminferenceservice.kserve-webhook-server.v1alpha2.validator | kserve |
| llminferenceserviceconfig.kserve-webhook-server.v1alpha1.validator | kserve |
| llminferenceserviceconfig.kserve-webhook-server.v1alpha2.validator | kserve |
| servingruntime.kserve-webhook-server.validator | kserve |
| trainedmodel.kserve-webhook-server.validator | kserve |
| namespace.sidecar-injector.istio.io | llm-d-inference-scheduler |
| object.sidecar-injector.istio.io | llm-d-inference-scheduler |
| rev.namespace.sidecar-injector.istio.io | llm-d-inference-scheduler |
| rev.object.sidecar-injector.istio.io | llm-d-inference-scheduler |
| namespace.sidecar-injector.istio.io | llm-d-router |
| object.sidecar-injector.istio.io | llm-d-router |
| rev.namespace.sidecar-injector.istio.io | llm-d-router |
| rev.object.sidecar-injector.istio.io | llm-d-router |
| servingruntime.modelmesh-webhook-server.default | modelmesh-serving |
| connection-llmisvc-v1alpha1.odh-model-controller.opendatahub.io | odh-model-controller |
| connection-llmisvc-v1alpha2.odh-model-controller.opendatahub.io | odh-model-controller |
| minferencegraph-v1alpha1.odh-model-controller.opendatahub.io | odh-model-controller |
| minferenceservice-v1beta1.odh-model-controller.opendatahub.io | odh-model-controller |
| mutating.pod.odh-model-controller.opendatahub.io | odh-model-controller |
| validating.isvc.odh-model-controller.opendatahub.io | odh-model-controller |
| vinferencegraph-v1alpha1.odh-model-controller.opendatahub.io | odh-model-controller |
| mutate-pod.sparkoperator.k8s.io | spark-operator |

## Data Flows

### Flow 1: Model Loading at Startup

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | KServe Controller | AutoGluon Server Pod | -- | -- | -- | -- |
| 2 | AutoGluon Server | Model Storage (S3/GCS/Azure/HF) | 443/TCP | HTTPS | TLS 1.2+ | Storage credentials (IAM/SA/Token) |
| 3 | AutoGluon Server | Local filesystem | -- | -- | -- | -- |
| 4 | AutoGluon Server | AutoGluon Predictor.load() | -- | -- | -- | -- |

_At startup, the server downloads the model from the configured `storageUri` via kserve-storage, saves it locally, then loads it with `detect_and_load_predictor()` which tries TimeSeriesPredictor first, then TabularPredictor._

### Flow 2: Tabular Inference Request (v2)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | kube-rbac-proxy sidecar | 8443/TCP | HTTPS | TLS 1.3 | Bearer Token |
| 2 | kube-rbac-proxy | AutoGluon Server | 8080/TCP | HTTP | None (localhost) | None (pre-authenticated) |
| 3 | AutoGluon Server | TabularPredictor.predict() / predict_proba() | -- | in-process | -- | -- |
| 4 | AutoGluon Server | Client (via kube-rbac-proxy) | 8443/TCP | HTTPS | TLS 1.3 | -- |

### Flow 3: Time Series Inference Request (v1)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | kube-rbac-proxy sidecar | 8443/TCP | HTTPS | TLS 1.3 | Bearer Token |
| 2 | kube-rbac-proxy | AutoGluon Server | 8080/TCP | HTTP | None (localhost) | None (pre-authenticated) |
| 3 | AutoGluon Server | TimeSeriesPredictor.predict() | -- | in-process | -- | -- |
| 4 | AutoGluon Server | Client (via kube-rbac-proxy) | 8443/TCP | HTTPS | TLS 1.3 | -- |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| KServe Controller | CRD Watch (InferenceService) | -- | -- | -- | Controller creates pods running this image when InferenceService specifies autogluon format |
| ClusterServingRuntime | CRD | -- | -- | -- | Defines this runtime's image, supported model formats, and container spec |
| kube-rbac-proxy | Sidecar (injected) | 8443/TCP → 8080/TCP | HTTPS → HTTP | TLS termination at sidecar | Auth enforcement proxy fronting the inference server |
| S3-compatible storage | REST API | 443/TCP | HTTPS | TLS 1.2+ | Download model artifacts via boto3/kserve-storage |
| HuggingFace Hub | REST API | 443/TCP | HTTPS | TLS 1.2+ | Download model artifacts via huggingface-hub/kserve-storage |
| Azure Blob Storage | REST API | 443/TCP | HTTPS | TLS 1.2+ | Download model artifacts via azure-core/kserve-storage |
| RHEL AI Python Package Index | PyPI API | 443/TCP | HTTPS | TLS 1.2+ | Build-time only: pip install from Red Hat's secure package index |

## Architectural Analysis

The AutoGluon server is architecturally a thin wrapper around AutoGluon's predictor classes, adapting them to the KServe inference protocol. The core design pattern is a **strategy pattern with auto-detection**: `AutoGluonDetectedModel` acts as a facade that detects whether a saved model is tabular or time series, then delegates all inference calls to the appropriate implementation (`AutoGluonTabularModel` or `AutoGluonTimeSeriesModel`). This auto-detection is implemented via a try-load approach in `predictor_detect.py` — it attempts `TimeSeriesPredictor.load()` first (because tabular models are more common and would fail faster as a fallback), then falls back to `TabularPredictor.load()`.

The tabular model implementation (`tabular_model.py`) is significantly more complex than time series, supporting both KServe v1 (JSON dict) and v2 (InferRequest tensor) protocols. The v2 support includes sophisticated DataFrame construction from per-feature tensors (`_infer_request_to_dataframe`), proper v2 datatype mapping from AutoGluon's internal type system (`_feature_to_v2_datatype`), and dynamic output schema generation based on problem type (regression returns FP64, classification returns class-specific datatypes, quantile returns multi-column FP64). The `predict_proba` mode (controlled by `PREDICT_PROBA` env var) generates per-class probability outputs with sanitized tensor names, which is essential for production use cases requiring confidence scores.

The `version_compat.py` module addresses a practical deployment challenge: AutoGluon enforces strict version matching when loading saved predictors, but downstream builds use patched versions (e.g., `1.5.0+rhaiv.3` vs upstream `1.5.0`). The compatibility layer reads the saved version file, compares only major.minor, and passes `require_version_match=False` for patch-level differences. This is critical for RHOAI where the `+rhaiv.N` local version suffix would otherwise prevent loading models trained with upstream AutoGluon.

The `runtime_paths.py` module handles a container-specific concern: AutoGluon's SeasonalNaive fallback (used for short time series) calls `setup_outputdir` under the process CWD, which defaults to `/` in containers — a non-writable path for non-root users (UID 1001). The module finds a writable directory (`/tmp`, `$HOME`, `/home/kserve`) and changes the process CWD at startup. The matplotlib cache directory is similarly redirected to `/tmp/matplotlib` to avoid home directory write failures.

The build architecture uses a multi-stage Dockerfile with the AIPCC CPU base image. All Python dependencies are installed from the RHEL AI Python Package Index (`console.redhat.com/api/pypi/public-rhai/rhoai/3.5-EA2/cpu-ubi9/simple/`), ensuring only Red Hat-built wheels are used. The `autogluon-all-requirements.txt` file is a comprehensive hash-locked requirements file generated by `uv export`, enabling fully hermetic builds via Hermeto pip prefetch in the Tekton pipeline. The AutoGluon packages themselves carry the `+rhaiv.3` local version suffix, indicating they are Red Hat-patched rebuilds of upstream 1.5.0.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 77c3b245f | 2026-07-22 | chore: Update of RH Index for RHOAI 3.5 EA2 |
| 16821d448 | 2026 | chore: autogluon==1.5.0+rhaiv.2 for hermetic build |
| 9a42919f5 | 2026 | chore: Updated autogluon version to 1.5.0+rhaiv.2 |
| 66668dc71 | 2026 | feat: full init hermetic build |
| 7bc4e61ba | 2026 | feat: Port all changes from upstream to downstream |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| Dockerfile.konflux.autogluon | 1-62 | Metadata, AIPCC Ecosystems Use, Architecture Components, Security (FIPS, Build Hermeticity) |
| python/autogluonserver/autogluonserver/__main__.py | 1-49 | Purpose, APIs Exposed, Architecture Components, Data Flows |
| python/autogluonserver/autogluonserver/__init__.py | 1-27 | Architecture Components |
| python/autogluonserver/autogluonserver/tabular_model.py | 1-498 | APIs Exposed, Data Flows, Architectural Analysis |
| python/autogluonserver/autogluonserver/timeseries_model.py | 1-439 | APIs Exposed, Data Flows, Architectural Analysis |
| python/autogluonserver/autogluonserver/predictor_factory.py | 1-91 | Architecture Components, Architectural Analysis |
| python/autogluonserver/autogluonserver/predictor_detect.py | 1-59 | Architecture Components, Architectural Analysis |
| python/autogluonserver/autogluonserver/autogluon_model_repository.py | 1-34 | Architecture Components |
| python/autogluonserver/autogluonserver/runtime_paths.py | 1-60 | Architectural Analysis |
| python/autogluonserver/autogluonserver/version_compat.py | 1-93 | Architectural Analysis |
| python/autogluonserver/pyproject.toml | 1-40 | Dependencies, Metadata |
| python/autogluonserver/autogluon-all-requirements.txt | 1-1092 | Dependencies, AIPCC Ecosystems Use, Security (Build Hermeticity) |
| python/autogluonserver/README.md | 1-343 | Purpose, APIs Exposed, Dependencies, Architectural Analysis |
| python/autogluonserver/Makefile | 1-11 | Architecture Components |
| python/kserve/pyproject.toml | 1-116 | Dependencies, APIs Exposed |
| python/storage/pyproject.toml | 1-40 | Dependencies, Egress |
| python/VERSION | 1 | Metadata |
| PROJECT | 1-3 | Metadata |
| .tekton/odh-kserve-autogluon-on-pull-request.yaml | 1-83 | Security (Build Hermeticity), Deployment |
| overlays/0017-aipcc-base-images.md | 1-237 | AIPCC Ecosystems Use |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `*Dockerfile*konflux*` (glob) | Dockerfile.konflux.autogluon | Architecture Components, AIPCC Ecosystems Use |
| `cryptography\|pyOpenSSL\|pycryptodome` (grep in python/autogluonserver/) | 0 matches | Security (FIPS Application-Level Crypto) |
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED\|FIPS` (grep in Dockerfile) | 0 matches | Security (FIPS Build-Time) |
| `rhaipcc\|/usr/libexec/rhaipcc` (grep in Dockerfile) | 0 matches | AIPCC Ecosystems Use (Tooling) |
| `cachi2\|hermeto\|REMOTE_SOURCES` (grep in Dockerfiles) | 1 match (comment in Dockerfile.konflux.autogluon) | Security (Build Hermeticity) |
| `rpms.lock.yaml\|uv.lock\|go.sum` (find) | go.sum + 13 uv.lock files (other runtimes) | Security (Build Hermeticity) |
| sync workflows (find in .github/workflows/) | 0 matches | Provenance |
| `upstream\|fork\|sync` (grep in .github/workflows/) | 0 matches | Provenance |

### Summary

- **Total files read**: 20
- **Total lines referenced**: ~3,194
- **Coverage**: All sections have direct source backing. Provenance repo lineage is inferred from git remote URL and absence of sync workflows (marked as `local_analysis`). Network Architecture ingress details are inferred from KServe platform architecture (auth sidecar pattern) rather than this component's code, as the component does not manage its own ingress.

---
*Generated in 4m 19s (259s total)*
