# Component: Gateway API Inference Extension

## Metadata

- **Repository**: https://github.com/red-hat-data-services/gateway-api-inference-extension
- **Version**: 768b9ccc (main branch)
- **Distribution**: RHOAI
- **Languages**: Go 1.25, Python 3.x
- **Deployment Type**: Envoy ext-proc gRPC Service / Controller (Kubernetes-native)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/kubernetes-sigs/gateway-api-inference-extension | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/gateway-api-inference-extension | manual | main | -- | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Extends any Envoy-based Gateway API proxy into an inference gateway that provides intelligent, KV-cache-aware load balancing and request scheduling for self-hosted large language models on Kubernetes.

**Detailed**: Gateway API Inference Extension (also known as Inference Gateway / IGW) is a Kubernetes SIG project that optimizes serving generative AI models. It works by implementing Envoy's External Processing (ext-proc) gRPC protocol to intercept inference requests and responses flowing through any Gateway API-compatible proxy (Envoy Gateway, Istio, GKE Gateway, etc.), enabling intelligent endpoint selection based on real-time model server metrics.

The project consists of two primary components: the Endpoint Picker (EPP) and the Body Based Router (BBR). The EPP is the core scheduling engine that watches InferencePool CRDs to discover model server pods, scrapes their Prometheus metrics (queue depth, KV-cache utilization, running requests, LoRA adapter availability), and uses a pluggable framework of filters, scorers, and pickers to select the optimal backend for each request. The BBR is an optional ext-proc server that parses HTTP request bodies to extract model names and other routing metadata into headers that the gateway can use for routing decisions before the request reaches the EPP.

The project also defines the InferencePool CRD (GA in v1) along with experimental extensions: InferenceObjective (priority and fairness), InferenceModelRewrite (model name routing and traffic splitting), InferencePoolImport (multi-cluster federation), and EndpointPickerConfig (plugin configuration). An optional latency predictor subsystem (Python training/prediction servers + Go async sidecar client) provides TTFT/TPOT prediction capabilities for latency-aware scheduling.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| Endpoint Picker (EPP) | Go gRPC Service (ext-proc) | Core scheduling engine; intercepts Envoy traffic, selects optimal model server endpoints using pluggable filters/scorers/pickers, manages InferencePool/InferenceObjective/InferenceModelRewrite CRDs |
| Body Based Router (BBR) | Go gRPC Service (ext-proc) | Optional body parser; extracts model name from JSON request body into routing headers for gateway-level routing before EPP |
| Latency Predictor Training Server | Python FastAPI Service | Collects runtime latency observations, trains Bayesian Ridge / XGBoost / LightGBM models for TTFT/TPOT prediction |
| Latency Predictor Prediction Server | Python FastAPI Service | Serves trained models for real-time latency predictions; supports bulk and strict-bulk endpoints |
| Latency Predictor Async Client | Go Sidecar Library | Async Go client for latency prediction servers; runs as EPP sidecar with request coalescing and background training data submission |
| client-go | Go Library | Generated Kubernetes client, informers, and listers for InferencePool, InferenceObjective, InferenceModelRewrite, InferencePoolImport CRDs |
| CRD Generator | Go Utility | Generates JSON schema contract files for CRDs in contracts/schemas/ |

## Sub-Component Details

### Endpoint Picker (EPP)

**Dockerfile**: `Dockerfile`
**Intent**: Primary service — the core ext-proc server that intercepts all inference traffic flowing through the gateway. Deployed per InferencePool as a Kubernetes Deployment with optional leader election for HA.
**Image**: `epp`
**Language**: Go 1.25
**Port**: 9002/TCP (gRPC ext-proc), 9003/TCP (gRPC health), 9090/TCP (HTTP metrics)

#### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| envoy.service.ext_proc.v3.ExternalProcessor | 9002/TCP | gRPC | TLS (optional, self-signed or provided certs) | None | Bidirectional streaming ext-proc for request/response interception |
| grpc.health.v1.Health | 9003/TCP | gRPC | plaintext | None | Liveness/readiness health checks; leader-aware in HA mode |

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Model server pods (vLLM, SGLang, Triton, trtllm-serve) | HTTP/HTTPS | Configurable (default 8000/TCP) | None | Scrapes Prometheus /metrics for queue depth, KV-cache utilization, running requests, LoRA info |
| Kubernetes API server | HTTPS | 443/TCP | ServiceAccount token | Watches Pods, InferencePool, InferenceObjective, InferenceModelRewrite CRDs |
| Latency Prediction Server (optional) | HTTP | 8001/TCP | None | TTFT/TPOT predictions via /predict, /predict/bulk, /predict/bulk/strict endpoints |
| Latency Training Server (optional) | HTTP | 8000/TCP | None | Submits training data via /add_training_data_bulk; fetches model coefficients via /metrics |
| OpenTelemetry Collector (optional) | gRPC | 4317/TCP | None | Exports distributed traces via OTLP |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| --grpc-port | flag | 9002 | gRPC port for Envoy ext-proc communication |
| --pool-name | flag | (required) | Name of the InferencePool resource to watch |
| --pool-namespace | flag | NAMESPACE env / "default" | Namespace of the InferencePool resource |
| --pool-group | flag | inference.networking.k8s.io | API group for InferencePool CRD |
| --ha-enable-leader-election | flag | false | Enable Kubernetes lease-based leader election for HA |
| --secure-serving | flag | false | Enable TLS on gRPC server |
| --cert-path | flag | "" | Path to TLS certificate directory (tls.crt/tls.key) |
| --enable-cert-reload | flag | false | Watch cert-path for live certificate rotation |
| --config-file | flag | "" | Path to EndpointPickerConfig YAML file |
| --config-text | flag | "" | Inline EndpointPickerConfig YAML |
| --refresh-metrics-interval | flag | 50ms | Backend metrics polling interval |
| --refresh-prometheus-metrics-interval | flag | 5s | Prometheus metrics flush interval |
| --tracing | flag | false | Enable OpenTelemetry tracing |
| --enable-pprof | flag | true | Enable pprof profiling endpoints |
| --metrics-port | flag | 9090 | HTTP port for Prometheus metrics |
| --metrics-endpoint-auth | flag | true | Enable Kubernetes RBAC on /metrics endpoint |
| -v | flag | 2 | Log verbosity level (2=default, 3=verbose, 4=debug, 5=trace) |
| NAMESPACE | env | "" | Kubernetes namespace (injected by downward API) |
| POD_NAME | env | "" | Pod name (injected by downward API) |

### Body Based Router (BBR)

**Dockerfile**: `bbr.Dockerfile`
**Intent**: Optional module — parses HTTP request bodies to extract model names into headers for gateway-level routing. Deployed only when body-based routing is needed (e.g., OpenAI-compatible endpoints where model name is in the JSON body).
**Image**: `bbr`
**Language**: Go 1.25
**Port**: 9004/TCP (gRPC ext-proc), 9005/TCP (gRPC health), 9090/TCP (HTTP metrics)

#### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| envoy.service.ext_proc.v3.ExternalProcessor | 9004/TCP | gRPC | TLS (self-signed by default) | None | Parses request/response bodies, extracts model name to X-Gateway-Model-Name header |
| grpc.health.v1.Health | 9005/TCP | gRPC | plaintext | None | Health check returning SERVING for ext_proc service |

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Kubernetes API server | HTTPS | 443/TCP | ServiceAccount token | Watches ConfigMaps with label inference.networking.k8s.io/bbr-managed=true for LoRA adapter mappings |
| OpenTelemetry Collector (optional) | gRPC | 4317/TCP | None | Exports distributed traces via OTLP |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| --grpc-port | flag | 9004 | gRPC port for Envoy ext-proc |
| --grpc-health-port | flag | 9005 | gRPC health check port |
| --metrics-port | flag | 9090 | Prometheus metrics port |
| --streaming | flag | false | Enable streaming mode for body processing |
| --secure-serving | flag | true | Enable TLS on gRPC server |
| --plugin | flag | (default plugins) | Plugin spec in format type:name:params |
| --tracing | flag | true | Enable OpenTelemetry tracing |
| --enable-pprof | flag | true | Enable pprof profiling |
| --metrics-endpoint-auth | flag | true | Enable RBAC on /metrics |

### Latency Predictor Training Server

**Dockerfile**: `latencypredictor/Dockerfile-training`
**Intent**: Optional module — collects latency observations from EPP and trains prediction models (Bayesian Ridge, XGBoost, LightGBM). Deployed as a sidecar container within the EPP pod when latency prediction is enabled.
**Image**: `latency-training-server`
**Language**: Python (FastAPI + scikit-learn + XGBoost + LightGBM)
**Port**: 8000/TCP

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|
| /add_training_data_bulk | POST | -- | None | Accepts training data samples from EPP async client |
| /metrics | GET | -- | None | Exposes Bayesian Ridge coefficients in Prometheus format |
| /model/download/info | GET | -- | None | Returns current model type and training status |
| /model/ttft/xgb/json | GET | -- | None | Returns XGBoost TTFT tree structure as JSON |
| /model/tpot/xgb/json | GET | -- | None | Returns XGBoost TPOT tree structure as JSON |
| /healthz | GET | -- | None | Liveness probe |
| /readyz | GET | -- | None | Readiness probe |

### Latency Predictor Prediction Server

**Dockerfile**: `latencypredictor/Dockerfile-prediction`
**Intent**: Optional module — serves trained models for real-time TTFT/TPOT predictions. Deployed as sidecar container(s) within the EPP pod; supports multiple replicas for throughput.
**Image**: `latency-prediction-server`
**Language**: Python (FastAPI + XGBoost + LightGBM)
**Port**: 8001/TCP (configurable, increments for multiple replicas)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|
| /predict | POST | -- | None | Single latency prediction |
| /predict/bulk | POST | -- | None | Bulk predictions with partial failure tolerance |
| /predict/bulk/strict | POST | -- | None | Bulk predictions that fail atomically |
| /status | GET | -- | None | Current model type and readiness |
| /healthz | GET | -- | None | Liveness probe |
| /readyz | GET | -- | None | Readiness probe |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|
| inference.networking.k8s.io | v1 | InferencePool | Namespaced | Defines a pool of model server pods with label selector, target ports, app protocol, and EPP reference |
| inference.networking.x-k8s.io | v1alpha2 | InferenceObjective | Namespaced | Associates a priority level with an InferencePool for flow control fairness/preemption |
| inference.networking.x-k8s.io | v1alpha2 | InferenceModelRewrite | Namespaced | Defines model name rewrite rules with weighted traffic splitting for A/B testing and canary rollouts |
| inference.networking.x-k8s.io | v1alpha1 | InferencePoolImport | Namespaced | Multi-cluster federation — imports InferencePool from remote clusters |
| inference.networking.x-k8s.io | config/v1alpha1 | EndpointPickerConfig | N/A (config object) | Configures EPP plugins, scheduling profiles, data layer, flow control, and parser |

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| /metrics | GET | 9090/TCP | HTTP | None | Optional RBAC (--metrics-endpoint-auth) | Prometheus metrics for request lifecycle, SLOs, pool health, scheduling performance |
| /debug/pprof/* | GET | 9090/TCP | HTTP | None | None | Runtime profiling (heap, goroutine, allocs, block, mutex) when --enable-pprof |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| envoy.service.ext_proc.v3.ExternalProcessor (EPP) | 9002/TCP | gRPC | TLS (optional) | None | Core ext-proc stream for request scheduling and endpoint selection |
| envoy.service.ext_proc.v3.ExternalProcessor (BBR) | 9004/TCP | gRPC | TLS (default) | None | Body parsing ext-proc for model name extraction |
| grpc.health.v1.Health (EPP) | 9003/TCP | gRPC | plaintext | None | EPP health checks (liveness, readiness, ext_proc service) |
| grpc.health.v1.Health (BBR) | 9005/TCP | gRPC | plaintext | None | BBR health checks |

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| Envoy-based Gateway API proxy | Any (Envoy Gateway, Istio, GKE Gateway, kgateway) | Yes | Gateway that routes inference traffic and supports ext-proc filter |
| Gateway API CRDs | v1 | Yes | HTTPRoute CRDs for routing inference traffic to InferencePool backends |
| controller-runtime | v0.23.3 | Yes | Kubernetes controller framework for CRD reconciliation and manager lifecycle |
| Prometheus client_golang | v1.23.2 | Yes | Metrics collection and exposition |
| OpenTelemetry SDK | v1.43.0 | No | Distributed tracing (optional) |
| envoy go-control-plane | v1.37.0 | Yes | Envoy ext-proc protocol definitions |
| vLLM / SGLang / Triton / trtllm-serve | Any | Yes | Model server(s) exposing Prometheus metrics for queue depth, KV-cache, LoRA info |
| XGBoost | latest | No | Latency prediction model training/serving (latencypredictor) |
| LightGBM | latest | No | Alternative latency prediction model (latencypredictor) |
| scikit-learn | latest | No | Bayesian Ridge model for latency prediction (latencypredictor) |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| Envoy Gateway / Istio / GKE Gateway | ext-proc gRPC integration | Gateway sends traffic through ext-proc filter chain to EPP/BBR |
| Model server pods | Prometheus metrics scraping (HTTP) | EPP polls /metrics for scheduling decisions |
| Kubernetes API server | CRD watch, Pod watch | CRD reconciliation and endpoint discovery |

## Deployment Manifests

### Helm Charts

| Chart | Path | Purpose |
|-------|------|---------|
| inferencepool | config/charts/inferencepool/ | Deploys EPP Deployment + Service, InferencePool CR, HTTPRoute, RBAC; depends on epplib |
| body-based-routing | config/charts/body-based-routing/ | Deploys BBR Deployment + Service, RBAC |
| epplib | config/charts/epplib/ | Shared Helm library chart with EPP deployment, RBAC, service, config templates |
| standalone | config/charts/standalone/ | EPP in standalone mode (no InferencePool CRD, fixed endpoint selector) |

### Kustomize Structure

| Base / Overlay | Path | Purpose |
|----------------|------|---------|
| base | config/crd/ | CRD definitions (InferencePool, InferenceObjective, InferenceModelRewrite, InferencePoolImport) |
| base | latencypredictor/manifests/ | Latency predictor dual-server deployment kustomization |

### Parameterization

| Parameter | Source | Default | Purpose |
|-----------|--------|---------|---------|
| inferenceExtension.image.tag | Helm values | main | EPP container image tag |
| inferenceExtension.extProcPort | Helm values | 9002 | EPP gRPC ext-proc port |
| inferenceExtension.replicas | Helm values | 1 | EPP replica count; >1 enables leader election |
| inferencePool.targetPorts[0].number | Helm values | 8000 | Model server port |
| inferencePool.modelServerType | Helm values | vllm | Model server type for metrics mapping (vllm/sglang/triton-tensorrt-llm/trtllm-serve) |
| provider.name | Helm values | none | Gateway provider (gke/istio/none) |
| latencyPredictor.enabled | Helm values | false | Enable latency prediction sidecar |

### Distribution Variants

| Variant | Path | Differences |
|---------|------|-------------|
| GKE | config/charts/*/templates/gke.yaml | GKE-specific GCPBackendPolicy and HealthCheckPolicy resources |
| Istio | config/charts/*/templates/istio.yaml | Istio DestinationRule for ext-proc service connection pooling |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| EPP ext-proc Service | ClusterIP | 9002/TCP | 9002 | gRPC | TLS (optional) | None | Internal |
| EPP health Service | ClusterIP | 9003/TCP | 9003 | gRPC | plaintext | None | Internal |
| EPP metrics Service | ClusterIP | 9090/TCP | 9090 | HTTP | None | Optional RBAC | Internal |
| BBR ext-proc Service | ClusterIP | 9004/TCP | 9004 | gRPC | TLS (default) | None | Internal |
| BBR health Service | ClusterIP | 9005/TCP | 9005 | gRPC | plaintext | None | Internal |
| Latency Training Server | localhost (sidecar) | 8000/TCP | 8000 | HTTP | plaintext | None | Pod-internal |
| Latency Prediction Server | localhost (sidecar) | 8001/TCP | 8001 | HTTP | plaintext | None | Pod-internal |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

_No ingress resources created. EPP/BBR are backend services consumed by gateway ext-proc filters._

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| Model server pods (/metrics) | Configurable (default 8000/TCP) | HTTP/HTTPS | Configurable (InsecureSkipVerify default true) | None | Scrape Prometheus metrics for scheduling decisions |
| Kubernetes API server | 443/TCP | HTTPS | TLS | ServiceAccount token (Bearer) | Watch Pods, InferencePool, InferenceObjective, InferenceModelRewrite CRDs |
| OpenTelemetry Collector (optional) | 4317/TCP | gRPC | plaintext | None | Export distributed traces via OTLP |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| EPP metrics auth (conditional) | authentication.k8s.io | tokenreviews | create |
| EPP metrics auth (conditional) | authorization.k8s.io | subjectaccessreviews | create |
| EPP metrics auth (conditional) | (non-resource) | /metrics | get |

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| {release}-sa | Release namespace | {release}-sa Role | {release} ServiceAccount |

### RBAC - Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| {release}-sa | "" | pods | get, watch, list |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| inference-gateway-sa-metrics-reader-secret | kubernetes.io/service-account-token | ServiceMonitor auth for metrics scraping | Helm chart (conditional) | No |
| TLS certificates (EPP) | tls.crt/tls.key files | gRPC server TLS when --secure-serving | Self-signed generation or user-provided | Yes (--enable-cert-reload via fsnotify) |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| ext-proc gRPC (EPP) | Process | None (network-level trust) | Gateway configuration | Gateway controls which ext-proc filter chains are active |
| ext-proc gRPC (BBR) | Process | None (network-level trust) | Gateway configuration | Gateway controls ext-proc filter chain |
| /metrics (EPP) | GET | Kubernetes TokenReview + SubjectAccessReview | kube-rbac-proxy-style in-process filter | Optional via --metrics-endpoint-auth flag |
| /metrics (BBR) | GET | Kubernetes TokenReview + SubjectAccessReview | kube-rbac-proxy-style in-process filter | Optional via --metrics-endpoint-auth flag |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | CGO_ENABLED=0 (production Dockerfile); no GOEXPERIMENT=strictfipsruntime | Dockerfile:9 |
| **Linking** | Static (CGO disabled) | Dockerfile:9 |
| **OpenSSL in image** | Not present (distroless/static:nonroot base) | Dockerfile:3 |
| **OLM FIPS annotation** | Not present (no OLM bundle) | N/A |

_This is an upstream Kubernetes SIG project using distroless static images with CGO_ENABLED=0. It would not pass check-payload validation as-is. Red Hat downstream builds would need to add Konflux Dockerfiles with GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, and UBI base images for FIPS compliance._

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | Uses crypto/tls with default config for gRPC servers; InsecureSkipVerify=true as default for model server metrics scraping | pkg/bbr/server/runserver.go:59-72, pkg/epp/backend/metrics/metrics.go, pkg/epp/framework/plugins/datalayer/source/http/datasource.go |
| **Crypto libraries** | stdlib crypto/tls, x509 (FIPS via OpenSSL when properly built) | internal/tls/tls.go:34-73 |
| **Certificate handling** | Self-signed RSA 4096-bit certs generated at startup; optional cert-path with fsnotify-based hot reload | internal/tls/tls.go:34-73, pkg/common/certs.go:39-103 |
| **Non-FIPS crypto risks** | InsecureSkipVerify=true default for metrics scraping; no non-FIPS algorithms detected (no MD5, RC4, DES) | pkg/epp/framework/plugins/datalayer/source/metrics/datasource.go |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (upstream branch, no Konflux pipeline) |
| **Language deps** | go.sum | Yes | go mod | go.sum |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | cachi2.env | No | Hermeto (formerly cachi2) | N/A |

_This is an upstream branch (main) from a Kubernetes SIG project. No Konflux Dockerfiles or downstream release infrastructure is present. The downstream RHOAI release branch would be expected to add rpms.lock.yaml, Konflux Dockerfiles, and Hermeto prefetch configuration._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-InferencePool (namespace-scoped) | api/v1/inferencepool_types.go:60-70, PROJECT:11-17 |
| **Deployment model** | per-namespace instance — each InferencePool gets its own EPP deployment | config/charts/inferencepool/values.yaml:1-9 |
| **Tenant identifier** | InferencePool name + namespace; InferenceObjective name for fairness within a pool | api/v1/inferencepool_types.go:30-48, apix/v1alpha2/inferenceobjective_types.go:30-38 |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | RBAC Role scoped to namespace; each EPP ServiceAccount has pod get/watch/list only in its namespace | Kubernetes RBAC | ext-proc gRPC itself has no auth — relies on gateway network-level trust and namespace isolation |
| Data storage | No persistent storage; in-memory datastore per EPP instance | Application | Endpoint state is ephemeral; no cross-pool data leakage risk |
| Network traffic | EPP only watches pods in its own namespace (via NAMESPACE env filter) | Application + Kubernetes | No NetworkPolicy resources defined in Helm charts; namespace isolation depends on cluster-level policies |
| Compute & resources | Per-pool EPP deployment with configurable resource requests/limits (default 4 CPU, 8Gi mem request) | Kubernetes | No ResourceQuota or LimitRange in Helm charts |
| Configuration & secrets | Per-pool ConfigMap for plugin config; per-pool ServiceAccount | Kubernetes | TLS certs are self-signed per instance; no shared secrets across pools |
| API scoping | InferencePool reconciler filters by pool-name and pool-namespace flags; Pod reconciler uses namespace-scoped watches | Application | EPP cannot see resources outside its configured namespace |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| Model server pods (backend) | per-InferencePool via label selector | Pods selected by matchLabels; different pools can share model servers if labels overlap — this is by design for resource efficiency |
| Gateway (Envoy proxy) | per-HTTPRoute | Gateway API HTTPRoute rules determine which traffic reaches which InferencePool; multiple pools can share a gateway |

## Data Flows

### Flow 1: Inference Request Scheduling (EPP)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | Gateway (Envoy) | 443/TCP | HTTPS | TLS | Varies (gateway config) |
| 2 | Gateway (Envoy) | EPP ext-proc | 9002/TCP | gRPC | TLS (optional) | None |
| 3 | EPP | Model server pods | 8000/TCP | HTTP | Configurable | None |
| 4 | EPP | Gateway (Envoy) | -- | gRPC response | -- | -- |
| 5 | Gateway (Envoy) | Selected model server pod | 8000/TCP | HTTP | None | None |
| 6 | Model server pod | Gateway (Envoy) | -- | HTTP response | -- | -- |
| 7 | Gateway (Envoy) | EPP ext-proc (response phase) | 9002/TCP | gRPC | TLS (optional) | None |
| 8 | Gateway (Envoy) | Client | 443/TCP | HTTPS | TLS | -- |

### Flow 2: Body-Based Routing (BBR + EPP)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | Gateway (Envoy) | 443/TCP | HTTPS | TLS | Varies |
| 2 | Gateway (Envoy) | BBR ext-proc | 9004/TCP | gRPC | TLS | None |
| 3 | BBR | Gateway (Envoy) | -- | gRPC response (model name header added) | -- | -- |
| 4 | Gateway (Envoy) | EPP ext-proc | 9002/TCP | gRPC | TLS (optional) | None |
| 5 | EPP | Gateway (Envoy) | -- | gRPC response (endpoint selected) | -- | -- |
| 6 | Gateway (Envoy) | Selected model server pod | 8000/TCP | HTTP | None | None |

### Flow 3: Latency Prediction Feedback Loop

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | EPP (async client) | Training Server (sidecar) | 8000/TCP | HTTP | plaintext | None |
| 2 | Training Server | Training Server (internal) | -- | -- | -- | -- |
| 3 | Prediction Server | Training Server | 8000/TCP | HTTP | plaintext | None |
| 4 | EPP (async client) | Prediction Server (sidecar) | 8001/TCP | HTTP | plaintext | None |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| Envoy Gateway / Istio / GKE Gateway / kgateway | ext-proc gRPC filter | 9002/TCP, 9004/TCP | gRPC | TLS (optional) | Gateway sends traffic through ext-proc filter chain to EPP/BBR for intelligent routing |
| vLLM model server | Prometheus metrics scraping | 8000/TCP | HTTP | Configurable | Scrapes vllm:num_requests_waiting, vllm:num_requests_running, vllm:gpu_cache_usage_perc, vllm:lora_requests_info |
| SGLang model server | Prometheus metrics scraping | 8000/TCP | HTTP | Configurable | Scrapes sglang:num_queue_reqs, sglang:num_running_reqs, sglang:token_usage |
| Triton TensorRT-LLM model server | Prometheus metrics scraping | 8000/TCP | HTTP | Configurable | Scrapes nv_trt_llm_request_metrics, nv_trt_llm_kv_cache_block_metrics |
| trtllm-serve model server | Prometheus metrics scraping | 8000/TCP | HTTP | Configurable | Scrapes trtllm_num_requests_waiting, trtllm_num_requests_running, trtllm_kv_cache_utilization |
| Kubernetes API server | CRD Watch (InferencePool v1) | 443/TCP | HTTPS | TLS | Discovers pool configuration, selector, target ports, EPP reference |
| Kubernetes API server | CRD Watch (InferenceObjective v1alpha2) | 443/TCP | HTTPS | TLS | Tracks priority levels for flow control fairness |
| Kubernetes API server | CRD Watch (InferenceModelRewrite v1alpha2) | 443/TCP | HTTPS | TLS | Watches model name rewrite rules for traffic splitting |
| Kubernetes API server | Pod Watch | 443/TCP | HTTPS | TLS | Discovers and tracks model server pod endpoints matching pool label selector |
| Kubernetes API server | ConfigMap Watch (BBR) | 443/TCP | HTTPS | TLS | Watches ConfigMaps with inference.networking.k8s.io/bbr-managed=true for LoRA adapter mappings |
| Kubernetes API server | Lease (leader election) | 443/TCP | HTTPS | TLS | Coordinates HA leader election when replicas > 1 |
| OpenTelemetry Collector | OTLP trace export | 4317/TCP | gRPC | plaintext | Exports distributed traces for request lifecycle observability |
| Prometheus | ServiceMonitor scrape | 9090/TCP | HTTP | None | Scrapes EPP/BBR metrics (request counts, latency histograms, SLO violations, pool health) |
| llm-d Inference Scheduler | Plugin integration | -- | -- | -- | External scheduler plugin for disaggregated vLLM serving via pluggable EPP framework |

## Architectural Analysis

The Gateway API Inference Extension employs a deeply layered plugin architecture that separates concerns across multiple processing phases. The EPP framework defines distinct plugin types for scheduling (Filter, Scorer, Picker, ProfileHandler), request control (AdmissionPlugin, PrepareDataPlugin, PreRequest, ResponseHeader, ResponseBody), flow control (FairnessPolicy, OrderingPolicy, UsageLimitPolicy, EvictionFilterPolicy, EvictionOrderingPolicy), and data layer (DataSource, EndpointExtractor, K8sNotificationExtractor). This design allows the scheduling algorithm to be fully customized via the EndpointPickerConfig CRD without modifying core code — the same mechanism that enables llm-d to integrate its disaggregated serving scheduler as a plugin.

The data layer subsystem represents a significant architectural evolution. Rather than having the EPP directly scrape model server metrics via hardcoded HTTP clients, the new data layer introduces a pluggable DataSource→Extractor→AttributeMap pipeline. DataSources (HTTP polling, Kubernetes notifications) feed raw data to Extractors that parse it into typed attributes (concurrency, latency, prefix cache state). These attributes are stored in a directed acyclic graph (DAG) with topological ordering that ensures data dependencies are resolved before plugins consume them. This architecture supports heterogeneous model servers and custom metrics without code changes.

The flow control subsystem (behind a feature gate) implements a sophisticated multi-priority queuing system. It uses sharded registries for lock-free hot paths, join-shortest-queue-by-bytes (JSQ-Bytes) distribution, and pluggable fairness/ordering policies. Priority bands are dynamically provisioned via JIT allocation, and each band can have its own fairness policy (global-strict or round-robin) and ordering policy (FCFS, EDF, or SLO-deadline). The eviction system uses a max-min heap for efficient request preemption when capacity is exhausted. This design mirrors production-grade API gateway rate limiting systems but is specialized for inference workloads where request cost (in tokens/bytes) varies dramatically.

The BBR component demonstrates clean separation of the body-parsing concern. By extracting model names from request bodies into headers before the EPP processes the request, the gateway can perform header-based routing (via HTTPRoute rules) without needing to buffer and parse request bodies itself. The BBR plugin framework mirrors the EPP's extensibility — custom request/response processors can be registered via factory functions and configured via CLI flags. The ConfigMap-based LoRA adapter mapping is a practical pattern for environments where adapter-to-base-model relationships change dynamically.

A notable architectural risk is the InsecureSkipVerify=true default for model server metrics scraping. While pragmatic for development environments where model servers don't typically serve TLS, this bypasses certificate validation in production. The configurable nature (via Helm values and plugin parameters) mitigates this, but the default-insecure stance should be documented for security reviews. Similarly, the ext-proc gRPC interface itself has no authentication — security relies entirely on network-level isolation between the gateway and the EPP/BBR pods, typically enforced by Kubernetes namespace boundaries and optional NetworkPolicies.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 768b9ccc | 2026-07-22 | OWNERS: add llm-d-team + RHOAI team |
| 99337eb6 | Recent | docs: finalize flow control ordering policies docs |
| bca878b3 | Recent | docs: finalize flow control fairness policies docs |
| abc557ea | Recent | docs(epp): refactor pickers and add READMEs |
| eecbdc17 | Recent | [pluggable parser] add AppProtocol validation in InferencePool Reconcile loop |
| f523eec0 | Recent | token_estimator: handles Responses and Conversations API |
| 5e05237c | Recent | feat(Flow Control)/Expand Flow Control capacity limits schema (maxRequests) |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| README.md | 1-124 | Purpose, Architecture Components, Integration Points |
| PROJECT | 1-36 | APIs Exposed (CRDs), Metadata |
| go.mod | 1-151 | Dependencies, Metadata (Go version) |
| Dockerfile | 1-38 | Architecture Components (EPP), Security (FIPS), Build Hermeticity |
| bbr.Dockerfile | 1-35 | Architecture Components (BBR), Sub-Component Details |
| Makefile | 1-627 | Architecture Components, Deployment Manifests |
| api/v1/inferencepool_types.go | 1-403 | APIs Exposed (CRDs), Multi-Tenancy |
| api/v1/shared_types.go | 1-144 | APIs Exposed (CRDs) |
| apix/v1alpha2/inferenceobjective_types.go | 1-122 | APIs Exposed (CRDs), Multi-Tenancy |
| apix/v1alpha2/inferencemodelrewrite_types.go | 1-200 | APIs Exposed (CRDs) |
| apix/v1alpha1/inferencepoolimport_types.go | 1-144 | APIs Exposed (CRDs) |
| apix/config/v1alpha1/endpointpickerconfig_types.go | 1-422 | APIs Exposed (CRDs), Configuration |
| cmd/epp/main.go | 27-33 | Architecture Components (EPP entry point) |
| cmd/epp/runner/runner.go | 87-590 | Configuration, Architecture Components, Data Flows |
| cmd/epp/runner/health.go | 33-153 | Health & Metrics, gRPC Services |
| pkg/epp/server/runserver.go | 50-288 | Network Architecture, Security (TLS), gRPC Services |
| pkg/epp/server/options.go | 93-191 | Configuration |
| pkg/epp/server/controller_manager.go | 1-100 | Architecture Components (controller setup) |
| pkg/epp/controller/inferencepool_reconciler.go | 42-72 | Integration Points, Data Flows |
| pkg/epp/controller/inferenceobjective_reconciler.go | 42-69 | Integration Points |
| pkg/epp/controller/inferencemodelrewrite_reconciler.go | 42-74 | Integration Points |
| pkg/epp/controller/pod_reconciler.go | 41-62 | Integration Points, Network Architecture |
| pkg/epp/handlers/server.go | 37-328 | gRPC Services, Data Flows |
| pkg/epp/handlers/request.go | 75-100 | Data Flows |
| pkg/epp/handlers/response.go | 44-88 | Data Flows |
| pkg/epp/handlers/parsers.go | 1-50 | Architecture Components |
| pkg/epp/metrics/metrics.go | 83-273 | HTTP Endpoints, Health & Metrics |
| pkg/epp/metrics/collectors/inference_pool.go | 47-79 | Health & Metrics |
| pkg/epp/config/loader/configloader.go | 66-161 | Configuration |
| pkg/epp/config/loader/defaults.go | 46-103 | Configuration |
| pkg/epp/config/loader/validation.go | 29-40 | Configuration |
| internal/tls/tls.go | 34-73 | Security (TLS, FIPS) |
| internal/runnable/grpc.go | 1-50 | Architecture Components |
| cmd/bbr/main.go | 27-30 | Architecture Components (BBR entry point) |
| cmd/bbr/runner/runner.go | 87-266 | Configuration, Network Architecture |
| cmd/bbr/runner/health.go | 33-61 | Health & Metrics |
| pkg/bbr/server/runserver.go | 45-72 | Network Architecture, Security (TLS) |
| pkg/bbr/server/options.go | 28-96 | Configuration |
| pkg/bbr/handlers/server.go | 73-181 | gRPC Services, Data Flows |
| pkg/bbr/handlers/request.go | 38-138 | Data Flows |
| pkg/bbr/handlers/response.go | 37-151 | Data Flows |
| pkg/bbr/framework/plugins.go | 29-41 | Architectural Analysis |
| pkg/bbr/framework/registry.go | 23-33 | Architectural Analysis |
| pkg/bbr/metrics/metrics.go | 34-122 | Health & Metrics |
| pkg/bbr/plugins/basemodelextractor/configmap_reconciler.go | 58-80 | Integration Points |
| pkg/bbr/config/spec.go | 25-70 | Configuration |
| pkg/common/certs.go | 39-103 | Security (TLS) |
| pkg/common/envoy/chunking.go | 29-74 | Architectural Analysis |
| pkg/common/observability/tracing/telemetry.go | 46-140 | Integration Points (OpenTelemetry) |
| pkg/common/observability/profiling/pprof.go | 28-47 | HTTP Endpoints |
| pkg/common/observability/logging/options.go | 32-86 | Configuration |
| pkg/common/error/error.go | 27-99 | Architectural Analysis |
| pkg/epp/scheduling/scheduler.go | 1-100 | Architectural Analysis |
| pkg/epp/scheduling/scheduler_config.go | 1-50 | Architectural Analysis |
| pkg/epp/scheduling/scheduler_profile.go | 1-100 | Architectural Analysis |
| pkg/epp/requestcontrol/director.go | 1-100 | Architectural Analysis |
| pkg/epp/datastore/datastore.go | 1-100 | Multi-Tenancy |
| pkg/epp/datastore/modelrewritestore.go | 1-100 | Architectural Analysis |
| pkg/epp/datalayer/runtime.go | 1-100 | Architectural Analysis |
| pkg/epp/datalayer/data_graph.go | 1-100 | Architectural Analysis |
| pkg/epp/flowcontrol/controller/controller.go | 1-100 | Architectural Analysis |
| pkg/epp/flowcontrol/registry/registry.go | 1-100 | Architectural Analysis |
| pkg/epp/flowcontrol/registry/shard.go | 1-100 | Architectural Analysis |
| pkg/epp/backend/metrics/metrics.go | 1-100 | Integration Points, Egress |
| pkg/epp/framework/interface/plugin/plugins.go | 1-100 | Architectural Analysis |
| pkg/epp/framework/interface/scheduling/plugins.go | 1-100 | Architectural Analysis |
| sidecars/latencypredictorasync/latencypredictor_async.go | 38-137 | Sub-Component Details |
| sidecars/latencypredictorasync/prediction.go | 32-322 | Sub-Component Details |
| sidecars/latencypredictorasync/training.go | 31-410 | Sub-Component Details |
| sidecars/latencypredictorasync/coalescer.go | 53-225 | Sub-Component Details |
| sidecars/latencypredictorasync/metrics.go | 31-169 | Sub-Component Details |
| sidecars/latencypredictorasync/types.go | 29-152 | Configuration |
| latencypredictor/requirements.txt | 1-13 | Dependencies |
| latencypredictor/Dockerfile-training | 1-20 | Sub-Component Details |
| latencypredictor/Dockerfile-prediction | 1-20 | Sub-Component Details |
| version/version.go | 20-33 | Metadata |
| config/crd/kustomization.yaml | 1-22 | Deployment Manifests |
| config/charts/inferencepool/values.yaml | 1-160 | Deployment Manifests, Configuration |
| config/charts/epplib/values.yaml | 1-90 | Deployment Manifests, Configuration |
| config/charts/epplib/templates/_rbac.yaml | 1-76 | Security (RBAC) |
| config/charts/epplib/templates/_deployment.yaml | 1-248 | Network Architecture, Configuration |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| sigs.k8s.io/controller-runtime | internal/runnable/grpc.go, test/*, conformance/* | Architecture Components, Dependencies |
| GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED | Dockerfile, Makefile, sidecars/*/Dockerfile | Security (FIPS) |
| tls\.Config\|CipherSuites\|MinVersion\|InsecureSkipVerify | pkg/bbr/server/runserver.go, pkg/epp/backend/metrics/metrics.go, pkg/epp/framework/plugins/datalayer/source/http/datasource.go, pkg/epp/server/runserver.go | Security (FIPS, TLS) |
| crypto/md5\|crypto/rc4\|crypto/des | (no matches) | Security (FIPS) |
| *Dockerfile*konflux* | (no matches) | Architecture Components (Konflux) |
| rpms.lock.yaml\|go.sum\|uv.lock\|artifacts.lock.yaml | go.sum, conformance/go.sum | Security (Build Hermeticity) |
| find . -name "*.go" (non-test) | 200+ files | All code analysis sections |

### Summary

- **Total files read**: 78
- **Total lines referenced**: ~8,500
- **Coverage**: All sections have direct source file backing. CRDs, Network Architecture, Security, Integration Points, Configuration, and Architectural Analysis are fully sourced from code. Multi-Tenancy is informed by CRD scope definitions and controller namespace filtering patterns. Provenance is based on local_analysis (no component-map.json available). FIPS section documents upstream defaults which would need modification for RHOAI downstream builds.

---
*Generated in 8m 3s (483s total)*
