┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Bearer Token (Gateway-enforced)                              │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway Layer)                                                     │
│                                                                              │
│  [Envoy-based Gateway (Envoy Gateway / Istio / GKE / nginx)]                │
│  - Port: 443/TCP (external) → internal routing                              │
│  - TLS Termination: TLS 1.2+                                                │
│  - AuthN: Gateway-enforced (Bearer Token validation)                        │
│  - HTTPRoute → ExtensionRef → InferencePool                                 │
│         │                              │                                     │
│         │ EPP Path                     │ BBR Path                            │
│         │ Port: 9002/TCP               │ Port: 9004/TCP                      │
│         │ Protocol: gRPC               │ Protocol: gRPC                      │
│         │ Encryption: TLS              │ Encryption: TLS                     │
│         │   (self-signed or provided)  │   (self-signed or provided)         │
│         │ Auth: None (delegated        │ Auth: None (delegated               │
│         │   to Gateway)                │   to Gateway)                       │
│         ▼                              ▼                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│  APPLICATION LAYER (Internal - ClusterIP Services)                           │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │  Endpoint Picker Proxy (EPP)                            │                 │
│  │  - Namespace: {pool-namespace}                          │                 │
│  │  - ServiceAccount: {release-name}                       │                 │
│  │  - Image: gateway-api-inference-extension/epp           │                 │
│  │  - Ports:                                               │                 │
│  │    - 9002/TCP: gRPC ext-proc (TLS, self-signed)         │                 │
│  │    - 9003/TCP: gRPC health (plaintext)                  │                 │
│  │    - 9090/TCP: HTTP metrics (optional TokenReview auth) │                 │
│  │  - Strategy: Recreate (stateful in-memory caches)       │                 │
│  │  - HA: Leader election via Kubernetes Leases            │                 │
│  │  - Termination Grace: 130s                              │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│         │                                                                    │
│         │ Optional Sidecars (same Pod):                                      │
│         │                                                                    │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │  Latency Training Server (sidecar)                      │                 │
│  │  - Port: 8000/TCP HTTP (plaintext, pod-local)           │                 │
│  │  - Language: Python                                     │                 │
│  │  - Auth: None                                           │                 │
│  │  - Models: Bayesian Ridge, XGBoost, LightGBM            │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │  Latency Prediction Server (sidecar)                    │                 │
│  │  - Port: 8001+/TCP HTTP (plaintext, pod-local)          │                 │
│  │  - Language: Python (28 uvicorn workers)                 │                 │
│  │  - Auth: None                                           │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │  Body-Based Router (BBR) - Separate Deployment          │                 │
│  │  - Namespace: {namespace}                               │                 │
│  │  - ServiceAccount: {release-name}                       │                 │
│  │  - Image: gateway-api-inference-extension/bbr           │                 │
│  │  - Ports:                                               │                 │
│  │    - 9004/TCP: gRPC ext-proc (TLS, self-signed)         │                 │
│  │    - 9005/TCP: gRPC health (plaintext)                  │                 │
│  │    - 9090/TCP: HTTP metrics (optional TokenReview auth) │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│         │                                                                    │
│         │ Metrics Scraping (EPP → Model Servers)                             │
│         │ Port: Configurable (default: container port)                       │
│         │ Protocol: HTTP/HTTPS                                               │
│         │ Encryption: Configurable (InsecureSkipVerify=true by default)      │
│         │ Auth: None                                                         │
│         │ Interval: 50ms (configurable)                                      │
│         ▼                                                                    │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │  Model Server Pods                                      │                 │
│  │  - Types: vLLM, SGLang, TensorRT-LLM, trtllm-serve     │                 │
│  │  - Port: 8000/TCP (default) HTTP/gRPC                   │                 │
│  │  - Metrics: /metrics (Prometheus format)                │                 │
│  │    - queue_depth, kv_cache_usage, running_requests      │                 │
│  │    - lora_adapter_info, prefix_cache_stats              │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│         │                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CONTROL PLANE                                                               │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS (REST/HTTP2)                                              │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: ServiceAccount Token (Bearer)                                       │
│  - EPP watches: InferencePool, InferenceObjective, InferenceModelRewrite,    │
│    InferencePoolImport, Pods                                                 │
│  - BBR watches: ConfigMaps (bbr-managed label)                               │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY (Optional)                                                    │
│                                                                              │
│  [Prometheus / ServiceMonitor]                                               │
│  - Scrapes: 9090/TCP HTTP                                                    │
│  - Auth: Optional (TokenReview + SubjectAccessReview)                        │
│                                                                              │
│  [OTLP Collector]                                                            │
│  - Port: 4317/TCP                                                            │
│  - Protocol: gRPC                                                            │
│  - Encryption: plaintext                                                     │
│  - Auth: None                                                                │
│  - Purpose: Distributed tracing export                                       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  ClusterRole: {release-name}-metrics-reader (conditional)
  - authentication.k8s.io / tokenreviews: create
  - authorization.k8s.io / subjectaccessreviews: create
  - Non-resource: /metrics: get

  Role (namespace-scoped): {release-name}-sa
  - core / pods: get, watch, list
  - core / configmaps: get, watch, list (BBR)

  Role (namespace-scoped): {release-name}-leader-election (conditional)
  - coordination.k8s.io / leases: get, list, watch, create, update, patch, delete
  - core / events: create, patch

  CRD Access (via controller-runtime scheme registration):
  - inference.networking.k8s.io / InferencePool: full CRUD
  - inference.networking.x-k8s.io / InferenceObjective: full CRUD
  - inference.networking.x-k8s.io / InferenceModelRewrite: full CRUD
  - inference.networking.x-k8s.io / InferencePoolImport: full CRUD

Secrets:

  inference-gateway-sa-metrics-reader-secret (conditional)
  - Type: kubernetes.io/service-account-token
  - Purpose: Prometheus metrics endpoint authentication
  - Provisioned by: Kubernetes ServiceAccount token controller
  - Auto-rotate: Yes

  Self-signed TLS cert (in-memory, not a K8s Secret)
  - Purpose: gRPC server TLS when no --cert-path provided
  - Generated: At startup (10-year validity)
  - Auto-rotate: No (but supports --enable-cert-reload for file-based certs)

FIPS Compliance:

  CURRENT STATE (upstream): NOT FIPS-compliant
  - CGO_ENABLED=0 (statically linked, no OpenSSL)
  - Base image: distroless/static:nonroot (no OpenSSL)
  - Would NOT pass check-payload validation

  REQUIRED FOR RHOAI:
  - CGO_ENABLED=1 with GOEXPERIMENT=strictfipsruntime
  - UBI base image with OpenSSL
  - Konflux Dockerfiles with Hermeto prefetch

TLS Configuration:

  EPP gRPC (9002/TCP):
  - Server: TLS (self-signed or provided via --cert-path)
  - Cipher: Go stdlib crypto/tls defaults
  - Client verification: None

  BBR gRPC (9004/TCP):
  - Server: TLS (self-signed or provided via --cert-path)
  - Cipher: Go stdlib crypto/tls defaults
  - Client verification: None

  Health endpoints (9003/9005/TCP):
  - plaintext (no TLS)

  Model Server metrics scraping:
  - InsecureSkipVerify=true by default
  - Configurable via --model-server-metrics-scheme

  Kubernetes API:
  - TLS 1.2+ (in-cluster config)
  - Auth: ServiceAccount Token (Bearer)

  OTLP Collector (4317/TCP):
  - plaintext gRPC

  Latency sidecars (8000/8001+/TCP):
  - plaintext HTTP (pod-local, localhost only)
