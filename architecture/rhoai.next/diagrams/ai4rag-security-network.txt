┌─────────────────────────────────────────────────────────────────────────┐
│  CALLING CONTEXT (Trusted)                                              │
│                                                                          │
│  [KFP Pipeline Component / Python Script]                                │
│  - Runtime: Python 3.x process                                           │
│  - Identity: Process owner (pod ServiceAccount in KFP)                   │
│         │                                                                │
│         │ Interaction: In-process Python API call                         │
│         │ Encryption: N/A (same process)                                  │
│         │ Auth: N/A                                                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  ai4rag LIBRARY (In-Process)                                             │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  AI4RAGExperiment                                                │    │
│  │  ├── ModelsPreSelector (pre-selects top models)                  │    │
│  │  ├── GAMOptimizer / RandomOptimizer (HPO loop)                   │    │
│  │  ├── SimpleRAGTemplate (prompt + retrieve + generate)            │    │
│  │  ├── LangChainChunker (document splitting)                       │    │
│  │  └── UnitxtEvaluator (metrics: faithfulness, correctness)        │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│         │                              │                                  │
│         │ (Remote API calls)           │ (Local in-process)               │
│         │                              │                                  │
│         │                              ▼                                  │
│         │                    ┌──────────────────┐                        │
│         │                    │ ChromaDB           │                       │
│         │                    │ (In-Memory)        │                       │
│         │                    │ Port: N/A          │                       │
│         │                    │ Protocol: N/A      │                       │
│         │                    │ Encryption: N/A    │                       │
│         │                    │ Auth: None          │                      │
│         │                    │ Purpose: MPS local  │                      │
│         │                    │   vector store      │                      │
│         │                    └──────────────────┘                        │
│         │                                                                │
│         │ Port: Configurable (user-defined)                              │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (delegated to ogx-client/httpx)                │
│         │ Auth: API Key (Bearer token)                                    │
│         │ Credential Source: User-provided (env var or constructor)       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [OGX Server]                                                            │
│  - Purpose: Foundation model inference, embedding generation,            │
│             vector store operations (Milvus, Qdrant backends)            │
│  - Endpoints consumed:                                                   │
│    • POST /chat/completions      — Foundation model inference            │
│    • POST /embeddings            — Text embedding generation             │
│    • POST /vector_stores         — Create vector store instances         │
│    • POST /vector_io/insert      — Insert document chunks                │
│    • POST /vector_io/query       — Similarity/hybrid search             │
│    • POST /vector_stores/search  — Vector store search (v1.0+)           │
│    • GET  /models                — List available models                 │
│  - Port: Configurable                                                    │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS (transport via ogx-client → httpx)                    │
│  - Auth: API Key (Bearer token in Authorization header)                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK SUMMARY:
================

Ingress:
  None — ai4rag is a Python library, not a network service.
  It does not listen on any ports or accept incoming connections.

Egress:
  ┌───────────────────┬──────────────┬──────────┬────────────┬──────────────────┐
  │ Destination       │ Port         │ Protocol │ Encryption │ Auth             │
  ├───────────────────┼──────────────┼──────────┼────────────┼──────────────────┤
  │ OGX Server        │ Configurable │ HTTPS    │ TLS        │ API Key (Bearer) │
  │ ChromaDB (local)  │ In-process   │ N/A      │ N/A        │ None             │
  └───────────────────┴──────────────┴──────────┴────────────┴──────────────────┘

RBAC Summary:
  No Kubernetes RBAC — ai4rag is a Python library with no cluster permissions.
  When running inside a KFP pipeline, RBAC is inherited from the pipeline
  component's pod ServiceAccount.

Secrets:
  ┌──────────────────┬──────────────┬──────────────────────────────────────────┐
  │ Secret           │ Type         │ Purpose                                  │
  ├──────────────────┼──────────────┼──────────────────────────────────────────┤
  │ OGX API key      │ API key str  │ Auth with OGX server for model           │
  │                  │              │ inference and vector store access         │
  │                  │              │ Provisioned by: User (env var or ctor)    │
  │                  │              │ Auto-Rotate: No                           │
  └──────────────────┴──────────────┴──────────────────────────────────────────┘

TLS Configuration:
  - ai4rag does NOT configure TLS directly
  - TLS is delegated to ogx-client library (built on httpx)
  - Certificate handling: Inherits system trust store via httpx defaults
  - No custom certificate pinning or TLS version enforcement in ai4rag code

Crypto Notes:
  - hashlib.sha256 used for document deduplication (NOT security-relevant)
  - No direct crypto library usage — all transport encryption via ogx-client/httpx
  - No FIPS compliance concerns specific to ai4rag (pure Python library)

Build Hermeticity:
  - No container image — distributed as Python package (PyPI)
  - Language deps locked: uv.lock (658,783 bytes)
  - No RPM-level or artifact-level locks (no Dockerfile)
