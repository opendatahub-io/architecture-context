┌─────────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                           │
│                                                                                  │
│  Data Scientists, API Consumers, CI/CD Systems                                   │
│         │                                                                        │
├─────────┼────────────────────────────────────────────────────────────────────────┤
│  INGRESS│(DMZ) — 26 OpenShift Routes + 21 HTTPRoutes via Gateway API            │
│         │                                                                        │
│         ├─→ ODH Dashboard                                                        │
│         │   Route: dashboard-redhat-ods-applications.apps.cluster.com            │
│         │   Port: 443/TCP (ext) → 8080/TCP (int)                                │
│         │   Protocol: HTTPS, Encryption: TLS (cluster security profile)          │
│         │   Auth: OAuth2/OIDC via kube-auth-proxy (supports OpenShift OAuth,     │
│         │         external OIDC, and auth bypass modes)                           │
│         │                                                                        │
│         ├─→ KServe LLMInferenceService Gateway                                   │
│         │   HTTPRoute via data-science-gateway (Envoy)                            │
│         │   Port: 443/TCP (ext) → InferencePool → EPP → vLLM pod                │
│         │   Protocol: HTTPS, Encryption: TLS                                     │
│         │   Auth: Kuadrant AuthPolicy (per-route); optional Bearer Token          │
│         │                                                                        │
│         ├─→ MaaS Gateway (Models as a Service)                                   │
│         │   HTTPRoute via data-science-gateway (Envoy)                            │
│         │   Port: 443/TCP                                                        │
│         │   Protocol: HTTPS, Encryption: TLS                                     │
│         │   Auth: API Key (SHA-256 hashed) validated via Authorino HTTP callback  │
│         │         + Kuadrant AuthPolicy/TokenRateLimitPolicy per tenant           │
│         │                                                                        │
│         ├─→ Notebook Routes (per-user)                                           │
│         │   Route: notebook-*.apps.cluster.com                                   │
│         │   Port: 443/TCP (ext) → 8888/TCP (int)                                │
│         │   Protocol: HTTPS, Encryption: edge TLS                                │
│         │   Auth: OAuth proxy sidecar                                            │
│         │                                                                        │
│         └─→ Data Science Pipelines UI                                            │
│             Route: ds-pipeline-*.apps.cluster.com                                │
│             Port: 443/TCP (ext) → 8888/TCP (int)                                │
│             Protocol: HTTPS, Encryption: edge TLS                                │
│             Auth: kube-rbac-proxy (SubjectAccessReview)                           │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-operator / opendatahub-operators)                │
│                                                                                  │
│  ├─→ rhods-operator                                                              │
│  │   ServiceAccount: rhods-operator                                              │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token) — CRD/CR lifecycle                │
│  │   ├─→ Webhook Server (9443/TCP, TLS — cert from cluster profile)             │
│  │   └─→ Metrics (8443/TCP, kube-rbac-proxy)                                    │
│  │                                                                               │
│  ├─→ AI Gateway Operator (Module Operator)                                       │
│  │   ServiceAccount: ai-gateway-operator                                         │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                                   │
│  │   └─→ Version handshake ConfigMap (reads/writes)                              │
│  │                                                                               │
│  ├─→ MLflow Operator (Module Operator)                                           │
│  │   ServiceAccount: mlflow-operator                                             │
│  │   └─→ Kubernetes API (HTTPS/443, SA token)                                   │
│  │                                                                               │
│  └─→ MCP Lifecycle Operator (Module Operator)                                    │
│      ServiceAccount: mcp-lifecycle-operator                                      │
│      └─→ Kubernetes API (HTTPS/443, SA token)                                   │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM NAMESPACE (redhat-ods-applications / opendatahub)                      │
│                                                                                  │
│  ├─→ odh-dashboard (up to 10 containers in sidecar mode)                         │
│  │   ServiceAccount: odh-dashboard (shell), odh-dashboard-modules (BFFs)         │
│  │   ├─→ KServe (HTTP, K8s API)                                                 │
│  │   ├─→ Model Registry (HTTP)                                                   │
│  │   ├─→ Kueue (K8s API)                                                        │
│  │   ├─→ Kubernetes API (HTTPS/443, projected SA token)                          │
│  │   ├─→ Thanos Querier in openshift-monitoring (metrics proxy)                  │
│  │   └─→ kube-rbac-proxy sidecar (SubjectAccessReview, TLS)                     │
│  │                                                                               │
│  ├─→ KServe Controller                                                           │
│  │   ServiceAccount: kserve-controller-manager                                   │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                                   │
│  │   ├─→ Webhook Server (9443/TCP, TLS — cert-manager or OCP serving cert)      │
│  │   ├─→ Metrics (8443/TCP, kube-rbac-proxy)                                    │
│  │   └─→ Gateway API (HTTPRoute, InferencePool CRs)                             │
│  │                                                                               │
│  ├─→ llmisvc-controller                                                          │
│  │   ServiceAccount: kserve-controller-manager                                   │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                                   │
│  │   └─→ Creates Deployment, InferencePool, HTTPRoute, LeaderWorkerSet           │
│  │                                                                               │
│  ├─→ odh-model-controller                                                        │
│  │   ServiceAccount: odh-model-controller                                        │
│  │   └─→ KServe InferenceService global config (reads)                           │
│  │                                                                               │
│  ├─→ kube-auth-proxy (Platform Gateway Auth)                                     │
│  │   ├─→ OpenShift OAuth server (token validation)                               │
│  │   └─→ Backend applications (proxied requests)                                 │
│  │                                                                               │
│  └─→ Monitoring Stack                                                            │
│      ├─→ Prometheus (MonitoringStack CR)                                         │
│      │   Scrapes all /metrics endpoints via kube-rbac-proxy or direct            │
│      ├─→ Tempo (distributed tracing, OTLP receiver)                              │
│      ├─→ OpenTelemetry Collector (auto-instrumentation)                          │
│      └─→ Perses (dashboard UI)                                                   │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  MaaS INFRASTRUCTURE NAMESPACE (odh-ai-gateway-infra)                            │
│                                                                                  │
│  ├─→ maas-api Deployments                                                        │
│  │   ServiceAccount: maas-controller                                             │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                                   │
│  │   └─→ API key validation endpoint (HTTP callback for Authorino)               │
│  │                                                                               │
│  ├─→ ai-gateway-payload-processing (Envoy ext_proc)                              │
│  │   Two-stage pipeline: pre-auth and post-auth                                  │
│  │   ├─→ Pre-auth: extract model name, set X-Gateway-Model-Name header           │
│  │   ├─→ Post-auth: API translation (OpenAI ↔ native), credential injection     │
│  │   └─→ External LLM providers (HTTPS/443, injected credentials)                │
│  │                                                                               │
│  └─→ Authorino                                                                   │
│      ├─→ maas-api (HTTP callback for API key validation)                         │
│      └─→ Kuadrant AuthPolicy/TokenRateLimitPolicy enforcement                   │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  USER PROJECT NAMESPACES (per data science project)                              │
│                                                                                  │
│  ├─→ Jupyter Workbenches (Notebook CRs)                                          │
│  │   Per-user ServiceAccount                                                     │
│  │   ├─→ S3/Object Storage (HTTPS/443, AWS IAM / static creds)                  │
│  │   ├─→ Data Science Pipelines (connection secret mounted)                      │
│  │   └─→ Feast client ConfigMaps (injected by Feast operator)                    │
│  │                                                                               │
│  ├─→ InferenceServices (KServe — vLLM, OVMS, Caikit, MLServer)                  │
│  │   Per-model ServiceAccount                                                    │
│  │   ├─→ S3/Object Storage (HTTPS/443, model artifacts)                          │
│  │   ├─→ Container Registry (HTTPS/443, runtime images)                          │
│  │   ├─→ Metrics endpoint (/metrics, direct or kube-rbac-proxy)                  │
│  │   └─→ Istio sidecar (mTLS, where mesh enabled)                               │
│  │                                                                               │
│  ├─→ LLMInferenceServices (KServe + EPP + Gateway API)                           │
│  │   Per-model ServiceAccount                                                    │
│  │   ├─→ S3/HuggingFace (HTTPS/443, model download via storage-initializer)     │
│  │   ├─→ Container Registry (HTTPS/443, runtime images)                          │
│  │   ├─→ Gateway API Inference Extension EPP (metrics scraping)                  │
│  │   └─→ EnvoyGW (HTTPRoute → InferencePool → EPP → vLLM pod)                  │
│  │                                                                               │
│  ├─→ Training Jobs (PyTorchJob, TrainJob, RayCluster, SparkApplication)          │
│  │   Per-job ServiceAccount                                                      │
│  │   ├─→ S3/Object Storage (HTTPS/443, training data and checkpoints)            │
│  │   ├─→ Kueue (ClusterQueue/LocalQueue admission)                               │
│  │   └─→ Inter-worker communication (NCCL, MPI — internal pod network)           │
│  │                                                                               │
│  ├─→ Pipeline Runs (Argo Workflows)                                              │
│  │   ServiceAccount: pipeline-runner                                              │
│  │   ├─→ ML Metadata store (gRPC, pipeline artifacts/lineage)                   │
│  │   ├─→ S3/Object Storage (HTTPS/443, artifacts)                                │
│  │   └─→ Model Registry (gRPC, model registration)                               │
│  │                                                                               │
│  └─→ Feast Feature Stores                                                        │
│      ├─→ Offline store (Spark-based)                                             │
│      └─→ Online store (Redis/SQLite)                                             │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH CONTROL PLANE (istio-system / openshift-gateway)                   │
│                                                                                  │
│  ├─→ Istio Control Plane                                                         │
│  │   mTLS on serving paths, MaaS gateway, Caikit-TGIS                           │
│  │   DestinationRules for TLS origination                                        │
│  │                                                                               │
│  └─→ Envoy Gateway (data-science-gateway)                                        │
│      GatewayClass → Gateway → Envoy proxy                                        │
│      EnvoyFilter (ext_authz + Lua) → kube-auth-proxy → backend                  │
│      Supports OpenShift Route and LoadBalancer ingress modes                      │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER MONITORING (openshift-monitoring)                                       │
│                                                                                  │
│  ├─→ Prometheus (cluster) — federation from platform Prometheus                  │
│  └─→ Thanos Querier — dashboard metrics proxy endpoint                           │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External Services)                                                      │
│                                                                                  │
│  S3 / Object Storage (s3.amazonaws.com, MinIO, etc.)                             │
│  - Purpose: Model artifacts, training data, pipeline artifacts, feature data     │
│  - Auth: AWS IAM (IRSA or static credentials)                                   │
│  - Encryption: TLS 1.2+                                                          │
│  - Components: KServe, workbenches, pipelines, Model Registry, Feast             │
│                                                                                  │
│  Container Registry (quay.io, registry.redhat.io)                                │
│  - Purpose: Pull runtime images, serving runtime images                          │
│  - Auth: Pull secrets (ImagePullSecrets)                                         │
│  - Encryption: TLS 1.2+                                                          │
│  - Components: KServe, workbenches, training jobs, vLLM                          │
│                                                                                  │
│  HuggingFace Hub (huggingface.co)                                                │
│  - Purpose: Model download for LLMInferenceService                               │
│  - Auth: API Token                                                               │
│  - Encryption: TLS 1.2+                                                          │
│  - Components: KServe storage-initializer                                        │
│                                                                                  │
│  External LLM Providers (OpenAI, Azure OpenAI, etc.)                             │
│  - Purpose: Proxied model inference for MaaS tenants                             │
│  - Auth: API keys (injected by ai-gateway-payload-processing)                    │
│  - Encryption: TLS 1.2+                                                          │
│  - Components: MaaS (ai-gateway-payload-processing)                              │
│                                                                                  │
│  Kubernetes API Server (apiserver.cluster.local)                                 │
│  - Purpose: CRD/CR lifecycle, watch, RBAC enforcement                            │
│  - Auth: ServiceAccount tokens (projected)                                       │
│  - Encryption: TLS 1.2+                                                          │
│  - Components: All operators, controllers, dashboard                             │
└──────────────────────────────────────────────────────────────────────────────────┘

NAMESPACES:
  - redhat-ods-operator:           Platform operator + module operators
  - redhat-ods-applications:       Platform services (dashboard, controllers, monitoring)
  - odh-ai-gateway-infra:          MaaS API and payload processing (isolated)
  - User project namespaces:       Per-user workloads (notebooks, models, training, pipelines)
  - knative-serving (optional):    Serverless inference (KServe serverless mode)
  - istio-system / openshift-gateway: Service mesh control plane, Envoy Gateway
  - openshift-monitoring:          Cluster Prometheus, Thanos Querier

NETWORK POLICY SUMMARY:
  - 32 NetworkPolicies across 12 components
  - modelmesh-serving: 5 policies
  - model-registry-operator: 4 policies
  - models-as-a-service: 3 policies
  - ai-gateway-operator, kserve, data-science-pipelines: 1-2 policies each

INTERNAL SERVICE MESH:
  - 175 ClusterIP services
  - 583 HTTP/HTTPS endpoints
  - 91 gRPC service definitions across 15+ components
  - mTLS: Istio mesh-level (where enabled) — KServe serving paths, MaaS, Caikit-TGIS
  - Note: mTLS is MIXED — not mesh-wide STRICT mode

AUTHENTICATION SUMMARY:
  1. kube-rbac-proxy (SubjectAccessReview) — 8+ components (metrics/API protection)
  2. OAuth2/OIDC via kube-auth-proxy — platform gateway (dashboard, gateway-routed services)
  3. Kuadrant AuthPolicy + TokenRateLimitPolicy — MaaS, KServe LLMISvc (per-route)
  4. API Key (SHA-256 hashed) — MaaS batch-gateway, maas-api (per-tenant)
  5. Bearer token (ServiceAccount) — all controllers to K8s API
  6. mTLS (Istio) — serving paths, MaaS gateway, Caikit-TGIS
  7. SA token isolation — odh-dashboard pod (separate tokens for shell vs BFF modules)

RBAC SUMMARY:
  - 808 RBAC roles across all components
  - 175 CRDs across 41 API groups
  - Top privileged roles:
    * kserve-manager-role: serving.kserve.io/*, core/services, apps/deployments
    * rhods-operator: components.platform.opendatahub.io/*, datascienceclusters/*
    * kueue-manager-role: kueue.x-k8s.io/*, batch/jobs
    * ai-gateway-operator: maas.opendatahub.io/*, gateway.networking.k8s.io/*
    * odh-dashboard-role: opendatahub.io/*, monitoring.coreos.com/servicemonitors
