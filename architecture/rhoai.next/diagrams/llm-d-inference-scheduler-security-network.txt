┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client]                                                          │
│         │                                                                   │
│         │ Port: 80/443/TCP                                                  │
│         │ Protocol: HTTP/HTTPS                                              │
│         │ Encryption: TLS (at gateway)                                      │
│         │ Auth: Bearer Token (application-level)                            │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway)                                                          │
│                                                                             │
│  [Envoy/Istio Inference Gateway]                                            │
│  - Type: Gateway API HTTPRoute                                              │
│  - Port: 80/TCP (external) → backend (internal)                            │
│  - TLS Termination: At gateway (configurable)                               │
│  - Routing: HTTPRoute → InferencePool → EPP ext-proc                       │
│         │                                                                   │
│         ├──────────────────────────────────────────┐                        │
│         │ Port: 9002/TCP                           │                        │
│         │ Protocol: gRPC/HTTP2 (h2c)               │                        │
│         │ Encryption: TLS (self-signed or mounted)  │                        │
│         │ Auth: None (TLS identity only)            │                        │
│         ▼                                           │                        │
├──────────────────────────────────────────────────────────────────────────── ┤
│  EPP SERVICE (Endpoint Picker)                                              │
│                                                                             │
│  [Endpoint Picker - ext-proc gRPC Service]                                  │
│  - Namespace: ${pool-namespace}                                             │
│  - ServiceAccount: ${EPP_NAME}                                              │
│  - Image: odh-llm-d-inference-scheduler                                     │
│  - Ports:                                                                   │
│    - 9002/TCP: gRPC ext-proc (TLS, self-signed/mounted)                    │
│    - 9003/TCP: gRPC health check (plaintext)                               │
│    - 9090/TCP: Prometheus metrics (HTTP, plaintext)                        │
│    - 5557/TCP: ZMQ KV-cache events (TCP, plaintext)                        │
│                                                                             │
│  - FIPS Note: EPP missing -tags strictfipsruntime in Dockerfile.epp.konflux │
│    May use pure-Go crypto instead of OpenSSL-backed FIPS module             │
│                                                                             │
│         │                          │                    │                    │
│         │ Port: 443/TCP            │ Port: var/TCP      │ Port: 5557/TCP    │
│         │ Protocol: HTTPS          │ Protocol: HTTP     │ Protocol: ZMQ     │
│         │ Encryption: TLS 1.2+     │ Encryption: None   │ Encryption: None  │
│         │ Auth: SA Token           │ Auth: None         │ Auth: None        │
│         ▼                          ▼                    ▼                    │
│  [K8s API Server]          [Model Server /metrics]  [KV-Cache Events]       │
│                                                                             │
│         └───────────── EPP returns ext-proc response ──────────┐            │
│                        (target endpoint + headers)             │            │
│                                                                ▼            │
│  ┌────────────────────────────── (Gateway forwards to target) ──────────┐   │
│  │                                                                      │   │
│  ▼                                                                      │   │
├─────────────────────────────────────────────────────────────────────────────┤
│  PD-SIDECAR (per model server pod) — Only in disaggregated P/D mode        │
│                                                                             │
│  [PD-Sidecar - HTTP Reverse Proxy]                                          │
│  - Port: 8000/TCP (HTTP/HTTPS, TLS optional)                               │
│  - Image: odh-llm-d-routing-sidecar                                        │
│  - FIPS: -tags strictfipsruntime present, TLS 1.2 min,                     │
│    ECDHE-RSA/ECDSA-AES-GCM, ChaCha20-Poly1305                             │
│  - SSRF Protection: Optional allowlist from InferencePool pod membership    │
│         │                              │                    │               │
│         │ Port: configurable/TCP       │ Port: 8001/TCP     │ Port: var/TCP │
│         │ Protocol: HTTP/HTTPS         │ Protocol: HTTP/HTTPS│ Protocol: HTTP│
│         │ Encryption: TLS (optional)   │ Encryption: TLS opt│ Encryption:TLS│
│         │ Auth: None                   │ Auth: None         │ Auth: None    │
│         ▼                              ▼                    ▼               │
│  [Remote Prefiller Pod]         [Local Decoder (vLLM)]  [Remote Encoder]    │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [Kubernetes API Server - kubernetes.default.svc]                           │
│  - Purpose: Watch InferencePool, Pod, InferenceObjective, InferenceModelRewrite CRs  │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: ServiceAccount Token (projected volume)                            │
│                                                                             │
│  [OTEL Collector]                                                           │
│  - Purpose: Distributed tracing export                                      │
│  - Port: 4317/TCP                                                           │
│  - Protocol: gRPC OTLP                                                      │
│  - Encryption: TLS (optional)                                               │
│  - Auth: None                                                               │
│                                                                             │
│  [Prometheus]                                                               │
│  - Purpose: Metrics scrape                                                  │
│  - Port: 9090/TCP                                                           │
│  - Protocol: HTTP                                                           │
│  - Encryption: None (plaintext)                                             │
│  - Auth: Bearer Token (optional via --metrics-endpoint-auth)                │
│                                                                             │
│  [Mooncake Bootstrap Server]                                                │
│  - Purpose: dp_rank to engine_id mapping (PD-Sidecar only)                 │
│  - Port: 8998/TCP                                                           │
│  - Protocol: HTTP                                                           │
│  - Encryption: None                                                         │
│  - Auth: None                                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: epp-metrics-auth
  - API Groups: authentication.k8s.io
  - Resources: tokenreviews
  - Verbs: create
  - API Groups: authorization.k8s.io
  - Resources: subjectaccessreviews
  - Verbs: create
  - Non-resource: /metrics
  - Verbs: get

- RoleBinding: epp-pod-reader (pool namespace)
  - Role: pod-reader
  - ServiceAccount: ${EPP_NAME}

- RoleBinding: epp-inference-cr-reader (pool namespace)
  - Role: inference-cr-reader
  - ServiceAccount: ${EPP_NAME}

- RoleBinding: epp-leader-election (pool namespace)
  - Role: leader-election
  - ServiceAccount: ${EPP_NAME}

- ClusterRoleBinding: epp-metrics-auth
  - ClusterRole: epp-metrics-auth
  - ServiceAccount: ${EPP_NAME}

TLS Configuration:
- EPP gRPC Server:
  - crypto/tls with Go defaults
  - Self-signed 4096-bit RSA, 10-year validity
  - NextProtos: h2
  - Certificate reload via file watcher
  - WARNING: Missing -tags strictfipsruntime (may bypass FIPS)

- PD-Sidecar Proxy:
  - crypto/tls with explicit MinVersion TLS 1.2
  - Cipher suites: ECDHE-RSA-AES-GCM-SHA256, ECDHE-RSA-AES-GCM-SHA384,
    ECDHE-ECDSA-AES-GCM-SHA256, ECDHE-ECDSA-AES-GCM-SHA384,
    ChaCha20-Poly1305
  - -tags strictfipsruntime present (FIPS-compliant)

Authentication & Authorization:
- gRPC ext-proc (9002): TLS (mutual optional), no app-level auth
- /metrics (9090): Bearer Token + RBAC (optional via --metrics-endpoint-auth)
- PD-Sidecar (8000): TLS (optional), no application-level auth
- Prefill/Encoder targets: SSRF Allowlist from InferencePool pod membership (optional)

Secrets:
- TLS cert/key (kubernetes.io/tls) - gRPC server TLS, self-signed or mounted, file watcher auto-reload
- inference-gateway-sa-metrics-reader-secret (kubernetes.io/service-account-token) - Metrics auth, Helm provisioned, no auto-rotate

FIPS Compliance Gaps:
- EPP (Dockerfile.epp.konflux): CGO_ENABLED=1 but MISSING -tags strictfipsruntime
  Risk: Go binary may use pure-Go crypto instead of OpenSSL-backed FIPS module
- Sidecar (Dockerfile.sidecar.konflux): CGO_ENABLED=1 WITH -tags strictfipsruntime (compliant)
