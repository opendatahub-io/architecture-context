┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client]                                                          │
│         │                                                                   │
│         │ Port: 8081/TCP (standalone) or 80/TCP (Gateway mode)              │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: None (or TLS via Gateway)                             │
│         │ Auth: None                                                        │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (L7 Proxy)                                                         │
│                                                                             │
│  [Envoy Proxy]                                                              │
│  - Port: 8081/TCP (standalone) or via Gateway API (80/TCP)                  │
│  - Mode: Sidecar or Service                                                 │
│  - ext-proc filter enabled                                                  │
│         │                                                                   │
│         │ Port: 9002/TCP                                                    │
│         │ Protocol: gRPC (ext-proc bidirectional stream)                    │
│         │ Encryption: TLS 1.2+ (self-signed or mounted cert)               │
│         │ Auth: None (TLS server auth only, no client cert)                 │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  EPP SERVICE (Routing Engine)                                               │
│                                                                             │
│  [Endpoint Picker (EPP)]                                                    │
│  - Namespace: {pool-namespace}                                              │
│  - ServiceAccount: {pool}-epp                                               │
│  - Image: odh-llm-d-inference-scheduler                                     │
│  - FIPS: CGO_ENABLED=1 (no strictfipsruntime tag — gap)                     │
│  - Ports:                                                                   │
│    - 9002/TCP gRPC ext-proc (TLS 1.2+)                                      │
│    - 9003/TCP gRPC Health (plaintext)                                        │
│    - 9090/TCP HTTP Metrics (plaintext, optional auth via TokenReview)        │
│    - 5557/TCP ZMQ KV-events (plaintext)                                     │
│         │                                                                   │
│         │ Port: 8000/TCP                                                    │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: None (insecureSkipVerify=true)                        │
│         │ Auth: None                                                        │
│         │ Purpose: Scrape model server Prometheus metrics                   │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  MODEL SERVERS (vLLM / SGLang Pods)                                         │
│                                                                             │
│  [Model Server Pod]                                                         │
│  - Namespace: {pool-namespace}                                              │
│  - Selected by InferencePool label selector                                 │
│  - Port: 8000/TCP (inference + metrics)                                     │
│                                                                             │
│  ──── Disaggregation Path (when P/D or E/P/D enabled) ────                  │
│                                                                             │
│  [pd-sidecar] (co-located with vLLM decoder)                                │
│  - Port: 8000/TCP HTTP/HTTPS (TLS 1.2+ configurable)                       │
│  - FIPS: CGO_ENABLED=1, -tags strictfipsruntime ✓                           │
│  - Image: odh-llm-d-routing-sidecar                                         │
│  - SSRF Protection: Optional (validates against InferencePool allowlist)    │
│         │                                                                   │
│         ├──── Port: 8001/TCP                                                │
│         │     Protocol: HTTP/HTTPS                                          │
│         │     Encryption: Configurable TLS                                  │
│         │     Auth: None                                                    │
│         │     Dest: Local vLLM decoder (same pod)                           │
│         │                                                                   │
│         ├──── Port: 8000/TCP                                                │
│         │     Protocol: HTTP/HTTPS                                          │
│         │     Encryption: Configurable TLS                                  │
│         │     Auth: None                                                    │
│         │     Dest: Remote prefill workers                                  │
│         │                                                                   │
│         └──── Port: 8000/TCP                                                │
│               Protocol: HTTP/HTTPS                                          │
│               Encryption: Configurable TLS                                  │
│               Auth: None                                                    │
│               Dest: Remote encoder workers (E/P/D mode)                     │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SERVICES                                                          │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: ServiceAccount token                                               │
│  - Purpose: CRD watches (InferencePool, InferenceObjective,                 │
│             InferenceModelRewrite), Pod discovery, leader election           │
│                                                                             │
│  [Prometheus]                                                               │
│  - Port: 9090/TCP (scrapes EPP)                                             │
│  - Protocol: HTTP                                                           │
│  - Encryption: None                                                         │
│  - Auth: Optional (TokenReview + SubjectAccessReview)                       │
│                                                                             │
│  [OpenTelemetry Collector] (optional)                                       │
│  - Port: 4317/TCP                                                           │
│  - Protocol: gRPC                                                           │
│  - Encryption: TLS                                                          │
│  - Auth: None                                                               │
│  - Purpose: Distributed tracing export                                      │
│                                                                             │
│  [Istio] (optional, Gateway mode)                                           │
│  - DestinationRule for mTLS + connection pooling                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRoles:
- {pool}-epp (metrics auth):
  - authentication.k8s.io / tokenreviews: create
  - authorization.k8s.io / subjectaccessreviews: create
  - nonResourceURL /metrics: get

Namespace-scoped RoleBindings:
- {pool}-epp → {pool}-epp-role (SA: {pool}-epp)
- {pool}-epp-pods → {pool}-epp-pods-role (SA: {pool}-epp)
- {pool}-epp-leader-election → {pool}-epp-leader-election-role (SA: {pool}-epp)

Secrets:
- Self-signed TLS cert (EPP): In-memory, RSA 4096, 10yr validity, auto-generated at startup, NO auto-rotate
- Self-signed TLS cert (sidecar): In-memory, 10yr validity, auto-generated at startup, NO auto-rotate
- Mounted TLS cert (optional): kubernetes.io/tls, provisioned by cert-manager or manual, rotation depends on source

Security Observations:
- ⚠️  EPP Dockerfile missing -tags strictfipsruntime (sidecar has it)
- ⚠️  Model server metrics scraping uses insecureSkipVerify=true by default
- ⚠️  ext-proc gRPC has TLS but no client authentication (any Envoy can connect)
- ⚠️  No NetworkPolicy shipped by default — relies on cluster-level isolation
- ✓  Sidecar SSRF protection available via InferencePool allowlist validation
- ✓  Flow control uses actor model to prevent lock contention on hot path
- ✓  Namespace-scoped CRD watches with label filtering for tenant isolation

Authentication Summary:
- ext-proc gRPC (9002): TLS server auth only, no client cert
- /metrics (9090): Optional TokenReview + SubjectAccessReview
- Health (9003): Open (no auth)
- Sidecar (8000): TLS server auth (configurable cipher suites)
