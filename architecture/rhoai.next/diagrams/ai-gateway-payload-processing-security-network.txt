┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Gateway-level auth (kube-rbac-proxy or platform auth)        │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (AI Gateway / DMZ)                                                  │
│                                                                              │
│  [AI Gateway — Istio Gateway + EnvoyFilter]                                  │
│  - Port: 443/TCP (external) → EnvoyFilter ext_proc                          │
│  - TLS Termination: TLS 1.2+                                                │
│  - HTTPRoute matching: path-prefix /{namespace}/{model}                      │
│                        header X-Gateway-Model-Name                           │
│         │                                                                    │
│         │ Port: 9004/TCP                                                     │
│         │ Protocol: gRPC/HTTP2                                               │
│         │ Encryption: mTLS (Istio STRICT)                                    │
│         │ Auth: None (in-mesh, EnvoyFilter attachment)                       │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted — Istio mTLS STRICT)                                  │
│                                                                              │
│  [ai-gateway-payload-processing — ext_proc Service]                          │
│  - Namespace: gateway namespace                                              │
│  - ServiceAccount: payload-processing-{release}                              │
│  - Image: odh-ai-gateway-payload-processing-rhel9                            │
│  - Ports: 9004/TCP (ext_proc gRPC), 9005/TCP (health)                       │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, UBI9 + OpenSSL      │
│                                                                              │
│  Plugin Pipeline (in-process):                                               │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │ 1. maas-headers-guard     — strip x-maas-* headers     │                 │
│  │ 2. body-field-to-header   — extract model name          │                 │
│  │ 3. model-provider-resolver — resolve model → provider   │                 │
│  │ 4. api-translation        — translate request format    │                 │
│  │ 5. apikey-injection       — inject provider credentials │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│                                                                              │
│  Controllers (in-process):                                                   │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │ ExternalProvider Controller → Service, ServiceEntry,    │                 │
│  │                               DestinationRule           │                 │
│  │ ExternalModel Controller    → HTTPRoute                 │                 │
│  │ Legacy Migration Controller → ExternalModel,            │                 │
│  │                               ExternalProvider CRs      │                 │
│  └─────────────────────────────────────────────────────────┘                 │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Bearer Token                                  │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - CRD Watches: ExternalModel, ExternalProvider, Secrets                     │
│  - Resource Creates: HTTPRoute, Service, ServiceEntry, DestinationRule       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Mesh-External — Istio DestinationRule TLS SIMPLE)                   │
│                                                                              │
│  Traffic flows: Envoy → ExternalName Service → ServiceEntry → Provider       │
│                                                                              │
│  [OpenAI — api.openai.com]                                                   │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (Istio DestinationRule SIMPLE origination)           │
│  - Auth: Bearer Token (Authorization header)                                 │
│                                                                              │
│  [Anthropic — api.anthropic.com]                                             │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (Istio DestinationRule SIMPLE origination)           │
│  - Auth: API Key (x-api-key header)                                          │
│                                                                              │
│  [Azure OpenAI — {resource}.openai.azure.com]                                │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (Istio DestinationRule SIMPLE origination)           │
│  - Auth: API Key (api-key header)                                            │
│                                                                              │
│  [AWS Bedrock — bedrock-runtime.{region}.amazonaws.com]                      │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (Istio DestinationRule SIMPLE origination)           │
│  - Auth: AWS SigV4 (Authorization + X-Amz-Date + X-Amz-Content-Sha256)      │
│  - Crypto: SHA-256 body hash (FIPS-compliant via OpenSSL backend)            │
│                                                                              │
│  [Google Vertex AI — aiplatform.googleapis.com]                              │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (Istio DestinationRule SIMPLE origination)           │
│  - Auth: Bearer Token (Authorization header)                                 │
│                                                                              │
│  [NeMo Guardrails — optional]                                                │
│  - Port: configurable                                                        │
│  - Protocol: HTTP                                                            │
│  - Encryption: plaintext                                                     │
│  - Auth: None                                                                │
│  - Endpoint: POST /v1/guardrail/checks                                       │
│  - Fail mode: CLOSED (unknown status → block)                                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  ClusterRole: {release}-externalmodels-reader
    ServiceAccount: payload-processing-{release} (gateway namespace)

  API Group: "" (core)
    - secrets: get, list, watch
    - services: get, list, watch, create, update, delete

  API Group: maas.opendatahub.io
    - externalmodels: get, list, watch

  API Group: inference.opendatahub.io
    - externalproviders: get, list, watch, create, update
    - externalmodels: get, list, watch, create, update
    - externalproviders/status: get, update, patch
    - externalmodels/status: get, update, patch
    - externalproviders/finalizers: update
    - externalmodels/finalizers: update

  API Group: gateway.networking.k8s.io
    - httproutes: get, list, watch, create, update, delete

  API Group: networking.istio.io
    - serviceentries: get, list, watch, create, update, delete
    - destinationrules: get, list, watch, create, update, delete

Secrets:
  {provider-secret} (Opaque, user-created per ExternalProvider)
    - Simple auth: api-key field → Bearer/header-based injection
    - SigV4 auth: aws-access-key-id, aws-secret-access-key, aws-session-token fields
    - Labeled: inference.networking.k8s.io/bbr-managed=true
    - Auto-Rotate: No (user-managed)

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
  - Base image: registry.access.redhat.com/ubi9/ubi-minimal (OpenSSL)
  - Crypto: crypto/sha256 (FIPS-approved via OpenSSL backend)
  - No non-FIPS crypto detected (no MD5, RC4, DES, custom crypto)
  - TLS: No direct TLS — Istio handles all TLS origination/termination
