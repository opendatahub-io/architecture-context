┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client]                                                          │
│         │                                                                   │
│         │ Port: Operator-managed (TLS termination at ingress)               │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (managed by ingress/service mesh)                 │
│         │ Auth: OAuth2 JWT Bearer token (conditional on AUTH_ISSUER)        │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) — Managed by rhods-operator / opendatahub-operator           │
│                                                                             │
│  [Ingress / Service Mesh Sidecar]                                           │
│  - TLS Termination: At ingress layer (not in OGX container)                │
│  - AuthN: Delegated to OGX server when AUTH_ISSUER is set                  │
│         │                                                                   │
│         │ Port: 8321/TCP                                                    │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: None (plaintext; TLS expected from ingress/sidecar)   │
│         │ Auth: OAuth2 JWT (conditional)                                    │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (OGX Server Container)                                         │
│                                                                             │
│  [OGX Server — ogx-core]                                                    │
│  - Base Image: quay.io/aipcc/base-images/cpu:3.5.0 (RHEL 9.6 UBI)        │
│  - Port: 8321/TCP (HTTP, plaintext)                                        │
│  - ServiceAccount: (assigned by deploying operator)                         │
│  - Image: quay.io/opendatahub/odh-ogx-core                                │
│  - Auth Middleware: OAuth2 JWT validation when AUTH_ISSUER is set           │
│    - JWKS recheck: AUTH_JWKS_RECHECK_PERIOD (default 3600s)                │
│    - Audience: AUTH_AUDIENCE (default "ogx")                                │
│    - TLS verify: AUTH_VERIFY_TLS (default true)                            │
│  - Secret Resolution: entrypoint.sh resolves _FILE variants for 18 secrets │
│  - Provider Activation: ${env.VAR:+provider-id} pattern                    │
│                                                                             │
│  HTTP Endpoints:                                                            │
│  - POST /v1/responses          — Agentic workflows with tool use           │
│  - POST /v1/messages           — Anthropic Messages API                    │
│  - POST /v1/inference/*        — Chat completion, embeddings               │
│  - POST /v1/vector-io/*        — Vector DB operations                      │
│  - GET/POST/DELETE /v1/files/* — File management                           │
│  - POST /v1/file-processors/*  — Document processing                      │
│  - POST /v1/tool-runtime/*     — Tool execution (search, MCP)             │
│  - GET/POST /v1/batches/*      — Batch inference jobs                      │
│                                                                             │
│  EGRESS CONNECTIONS:                                                        │
│         │                                                                   │
│         ├──── Port: Configurable                                            │
│         │     Protocol: HTTP/HTTPS                                          │
│         │     Encryption: TLS (configurable via VLLM_TLS_VERIFY)           │
│         │     Auth: Bearer token                                            │
│         │     Dest: vLLM Inference Endpoint                                │
│         │                                                                   │
│         ├──── Port: Configurable                                            │
│         │     Protocol: HTTP/HTTPS                                          │
│         │     Encryption: TLS (configurable via VLLM_EMBEDDING_TLS_VERIFY) │
│         │     Auth: Bearer token                                            │
│         │     Dest: vLLM Embedding Endpoint                                │
│         │                                                                   │
│         ├──── Port: 5432/TCP                                                │
│         │     Protocol: PostgreSQL wire protocol                            │
│         │     Encryption: Plaintext (configurable)                          │
│         │     Auth: Username/password (POSTGRES_PASSWORD)                   │
│         │     Dest: PostgreSQL (state storage)                              │
│         │                                                                   │
│         ├──── Port: 5432/TCP                                                │
│         │     Protocol: PostgreSQL wire protocol                            │
│         │     Encryption: Plaintext (configurable)                          │
│         │     Auth: Username/password (PGVECTOR_PASSWORD)                   │
│         │     Dest: pgvector (vector storage)                               │
│         │                                                                   │
│         ├──── Port: Configurable                                            │
│         │     Protocol: HTTP/gRPC                                           │
│         │     Encryption: mTLS (configurable via ca_pem_path, client certs)│
│         │     Auth: Token (MILVUS_TOKEN)                                    │
│         │     Dest: Milvus (vector storage)                                 │
│         │                                                                   │
│         ├──── Port: 6333/TCP (REST), 6334/TCP (gRPC)                       │
│         │     Protocol: HTTP/gRPC                                           │
│         │     Encryption: TLS (configurable via QDRANT_HTTPS)              │
│         │     Auth: API key (QDRANT_API_KEY)                                │
│         │     Dest: Qdrant (vector storage)                                 │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: AWS IAM (STS/IRSA)                                      │
│         │     Dest: AWS Bedrock                                             │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key (WATSONX_API_KEY)                               │
│         │     Dest: IBM WatsonX                                             │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key (AZURE_OPENAI_API_KEY)                          │
│         │     Dest: Azure OpenAI                                            │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: Google ADC/OAuth2                                       │
│         │     Dest: Google Vertex AI                                        │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key (OPENAI_API_KEY)                                │
│         │     Dest: OpenAI API                                              │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key / access token (GEMINI_API_KEY)                 │
│         │     Dest: Google Gemini API                                       │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key (ANTHROPIC_API_KEY)                             │
│         │     Dest: Anthropic API                                           │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key (BRAVE_SEARCH_API_KEY)                          │
│         │     Dest: Brave Search API                                        │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: API key (TAVILY_SEARCH_API_KEY)                         │
│         │     Dest: Tavily Search API                                       │
│         │                                                                   │
│         ├──── Port: 443/TCP                                                 │
│         │     Protocol: HTTPS                                               │
│         │     Encryption: TLS 1.2+                                          │
│         │     Auth: AWS IAM credentials                                     │
│         │     Dest: S3 / Object Storage                                     │
│         │                                                                   │
│         ├──── Port: Configurable                                            │
│         │     Protocol: HTTP/HTTPS                                          │
│         │     Encryption: Configurable                                      │
│         │     Auth: API key                                                 │
│         │     Dest: Docling Serve (document processing)                     │
│         │                                                                   │
│         ├──── Port: Configurable                                            │
│         │     Protocol: HTTP/HTTPS                                          │
│         │     Encryption: Configurable                                      │
│         │     Auth: Configurable                                            │
│         │     Dest: MCP Servers (external tool execution)                   │
│         │                                                                   │
│         ├──── Port: Configurable                                            │
│         │     Protocol: OTLP (HTTP/gRPC)                                    │
│         │     Encryption: Configurable                                      │
│         │     Auth: None                                                    │
│         │     Dest: OTEL Collector (traces, metrics)                        │
│         │                                                                   │
│         └──── Port: 443/TCP                                                 │
│               Protocol: HTTPS                                               │
│               Encryption: TLS (configurable via AUTH_VERIFY_TLS)            │
│               Auth: None                                                    │
│               Dest: JWKS Endpoint (OAuth2 key retrieval)                    │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [vLLM Inference Endpoint]                                                  │
│  - Purpose: LLM inference and chat completions                              │
│  - Auth: Bearer token                                                       │
│  - Encryption: TLS (configurable)                                           │
│                                                                             │
│  [PostgreSQL]                                                               │
│  - Purpose: State storage (conversations, batches, agent state, metadata)   │
│  - Auth: Username/password                                                  │
│  - Encryption: Plaintext (configurable)                                     │
│                                                                             │
│  [Vector Databases — Milvus / pgvector / Qdrant]                            │
│  - Purpose: Vector similarity search for RAG                                │
│  - Auth: Token / password / API key                                         │
│  - Encryption: Varies (mTLS, plaintext, TLS)                               │
│                                                                             │
│  [Cloud Inference — Bedrock / WatsonX / Azure / Vertex AI / OpenAI /        │
│   Gemini / Anthropic]                                                       │
│  - Purpose: Multi-cloud LLM inference                                       │
│  - Auth: API key / IAM / ADC                                                │
│  - Encryption: TLS 1.2+                                                     │
│                                                                             │
│  [Search APIs — Brave / Tavily]                                             │
│  - Purpose: Web search tool for agents                                      │
│  - Auth: API key                                                            │
│  - Encryption: TLS 1.2+                                                     │
│                                                                             │
│  [S3 / Object Storage]                                                      │
│  - Purpose: File storage backend                                            │
│  - Auth: AWS IAM credentials                                                │
│  - Encryption: TLS 1.2+                                                     │
│                                                                             │
│  [JWKS Endpoint]                                                            │
│  - Purpose: OAuth2 token validation key retrieval                           │
│  - Auth: None                                                               │
│  - Encryption: TLS (configurable)                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC — this is a container image deployed by an operator.
- RBAC is managed by the deploying operator (rhods-operator / opendatahub-operator).

Application-Level Authorization (when AUTH_ISSUER is set):
- Policy Engine: OGX auth policy engine (OAuth2 JWT)
- Read system resources: Permit for all authenticated users
- Create resources: Permit for authenticated users
- Manage owned resources: Permit read/update/delete only when user is resource owner

Secrets:
- Provider API keys (env vars):
  - OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY — Provider auth
  - AZURE_OPENAI_API_KEY — Azure OpenAI auth
  - WATSONX_API_KEY — IBM WatsonX auth
  - BRAVE_SEARCH_API_KEY, TAVILY_SEARCH_API_KEY — Search tool auth
  - AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY — S3 and Bedrock auth
  - POSTGRES_PASSWORD — PostgreSQL auth
  - PGVECTOR_PASSWORD — pgvector auth
  - MILVUS_TOKEN — Milvus auth
  - QDRANT_API_KEY — Qdrant auth
- All secrets support _FILE variant (mount as files, not env vars)
- Total: 18 secret environment variables with _FILE support
- Provisioned by: Manual / Kubernetes Secret / IRSA (varies)
- Auto-Rotate: No (except AWS credentials via IRSA)

FIPS Compliance:
- Python application — FIPS inherited from AIPCC base image (RHEL 9.6 + FIPS-validated OpenSSL)
- No compiled binaries, no custom cryptographic code in distribution layer
- TLS configuration delegated to OGX server (upstream code)

Build Hermeticity:
- Python deps: requirements-lock-konflux.txt (uv pip compile, with hashes) ✓
- ML artifacts: artifacts.lock.yaml (SHA-256 checksums, 25 files) ✓
- Hermeto prefetch: cachi2.env sourced, --no-index --find-links ✓
- RPM lock: rpms.lock.yaml — NOT present (added on release branches)
- Tekton: hermetic: true ✓

Service Mesh Configuration:
- Not directly configured by this component
- TLS and service mesh are managed by the deploying operator
- OGX server runs plaintext HTTP on 8321/TCP
- TLS termination expected at ingress/sidecar layer
