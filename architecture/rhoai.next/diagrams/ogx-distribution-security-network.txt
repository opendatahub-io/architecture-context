┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 8321/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (plaintext -- platform handles TLS termination)   │
│         │ Auth: OAuth2 Bearer Token (optional, when AUTH_ISSUER is set)      │
│         │       JWT validated against JWKS endpoint (AUTH_JWKS_URI)          │
│         │       Audience: AUTH_AUDIENCE (default "ogx")                      │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (OGX Server Container)                                          │
│                                                                              │
│  [OGX Server - odh-ogx-core]                                                │
│  - Container Port: 8321/TCP (HTTP, plaintext)                                │
│  - Base Image: quay.io/aipcc/base-images/cpu:3.5.0-ea.2                     │
│  - Runtime: Python 3.12, RHEL 9.6                                           │
│  - Auth: OAuth2 token provider (validates JWT if AUTH_ISSUER set)            │
│  - Access Policy: Owner-based (system resources: all read;                   │
│                   user resources: owner CRUD, others no access)              │
│  - OpenTelemetry: Optional (when OTEL_SERVICE_NAME is set)                  │
│         │                                                                    │
│         ├─── Inference Egress ─────────────────────────────────────────      │
│         │                                                                    │
│         │    Port: 8000/TCP (configurable)                                   │
│         │    Protocol: HTTP/HTTPS                                            │
│         │    Encryption: TLS (configurable via VLLM_TLS_VERIFY, default on)  │
│         │    Auth: API Token (Bearer, VLLM_API_TOKEN)                        │
│         │    ▼                                                               │
│         │    [vLLM Inference Endpoint]                                       │
│         │    - Purpose: LLM chat completions and text generation             │
│         │                                                                    │
│         │    Port: 8001/TCP (configurable)                                   │
│         │    Protocol: HTTP/HTTPS                                            │
│         │    Encryption: TLS (configurable via VLLM_EMBEDDING_TLS_VERIFY)    │
│         │    Auth: API Token (Bearer, VLLM_EMBEDDING_API_TOKEN)              │
│         │    ▼                                                               │
│         │    [vLLM Embedding Endpoint]                                       │
│         │    - Purpose: Embedding generation for RAG                         │
│         │                                                                    │
│         ├─── State Store Egress ───────────────────────────────────────      │
│         │                                                                    │
│         │    Port: 5432/TCP                                                  │
│         │    Protocol: PostgreSQL wire protocol                              │
│         │    Encryption: Plaintext (configurable)                            │
│         │    Auth: Username/Password (POSTGRES_USER, POSTGRES_PASSWORD)      │
│         │    ▼                                                               │
│         │    [PostgreSQL]                                                     │
│         │    - Purpose: KV store, SQL store, inference logs, agent state,    │
│         │      batch jobs, conversations, file metadata, vector metadata     │
│         │                                                                    │
│         ├─── Cloud Provider Egress ────────────────────────────────────      │
│         │                                                                    │
│         │    Port: 443/TCP (all providers)                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    ▼                                                               │
│         │    [AWS Bedrock]                                                    │
│         │    - Auth: IAM Role ARN + Web Identity Token (IRSA)               │
│         │    - Env: ENABLE_BEDROCK, AWS_ROLE_ARN, AWS_WEB_IDENTITY_TOKEN    │
│         │                                                                    │
│         │    [IBM WatsonX]                                                    │
│         │    - Auth: API Key (WATSONX_API_KEY)                              │
│         │                                                                    │
│         │    [Azure OpenAI]                                                   │
│         │    - Auth: API Key (AZURE_API_KEY)                                │
│         │                                                                    │
│         │    [Google Vertex AI]                                               │
│         │    - Auth: GCP Application Default Credentials                    │
│         │    - Env: VERTEX_AI_PROJECT                                       │
│         │                                                                    │
│         │    [OpenAI]                                                         │
│         │    - Auth: API Key (OPENAI_API_KEY, Bearer)                       │
│         │                                                                    │
│         │    [Google Gemini]                                                  │
│         │    - Auth: API Key or Access Token (GEMINI_API_KEY)               │
│         │    - Env: ENABLE_GEMINI                                           │
│         │                                                                    │
│         │    [Anthropic]                                                      │
│         │    - Auth: API Key (ANTHROPIC_API_KEY)                            │
│         │                                                                    │
│         ├─── Vector DB Egress ─────────────────────────────────────────      │
│         │                                                                    │
│         │    [Milvus (Remote)]                                               │
│         │    - Port: Configurable                                           │
│         │    - Protocol: gRPC/HTTPS                                         │
│         │    - Encryption: TLS + mTLS (configurable)                        │
│         │    - Auth: Token (MILVUS_TOKEN)                                   │
│         │    - mTLS: CA cert (MILVUS_CA_PEM_PATH),                          │
│         │           Client cert (MILVUS_CLIENT_PEM_PATH),                   │
│         │           Client key (MILVUS_CLIENT_KEY_PATH)                     │
│         │                                                                    │
│         │    [pgvector (PostgreSQL)]                                         │
│         │    - Port: 5432/TCP                                               │
│         │    - Protocol: PostgreSQL wire protocol                           │
│         │    - Encryption: Plaintext (configurable)                         │
│         │    - Auth: Username/Password (PGVECTOR_PASSWORD)                  │
│         │                                                                    │
│         │    [Qdrant]                                                        │
│         │    - Port: 6333/TCP (HTTP) or 6334/TCP (gRPC)                    │
│         │    - Protocol: HTTP/gRPC                                          │
│         │    - Encryption: HTTPS (configurable)                             │
│         │    - Auth: API Key (QDRANT_API_KEY)                               │
│         │                                                                    │
│         ├─── Tool Runtime Egress ──────────────────────────────────────      │
│         │                                                                    │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    ▼                                                               │
│         │    [Brave Search API]                                              │
│         │    - Auth: API Key (BRAVE_SEARCH_API_KEY)                         │
│         │                                                                    │
│         │    [Tavily Search API]                                              │
│         │    - Auth: API Key (TAVILY_SEARCH_API_KEY)                        │
│         │                                                                    │
│         │    [MCP Servers]                                                    │
│         │    - Port: Configurable                                           │
│         │    - Protocol: HTTP/HTTPS                                         │
│         │    - Encryption: Configurable                                     │
│         │    - Auth: Configurable                                           │
│         │                                                                    │
│         ├─── File Storage Egress ──────────────────────────────────────      │
│         │                                                                    │
│         │    [AWS S3]                                                         │
│         │    - Port: 443/TCP                                                │
│         │    - Protocol: HTTPS                                              │
│         │    - Encryption: TLS 1.2+                                         │
│         │    - Auth: AWS IAM (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)     │
│         │    - Env: ENABLE_S3                                               │
│         │                                                                    │
│         ├─── Document Processing Egress ───────────────────────────────      │
│         │                                                                    │
│         │    [Docling Serve]                                                 │
│         │    - Port: Configurable                                           │
│         │    - Protocol: HTTP/HTTPS                                         │
│         │    - Encryption: Configurable                                     │
│         │    - Auth: API Key                                                │
│         │    - Env: DOCLING_SERVE_URL                                       │
│         │                                                                    │
│         ├─── Observability Egress ─────────────────────────────────────      │
│         │                                                                    │
│         │    [OpenTelemetry Collector]                                        │
│         │    - Port: Configurable                                           │
│         │    - Protocol: gRPC (OTLP) or HTTP (OTLP)                        │
│         │    - Encryption: Configurable                                     │
│         │    - Auth: None                                                   │
│         │    - Env: OTEL_SERVICE_NAME (activation trigger)                  │
│         │                                                                    │
│         └─── Auth Egress ──────────────────────────────────────────────      │
│                                                                              │
│              [OAuth2/OIDC Issuer]                                             │
│              - Port: 443/TCP                                                │
│              - Protocol: HTTPS                                              │
│              - Encryption: TLS (configurable via AUTH_VERIFY_TLS)           │
│              - Purpose: JWKS key retrieval for JWT token validation         │
│              - Env: AUTH_ISSUER, AUTH_JWKS_URI                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined (container image, not a Kubernetes operator)
- Access control is application-level via OAuth2 token provider
- Resource ownership model:
  - System resources: All authenticated users can read
  - User resources: Creator (owner) has full CRUD
  - Unauthenticated: Allowed when AUTH_ISSUER is not set

Service Mesh Configuration:
- No service mesh configuration defined in this repository
- Ingress/TLS termination expected to be handled by deploying operator
- Container exposes plaintext HTTP on 8321/TCP

Secrets (Environment Variables):
- PostgreSQL: POSTGRES_USER, POSTGRES_PASSWORD (Username/Password, no auto-rotate)
- vLLM: VLLM_API_TOKEN, VLLM_EMBEDDING_API_TOKEN (Bearer tokens, no auto-rotate)
- AWS Bedrock: AWS_BEDROCK_BEARER_TOKEN, AWS_ROLE_ARN, AWS_WEB_IDENTITY_TOKEN_FILE (IAM, token refresh)
- IBM WatsonX: WATSONX_API_KEY (API Key, no auto-rotate)
- Azure: AZURE_API_KEY (API Key, no auto-rotate)
- OpenAI: OPENAI_API_KEY (API Key, no auto-rotate)
- Google Gemini: GEMINI_API_KEY, GEMINI_ACCESS_TOKEN (API Key/Token, no auto-rotate)
- Anthropic: ANTHROPIC_API_KEY (API Key, no auto-rotate)
- Milvus: MILVUS_TOKEN (Token, no auto-rotate)
- pgvector: PGVECTOR_PASSWORD (Password, no auto-rotate)
- Qdrant: QDRANT_API_KEY (API Key, no auto-rotate)
- AWS S3: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY (IAM, no auto-rotate)
- Brave Search: BRAVE_SEARCH_API_KEY (API Key, no auto-rotate)
- Tavily Search: TAVILY_SEARCH_API_KEY (API Key, no auto-rotate)
- OAuth2: AUTH_ISSUER, AUTH_JWKS_URI, AUTH_AUDIENCE (Config, no auto-rotate)

FIPS Compliance:
- Python application; FIPS compliance inherited from AIPCC base image
- OpenSSL from RHEL 9.6 UBI (via AIPCC base image)
- No custom TLS configuration; uses Python httpx/aiohttp with system crypto
- vLLM TLS verification: configurable (VLLM_TLS_VERIFY, default: true)
- Auth TLS verification: configurable (AUTH_VERIFY_TLS, default: true)
