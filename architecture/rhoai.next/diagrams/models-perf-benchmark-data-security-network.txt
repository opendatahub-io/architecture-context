================================================================================
  SECURITY NETWORK DIAGRAM: models-perf-benchmark-data
  Generated: 2026-06-22
================================================================================

NOTE: This component is a DATA-ONLY container. It exposes NO network services.
The network flows below describe the data ingestion utilities (kafka-processor,
api-client) which are NOT shipped in the production container image. They run
in CI/dev environments to populate the data/ directory before container builds.

┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL DATA SOURCES (Untrusted)                                         │
│                                                                             │
│  [Kafka Brokers]                                                            │
│  - Hosts: (configured via BOOTSTRAP env var)                                │
│  - Topics: dev.aihub-public-* (5 topics)                                    │
│         │                                                                   │
│         │ Port: 9092/TCP or 9096/TCP                                        │
│         │ Protocol: Kafka                                                   │
│         │ Encryption: SASL_SSL (TLS + SCRAM-SHA-512)                        │
│         │ Auth: SASL_USERNAME / SASL_PASSWORD                               │
│         │ Direction: EGRESS (consumer pulls from broker)                     │
│         ▼                                                                   │
│  [model-validation-hub]                                                     │
│  - Host: (configured via API_BASE_URL env var)                              │
│  - Endpoints: /api/v1/{providers,models,evaluations,performances,           │
│               validations,variant-groups}                                    │
│         │                                                                   │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS (REST)                                             │
│         │ Encryption: TLS (custom CA cert supported)                         │
│         │ Auth: None (TLS server auth only)                                  │
│         │ Note: Hostname verification optionally skipped                     │
│         │       (API_TLS_HOSTNAME_CHECK=false)                               │
│         │ Direction: EGRESS (client fetches from API)                         │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  DATA INGESTION LAYER (CI/Dev Environment - Not Shipped)                    │
│                                                                             │
│  [kafka-processor]                                                          │
│  - Language: Python 3.12                                                    │
│  - Libraries: confluent-kafka, lz4, librdkafka                              │
│  - Network: Consumer only (outbound to Kafka brokers)                        │
│  - No listening ports                                                        │
│         │                                                                   │
│         │ File I/O                                                           │
│         ▼                                                                   │
│  [api-client]                                                               │
│  - Language: Python 3.12                                                    │
│  - Libraries: requests (urllib3/OpenSSL)                                      │
│  - Network: REST client only (outbound to model-validation-hub)              │
│  - No listening ports                                                        │
│  - WARNING: _CertNoHostnameAdapter skips hostname verification               │
│         │                                                                   │
│         │ File I/O                                                           │
│         ▼                                                                   │
│  [enrich_models.py]                                                         │
│  - Language: Python                                                         │
│  - Network: None (file I/O only)                                             │
│         │                                                                   │
│         │ File I/O                                                           │
│         ▼                                                                   │
│  [data/ directory]                                                          │
│  - Format: JSON/NDJSON                                                       │
│  - Structure: provider/model/{metadata.json, performance.ndjson,             │
│               evaluations.ndjson, validation.ndjson} + manifest.json         │
│         │                                                                   │
│         │ Dockerfile.konflux COPY                                            │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  PRODUCTION CONTAINER (Shipped via Konflux)                                 │
│                                                                             │
│  [odh-model-performance-data]                                               │
│  - Image: managed-open-data-hub/odh-model-performance-data-rhel9            │
│  - Base: UBI9-minimal                                                        │
│  - Content: Static data files only (NO executable code)                      │
│  - CMD: sleep infinity (overridden by K8s init container spec)               │
│  - Network: NONE (no ports, no services, no connections)                     │
│  - FIPS: Inherited from UBI9-minimal base                                    │
│         │                                                                   │
│         │ Volume mount (init container pattern)                              │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CLUSTER (odh-model-registries namespace)                        │
│                                                                             │
│  [model-catalog Pod]                                                        │
│  - Init Container: odh-model-performance-data                                │
│    - Copies /app/benchmarks → shared volume                                  │
│  - Main Container: model-catalog                                             │
│    - Reads benchmark data from shared volume                                 │
│    - Serves data to RHOAI Dashboard                                          │
│                                                                             │
│  [model-registry-operator]                                                  │
│  - Manages model-catalog deployment                                          │
│  - Injects init container image reference                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- This component defines NO RBAC resources (ClusterRoles, RoleBindings, etc.)
- RBAC for the model-catalog pod is managed by model-registry-operator

Secrets:
- Kafka SASL credentials (Opaque - env vars)
  - SASL_USERNAME, SASL_PASSWORD
  - Provisioned: Manual / CI configuration
  - Auto-Rotate: No
  - Used by: kafka-processor only (not in production image)

- API TLS certificate (File - PEM)
  - Custom CA cert for model-validation-hub TLS
  - Provisioned: Manual
  - Auto-Rotate: No
  - Used by: api-client only (not in production image)

Authentication & Authorization:
- Kafka brokers: SASL_SSL (SCRAM-SHA-512) with topic-level ACLs
- model-validation-hub API: TLS only (no client auth required)

Security Notes:
- SHIPPED ARTIFACT: Contains NO executable code, NO network services
- Data ingestion utilities are NOT included in the shipped container image
- api-client uses _CertNoHostnameAdapter (check_hostname=False) for
  model-validation-hub self-signed cert - reduces TLS security
- Python utilities delegate TLS to system OpenSSL (FIPS-compliant on
  FIPS-enabled hosts)
