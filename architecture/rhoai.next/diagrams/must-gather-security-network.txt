┌─────────────────────────────────────────────────────────────────────────┐
│  USER WORKSTATION (Trusted)                                             │
│                                                                          │
│  [User / SRE]                                                            │
│         │                                                                │
│         │ Command: oc adm must-gather --image=<registry>/must-gather:tag │
│         │    (OpenShift)                                                  │
│         │ Command: kubectl apply -f job.yaml                             │
│         │    (xKS: AKS, EKS, CKS)                                       │
│         │ Auth: kubeconfig (user credentials)                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE (Trusted)                                     │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount Bearer Token (in-cluster)                       │
│         │                                                                │
│         │ Creates must-gather pod / Job pod                              │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  MUST-GATHER POD (Ephemeral - Diagnostic Container)                     │
│                                                                          │
│  [must-gather Container]                                                 │
│  - Image: registry.redhat.io/rhoai/odh-must-gather-rhel9:latest        │
│  - Base: ose-must-gather-rhel9:v4.20 (OpenShift)                       │
│  - ServiceAccount: system:serviceaccount (OpenShift, cluster-admin)     │
│                    must-gather-sa (xKS, must-gather-reader ClusterRole) │
│  - Runs as: one-shot diagnostic container                                │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Bundled CLI Tools                                                   │ │
│  │ - oc CLI (inherited from base image, OpenShift only)                │ │
│  │ - kubectl v1.33.3 (Konflux) / v1.34.7 (community)                  │ │
│  │ - helm v4.1.4 (Git LFS binary, sha256:210d576a...)                  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Collection Scripts                                                  │ │
│  │                                                                      │ │
│  │ gather.sh → detects platform → dispatches:                          │ │
│  │                                                                      │ │
│  │ OpenShift Path (13 scripts, parallel):                              │ │
│  │   gather_serving.sh, gather_data_science_pipelines.sh,              │ │
│  │   gather_notebooks.sh, gather_dashboard.sh, gather_kuberay.sh,     │ │
│  │   gather_kueue.sh, gather_kfto.sh, gather_mr.sh,                   │ │
│  │   gather_trustyai.sh, gather_feastoperator.sh, gather_lls.sh,      │ │
│  │   gather_mlflow.sh, gather_sparkoperator.sh,                        │ │
│  │   gather_models_as_a_service.sh                                     │ │
│  │                                                                      │ │
│  │ xKS Path:                                                           │ │
│  │   gather_llmd.sh → gather_cluster.sh, gather_o11y.sh               │ │
│  │   Dependencies (parallel): cert-manager.sh, sail.sh, lws.sh        │ │
│  │   Optional: gather_wva.sh, gather_batch_gateway.sh                  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│         │                                                                │
│         │ ALL TRAFFIC: Read-only API calls                               │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Bearer Token (mounted at                  │
│         │       /var/run/secrets/kubernetes.io/serviceaccount/token)     │
│         │ Methods: GET, LIST only                                        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES API SERVER (Target Resources)                               │
│                                                                          │
│  Resources collected (read-only):                                        │
│                                                                          │
│  Platform CRDs:                                                          │
│  - DSCInitialization, DataScienceCluster, Auth, Monitoring              │
│  - GatewayConfig, HardwareProfile, AcceleratorProfile                   │
│                                                                          │
│  Component CRDs (20+ types):                                             │
│  - InferenceService, ServingRuntime, LLMInferenceService, InferencePool │
│  - DSPA, RayCluster, ClusterQueue, PyTorchJob, ModelRegistry            │
│  - TrustyAIService, FeatureStore, LlamaStackDistribution, MLflow       │
│  - SparkApplication, MaaSModelRef, Tenant                               │
│                                                                          │
│  Networking CRDs:                                                        │
│  - Gateway, HTTPRoute, GRPCRoute, ReferenceGrant, GatewayClass          │
│  - VirtualService, DestinationRule, EnvoyFilter                          │
│  - AuthorizationPolicy, PeerAuthentication, RequestAuthentication       │
│                                                                          │
│  Infrastructure CRDs:                                                    │
│  - Issuer, ClusterIssuer, Certificate (cert-manager)                    │
│  - LeaderWorkerSet (LWS)                                                 │
│  - Istio, IstioRevision (Sail)                                           │
│  - ServiceMonitor, PodMonitor, PrometheusRule (Prometheus)              │
│  - ScaledObject, ScaledJob (KEDA, optional)                              │
│  - ValidatingAdmissionPolicy, MutatingWebhookConfiguration             │
│                                                                          │
│  Core Kubernetes Resources:                                              │
│  - Pods, Deployments, Services, Events, ConfigMaps                      │
│  - Pod logs (current + previous containers)                              │
│  - Nodes, Namespaces, API Resources                                      │
│                                                                          │
│  Helm Data:                                                              │
│  - Helm release values (helm get values)                                 │
│  - Helm release manifests (helm get manifest)                            │
│  - Stored as Kubernetes Secrets (type: helm.sh/release.v1)              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  COLLECTION OUTPUT (Local to Pod)                                       │
│                                                                          │
│  [/must-gather/] (OpenShift) or [/tmp/must-gather/] (xKS)              │
│  - Collected resource YAMLs                                              │
│  - Pod logs (stdout/stderr, current + previous)                          │
│  - Namespace inspection dumps                                            │
│  - Helm release values and manifests                                     │
│  - Cluster metadata (API resources, node info)                           │
│                                                                          │
│  Retrieved by:                                                           │
│  - OpenShift: oc adm must-gather framework (automatic tarball)          │
│  - xKS: kubectl cp from Job pod (manual retrieval)                      │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  OpenShift Path:
  - ServiceAccount: Inherited from oc adm must-gather framework
  - Effective RBAC: cluster-admin (full cluster read access)
  - Scope: All namespaces, all resource types

  xKS Path (AKS/EKS/CKS):
  - ServiceAccount: must-gather-sa (namespace: must-gather)
  - ClusterRole: must-gather-reader
    - API Groups: * (all)
    - Resources: * (all)
    - Verbs: get, list, watch
  - ClusterRoleBinding: must-gather-reader-binding
    - Binds must-gather-reader to must-gather-sa

Secrets:
  - redhat-pull-secret (xKS only)
    - Type: kubernetes.io/dockerconfigjson
    - Purpose: Image pull secret for registry.redhat.io
    - Provisioned By: Manual (user creates from podman auth)
    - Auto-Rotate: No
  - ServiceAccount token (both paths)
    - Type: Projected volume (Kubernetes-managed)
    - Purpose: API authentication
    - Provisioned By: Kubernetes
    - Auto-Rotate: Yes (token rotation by kubelet)

Network Summary:
  - Ingress: None (diagnostic container, not a service)
  - Egress: Kubernetes API Server only (443/TCP, HTTPS, TLS 1.2+)
  - No external network calls (no internet access required)
  - No service mesh participation
  - No inter-pod communication
  - Purely read-only Kubernetes API access

FIPS Compliance:
  - No compiled binaries in this component (Bash scripts only)
  - TLS delegated to oc/kubectl CLI tools (use system OpenSSL)
  - Base image: UBI9 / ose-must-gather-rhel9 (FIPS-capable)
  - No custom cryptographic operations

Build Hermeticity:
  - Tekton pipeline: hermetic=true
  - kubectl: extracted from ose-cli-rhel9 builder stage
  - helm: Git LFS binary (sha256:210d576a6dc04b7864af4b5d9a5af58860bbc03d19174ffeddbd29d01d1b59a9)
  - No rpms.lock.yaml or artifacts.lock.yaml on main branch
  - Multi-arch: x86_64, ppc64le, s390x, arm64
