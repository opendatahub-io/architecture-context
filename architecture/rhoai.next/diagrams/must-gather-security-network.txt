┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (User Workstation)                                            │
│                                                                          │
│  [User / SRE]                                                            │
│         │                                                                │
│         │ Command: oc adm must-gather (OCP) / kubectl apply (xKS)       │
│         │ Auth: kubeconfig (client certificate or OIDC token)            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER (Trusted - kube-apiserver enforced RBAC)                       │
│                                                                          │
│  [must-gather Pod]                                                       │
│  - Namespace: openshift-must-gather-XXXXX (OCP) / must-gather (xKS)    │
│  - Image: registry.redhat.io/rhoai/odh-must-gather-rhel9:latest        │
│  - Base: ose-must-gather-rhel9:v4.20                                    │
│  - ServiceAccount: must-gather (OCP) / must-gather-sa (xKS)            │
│  - No compiled binaries — pure Bash scripts                             │
│  - No TLS termination — delegates all crypto to oc/kubectl CLIs         │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS (REST API)                                     │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Bearer Token (automounted)                │
│         │ Operations: GET, LIST only (read-only)                         │
│         ▼                                                                │
│  [kube-apiserver]                                                        │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - RBAC enforced: must-gather ClusterRole (OCP) /                       │
│                   must-gather-reader ClusterRole (xKS)                   │
│                                                                          │
│  Resources Accessed (read-only via API server):                          │
│  ┌────────────────────────────────────────────────────────────────┐      │
│  │  RHOAI Platform CRDs                                          │      │
│  │  - DSCInitialization, DataScienceCluster                      │      │
│  │  - InferenceService, ServingRuntime, ClusterServingRuntime    │      │
│  │  - DataSciencePipelinesApplication, Workflow                  │      │
│  │  - OdhDashboardConfig, AcceleratorProfile                     │      │
│  │  - RayCluster, RayJob, RayService                             │      │
│  │  - ClusterQueue, LocalQueue, Workload                         │      │
│  │  - PyTorchJob, TrainJob, TrainingRuntime                      │      │
│  │  - ModelRegistry, FeatureStore, MLflow                        │      │
│  │  - TrustyAIService, LMEvalJob, GuardrailsOrchestrator        │      │
│  │  - LlamaStackDistribution, SparkApplication                   │      │
│  │  - AITenant, MaaSSubscription, ExternalModel                  │      │
│  │  - MCPServer                                                   │      │
│  └────────────────────────────────────────────────────────────────┘      │
│  ┌────────────────────────────────────────────────────────────────┐      │
│  │  Networking / Ingress CRDs                                     │      │
│  │  - Gateway, HTTPRoute, GRPCRoute, ReferenceGrant              │      │
│  │  - GatewayClass                                                │      │
│  │  - VirtualService, DestinationRule, EnvoyFilter               │      │
│  │  - AuthorizationPolicy, PeerAuthentication                     │      │
│  │  - RequestAuthentication, Telemetry                            │      │
│  │  - Istio, IstioRevision                                        │      │
│  └────────────────────────────────────────────────────────────────┘      │
│  ┌────────────────────────────────────────────────────────────────┐      │
│  │  Dependency Operator CRDs (LLM-D path)                        │      │
│  │  - Certificate, Issuer, ClusterIssuer, CertManager            │      │
│  │  - LeaderWorkerSet, LeaderWorkerSetOperator                    │      │
│  │  - ScaledObject, ScaledJob, TriggerAuthentication (KEDA)      │      │
│  │  - Kuadrant, AuthPolicy, RateLimitPolicy                      │      │
│  └────────────────────────────────────────────────────────────────┘      │
│  ┌────────────────────────────────────────────────────────────────┐      │
│  │  Monitoring CRDs                                               │      │
│  │  - ServiceMonitor, PodMonitor, PrometheusRule                 │      │
│  │  - Prometheus, Alertmanager                                    │      │
│  └────────────────────────────────────────────────────────────────┘      │
│  ┌────────────────────────────────────────────────────────────────┐      │
│  │  Namespace Inspections (pod YAML, logs, events)               │      │
│  │  - redhat-ods-operator (rhods-operator)                       │      │
│  │  - redhat-ods-applications (RHOAI components)                 │      │
│  │  - redhat-ods-monitoring (monitoring stack)                   │      │
│  │  - rhods-notebooks (workbenches)                              │      │
│  │  - rhoai-model-registries (model registry)                    │      │
│  │  - openshift-ingress (Gateway API)                            │      │
│  │  - kuadrant-system (API gateway policies)                     │      │
│  │  - rhai-gitops (Helm chart releases)                          │      │
│  │  - Dynamic: all namespaces containing component CRDs         │      │
│  └────────────────────────────────────────────────────────────────┘      │
│                                                                          │
│  [Output: /must-gather/ directory]                                       │
│  - Filesystem only — no network egress                                   │
│  - Tarball transferred back to user via oc rsync (OCP) /               │
│    kubectl cp (xKS)                                                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NO EXTERNAL EGRESS — All data collection is via the in-cluster kube-apiserver.
No outbound network connections to external services.

RBAC Summary:
=============

OpenShift (OCP):
- ClusterRole: must-gather (provisioned by `oc adm must-gather`)
  - API Groups: * (all)
  - Resources: * (all)
  - Verbs: get, list, watch (read-only)
  - Note: Standard OpenShift must-gather role, not custom

xKS (Non-OpenShift):
- ClusterRole: must-gather-reader
  - API Groups: * (all)
  - Resources: * (all)
  - Verbs: get, list, watch (read-only)
- ClusterRoleBinding: must-gather-reader-binding
  - ServiceAccount: must-gather-sa (cluster-scoped)

Authentication & Authorization:
===============================
- Endpoint: Kubernetes API (kube-apiserver)
- Methods: GET, LIST (read-only)
- Auth Mechanism: ServiceAccount Bearer Token (automounted)
- Enforcement: kube-apiserver RBAC
- Policy: Read-only access to all API groups and resources

Secrets:
========
- redhat-pull-secret (xKS only)
  - Type: kubernetes.io/dockerconfigjson
  - Purpose: Image pull secret for registry.redhat.io
  - Provisioned By: Manual (user-created)
  - Auto-Rotate: No

FIPS Compliance:
================
- No compiled binaries — pure Bash scripts
- No cryptographic operations in shell scripts
- TLS delegated to oc/kubectl CLIs which use system OpenSSL
- Base image (ose-must-gather-rhel9) provides FIPS-compliant OpenSSL
- Build: Konflux hermetic build with digest-pinned base images

Build Hermeticity:
==================
- Konflux: hermetic: true, digest-pinned base images
- helm binary: vendored via Git LFS (not downloaded at build time)
- kubectl binary: copied from ose-cli-rhel9 builder stage
- No RPM lock files on main branch
- No language-level dependencies (pure Bash)
