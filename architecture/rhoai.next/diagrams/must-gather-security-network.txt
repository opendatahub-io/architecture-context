┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (User / SRE Workstation)                                       │
│                                                                          │
│  [User / SRE]                                                            │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: User kubeconfig (x509 cert or token)                     │
│         │ Tool: oc adm must-gather (OCP) / kubectl (xKS)                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP (external) → 6443/TCP (internal)                      │
│  - TLS Termination: TLS 1.2+                                            │
│  - AuthN: x509 client certs, Bearer tokens, OIDC                       │
│  - AuthZ: RBAC                                                          │
│         │                                                                │
│         │ Action: Pod Scheduling                                         │
│         │ Protocol: Internal Kubernetes scheduling                       │
│         │ Auth: ServiceAccount binding                                   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  MUST-GATHER POD (Ephemeral - runs as batch job)                         │
│                                                                          │
│  [must-gather Pod]                                                       │
│  - Namespace: openshift-must-gather-* (OCP) / must-gather (xKS)        │
│  - ServiceAccount: cluster-admin temp binding (OCP)                     │
│                     must-gather-sa (xKS)                                │
│  - Image: registry.redhat.io/rhoai/odh-must-gather-rhel9:rhoai-2.x    │
│  - Base: ose-must-gather-rhel9 (RHEL 9 UBI)                            │
│  - Capabilities: Read-only cluster access (no write operations)         │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  gather.sh (Entry Point)                                        │    │
│  │  - Detects K8s distribution (OCP/AKS/EKS/CKS)                 │    │
│  │  - Sets namespace list                                          │    │
│  │  - Dispatches 13 component gatherers in parallel               │    │
│  │                                                                 │    │
│  │  ┌───────────────────────────────────────────────────────────┐  │    │
│  │  │  Component Gatherers (13 parallel background processes)   │  │    │
│  │  │  - gather_serving.sh (KServe, Authorino, NIM, LLM-D)    │  │    │
│  │  │  - gather_data_science_pipelines.sh (DSP, Argo)          │  │    │
│  │  │  - gather_notebooks.sh (Workbenches)                     │  │    │
│  │  │  - gather_dashboard.sh (Dashboard config)                │  │    │
│  │  │  - gather_kuberay.sh (RayCluster/RayJob)                 │  │    │
│  │  │  - gather_kueue.sh (Queues, Workloads)                   │  │    │
│  │  │  - gather_kfto.sh (Training Operator)                    │  │    │
│  │  │  - gather_mr.sh (Model Registry)                         │  │    │
│  │  │  - gather_trustyai.sh (TrustyAI, LMEval)                │  │    │
│  │  │  - gather_feastoperator.sh (FeatureStore)                │  │    │
│  │  │  - gather_lls.sh (LlamaStack)                            │  │    │
│  │  │  - gather_mlflow.sh (MLflow)                             │  │    │
│  │  │  - gather_sparkoperator.sh (Spark)                       │  │    │
│  │  └───────────────────────────────────────────────────────────┘  │    │
│  │                                                                 │    │
│  │  ┌───────────────────────────────────────────────────────────┐  │    │
│  │  │  LLM-D Sub-Gatherers (nested parallelism)                │  │    │
│  │  │  - gather_llmd.sh → cert-manager.sh, lws.sh, sail.sh    │  │    │
│  │  │  - gather_wva.sh (optional KEDA/WVA)                    │  │    │
│  │  │  - gather_batch_gateway.sh (optional)                    │  │    │
│  │  └───────────────────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount token (in-cluster)                        │
│         │ Operations: GET only (read-only)                               │
│         │ Tools: oc adm inspect (OCP) / kubectl get (xKS)               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  TARGET NAMESPACES (Read-Only Inspection)                                │
│                                                                          │
│  [redhat-ods-operator]         Operator pods, logs, DSC, DSCI           │
│  [redhat-ods-applications]     Application component pods, configs      │
│  [rhods-notebooks]             Notebook pods, logs                       │
│  [redhat-ods-monitoring]       Monitoring stack                          │
│  [rhoai-model-registries]      Model registry pods                      │
│  [openshift-ingress]           Gateway/ingress controller state         │
│  [kuadrant-system]             Kuadrant operator state                  │
│  [istio-system]                Istio/Sail control plane                 │
│  [cert-manager]                cert-manager operator, certificates      │
│  [rhai-gitops]                 Helm releases (RHAII)                    │
│  [rhai-cloudmanager-system]    Cloud manager state (xKS)                │
│  [kube-system]                 Azure Managed Prometheus (AKS only)      │
│  [Dynamic namespaces]          Discovered via CRD instance listing      │
│                                                                          │
│  Collected data:                                                         │
│  - Custom Resource instances (YAML serialization)                       │
│  - Pod logs (current + previous containers)                             │
│  - Events                                                                │
│  - ConfigMaps, Secrets (metadata only)                                  │
│  - Deployments, ReplicaSets, StatefulSets                               │
│  - Gateway API resources (Gateway, HTTPRoute, GRPCRoute)                │
│  - Istio resources (EnvoyFilter, DestinationRule, VirtualService)       │
│  - Helm release values and manifests (RHAII)                            │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  DATA OUTPUT                                                             │
│                                                                          │
│  [Local Filesystem - $DST_DIR]                                           │
│  - Purpose: Temporary storage of collected diagnostic data              │
│  - Transport (OCP): tar stream → copy container → oc rsync → user      │
│  - Transport (xKS): kubectl cp (tar over HTTPS) → user                 │
│  - Encryption at rest: None (ephemeral pod filesystem)                  │
│  - Contains: YAML resources, pod logs, events, cluster info             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

OpenShift Mode (oc adm must-gather):
- Temporary ClusterRoleBinding to cluster-admin
  - Created by: oc adm must-gather framework
  - Lifetime: Duration of must-gather pod execution
  - Verbs: ALL (cluster-admin)
  - Cleanup: Automatic on pod termination

xKS Job Mode:
- ClusterRole: must-gather-reader
  - API Groups: * (all)
  - Resources: * (all)
  - Verbs: get, list, watch (read-only)
- ClusterRoleBinding: must-gather-reader-binding
  - ServiceAccount: must-gather-sa
  - Scope: Cluster-wide

Secrets:
- redhat-pull-secret (kubernetes.io/dockerconfigjson)
  - Purpose: Pull must-gather image from registry.redhat.io
  - Provisioned: Manual (user-created for xKS Job usage)
  - Auto-Rotate: No

FIPS Compliance:
- Pure Bash container (no compiled binaries from this repo)
- kubectl/helm binaries inherited from upstream images
- TLS configuration inherited from oc/kubectl CLI tools
- No direct cryptographic operations in Bash scripts
- Base image: ose-must-gather-rhel9 (RHEL 9 UBI with OpenSSL)

Network Summary:
- INGRESS: None (batch job, no exposed services)
- EGRESS: 443/TCP HTTPS to Kubernetes API server only
- No service mesh participation
- No inter-pod communication
- No external service calls (internet-isolated)
