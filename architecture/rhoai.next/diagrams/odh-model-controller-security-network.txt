┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / kubectl]                                                            │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: kubectl/oc credentials (Bearer token / client cert)          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE (Trusted)                                          │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 6443/TCP                                                            │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Bearer token, client certificate                                    │
│         │                                                                    │
│         │ Port: 9443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (OpenShift service-cert)                           │
│         │ Auth: mTLS (Kubernetes webhook config)                             │
│         │ Policy: failurePolicy: Fail (fail-closed)                          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  ADMISSION WEBHOOKS (Trusted - Service Mesh)                                 │
│                                                                              │
│  [Webhook Server]                                                            │
│  - Namespace: opendatahub                                                    │
│  - Service: odh-model-controller-webhook-service                             │
│  - Port: 443/TCP (external) → 9443/TCP (target)                             │
│  - Encryption: TLS (OpenShift CA injection)                                  │
│  - Secret: webhook-server-cert (kubernetes.io/tls)                           │
│  - 5 mutating webhooks:                                                      │
│    - InferenceService defaulter (credentials + HardwareProfile injection)    │
│    - LLMInferenceService defaulter v1alpha1 (credentials + HardwareProfile)  │
│    - LLMInferenceService defaulter v1alpha2 (credentials + HardwareProfile)  │
│    - InferenceGraph defaulter (no-op placeholder)                            │
│    - Pod mutator (Ray TLS init-container injection)                          │
│  - 3 validating webhooks:                                                    │
│    - InferenceService validator (namespace + name-length checks)             │
│    - NIM Account validator (singleton per namespace)                         │
│    - InferenceGraph validator (cross-namespace URL rejection)                │
│                                                                              │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         ▼                                                                    │
│  [HardwareProfile CRD / Connection Secrets]                                  │
│  - Accessed via Kubernetes API                                               │
│  - Purpose: Resolve scheduling constraints and credentials at admission      │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (opendatahub)                                            │
│                                                                              │
│  [odh-model-controller]                                                      │
│  - Namespace: opendatahub                                                    │
│  - ServiceAccount: odh-model-controller                                      │
│  - Image: quay.io/opendatahub/odh-model-controller                          │
│  - Replicas: 1 (leader election)                                             │
│  - Ports:                                                                    │
│    - 8080/TCP (metrics, HTTP, no encryption)                                 │
│    - 8081/TCP (health probes, HTTP, no encryption)                           │
│    - 9443/TCP (webhooks, HTTPS, TLS service-cert)                            │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - Creates: Routes, AuthPolicies, EnvoyFilters, ServiceMonitors,            │
│    PodMonitors, NetworkPolicies, ClusterRoleBindings, TriggerAuths,         │
│    Templates, Secrets, ConfigMaps                                            │
│                                                                              │
│  ─────────────────────────────────────────────────                           │
│                                                                              │
│  [model-serving-api]                                                         │
│  - Namespace: opendatahub                                                    │
│  - ServiceAccount: model-serving-api                                         │
│  - Image: quay.io/opendatahub/odh-model-serving-api                         │
│  - Service: model-serving-api                                                │
│  - Ports:                                                                    │
│    - 443/TCP (external) → 8443/TCP (HTTPS, FIPS cipher suites)              │
│    - 8080/TCP (metrics, HTTPS, TLS 1.2+)                                    │
│  - Secret: model-serving-api-tls (kubernetes.io/tls, OpenShift service-cert)│
│  - TLS Config: TLS 1.2+ min, ECDHE+AES-GCM only, NIST P-256/P-384 curves  │
│  - Cert reload: fsnotify hot-reload                                          │
│                                                                              │
│    Endpoint: GET /api/v1/gateways                                            │
│    - Auth: Bearer token → SelfSubjectAccessReview (K8s RBAC)                │
│    - Policy: User must have 'create' on llminferenceservices in namespace    │
│                                                                              │
│    Endpoint: GET /api/v1/samples/llm-d                                       │
│    - Auth: None                                                              │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  USER NAMESPACES (per-tenant)                                                │
│                                                                              │
│  [InferenceService Resources]                                                │
│  - Dynamic Route: {isvc-name}-{namespace}.apps.cluster                      │
│    - Port: 443/TCP                                                           │
│    - Encryption: TLS (Edge or Re-encrypt)                                    │
│    - Auth: OpenShift Route TLS only (no per-tenant auth)                     │
│                                                                              │
│  [LLMInferenceService Resources]                                             │
│  - Dynamic HTTPRoute via Gateway API                                         │
│    - Auth: Kuadrant AuthPolicy (Anonymous or UserDefined)                    │
│    - Enforcement: Authorino via EnvoyFilter                                  │
│                                                                              │
│  [Dynamic Secrets]                                                           │
│  - storage-config (Opaque): Aggregated S3 credentials                       │
│  - {account}-pull (dockerconfigjson): NIM pull secrets                      │
│  - ray-ca-tls / ray-tls (Opaque): Ray multi-node TLS certs (self-signed)   │
│  - inference-prometheus-auth (SA token): KEDA auth                          │
│                                                                              │
│  [Dynamic Resources]                                                         │
│  - NetworkPolicy (per InferenceService)                                      │
│  - ServiceMonitor (per InferenceService)                                     │
│  - ClusterRoleBinding (auth delegation)                                      │
│  - TriggerAuthentication (KEDA autoscaling)                                  │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SERVICES (Trusted)                                                 │
│                                                                              │
│  [Kuadrant / Authorino]                                                      │
│  - Purpose: Auth bootstrapping, per-service AuthPolicy                       │
│  - Interaction: CRD Watch + Create (AuthPolicy)                              │
│  - Encryption: TLS 1.2+ (Kubernetes API)                                     │
│                                                                              │
│  [Istio Control Plane]                                                       │
│  - Purpose: EnvoyFilter for Authorino TLS on Gateway                         │
│  - Interaction: CRD Create (EnvoyFilter)                                     │
│  - Encryption: TLS 1.2+ (Kubernetes API)                                     │
│                                                                              │
│  [KEDA]                                                                      │
│  - Purpose: Autoscaling via TriggerAuthentication                            │
│  - Interaction: CRD Create (TriggerAuthentication)                           │
│  - Encryption: TLS 1.2+ (Kubernetes API)                                     │
│                                                                              │
│  [Prometheus Operator]                                                       │
│  - Purpose: Metrics scraping configuration                                   │
│  - Interaction: CRD Create (ServiceMonitor, PodMonitor)                      │
│  - Encryption: TLS 1.2+ (Kubernetes API)                                     │
│                                                                              │
│  [OpenShift Router]                                                          │
│  - Purpose: External access for InferenceServices                            │
│  - Interaction: CRD Create (Route)                                           │
│  - Port: 443/TCP (external TLS)                                             │
│                                                                              │
│  [OpenShift Monitoring (Prometheus)]                                         │
│  - Purpose: KEDA metrics source                                              │
│  - Auth: ServiceAccount token                                                │
│  - Encryption: HTTPS TLS 1.2+                                               │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [NVIDIA NGC API - api.ngc.nvidia.com]                                       │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: NGC API Key                                                         │
│  - Purpose: NIM catalog discovery, API key validation, model metadata        │
│  - Risk: nim.go uses custom tls.Config with MinVersion TLS 1.2              │
│    (no explicit cipher suite restriction — potential non-FIPS crypto)         │
│                                                                              │
│  [Model Registry]                                                            │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (configurable skip via MR_SKIP_TLS_VERIFY)          │
│  - Auth: Bearer token                                                        │
│  - Purpose: Model registration sync (optional)                               │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRole: odh-model-controller-role
  ServiceAccount: odh-model-controller (namespace: opendatahub)
  Binding: odh-model-controller-rolebinding (cluster-wide)
  Permissions (26 rules):
    - core: configmaps, secrets, serviceaccounts, services → CRUD + watch
    - core: endpoints, namespaces, pods → create, get, list, patch, update, watch
    - core: events → create, patch
    - serving.kserve.io: inferenceservices, servingruntimes → CRUD + watch
    - serving.kserve.io: llminferenceservices → CRUD + watch + status
    - serving.kserve.io: inferencegraphs → get, list, watch + finalizers
    - nim.opendatahub.io: accounts → get, list, patch, update, watch + status/finalizers
    - kuadrant.io: authpolicies → CRUD + watch
    - kuadrant.io: kuadrants, operator.authorino.kuadrant.io: authorinos → read-only
    - gateway.networking.k8s.io: gateways, httproutes → get, list, patch, update, watch
    - networking.istio.io: envoyfilters → CRUD + watch
    - route.openshift.io: routes, routes/custom-host → CRUD + watch
    - monitoring.coreos.com: podmonitors, servicemonitors → CRUD + watch
    - networking.k8s.io: networkpolicies → CRUD + watch
    - keda.sh: triggerauthentications → CRUD + watch
    - rbac.authorization.k8s.io: clusterrolebindings, rolebindings, roles → CRUD + watch
    - template.openshift.io: templates → CRUD + watch
    - config.openshift.io: authentications → read-only
    - datasciencecluster.opendatahub.io: datascienceclusters → read-only
    - dscinitialization.opendatahub.io: dscinitializations → read-only
    - infrastructure.opendatahub.io: hardwareprofiles → read-only
    - metrics.k8s.io: nodes, pods → read-only

ClusterRole: proxy-role
  ServiceAccount: odh-model-controller
  Permissions:
    - authentication.k8s.io: tokenreviews → create
    - authorization.k8s.io: subjectaccessreviews → create

ClusterRole: model-serving-api
  ServiceAccount: model-serving-api (namespace: opendatahub)
  Permissions:
    - gateway.networking.k8s.io: gateways → list
    - core: namespaces → get

ClusterRole: kserve-prometheus-k8s
  Permissions:
    - core: services, endpoints, pods → get, list, watch

Dynamic Bindings:
  - {namespace}-{sa}-auth-delegator → system:auth-delegator (per InferenceService SA)
  - inference-prometheus-auth → per-namespace KEDA auth

Secrets:
  - model-serving-api-tls (kubernetes.io/tls) — HTTPS for API server, OpenShift service-cert, auto-rotate
  - webhook-server-cert (kubernetes.io/tls) — HTTPS for webhooks, OpenShift CA injection, auto-rotate
  - ray-ca-tls (Opaque) — Self-signed CA for Ray multi-node TLS, auto-rotate on expiry
  - ray-tls (Opaque) — Per-pod TLS certs for Ray workers, signed by ray-ca-tls, auto-rotate
  - storage-config (Opaque) — Aggregated S3 data connection credentials, no auto-rotate
  - {account}-pull (dockerconfigjson) — NIM container pull secrets, no auto-rotate
  - inference-prometheus-auth (SA token) — KEDA Prometheus auth, no auto-rotate

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking, UBI9 base
  - model-serving-api TLS: FIPS-compliant (TLS 1.2+, ECDHE+AES-GCM only, NIST curves)
  - Risk: NIM API client (nim.go) has no explicit FIPS cipher suite restriction
