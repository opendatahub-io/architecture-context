┌─────────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (WVA Controller)                                          │
│                                                                              │
│  [WVA Controller Manager]                                                    │
│  - Namespace: wva-system                                                     │
│  - ServiceAccount: controller-manager                                        │
│  - Image: quay.io/opendatahub/workload-variant-autoscaler:odh-stable         │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1                       │
│                                                                              │
│  Exposed Services:                                                           │
│  ┌─────────────────────────────────────────────────────────────┐              │
│  │ controller-manager-metrics-service (ClusterIP)              │              │
│  │ - Port: 8443/TCP                                            │              │
│  │ - Protocol: HTTPS                                           │              │
│  │ - Encryption: TLS (self-signed or Service CA)               │              │
│  │ - Auth: Bearer Token (SA token) + authn/authz filter        │              │
│  │ - Consumers: Prometheus/ServiceMonitor, HPA/KEDA            │              │
│  └─────────────────────────────────────────────────────────────┘              │
│  ┌─────────────────────────────────────────────────────────────┐              │
│  │ controller-manager-health-service (ClusterIP)               │              │
│  │ - Port: 8081/TCP                                            │              │
│  │ - Protocol: HTTP                                            │              │
│  │ - Encryption: None                                          │              │
│  │ - Auth: None                                                │              │
│  │ - Endpoints: /healthz (liveness), /readyz (readiness)       │              │
│  └─────────────────────────────────────────────────────────────┘              │
│                                                                              │
│         │                                                                    │
│         │ EGRESS CONNECTIONS                                                 │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS: Prometheus / Thanos Querier                                         │
│                                                                              │
│  [Prometheus / Thanos Querier]                                               │
│  - Port: 443/TCP or 9090/TCP                                                 │
│  - Protocol: HTTPS or HTTP                                                   │
│  - Encryption: TLS 1.2+ (configurable)                                       │
│  - Auth: Bearer Token (SA token or PROMETHEUS_BEARER_TOKEN)                  │
│  - Note: HTTP allowed only with PROMETHEUS_ALLOW_HTTP=true                   │
│  - Note: InsecureSkipVerify available (disabled by default)                  │
│  - Purpose: Query inference engine metrics                                   │
│    (KV cache, queue depth, TTFT, ITL, token rates, request counts)           │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS: Kubernetes API Server                                               │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Bearer Token (SA token)                                             │
│  - Purpose: Watch/patch HPAs, ScaledObjects, Deployments, Nodes,             │
│    Pods, ConfigMaps, InferencePools, ResourceQuotas, Events                  │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS: Gateway API EPP (optional)                                          │
│                                                                              │
│  [EPP Metrics Endpoint]                                                      │
│  - Port: Pod port (variable)                                                 │
│  - Protocol: HTTP or HTTPS                                                   │
│  - Encryption: Configurable                                                  │
│  - Auth: Bearer Token (optional)                                             │
│  - Purpose: Scrape flow-control queue metrics for scale-from-zero            │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS: Metrics Consumers                                                  │
│                                                                              │
│  [HPA / KEDA]                                                                │
│  - Port: 8443/TCP → controller-manager-metrics-service                       │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS                                                           │
│  - Auth: Bearer Token                                                        │
│  - Purpose: Read wva_desired_replicas external metric                        │
│                                                                              │
│  [Prometheus (ServiceMonitor)]                                               │
│  - Port: 8443/TCP → controller-manager-metrics-service                       │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS                                                           │
│  - Auth: Bearer Token (SA token from controller-manager-token secret)        │
│  - Purpose: Scrape operational metrics                                       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: manager-role
  Core API:
  - pods, nodes, nodes/status, configmaps, namespaces, resourcequotas, services → get, list, watch
  - configmaps/status → get
  - events → create, patch
  Apps API:
  - deployments → get, list, patch, update, watch
  - deployments/scale → get, update
  - replicasets, statefulsets → get, list, watch
  Autoscaling API:
  - horizontalpodautoscalers → get, list, patch, update, watch
  inference.networking.k8s.io / inference.networking.x-k8s.io:
  - inferencepools → get, list, watch
  keda.sh:
  - scaledobjects → get, list, patch, update, watch
  leaderworkerset.x-k8s.io:
  - leaderworkersets → get, list, patch, update, watch
  - leaderworkersets/scale → get, update
  monitoring.coreos.com:
  - servicemonitors → get, list, watch

- ClusterRoleBinding: manager-rolebinding
  - Role: manager-role (ClusterRole)
  - Subject: controller-manager SA in wva-system

- RoleBinding: leader-election-rolebinding (wva-system)
  - Role: leader-election-role (Role)
  - Subject: controller-manager SA

Secrets:
- controller-manager-token (kubernetes.io/service-account-token)
  Purpose: Long-lived SA token for ServiceMonitor bearer auth (OpenShift)
  Provisioned By: OpenShift component kustomization
  Auto-Rotate: No

- webhook-cert (kubernetes.io/tls, optional)
  Purpose: Webhook server TLS certificate
  Provisioned By: Manual or cert-manager

- metrics-cert (kubernetes.io/tls, optional)
  Purpose: Metrics server TLS certificate
  Provisioned By: Manual, cert-manager, or Service CA (OpenShift)

TLS Configuration:
- MinVersion: TLS 1.2 enforced (internal/utils/tls.go)
- FIPS: strictfipsruntime with OpenSSL via CGO
- InsecureSkipVerify: Available via PROMETHEUS_TLS_INSECURE_SKIP_VERIFY (disabled by default)
- No MD5/RC4/DES usage detected

Multi-Tenancy Notes:
- Tenant boundary: per-namespace
- Namespace opt-in: llm-d.io/namespace-config-enabled=true label
- Namespace exclusion: llm-d.io/namespace-exclude=true annotation
- Annotation-based filtering (llm-d.ai/managed=true) is NOT a security boundary
- ClusterRole grants cluster-wide read access to pods, nodes, namespaces
