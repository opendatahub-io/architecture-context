┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: Authorization header (pass-through, not enforced)        │
│         │                                                                │
│         │ Note: TLS termination expected at ingress/service mesh layer   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (vllm-orchestrator-gateway)                                │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Language: Rust 1.86.0 (axum framework)                               │
│  - Listener: 0.0.0.0:8090/TCP (HTTP, plaintext)                        │
│  - Configurable via HTTP_PORT env var                                    │
│  - Endpoints: /{route-name}/v1/chat/completions (POST)                  │
│  - Routes dynamically generated from config.yaml                        │
│  - No authentication enforcement at this layer                          │
│  - No RBAC / No Kubernetes API access                                   │
│  - Stateless — no persistent storage                                    │
│         │                                                                │
│         │ Port: 8085/TCP (configurable)                                  │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS                                      │
│         │   - When certs present: mTLS with PKCS12 identity             │
│         │   - When certs absent: plaintext HTTP fallback                 │
│         │ Auth: Client certificate (mTLS) +                              │
│         │       forwarded Authorization header +                         │
│         │       forwarded X-Forwarded-* headers                          │
│         │                                                                │
│         │ mTLS Details:                                                   │
│         │   Client cert: /etc/tls/private/tls.crt                       │
│         │   Client key:  /etc/tls/private/tls.key                       │
│         │   CA cert:     /etc/tls/ca/service-ca.crt                     │
│         │   Identity:    PKCS12 bundle (constructed at startup)          │
│         │                                                                │
│         │ Note: danger_accept_invalid_hostnames(true) when               │
│         │       orchestrator host is localhost (dev only)                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INTERNAL SERVICES (TrustyAI Ecosystem)                                 │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Endpoint: /api/v2/chat/completions-detection                         │
│  - Port: 8085/TCP (configurable)                                        │
│  - Purpose: LLM inference with detector-based content filtering         │
│  - Auth: Receives forwarded Authorization header                        │
│         │                                                                │
│         │ (Internal orchestrator calls — not gateway-initiated)          │
│         ▼                                                                │
│  [Detector Services]                                                     │
│  - e.g., guardrails-regex-detector                                       │
│  - Purpose: Content detection (PII, regex-based filters)                │
│  - Called by orchestrator, not by gateway directly                       │
│                                                                          │
│  [vLLM Inference Server]                                                 │
│  - Purpose: LLM serving backend                                         │
│  - Called by orchestrator for chat completions                           │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CERTIFICATE INFRASTRUCTURE (OpenShift)                                 │
│                                                                          │
│  [service-serving-cert-signer]                                           │
│  - Provisions: /etc/tls/private/tls.crt, /etc/tls/private/tls.key      │
│  - Type: kubernetes.io/tls                                               │
│  - Auto-Rotate: Yes (OpenShift managed)                                  │
│                                                                          │
│  [service-ca-operator]                                                   │
│  - Provisions: /etc/tls/ca/service-ca.crt                               │
│  - Type: ConfigMap (projected)                                           │
│  - Auto-Rotate: Yes (OpenShift managed)                                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC manifests present
- No ClusterRoles, Roles, or RoleBindings defined
- No ServiceAccount definitions in this repository
- This is a stateless HTTP proxy without Kubernetes API access

Authentication & Authorization:
- Gateway Level: NONE — no authentication enforced
- Pass-through: Authorization header forwarded to orchestrator
- Pass-through: X-Forwarded-* headers forwarded to orchestrator
- Security relies on:
  - Network-level isolation (NetworkPolicy — not shipped in this repo)
  - Service mesh (expected but not configured in this repo)
  - Orchestrator's own auth enforcement

Secrets:
- TLS client cert (/etc/tls/private/tls.crt, tls.key)
  - Type: kubernetes.io/tls
  - Purpose: mTLS client identity for outbound orchestrator communication
  - Provisioned By: OpenShift service-serving-cert-signer
  - Auto-Rotate: Yes
- CA certificate (/etc/tls/ca/service-ca.crt)
  - Type: ConfigMap (projected)
  - Purpose: Validate orchestrator's server certificate
  - Provisioned By: OpenShift service-ca-operator
  - Auto-Rotate: Yes

FIPS Compliance Notes:
- Rust binary dynamically links against system OpenSSL (openssl-libs via UBI9)
- openssl crate v0.10.73 (FFI to system OpenSSL)
- native-tls crate v0.2.12 (wraps system OpenSSL on Linux)
- No non-FIPS cipher suites detected
- Risk: danger_accept_invalid_hostnames(true) when host=localhost (dev only)

Security Gaps:
- No NetworkPolicy shipped — relies on deployment environment
- No authentication at gateway level — any network-reachable client can send requests
- No resource limits defined — relies on external ResourceQuota/LimitRange
- Streaming: partial content may be delivered before detection triggers on later chunk
