┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLUSTER (Trusted Zone)                                         │
│                                                                          │
│  NOTE: This service has NO ingress from outside the cluster.             │
│  All access is internal-only, relying on network-level isolation.        │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CALLER SERVICE                                                          │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: (platform-managed)                                         │
│  - Role: Sends detection requests to regex detector                      │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None                                                     │
│         │ Method: POST /api/v1/text/contents                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  TARGET SERVICE                                                          │
│                                                                          │
│  [guardrails-regex-detector]                                             │
│  - Language: Rust (axum + tokio)                                         │
│  - Listen: 0.0.0.0:8080 (configurable via HTTP_PORT, HOST env vars)     │
│  - Base Image: ubi9/ubi-minimal                                          │
│  - User: root (no USER directive in Dockerfile — security concern)       │
│  - Stateless: Yes (no DB, no state, no outbound calls)                   │
│                                                                          │
│  Endpoints:                                                              │
│  ┌────────────────────────────────────────────────┐                      │
│  │ GET  /health              → "healthy"          │                      │
│  │ POST /api/v1/text/contents → detection results │                      │
│  └────────────────────────────────────────────────┘                      │
│                                                                          │
│  No outbound connections.                                                │
│         │                                                                │
│         │ Response: HTTP 200                                             │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         ▼                                                                │
│                                                                          │
│  [FMS Guardrails Orchestrator]  ← receives detection results             │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None — no outbound network calls.                                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK EXPOSURE:
- External Ingress: None
- Internal Ingress: 8080/TCP HTTP (plaintext, unauthenticated)
- Egress: None

RBAC Summary:
- No RBAC — service does not interact with the Kubernetes API

Service Mesh Configuration:
- No service mesh configuration in repository
- TLS/mTLS enforcement assumed to be provided externally if required
- No PeerAuthentication or AuthorizationPolicy defined

Secrets:
- No secrets referenced or required

Authentication & Authorization:
- No authentication mechanism implemented
- No authorization mechanism implemented
- Access control relies entirely on network-level isolation:
  - Kubernetes NetworkPolicy (if configured externally)
  - Service mesh mTLS (if deployed with sidecar)
  - No manifests in repo to establish isolation

SECURITY CONCERNS:
1. Container runs as root (no USER directive in Dockerfile)
2. No TLS — plaintext HTTP only (relies on external TLS termination)
3. No authentication — any pod with network access can call the API
4. No Konflux build pipeline — misses check-payload, hermetic builds, CVE scanning
5. OS packages (compat-openssl11, shadow-utils) installed without version pinning

FIPS COMPLIANCE:
- No cryptographic operations performed
- No TLS implementation (plaintext HTTP)
- compat-openssl11 installed in runtime image for compatibility, not for crypto
- Rust binary dynamically linked against glibc
- No Go FIPS mechanisms applicable (Rust binary)
