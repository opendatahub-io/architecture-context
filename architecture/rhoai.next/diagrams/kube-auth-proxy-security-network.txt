┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL CLIENTS (Untrusted)                                               │
│                                                                              │
│  [Browser / Client]                                                          │
│         │                                                                    │
│         │ Port: 4180/TCP (HTTP) or 443/TCP (HTTPS)                          │
│         │ Protocol: HTTP or HTTPS                                            │
│         │ Encryption: None (HTTP) or TLS 1.2+ (HTTPS)                       │
│         │ Auth: None (sign-in), Session Cookie, Bearer JWT                   │
│         ▼                                                                    │
│  [Envoy ext_authz (Service Mesh)]                                            │
│         │                                                                    │
│         │ Port: 4180/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Path: /oauth2/auth                                                 │
│         │ Auth: Forwarded cookies / Bearer token                             │
│         │ Response: 202 Accepted (pass) or 401/403 (deny)                   │
│         ▼                                                                    │
│  [Kubernetes Client (Service Account)]                                       │
│         │                                                                    │
│         │ Port: 4180/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Auth: Authorization: Bearer <SA token>                             │
│         ▼                                                                    │
│  [MLflow Python SDK]                                                         │
│         │                                                                    │
│         │ Port: 4180/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Auth: Bearer token / None                                          │
│         │ User-Agent: mlflow-python-client/*                                 │
│         ▼                                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBE-AUTH-PROXY (Sidecar / Reverse Proxy)                                  │
│  Deployment: Sidecar container injected by rhods-operator                    │
│  Image: UBI 9 Minimal (FIPS-compliant)                                      │
│  Build: CGO_ENABLED=1 GOEXPERIMENT=strictfipsruntime                        │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Listeners                                                          │    │
│  │  - HTTP:  4180/TCP (default proxy port)                             │    │
│  │  - HTTPS: 443/TCP  (if --tls-cert-file/--tls-key-file configured)  │    │
│  │  - Metrics: configurable port (separate server)                     │    │
│  │                                                                     │    │
│  │  TLS Configuration (when HTTPS enabled):                            │    │
│  │  - MinVersion: TLS 1.2                                              │    │
│  │  - MaxVersion: TLS 1.3                                              │    │
│  │  - NextProtos: ["http/1.1"]                                         │    │
│  │  - CipherSuites: configurable (defaults to Go stdlib)               │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Authentication Chain (middleware pipeline)                         │    │
│  │                                                                     │    │
│  │  1. K8s SA Token → TokenReview API validation                      │    │
│  │  2. JWT/OAuth Bearer → OIDC ID token verification (JWKS)           │    │
│  │  3. Basic Auth → htpasswd file validation                          │    │
│  │  4. Stored Session → Cookie decryption + session validation         │    │
│  │                                                                     │    │
│  │  Authorization:                                                     │    │
│  │  - allowed_groups (group membership check)                          │    │
│  │  - allowed_emails (email allowlist)                                 │    │
│  │  - allowed_email_domains (domain allowlist)                         │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Session Encryption                                                 │    │
│  │  - Algorithm: AES-GCM (primary) / AES-CFB (fallback)              │    │
│  │  - HMAC: SHA-256 for session integrity                              │    │
│  │  - Cookie splitting: auto-split at 4KB limit                       │    │
│  │  - Distributed lock: prevents thundering herd on token refresh     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│         │                                                                    │
│         │ Authenticated request                                              │
│         │ Headers injected:                                                  │
│         │   X-Forwarded-User, X-Forwarded-Email,                            │
│         │   X-Forwarded-Groups, X-Forwarded-Preferred-Username              │
│         ▼                                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  UPSTREAM APPLICATION (Pod-local)                                            │
│                                                                              │
│  [Upstream Application]                                                      │
│  - Port: configurable                                                        │
│  - Protocol: HTTP / HTTPS / Unix socket                                      │
│  - Encryption: Configurable (InsecureSkipVerify option, disabled by default)│
│  - Auth: None (trusts injected identity headers from kube-auth-proxy)       │
│  - Transport: ForceAttemptHTTP2, NextProtos, TLS 1.2+ min                   │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS - IDENTITY PROVIDERS                                                 │
│                                                                              │
│  [OIDC Identity Provider]                                                    │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Endpoints:                                                                │
│    - GET  /.well-known/openid-configuration  (discovery, no auth)           │
│    - GET  /jwks                              (JWKS, no auth)                │
│    - GET  /authorize                         (auth code flow, Client ID)    │
│    - POST /token                             (code exchange, Client Secret) │
│    - GET  /userinfo                          (profile, Bearer Token)        │
│    - GET  /end_session                       (RP-initiated logout)          │
│  - Auth: Client ID/Secret (Basic or POST body), PKCE code_challenge         │
│                                                                              │
│  [OpenShift OAuth Server]                                                    │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (custom CA bundle)                                  │
│  - Endpoints:                                                                │
│    - GET  /.well-known/oauth-authorization-server (discovery, no auth)      │
│    - GET  /authorize                              (auth code flow)          │
│    - POST /token                                  (code exchange)           │
│  - Auth: Client ID/Secret                                                   │
│  - CA: Configurable custom CA bundle file                                    │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS - KUBERNETES APIs                                                    │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (SA CA: /var/run/secrets/.../ca.crt)               │
│  - Endpoints:                                                                │
│    - POST /api/v1/tokenreviews                                              │
│      Auth: Service Account Token (auto-mounted)                              │
│      Purpose: Validate K8s SA bearer tokens                                  │
│                                                                              │
│  [OpenShift API Server]                                                      │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (SA CA)                                             │
│  - Endpoints:                                                                │
│    - GET    /apis/user.openshift.io/v1/users/~                              │
│      Auth: Bearer Token                                                      │
│      Purpose: Retrieve user info (name, groups, email)                       │
│    - DELETE /apis/oauth.openshift.io/v1/oauthaccesstokens/{sha256~hash}    │
│      Auth: Bearer Token                                                      │
│      Purpose: Clean up OAuthAccessToken on sign-out                          │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS - OPTIONAL SERVICES                                                  │
│                                                                              │
│  [Redis - Session Store]                                                     │
│  - Port: 6379/TCP                                                            │
│  - Protocol: Redis                                                           │
│  - Encryption: Optional TLS (custom CA)                                      │
│  - Auth: Password or ACL                                                     │
│  - Modes: Standalone, Sentinel, Cluster                                      │
│  - Purpose: Server-side session persistence, distributed lock                │
│                                                                              │
│  [Backend Logout URL (optional)]                                             │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: id_token                                                            │
│  - Purpose: Server-side IdP logout notification                              │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                  │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: configurable (metrics server)                                       │
│  - Protocol: HTTP or HTTPS                                                   │
│  - Encryption: Configurable TLS                                              │
│  - Auth: None                                                                │
│  - Metrics:                                                                  │
│    - oauth2_proxy_requests_total (counter)                                   │
│    - oauth2_proxy_requests_in_flight (gauge)                                 │
│    - oauth2_proxy_response_duration_seconds (histogram)                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ServiceAccount: configured by rhods-operator (deploying operator)
- Required API access (when K8s token validation enabled):
  - API Group: authentication.k8s.io
  - Resources: tokenreviews
  - Verbs: create

Secrets:
- cookie-secret (Opaque) - AES-GCM/CFB + HMAC encryption key for session cookies, provisioned by Operator/Manual, NO auto-rotate
- client-secret (Opaque) - OAuth2 client secret for OIDC/OpenShift, provisioned by IdP registration/Manual, NO auto-rotate
- client-secret-file (Opaque) - File-based OAuth2 client secret (alternative), provisioned by Operator/Manual, YES file watch auto-reload
- tls-cert/tls-key (kubernetes.io/tls) - TLS cert+key for HTTPS listener, provisioned by cert-manager/Manual, Manual rotation
- redis-password (Opaque) - Redis auth password (if Redis sessions), provisioned Manually, NO auto-rotate
- htpasswd-file (Opaque) - htpasswd for basic auth (optional), provisioned Manually, YES file watch auto-reload
- SA token (kubernetes.io/service-account-token) - In-cluster K8s auth, provisioned by Kubernetes, YES auto-rotate

FIPS Compliance:
- Build: CGO_ENABLED=1 GOEXPERIMENT=strictfipsruntime -tags strictfipsruntime
- Base image: UBI 9 Minimal (OpenSSL included)
- TLS: 1.2 minimum, 1.3 maximum, configurable cipher suites
- Crypto: AES-GCM, AES-CFB, HMAC-SHA256, crypto/rand (stdlib via OpenSSL at runtime)
- No deprecated crypto: No MD5, RC4, or DES usage detected
- CI: Automated check-payload validation in GitHub Actions
