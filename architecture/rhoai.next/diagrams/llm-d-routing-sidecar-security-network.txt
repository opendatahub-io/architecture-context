┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client / Gateway EPP]                                                  │
│         │                                                                │
│         │ Port: 8000/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+ (6 AEAD ciphers: AES-GCM,               │
│         │             ChaCha20-Poly1305 with ECDHE)                      │
│         │ Auth: None                                                     │
│         │ Header: x-prefiller-host-port (optional, set by EPP)          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SIDECAR (Pod-local Proxy)                                              │
│                                                                          │
│  [llm-d-routing-sidecar]                                                │
│  - Port: 8000/TCP (listener, HTTPS)                                     │
│  - TLS: Self-signed RSA 4096-bit (default) or external cert             │
│    via --cert-path (tls.crt/tls.key)                                    │
│  - MinVersion: TLS 1.2                                                   │
│  - CipherSuites: TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,               │
│    TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,                              │
│    TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256,                            │
│    TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384,                            │
│    TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256,                        │
│    TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256                       │
│  - Namespace: injected into decoder pod namespace                        │
│  - ServiceAccount: pod's ServiceAccount (patched at deploy)             │
│                                                                          │
│  [AllowlistValidator] (in-process)                                       │
│  - Watches: InferencePool CRs + matching Pods                           │
│  - SSRF Protection: validates prefiller target IPs against allowlist    │
│  - Resync: 30 seconds                                                    │
│  - Response: 403 Forbidden if target not in allowlist                    │
│         │                           │                                    │
│         │ (P/D flow)                │ (Direct decode flow)               │
│         ▼                           ▼                                    │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS - INFERENCE TARGETS                                             │
│                                                                          │
│  [Remote Prefiller Pod]                                                  │
│  - Port: dynamic (from x-prefiller-host-port header)                    │
│  - Protocol: HTTP or HTTPS                                               │
│  - Encryption: Optional TLS 1.2+ (--prefiller-use-tls flag)            │
│  - InsecureSkipVerify: available (--prefiller-insecure-tls, off default)│
│  - Auth: None                                                            │
│  - SSRF: Target IP validated against InferencePool pod allowlist        │
│  - Data: Sends modified request (do_remote_decode=true, stream=false,   │
│          max_tokens=1); receives kv_transfer_params in response          │
│                                                                          │
│  [Local vLLM Decoder] (co-located in same pod)                          │
│  - Port: configurable (default 8001/TCP)                                │
│  - Protocol: HTTP or HTTPS                                               │
│  - Encryption: Optional TLS 1.2+ (--decoder-use-tls flag)              │
│  - InsecureSkipVerify: available (--decoder-insecure-tls, off default)  │
│  - Auth: None                                                            │
│  - Data: Receives original request + kv_transfer_params from prefiller  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS - CONTROL PLANE                                                 │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount Bearer Token                                     │
│  - Purpose: Watch InferencePool CRs and Pods for SSRF allowlist        │
│  - API Groups: inference.networking.x-k8s.io (inferencepools),          │
│                core (pods)                                               │
│  - Verbs: get, watch, list                                               │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Standalone Testing Only)                                      │
│                                                                          │
│  [OpenShift Route]                                                       │
│  - Port: 443/TCP (external) → 8080/TCP (internal)                      │
│  - TLS Mode: Edge termination                                            │
│  - Note: Only in standalone deploy manifests, NOT used in production    │
│          RHOAI where sidecar is pod-local                                │
│                                                                          │
│  [ClusterIP Service]                                                     │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None                                                      │
│  - Exposure: Internal only                                               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- Role: ${PROJECT_NAME}-exec-role
  - API Groups: ""
  - Resources: pods/exec
  - Verbs: create
  - Binding: system:authenticated (Group)

- Role: ssrf-allowlist-role
  - API Groups: ""
  - Resources: pods
  - Verbs: get, watch, list
  - API Groups: inference.networking.x-k8s.io
  - Resources: inferencepools
  - Verbs: get, watch, list
  - Binding: ServiceAccount (patched at deploy)

Secrets:
- TLS certificate (via --cert-path): kubernetes.io/tls - TLS termination
  - Provisioned by: External (cert-manager or manual)
  - Fallback: Self-signed RSA 4096-bit, 10-year validity (testing only)

Security Considerations:
- InsecureSkipVerify available for both prefiller and decoder TLS
  (disabled by default, enabled via CLI flags)
- Self-signed cert fallback is for testing only
- SSRF allowlist is optional (requires K8s RBAC for InferencePool watching)
- No authentication on any endpoint (auth delegated to upstream gateway)
- FIPS: CGO_ENABLED=1 with UBI 9 base (Konflux Dockerfile only);
  no GOEXPERIMENT=strictfipsruntime set

FIPS Compliance:
- Build: CGO_ENABLED=1, dynamic linking against OpenSSL (UBI 9 base)
- Konflux Dockerfile only (upstream Dockerfile uses CGO_ENABLED=0, would fail check-payload)
- TLS: MinVersion TLS 1.2, AEAD-only cipher suites (no CBC)
- Crypto: stdlib crypto/* via OpenSSL when CGO_ENABLED=1
- Risk: No strictfipsruntime build tag enforced
