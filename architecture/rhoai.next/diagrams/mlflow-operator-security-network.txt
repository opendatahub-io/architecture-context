┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client]                                                          │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Platform auth (kube-rbac-proxy in gateway stack)            │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                      │
│                                                                             │
│  [data-science-gateway (Envoy)]                                             │
│  - Namespace: openshift-ingress                                             │
│  - Port: 443/TCP (external) → 8443/TCP (internal via HTTPRoute)            │
│  - TLS Mode: SIMPLE (TLS 1.2+, gateway-managed)                            │
│  - Auth: Platform-level auth (kube-rbac-proxy)                              │
│         │                                                                   │
│         │ Port: 8443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (OpenShift service-ca auto-provisioned cert)      │
│         │ Auth: Kubernetes auth token                                        │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-applications)                               │
│                                                                             │
│  [mlflow-operator Pod]                                                      │
│  - ServiceAccount: mlflow-operator-controller-manager                       │
│  - Image: odh-mlflow-operator-rhel9                                         │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1                     │
│  - TLS Profile: OpenShift APIServer TLS profile (Intermediate fallback)    │
│  - Ports:                                                                   │
│    - 8081/TCP (HTTP) - Health probes (/healthz, /readyz) - No auth         │
│    - 8443/TCP (HTTPS) - Metrics (/metrics) - authn/authz filter            │
│         │                                                                   │
│         │ Port: 6443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount token                                        │
│         ▼                                                                   │
│  [Kubernetes API Server]                                                    │
│  - Purpose: CRUD on all managed resources (Server-Side Apply)              │
│  - Resources: Deployments, Services, NetworkPolicies, SAs, RBAC,           │
│    PVCs, ServiceMonitors, CronJobs, HTTPRoutes, ConsoleLinks               │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  TARGET NAMESPACE (redhat-ods-applications / configurable)                  │
│                                                                             │
│  [MLflow Server Pod]                                                        │
│  - ServiceAccount: mlflow-sa                                                │
│  - Port: 8443/TCP (HTTPS)                                                  │
│  - Encryption: TLS (service-ca auto-provisioned cert via mlflow-tls Secret)│
│  - Auth: SelfSubjectAccessReview per request (kubernetes-auth plugin)      │
│  - CA Trust: system CAs + platform CAs (odh-trusted-ca-bundle) + custom   │
│         │                                                                   │
│         │ NetworkPolicy Ingress: Any namespace on port 8443/TCP            │
│         │                                                                   │
│         │                                                                   │
│         ├──────────────────────────────────────┐                            │
│         │                                      │                            │
│         │ Port: 5432/TCP                       │ Port: 443/TCP or 9000/TCP  │
│         │ Protocol: PostgreSQL                 │ Protocol: HTTPS            │
│         │ Encryption: TLS (PGSSLMODE)          │ Encryption: TLS 1.2+      │
│         │ Auth: Connection string credentials  │ Auth: AWS IAM / credentials│
│         ▼                                      ▼                            │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [PostgreSQL Database]                                                      │
│  - Port: 5432/TCP                                                           │
│  - Purpose: Backend/registry store                                          │
│  - Encryption: TLS (configurable via PGSSLMODE)                            │
│  - Auth: Connection string credentials                                      │
│                                                                             │
│  [S3-Compatible Storage]                                                    │
│  - Ports: 443/TCP, 9000/TCP, 8333/TCP, 8334/TCP                           │
│  - Purpose: Artifact storage (MinIO, SeaweedFS, AWS S3, GCS)              │
│  - Encryption: TLS 1.2+                                                    │
│  - Auth: AWS IAM / per-namespace credentials (mlflow-artifact-connection)  │
│                                                                             │
│  [DNS (CoreDNS)]                                                            │
│  - Ports: 53/UDP, 53/TCP, 5353/UDP, 5353/TCP                              │
│  - Purpose: Name resolution                                                │
│  - Encryption: None                                                         │
│  - Auth: None                                                               │
│                                                                             │
│  [MySQL Database] (alternative)                                             │
│  - Port: 3306/TCP                                                           │
│  - Purpose: Backend/registry store (alternative to PostgreSQL)             │
│  - Encryption: TLS (configurable)                                          │
│  - Auth: Connection string credentials                                      │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  Operator Service Account: mlflow-operator-controller-manager
  ─────────────────────────────────────────────────────────────
  ClusterRole: manager-role
    - mlflow.opendatahub.io: mlflows, mlflows/status, mlflows/finalizers
      Verbs: get, list, watch, create, update, patch, delete
    - components.platform.opendatahub.io: mlflowoperators, mlflowoperators/status, mlflowoperators/finalizers
      Verbs: get, list, watch, update, patch
    - gateway.networking.k8s.io: httproutes
      Verbs: get, list, watch, create, update, patch, delete
    - console.openshift.io: consolelinks
      Verbs: get, list, watch, create, update, patch, delete
    - config.openshift.io: apiservers
      Verbs: get, list, watch
    - "": namespaces
      Verbs: get, list, watch
    - "": secrets (resourceNames: mlflow-artifact-connection)
      Verbs: get, list, watch
    - rbac.authorization.k8s.io: clusterroles, clusterrolebindings, rolebindings
      Verbs: create + scoped update/delete by resourceNames
    - services.platform.opendatahub.io: auths (resourceNames: auth)
      Verbs: get, list, watch

  Role (namespace-scoped): manager-role
    - "": configmaps, secrets, serviceaccounts, services, persistentvolumeclaims
      Verbs: create, delete, get, list, patch, update, watch
    - apps: deployments
      Verbs: create, delete, get, list, patch, update, watch
    - batch: jobs, cronjobs
      Verbs: create, delete, get, list, patch, update, watch
    - networking.k8s.io: networkpolicies
      Verbs: create, delete, get, list, patch, update, watch
    - monitoring.coreos.com: servicemonitors
      Verbs: create, delete, get, list, patch, update, watch

  MLflow Server Service Account: mlflow-sa
  ─────────────────────────────────────────
  ClusterRole: mlflow
    - "": namespaces - get, list, watch
    - "": secrets (resourceNames: mlflow-artifact-connection) - get, list, watch
    - mlflow.kubeflow.org: mlflowconfigs - get, list, watch

  Workspace RBAC (per-namespace):
    - mlflow-view → Auth CR allowedGroups + adminGroups
    - mlflow-edit → Auth CR adminGroups only

Secrets:
  - mlflow-tls (kubernetes.io/tls)
    - Purpose: TLS certificate for MLflow HTTPS server
    - Provisioned: OpenShift service-ca (auto-rotation)
  - mlflow-artifact-connection (Opaque)
    - Purpose: Per-namespace artifact storage credentials (S3/GCS keys)
    - Provisioned: User/admin (manual)

NetworkPolicy:
  - Ingress: Any namespace → MLflow pod on port 8443/TCP
  - Egress: DNS (53/UDP,TCP, 5353/UDP,TCP), HTTPS (443, 6443, 8443/TCP),
            PostgreSQL (5432/TCP), MySQL (3306/TCP),
            S3-compatible (9000, 8333, 8334/TCP)

Authentication Summary:
  - MLflow server API: SelfSubjectAccessReview per request (kubernetes-auth plugin)
  - Operator /metrics: authn/authz filter (TokenReview + SubjectAccessReview)
  - Gateway-exposed /mlflow/*: Platform gateway auth (kube-rbac-proxy)

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
  - Runtime: OpenSSL via stdlib crypto/tls (strictfipsruntime)
  - TLS: OpenShift TLS profile with Intermediate defaults fallback
