┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [ML Training Code / Python SDK]                                         │
│  [Client Application]                                                    │
│  [OTel-instrumented Service]                                             │
│  [ML Workload Pod]                                                       │
│         │                                                                │
│         │ Port: 443/TCP (via Ingress) or 5000/TCP (direct)               │
│         │ Protocol: HTTPS (Ingress) / HTTP (direct)                      │
│         │ Encryption: TLS (Ingress) / Optional TLS (direct)              │
│         │ Auth: Basic Auth (username/password)                           │
│         │       OR Kubernetes Token (Bearer)                             │
│         │       OR Kubernetes Namespaced (Bearer + X-MLFLOW-WORKSPACE)   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) - Optional, when ingress.enabled=true                    │
│                                                                          │
│  [Kubernetes Ingress]                                                    │
│  - Port: 443/TCP (external) → 5000/TCP (internal)                      │
│  - TLS Termination: TLS (configurable via ingress.tls)                  │
│  - Hosts: Configurable (ingress.hosts)                                  │
│  - TLS Mode: SIMPLE                                                      │
│         │                                                                │
│         │ Port: 5000/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (cluster-internal)                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (Cluster-Internal)                                          │
│                                                                          │
│  [MLflow Server]                                                         │
│  - Service: mlflow (ClusterIP)                                           │
│  - Port: 5000/TCP                                                        │
│  - Protocol: HTTP (TLS optional via Helm tls.enabled)                   │
│  - ServiceAccount: mlflow                                                │
│  - automountServiceAccountToken: false (default)                        │
│  - Security Context:                                                     │
│    - runAsNonRoot: true                                                  │
│    - readOnlyRootFilesystem: true                                        │
│    - seccompProfile: RuntimeDefault                                      │
│    - capabilities: drop ALL                                              │
│  - Auth Enforcement:                                                     │
│    - Flask before_request (Basic Auth, RBAC)                            │
│    - FastAPI middleware (Bearer Token, workspace mapping)                │
│    - CORS origin validation (blocks cross-origin state changes)         │
│    - Host header validation (DNS rebinding protection)                  │
│                                                                          │
│  Subcomponents:                                                          │
│  ┌─────────────────────────────────────────────────────────────┐        │
│  │ FastAPI Layer (mounted first)                                │        │
│  │ - AI Gateway: /gateway/* (LLM proxy)                         │        │
│  │ - MCP Registry: /ajax-api/3.0/mlflow/mcp-servers/*           │        │
│  │ - OTel Ingestion: /v1/traces (OTLP/HTTP)                    │        │
│  │ - Job Engine: /ajax-api/3.0/jobs/*                           │        │
│  └─────────────────────────────────────────────────────────────┘        │
│  ┌─────────────────────────────────────────────────────────────┐        │
│  │ Flask Layer (via WSGIMiddleware, catch-all)                   │        │
│  │ - Tracking API: /api/2.0/mlflow/experiments|runs/*           │        │
│  │ - Model Registry: /api/2.0/mlflow/registered-models|versions│        │
│  │ - Artifact API: /api/2.0/mlflow/artifacts/*                  │        │
│  │ - GraphQL: /graphql                                          │        │
│  │ - Auth Routes: /api/2.0/mlflow/users|permissions             │        │
│  └─────────────────────────────────────────────────────────────┘        │
│         │                                                                │
│         │ Egress connections (per NetworkPolicy):                        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND STORAGE                                                         │
│                                                                          │
│  [PostgreSQL]                                                            │
│  - Port: 5432/TCP                                                        │
│  - Protocol: PostgreSQL wire protocol                                    │
│  - Encryption: TLS optional                                              │
│  - Auth: Password                                                        │
│  - Purpose: Experiment metadata, run data, model registry,              │
│             workspace store, job state, trace storage                    │
│                                                                          │
│  [MySQL] (alternative)                                                   │
│  - Port: 3306/TCP                                                        │
│  - Protocol: MySQL wire protocol                                         │
│  - Encryption: TLS optional                                              │
│  - Auth: Password                                                        │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3 / MinIO / GCS / Azure Blob]                                         │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: AWS IAM / Access Key                                            │
│  - Purpose: Artifact storage (models, datasets, files)                  │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 6443/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (ServiceAccount)                                   │
│  - Purpose: SA token validation, namespace discovery,                   │
│             workspace mapping (when workspace features enabled)         │
│                                                                          │
│  [LLM Provider APIs]                                                     │
│  - OpenAI API (api.openai.com)                                           │
│  - Anthropic API (api.anthropic.com)                                     │
│  - Google Gemini API (generativelanguage.googleapis.com)                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: API Key (Bearer Token)                                          │
│  - Purpose: AI Gateway proxy for LLM inference                          │
│                                                                          │
│  [DNS]                                                                   │
│  - Port: 53/TCP, 53/UDP                                                  │
│  - Protocol: DNS                                                         │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Name resolution                                              │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ServiceAccount: mlflow (release namespace)
- automountServiceAccountToken: false (default; opt-in for workspace features)
- ClusterRole: Configurable via Helm `cluster_rbac.extraRules`
  - API Groups: (user-defined)
  - Resources: (user-defined)
  - Verbs: (user-defined)
- Role: Configurable via Helm `namespace_rbac.extraRules`
  - Bound to: mlflow ServiceAccount
  - Namespace: Release namespace

NetworkPolicy (when enabled):
- Ingress: Allow traffic to port 5000/TCP (from configurable sources)
- Egress rules:
  - DNS: 53/TCP, 53/UDP (kube-system)
  - HTTPS: 443/TCP (S3, LLM providers)
  - Kubernetes API: 6443/TCP
  - PostgreSQL: 5432/TCP
  - MySQL: 3306/TCP

Secrets:
- mlflow-tls (kubernetes.io/tls) - TLS certificate for HTTPS server, Manual/cert-manager, No auto-rotate
- mlflow-db-secret (Opaque) - Backend store database URI, Manual, No auto-rotate
- MLFLOW_CRYPTO_KEK_PASSPHRASE (Env var / Secret) - AES-GCM-256 KEK for secrets at rest, Manual, Yes (via `mlflow crypto rotate-kek`)

Encryption at Rest:
- Secrets Manager: AES-GCM-256 encryption with PBKDF2-derived keys
- KEK rotation: Versioned, atomic, idempotent, zero-downtime
- Used for: LLM provider API keys stored by AI Gateway

Pod Security Context:
- runAsNonRoot: true
- readOnlyRootFilesystem: true
- seccompProfile: RuntimeDefault
- capabilities: drop ALL

FIPS Compliance:
- OpenSSL provided by UBI 9 base image
- cryptography>=43.0.0 uses PyCA/OpenSSL backend (FIPS-capable)
- hashlib.md5() used with usedforsecurity=False (non-security only)
- No custom TLS config in Python; TLS via uvicorn --ssl-certfile/--ssl-keyfile
