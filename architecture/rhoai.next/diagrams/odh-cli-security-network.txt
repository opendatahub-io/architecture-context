┌─────────────────────────────────────────────────────────────────────────┐
│  LOCAL EXECUTION ENVIRONMENT                                            │
│                                                                          │
│  [rhai-cli / kubectl-odh]                                                │
│  - Binary: Go CLI tool (kubectl plugin)                                  │
│  - Auth Identity: kubeconfig user (Bearer Token / Client Cert / OIDC)    │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1                   │
│  - API Throttle: QPS=50, Burst=100 (configurable)                        │
│         │                    │                    │                       │
│         │                    │                    │                       │
│    ┌────┴────┐          ┌────┴────┐          ┌────┴────┐                 │
│    │ stdin   │          │ stdout  │          │ file    │                 │
│    │ (MCP)   │          │ output  │          │ backup  │                 │
│    └────┬────┘          └─────────┘          └─────────┘                 │
│         │                                                                │
│         │ Protocol: MCP (stdio transport)                                │
│         │ Encryption: None                                               │
│         │ Auth: None                                                     │
│         ▼                                                                │
│  [AI Agent / MCP Client] (optional)                                      │
│  - 12 tools exposed via MCP                                              │
│  - Read-only / destructive annotations per tool                          │
│                                                                          │
│  [MCP SSE Server] (optional, on-demand only)                             │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP (SSE)                                                  │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Exposure: Localhost only                                              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound Connections)                                           │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  Connection 1: Kubernetes API Server                             │    │
│  │  - Destination: Kubernetes API Server (in-cluster or remote)     │    │
│  │  - Port: 6443/TCP                                                │    │
│  │  - Protocol: HTTPS (REST via client-go)                          │    │
│  │  - Encryption: TLS 1.2+                                          │    │
│  │  - Auth: Bearer Token / Client Certificate / OIDC                │    │
│  │         (from kubeconfig)                                        │    │
│  │  - Purpose: ALL cluster operations                                │    │
│  │    · Read: DSC, DSCI, CSVs, CRDs, Subscriptions, Namespaces     │    │
│  │    · Read: Nodes, Deployments, Pods, Events, ResourceQuotas      │    │
│  │    · Read: Notebooks, DSPA, InferenceService, RayCluster         │    │
│  │    · Read: TrustyAIService, GuardrailsOrchestrator, LlamaStack   │    │
│  │    · Read: AcceleratorProfile, HardwareProfile, ModelMesh         │    │
│  │    · Read: Certificate, Issuer, ServiceMeshControlPlane           │    │
│  │    · Read: PyTorchJob, TFJob, MPIJob, XGBoostJob                 │    │
│  │    · Write: DataScienceCluster (enable/disable components)       │    │
│  │    · Write: Notebook CRs (migration patches)                     │    │
│  │    · Write: DSPA CRD (stored version migration)                  │    │
│  │    · Write: Subscriptions (deps install)                         │    │
│  │    · Write: Roles, RoleBindings (RBAC migration)                 │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  Connection 2: TrustyAI Service Routes                           │    │
│  │  - Destination: TrustyAI OpenShift Routes (in-cluster)           │    │
│  │  - Port: 443/TCP                                                 │    │
│  │  - Protocol: HTTPS (REST)                                        │    │
│  │  - Encryption: TLS 1.2 (MinVersion enforced)                     │    │
│  │  - TLS Gap: InsecureSkipVerify=true (self-signed in-cluster)     │    │
│  │  - Auth: Bearer Token (from kubeconfig REST config)              │    │
│  │  - Purpose: Metrics backup/restore during TrustyAI migration     │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  Connection 3: GitHub (odh-gitops)                               │    │
│  │  - Destination: raw.githubusercontent.com                        │    │
│  │  - Port: 443/TCP                                                 │    │
│  │  - Protocol: HTTPS                                               │    │
│  │  - Encryption: TLS 1.2+                                          │    │
│  │  - Auth: None (public repository)                                │    │
│  │  - Purpose: Fetch dependency manifest on `deps --refresh`        │    │
│  │  - Pinned to: commit 1a55af0                                     │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  No custom RBAC roles defined. CLI inherits authenticated user's RBAC permissions.

  Required permissions by command:
  ┌──────────────────┬───────────────────────────────────────────────────────┐
  │ Command          │ Required Permissions                                  │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ lint             │ Read: DSC, DSCI, CRDs, CSVs, Subscriptions,          │
  │                  │       Namespaces, all workload CRs (cluster-wide)     │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ status           │ Read: Nodes, Deployments, Pods, Events,               │
  │                  │       ResourceQuotas, DSC, DSCI, CSVs                 │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ components       │ Read: DataScienceCluster                              │
  │ enable/disable   │ Patch: DataScienceCluster                             │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ backup           │ Read: all workload CRs, ConfigMaps, PVCs, Secrets     │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ deps             │ Read: Subscriptions, CSVs, PackageManifests           │
  │ deps install     │ Create/Update: Subscriptions, OperatorGroups          │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ migrate          │ Read/Write: workload CRs, CRDs, Roles,               │
  │                  │            RoleBindings, Pods, Deployments             │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ get              │ Read: requested resource types                        │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ events           │ Read: Events, Pods, Deployments                       │
  ├──────────────────┼───────────────────────────────────────────────────────┤
  │ logs             │ Read: Pods, Pod/log subresource                       │
  └──────────────────┴───────────────────────────────────────────────────────┘

Authentication & Authorization:
  ┌────────────────────────┬──────────────────────┬──────────────────────────┐
  │ Endpoint               │ Auth Mechanism        │ Enforcement Point        │
  ├────────────────────────┼──────────────────────┼──────────────────────────┤
  │ Kubernetes API         │ Bearer Token /        │ Kubernetes API Server    │
  │                        │ Client Cert / OIDC    │ (user's RBAC)            │
  ├────────────────────────┼──────────────────────┼──────────────────────────┤
  │ TrustyAI Route         │ Bearer Token          │ OpenShift Route /        │
  │                        │ (from kubeconfig)     │ TrustyAI Service         │
  ├────────────────────────┼──────────────────────┼──────────────────────────┤
  │ MCP SSE Server         │ None                  │ N/A (local access only)  │
  └────────────────────────┴──────────────────────┴──────────────────────────┘

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
  - Linking: Dynamic (CGO_ENABLED=1), OpenSSL via UBI9 base image
  - TLS: MinVersion=TLS 1.2 enforced
  - Gap: InsecureSkipVerify=true on TrustyAI metrics client
    (intentional for in-cluster self-signed certs, matches curl -sk behavior)

Secrets:
  - No secrets created or managed by the CLI
  - Backup command may export secrets when --dependencies=true is set
  - Kubeconfig credentials are used but never persisted by the tool

Build Hermeticity:
  - RPM lock: rpms.lock.yaml (aarch64, ppc64le, x86_64)
  - Go deps: go.sum
  - Gap: No Hermeto/cachi2 prefetch; go mod download during build is not fully hermetic
