┌─────────────────────────────────────────────────────────────────────────┐
│  CLIENT-SIDE (User Workstation or Container)                            │
│                                                                          │
│  [rhai-cli Binary]                                                       │
│  - Deployment: kubectl plugin / standalone binary / container image      │
│  - Build: FIPS-compliant (GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1) │
│  - Runtime: ose-cli-rhel9 base image (OpenSSL)                          │
│  - Auth source: kubeconfig (Bearer Token / Client Certificate)           │
│         │                                                                │
│         │ [MCP SSE Transport - Internal]                                 │
│         │ Port: 8080/TCP                                                 │
│         │ Bind: 127.0.0.1 (localhost ONLY)                               │
│         │ Protocol: HTTP/SSE                                             │
│         │ Encryption: None                                               │
│         │ Auth: None (network isolation)                                  │
│         │                                                                │
│  [AI Agent (e.g. Claude)]                                                │
│  - Transport: stdio (in-process) or SSE (localhost:8080)                 │
│  - Auth: None                                                            │
│  - Isolation: localhost binding only                                     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS TO CLUSTER (Trusted - TLS)                                      │
│                                                                          │
│  [rhai-cli] ──────────────────────────────────────────────────────►      │
│                                                                          │
│  Destination: Kubernetes API Server                                      │
│  - Port: 6443/TCP                                                        │
│  - Protocol: HTTPS (REST via client-go)                                  │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token / Client Certificate (from kubeconfig)             │
│  - Operations: CRD read/write, pod discovery, log streaming,             │
│                event watching, pod exec, SubjectAccessReview             │
│                                                                          │
│  Accessed CRD API Groups:                                                │
│  - datasciencecluster.opendatahub.io (DSC, DSCI)                       │
│  - serving.kserve.io (InferenceService, ServingRuntime)                  │
│  - kubeflow.org (Notebook)                                               │
│  - kueue.x-k8s.io (ClusterQueue, LocalQueue)                            │
│  - datasciencepipelinesapplications.opendatahub.io (DSPA)               │
│  - trustyai.opendatahub.io (TrustyAIService, GuardrailsOrchestrator)   │
│  - llamastack.io (LlamaStackDistribution)                                │
│  - ray.io (RayCluster, RayJob)                                          │
│  - kubeflow.org (PyTorchJob, TFJob, MPIJob)                             │
│  - maistra.io (ServiceMeshControlPlane, SMMR)                           │
│  - operator.knative.dev (KnativeServing, KnativeEventing)               │
│  - kuadrant.io / authorino.kuadrant.io                                   │
│  - cert-manager.io (Certificate)                                         │
│  - operators.coreos.com (Subscription, CSV, PackageManifest)             │
│  - config.openshift.io (ClusterVersion)                                  │
│  - route.openshift.io (Route)                                            │
│  - oauth.openshift.io (OAuthClient)                                      │
│  - dashboard.opendatahub.io (AcceleratorProfile, HardwareProfile)       │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS TO IN-CLUSTER SERVICES (Semi-Trusted)                           │
│                                                                          │
│  [rhai-cli] ──────────────────────────────────────────────────────►      │
│                                                                          │
│  Destination: TrustyAI Service (via OpenShift Route)                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS (REST API)                                            │
│  - Encryption: TLS 1.2 (InsecureSkipVerify: true)                       │
│  - Auth: Bearer Token (re-uses kubeconfig cluster token)                 │
│  - Purpose: Backup/restore scheduled fairness & drift metrics            │
│  - WARNING: Certificate validation DISABLED (self-signed route certs)    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS TO EXTERNAL SERVICES                                             │
│                                                                          │
│  [rhai-cli] ──────────────────────────────────────────────────────►      │
│                                                                          │
│  Destination: raw.githubusercontent.com (odh-gitops)                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None (public repository)                                        │
│  - Purpose: Fetch dependency manifest (values.yaml, Chart.yaml)          │
│  - Triggered by: `deps --refresh` / `deps install`                       │
│                                                                          │
│  Destination: dl.k8s.io (kubectl download)                               │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None                                                            │
│  - Purpose: Download kubectl binary (container build ONLY)               │
│                                                                          │
│  Destination: mirror.openshift.com (oc download)                         │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None                                                            │
│  - Purpose: Download oc CLI (container build ONLY)                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles/ClusterRoleBindings deployed by the CLI
- CLI operates with the authenticated user's kubeconfig permissions
- Migration framework performs SubjectAccessReview preflight checks
  before modifying resources (RHBOK migration)
- Temporary ServiceAccount/Role/RoleBinding created for InferenceService
  owner references during ModelMesh→RawDeployment migration
  (pkg/migrate/actions/modelserving/support.go)
- OLM catalog source access restricted to whitelist:
  redhat-operators, certified-operators, community-operators

Authentication Summary:
- Kubernetes API: Bearer Token / Client Certificate (kubeconfig)
- TrustyAI REST: Bearer Token (re-uses cluster token from kubeconfig)
- MCP SSE server: None (localhost 127.0.0.1 binding — network isolation)
- OLM catalog: kubeconfig (whitelist validation)

Secrets:
- No Secrets created or managed by the CLI
- Bearer tokens read from kubeconfig and bearer token files
- TrustyAI auth re-uses cluster token (no separate credential storage)

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
- Runtime: ose-cli-rhel9 base image with OpenSSL
- Crypto: stdlib crypto/tls (FIPS via OpenSSL at runtime)
- Risk: InsecureSkipVerify=true for TrustyAI route (in-cluster self-signed)
- No non-FIPS algorithms (MD5, RC4, DES) found in codebase

Build Hermeticity:
- OS packages: rpms.lock.yaml (3 architectures: aarch64, ppc64le, x86_64)
- Go deps: go.sum (locked)
- kubectl/oc: Downloaded from external URLs at build time (not hermetically locked,
  pinned to stable release channels)
- Hermeto/cachi2: Not present
