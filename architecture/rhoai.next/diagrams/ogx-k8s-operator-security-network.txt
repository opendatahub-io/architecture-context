┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / kubectl]                                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: kubeconfig / Bearer Token                                    │
│         ▼                                                                    │
│  [OCI Container Registry]                                                    │
│  - Purpose: Distribution image label fetching                                │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Anonymous or Bearer Token                                           │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                    │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: ServiceAccount Token                                                │
│         │                                                                    │
│         │ Port: 9443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (service-ca / cert-manager)                        │
│         │ Auth: mTLS (API Server → Webhook)                                  │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-applications / opendatahub)                  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────┐           │
│  │  ogx-module (Platform Module Controller)                      │           │
│  │  - Image: rhoai/odh-ogx-module-rhel9                         │           │
│  │  - ServiceAccount: controller-manager                         │           │
│  │  - Port: 8081/TCP (health, HTTP, no auth)                    │           │
│  │  - Port: 8080/TCP (metrics, HTTP, localhost only)            │           │
│  │         │                                                     │           │
│  │         │ Port: 443/TCP                                       │           │
│  │         │ Protocol: HTTPS                                     │           │
│  │         │ Encryption: TLS 1.2+                                │           │
│  │         │ Auth: ServiceAccount Token                          │           │
│  │         │ Purpose: Deploy root operator, watch OGX CR         │           │
│  │         ▼                                                     │           │
│  │  [Kubernetes API Server]                                      │           │
│  └────────────────────────────────────────────────────────────────┘           │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────┐           │
│  │  ogx-k8s-operator (Root Operator)                             │           │
│  │  - Image: rhoai/odh-ogx-operator-rhel9                       │           │
│  │  - ServiceAccount: controller-manager                         │           │
│  │  - Port: 8081/TCP (health, HTTP, no auth)                    │           │
│  │  - Port: 9443/TCP (webhook, HTTPS, TLS service-ca, mTLS)    │           │
│  │  - Port: 8080/TCP (metrics, HTTP, bound to 127.0.0.1)       │           │
│  │  │                                                            │           │
│  │  │  [kube-rbac-proxy sidecar]                                 │           │
│  │  │  - Port: 8443/TCP (metrics proxy, HTTPS, TLS)             │           │
│  │  │  - Auth: Bearer Token (TokenReview + SubjectAccessReview) │           │
│  │  │  - Proxies to: 127.0.0.1:8080                            │           │
│  └────────────────────────────────────────────────────────────────┘           │
│         │                                                                    │
│         │ Outbound connections:                                               │
│         │                                                                    │
│         ├──► OCI Registry (443/TCP, HTTPS, TLS 1.2+, Anonymous/Token)       │
│         ├──► Kubernetes API (443/TCP, HTTPS, TLS 1.2+, SA Token)            │
│         ├──► OpenShift API (443/TCP, HTTPS, TLS 1.2+, SA Token)             │
│         └──► OGX Server Pod (8321/TCP, HTTP, None, No Auth)                 │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  USER NAMESPACE (per-tenant)                                                 │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────┐           │
│  │  OGX Server Pod (managed by operator)                         │           │
│  │  - Image: {distribution-image}                                │           │
│  │  - ServiceAccount: {instance}-sa                              │           │
│  │  - Port: 8321/TCP (API, HTTP, no auth)                       │           │
│  │  - Port: 9464/TCP (metrics, HTTP, optional)                  │           │
│  │  - Config: mounted from content-hashed ConfigMap              │           │
│  │  - Secrets: injected as env vars per provider                 │           │
│  └────────────────────────────────────────────────────────────────┘           │
│                                                                              │
│  NetworkPolicy (per-OGXServer):                                              │
│  - Ingress ALLOW: same-namespace pods, operator-namespace pods              │
│  - Ingress ALLOW: OpenShift router namespace (auto-detected)                │
│  - Ingress ALLOW: monitoring namespace (if metrics port configured)         │
│  - Egress: all (default); custom rules via spec.network.egress             │
│                                                                              │
│  Services:                                                                   │
│  - {instance}-service: ClusterIP, 8321/TCP (API), 9464/TCP (metrics)       │
│                                                                              │
│  Ingress (optional, when spec.network.externalAccess.enabled=true):         │
│  - {instance}-ingress: 80/TCP, HTTP, no encryption                          │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPTIONAL EXTERNAL BACKENDS                                                  │
│                                                                              │
│  [Redis] (optional)                                                          │
│  - Port: 6379/TCP                                                            │
│  - Protocol: TCP                                                             │
│  - Encryption: Configurable                                                  │
│  - Auth: Password (from Secret)                                              │
│  - Purpose: KV storage backend (spec.storage.kv.type=redis)                 │
│                                                                              │
│  [PostgreSQL] (optional)                                                     │
│  - Port: 5432/TCP                                                            │
│  - Protocol: TCP                                                             │
│  - Encryption: Configurable                                                  │
│  - Auth: Connection string (from Secret)                                     │
│  - Purpose: SQL storage backend (spec.storage.sql.type=postgres)            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: manager-role
  - API Groups: ogx.io, core, apps, autoscaling, networking.k8s.io, policy,
                rbac.authorization.k8s.io, monitoring.coreos.com,
                apiextensions.k8s.io, config.openshift.io, security.openshift.io
  - Key Resources: ogxservers, deployments, services, configmaps, secrets,
                   persistentvolumeclaims, networkpolicies, horizontalpodautoscalers,
                   poddisruptionbudgets, servicemonitors, prometheusrules
  - Verbs: get, list, watch, create, update, patch, delete (varies per resource)

- ClusterRole: proxy-role
  - API Groups: authentication.k8s.io, authorization.k8s.io
  - Resources: tokenreviews, subjectaccessreviews
  - Verbs: create

- ClusterRole: ogxserver-editor-role (aggregated)
  - ogx.io: ogxservers, ogxservers/status
  - Verbs: create, delete, get, list, patch, update, watch

- ClusterRole: ogxserver-viewer-role (aggregated)
  - ogx.io: ogxservers, ogxservers/status
  - Verbs: get, list, watch

- Role: leader-election-role (operator namespace)
  - core: configmaps, events
  - coordination.k8s.io: leases
  - Verbs: get, list, watch, create, update, patch, delete

Webhook Configuration:
- Name: vogxserver.kb.io
- Type: Validating (fail-closed)
- Target: ogxservers (create, update)
- Port: 9443/TCP
- TLS: service-ca certificate (OpenShift) or cert-manager
- Validation rules:
  - Distribution name exists in operator's known registry
  - Provider ID uniqueness across all provider types
  - Model-to-provider reference integrity
  - Adoption annotation value != CR name (prevents name conflicts)

Secrets:
- ogx-k8s-operator-webhook-cert (kubernetes.io/tls)
  - Purpose: Webhook server TLS certificate
  - Provisioned by: OpenShift service-ca / cert-manager
  - Auto-rotate: Yes

- User-provided per-provider secrets (Opaque)
  - Purpose: API keys, credentials for inference/storage/tool providers
  - Naming: Injected as OGX_<PROVIDER_ID>_<FIELD> environment variables
  - Auto-rotate: No (user-managed)

FIPS Compliance:
- Build: CGO_ENABLED=1, GOEXPERIMENT=strictfipsruntime
- Crypto: stdlib crypto/tls with OpenSSL backend via UBI9
- OLM Annotation: features.operators.openshift.io/fips-compliant: "true"
- No non-FIPS crypto detected (no MD5/RC4/DES usage)

Multi-Tenancy:
- Tenant boundary: per-namespace (OGXServer is namespace-scoped)
- Single operator instance manages all namespaces
- Isolation: NetworkPolicy per instance, separate PVCs, namespace RBAC
- Gap: Shared PostgreSQL/Redis backends not tenant-isolated by default
