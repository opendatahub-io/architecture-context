┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / Data Scientist]                                                     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS                                                    │
│         │ Auth: User credentials (kubeconfig)                                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES API LAYER                                                        │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS                                                          │
│  - Auth: ServiceAccount token, User creds                                   │
│         │                                                                    │
│         │ Port: 9443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (service-serving-cert on OpenShift /               │
│         │              cert-manager on vanilla K8s)                          │
│         │ Auth: API server client certificate                                │
│         │ Policy: failurePolicy: Fail (fail-closed)                          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-applications)                                │
│                                                                              │
│  [OGX Operator Pod]                                                          │
│  - Image: rhoai/odh-ogx-operator-rhel9                                      │
│  - ServiceAccount: controller-manager                                        │
│  - FIPS: CGO_ENABLED=1, UBI9 base, FIPS annotation: "true"                 │
│  - TLS Profile: OpenShift cluster TLS profile (auto-detected)               │
│  - GOMEMLIMIT: 800MiB                                                       │
│                                                                              │
│    ┌─────────────────────┐  ┌─────────────────────┐                         │
│    │ Validating Webhook   │  │ Metrics Endpoint     │                        │
│    │ Port: 9443/TCP       │  │ Port: 8080/TCP       │                        │
│    │ Protocol: HTTPS      │  │ Protocol: HTTPS      │                        │
│    │ Encryption: TLS      │  │ Encryption: TLS      │                        │
│    │   (service-serving-  │  │   (OpenShift TLS     │                        │
│    │    cert / cert-mgr)  │  │    security profile) │                        │
│    │ Auth: K8s API server │  │ Auth: Bearer token   │                        │
│    │   client cert        │  │   (SA / cluster-     │                        │
│    └─────────────────────┘  │    admin)             │                        │
│                              └─────────────────────┘                         │
│    ┌─────────────────────┐                                                   │
│    │ Health Probes        │                                                   │
│    │ Port: 8081/TCP       │                                                   │
│    │ Protocol: HTTP       │                                                   │
│    │ Encryption: None     │                                                   │
│    │ Auth: None           │                                                   │
│    │ /healthz, /readyz    │                                                   │
│    └─────────────────────┘                                                   │
│         │                                                                    │
│         │ Egress flows from operator:                                         │
│         │                                                                    │
├─────────┼────────────────────────────────────────────────────────────────────┤
│  INSTANCE NAMESPACES (user namespaces)                                       │
│         │                                                                    │
│         │ Port: configurable (default 8000/TCP)                              │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (plaintext)                                       │
│         │ Auth: None                                                         │
│         │ Purpose: Status polling (/v1/providers, /v1/version)               │
│         │ Timeout: 5 seconds                                                 │
│         ▼                                                                    │
│  [OGX Server Pod]                                                            │
│  - Namespace: {user-namespace}                                               │
│  - ServiceAccount: {instance}-sa                                             │
│  - SCC: anyuid (via RoleBinding)                                             │
│  - NetworkPolicy: per-instance (configurable ingress)                        │
│                                                                              │
│    ┌─────────────────────┐  ┌─────────────────────┐                         │
│    │ {instance}-service   │  │ {instance}-ingress   │                        │
│    │ Type: ClusterIP      │  │ Type: Ingress        │                        │
│    │ Port: 8000/TCP       │  │ Port: 80/TCP         │                        │
│    │ Protocol: HTTP       │  │ Protocol: HTTP       │                        │
│    │ Encryption: None     │  │ Encryption: Depends  │                        │
│    │ Exposure: Internal   │  │   on Ingress ctrl    │                        │
│    └─────────────────────┘  │ Exposure: External   │                        │
│                              │ (optional)            │                        │
│                              └─────────────────────┘                         │
│         │                                                                    │
│         │ Egress from OGX Server pods (configured by operator):              │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (AI/ML Providers)                                         │
│                                                                              │
│  Inference Providers:                                                        │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐        │
│  │ vLLM endpoints    │  │ OpenAI API        │  │ Azure OpenAI      │        │
│  │ Port: configurable│  │ Port: 443/TCP     │  │ Port: 443/TCP     │        │
│  │ Proto: HTTP/HTTPS │  │ Proto: HTTPS      │  │ Proto: HTTPS      │        │
│  │ TLS: configurable │  │ Encrypt: TLS      │  │ Encrypt: TLS      │        │
│  │ Auth: API token   │  │ Auth: API key     │  │ Auth: API key     │        │
│  │   (optional)      │  │   (SecretRef)     │  │   (SecretRef)     │        │
│  └───────────────────┘  └───────────────────┘  └───────────────────┘        │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐        │
│  │ AWS Bedrock       │  │ Google Vertex AI  │  │ IBM Watsonx       │        │
│  │ Port: 443/TCP     │  │ Port: 443/TCP     │  │ Port: 443/TCP     │        │
│  │ Proto: HTTPS      │  │ Proto: HTTPS      │  │ Proto: HTTPS      │        │
│  │ Encrypt: TLS      │  │ Encrypt: TLS      │  │ Encrypt: TLS      │        │
│  │ Auth: AWS IAM     │  │ Auth: SA creds    │  │ Auth: API key     │        │
│  │   (SecretRef)     │  │                   │  │   (SecretRef)     │        │
│  └───────────────────┘  └───────────────────┘  └───────────────────┘        │
│                                                                              │
│  Vector I/O Providers:                                                       │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐        │
│  │ PostgreSQL        │  │ Milvus            │  │ Qdrant            │        │
│  │ (pgvector)        │  │ Port: configurable│  │ Port: configurable│        │
│  │ Port: 5432/TCP    │  │ Proto: HTTP/gRPC  │  │ Proto: HTTP/gRPC  │        │
│  │ Proto: PostgreSQL │  │ TLS: configurable │  │ TLS: configurable │        │
│  │ TLS: configurable │  │ Auth: Token       │  │ Auth: API key     │        │
│  │ Auth: Password    │  │   (optional)      │  │   (optional)      │        │
│  │   (SecretRef)     │  └───────────────────┘  └───────────────────┘        │
│  └───────────────────┘                                                       │
│                                                                              │
│  Tool Runtime & Storage:                                                     │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐        │
│  │ Brave Search API  │  │ Tavily Search API │  │ S3-compatible     │        │
│  │ Port: 443/TCP     │  │ Port: 443/TCP     │  │ Port: 443/TCP     │        │
│  │ Proto: HTTPS      │  │ Proto: HTTPS      │  │ Proto: HTTPS      │        │
│  │ Encrypt: TLS      │  │ Encrypt: TLS      │  │ Encrypt: TLS      │        │
│  │ Auth: API key     │  │ Auth: API key     │  │ Auth: AWS creds   │        │
│  │   (SecretRef)     │  │   (SecretRef)     │  │   (SecretRef)     │        │
│  └───────────────────┘  └───────────────────┘  └───────────────────┘        │
│  ┌───────────────────┐                                                       │
│  │ Docling Serve     │                                                       │
│  │ Port: configurable│                                                       │
│  │ Proto: HTTP/HTTPS │                                                       │
│  │ TLS: configurable │                                                       │
│  │ Auth: API key     │                                                       │
│  │   (optional)      │                                                       │
│  └───────────────────┘                                                       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  INFRASTRUCTURE SERVICES                                                     │
│                                                                              │
│  ┌───────────────────┐  ┌───────────────────┐                               │
│  │ OCI Registries    │  │ Prometheus / CMO  │                               │
│  │ Port: 443/TCP     │  │ Scrape: 8080/TCP  │                               │
│  │ Proto: HTTPS      │  │   (operator, HTTPS)│                              │
│  │ Encrypt: TLS      │  │ Scrape: 9464/TCP  │                               │
│  │ Auth: Registry    │  │   (OGX server,    │                               │
│  │   credentials     │  │    HTTP)           │                               │
│  │ Purpose: Image    │  │ Auth: Bearer token │                              │
│  │   label fetch     │  │ Via: ServiceMonitor│                              │
│  └───────────────────┘  └───────────────────┘                               │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: manager-role
  - ogx.io: ogxservers (get, list, watch, create, update, patch, delete)
  - ogx.io: ogxservers/status (get, patch, update)
  - ogx.io: ogxservers/finalizers (update)
  - apps: deployments (get, list, watch, create, update, patch, delete)
  - apps: replicasets (get, list, watch)
  - "": services, serviceaccounts (get, list, watch, create, update, patch, delete)
  - "": configmaps, persistentvolumeclaims (get, list, watch, create, update, patch)
  - "": secrets (get, list, watch)
  - "": pods (list)
  - networking.k8s.io: ingresses, networkpolicies (CRUD)
  - autoscaling: horizontalpodautoscalers (CRUD)
  - policy: poddisruptionbudgets (CRUD)
  - monitoring.coreos.com: servicemonitors, prometheusrules (CRUD)
  - rbac.authorization.k8s.io: clusterrolebindings (get, list, delete)
  - rbac.authorization.k8s.io: clusterroles (get, list, watch)
  - rbac.authorization.k8s.io: rolebindings (CRUD)
  - security.openshift.io: securitycontextconstraints (use)
  - config.openshift.io: apiservers (get, list, watch)
  - apiextensions.k8s.io: customresourcedefinitions (get, list, watch)

- Role: leader-election-role (namespace-scoped)
  - "": configmaps (CRUD)
  - coordination.k8s.io: leases (CRUD)
  - "": events (create, patch)

TLS Configuration:
- Webhook: service-serving-cert-signer (OpenShift) / cert-manager (vanilla K8s)
- Metrics: OpenShift TLS security profile (auto-detected from config.openshift.io/APIServer)
  - Falls back to Intermediate profile on non-OpenShift
  - SecurityProfileWatcher triggers graceful restart on profile changes
- CA Bundles: Auto-detected odh-trusted-ca-bundle ConfigMap injected into OGX server pods

Secrets:
- ogx-k8s-operator-webhook-cert (kubernetes.io/tls) - Webhook TLS cert
  - Provisioned by: service-serving-cert-signer (OpenShift) / cert-manager (K8s)
  - Auto-Rotate: Yes
- User-referenced provider secrets (Opaque) - API keys, passwords, AWS credentials
  - Provisioned by: User-managed
  - Auto-Rotate: No
  - Hash-based rollout: Operator computes hashes and stores as pod annotations
    to force restarts when credentials change

FIPS Compliance:
- Build: CGO_ENABLED=1 (Konflux), GOEXPERIMENT=strictfipsruntime (community)
- Runtime: UBI9 minimal base with OpenSSL, RHEL9 patched Go toolchain
- OLM Annotation: features.operators.openshift.io/fips-compliant: "true"
- Crypto: stdlib crypto/tls via OpenShift controller-runtime-common

NetworkPolicy:
- Per-instance NetworkPolicy with configurable ingress rules
  - Namespace labels (user-defined)
  - Monitoring namespace (Prometheus scrape)
  - OpenShift router namespace
- Egress: Not restricted by operator-created NetworkPolicy

Admission Webhooks:
- vogxserver.kb.io (validating, fail-closed)
  - Validates: distribution names, provider ID uniqueness,
    model-resource references, adoption annotation safety
  - Warns: deprecated distribution names
