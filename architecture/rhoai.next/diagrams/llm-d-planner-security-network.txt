┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User Browser]                                                              │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (edge termination at Route)                        │
│         │ Auth: None                                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (OpenShift Routes — TLS Edge Termination)                           │
│                                                                              │
│  [Route: planner]                                                            │
│  - External: 443/TCP HTTPS → Internal: 8501/TCP HTTP                        │
│  - TLS Mode: edge                                                            │
│  - Dest: Streamlit UI                                                        │
│                                                                              │
│  [Route: planner-backend]                                                    │
│  - External: 443/TCP HTTPS → Internal: 8000/TCP HTTP                        │
│  - TLS Mode: edge                                                            │
│  - Dest: Backend API                                                         │
│         │                                                                    │
│         │ Port: 8501/TCP (UI), 8000/TCP (Backend)                            │
│         │ Protocol: HTTP (plaintext after TLS termination)                   │
│         │ Encryption: None (within cluster after edge)                       │
│         │ Auth: None                                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  PLANNER NAMESPACE (Internal — No Service Mesh)                              │
│                                                                              │
│  [Streamlit UI]                                                              │
│  - Service: ui (ClusterIP)                                                   │
│  - Port: 8501/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Image: quay.io/llm-d-planner/llm-d-planner-ui:latest                    │
│  - Runs as: UID 1001 (non-root)                                             │
│  - Security: runAsNonRoot, drop ALL caps, RuntimeDefault seccomp            │
│         │                                                                    │
│         │ Port: 8000/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None                                                   │
│         │ Auth: None                                                         │
│         ▼                                                                    │
│  [Backend API (FastAPI)]                                                     │
│  - Service: backend (ClusterIP)                                              │
│  - Port: 8000/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Image: quay.io/llm-d-planner/llm-d-planner-backend:latest               │
│  - Runs as: UID 1001 (non-root)                                             │
│  - Security: runAsNonRoot, drop ALL caps, RuntimeDefault seccomp            │
│  - Auth: DB admin ops only — HMAC password (x-admin-password header)        │
│  - ALL other endpoints: NO AUTHENTICATION                                    │
│         │                              │                                     │
│         │ Port: 5432/TCP               │ Port: 11434/TCP                     │
│         │ Protocol: PostgreSQL         │ Protocol: HTTP                      │
│         │ Encryption: None             │ Encryption: None                    │
│         │ Auth: Password               │ Auth: None                          │
│         ▼                              ▼                                     │
│  [PostgreSQL]                    [Ollama]                                     │
│  - Service: postgres (ClusterIP) - Service: ollama (ClusterIP)              │
│  - Port: 5432/TCP                - Port: 11434/TCP                           │
│  - Auth: Password                - Auth: None                                │
│  - Data: Benchmark records       - Purpose: LLM intent extraction            │
│                                                                              │
│  [vLLM Simulator] (dev/test only)                                            │
│  - Port: 8080/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Auth: None                                                                │
│  - Purpose: Mock vLLM for GPU-free testing                                   │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS — External APIs                                                      │
│                                                                              │
│  Backend API ──────────────────────────────────────────────────────────────  │
│         │                                                                    │
│         ├──→ [HuggingFace Hub API — huggingface.co]                         │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: HF_TOKEN (optional Bearer)                                │
│         │    Purpose: Model metadata, config, safetensors info               │
│         │                                                                    │
│         ├──→ [OpenAI API — api.openai.com]                                  │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: API key (Bearer, OPENAI_API_KEY)                          │
│         │    Purpose: LLM intent extraction (optional provider)              │
│         │                                                                    │
│         ├──→ [Vertex AI — googleapis.com]                                   │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: GCP credentials (GCP_CREDENTIALS_JSON)                   │
│         │    Purpose: LLM intent extraction (optional provider)              │
│         │                                                                    │
│         ├──→ [RHOAI Model Catalog — in-cluster]                             │
│         │    Port: 8443/TCP                                                  │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS (service-serving CA)                            │
│         │    Auth: Bearer token (ServiceAccount mount)                       │
│         │    CA: /etc/pki/service-ca/service-ca.crt (injected)              │
│         │    Purpose: Benchmark data sync                                    │
│         │                                                                    │
│         └──→ [Kubernetes API]                                               │
│              Port: 6443/TCP                                                  │
│              Protocol: HTTPS                                                 │
│              Encryption: TLS 1.2+                                            │
│              Auth: ServiceAccount token                                      │
│              Purpose: GPU detection (node labels), InferenceService CRUD     │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
┌────────────────────────────────────────────────────────────────────────┐
│  ClusterRole: planner-gpu-reader                                       │
│  - API Group: "" (core)                                                │
│  - Resources: nodes                                                    │
│  - Verbs: list                                                         │
│                                                                        │
│  ClusterRoleBinding: planner-gpu-reader                                │
│  - Scope: cluster-wide                                                 │
│  - ServiceAccount: planner-backend (planner namespace)                 │
│                                                                        │
│  Note: The backend also performs InferenceService CRUD via kubectl,     │
│  which requires additional RBAC not explicitly defined in the          │
│  analyzed manifests (likely granted via namespace-scoped roles or      │
│  cluster-admin for demo purposes).                                     │
└────────────────────────────────────────────────────────────────────────┘

Secrets:
┌────────────────────────────────────────────────────────────────────────┐
│  planner-secrets (Opaque)                                              │
│  - DATABASE_URL: PostgreSQL connection string                          │
│  - HF_TOKEN: HuggingFace API token                                    │
│  - MODEL_CATALOG_TOKEN: Model Catalog bearer token                    │
│  - GCP_CREDENTIALS_JSON: Vertex AI service account credentials        │
│  - OPENAI_API_KEY: OpenAI API key                                      │
│  - DB_ADMIN_PASSWORD: Admin password for DB operations                 │
│  - Provisioned by: Manual (deploy-all.sh injects from env)            │
│  - Auto-Rotate: No                                                     │
│                                                                        │
│  planner-service-ca (ConfigMap — injected)                             │
│  - service-ca.crt: OpenShift service-serving CA bundle                 │
│  - Provisioned by: OpenShift service-ca operator (annotation-based)   │
│  - Auto-Rotate: Yes (auto-injected)                                    │
└────────────────────────────────────────────────────────────────────────┘

Authentication & Authorization:
┌────────────────────────────────────────────────────────────────────────┐
│  DB Admin Endpoints (/api/v1/db/upload-benchmarks, /reset, /verify):   │
│  - Auth: HMAC-verified password via x-admin-password header            │
│  - Enforcement: Application code (hmac.compare_digest)                 │
│  - Policy: Optional — only if DB_ADMIN_PASSWORD env var is set         │
│                                                                        │
│  ALL OTHER ENDPOINTS:                                                   │
│  - Auth: NONE                                                          │
│  - ⚠️  No authentication on recommendation, deployment, or cluster    │
│     management endpoints. External access protected only by            │
│     OpenShift Route TLS edge termination (no authN/authZ at Route).   │
│                                                                        │
│  Pod Security:                                                          │
│  - runAsNonRoot: true (UID 1001)                                       │
│  - allowPrivilegeEscalation: false                                     │
│  - capabilities: drop ALL                                              │
│  - seccompProfile: RuntimeDefault                                      │
└────────────────────────────────────────────────────────────────────────┘

Security Observations:
┌────────────────────────────────────────────────────────────────────────┐
│  1. No service mesh — all intra-namespace traffic is plaintext HTTP    │
│  2. No mTLS between UI → Backend or Backend → PostgreSQL/Ollama       │
│  3. Backend → PostgreSQL uses password auth but no encryption          │
│  4. API endpoints are unauthenticated (except optional DB admin)       │
│  5. Base images are python:3.14-slim (not UBI) — no FIPS compliance   │
│  6. uv.lock is gitignored — builds not reproducible from repo alone   │
│  7. Good pod security context (non-root, drop caps, seccomp)          │
│  8. Model Catalog connection uses proper TLS + SA bearer token        │
│  9. All external API egress uses TLS 1.2+                              │
└────────────────────────────────────────────────────────────────────────┘
