┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client via Gateway/Service]                                            │
│         │                                                                │
│         │ Port: 8443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+ (configurable cipher suites)              │
│         │ Auth: Bearer Token (TokenReview) or OIDC JWT or mTLS cert      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Pod Boundary)                                                  │
│                                                                          │
│  [Parent Pod Service]                                                    │
│  - Type: ClusterIP (via consuming component)                             │
│  - Port: 8443/TCP (external) → 8443/TCP (container)                    │
│  - TLS Mode: SIMPLE (server cert) or MUTUAL (with --client-ca-file)     │
│  - Cert: TLS cert/key pair (kubernetes.io/tls, cert-manager provisioned) │
│  - Cert Rotation: Hot-reload via CertReloader (no restart required)      │
│         │                                                                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SIDECAR PROXY (Trust Boundary - kube-rbac-proxy)                       │
│                                                                          │
│  [kube-rbac-proxy]                                                       │
│  - Container: sidecar within application pod                             │
│  - Listening: 8443/TCP HTTPS                                             │
│  - TLS: MinVersion TLS 1.2, configurable cipher suites                  │
│  - HTTP/2: Always enabled (ALPN: h2, http/1.1)                          │
│  - FIPS: strictfipsruntime (CGO_ENABLED=1, OpenSSL via UBI 9)           │
│                                                                          │
│  Authentication (pluggable, evaluated in order):                         │
│  1. Delegating AuthN: TokenReview against K8s API Server                │
│     - Cache: 2-minute TTL, webhook retry backoff                         │
│  2. OIDC AuthN: JWT verification against OIDC provider                  │
│     - Dynamic CA reload via DynamicFileCAContent                        │
│  3. X.509 Client Cert: CommonName extracted as identity                  │
│                                                                          │
│  Authorization (chain via union.New, evaluated in order):                │
│  1. Hardcoded Metrics Authorizer                                         │
│     - Always allows: system:serviceaccount:openshift-monitoring:         │
│       prometheus-k8s → GET /metrics                                      │
│  2. Static Authorizer (pre-configured allow-list)                       │
│  3. SAR Authorizer (SubjectAccessReview against K8s API)                │
│     - Allow cache: 5-minute TTL                                          │
│     - Deny cache: 30-second TTL                                          │
│     - Config: Format1 (resourceAttributes + rewrites) or                │
│       Format2 (path-scoped endpoint rules)                               │
│                                                                          │
│  Path Filter:                                                            │
│  - --ignore-paths: matching requests bypass authn/authz entirely         │
│                                                                          │
│  Log Sanitization:                                                       │
│  - Bearer tokens masked in TokenReview log entries                       │
│         │                                                                │
│         │ Port: 8080/TCP (or 8081/TCP)                                   │
│         │ Protocol: HTTP (or HTTPS with --upstream-ca-file)               │
│         │ Encryption: None (localhost loopback) or TLS (custom CA)        │
│         │ Auth: None (pre-authorized by kube-rbac-proxy)                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  UPSTREAM APPLICATION (Trusted - localhost only)                         │
│                                                                          │
│  [Upstream Application Container]                                        │
│  - Port: 8080/TCP or 8081/TCP                                            │
│  - Protocol: HTTP (insecure, localhost only)                              │
│  - Access: Only reachable via loopback interface                         │
│  - Auth: None required (kube-rbac-proxy pre-authorizes all requests)     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (kube-rbac-proxy outbound connections)                           │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Destination: kubernetes.default.svc                                   │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: In-cluster Service Account Token                                │
│  - Purpose: TokenReview + SubjectAccessReview API calls                  │
│  - Transport TLS: MinVersion TLS 1.2 enforced unconditionally            │
│  - ALPN: h2, http/1.1                                                    │
│                                                                          │
│  [OIDC Identity Provider] (optional, when --oidc-issuer is set)          │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None (public OIDC discovery endpoint)                           │
│  - Purpose: OIDC Discovery (.well-known) and JWKS retrieval              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  MONITORING (Inbound)                                                    │
│                                                                          │
│  [Prometheus - openshift-monitoring/prometheus-k8s]                      │
│  - Port: 8443/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (SA token, hardcoded allow)                        │
│  - Purpose: Metrics scraping at /metrics                                 │
│  - Policy: Unconditionally allowed by hardcoded metrics authorizer       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  ClusterRole: kube-rbac-proxy
    - API Group: authentication.k8s.io
      Resources: tokenreviews
      Verbs: create
    - API Group: authorization.k8s.io
      Resources: subjectaccessreviews
      Verbs: create

  ClusterRoleBinding: kube-rbac-proxy
    - Scope: cluster-scoped
    - Role: ClusterRole/kube-rbac-proxy
    - ServiceAccount: kube-rbac-proxy (in component namespace)

Authentication & Authorization Summary:
  Endpoint: /* (all proxied paths)
    - AuthN: TokenReview (delegating) | OIDC JWT | X.509 client cert
    - AuthZ: Hardcoded metrics → Static → SAR (chain)

  Endpoint: /metrics (GET)
    - AuthN: TokenReview (identifies SA)
    - AuthZ: Hardcoded ALLOW for system:serviceaccount:openshift-monitoring:prometheus-k8s

  Endpoint: --ignore-paths patterns
    - AuthN: BYPASSED
    - AuthZ: BYPASSED

Secrets:
  - TLS cert/key pair (kubernetes.io/tls) - Server TLS cert, cert-manager provisioned, hot-reload via CertReloader
  - Client CA bundle (Opaque) - Client cert authentication CA, platform operator provisioned, no auto-rotate
  - Upstream client cert (kubernetes.io/tls) - mTLS to upstream service, platform operator provisioned, no auto-rotate

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
  - Runtime: OpenSSL via UBI 9 minimal base image
  - TLS: MinVersion TLS 1.2 enforced on listener AND upstream transport
  - Crypto: stdlib crypto/tls + crypto/x509 (FIPS via OpenSSL with strictfipsruntime)
  - Validation: check-payload CI scan via GitHub Actions
  - Non-FIPS risks: None detected (no MD5/RC4/DES, no InsecureSkipVerify)
