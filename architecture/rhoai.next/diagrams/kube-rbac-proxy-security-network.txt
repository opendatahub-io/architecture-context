┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]  (browser, SDK, curl, etc.)                          │
│         │                                                                │
│         │ Port: 8443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+ (configurable via --tls-min-version)      │
│         │ Auth: Bearer Token (TokenReview) or OIDC JWT or X.509 cert     │
│         │ Cipher Suites: Configurable via --tls-cipher-suites            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SIDECAR PROXY (Authentication & Authorization Boundary)                │
│                                                                          │
│  [kube-rbac-proxy]                                                       │
│  - Deployed as: Sidecar container in component pods                      │
│  - Port: 8443/TCP (HTTPS listener)                                       │
│  - TLS: Server cert from --tls-cert-file / --tls-private-key-file        │
│  - TLS Min Version: TLS 1.2 (default, configurable)                     │
│  - Client Auth: tls.RequestClientCert (optional X.509)                   │
│  - Cert Reload: Hot-reload at --tls-reload-interval                      │
│  - FIPS: strictfipsruntime (OpenSSL via CGO)                             │
│                                                                          │
│  Authentication Chain:                                                   │
│    1. Delegating Authenticator → TokenReview API (K8s API Server)        │
│    2. OIDC Authenticator → JWT validation (when --oidc-issuer set)       │
│    3. X.509 Client Cert → CA validation (when --client-ca-file set)      │
│                                                                          │
│  Authorization Chain (union, first-match):                               │
│    1. Hardcoded Metrics Authorizer                                       │
│       → ALLOW: system:serviceaccount:openshift-monitoring:prometheus-k8s │
│         on GET /metrics (non-resource URL)                               │
│    2. Static Authorizer (pre-configured rules)                           │
│    3. SAR Authorizer → SubjectAccessReview API (K8s API Server)          │
│       → Format1: top-level resourceAttributes + rewrites                 │
│       → Format2: path-scoped endpoint rules                              │
│                                                                          │
│  Security Features:                                                      │
│    - Log Sanitizer: masks bearer tokens in TokenReview log entries       │
│    - --ignore-paths: bypasses authn/authz entirely (use with caution)    │
│    - --allow-paths: restricts proxied paths (404 for non-matching)       │
│         │                                                                │
│         │ Port: 8081/TCP (configurable via --upstream)                    │
│         │ Protocol: HTTP (or HTTPS with --upstream-ca-file)              │
│         │ Encryption: Plaintext (localhost loopback)                      │
│         │   or TLS with --upstream-ca-file + --upstream-client-cert-file  │
│         │ Auth: None (or mTLS with --upstream-client-cert-file)          │
│         │ Headers: X-Remote-User, X-Remote-Groups (injected)             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  UPSTREAM APPLICATION (Same Pod - localhost)                             │
│                                                                          │
│  [Component Application Container]                                       │
│  - Examples: Notebook Server, Model Server, Dashboard Backend            │
│  - Port: 8081/TCP (configurable)                                         │
│  - Protocol: HTTP (typically)                                            │
│  - Trust: Implicitly trusted (same pod network namespace)                │
│  - Receives: Authenticated user identity via X-Remote-User headers       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘


EGRESS CONNECTIONS FROM kube-rbac-proxy:

┌─────────────────────────────────────────────────────────────────────────┐
│  KUBERNETES CONTROL PLANE                                                │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Service Account Token (in-cluster) or kubeconfig                │
│  - APIs Called:                                                          │
│    - POST /apis/authentication.k8s.io/v1/tokenreviews                   │
│    - POST /apis/authorization.k8s.io/v1/subjectaccessreviews            │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL IDENTITY PROVIDER (Optional)                                   │
│                                                                          │
│  [OIDC Issuer]                                                           │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None                                                            │
│  - Purpose: Fetch OIDC discovery document (.well-known/openid-config)    │
│             and JWKS (JSON Web Key Set) for JWT validation               │
│  - Only active when: --oidc-issuer is configured                         │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘


INBOUND CONNECTIONS TO kube-rbac-proxy:

┌─────────────────────────────────────────────────────────────────────────┐
│  MONITORING                                                              │
│                                                                          │
│  [prometheus-k8s]                                                        │
│  - Namespace: openshift-monitoring                                       │
│  - ServiceAccount: prometheus-k8s                                        │
│  - Port: 8443/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (SA token)                                         │
│  - Authorization: Hardcoded ALLOW (no SAR round-trip)                    │
│  - Path: GET /metrics                                                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘


HEALTH CHECK ENDPOINT (Separate Port):

┌─────────────────────────────────────────────────────────────────────────┐
│  [/healthz Endpoint]                                                     │
│  - Port: configurable via --proxy-endpoints-port                         │
│  - Protocol: HTTPS (shares main server TLS config)                       │
│  - Auth: None                                                            │
│  - Response: "ok"                                                        │
│  - Purpose: Liveness/readiness probes                                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘


RBAC Summary:

  ClusterRole: kube-rbac-proxy
  ┌────────────────────────────────────────────────────────────────────┐
  │ API Group                    │ Resources              │ Verbs     │
  ├──────────────────────────────┼────────────────────────┼───────────┤
  │ authentication.k8s.io        │ tokenreviews           │ create    │
  │ authorization.k8s.io         │ subjectaccessreviews   │ create    │
  └────────────────────────────────────────────────────────────────────┘

  ClusterRoleBinding: kube-rbac-proxy
  - Scope: cluster-scoped
  - Role: ClusterRole/kube-rbac-proxy
  - ServiceAccount: kube-rbac-proxy (per-namespace, set by deploying operator)


Secrets:

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │ Secret                        │ Type              │ Provisioned By    │ Rotate │
  ├───────────────────────────────┼───────────────────┼───────────────────┼────────┤
  │ TLS cert/key                  │ kubernetes.io/tls │ cert-manager /    │ Yes    │
  │ (--tls-cert-file,             │                   │ operator          │ (hot-  │
  │  --tls-private-key-file)      │                   │                   │ reload)│
  ├───────────────────────────────┼───────────────────┼───────────────────┼────────┤
  │ Upstream client cert          │ kubernetes.io/tls │ Operator          │ No     │
  │ (--upstream-client-cert-file) │                   │                   │(restart│
  │                               │                   │                   │needed) │
  ├───────────────────────────────┼───────────────────┼───────────────────┼────────┤
  │ OIDC CA file                  │ Opaque (CA)       │ Manual / operator │ No     │
  │ (--oidc-ca-file)              │                   │                   │        │
  ├───────────────────────────────┼───────────────────┼───────────────────┼────────┤
  │ Client CA file                │ Opaque (CA)       │ Manual / operator │ Yes    │
  │ (--client-ca-file)            │                   │                   │(dynamic│
  │                               │                   │                   │reload) │
  └──────────────────────────────────────────────────────────────────────────────┘


FIPS Compliance:

  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
  - Crypto: stdlib crypto/tls + crypto/x509 via OpenSSL (strictfipsruntime)
  - TLS: MinVersion TLS 1.2+ enforced on all connections (server + upstream)
  - No non-FIPS algorithms detected (no MD5, RC4, DES, InsecureSkipVerify)
  - Base image: ubi9/ubi-minimal (OpenSSL inherited from UBI)


Deployment Context:

  - Injected by: rhods-operator (gateway controller) as sidecar container
  - Replaces: oauth-proxy sidecar from RHOAI 2.x
  - Traffic routing: HTTPRoute resources route to Service fronting 8443/TCP
  - No standalone Ingress/Route (relies on parent component's exposure)
