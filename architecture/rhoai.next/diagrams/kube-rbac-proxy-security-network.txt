┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client (via Platform Gateway)]                                         │
│         │                                                                │
│         │ Port: 8443/TCP                                                 │
│         │ Protocol: HTTPS (HTTP/2, HTTP/1.1)                             │
│         │ Encryption: TLS 1.2+ (configurable via --tls-min-version)      │
│         │ Auth: Bearer Token / Client TLS Certificate / OIDC JWT         │
│         │ ALPN: h2, http/1.1                                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SIDECAR PROXY (kube-rbac-proxy)                                        │
│                                                                          │
│  [kube-rbac-proxy]                                                       │
│  - Deployment: Sidecar container (injected by rhods-operator)            │
│  - Listen: 0.0.0.0:8443 (--secure-listen-address)                       │
│  - TLS: Server cert/key (--tls-cert-file, --tls-private-key-file)       │
│  - TLS Min Version: TLS 1.2 (configurable)                              │
│  - Client Auth: tls.RequestClientCert (optional client cert)             │
│  - Cipher Suites: Configurable via k8sapiflag.TLSCipherSuites           │
│  - Cert Reload: CertReloader polls every 1m (--tls-reload-interval)     │
│  - Token Sanitization: SanitizingFilter masks bearer tokens in logs      │
│                                                                          │
│  Authentication Chain:                                                   │
│  1. Bearer Token → TokenReview API (authentication.k8s.io/v1)           │
│  2. Client TLS Certificate → CN extraction (--client-ca-file)           │
│  3. OIDC JWT → OIDC Discovery + JWKS validation (--oidc-issuer)         │
│                                                                          │
│  Authorization Chain:                                                    │
│  1. Hardcoded Metrics: prometheus-k8s SA + GET /metrics → ALLOW          │
│  2. Static Authorizer: configurable allow-list                           │
│  3. SAR Authorizer: SubjectAccessReview (authorization.k8s.io/v1)        │
│     - Format1: top-level resourceAttributes (simple)                     │
│     - Format2: path-scoped endpoints with per-method rules (fine-grained)│
│         │                                                                │
│         │ Port: 8080/TCP (configurable, typically localhost)              │
│         │ Protocol: HTTP (or HTTPS with --upstream-ca-file)              │
│         │ Encryption: None (localhost) or TLS 1.2+ (upstream CA)         │
│         │ Auth: None (or mTLS via --upstream-client-cert-file)           │
│         │ Headers: x-remote-user, x-remote-groups (injected)             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION CONTAINER (Upstream - same pod)                             │
│                                                                          │
│  [Component Application]                                                 │
│  - Namespace: {component-namespace}                                      │
│  - Listens on: 127.0.0.1:8080 (or configurable)                        │
│  - Purpose: Protected application service                                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES (Trusted - in-cluster)                                 │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount token (in-cluster credentials)                   │
│  - Purpose: TokenReview + SubjectAccessReview API calls                  │
│  - SAR Caching: allow 5m, deny 30s                                       │
│                                                                          │
│  [OpenShift Monitoring Prometheus]                                       │
│  - Namespace: openshift-monitoring                                       │
│  - SA: system:serviceaccount:openshift-monitoring:prometheus-k8s         │
│  - Port: 8443/TCP (inbound to kube-rbac-proxy)                          │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (SA token)                                         │
│  - Purpose: Metrics scraping (/metrics) - hardcoded allow                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (if configured)                                       │
│                                                                          │
│  [OIDC Identity Provider (--oidc-issuer)]                                │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS (configurable CA via --oidc-ca-file)                  │
│  - Auth: None (public OIDC discovery endpoint)                           │
│  - Purpose: OIDC Discovery (.well-known) and JWKS key retrieval          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: kube-rbac-proxy (name set by deploying operator)
  - API Groups: authentication.k8s.io
  - Resources: tokenreviews
  - Verbs: create
  - API Groups: authorization.k8s.io
  - Resources: subjectaccessreviews
  - Verbs: create

Hardcoded Authorization:
- ServiceAccount: system:serviceaccount:openshift-monitoring:prometheus-k8s
  - Allowed: GET /metrics (unconditional, no SAR check)
  - Purpose: OpenShift monitoring out-of-the-box
  - Status: Planned for removal (TODO: replace with static authorizer config via CMO)

Secrets:
- TLS serving cert/key (kubernetes.io/tls)
  - Provisioned by: Deploying operator / cert-manager
  - Auto-rotate: Yes (CertReloader, 1m poll interval)
  - Flags: --tls-cert-file, --tls-private-key-file

- Client CA bundle (Opaque)
  - Provisioned by: Deploying operator
  - Auto-rotate: Yes (DynamicFileCAContent)
  - Flag: --client-ca-file

- OIDC CA bundle (Opaque)
  - Provisioned by: Deploying operator
  - Auto-rotate: Yes (DynamicFileCAContent)
  - Flag: --oidc-ca-file

- Upstream client cert/key (kubernetes.io/tls)
  - Provisioned by: Deploying operator
  - Auto-rotate: No (loaded once at startup)
  - Flags: --upstream-client-cert-file, --upstream-client-key-file

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
- Base image: registry.redhat.io/ubi9/ubi-minimal (OpenSSL present)
- TLS: stdlib crypto/tls (FIPS via OpenSSL with strictfipsruntime)
- Cipher suites: Configurable via k8sapiflag.TLSCipherSuites (k8s component-base)
- CI validation: .github/workflows/fips-compliance.yml (check-payload scan)
- Non-FIPS crypto: None detected (no md5, rc4, des usage)
