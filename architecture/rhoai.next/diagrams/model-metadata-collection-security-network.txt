┌─────────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME PIPELINE (CI/CD Environment)                                    │
│                                                                             │
│  [model-extractor CLI]                                                      │
│  - Binary: model-extractor (Go, CGO_ENABLED=0)                             │
│  - Runs: CI/CD pipeline only (not shipped in image)                         │
│  - No network listeners                                                     │
│         │                                                                   │
│         │ EGRESS ONLY (no ingress)                                          │
│         │                                                                   │
│         ├──────────────────────────────────────────────────────────────────┐ │
│         │ Port: 443/TCP                                                   │ │
│         │ Protocol: HTTPS                                                 │ │
│         │ Encryption: TLS 1.2+                                            │ │
│         │ Auth: Bearer Token (HF_TOKEN, optional)                         │ │
│         │ Destination: huggingface.co                                     │ │
│         │ Purpose: Fetch model collections, details, tags, READMEs,       │ │
│         │          YAML frontmatter for metadata enrichment               │ │
│         │ Graceful degradation: --skip-huggingface / --skip-enrichment    │ │
│         ├──────────────────────────────────────────────────────────────────┘ │
│         │                                                                   │
│         ├──────────────────────────────────────────────────────────────────┐ │
│         │ Port: 443/TCP                                                   │ │
│         │ Protocol: HTTPS                                                 │ │
│         │ Encryption: TLS 1.2+                                            │ │
│         │ Auth: Bearer Token (GITHUB_TOKEN, optional)                     │ │
│         │ Destination: api.github.com, raw.githubusercontent.com          │ │
│         │ Purpose: Validate branches, fetch agent.yaml and README.md      │ │
│         │ Graceful degradation: --skip-agent-enrichment                   │ │
│         ├──────────────────────────────────────────────────────────────────┘ │
│         │                                                                   │
│         ├──────────────────────────────────────────────────────────────────┐ │
│         │ Port: 443/TCP                                                   │ │
│         │ Protocol: HTTPS (Docker Registry v2)                            │ │
│         │ Encryption: TLS 1.2+                                            │ │
│         │ Auth: None (public images)                                      │ │
│         │ Destination: registry.redhat.io                                 │ │
│         │ Purpose: Pull modelcar image manifests, layers, config blobs    │ │
│         │ Retry: Exponential backoff for transient failures               │ │
│         ├──────────────────────────────────────────────────────────────────┘ │
│         │                                                                   │
│         ▼                                                                   │
│  [Output: data/*.yaml]                                                      │
│  - models-catalog.yaml                                                      │
│  - validated-models-catalog.yaml                                            │
│  - *-mcp-servers-catalog.yaml                                               │
│  - redhat-agents-catalog.yaml                                               │
│  - enrichment.yaml (provenance tracking)                                    │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  CONTAINER IMAGE (Runtime - No Network Activity)                            │
│                                                                             │
│  [Data Container Image]                                                     │
│  - Base: UBI9-minimal (multi-arch: x86_64, ppc64le, s390x, arm64)          │
│  - Entrypoint: sleep infinity                                               │
│  - Volume: /app/data (read-only catalog YAML files)                         │
│  - No Go binaries (static data only)                                        │
│  - No network listeners                                                     │
│  - No egress                                                                │
│  - No ingress                                                               │
│         │                                                                   │
│         │ Volume Mount (no network)                                         │
│         │ Protocol: Kubernetes Volume                                       │
│         │ Auth: Pod-level access control                                    │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  CONSUMER                                                                   │
│                                                                             │
│  [RHOAI Dashboard]                                                          │
│  - Mounts /app/data from data container                                     │
│  - Reads catalog YAML files                                                 │
│  - Displays model, MCP server, and agent metadata to users                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC roles or bindings -- this is a data container, not a Kubernetes controller.
- No ServiceAccount with elevated permissions.
- Container runs as non-root (UBI9-minimal defaults).

Service Mesh Configuration:
- Not applicable -- no network services exposed.
- No PeerAuthentication or AuthorizationPolicy required.

Secrets:
- HF_TOKEN (Opaque, env var) - HuggingFace API auth, optional, CI-time only, manual provisioning, no auto-rotate
- GITHUB_TOKEN (Opaque, env var) - GitHub API auth, optional, CI-time only, manual provisioning, no auto-rotate
- git_auth_secret (kubernetes.io/basic-auth) - Tekton PipelineRun git auth, Konflux/PipelinesAsCode, no auto-rotate

Security Notes:
- All secrets are used during CI/CD pipeline execution only, NOT at container runtime.
- The shipped container image contains NO secrets, NO Go binaries, only static YAML data files.
- FIPS: Go binaries are CGO_ENABLED=0 (would fail check-payload), but no Go binaries are shipped in the image.
- Build hermeticity: Konflux PipelineRun sets hermetic=true with gomod prefetch.
- TLS: Default Go net/http client, no custom TLS configuration, system trust store.
