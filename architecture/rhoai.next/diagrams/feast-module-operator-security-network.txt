┌─────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted)                                              │
│                                                                          │
│  [ODH Platform Operator]                                                 │
│  - Namespace: redhat-ods-operator (RHOAI) / opendatahub (ODH)           │
│  - ServiceAccount: opendatahub-operator                                  │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Bearer Token                              │
│         │ Action: Creates FeastOperator CR (singleton: default-feastop.) │
│         │         Writes platformVersion to ConfigMap                     │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES API SERVER                                                   │
│                                                                          │
│  [kube-apiserver]                                                        │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount Bearer Tokens                                    │
│         │                                                                │
│         │ Watch events (FeastOperator CR, ConfigMap)                      │
│         │ Resource CRUD (Deployments, RBAC, CRDs, Services, etc.)        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE                                                      │
│  (redhat-ods-applications [RHOAI] / opendatahub [ODH])                  │
│                                                                          │
│  [feast-module-operator Pod]                                             │
│  - ServiceAccount: opendatahub-feast-operator                            │
│  - Image: feast-module-operator controller                               │
│  - Resources: 500m CPU / 512Mi Memory (limits)                          │
│  - Init Container: copy-manifests (copies kustomize to emptyDir)        │
│                                                                          │
│     Exposed Ports:                                                       │
│     ┌──────────────────────────────────────────────────────────┐        │
│     │ 8081/TCP  - HTTP  - No encryption - No auth             │        │
│     │   /healthz (liveness probe)                              │        │
│     │   /readyz  (readiness probe)                             │        │
│     ├──────────────────────────────────────────────────────────┤        │
│     │ 8443/TCP  - HTTPS - TLS          - Bearer Token (SA)    │        │
│     │   /metrics (Prometheus scrape)                           │        │
│     └──────────────────────────────────────────────────────────┘        │
│                                                                          │
│  [opendatahub-feast-metrics-service]                                     │
│  - Type: ClusterIP                                                       │
│  - Port: 8443/TCP → 8443/TCP                                           │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS                                                       │
│  - Auth: Bearer Token (ServiceAccount)                                   │
│  - Exposure: Internal only                                               │
│         ▲                                                                │
│         │ Port: 8443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS                                                │
│         │ Auth: Bearer Token (ServiceAccount)                            │
│         │                                                                │
│  [Prometheus / ServiceMonitor]                                           │
│  - Scrapes metrics via ServiceMonitor resource                           │
│  - Namespace label selector: metrics: enabled (NetworkPolicy)            │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  DEPLOYED WORKLOAD (by feast-module-operator)                            │
│                                                                          │
│  [feast-operator controller-manager] (Deployment)                        │
│  - Deployed via kustomize manifests (ODH or RHOAI overlay)              │
│  - Manages FeatureStore CRs (feast.dev/v1)                              │
│  - RBAC: Dedicated ClusterRole with CRUD on feast.dev, core, apps, etc. │
│                                                                          │
│  [FeatureStore CRD] (feast.dev/v1, v1alpha1)                            │
│  - Deployed by module operator                                           │
│  - Managed by the deployed feast-operator                                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK TRAFFIC SUMMARY:

  Egress (from feast-module-operator):
  ┌──────────────────────┬──────────┬──────────┬──────────────┬─────────────────────────┐
  │ Destination          │ Port     │ Protocol │ Encryption   │ Auth                    │
  ├──────────────────────┼──────────┼──────────┼──────────────┼─────────────────────────┤
  │ Kubernetes API server│ 443/TCP  │ HTTPS    │ TLS 1.2+     │ ServiceAccount Token    │
  └──────────────────────┴──────────┴──────────┴──────────────┴─────────────────────────┘

  Ingress (to feast-module-operator):
  ┌──────────────────────┬──────────┬──────────┬──────────────┬─────────────────────────┐
  │ Source               │ Port     │ Protocol │ Encryption   │ Auth                    │
  ├──────────────────────┼──────────┼──────────┼──────────────┼─────────────────────────┤
  │ kubelet (probes)     │ 8081/TCP │ HTTP     │ None         │ None                    │
  │ Prometheus           │ 8443/TCP │ HTTPS    │ TLS          │ Bearer Token (SA)       │
  └──────────────────────┴──────────┴──────────┴──────────────┴─────────────────────────┘

  No external ingress (no Ingress/Route resources).
  No egress NetworkPolicy defined (potential gap).

RBAC Summary:

  ServiceAccount: opendatahub-feast-operator
  Namespace: opendatahub-feast-system (operator) / cluster-wide (manager role)

  ClusterRole: opendatahub-feast-manager-role
  - components.platform.opendatahub.io: feastoperators (full CRUD + status/finalizers)
  - apps: deployments (full CRUD)
  - core: services, serviceaccounts, configmaps (full CRUD)
  - core: secrets, namespaces (get, list, watch, patch, update)
  - core: pods, pods/exec (get, list, watch, create)
  - core: persistentvolumeclaims (get, list, watch, create, delete, update)
  - core: events (create, patch)
  - rbac.authorization.k8s.io: clusterroles, clusterrolebindings, roles, rolebindings (full CRUD)
  - rbac.authorization.k8s.io: subjectaccessreviews (get, list, watch, create, update, delete)
  - apiextensions.k8s.io: customresourcedefinitions (full CRUD)
  - feast.dev: featurestores + status/finalizers (full CRUD)
  - kubeflow.org: notebooks (get, list, watch)
  - batch: jobs, cronjobs (full CRUD)
  - monitoring.coreos.com: servicemonitors (full CRUD)
  - route.openshift.io: routes (get, list, watch, create, delete, update)
  - autoscaling: horizontalpodautoscalers (full CRUD)
  - policy: poddisruptionbudgets (full CRUD)
  - coordination.k8s.io: leases (full CRUD)
  - authentication.k8s.io: tokenreviews (create)
  - authorization.k8s.io: subjectaccessreviews (create)
  - non-resource URL: /metrics (get)

  ClusterRole: opendatahub-feast-leader-election-role (namespace-scoped binding)
  ClusterRole: opendatahub-feast-metrics-auth-role

NetworkPolicy:
  - Ingress rule restricts /metrics access to pods from namespaces with label metrics: enabled
  - No egress NetworkPolicy defined

Secrets:
  - No secrets defined by the operator itself

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
  - Runtime: stdlib crypto/tls via OpenSSL (FIPS mode from UBI 9 base)
  - No custom TLS configuration; uses controller-runtime defaults
  - No non-FIPS crypto risks detected
