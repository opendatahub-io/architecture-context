# Component: vllm-orchestrator-gateway

## Metadata

- **Repository**: https://github.com/red-hat-data-services/vllm-orchestrator-gateway
- **Version**: 6f85bfe (main)
- **Distribution**: RHOAI
- **Languages**: Rust 1.86.0
- **Deployment Type**: Service
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Downstream | https://github.com/red-hat-data-services/vllm-orchestrator-gateway | -- | main | -- | local_analysis |

_No upstream or midstream repositories were detected. The repository has no sync workflows and no fork indicators. It appears to be an internally-originated downstream component within `red-hat-data-services`._

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

_No aliases detected._

## Purpose

**Short**: An HTTP gateway that provides multiple OpenAI-compatible chat completion endpoints with configurable detector-based content filtering, routing requests to the FMS Guardrails Orchestrator.

**Detailed**: The vllm-orchestrator-gateway is a Rust-based HTTP reverse proxy that sits between clients and the FMS Guardrails Orchestrator. It enables controlled interaction with LLM inference services by enforcing configurable detector pipelines on chat completion requests. Each configured route exposes a unique `/{route-name}/v1/chat/completions` endpoint that maps to a specific set of content detectors (e.g., PII detection, regex-based filters).

When a request arrives, the gateway injects the route's detector configuration into the payload and forwards it to the orchestrator's `/api/v2/chat/completions-detection` endpoint. If the orchestrator reports a detection (e.g., PII found in output), the gateway replaces the response with a configurable fallback message. The gateway supports both streaming (SSE) and non-streaming chat completions, and it supports mTLS for secure communication with the orchestrator using OpenShift service-serving certificates.

The gateway is designed as a drop-in replacement for direct chat completion endpoints, allowing operators to add guardrails to any vLLM-served model without modifying client code. It is part of the TrustyAI ecosystem within RHOAI, working alongside the FMS Guardrails Orchestrator and various detector services.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| vllm-orchestrator-gateway | Rust Service (axum) | HTTP reverse proxy that routes OpenAI-compatible chat completion requests through configurable detector pipelines via the FMS Guardrails Orchestrator |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_No CRDs. This is a stateless HTTP service, not an operator._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/{route-name}/v1/chat/completions` | POST | 8090/TCP | HTTP | None (plaintext) | Pass-through (forwards `Authorization` header) | OpenAI-compatible chat completion with detector-based filtering; one endpoint per configured route |

_Routes are dynamically generated from the `routes` section of `config.yaml`. Example routes: `/pii/v1/chat/completions`, `/passthrough/v1/chat/completions`. Streaming is determined by the `stream` boolean in the JSON payload._

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

_No gRPC services._

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| axum | 0.7.9 | Yes | HTTP framework for routing and request handling |
| reqwest | 0.12.12 | Yes | HTTP client for orchestrator communication (with native-tls and streaming support) |
| tokio | 1.41.1 | Yes | Async runtime (multi-threaded) |
| serde / serde_json | 1.0.215 / 1.0.133 | Yes | JSON serialization/deserialization for OpenAI-compatible request/response payloads |
| serde_yml | 0.0.12 | Yes | YAML config file parsing |
| tower-http | 0.6.2 | Yes | HTTP tracing middleware |
| openssl | 0.10.73 | Yes | TLS certificate handling (PKCS12 identity construction for mTLS) |
| native-tls | 0.2.12 | Yes | TLS backend for reqwest HTTP client |
| futures | 0.3.30 | Yes | Stream combinators for SSE streaming responses |
| regex | 1.11.1 | Yes | Regular expression support (used by detector configurations) |
| tracing / tracing-subscriber | 0.1.41 / 0.3.19 | Yes | Structured logging |
| anyhow | 1.0.94 | Yes | Error handling |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| FMS Guardrails Orchestrator | REST API (HTTP/HTTPS) | Backend service that performs LLM inference with detector-based content filtering |
| Detector services (e.g., guardrails-regex-detector) | Indirect (via orchestrator) | Content detection services configured in the orchestrator; the gateway references detector names but does not call them directly |
| vLLM inference server | Indirect (via orchestrator) | LLM serving backend that the orchestrator calls for chat completions |

## Deployment Manifests

_No kustomize manifests, Helm charts, or Kubernetes deployment YAML files are present in this repository. The component is deployed via its container image; deployment configuration is managed externally (likely by the platform operator or a separate deployment repository)._

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| vllm-orchestrator-gateway | N/A (no Service manifest in repo) | 8090/TCP | 8090 | HTTP | None (plaintext listener) | None (pass-through of `Authorization` header) | Internal (binds 0.0.0.0) |

_The gateway listens on port 8090/TCP by default (configurable via `HTTP_PORT` env var). The listener is plaintext HTTP — TLS termination is expected to happen at the ingress/service mesh layer. The gateway's mTLS capability is for its outbound connection to the orchestrator, not for its own listener._

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

_No ingress resources defined in this repository. Ingress is managed externally._

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| FMS Guardrails Orchestrator | 8085/TCP (default, configurable) | HTTP or HTTPS | Optional mTLS (when `/etc/tls/private/tls.crt` and `/etc/tls/private/tls.key` are present) | Client certificate (mTLS) + forwarded `Authorization` header | Forward chat completion requests with injected detector configuration to `/api/v2/chat/completions-detection` |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_No RBAC manifests. This is a stateless HTTP service without Kubernetes API access._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

_No role bindings defined._

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| TLS client cert (`/etc/tls/private/tls.crt`, `/etc/tls/private/tls.key`) | kubernetes.io/tls | mTLS client identity for outbound orchestrator communication | OpenShift service-serving-cert-signer (expected) | Yes (auto-rotated by OpenShift) |
| CA certificate (`/etc/tls/ca/service-ca.crt`) | ConfigMap (projected) | Custom CA for validating orchestrator's server certificate | OpenShift service-ca-operator (expected) | Yes |

_Secrets are consumed via well-known OpenShift service-serving certificate paths. The gateway checks for their presence at startup and falls back to plaintext HTTP if they are not mounted._

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| `/{route}/v1/chat/completions` | POST | Pass-through (`Authorization` header forwarded to orchestrator) | FMS Guardrails Orchestrator (downstream) | No authentication enforced at the gateway level; the gateway forwards `Authorization` and `X-Forwarded-*` headers to the orchestrator |

_The gateway itself performs no authentication or authorization. It is a transparent proxy for auth headers. Security depends on network-level isolation (e.g., NetworkPolicy) and the orchestrator's own auth enforcement._

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A (Rust binary, not Go) | Dockerfile.konflux |
| **Linking** | Dynamic (links against `openssl-libs` at runtime) | Dockerfile.konflux:48-51 |
| **OpenSSL in image** | Yes — explicitly installed (`openssl-libs` via `microdnf install`) | Dockerfile.konflux:48 |
| **OLM FIPS annotation** | N/A (no OLM bundle; this is a service, not an operator) | -- |

_check-payload primarily targets Go binaries. For Rust binaries, the relevant check is that the binary dynamically links against system OpenSSL rather than bundling its own crypto. The `openssl` crate (v0.10.73) in `Cargo.toml` links against the system `libssl`/`libcrypto` provided by `openssl-libs` in the UBI9 base image. The `native-tls` crate uses the openssl backend on Linux, ensuring system OpenSSL is used for TLS operations._

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | Uses `openssl` crate for PKCS12 identity construction; `native-tls` for TLS connections via reqwest; custom CA certificate loading | src/main.rs:351-406 |
| **Crypto libraries** | `openssl` 0.10.73 (FFI to system OpenSSL), `native-tls` 0.2.12 (wraps system OpenSSL on Linux) | Cargo.toml:19-20 |
| **Certificate handling** | Loads CA cert from `/etc/tls/ca/service-ca.crt`, client cert/key from `/etc/tls/private/tls.{crt,key}`; constructs PKCS12 identity for mTLS | src/main.rs:359-396 |
| **Non-FIPS crypto risks** | `danger_accept_invalid_hostnames(true)` when orchestrator host is `localhost` — development convenience that bypasses hostname verification; no non-FIPS cipher suites detected; no MD5/RC4/DES usage | src/main.rs:373 |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | Yes | rpm-lockfile-prototype | rpms.lock.yaml (302KB, covers x86_64, aarch64, ppc64le, s390x) |
| **Language deps** | Cargo.lock | Yes | cargo | Cargo.lock (1834 lines) |
| **Artifacts** | not found | No | Hermeto | N/A |
| **Hermeto prefetch** | Tekton PipelineRun configures `prefetch-input` with `[{"type":"cargo","path":"."},{"type":"rpm","path":"."}]` | Yes | Hermeto (formerly cachi2) | .tekton/odh-trustyai-vllm-orchestrator-gateway-pull-request.yaml:43 |

_The build is fully hermetic. The Tekton PipelineRun sets `hermetic: "true"` and prefetches both Cargo (Rust crate) and RPM dependencies. The `Dockerfile.konflux` copies `.cargo/config.toml` which configures Cargo to use the prefetched vendor directory. The `rpms.in.yaml` specifies RPM dependencies for lockfile generation across four architectures._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | N/A | Inferred — no tenant-scoping logic in source code |
| **Deployment model** | Single-tenant (one gateway instance per configuration) | src/main.rs:58-136 |
| **Tenant identifier** | N/A | No tenant concept in code |

_The gateway is a stateless HTTP proxy with no tenant awareness. It serves all configured routes to any caller that can reach it over the network. Multi-tenancy, if required, would be achieved by deploying separate gateway instances per tenant or by relying on the upstream orchestrator's tenant isolation._

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | None at gateway level; `Authorization` header forwarded to orchestrator | Orchestrator / upstream infrastructure | Gateway performs no authentication — any network-reachable client can send requests |
| Data storage | N/A — stateless service, no persistent storage | -- | No risk — no data stored |
| Network traffic | None defined in this repo | External (NetworkPolicy, service mesh expected) | No NetworkPolicy shipped; relies on deployment environment for network isolation |
| Compute & resources | None defined in this repo | External (ResourceQuota/LimitRange expected) | No resource limits in repo |
| Configuration & secrets | Single config file shared across all routes | Application | All routes share the same detector configuration and orchestrator endpoint; no per-tenant config |
| API scoping | Routes are config-driven but not tenant-scoped | Application | All configured routes are exposed to all callers |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| FMS Guardrails Orchestrator | None — single shared backend | All gateway routes forward to the same orchestrator instance; no per-tenant routing |

## Data Flows

### Flow 1: Non-Streaming Chat Completion with Detection

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | vllm-orchestrator-gateway | 8090/TCP | HTTP | None (plaintext) | Authorization header (pass-through) |
| 2 | vllm-orchestrator-gateway | FMS Guardrails Orchestrator | 8085/TCP (configurable) | HTTP or HTTPS | Optional mTLS | Client cert + forwarded Authorization header |
| 3 | FMS Guardrails Orchestrator | vllm-orchestrator-gateway | -- | HTTP/HTTPS response | Same as request | -- |
| 4 | vllm-orchestrator-gateway | Client | -- | HTTP response | None | -- |

_At step 2, the gateway injects detector configuration (input/output detectors with parameters) into the payload before forwarding to the orchestrator's `/api/v2/chat/completions-detection` endpoint. At step 3, if detections are found in the response and a `fallback_message` is configured for the route, the gateway replaces the response content with the fallback message._

### Flow 2: Streaming Chat Completion with Detection

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | vllm-orchestrator-gateway | 8090/TCP | HTTP | None (plaintext) | Authorization header (pass-through) |
| 2 | vllm-orchestrator-gateway | FMS Guardrails Orchestrator | 8085/TCP (configurable) | HTTP or HTTPS | Optional mTLS | Client cert + forwarded Authorization header |
| 3 | FMS Guardrails Orchestrator | vllm-orchestrator-gateway | -- | SSE stream (chunked) | Same as request | -- |
| 4 | vllm-orchestrator-gateway | Client | -- | SSE stream (Server-Sent Events) | None | -- |

_The streaming flow uses SSE (Server-Sent Events) with `KeepAlive`. The gateway parses each SSE chunk, checks for detections, and replaces the content with the fallback message on the specific chunk where a detection is found._

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| FMS Guardrails Orchestrator | REST API (POST) | 8085/TCP (configurable) | HTTP or HTTPS | Optional mTLS (client cert from `/etc/tls/private/`) | Forward chat completions with detector injection to `/api/v2/chat/completions-detection` |
| OpenShift service-serving-cert-signer | Certificate provisioning | -- | -- | -- | Provides TLS client certificate and key at `/etc/tls/private/` for mTLS with orchestrator |
| OpenShift service-ca-operator | CA bundle provisioning | -- | -- | -- | Provides CA certificate at `/etc/tls/ca/service-ca.crt` for validating orchestrator's server certificate |
| Detector services (e.g., guardrails-regex-detector) | Indirect (configured by name in `config.yaml`, called by orchestrator) | -- | -- | -- | Content detectors referenced in gateway config; the orchestrator dispatches to them |
| vLLM inference server | Indirect (orchestrator calls it) | -- | -- | -- | LLM backend for generating chat completions |

## Architectural Analysis

The vllm-orchestrator-gateway is architecturally simple by design — a stateless Rust HTTP proxy with a single responsibility: route-specific detector injection. Its entire codebase fits in three source files (~370 lines of application logic excluding tests), making it highly maintainable but also limited in scope.

**Configuration-driven routing**: The gateway's core pattern is elegant — each route in `config.yaml` generates a distinct `/{name}/v1/chat/completions` endpoint backed by a specific set of detectors. This allows operators to expose multiple guardrailed endpoints from a single gateway instance, each with different safety policies (e.g., `/pii/v1/chat/completions` with PII detection vs. `/passthrough/v1/chat/completions` with no detectors). The route configuration also supports per-route `fallback_message` strings, enabling different user-facing error messages when detections trigger.

**mTLS implementation**: The TLS code in `build_orchestrator_client()` (main.rs:351-406) demonstrates a carefully layered approach — it checks for the presence of service-serving certificates at well-known OpenShift paths and gracefully falls back to plaintext HTTP if they're absent. The `danger_accept_invalid_hostnames(true)` call when the orchestrator host is `localhost` is a development convenience that should be noted in security reviews but does not affect production deployments where the hostname will match the certificate's SAN. The PKCS12 identity construction is necessary because the `native-tls` crate requires DER-encoded PKCS12 bundles rather than separate PEM files.

**Security posture gap**: The gateway performs no authentication or authorization itself — it simply forwards `Authorization` and `X-Forwarded-*` headers. This means it relies entirely on network-level isolation (NetworkPolicy, service mesh) and the orchestrator's own auth enforcement. No RBAC manifests, ServiceAccount definitions, or admission webhook configurations are shipped with this repo, which is appropriate for a simple proxy but means the deployment must be carefully secured externally.

**Streaming architecture**: The streaming implementation (main.rs:259-349) processes orchestrator SSE responses chunk by chunk, parsing each for detection results. When a detection triggers on a streaming chunk, it replaces the content in that single chunk and sets `finish_reason: "stop"`, effectively truncating the stream. This is a pragmatic approach but means partial content may have already been streamed to the client before a detection is found in a later chunk — the gateway cannot retroactively redact earlier chunks.

**Build maturity**: The Konflux build pipeline is well-configured with full hermeticity (prefetched Cargo crates and RPMs, `rpms.lock.yaml` covering four architectures). The multi-arch build targets x86_64, arm64, ppc64le, and s390x. The repository has Tier 1 testing (unit tests, clippy, formatting) via GitHub Actions and Trivy filesystem security scanning.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 13d7736 | 2026-06-22 | Regenerate RPM lock files |
| 08bd684 | 2026-06-22 | Fix UBI repo IDs to match conforma known_rpm_repositories list |
| 66f734d | 2026-06-12 | Build image hermetically (added .cargo/config.toml, rpms.lock.yaml, rpms.in.yaml, ubi.repo) |
| d826d9a | 2026-04-15 | Update Rust toolchain to 1.86.0 |
| 2375d33 | 2025-10-09 | Add support for streaming chat completions |
| a7b9a3e | 2025-10-08 | Update config validation for efficiency, report all issues together |
| e8bd4f9 | 2025-10-07 | Add default orchestrator config and allow multiple detector definitions of same server |
| 4475981 | 2025-09-26 | Choose HTTP/HTTPS scheme based on presence of TLS certs |
| 91c4269 | 2025-09-24 | Add TLS support and passthrough headers |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| Cargo.toml | 1-25 | Metadata, Dependencies, Architecture Components |
| Dockerfile.konflux | 1-62 | Architecture Components, Security (FIPS, Build Hermeticity), Deployment |
| Dockerfile | 1-49 | Architecture Components (comparison with Konflux variant) |
| src/main.rs | 1-548 | APIs Exposed, Network Architecture, Security, Data Flows, Integration Points, Architectural Analysis |
| src/api.rs | 1-110 | APIs Exposed, Data Flows |
| src/config.rs | 1-62 | APIs Exposed, Dependencies, Architectural Analysis |
| config/config.yaml | 1-19 | APIs Exposed, Integration Points, Data Flows |
| README.md | 1-152 | Purpose, APIs Exposed, Data Flows |
| rust-toolchain.toml | 1-3 | Metadata |
| rpms.in.yaml | 1-20 | Security (Build Hermeticity) |
| .tekton/odh-trustyai-vllm-orchestrator-gateway-pull-request.yaml | 1-72 | Security (Build Hermeticity), Architecture Components |
| .tekton/README.md | 1-40 | Provenance |
| .github/workflows/tests.yaml | 1-54 | Recent Changes, Architectural Analysis |
| .github/workflows/security-scan.yaml | 1-39 | Security |
| .gitignore | 1-6 | Security (Build Hermeticity — Hermeto references) |
| .dockerignore | 1-4 | Security (Build Hermeticity) |
| curl.sh | 1-19 | APIs Exposed, Data Flows |
| ubi.repo | 1-28 | Security (Build Hermeticity) |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `ring\|rustls\|openssl` in `*.toml` | Cargo.toml | Security (FIPS, crypto libraries) |
| `tls\|TLS\|InsecureSkipVerify\|danger_accept_invalid` in `*.rs` | src/main.rs | Security (FIPS Application-Level Crypto) |
| `cachi2\|hermeto\|REMOTE_SOURCES` in Dockerfile* | (none matched in Dockerfiles) | Security (Build Hermeticity) |
| `sync\|upstream\|fork` in `.github/workflows/` | (no matches) | Provenance |
| `Dockerfile*konflux*` file search | Dockerfile.konflux | Architecture Components |
| `rpms.lock.yaml` file search | rpms.lock.yaml | Security (Build Hermeticity) |
| `Cargo.lock` file search | Cargo.lock | Security (Build Hermeticity) |

### Summary

- **Total files read**: 18
- **Total lines referenced**: ~1100
- **Coverage**: All sections have direct source backing. Provenance is based on local analysis (no component-map.json available). Multi-tenancy finding of "N/A" is based on absence of tenant-scoping code across all source files. Deployment Manifests section notes absence — no manifests exist in this repository.

---
*Generated in 3m 55s (236s total)*
