# Component: model-metadata-collection

## Metadata

- **Repository**: https://github.com/red-hat-data-services/model-metadata-collection
- **Version**: 57ec532
- **Distribution**: RHOAI
- **Languages**: Go
- **Deployment Type**: Data Container (init container / sidecar volume source)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/opendatahub-io/model-metadata-collection | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/model-metadata-collection | auto_merge | main | `sync-branch-stable.yml`, `sync-branch-stable2x.yml` | local_analysis |

_The `sync-branch-stable.yml` workflow (line 1-34) syncs `main` to `stable` in the upstream opendatahub-io org. The downstream red-hat-data-services fork mirrors the upstream. The sync workflows only trigger when `github.repository == 'opendatahub-io/model-metadata-collection'` (line 11), confirming the upstream/downstream relationship._

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: CLI tool and data container that collects, enriches, and catalogs AI model metadata, MCP server metadata, and agent metadata from OCI registries, HuggingFace, and GitHub for consumption by the RHOAI dashboard.

**Detailed**: The model-metadata-collection component is a Go-based metadata pipeline that produces YAML catalog files describing Red Hat AI models, MCP servers, and agent starter kits. It consists of two CLI binaries -- `model-extractor` (the primary pipeline) and `metadata-report` (a reporting tool) -- along with a data-only container image built by Konflux.

The `model-extractor` binary fetches OCI container manifests from registries (e.g., `registry.redhat.io`), extracts modelcard content from annotated OCI layers (`io.opendatahub.modelcar.layer.type: modelcard`), parses YAML frontmatter and markdown to extract structured metadata (name, provider, license, tasks, languages), then enriches this data by cross-referencing with HuggingFace APIs (model details, README frontmatter, collection indices) and OCI registry metadata (architectures, timestamps). The enriched metadata is written to `data/` as YAML catalogs (`models-catalog.yaml`, `mcp-servers-catalog.yaml`, `agents-catalog.yaml`, etc.), which are then baked into a minimal UBI9 container image via the Konflux Dockerfile.

The resulting container image is consumed as an init container or volume mount by the RHOAI dashboard and other platform components, providing a pre-built catalog of available AI models, MCP servers, and agent definitions without requiring runtime API calls.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| model-extractor | Go CLI | Primary pipeline: extracts modelcard metadata from OCI registries, enriches with HuggingFace data, generates model/MCP/agent catalogs |
| metadata-report | Go CLI | Generates metadata completeness reports (markdown + YAML) from catalog output |
| odh-model-metadata-collection | Data Container (UBI9-minimal) | Serves pre-generated YAML catalogs and benchmark data as a volume mount for platform consumers |

## Sub-Component Details

### model-extractor

**Dockerfile**: N/A (not containerized; runs in CI/CD to produce catalog data)
**Intent**: The primary data pipeline CLI. It extracts model metadata from OCI container registries, enriches it with HuggingFace API data, generates MCP server catalogs from index files, and produces agent catalogs from GitHub repositories. Runs during CI/CD to generate the `data/*.yaml` catalog files that are then copied into the container image.
**Language**: Go
**Port**: N/A (CLI tool, no network listener)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| registry.redhat.io | HTTPS | 443/TCP | Registry auth | Fetch OCI manifests and modelcard layers |
| quay.io | HTTPS | 443/TCP | Registry auth | Fetch OCI image architectures and timestamps |
| huggingface.co/api | HTTPS | 443/TCP | Bearer Token (HF_TOKEN, optional) | Fetch model details, collections, README content |
| raw.githubusercontent.com | HTTPS | 443/TCP | Bearer Token (GITHUB_TOKEN, optional) | Fetch agent.yaml and README.md for agent catalog |
| api.github.com | HTTPS | 443/TCP | Bearer Token (GITHUB_TOKEN, optional) | Validate branches, resolve commit SHAs |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `--input` | flag | `data/models-index.yaml` | Path to models index YAML file |
| `--output-dir` | flag | `output` | Output directory for extracted metadata |
| `--catalog-output` | flag | `data/models-catalog.yaml` | Path for the generated models catalog |
| `--max-concurrent` | flag | `5` | Maximum concurrent model processing jobs |
| `--skip-huggingface` | flag | `false` | Skip HuggingFace collection processing |
| `--skip-enrichment` | flag | `false` | Skip metadata enrichment from HuggingFace |
| `--skip-catalog` | flag | `false` | Skip catalog generation |
| `--mcp-index` | flag | `` | Path to MCP servers index YAML file |
| `--mcp-catalog-output` | flag | `data/redhat-mcp-servers-catalog.yaml` | Path for MCP servers catalog |
| `--skip-mcp-enrichment` | flag | `false` | Skip MCP server OCI image enrichment |
| `--agent-index` | flag | `` | Path to agents index YAML file |
| `--agent-catalog-output` | flag | `data/redhat-agents-catalog.yaml` | Path for agents catalog |
| `--agent-branch` | flag | `` | Override GitHub branch for agent metadata |
| `--skip-agent-enrichment` | flag | `false` | Skip fetching agent metadata from GitHub |
| `HF_TOKEN` | env | `` | HuggingFace API token for authenticated requests |
| `GITHUB_TOKEN` | env | `` | GitHub API token for agent metadata fetching |

### metadata-report

**Dockerfile**: N/A (not containerized; runs locally for reporting)
**Intent**: Generates metadata completeness reports from the model catalog and extracted output. Produces both human-readable markdown and machine-readable YAML reports tracking field-level data source provenance (modelcard YAML, regex parsing, HuggingFace API/tags, registry).
**Language**: Go
**Port**: N/A (CLI tool, no network listener)

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `--catalog` | flag | `data/models-catalog.yaml` | Path to the models catalog YAML file |
| `--output-dir` | flag | `output` | Directory containing model extraction output |
| `--report-dir` | flag | `` | Directory to write reports (defaults to output-dir) |

### odh-model-metadata-collection (container)

**Dockerfile**: `Dockerfile.konflux`
**Intent**: A minimal data-only container that ships pre-generated YAML catalogs and benchmark data. Deployed as an init container or sidecar volume mount by platform components (e.g., RHOAI dashboard) to provide AI model, MCP server, and agent catalog data without requiring runtime API calls. The default `CMD` is `sleep infinity` for persistent data access, but in Kubernetes deployments, the consuming pod typically overrides the command to copy data from the volume mount.
**Image**: `quay.io/rhoai/pull-request-pipelines:odh-model-metadata-collection-*` (PR), `quay.io/opendatahub/odh-model-metadata-collection` (release)
**Language**: N/A (data container, no application code)
**Port**: None

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|

_No HTTP endpoints -- this is a CLI tool and data container, not a service._

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| containers/image/v5 | v5.36.1 | Yes | OCI container image interaction (manifest, layers, config) |
| golang.org/x/text | v0.28.0 | Yes | Unicode text processing for model name normalization |
| gopkg.in/yaml.v3 | v3.0.1 | Yes | YAML parsing and generation for catalog files |
| HuggingFace API | N/A | No (optional enrichment) | Model metadata, collections, README content |
| GitHub API | N/A | No (optional enrichment) | Agent metadata and README content |
| OCI Registries (registry.redhat.io, quay.io) | N/A | Yes (for model extraction) | Container image manifests, modelcard layers, architecture metadata |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| RHOAI Dashboard | Volume mount (data consumer) | Consumes catalog YAML files for model/MCP/agent display |
| Konflux CI/CD | Build pipeline | Builds container image via `Dockerfile.konflux` |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|

_No Kubernetes services -- this is a data container, not a running service._

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| registry.redhat.io | 443/TCP | HTTPS | TLS 1.2+ | Registry credentials | Fetch OCI manifests and modelcard layer blobs |
| quay.io | 443/TCP | HTTPS | TLS 1.2+ | Registry credentials | Fetch OCI image architectures and timestamps |
| huggingface.co | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (HF_TOKEN, optional) | HuggingFace API calls for model details, collections, READMEs |
| api.github.com | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (GITHUB_TOKEN, optional) | GitHub API for branch validation, commit SHA resolution |
| raw.githubusercontent.com | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (GITHUB_TOKEN, optional) | Fetch agent.yaml and README.md for agent catalog |

_Egress is only from the `model-extractor` CLI during CI/CD pipeline execution. The container image itself has no runtime egress._

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_No Kubernetes RBAC -- this is a data container with no API access requirements._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| HF_TOKEN | Environment variable | HuggingFace API authentication for collection and model detail fetching | Manual (developer-provided) | No |
| GITHUB_TOKEN | Environment variable | GitHub API authentication for agent metadata fetching | Manual / CI secret | No |
| git_auth_secret | Kubernetes Secret | Git authentication for Konflux pipeline | Konflux / PipelinesAsCode | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|

_No exposed endpoints requiring auth. API tokens for HuggingFace and GitHub are optional CLI parameters used during CI/CD._

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | `CGO_ENABLED=0` (static linking, no FIPS) | Makefile:61, Makefile:218 |
| **Linking** | Static (CGO disabled) | Makefile:61 (`CGO_ENABLED=0 GOOS=linux GOARCH=amd64`) |
| **OpenSSL in image** | Yes (via UBI9-minimal base) | Dockerfile.konflux:3 (`registry.access.redhat.com/ubi9-minimal:latest`) |
| **OLM FIPS annotation** | Not present | N/A (not an OLM-managed operator) |

_The `model-extractor` Go binary is built with `CGO_ENABLED=0` (static linking), which would fail check-payload validation if the binary were shipped in the container image. However, the Konflux Dockerfile does NOT include the Go binary -- it only copies pre-generated `data/*.yaml` files. The container image contains no Go binaries, only static YAML files and SVG assets, so check-payload FIPS validation is not applicable to the shipped artifact._

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | Default Go `net/http` TLS (via `containers/image` library) | cmd/model-extractor/main.go:21 (docker import), internal/huggingface/client.go:22 (http.Client) |
| **Crypto libraries** | Standard Go `net/http` via `containers/image/v5` for OCI registry TLS | go.mod:8 |
| **Certificate handling** | System trust store (default Go behavior) | Inherited from `net/http` defaults |
| **Non-FIPS crypto risks** | None detected -- no custom cipher suites, no MD5/RC4/DES usage | Grep across all `.go` files |

_The component uses standard Go `net/http` for all HTTPS calls (HuggingFace API, GitHub API, OCI registries). All TLS is handled by the Go standard library using system certificates. Since the shipped container image contains only static data files (no executables), there is no runtime crypto behavior to evaluate._

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (main branch; likely present on downstream release branches) |
| **Language deps** | go.sum | Yes | go mod | `go.sum` |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | Not present in Dockerfile.konflux | No (in Dockerfile) | Hermeto (formerly cachi2) | N/A |

_The Tekton PipelineRun (`.tekton/odh-model-metadata-collection-pull-reques.yaml`, lines 41-44) specifies `hermetic: true` and `prefetch-input: {"type": "gomod", "path": "."}`, indicating that Hermeto/cachi2 prefetch is used at the Konflux pipeline level for Go module dependencies, even though the `Dockerfile.konflux` itself does not reference cachi2. The `go.sum` file provides Go dependency pinning. RPM lock files are absent on the `main` branch, which is expected for this data-only container since the Dockerfile performs no RPM installs beyond the UBI9-minimal base._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | N/A | CLI tool and data container; no tenant context |
| **Deployment model** | Single-tenant (shared data container across platform) | Dockerfile.konflux:1-53 |
| **Tenant identifier** | N/A | No tenant differentiation |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | N/A | N/A | Not applicable -- static data served via volume mount |
| Data storage | Shared YAML catalogs in container filesystem | Container image immutability | All tenants see the same model/MCP/agent catalog |
| Network traffic | N/A | N/A | No network listener; data is filesystem-only |
| Compute & resources | N/A | N/A | Init container with minimal resource usage |
| Configuration & secrets | N/A | N/A | No per-tenant configuration |
| API scoping | N/A | N/A | No API; data consumed via filesystem |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|

## Data Flows

### Flow 1: Model Metadata Collection Pipeline (CI/CD)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | model-extractor CLI | HuggingFace Collections API | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (HF_TOKEN) |
| 2 | model-extractor CLI | OCI Registry (registry.redhat.io) | 443/TCP | HTTPS | TLS 1.2+ | Registry credentials |
| 3 | model-extractor CLI | HuggingFace Model API | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (HF_TOKEN) |
| 4 | model-extractor CLI | HuggingFace README (raw) | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (HF_TOKEN) |
| 5 | model-extractor CLI | OCI Registry (quay.io) | 443/TCP | HTTPS | TLS 1.2+ | Registry credentials |
| 6 | model-extractor CLI | GitHub API | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (GITHUB_TOKEN) |
| 7 | model-extractor CLI | GitHub Raw Content | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (GITHUB_TOKEN) |
| 8 | model-extractor CLI | Local filesystem (data/*.yaml) | N/A | File I/O | N/A | N/A |

### Flow 2: Container Image Build (Konflux)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Konflux pipeline | Git repository | 443/TCP | HTTPS | TLS 1.2+ | Git credentials |
| 2 | Konflux pipeline | Container build (Dockerfile.konflux) | N/A | Local build | N/A | N/A |
| 3 | Container build | quay.io image registry | 443/TCP | HTTPS | TLS 1.2+ | Registry push credentials |

### Flow 3: Catalog Data Consumption (Runtime)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Container image (init container) | Pod volume mount (/app/data) | N/A | Filesystem | N/A | N/A |
| 2 | RHOAI Dashboard | Volume mount (/app/data/*.yaml) | N/A | Filesystem | N/A | N/A |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| HuggingFace API | REST (outbound) | 443/TCP | HTTPS | TLS 1.2+ | Fetch model collections, model details, and README content for metadata enrichment |
| OCI Registries (registry.redhat.io, quay.io) | OCI Distribution API (outbound) | 443/TCP | HTTPS | TLS 1.2+ | Fetch container manifests, modelcard layers, image configs, architecture metadata |
| GitHub API | REST (outbound) | 443/TCP | HTTPS | TLS 1.2+ | Validate branches, fetch agent.yaml and README.md for agent catalog |
| RHOAI Dashboard | Volume mount (consumer) | N/A | Filesystem | N/A | Consumes models-catalog.yaml, mcp-servers-catalog.yaml, agents-catalog.yaml |
| Konflux CI/CD | Build pipeline | N/A | Tekton PipelineRun | N/A | Builds multi-arch container image from Dockerfile.konflux |
| Red Hat AI Modelcars | OCI image content | 443/TCP | HTTPS | TLS 1.2+ | Modelcar OCI images contain annotated layers with modelcard.md content |

## Architectural Analysis

The model-metadata-collection component follows a "build-time enrichment, runtime static" architecture pattern. All expensive operations -- registry fetches, API calls to HuggingFace and GitHub, metadata parsing, and enrichment -- happen during the CI/CD pipeline execution. The resulting container image is intentionally inert: a UBI9-minimal base with static YAML files and no executable code. This design eliminates runtime dependencies on external services, making the catalog data available in air-gapped environments.

The metadata enrichment pipeline implements a sophisticated multi-source provenance system. Each metadata field (name, provider, description, license, etc.) tracks its data source through a `MetadataSource` type with `Value` and `Source` fields. Sources are hierarchically prioritized: `huggingface.yaml` (YAML frontmatter from HuggingFace README) takes precedence over `huggingface.api` (API response), which beats `modelcard.yaml` (container modelcard frontmatter), which beats `modelcard.regex` (regex text extraction). This provenance tracking is written to separate `enrichment.yaml` files alongside each model's `metadata.yaml`, enabling the `metadata-report` tool to generate data quality audits with per-field source attribution.

The concurrent processing model is straightforward: `processModelsInParallelWithMetadata` uses a semaphore-based goroutine pool (default 5 workers) to process models in parallel. Each worker fetches an OCI manifest, extracts modelcard layers, and writes metadata independently. The enrichment phase runs sequentially after extraction is complete, cross-referencing each registry model against HuggingFace data using a fuzzy string similarity matcher (`CalculateSimilarity`) with a 0.5 threshold, filtered by model family to prevent cross-family matches (e.g., llama container matching a granite HuggingFace entry).

The Konflux build pipeline (`.tekton/odh-model-metadata-collection-pull-reques.yaml`) builds multi-architecture images for x86_64, ppc64le, s390x, and arm64. The build uses hermetic mode with gomod prefetch, even though the `Dockerfile.konflux` only copies static data files. This ensures that any future changes that add Go binaries to the container will automatically get hermetic dependency resolution. The Dockerfile.konflux uses `ubi9-minimal` as the base, which is significantly smaller than the development Dockerfile's `ubi9-micro`, and copies all `data/*.yaml` files rather than listing them individually -- a pragmatic choice for a rapidly evolving catalog that frequently adds new file types.

The component supports three distinct catalog types -- models, MCP servers, and agents -- each with its own index format, enrichment pipeline, and output structure. The model catalog is the most complex, with OCI registry interaction, HuggingFace enrichment, tool-calling config extraction, vLLM recommended configuration injection, and model deduplication/merging. The MCP catalog focuses on OCI image architecture and timestamp enrichment. The agent catalog fetches structured metadata from GitHub repositories (agent.yaml files) and enriches with README content and template artifacts.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| e95c8cd | 2026-07 | Fix: sanitize HTML img tags with relative src in agent READMEs |
| 5499241 | 2026-07 | Fix: move 10 models without benchmark data from validated to redhat ai catalog |
| 74ca408 | 2026-07 | Update: community/partner MCP catalog entries to latest versions |
| fd6f60e | 2026-07 | Fix: align OCI v-prefixed versions with HuggingFace dot-notation |
| f36e19e | 2026-07 | Feature: add readmePath override for agent README locations |
| c343431 | 2026-07 | Feature: add labels, logo, and template artifacts for agents |
| 179dd47 | 2026-07 | Feature: add agent starter kit metadata pipeline |
| 7f89aea | 2026-06 | Fix: remove duplicate Nemotron FP8 entry causing catalog loader error |
| 3fe6378 | 2026-06 | Feature: add supportTier to MCP custom properties |
| e69b114 | 2026-06 | Feature: add EA2 models to Other models catalog for RHOAI 3.5 EA2 |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| go.mod | 1-41 | Metadata, Dependencies |
| Dockerfile.konflux | 1-43 | Architecture Components, Sub-Component Details, Security |
| Dockerfile | 1-62 | Architecture Components, Sub-Component Details |
| Makefile | 1-299 | Architecture Components, Sub-Component Details, Security (FIPS) |
| cmd/model-extractor/main.go | 1-934 | Purpose, Architecture Components, Sub-Component Details, Data Flows, Integration Points |
| cmd/metadata-report/main.go | 1-117 | Sub-Component Details |
| internal/catalog/catalog.go | 1-705 | Purpose, Architecture Components |
| internal/catalog/mcp_catalog.go | 1-138 | Architecture Components, Sub-Component Details |
| internal/catalog/agent_catalog.go | 1-277 | Architecture Components, Sub-Component Details, Integration Points |
| internal/catalog/mcp_enrichment.go | 1-226 | Architecture Components, Integration Points |
| internal/config/config.go | 1-75 | Architecture Components |
| internal/config/model_families.go | 1-80 | Architectural Analysis |
| internal/config/vllmconfig.go | 1-78 | Architecture Components, Architectural Analysis |
| internal/enrichment/enrichment.go | 1-704 | Purpose, Data Flows, Architectural Analysis |
| internal/enrichment/update.go | 1-461 | Architectural Analysis |
| internal/huggingface/client.go | 1-425 | Integration Points, Egress, Data Flows |
| internal/huggingface/collections.go | 1-283 | Data Flows, Integration Points |
| internal/huggingface/tags.go | 1-218 | Architectural Analysis |
| internal/metadata/parser.go | 1-512 | Architectural Analysis |
| internal/metadata/migration.go | 1-177 | Architectural Analysis |
| internal/github/client.go | 1-187 | Integration Points, Egress |
| internal/registry/registry.go | 1-397 | Integration Points, Egress, Data Flows |
| internal/report/metadata_report.go | 1-653 | Sub-Component Details |
| pkg/types/types.go | 1-328 | Architecture Components |
| pkg/types/agent.go | 1-92 | Architecture Components |
| pkg/types/mcpserver.go | 1-69 | Architecture Components |
| pkg/types/toolcalling.go | 1-117 | Architectural Analysis |
| pkg/types/vllmconfig.go | 1-93 | Architectural Analysis |
| pkg/utils/validation.go | 1-124 | Architectural Analysis |
| .env.example | 1-5 | Security (Secrets) |
| .tekton/odh-model-metadata-collection-pull-reques.yaml | 1-75 | Build Hermeticity, Architectural Analysis |
| .github/workflows/sync-branch-stable.yml | 1-34 | Provenance |
| scripts/update-collections.sh | 1-16 | Architecture Components |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` in Dockerfile*/Makefile | Makefile (2 matches) | Security (FIPS) |
| `fips-compliant\|fips\.enabled\|FIPS` in *.yaml/*.json | No matches | Security (FIPS) |
| `boring\|boringcrypto\|openssl\|crypto/tls\|CipherSuites` in *.go | No matches | Security (FIPS, Application-Level Crypto) |
| `rpms.lock.yaml` | No matches | Build Hermeticity |
| `go.sum\|uv.lock\|package-lock.json` etc. | go.sum | Build Hermeticity |
| `cachi2\|hermeto\|REMOTE_SOURCES` in Dockerfile* | No matches | Build Hermeticity |
| `Dockerfile*konflux*\|Containerfile*konflux*` | Dockerfile.konflux | Architecture Components |
| `sync*.yaml\|sync*.yml` in .github/workflows | 2 files | Provenance |

### Summary

- **Total files read**: 33
- **Total lines referenced**: ~7,500
- **Coverage**: All sections have direct source backing. Provenance detection used `local_analysis` (no component-map.json available). Multi-tenancy documented as N/A based on component type (data container with no tenant context). FIPS analysis confirmed no Go binaries in the shipped container image, making check-payload validation not applicable to the deployed artifact.

---
*Generated in 6m 12s (372s total)*
