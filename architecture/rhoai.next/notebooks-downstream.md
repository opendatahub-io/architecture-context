# Component: notebooks-downstream

## Metadata

- **Repository**: https://github.com/red-hat-data-services/notebooks-downstream
- **Version**: 8f013d48 (main)
- **Distribution**: RHOAI
- **Languages**: Python, Go (build tooling), Bash, R (RStudio images)
- **Deployment Type**: Container Images (Workbenches, Runtimes)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/opendatahub-io/notebooks | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/notebooks-downstream | sync_workflow | main | `sync-branches-through-pr.yml` | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Container image repository that builds and maintains workbench (Jupyter, Code Server, RStudio) and pipeline runtime images for Red Hat OpenShift AI.

**Detailed**: The notebooks-downstream repository is a downstream fork of the upstream opendatahub-io/notebooks project. It produces approximately 35 distinct container images that serve as interactive data science environments (workbenches) and pipeline execution environments (runtimes) within the RHOAI platform. Workbench images include Jupyter Notebook (minimal, data science, PyTorch, TensorFlow, TrustyAI variants), VS Code-based Code Server, and RStudio Server. Runtime images provide stripped-down execution environments for Elyra and Kubeflow Pipelines (KFP).

Each image variant is built for multiple accelerator backends (CPU, NVIDIA CUDA 12.6, AMD ROCm 6.2.4) and Python versions (3.11, 3.12), producing a matrix of ~35 images. The repository uses OpenShift ImageStream resources to manage 6 historical versions (N through N-5) per image type, enabling users to select specific software versions. Images are built by Konflux via Tekton pipelines and published to quay.io registries. The ODH Notebook Controller (from the odh-notebook-controller component) is responsible for launching these images as Kubernetes Pods when users create Notebook CRs.

The build process uses a sandboxed Makefile system with a Go-based `buildinputs` tool that analyzes Dockerfile dependencies to create minimal build contexts. Dependency management is handled via Pipenv (Pipfile.lock) with micropipenv for deterministic in-container installation. Image digests and commit hashes are tracked in `params.env` and `commit.env` files, injected into ImageStream definitions via Kustomize replacements.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| Jupyter Minimal | Container Image | Lightweight JupyterLab workbench with minimal Python data science packages |
| Jupyter DataScience | Container Image | Full-featured JupyterLab with scikit-learn, pandas, numpy, boto3, kubeflow, and database connectors |
| Jupyter PyTorch | Container Image | JupyterLab with PyTorch 2.6 deep learning framework (CUDA) |
| Jupyter TensorFlow | Container Image | JupyterLab with TensorFlow deep learning framework (CUDA) |
| Jupyter TrustyAI | Container Image | JupyterLab with TrustyAI explainability and Java 17 runtime |
| Jupyter ROCm PyTorch | Container Image | JupyterLab with PyTorch for AMD ROCm GPUs |
| Jupyter ROCm TensorFlow | Container Image | JupyterLab with TensorFlow for AMD ROCm GPUs |
| Code Server | Container Image | VS Code (code-server v4.98.0) browser-based IDE with NGINX reverse proxy and data science packages |
| RStudio (c9s) | Container Image | RStudio Server 2024.12.1 on CentOS Stream 9 with R 4.4.3 |
| RStudio (RHEL9) | Container Image | RStudio Server 2024.12.1 on RHEL 9 with subscription-manager integration |
| Runtime Minimal | Container Image | Minimal pipeline runtime for Elyra/KFP with Python and curl |
| Runtime DataScience | Container Image | Data science pipeline runtime with ML libraries |
| Runtime PyTorch | Container Image | PyTorch pipeline runtime (CUDA) |
| Runtime TensorFlow | Container Image | TensorFlow pipeline runtime (CUDA) |
| Runtime ROCm PyTorch | Container Image | PyTorch pipeline runtime for AMD GPUs |
| Runtime ROCm TensorFlow | Container Image | TensorFlow pipeline runtime for AMD GPUs |
| ImageStream Manifests | Kustomize Manifests | OpenShift ImageStream definitions for all image types with version history |
| buildinputs | Go CLI Tool | Analyzes Dockerfile COPY/ADD directives to compute minimal build contexts for sandboxed builds |

## AIPCC Ecosystems Use

All Dockerfiles in this repository install Python packages (via micropipenv from Pipfile.lock) but do NOT use AIPCC base images. The images use standard Red Hat UBI9 (`registry.access.redhat.com/ubi9/python-311:latest`, `registry.access.redhat.com/ubi9/python-312:latest`) or CentOS Stream 9 (`quay.io/sclorg/python-311-c9s:c9s`) base images instead.

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| CPU (py3.11) | `jupyter/minimal/ubi9-python-3.11/Dockerfile.cpu` | `registry.access.redhat.com/ubi9/python-311:latest` | CPU | N/A | x86_64 | Active |
| CPU (py3.12) | `jupyter/minimal/ubi9-python-3.12/Dockerfile.cpu` | `registry.access.redhat.com/ubi9/python-312:latest` | CPU | N/A | x86_64 | Active |
| CUDA (py3.11) | `jupyter/minimal/ubi9-python-3.11/Dockerfile.cuda` | `registry.access.redhat.com/ubi9/python-311:latest` + CUDA layers | NVIDIA CUDA | 12.6.3 | x86_64, aarch64 | Active |
| CUDA (py3.12) | `jupyter/minimal/ubi9-python-3.12/Dockerfile.cuda` | `registry.access.redhat.com/ubi9/python-312:latest` + CUDA layers | NVIDIA CUDA | 12.6.3 | x86_64, aarch64 | Active |
| ROCm (py3.11) | `jupyter/minimal/ubi9-python-3.11/Dockerfile.rocm` | `registry.access.redhat.com/ubi9/python-311:latest` + ROCm layers | AMD ROCm | 6.2.4 | x86_64 | Active |
| ROCm (py3.12) | `jupyter/minimal/ubi9-python-3.12/Dockerfile.rocm` | `registry.access.redhat.com/ubi9/python-312:latest` + ROCm layers | AMD ROCm | 6.2.4 | x86_64 | Active |

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | No -- uses standard PyPI and download.pytorch.org |
| **pip.conf / uv.toml configured** | No AIPCC configuration; standard PyPI mirrors |
| **Additional packages installed** | All ML/data science packages from standard PyPI (see per-image Pipfile.lock) |
| **Source** | All Dockerfiles line ~18: `pip install micropipenv[toml]` |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|
| `/usr/libexec/rhaipcc/dnf` | Not used | N/A |
| `/etc/rhaipcc/env` | Not used | N/A |

_This component does not use AIPCC base images or tooling. All images use standard Red Hat UBI9 or CentOS Stream 9 base images with CUDA/ROCm installed from vendor repositories (NVIDIA developer.download.nvidia.com, AMD repo.radeon.com). Migration to AIPCC base images would provide secure Python package builds and standardized accelerator support. This represents a gap in AIPCC adoption for workbench images._

## Sub-Component Details

### Jupyter Minimal (CPU)

**Dockerfile**: `jupyter/minimal/ubi9-python-3.11/Dockerfile.cpu`
**Intent**: The base Jupyter workbench with minimal packages. Serves as the foundation for all other Jupyter variants. Deployed when a user creates a Notebook CR requesting a minimal environment.
**Image**: `quay.io/opendatahub/workbench-images:jupyter-minimal-ubi9-python-3.11`
**Language**: Python
**Port**: 8888/TCP (JupyterLab)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| PyPI (pypi.org) | HTTPS | 443/TCP | None | Python package installation at build time |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `JUPYTER_REUSABLE_UTILS` | env | `jupyter/utils` | Path to shared utility scripts |

### Jupyter DataScience (CPU)

**Dockerfile**: `jupyter/datascience/ubi9-python-3.11/Dockerfile.cpu`
**Intent**: Full-featured data science workbench with ML libraries, database connectors, and MongoDB CLI. Deployed for general-purpose data science work. The most commonly selected workbench type.
**Image**: `quay.io/opendatahub/workbench-images:jupyter-datascience-ubi9-python-3.11`
**Language**: Python
**Port**: 8888/TCP (JupyterLab)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| PyPI | HTTPS | 443/TCP | None | Python packages |
| MongoDB Atlas | HTTPS | 443/TCP | Credentials | MongoDB CLI (mongocli v2.0.3, built with FIPS) |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `MONGOCLI_VERSION` | build arg | `2.0.3` | MongoDB CLI version compiled in multi-stage build |

### Jupyter PyTorch (CUDA)

**Dockerfile**: `jupyter/pytorch/ubi9-python-3.11/Dockerfile.cuda`
**Intent**: Deep learning workbench with PyTorch 2.6 and CUDA 12.6.3 GPU acceleration. Deployed when users need NVIDIA GPU-backed PyTorch training/inference in their notebook.
**Image**: `quay.io/opendatahub/workbench-images:cuda-jupyter-pytorch-ubi9-python-3.11`
**Language**: Python
**Port**: 8888/TCP (JupyterLab)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| NVIDIA CUDA repo | HTTPS | 443/TCP | GPG key | CUDA toolkit and libraries |
| PyTorch wheels (download.pytorch.org) | HTTPS | 443/TCP | None | PyTorch CUDA wheels |

### Jupyter TensorFlow (CUDA)

**Dockerfile**: `jupyter/tensorflow/ubi9-python-3.11/Dockerfile.cuda`
**Intent**: Deep learning workbench with TensorFlow and CUDA 12.6.3 GPU acceleration. Deployed for NVIDIA GPU-backed TensorFlow training/inference.
**Image**: `quay.io/opendatahub/workbench-images:cuda-jupyter-tensorflow-ubi9-python-3.11`
**Language**: Python
**Port**: 8888/TCP (JupyterLab)

### Jupyter TrustyAI (CPU)

**Dockerfile**: `jupyter/trustyai/ubi9-python-3.11/Dockerfile.cpu`
**Intent**: Specialized workbench for AI explainability and fairness analysis using TrustyAI. Requires Java 17 runtime. Deployed when users need model explainability tools.
**Image**: `quay.io/opendatahub/workbench-images:jupyter-trustyai-ubi9-python-3.11`
**Language**: Python, Java
**Port**: 8888/TCP (JupyterLab)

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `java-17-openjdk` | dnf package | latest | Java 17 runtime for TrustyAI Java components |

### Code Server (CPU)

**Dockerfile**: `codeserver/ubi9-python-3.11/Dockerfile.cpu`
**Intent**: Browser-based VS Code IDE (code-server v4.98.0) with NGINX reverse proxy, supervisord process management, and data science Python packages. Provides a full IDE experience instead of notebook-style workflows.
**Image**: `quay.io/opendatahub/workbench-images:codeserver-ubi9-python-3.11`
**Language**: Python, TypeScript (code-server)
**Port**: 8787/TCP (code-server via NGINX proxy)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| GitHub (code-server releases) | HTTPS | 443/TCP | None | Code-server binary download |
| VS Code Marketplace | HTTPS | 443/TCP | None | Python and Jupyter extensions |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `CODESERVER_VERSION` | build arg | `v4.98.0` | Code-server version |
| `NGINX_VERSION` | env | `1.24` | NGINX reverse proxy version |

### RStudio Server (c9s)

**Dockerfile**: `rstudio/c9s-python-3.11/Dockerfile.cpu`
**Intent**: RStudio Server IDE on CentOS Stream 9 with R 4.4.3 for statistical computing. Provides R-language workbench for data analysts. Also has a CUDA GPU variant.
**Image**: `quay.io/opendatahub/workbench-images:rstudio-c9s-python-3.11`
**Language**: R, Python
**Port**: 8787/TCP (RStudio via NGINX proxy)

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `R_VERSION` | env | `4.4.3` | R language version |
| `RSTUDIO_RPM` | env | `rstudio-server-rhel-2024.12.1-563` | RStudio Server version |

### RStudio Server (RHEL9)

**Dockerfile**: `rstudio/rhel9-python-3.11/Dockerfile.cpu`
**Intent**: RStudio Server on RHEL 9 with subscription-manager integration for customers requiring RHEL entitlement validation. Identical functionality to c9s variant but uses RHEL base and subscription credentials.
**Image**: `quay.io/opendatahub/workbench-images:rstudio-rhel9-python-3.11`
**Language**: R, Python
**Port**: 8787/TCP (RStudio via NGINX proxy)

### Runtime Minimal (CPU)

**Dockerfile**: `runtimes/minimal/ubi9-python-3.11/Dockerfile.cpu`
**Intent**: Minimal Python runtime for Elyra and Kubeflow Pipelines execution. Contains only curl, Python, and basic ML packages. Used as the execution environment when KFP/Elyra runs pipeline steps.
**Image**: `quay.io/opendatahub/workbench-images:runtime-minimal-ubi9-python-3.11`
**Language**: Python
**Port**: None (batch execution)

### Runtime DataScience (CPU)

**Dockerfile**: `runtimes/datascience/ubi9-python-3.11/Dockerfile.cpu`
**Intent**: Data science pipeline runtime with ML libraries matching the Jupyter DataScience workbench. Ensures pipeline steps have the same package environment as the interactive notebook.
**Image**: `quay.io/opendatahub/workbench-images:runtime-datascience-ubi9-python-3.11`
**Language**: Python
**Port**: None (batch execution)

### Runtime PyTorch (CUDA)

**Dockerfile**: `runtimes/pytorch/ubi9-python-3.11/Dockerfile.cuda`
**Intent**: CUDA-accelerated PyTorch pipeline runtime for GPU training/inference steps in KFP pipelines.
**Image**: `quay.io/opendatahub/workbench-images:runtime-cuda-pytorch-ubi9-python-3.11`
**Language**: Python
**Port**: None (batch execution)

### Runtime TensorFlow (CUDA)

**Dockerfile**: `runtimes/tensorflow/ubi9-python-3.11/Dockerfile.cuda`
**Intent**: CUDA-accelerated TensorFlow pipeline runtime for GPU training/inference steps in KFP pipelines.
**Image**: `quay.io/opendatahub/workbench-images:runtime-cuda-tensorflow-ubi9-python-3.11`
**Language**: Python
**Port**: None (batch execution)

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_This component does not define CRDs. The Notebook CR is defined by odh-notebook-controller._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/lab` | GET | 8888/TCP | HTTP | None (pod-internal) | None (delegated to kube-rbac-proxy) | JupyterLab web UI |
| `/api` | GET/POST | 8888/TCP | HTTP | None (pod-internal) | None (delegated to kube-rbac-proxy) | JupyterLab REST API |
| `/codeserver/` | GET | 8787/TCP | HTTP | None (pod-internal) | None (delegated to kube-rbac-proxy) | Code Server web UI via NGINX |
| `/codeserver/healthz` | GET | 8787/TCP | HTTP | None | None | Code Server health check (excluded from activity logging) |
| `/` | GET | 8787/TCP | HTTP | None (pod-internal) | None (delegated to kube-rbac-proxy) | RStudio Server web UI via NGINX |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| NVIDIA CUDA Toolkit | 12.6.3 | For CUDA variants | GPU compute runtime for NVIDIA GPUs |
| NVIDIA cuDNN | 9.5.1.17 | For CUDA variants | Deep neural network library for GPU acceleration |
| AMD ROCm | 6.2.4 | For ROCm variants | GPU compute runtime for AMD GPUs |
| RStudio Server | 2024.12.1-563 | For RStudio images | R IDE server |
| code-server | 4.98.0 | For Code Server images | VS Code in the browser |
| NGINX | 1.24 | For Code Server/RStudio | Reverse proxy with activity logging |
| supervisord | latest | For Code Server/RStudio | Process management (NGINX + backend) |
| R | 4.4.3 | For RStudio images | R language runtime |
| Java 17 OpenJDK | latest | For TrustyAI images | Java runtime for TrustyAI components |
| micropipenv | latest | Yes | Deterministic Python package installation from Pipfile.lock |
| MongoDB CLI (mongocli) | 2.0.3 | For DataScience/PyTorch variants | MongoDB Atlas administration tool |
| OpenShift CLI (oc) | latest | Yes | OpenShift cluster interaction from within workbenches |
| skopeo | latest | Yes | Container image inspection utility |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| odh-notebook-controller | Pod Launcher | Creates and manages Pods from these container images when users create Notebook CRs |
| rhods-operator / opendatahub-operator | ImageStream Management | Deploys ImageStream manifests that reference these images |
| kube-rbac-proxy | Auth Sidecar | Injected by the notebook controller as a sidecar to authenticate users before proxying to the workbench |
| Kubeflow Pipelines (KFP) | Runtime Execution | Uses runtime images as execution environments for pipeline steps |
| Elyra | Runtime Execution | Uses runtime images for notebook-to-pipeline execution |
| OpenShift OAuth / OIDC | Authentication | User authentication is handled externally by kube-rbac-proxy sidecar |

## Deployment Manifests

### Kustomize Structure

| Base / Overlay | Path | Purpose |
|----------------|------|---------|
| base | `manifests/base/` | Core ImageStream definitions, params.env, commit.env, and Kustomize replacements for all Python 3.11 images |
| overlay | `manifests/overlays/additional/` | Python 3.12 ImageStream variants extending the base |
| base (per-image) | `jupyter/*/ubi9-python-*/kustomize/base/` | Per-image Pod + Service definitions for standalone deployment/testing |
| base (per-image) | `codeserver/*/kustomize/base/` | Code Server Pod + Service definitions |
| base (per-image) | `rstudio/*/kustomize/base/` | RStudio Pod + Service definitions |

### Parameterization

| Parameter | Source | Default | Purpose |
|-----------|--------|---------|---------|
| `odh-workbench-jupyter-minimal-cpu-py311-ubi9-n` | `params.env` / `params-latest.env` | Image digest ref | Current image reference for Jupyter Minimal CPU |
| `odh-workbench-jupyter-minimal-cuda-py311-ubi9-n` | `params.env` / `params-latest.env` | Image digest ref | Current image reference for Jupyter Minimal CUDA |
| `odh-workbench-jupyter-pytorch-cuda-py311-ubi9-n` | `params.env` / `params-latest.env` | Image digest ref | Current image reference for Jupyter PyTorch |
| `odh-workbench-jupyter-minimal-cpu-py311-ubi9-commit-n` | `commit.env` / `commit-latest.env` | Commit hash | Build source commit for traceability |
| `notebook-image-params` | ConfigMap (generated) | N/A | Kustomize ConfigMap generator from params.env files |
| `notebook-image-commithash` | ConfigMap (generated) | N/A | Kustomize ConfigMap generator from commit.env files |

_Total of 65+ image reference parameters and corresponding commit hash parameters. Each image type × version (N through N-5) has a separate parameter._

### Distribution Variants

| Variant | Path | Differences |
|---------|------|-------------|
| RHOAI (base) | `manifests/base/` | Python 3.11 images, all accelerator types, 6 version tags per ImageStream |
| RHOAI (additional) | `manifests/overlays/additional/` | Adds Python 3.12 variants as additional ImageStreams |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| notebook (Jupyter) | ClusterIP | 8888/TCP | notebook-port | HTTP | None | Delegated to kube-rbac-proxy sidecar | Internal |
| notebook (Code Server) | ClusterIP | 8787/TCP | codeserver-port | HTTP | None | Delegated to kube-rbac-proxy sidecar | Internal |
| notebook (RStudio) | ClusterIP | 8787/TCP | rstudio-port | HTTP | None | Delegated to kube-rbac-proxy sidecar | Internal |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

_Ingress is managed by the odh-notebook-controller (creates HTTPRoutes referencing the platform Gateway) and the rhods-operator (provides the Gateway). This component only defines the container images and ClusterIP services._

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| PyPI (pypi.org) | 443/TCP | HTTPS | TLS 1.2+ | None | Python package installation at build time |
| NVIDIA CUDA repo | 443/TCP | HTTPS | TLS 1.2+ | GPG signature | CUDA toolkit packages at build time |
| AMD ROCm repo (repo.radeon.com) | 443/TCP | HTTPS | TLS 1.2+ | None | ROCm packages at build time |
| download.pytorch.org | 443/TCP | HTTPS | TLS 1.2+ | None | PyTorch CUDA/ROCm wheels at build time |
| mirror.openshift.com | 443/TCP | HTTPS | TLS 1.2+ | None | OpenShift CLI download at build time |
| download2.rstudio.org | 443/TCP | HTTPS | TLS 1.2+ | None | RStudio Server RPM at build time |
| dl.fedoraproject.org | 443/TCP | HTTPS | TLS 1.2+ | None | EPEL release RPM at build time |
| GitHub (coder/code-server) | 443/TCP | HTTPS | TLS 1.2+ | None | code-server binary download at build time |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_These are container images, not operators. RBAC is defined by the odh-notebook-controller that deploys them._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|

_No secrets are defined by these images. Secret injection is handled by the notebook controller at Pod creation time._

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| JupyterLab (8888/TCP) | GET, POST | Bearer token / OIDC | kube-rbac-proxy sidecar | Per-user Notebook CR access |
| Code Server (8787/TCP) | GET, POST | Bearer token / OIDC | kube-rbac-proxy sidecar | Per-user Notebook CR access |
| RStudio Server (8787/TCP) | GET, POST | Bearer token / OIDC | kube-rbac-proxy sidecar | Per-user Notebook CR access |

_Authentication is not implemented within the container images themselves. The odh-notebook-controller injects a kube-rbac-proxy sidecar container that intercepts all traffic to the workbench and enforces OIDC/Bearer token authentication before proxying requests to the application._

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | `CGO_ENABLED=1 GOOS=linux go build -a -tags strictfipsruntime` (mongocli only) | `jupyter/datascience/ubi9-python-3.11/Dockerfile.cpu` line ~10 (multi-stage mongocli-builder) |
| **Linking** | Dynamic (CGO) for mongocli; Python images are interpreted, not compiled | Dockerfile multi-stage build |
| **OpenSSL in image** | Yes (via UBI9 base image `registry.access.redhat.com/ubi9/python-3xx`) | All Dockerfiles line 4 |
| **OLM FIPS annotation** | N/A (not an OLM-managed operator) | N/A |
| **check-payload in CI** | Yes -- GitHub Actions workflow runs check-payload FIPS validation | `.github/workflows/build-notebooks-TEMPLATE.yaml` line ~51 |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No application-level TLS; HTTP within pod, TLS terminated by kube-rbac-proxy sidecar | Workbench processes listen on HTTP localhost |
| **Crypto libraries** | Python stdlib (inherits OpenSSL from UBI9 base); mongocli compiled with strictfipsruntime | UBI9 base image, Dockerfile mongocli build stage |
| **Certificate handling** | System trust store (inherited from UBI9) | Base image default |
| **Non-FIPS crypto risks** | None detected in the image build process; runtime crypto behavior depends on user code executed in notebooks | N/A |

_The container images themselves are largely FIPS-passive -- they run Python interpreters that inherit OpenSSL from the UBI9 base image. The only compiled Go binary (mongocli) is explicitly built with `CGO_ENABLED=1 -tags strictfipsruntime`. The GitHub Actions build workflow includes a check-payload FIPS compliance step. CUDA and ROCm libraries are vendor-provided and their FIPS status depends on the vendor's certification._

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (main branch; downstream release branches may add this) |
| **Language deps (Python)** | Pipfile.lock | Yes (26 files) | pipenv / micropipenv | `jupyter/*/ubi9-python-*/Pipfile.lock`, `runtimes/*/ubi9-python-*/Pipfile.lock`, `codeserver/*/Pipfile.lock`, `rstudio/*/Pipfile.lock` |
| **Language deps (Go)** | go.sum | Yes (2 files) | go mod | `scripts/buildinputs/go.sum`, `scripts/check-payload/go.sum` |
| **Language deps (Python - root)** | uv.lock | Yes | uv | `uv.lock` (root project for tests/CI tooling) |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | not present | No | Hermeto (formerly cachi2) | N/A |

_Python dependencies are fully locked via 26 Pipfile.lock files (one per image variant). RPM packages are not locked on the main branch -- downstream release branches (e.g., rhoai-3.x) may introduce rpms.lock.yaml. No Hermeto/cachi2 prefetch is configured, meaning container builds are not network-isolated. The Tekton/Konflux pipeline parameter `hermetic: "false"` (in multiarch-pull-request-pipeline.yaml) confirms non-hermetic builds._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-user (each user gets their own workbench Pod) | Inferred from Notebook CR design -- one Pod per user notebook |
| **Deployment model** | per-user instance | Each Notebook CR creates a dedicated Pod with the workbench image |
| **Tenant identifier** | Notebook CR name (typically includes username) + namespace | Kubernetes namespace + CR name |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | kube-rbac-proxy sidecar per Pod | Platform (odh-notebook-controller injects sidecar) | Images themselves have no auth; isolation depends entirely on sidecar injection |
| Data storage | Per-user PVC mounted into workbench Pod | Kubernetes PVC + StorageClass | Users with access to the namespace could potentially mount other users' PVCs |
| Network traffic | Per-namespace NetworkPolicy (if configured by platform) | Kubernetes / Platform operator | No NetworkPolicy defined in this repo; relies on platform-level policy |
| Compute & resources | ResourceQuota and LimitRange at namespace level | Kubernetes | No resource limits defined in image manifests; relies on Notebook CR and namespace quota |
| Configuration & secrets | Per-Pod environment variables from Notebook CR | Kubernetes | Secrets injected at Pod creation, not baked into images |
| API scoping | Each workbench runs as an independent Pod | Kubernetes | JupyterLab/RStudio within a Pod has cluster access via mounted ServiceAccount token |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|

_No shared services within this component. Each workbench is an independent Pod. Shared infrastructure (model registries, pipeline services) is provided by other platform components._

## Data Flows

### Flow 1: User Workbench Access

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | User Browser | OpenShift Gateway (Envoy) | 443/TCP | HTTPS | TLS 1.2+ | OIDC |
| 2 | Gateway | kube-rbac-proxy sidecar | 8443/TCP | HTTPS | TLS | Bearer Token |
| 3 | kube-rbac-proxy | Workbench container (JupyterLab/Code Server/RStudio) | 8888/TCP or 8787/TCP | HTTP | None (localhost) | None (pre-authenticated) |

### Flow 2: Pipeline Runtime Execution

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | KFP Pipeline Controller | Runtime container Pod | N/A | N/A | N/A | ServiceAccount |
| 2 | Runtime container | External data sources (S3, databases) | 443/TCP | HTTPS | TLS 1.2+ | AWS IAM / Credentials |

### Flow 3: Image Build (Konflux)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | GitHub PR event | PipelinesAsCode (Tekton) | 443/TCP | HTTPS | TLS 1.2+ | GitHub webhook secret |
| 2 | Tekton build task | quay.io registry | 443/TCP | HTTPS | TLS 1.2+ | Registry credentials |
| 3 | CI validation | quay.io (skopeo inspect) | 443/TCP | HTTPS | TLS 1.2+ | Registry credentials |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| odh-notebook-controller | Pod Management | N/A | Kubernetes API | TLS | Creates Pods from these images when Notebook CRs are created |
| rhods-operator | ImageStream Deployment | N/A | Kubernetes API | TLS | Deploys ImageStream manifests from manifests/ directory |
| kube-rbac-proxy | Auth Sidecar | 8443/TCP | HTTPS | TLS | Injected into each workbench Pod to authenticate users |
| OpenShift Gateway (Envoy) | Ingress | 443/TCP | HTTPS | TLS 1.2+ | Routes external traffic to workbench Pods via HTTPRoute |
| Kubeflow Pipelines | Runtime Execution | N/A | Kubernetes API | TLS | Uses runtime images as pipeline step containers |
| Elyra | Runtime Execution | N/A | Kubernetes API | TLS | Uses runtime images for notebook-to-pipeline execution |
| quay.io/opendatahub | Image Registry | 443/TCP | HTTPS | TLS 1.2+ | Konflux-built images published here |
| quay.io/modh | Image Registry | 443/TCP | HTTPS | TLS 1.2+ | Legacy pre-Konflux images |
| Konflux (AppStudio) | CI/CD Build | 443/TCP | HTTPS | TLS 1.2+ | Builds multi-arch images via Tekton pipelines |
| GitHub (PipelinesAsCode) | CI Trigger | 443/TCP | HTTPS | TLS 1.2+ | CEL-based PR triggers for selective image builds |
| OpenDataHub Dashboard | UI Integration | N/A | ImageStream annotations | N/A | Dashboard reads ImageStream annotations to present workbench options to users |

## Architectural Analysis

The notebooks-downstream repository is architecturally unusual compared to typical platform components -- it is not an operator or a service, but rather a container image factory. Its primary output is ~35 container images covering a matrix of workbench types (Jupyter, Code Server, RStudio) × accelerator backends (CPU, NVIDIA CUDA, AMD ROCm) × Python versions (3.11, 3.12). This matrix-driven design creates significant maintenance complexity: each new Python version or accelerator adds a row across all image types, and each new ML framework version must be coordinated across all variants that include it.

The build architecture uses a layered Dockerfile pattern where accelerator-specific stages (cuda-base, rocm-base) are shared across image types. CUDA images use multi-stage builds with architecture-conditional stages (`cuda-base-amd64` vs `cuda-base-arm64`) selected via `TARGETARCH`, enabling multi-architecture builds from a single Dockerfile. The ROCm images follow a simpler pattern since ROCm only targets x86_64. A notable optimization is the `scripts/sandbox.py` + `bin/buildinputs` Go tool that analyzes Dockerfile `COPY` directives to create minimal build contexts, avoiding sending the entire repository to the container engine.

The image versioning strategy uses OpenShift ImageStreams with 6 historical versions per image type (N through N-5), allowing users to pin to specific software versions (e.g., "2024.2" with CUDA 12.4 and PyTorch 2.4). This is managed through a sophisticated Kustomize replacement system: `params.env` and `commit.env` files contain image digests and commit hashes that are injected into ImageStream tag definitions via ~120 Kustomize replacements. The `ci/check-params-env.sh` script (781 lines) validates image references, sizes (with 10% / 100MB thresholds), and metadata consistency. This approach provides strong auditability -- every deployed image can be traced to a specific source commit.

The FIPS compliance story is interesting: the images are primarily Python interpreters running on UBI9, which inherit FIPS-compliant OpenSSL from the base image. The only explicitly FIPS-compiled component is `mongocli`, built in a multi-stage Dockerfile with `CGO_ENABLED=1 -tags strictfipsruntime`. The GitHub Actions workflow includes a check-payload FIPS validation step. However, the CUDA and ROCm vendor libraries are not FIPS-certified, which is an inherent limitation for GPU-accelerated workloads.

A significant gap identified in this analysis is the absence of AIPCC base image adoption. All images use standard UBI9 or CentOS Stream 9 base images with CUDA/ROCm installed from vendor repositories. Migration to AIPCC base images would provide standardized accelerator support, the RHEL AI Python Package Index for secure package builds, and consistent versioning across RHOAI components. Given that this repository produces the largest number of Python-heavy container images in the platform, it represents the most impactful AIPCC adoption candidate.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 8f013d48 | 2026-07-22 | Update check-params for 3.12 image names and sizes |
| 15194d8e | 2026-07-22 | Add script to generate pull-request-specific PipelineRun definitions |
| 55f273dc | 2026-07-22 | Factor out common multiarch-pull-request-pipeline into its own file |
| 918637e3 | 2026-07-22 | Add PipelineRun definitions for minimal CPU Python 3.11/3.12 UBI9 runtimes |
| b7ef7cc3 | 2026-07-22 | Update RStudio references to new registries |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| `README.md` | 1-195 | Purpose, Metadata |
| `Makefile` | 1-532 | Architecture Components, Sub-Component Details, Build process |
| `pyproject.toml` | 1-157 | Metadata, Dependencies |
| `jupyter/minimal/ubi9-python-3.11/Dockerfile.cpu` | 1-110 | Sub-Component Details, AIPCC Ecosystems Use, FIPS Compliance |
| `jupyter/datascience/ubi9-python-3.11/Dockerfile.cpu` | 1-120 | Sub-Component Details, FIPS Compliance (mongocli build) |
| `jupyter/datascience/ubi9-python-3.12/Dockerfile.cpu` | 1-120 | Sub-Component Details |
| `jupyter/pytorch/ubi9-python-3.11/Dockerfile.cuda` | 1-200 | Sub-Component Details, AIPCC, Network Architecture |
| `jupyter/pytorch/ubi9-python-3.12/Dockerfile.cuda` | 1-200 | Sub-Component Details |
| `jupyter/tensorflow/ubi9-python-3.11/Dockerfile.cuda` | 1-200 | Sub-Component Details |
| `jupyter/tensorflow/ubi9-python-3.12/Dockerfile.cuda` | 1-200 | Sub-Component Details |
| `jupyter/trustyai/ubi9-python-3.11/Dockerfile.cpu` | 1-110 | Sub-Component Details, FIPS Compliance |
| `jupyter/trustyai/ubi9-python-3.12/Dockerfile.cpu` | 1-110 | Sub-Component Details |
| `jupyter/minimal/ubi9-python-3.11/Dockerfile.cuda` | 1-200 | AIPCC, Architecture Components |
| `jupyter/minimal/ubi9-python-3.12/Dockerfile.cuda` | 1-200 | AIPCC |
| `jupyter/minimal/ubi9-python-3.11/Dockerfile.rocm` | 1-100 | AIPCC, Architecture Components |
| `jupyter/minimal/ubi9-python-3.12/Dockerfile.rocm` | 1-100 | AIPCC |
| `jupyter/rocm/pytorch/ubi9-python-3.11/Dockerfile.rocm` | 1-100 | Architecture Components |
| `jupyter/rocm/pytorch/ubi9-python-3.12/Dockerfile.rocm` | 1-100 | Architecture Components |
| `jupyter/rocm/tensorflow/ubi9-python-3.11/Dockerfile.rocm` | 1-100 | Architecture Components |
| `codeserver/ubi9-python-3.11/Dockerfile.cpu` | 1-149 | Sub-Component Details, Network Architecture |
| `codeserver/ubi9-python-3.12/Dockerfile.cpu` | 1-149 | Sub-Component Details |
| `codeserver/ubi9-python-3.11/nginx/httpconf/http.conf` | 1-47 | Network Architecture, APIs Exposed |
| `codeserver/ubi9-python-3.11/supervisord/supervisord.conf` | 1-10 | Sub-Component Details |
| `codeserver/ubi9-python-3.11/kustomize/base/kustomization.yaml` | 1-10 | Deployment Manifests |
| `rstudio/c9s-python-3.11/Dockerfile.cpu` | 1-172 | Sub-Component Details, Dependencies |
| `rstudio/rhel9-python-3.11/Dockerfile.cpu` | 1-194 | Sub-Component Details |
| `rstudio/c9s-python-3.11/Dockerfile.cuda` | 1-304 | Sub-Component Details, FIPS |
| `rstudio/rhel9-python-3.11/Dockerfile.cuda` | 1-352 | Sub-Component Details |
| `rstudio/c9s-python-3.11/rsession.conf` | 1-2 | Sub-Component Details |
| `rstudio/c9s-python-3.11/supervisord/supervisord.conf` | 1-10 | Sub-Component Details |
| `rstudio/c9s-python-3.11/kustomize/base/kustomization.yaml` | 1-10 | Deployment Manifests |
| `runtimes/minimal/ubi9-python-3.11/Dockerfile.cpu` | 1-60 | Sub-Component Details, Build Hermeticity |
| `runtimes/minimal/ubi9-python-3.12/Dockerfile.cpu` | 1-60 | Sub-Component Details |
| `runtimes/datascience/ubi9-python-3.11/Dockerfile.cpu` | 1-60 | Sub-Component Details |
| `runtimes/datascience/ubi9-python-3.12/Dockerfile.cpu` | 1-60 | Sub-Component Details |
| `runtimes/pytorch/ubi9-python-3.11/Dockerfile.cuda` | 1-190 | Sub-Component Details, Dependencies |
| `runtimes/pytorch/ubi9-python-3.12/Dockerfile.cuda` | 1-160 | Sub-Component Details |
| `runtimes/tensorflow/ubi9-python-3.11/Dockerfile.cuda` | 1-190 | Sub-Component Details |
| `runtimes/tensorflow/ubi9-python-3.12/Dockerfile.cuda` | 1-170 | Sub-Component Details |
| `runtimes/rocm-pytorch/ubi9-python-3.11/Dockerfile.rocm` | 1-96 | Sub-Component Details, Dependencies |
| `runtimes/rocm-pytorch/ubi9-python-3.12/Dockerfile.rocm` | 1-96 | Sub-Component Details |
| `runtimes/rocm-tensorflow/ubi9-python-3.11/Dockerfile.rocm` | 1-91 | Sub-Component Details |
| `runtimes/rocm-tensorflow/ubi9-python-3.12/Dockerfile.rocm` | 1-91 | Sub-Component Details |
| `manifests/base/kustomization.yaml` | 1-1290 | Deployment Manifests, Parameterization |
| `manifests/base/jupyter-minimal-gpu-notebook-imagestream.yaml` | 1-154 | Deployment Manifests, Architecture Components |
| `manifests/base/jupyter-pytorch-notebook-imagestream.yaml` | 1-243 | Dependencies, Architecture Components |
| `manifests/base/runtime-datascience-imagestream.yaml` | 1-39 | Deployment Manifests |
| `manifests/base/params.env` | 1-65 | Deployment Manifests, Parameterization |
| `manifests/base/commit.env` | 1-65 | Deployment Manifests |
| `manifests/overlays/additional/kustomization.yaml` | 1-50 | Deployment Manifests |
| `.tekton/multiarch-pull-request-pipeline.yaml` | 1-150 | Integration Points, Build Hermeticity |
| `.tekton/odh-workbench-jupyter-minimal-cpu-py311-ubi9-pull-request.yaml` | 1-56 | Integration Points |
| `.tekton/odh-pipeline-runtime-datascience-cpu-py311-ubi9-pull-request.yaml` | 1-56 | Integration Points |
| `ci/check-params-env.sh` | 1-781 | Deployment Manifests, Architectural Analysis |
| `ci/buildconfig-updater.sh` | 1-27 | Deployment Manifests |
| `ci/kustomize.sh` | 1-143 | Deployment Manifests |
| `.github/workflows/build-notebooks-TEMPLATE.yaml` | 1-100 | FIPS Compliance, Integration Points |
| `.github/workflows/sync-branches-through-pr.yml` | 1-48 | Provenance |
| `scripts/sandbox.py` | 1-80 | Architectural Analysis |
| `component-architecture.json` | 1-130 | Network Architecture |
| `cuda/cuda.repo-amd64` | 1-7 | Dependencies, Egress |
| `overlays/0017-aipcc-base-images.md` | 1-80 | AIPCC Ecosystems Use |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `Dockerfile*konflux*` (find) | 0 files | Architecture Components (no Konflux Dockerfiles found) |
| `Dockerfile*` (find) | 35 files | Architecture Components, Sub-Component Details |
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` | 11 Dockerfiles | FIPS Compliance |
| `fips-compliant\|FIPS` (YAML/JSON) | 2 GitHub workflow files | FIPS Compliance |
| `cachi2\|hermeto\|REMOTE_SOURCES` | 0 files | Build Hermeticity |
| `Pipfile.lock` (find) | 26 files | Build Hermeticity |
| `requirements.txt` (find) | 26 files | Build Hermeticity |
| `rpms.lock.yaml\|go.sum\|uv.lock` (find) | 4 files | Build Hermeticity |
| `sync*.yaml` (find in workflows) | 1 file | Provenance |
| `upstream\|fork\|sync` (grep in workflows) | 15 matches | Provenance |
| `kustomization.yaml` (find) | 8+ files | Deployment Manifests |

### Summary

- **Total files read**: 63
- **Total lines referenced**: ~12,000
- **Coverage**: All sections have direct source backing. Provenance section uses `local_analysis` detection method (no component-map.json available). Multi-Tenancy tenant model is inferred from Notebook CR design pattern (tenancy enforced by odh-notebook-controller, not by these images). Authentication section documents that auth is delegated to kube-rbac-proxy sidecar, not implemented within images.

---
*Generated in 7m 59s (479s total)*
