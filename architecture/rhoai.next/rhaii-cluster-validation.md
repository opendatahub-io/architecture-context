# Component: RHAII Cluster Validation

## Metadata

- **Repository**: https://github.com/red-hat-data-services/rhaii-cluster-validation
- **Version**: db30b88 (main)
- **Distribution**: RHOAI
- **Languages**: Go 1.25
- **Deployment Type**: CLI Tool / kubectl Plugin
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/opendatahub-io/rhaii-cluster-validation | -- | main | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/rhaii-cluster-validation | auto_merge | main | -- | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: kubectl plugin for validating GPU cluster readiness for AI/ML workloads by checking GPU hardware, RDMA connectivity, and cross-node network bandwidth.

**Detailed**: RHAII Cluster Validation is a preflight validation tool that runs on GPU clusters before deploying AI inference workloads. It ships as a kubectl plugin (`kubectl rhaii-validate`) and as a container image that can be run via podman. The tool auto-detects the target platform (AKS, EKS, CoreWeave, OpenShift), GPU vendor (NVIDIA/AMD), and cluster topology, then runs a series of validation checks.

The tool orchestrates validation by creating Kubernetes Jobs on GPU nodes. Per-node checks run privileged containers that access host hardware via `chroot /host` to validate GPU drivers, ECC memory errors, and GPU-NIC NUMA topology. Multi-node bandwidth tests use ring or star topologies with iperf3 (TCP) and ib_write_bw (RDMA) to measure cross-node bandwidth. RDMA connectivity is tested via ibv_rc_pingpong in a pairwise mesh across all GPU nodes. Results are stored in a ConfigMap as a structured JSON report.

The tool ships two container images: a validator image containing the Go binary with CLI commands and per-node check logic, and a tools image containing RDMA/network testing utilities (iperf3, perftest with CUDA support) used as workload containers for bandwidth Jobs.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| rhaii-validator | Go CLI (kubectl plugin) | Orchestrates cluster validation: discovers GPU nodes, deploys check/bandwidth Jobs, collects results, generates reports |
| validator-tools | Container image (C/CUDA binaries + utilities) | Provides iperf3 and perftest (ib_write_bw, ibv_rc_pingpong) with CUDA GPUDirect RDMA support for bandwidth testing |

## AIPCC Ecosystems Use

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| NVIDIA CUDA 13.0 (builder only) | Dockerfile.konflux.validator.tools | `registry.redhat.io/rhai/base-image-cuda-13.0-rhel9@sha256:e29b922...` | NVIDIA CUDA | 13.0 | aarch64, x86_64 | Active |
| NVIDIA CUDA 13.0 (builder only) | tools/Dockerfile.konflux | `registry.redhat.io/rhai/base-image-cuda-13.0-rhel9@sha256:e29b922...` | NVIDIA CUDA | 13.0 | aarch64, x86_64 | Active |

The AIPCC base image (`base-image-cuda-13.0-rhel9`) is used only as a **build stage** to compile perftest with CUDA support. The runtime stage uses vanilla `registry.redhat.io/ubi9/ubi` — the AIPCC Python package index and runtime environment are not used. The only artifact carried from the AIPCC builder is the compiled perftest binaries and `libcudart.so.*`.

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | No — AIPCC base image used only as build stage; runtime image is vanilla UBI9 |
| **pip.conf / uv.toml configured** | No — no Python packages installed at runtime |
| **Additional packages installed** | Runtime installs system RPMs only (iperf3, pciutils, rdma-core, etc.) via dnf |
| **Source** | Dockerfile.konflux.validator.tools:40-47 |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|
| `${APP_ROOT}/lib/tools/public-repos.sh` | Switch from internal mirrors to public vendor repos during build | Dockerfile.konflux.validator.tools:12, tools/Dockerfile.konflux:12 |

## Sub-Component Details

### rhaii-validator (cluster-validation)

**Dockerfile**: `Dockerfile.konflux` (also `Dockerfile.konflux.cluster-validation`)
**Intent**: Primary service — the Go CLI binary that orchestrates all cluster validation. Runs as a kubectl plugin on the user's workstation or inside a container with kubeconfig access. Discovers GPU nodes, deploys per-node check Jobs, runs bandwidth tests, and generates validation reports.
**Image**: `registry.redhat.io/rhoai/odh-rhaii-cluster-validator-rhel9:v3.4.0`
**Language**: Go
**Port**: N/A (CLI tool, no listening ports in normal mode)

#### API Routes

_N/A — CLI tool, no HTTP API exposed._

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Kubernetes API Server | HTTPS | 6443/TCP | Bearer Token (kubeconfig) | Create/manage Jobs, read Nodes, manage ConfigMaps, RBAC |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `RELATED_IMAGE_RHAII_CLUSTER_VALIDATOR` | env | `quay.io/opendatahub/odh-rhaii-cluster-validator:odh-stable` | Override validator container image |
| `RELATED_IMAGE_RHAII_VALIDATOR_TOOLS` | env | `quay.io/opendatahub/odh-rhaii-validator-tools:odh-stable` | Override tools container image |
| `--namespace` | flag | `rhaii-validation` | Target namespace for validation resources |
| `--timeout` | flag | `5m` | Check timeout duration |
| `--config` | flag | (none) | Platform config override file |
| `--debug` | flag | `false` | Keep pods alive for inspection |
| `--output` | flag | `table` | Output format (table or json) |
| `--pull-secret` | flag | (none) | Image pull secret name |
| `--server-node` | flag | (none) | Server node for star topology |
| `/etc/rhaii-validate/platform.yaml` | config file | (embedded platform defaults) | ConfigMap-mounted platform config |

### validator-tools

**Dockerfile**: `Dockerfile.konflux.validator.tools` (also `tools/Dockerfile.konflux`)
**Intent**: Utility image — provides RDMA and network bandwidth testing tools (iperf3, perftest suite including ib_write_bw, ib_read_bw, ibv_rc_pingpong) compiled with CUDA GPUDirect RDMA support. Deployed as workload containers inside bandwidth Jobs created by the validator.
**Image**: `registry.redhat.io/rhoai/odh-rhaii-validator-tools-rhel9:v3.4.0`
**Language**: C (compiled perftest binaries)
**Port**: Varies per test (iperf3: 5201/TCP, ib_write_bw: 18515+/TCP, pingmesh: 18515+/TCP, loopback: 20000+/TCP)

#### API Routes

_N/A — workload containers, no API exposed._

#### Upstream Dependencies

_N/A — standalone test binaries, no upstream service calls._

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| GPU device resources | k8s resources | (from platform config) | GPU devices for GPUDirect RDMA (nvidia.com/gpu) |
| RDMA device resources | k8s resources | (from platform config) | RDMA devices (rdma/rdma_shared_device_a, nvidia.com/roce) |

## APIs Exposed

### Custom Resource Definitions (CRDs)

_No CRDs defined by this component._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| NVIDIA GPU Driver | ≥535.0 (configurable) | Yes (NVIDIA) | GPU hardware checks via nvidia-smi |
| AMD ROCm Driver | ≥6.0.0 (configurable) | Yes (AMD) | GPU hardware checks via rocm-smi |
| iperf3 | any | Yes (bandwidth tests) | TCP bandwidth measurement between nodes |
| perftest (ib_write_bw, ibv_rc_pingpong) | 25.10.0 | Yes (RDMA tests) | RDMA bandwidth and connectivity testing |
| libcudart | CUDA 13.0 | Yes (RDMA BW) | CUDA runtime for GPUDirect RDMA in ib_write_bw |
| Kubernetes | v1.35+ | Yes | Job orchestration, node discovery, ConfigMap storage |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| Gateway API CRDs | CRD existence check | Validates gateways.gateway.networking.k8s.io and httproutes.gateway.networking.k8s.io are installed |
| Gateway API Inference Extension CRDs | CRD existence check | Validates inferencepools.inference.networking.x-k8s.io (with fallback to inference.networking.k8s.io) |
| LeaderWorkerSet CRDs | CRD existence check | Validates leaderworkersets.leaderworkerset.x-k8s.io is installed |
| cert-manager | CRD + operator check | Validates certificates.cert-manager.io CRD and cert-manager pods in cert-manager namespace |
| Istio | Operator check | Validates Istio pods running in istio-system namespace |
| LeaderWorkerSet Operator | Operator check | Validates LWS controller pods in openshift-lws-operator namespace |
| GPU Device Plugin | Resource provider | Exposes nvidia.com/gpu or amd.com/gpu extended resources on nodes |
| RDMA Device Plugin | Resource provider | Exposes RDMA resources (nvidia.com/roce, rdma/rdma_shared_device_a) on nodes |
| OpenShift SCC | Platform integration | Privileged SCC binding for sysfs/device access on OCP clusters |

## Deployment Manifests

_This component is a CLI tool — it does not use kustomize manifests for deployment. Embedded YAML templates (via `//go:embed`) define the Job and RBAC resources created at runtime._

### Embedded Templates

| Template | Path | Purpose |
|----------|------|---------|
| Node Check Job | deploy/node-check-job.yaml | Per-node validation Job template with privileged security context |
| RBAC | deploy/rbac.yaml | Namespace, ServiceAccount, ClusterRole, ClusterRoleBinding |
| Image References | manifests/image-references/image-references.yaml | Default container image references for validator and tools |

### Platform Configs (Embedded)

| Platform | Config File | Purpose |
|----------|------------|---------|
| AKS | platforms/aks.yaml | Azure AKS defaults (driver version, thresholds, RDMA resources) |
| EKS | platforms/eks.yaml | AWS EKS defaults |
| CoreWeave | platforms/coreweave.yaml | CoreWeave defaults |
| OCP | platforms/ocp.yaml | OpenShift defaults |

## Network Architecture

### Services

_No Kubernetes Services are created. All communication uses direct pod IPs within Jobs._

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| Kubernetes API Server | 6443/TCP | HTTPS | TLS 1.2+ | Bearer Token (kubeconfig) | Job lifecycle, node discovery, ConfigMap management, RBAC setup |
| GPU node sysfs | N/A (host path) | N/A | N/A | privileged container | nvidia-smi/rocm-smi, /sys/class/infiniband, /sys/bus/pci/devices |
| Inter-node pod network | 5201/TCP | TCP | plaintext | N/A | iperf3 bandwidth tests between node pairs |
| Inter-node pod network | 12865/TCP | TCP | plaintext | N/A | TCP latency tests (built-in echo server) |
| Inter-node pod network | 18515+/TCP | RDMA (InfiniBand/RoCE) | N/A | N/A | ib_write_bw and ibv_rc_pingpong RDMA tests |
| Intra-node loopback | 20000+/TCP | RDMA (InfiniBand/RoCE) | N/A | N/A | GPU-NIC loopback bandwidth probes |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| rhaii-validator | (empty) | (empty rules) | (empty) |

_The ClusterRole is intentionally empty — it exists only as an SCC subject on OpenShift. The controller runs under the invoking admin's kubeconfig, not the ServiceAccount. Workload pods never call the Kubernetes API._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| rhaii-validator | (cluster-scoped) | ClusterRole/rhaii-validator | rhaii-validation/rhaii-validator |
| rhaii-validator-scc-{namespace} | (cluster-scoped, OCP only) | ClusterRole/system:openshift:scc:privileged | rhaii-validation/rhaii-validator |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| (user-provided pull secret) | kubernetes.io/dockerconfigjson | Image pull for registry.redhat.io | User (--pull-secret flag) | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| Kubernetes API | All CRUD | Bearer Token (kubeconfig) | kube-apiserver | Cluster-admin or namespace-admin required |
| Job pods | N/A | AutomountServiceAccountToken: false | Pod spec | SA tokens explicitly disabled on all workload pods |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | `GOEXPERIMENT=strictfipsruntime`, `CGO_ENABLED=1` | Dockerfile.konflux:11, Dockerfile.konflux.cluster-validation:11 |
| **Linking** | Dynamic (CGO_ENABLED=1) | Dockerfile.konflux:11 |
| **OpenSSL in image** | Yes (via UBI9 base image) | Dockerfile.konflux:14 |
| **OLM FIPS annotation** | N/A — not an operator, no CSV | N/A |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | Uses k8s client-go default TLS config (inherits system OpenSSL via FIPS runtime) | go.mod:8-10 (k8s.io/client-go v0.35.3) |
| **Crypto libraries** | stdlib crypto/tls via Go FIPS runtime (strictfipsruntime) | Dockerfile.konflux:11 |
| **Certificate handling** | System trust store via kubeconfig | cmd/agent/main.go (kubeconfig flag) |
| **Non-FIPS crypto risks** | None detected — no custom TLS config, no MD5/RC4, no InsecureSkipVerify | grep results (no hits for crypto/md5, crypto/rc4, InsecureSkipVerify) |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | Yes | rpm-lockfile-prototype | rpms.lock.yaml (validator), tools/rpms.lock.yaml (tools), tools/builder/rpms.lock.yaml (tools builder) |
| **Language deps** | go.sum | Yes | go mod | go.sum |
| **Artifacts** | not found | No | Hermeto | N/A |
| **Hermeto prefetch** | not present | No | Hermeto (formerly cachi2) | N/A |

_RPM lock files are present for both the validator and tools images across aarch64 and x86_64 architectures. Go dependencies are locked via go.sum. No Hermeto/cachi2 prefetch integration was found in the Dockerfiles. The perftest source tarball (tools/perftest-25.10.0-0.128.gd01b183.tar.gz) is vendored directly in the repository rather than fetched at build time._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-namespace | pkg/controller/controller.go (--namespace flag, default rhaii-validation) |
| **Deployment model** | single-tenant | Inferred — CLI tool runs in a single namespace, no multi-tenant isolation |
| **Tenant identifier** | namespace name | cmd/agent/main.go (--namespace flag) |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | Invoking user's kubeconfig RBAC | Kubernetes | Requires cluster-admin or namespace-admin; creates cluster-scoped RBAC resources |
| Data storage | ConfigMaps in validation namespace | Kubernetes | Reports stored in namespace-scoped ConfigMaps; no cross-namespace data leakage |
| Network traffic | None | N/A | Bandwidth test Jobs communicate over pod network without NetworkPolicy; acceptable for ephemeral validation workloads |
| Compute & resources | GPU/RDMA resource requests on Jobs | Kubernetes scheduler | Jobs consume GPU and RDMA resources on target nodes; no ResourceQuota enforcement |
| Configuration & secrets | Per-namespace ConfigMap (rhaii-validate-config) | Kubernetes | Platform config is namespace-scoped |
| API scoping | N/A | N/A | CLI tool — not a long-running service with API scoping concerns |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|

## Data Flows

### Flow 1: GPU Hardware Validation

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | rhaii-validator CLI | Kubernetes API | 6443/TCP | HTTPS | TLS 1.2+ | Bearer Token |
| 2 | Kubernetes API | GPU node (Job scheduled) | N/A | N/A | N/A | N/A |
| 3 | Check Job pod | Host sysfs (nvidia-smi) | N/A | chroot /host | N/A | privileged container |
| 4 | Check Job pod | rhaii-validator CLI (pod logs) | N/A | Kubernetes API | TLS 1.2+ | Bearer Token |
| 5 | rhaii-validator CLI | ConfigMap (report) | 6443/TCP | HTTPS | TLS 1.2+ | Bearer Token |

### Flow 2: RDMA Bandwidth Test (Ring Topology)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | rhaii-validator CLI | Kubernetes API (create server Job) | 6443/TCP | HTTPS | TLS 1.2+ | Bearer Token |
| 2 | Server Job pod | Listen on RDMA port | 18515+/TCP | RDMA (IB/RoCE) | N/A | N/A |
| 3 | rhaii-validator CLI | Kubernetes API (create client Job) | 6443/TCP | HTTPS | TLS 1.2+ | Bearer Token |
| 4 | Client Job pod | Server Job pod (ib_write_bw) | 18515+/TCP | RDMA (IB/RoCE) | N/A | N/A |
| 5 | Client Job pod | rhaii-validator CLI (pod logs) | N/A | Kubernetes API | TLS 1.2+ | Bearer Token |
| 6 | rhaii-validator CLI | ConfigMap (report) | 6443/TCP | HTTPS | TLS 1.2+ | Bearer Token |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| Kubernetes API Server | REST API (client-go) | 6443/TCP | HTTPS | TLS 1.2+ | Node discovery, Job/Pod/ConfigMap CRUD, RBAC management |
| NVIDIA GPU Driver | Host command (nvidia-smi) | N/A | N/A | N/A | Query GPU driver version, ECC errors, PCI topology |
| AMD ROCm Driver | Host command (rocm-smi, amd-smi) | N/A | N/A | N/A | Query GPU driver version, RAS errors, GPU enumeration |
| NVIDIA Container Toolkit | Device mount | N/A | N/A | N/A | Mounts libcuda.so.1 and GPU devices into containers |
| RDMA Device Plugin | Resource advertisement | N/A | N/A | N/A | Exposes RDMA extended resources (nvidia.com/roce, rdma/*) on nodes |
| GPU Device Plugin | Resource advertisement | N/A | N/A | N/A | Exposes nvidia.com/gpu or amd.com/gpu on nodes |
| iperf3 | Pod-to-pod TCP | 5201/TCP | TCP | plaintext | TCP bandwidth measurement between node pairs |
| ib_write_bw (perftest) | Pod-to-pod RDMA | 18515+/TCP | RDMA | N/A | RDMA write bandwidth per GPU-NIC pair |
| ibv_rc_pingpong (perftest) | Pod-to-pod RDMA | 18515+/TCP | RDMA | N/A | RDMA connectivity verification (pingmesh) |
| Gateway API CRDs | CRD GET (apiextensions.k8s.io) | 6443/TCP | HTTPS | TLS 1.2+ | Validates Gateway API prerequisites are installed |
| InferencePool CRD | CRD GET (apiextensions.k8s.io) | 6443/TCP | HTTPS | TLS 1.2+ | Validates Gateway API Inference Extension prerequisites |
| LeaderWorkerSet CRD | CRD GET (apiextensions.k8s.io) | 6443/TCP | HTTPS | TLS 1.2+ | Validates LeaderWorkerSet operator prerequisites |
| cert-manager | Namespace/Pod list | 6443/TCP | HTTPS | TLS 1.2+ | Validates cert-manager operator health |
| Istio | Namespace/Pod list | 6443/TCP | HTTPS | TLS 1.2+ | Validates Istio service mesh operator health |
| OpenShift SCC | ClusterRoleBinding | 6443/TCP | HTTPS | TLS 1.2+ | Grants privileged SCC to validator ServiceAccount on OCP |

## Architectural Analysis

RHAII Cluster Validation follows a controller-agent architecture where the CLI binary serves dual roles: as an orchestration controller (when invoked by the user) and as a per-node agent (when running inside Jobs it creates). The `run` hidden subcommand is the agent entry point — it executes GPU, RDMA, and topology checks directly on the scheduled node, writing structured JSON to stdout and progress to stderr. The controller parses pod logs to collect results. This two-stream output design (JSON on stdout, progress on stderr) enables the controller to reliably parse structured results without interference from human-readable progress messages.

The job runner subsystem implements three test topologies: ring (each node tested as both sender and receiver in sequential pairs), star (one server, all clients), and pairwise (N-choose-2 round-robin tournament for RDMA connectivity). The pairwise scheduler ensures disjoint pairs within each round to avoid GPU/RDMA resource conflicts when running concurrent tests. Each bandwidth job follows a server-first lifecycle: the controller creates the server Job, waits for its pod IP, then creates client Jobs that connect to that IP. This avoids race conditions between server readiness and client connection attempts, with a 3-second delay after pod IP assignment for the listening process to start.

GPU-NIC pairing is one of the most sophisticated subsystems, implementing four strategies based on hardware topology. On bare metal with clear PCIe hierarchy, PCIe distance pairing finds the shortest-path GPU-NIC assignment. On VMs with flat NUMA topology (all devices report NUMA -1), the system falls back to NUMA affinity or bandwidth probing — the latter runs loopback ib_write_bw tests across the full GPU×NIC matrix and uses measured bandwidth to greedily assign optimal pairs. This is critical for RDMA performance: a mismatched GPU-NIC pair can degrade bandwidth by 50% or more due to cross-NUMA PCIe traffic.

Security is deliberately minimal but carefully considered. All workload pods disable ServiceAccount token mounting (`automountServiceAccountToken: false`) since they never call the Kubernetes API — they are pure compute/test containers. The validator ServiceAccount's ClusterRole is intentionally empty; it exists only as an SCC subject on OpenShift. The actual Kubernetes API calls are made by the CLI binary using the invoking user's kubeconfig credentials, not the ServiceAccount. Privileged containers are used only where hardware access requires it (RDMA device access, GPU sysfs queries, chroot /host for host tools). Device name validation via regex (`^[a-zA-Z0-9][a-zA-Z0-9_-]*$`) prevents argument injection through RDMA device names that could start with `-`.

The CRD validation subsystem is notable for its version comparison sophistication. It supports both Kubernetes API version ordering (v1alpha1 < v1beta1 < v1) and semver comparison with pre-release handling (v1.4.0-rc1 < v1.4.0). CRD version requirements include a fallback name mechanism to handle API graduation (e.g., InferencePool moving from `inference.networking.x-k8s.io` to `inference.networking.k8s.io`). Release versions are extracted from CRD annotations via JSONPath expressions, enabling validation of the specific bundle version installed rather than just the API version served.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| db30b88 | 2026-07 | Security hardening: defer RBAC cleanup, disable SA token mount, use IfNotPresent pull policy, validate device names, harden Job privilege profile |
| f2c1915 | 2026-07 | Remove open-data-hub-tenant PipelineRun manifests |
| 208a152 | 2026-07 | Add intra-host GPU-NIC bandwidth probe for optimal pairing on flat topologies |
| 677637e | 2026-07 | Add --pull-secret flag and remove RDMA from AKS default |
| 48e1743 | 2026-06 | Refresh rpm lockfiles, adding libibverbs-utils to tools |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| README.md | 1-475 | Purpose, Architecture Components, Dependencies, Data Flows |
| go.mod | 1-60 | Metadata, Dependencies, Security (FIPS) |
| Makefile | 1-170 | Metadata, Architecture Components |
| Dockerfile.konflux | 1-39 | Architecture Components, AIPCC, Security (FIPS, Build Hermeticity) |
| Dockerfile.konflux.cluster-validation | 1-38 | Sub-Component Details, Security (FIPS) |
| Dockerfile.konflux.validator.tools | 1-68 | Sub-Component Details, AIPCC Ecosystems Use, Security |
| tools/Dockerfile.konflux | 1-68 | Sub-Component Details, AIPCC Ecosystems Use |
| deploy/rbac.yaml | 1-32 | Security (RBAC) |
| deploy/node-check-job.yaml | 1-50 | Deployment Manifests, Security |
| deploy/embed.go | 1-11 | Deployment Manifests |
| manifests/image-references/image-references.yaml | 1-5 | Sub-Component Details, Configuration |
| manifests/image-references/embed.go | 1-10 | Architecture Components |
| examples/configmap.yaml | 1-48 | Configuration, Deployment Manifests |
| rpms.lock.yaml | 1-951 | Security (Build Hermeticity) |
| tools/rpms.lock.yaml | (checked existence) | Security (Build Hermeticity) |
| tools/builder/rpms.lock.yaml | (checked existence) | Security (Build Hermeticity) |
| component-architecture.json | 1-819 | Architecture Components, Configuration |
| cmd/agent/main.go | 1-429 | Sub-Component Details, Configuration, Data Flows |
| cmd/agent/tcplat.go | 1-199 | Network Architecture (ports), Sub-Component Details |
| pkg/controller/controller.go | 1-2652 | RBAC, Data Flows, Integration Points, Network Architecture |
| pkg/jobrunner/helpers.go | 1-100 | Security (pod spec), Deployment Manifests |
| pkg/jobrunner/job.go | 1-133 | Architecture Components |
| pkg/jobrunner/runner.go | 1-577 | Data Flows, Network Architecture |
| pkg/runner/runner.go | 1-80 | Architecture Components |
| pkg/checks/check.go | 1-203 | Architecture Components, Multi-Tenancy |
| pkg/checks/crd/crd.go | 1-413 | Integration Points, Dependencies |
| pkg/checks/gpu/driver.go | 1-135 | Dependencies, Integration Points |
| pkg/checks/gpu/ecc.go | 1-83 | Dependencies |
| pkg/checks/gpu/amd_driver.go | 1-119 | Dependencies, Integration Points |
| pkg/checks/gpu/amd_ecc.go | 1-70 | Dependencies |
| pkg/checks/operator/operator.go | 1-165 | Integration Points, Dependencies |
| pkg/config/detect.go | 1-61 | Architecture Components |
| pkg/config/images.go | 1-74 | Configuration, Sub-Component Details |
| pkg/config/loader.go | 1-54 | Configuration |
| pkg/config/platform.go | 1-184 | Configuration, Dependencies |
| pkg/checks/networking/bandwidth.go | 1-109 | Network Architecture |
| pkg/checks/networking/iperf_job.go | 1-110 | Network Architecture, Data Flows |
| pkg/checks/networking/tcplat_job.go | 1-123 | Network Architecture |
| pkg/checks/rdma/devices.go | 1-197 | Integration Points, Dependencies |
| pkg/checks/rdma/gpu_nic_lpbk_bw.go | 1-294 | Network Architecture, Architectural Analysis |
| pkg/checks/rdma/pingmesh_job.go | 1-303 | Network Architecture, Data Flows |
| pkg/checks/rdma/pingmesh_types.go | 1-71 | Architecture Components |
| pkg/checks/rdma/rdmabw_job.go | 1-175 | Network Architecture, Data Flows |
| pkg/checks/rdma/rdmawep_job.go | 1-204 | Network Architecture |
| pkg/checks/rdma/status.go | 1-235 | Integration Points |
| pkg/checks/rdma/topology.go | 1-665 | Architectural Analysis, Integration Points |
| .github/workflows/ | (directory listing) | Provenance |
| overlays/0017-aipcc-base-images.md | 1-100 | AIPCC Ecosystems Use |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` (Dockerfiles) | Dockerfile.konflux, Dockerfile.konflux.cluster-validation | Security (FIPS) |
| `cachi2\|hermeto\|REMOTE_SOURCES` (Dockerfiles) | (no matches) | Security (Build Hermeticity) |
| `rpms.lock.yaml` (find) | rpms.lock.yaml, tools/rpms.lock.yaml, tools/builder/rpms.lock.yaml | Security (Build Hermeticity) |
| `*.go` excluding `*_test.go` (find) | 30 Go source files | All code analysis sections |
| `Dockerfile*konflux*` (find) | Dockerfile.konflux, Dockerfile.konflux.cluster-validation, Dockerfile.konflux.validator.tools, tools/Dockerfile.konflux | Architecture Components, AIPCC |
| `git remote -v` | origin (red-hat-data-services) | Provenance |
| `git log --oneline -20` | 20 recent commits | Recent Changes |

### Summary

- **Total files read**: 45
- **Total lines referenced**: ~9,500
- **Coverage**: All sections have direct source backing. Provenance section uses `local_analysis` detection method (no component-map.json available). Multi-tenancy analysis is based on inference from the CLI tool nature of the component.

---
*Generated in 6m 37s (397s total)*
