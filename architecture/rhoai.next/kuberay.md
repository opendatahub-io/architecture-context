# Component: KubeRay Operator

## Metadata

- **Repository**: https://github.com/red-hat-data-services/kuberay
- **Version**: 4e68e2c8 (main branch, based on upstream KubeRay v1.6.2)
- **Distribution**: RHOAI
- **Languages**: Go 1.25
- **Deployment Type**: Operator (controller-runtime based)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/ray-project/kuberay | -- | -- | -- | local_analysis |
| Midstream | https://github.com/opendatahub-io/kuberay | sync_workflow | main | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/kuberay | auto_merge | main | `fast-forward-stable.yaml` | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Kubernetes operator that manages the lifecycle of Ray clusters, jobs, services, and scheduled jobs on OpenShift and Kubernetes.

**Detailed**: KubeRay is the Kubernetes operator for the Ray distributed computing framework. It defines four Custom Resource Definitions — RayCluster, RayJob, RayService, and RayCronJob — that allow users to declaratively manage Ray workloads. The operator handles pod lifecycle, autoscaling, network exposure, mutual TLS, and authentication for Ray clusters.

In the RHOAI distribution, KubeRay is enhanced with OpenShift-specific capabilities: Gateway API integration with HTTPRoute-based ingress, kube-rbac-proxy sidecar injection for OIDC-based authentication, NetworkPolicy enforcement for cluster isolation, OpenShift TLS security profile compliance, and SecurityContextConstraints for pod security. The operator creates cross-namespace HTTPRoutes in the platform namespace, referencing cluster-namespace Services via ReferenceGrants to enable centralized Gateway API authentication.

The operator supports batch scheduling integration (Volcano, Yunikorn, Kai-scheduler, scheduler-plugins), zero-downtime RayService upgrades via Gateway API traffic splitting, and optional mTLS via cert-manager for intra-cluster communication.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| kuberay-operator | Go Operator (controller-runtime) | Manages RayCluster, RayJob, RayService, RayCronJob CRDs and associated Kubernetes resources |
| RayCluster Controller | Controller | Reconciles RayCluster CRs — creates head/worker pods, services, ingress, RBAC, handles upgrades and GCS fault tolerance |
| RayJob Controller | Controller | Reconciles RayJob CRs — creates ephemeral RayClusters, submitter Jobs, monitors job execution via Ray Dashboard API |
| RayService Controller | Controller | Reconciles RayService CRs — manages active/pending clusters, serve application deployment, zero-downtime upgrades via Gateway API |
| RayCronJob Controller | Controller | Reconciles RayCronJob CRs — creates RayJob instances on cron schedules |
| RayCluster mTLS Controller | Controller | Manages mutual TLS certificate lifecycle via cert-manager for RayClusters with mTLS annotation |
| Authentication Controller | Controller | Manages kube-rbac-proxy sidecar injection, HTTPRoute creation, ReferenceGrant management, and ServiceAccount provisioning for OIDC authentication on OpenShift |
| NetworkPolicy Controller | Controller | Creates head and worker NetworkPolicies for RayClusters with secure-trusted-network annotation |
| RayCluster Mutating Webhook | Webhook | Defaults secure-trusted-network annotation and enforces enableIngress=false on OpenShift |
| RayCluster Validating Webhook | Webhook | Validates RayCluster metadata and worker group name uniqueness |
| RayJob Validating Webhook | Webhook | Validates RayJob spec |
| RayService Validating Webhook | Webhook | Validates RayService spec |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|
| ray.io | v1 | RayCluster | Namespaced | Declares a Ray cluster with head and worker group specs, autoscaling, auth options, and GCS fault tolerance |
| ray.io | v1 | RayJob | Namespaced | Declares a Ray job with entrypoint, submission mode (K8sJob/HTTP/Interactive/Sidecar), deletion strategy, and optional ephemeral cluster |
| ray.io | v1 | RayService | Namespaced | Declares a Ray Serve application with upgrade strategy (NewCluster/IncrementalUpgrade/None) and serve configuration |
| ray.io | v1 | RayCronJob | Namespaced | Declares a scheduled RayJob with cron expression and job template |
| config.ray.io | v1alpha1 | Configuration | Namespaced | Operator runtime configuration (metrics, probes, leader election, batch scheduler, reconcile concurrency) |

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| /metrics | GET | 8080/TCP | HTTP | TLS (OpenShift TLS profile) | None | Prometheus metrics endpoint |
| /healthz | GET | 8082/TCP | HTTP | None | None | Liveness probe |
| /readyz | GET | 8082/TCP | HTTP | None | None | Readiness probe |
| /mutate-ray-io-v1-raycluster | POST | 9443/TCP | HTTPS | TLS (serving-cert) | Kubernetes API server | Mutating webhook for RayCluster |
| /validate-ray-io-v1-raycluster | POST | 9443/TCP | HTTPS | TLS (serving-cert) | Kubernetes API server | Validating webhook for RayCluster |
| /validate-ray-io-v1-rayjob | POST | 9443/TCP | HTTPS | TLS (serving-cert) | Kubernetes API server | Validating webhook for RayJob |
| /validate-ray-io-v1-rayservice | POST | 9443/TCP | HTTPS | TLS (serving-cert) | Kubernetes API server | Validating webhook for RayService |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| controller-runtime | v0.22.4 | Yes | Kubernetes controller framework |
| cert-manager | v1.18.2 | No | mTLS certificate lifecycle management (required when mTLS annotation is set) |
| gateway-api | v1.4.1 | No | HTTPRoute/Gateway/ReferenceGrant for RHOAI ingress (required on OpenShift with auth) |
| openshift/api | v0.0.0-20251214 | No | OpenShift Route API, cluster config API (required on OpenShift) |
| scheduler-plugins | v0.33.5 | No | Batch scheduling integration |
| volcano.sh/apis | v1.13.0 | No | Volcano gang scheduling integration |
| robfig/cron/v3 | v3.0.1 | No | RayCronJob cron expression parsing (required when RayCronJob feature gate enabled) |
| kube-rbac-proxy | latest | No | OIDC authentication sidecar (required on OpenShift with auth) |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| rhods-operator / opendatahub-operator | Kustomize deployment | Deploys kuberay-operator via kustomize manifests from `config/openshift/` |
| Platform Gateway (data-science-gateway) | HTTPRoute parent reference | Authentication controller creates HTTPRoutes referencing the platform Gateway |
| kube-rbac-proxy image | Container image | Injected as sidecar into Ray head pods for OIDC authentication |
| Ray Dashboard API | REST API | Job submission, status polling, serve config deployment (accessed via head pod port 8265) |

## Deployment Manifests

### Kustomize Structure

| Base / Overlay | Path | Purpose |
|----------------|------|---------|
| base | `ray-operator/config/crd/` | CRD definitions for RayCluster, RayJob, RayService, RayCronJob |
| base | `ray-operator/config/rbac/` | ClusterRole, ClusterRoleBinding, ServiceAccount, leader election RBAC |
| base | `ray-operator/config/manager/` | Operator Deployment and metrics Service |
| base | `ray-operator/config/default/` | Combines CRD + RBAC + manager with default namespace |
| overlay | `ray-operator/config/openshift/` | OpenShift-specific: SCC, webhook configuration, Gateway API env vars, kube-rbac-proxy image, params.env |
| overlay | `ray-operator/config/default-with-webhooks/` | Adds webhook server port, cert volume mount, webhook Service |
| overlay | `ray-operator/config/certmanager/` | cert-manager Certificate for webhook TLS (non-OpenShift) |
| overlay | `ray-operator/config/overlays/test-overrides/` | Test deployment overrides |

### Parameterization

| Parameter | Source | Default | Purpose |
|-----------|--------|---------|---------|
| `namespace` | params.env | `opendatahub` | Target namespace for deployment |
| `odh-kuberay-operator-controller-image` | params.env | `quay.io/opendatahub/kuberay-operator:v1.6.2` | Operator container image |
| `odh-kube-rbac-proxy-image` | params.env | `quay.io/opendatahub/odh-kube-rbac-proxy:odh-stable` | kube-rbac-proxy sidecar image |
| `APPLICATION_NAMESPACE` | env var (from namespace param) | `opendatahub` | Platform namespace for HTTPRoute creation |
| `POD_NAMESPACE` | env var (downward API) | (pod namespace) | Operator pod namespace for fallback detection |
| `RELATED_IMAGE_ODH_KUBE_RBAC_PROXY_IMAGE` | env var (from params.env) | (from params.env) | kube-rbac-proxy image override for disconnected environments |
| `MONITORING_NAMESPACE` | env var | (empty) | Prometheus scraping namespace for NetworkPolicy rules |
| `GATEWAY_NAMESPACE` | env var | `openshift-ingress` (OpenShift) | Gateway API pods namespace for NetworkPolicy rules |
| `GATEWAY_NAME` | env var | (unset) | Gateway CR name for HTTPRoute parent reference |

### Distribution Variants

| Variant | Path | Differences |
|---------|------|-------------|
| Upstream (default) | `ray-operator/config/default/` | Basic operator deployment without webhooks or OpenShift features |
| OpenShift (RHOAI) | `ray-operator/config/openshift/` | Adds SCC, webhook config with OpenShift serving-cert, Gateway API env vars, kube-rbac-proxy image parameter, common labels |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| kuberay-operator | ClusterIP | 8080/TCP | 8080 | HTTP | TLS (OpenShift TLS profile) | None | Internal |
| kuberay-webhook-service | ClusterIP | 443/TCP | 9443 | HTTPS | TLS (OpenShift serving-cert) | Kubernetes API server | Internal |
| {cluster}-head-svc | ClusterIP (headless default) | 10001/TCP, 6379/TCP, 8265/TCP, 8080/TCP, 8000/TCP | Same | HTTP/Ray RPC | None (plaintext) or mTLS (if annotation set) | None / kube-rbac-proxy | Internal |
| {cluster}-serve-svc | ClusterIP | 8000/TCP (serve) | Same | HTTP | None | None | Internal |
| {cluster}-headless-workers | ClusterIP (headless) | None | None | Ray RPC | None or mTLS | None | Internal |
| kube-rbac-proxy sidecar | (container port) | 8443/TCP | 8443 | HTTPS | TLS | OIDC via SubjectAccessReview | Internal (fronts dashboard) |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|
| {ns}-{cluster} HTTPRoute | HTTPRoute (Gateway API) | /ray/{namespace}/{cluster}/ | 8265/TCP | HTTPS | TLS (Gateway-terminated) | SIMPLE | External (via platform Gateway) |
| {cluster}-ray Route | Route (OpenShift) | Auto-generated | 8265/TCP | HTTPS | Edge | Edge | External (legacy, disabled on RHOAI with auth) |
| {cluster} Ingress | Ingress (Kubernetes) | Configurable | 8265/TCP | HTTP/HTTPS | Configurable | Configurable | External (non-OpenShift) |
| RayService Gateway | Gateway (Gateway API) | Configurable | Configurable | HTTPS | TLS | SIMPLE | External (incremental upgrade) |
| RayService HTTPRoute | HTTPRoute (Gateway API) | Configurable | 8000/TCP (serve) | HTTPS | TLS (Gateway-terminated) | SIMPLE | External (incremental upgrade traffic splitting) |

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| Kubernetes API server | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token | Controller-runtime operations, RBAC, resource management |
| Ray Head Dashboard | 8265/TCP | HTTP | None (in-cluster) | None | Job submission, status polling, serve config deployment |
| Redis (external) | 6379/TCP | TCP | Configurable | Password (optional) | GCS fault tolerance external storage |
| cert-manager API | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token | Certificate and Issuer lifecycle management |
| OpenShift API (config.openshift.io) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token | TLS security profile resolution, auth mode detection |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| kuberay-operator | "" (core) | pods | get, list, watch, create, update, patch, delete, deletecollection |
| kuberay-operator | "" (core) | pods/resize | patch |
| kuberay-operator | "" (core) | pods/proxy | get, patch, update |
| kuberay-operator | "" (core) | configmaps, events, pods/status, serviceaccounts, services | get, list, watch, create, update, patch, delete |
| kuberay-operator | "" (core) | secrets | get, list, watch, create, delete |
| kuberay-operator | "" (core) | services/proxy | get, create, patch, update |
| kuberay-operator | "" (core) | services/status | get, patch, update |
| kuberay-operator | apps | deployments | get, list, watch, patch, update |
| kuberay-operator | batch | jobs | get, list, watch, create, update, patch, delete |
| kuberay-operator | ray.io | rayclusters, rayjobs, rayservices, raycronjobs | get, list, watch, create, update, patch, delete |
| kuberay-operator | ray.io | rayclusters/status, rayjobs/status, rayservices/status, raycronjobs/status | get, patch, update |
| kuberay-operator | ray.io | rayclusters/finalizers, rayjobs/finalizers, rayservices/finalizers, raycronjobs/finalizers | update |
| kuberay-operator | gateway.networking.k8s.io | gateways | get, list, watch, create, update |
| kuberay-operator | gateway.networking.k8s.io | httproutes, referencegrants | get, list, watch, create, update, patch, delete |
| kuberay-operator | networking.k8s.io | ingresses, networkpolicies | get, list, watch, create, update, patch, delete |
| kuberay-operator | networking.k8s.io | ingressclasses | get, list, watch |
| kuberay-operator | extensions | ingresses | get, list, watch, create, update, patch, delete |
| kuberay-operator | route.openshift.io | routes | get, list, watch, create, update, patch, delete |
| kuberay-operator | rbac.authorization.k8s.io | roles | get, list, watch, create, update, delete |
| kuberay-operator | rbac.authorization.k8s.io | rolebindings | get, list, watch, create, update, patch, delete |
| kuberay-operator | rbac.authorization.k8s.io | clusterroles, clusterrolebindings | get, list, watch |
| kuberay-operator | cert-manager.io | certificates, issuers | get, list, watch, create, update, patch, delete |
| kuberay-operator | cert-manager.io | certificates/status | get, patch, update |
| kuberay-operator | coordination.k8s.io | leases | get, list, create, update |
| kuberay-operator | discovery.k8s.io | endpointslices | get, list, watch |
| kuberay-operator | authentication.k8s.io | tokenreviews | create |
| kuberay-operator | authorization.k8s.io | subjectaccessreviews | create |
| kuberay-operator | config.openshift.io | apiservers | get |
| kuberay-operator | config.openshift.io | authentications, authentications/status, oauths, oauths/status | get, list, watch |
| kuberay-operator | operator.openshift.io | kubeapiservers, kubeapiservers/status | get, list, watch |

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| kuberay-operator | (cluster-wide) | kuberay-operator (ClusterRole) | kuberay-operator |
| {cluster-name} | (cluster namespace) | {cluster-name} (Role) | {cluster-name}-autoscaler-sa (head group SA) |
| {cluster-name} (patched) | (cluster namespace) | {cluster-name} (Role) | {cluster-name}-oidc-proxy-sa (auth proxy SA) |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| kuberay-webhook-server-cert | kubernetes.io/tls | Webhook server TLS certificate | OpenShift service-ca (via serving-cert annotation) | Yes |
| {cluster}-ca-secret-{uid} | Opaque | mTLS root CA certificate and key | cert-manager (via mTLS controller) | Yes (30-day renewal) |
| {cluster}-head-cert | kubernetes.io/tls | Head node TLS certificate for mTLS | cert-manager | Yes (15-day renewal before 90-day expiry) |
| {cluster}-worker-cert | kubernetes.io/tls | Worker node TLS certificate for mTLS | cert-manager | Yes (15-day renewal before 90-day expiry) |
| {cluster}-oidc-tls | kubernetes.io/tls | TLS secret for OIDC auth proxy | Platform (if configured) | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| Ray Dashboard (8265) | GET, POST | OIDC via kube-rbac-proxy SubjectAccessReview | kube-rbac-proxy sidecar (port 8443) | User must have `get` permission on `{cluster}-head-svc` Service |
| Ray Dashboard (8265) | GET, POST | Token auth (non-OpenShift) | Ray internal auth | Bearer token from Secret `auth_token` key |
| HTTPRoute /ray/{ns}/{cluster}/ | GET | Gateway API + kube-rbac-proxy | Platform Gateway → kube-rbac-proxy (8443) → Ray Dashboard (8265) | Cross-namespace ReferenceGrant enables Service reference |
| Webhook endpoints (9443) | POST | Kubernetes API server mutual TLS | kube-apiserver → webhook Service | Kubernetes admission control |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | `GOEXPERIMENT=strictfipsruntime`, `CGO_ENABLED=1`, `-tags strictfipsruntime` | `ray-operator/Dockerfile.konflux:30` |
| **Linking** | Dynamic (CGO_ENABLED=1) | `ray-operator/Dockerfile.konflux:30` |
| **OpenSSL in image** | Yes (via UBI9 ubi-minimal base) | `ray-operator/Dockerfile.konflux:32` |
| **OLM FIPS annotation** | Not present (no CSV in repo) | N/A |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | OpenShift TLS security profile compliant; resolves `apiservers.config.openshift.io/cluster` to determine MinVersion and CipherSuites; falls back to Intermediate profile (TLS 1.2+, ECDHE ciphers) | `ray-operator/pkg/tls/tls.go:1-90` |
| **Crypto libraries** | stdlib `crypto/tls` (FIPS via OpenSSL at runtime with strictfipsruntime) | `ray-operator/pkg/tls/tls.go` |
| **Certificate handling** | cert-manager for mTLS certificates; OpenShift service-ca for webhook TLS; system trust store for API server connections | `ray-operator/controllers/ray/raycluster_mtls_controller.go`, `ray-operator/config/openshift/webhook.yaml` |
| **Non-FIPS crypto risks** | None detected. TLS config uses FIPS-approved ECDHE cipher suites. No MD5/RC4/DES usage found. | Grep of `crypto/md5`, `crypto/rc4`, `crypto/des`, `InsecureSkipVerify` across all Go source |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (expected on downstream release branches) |
| **Language deps** | go.sum | Yes | go mod | `ray-operator/go.sum`, `go.sum` |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | cachi2.env / hermeto | No | Hermeto (formerly cachi2) | Not present in Dockerfile.konflux |

_Note: This is the `main` branch. Downstream release branches (e.g., `rhoai-3.x`) likely add `rpms.lock.yaml` and Hermeto prefetch integration during release hardening. Go dependencies are hermetically locked via `go.sum`._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-namespace (CRDs are namespace-scoped) | `ray-operator/apis/ray/v1/raycluster_types.go` — all CRDs are `Namespaced` scope |
| **Deployment model** | multi-tenant (single operator instance manages all namespaces or configurable subset) | `ray-operator/main.go:93-97` — `--watch-namespace` flag |
| **Tenant identifier** | Namespace name | `ray-operator/main.go:229-241` — namespace-scoped cache configuration |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | Per-cluster kube-rbac-proxy sidecar validates user has `get` permission on head Service; per-cluster ServiceAccount for autoscaler and auth | Kubernetes RBAC + kube-rbac-proxy | Auth only enforced when `ray.io/enable-secure-trusted-network` annotation is set; without it, Ray dashboard is unauthenticated |
| Data storage | Per-namespace PVCs (user-configured in pod templates); per-cluster Secrets for auth tokens and certificates | Kubernetes namespace isolation | No operator-level data isolation enforcement — depends on user-configured pod templates |
| Network traffic | Per-cluster NetworkPolicies (head and worker) restrict ingress when annotation enabled; worker pods only accept intra-cluster traffic | NetworkPolicy controller | NetworkPolicies only created when annotation `odh.ray.io/secure-trusted-network` is set; without it, no network isolation |
| Compute & resources | User-configured ResourceRequirements on pod templates; autoscaler min/max replicas | Kubernetes scheduler | No operator-enforced resource quotas — relies on namespace-level ResourceQuota |
| Configuration & secrets | Per-cluster Secrets, ConfigMaps, ServiceAccounts | Kubernetes namespace isolation | kube-rbac-proxy ConfigMap and ServiceAccount are per-cluster; ReferenceGrant is shared per namespace |
| API scoping | Namespace-scoped CRD watches (configurable via `--watch-namespace`); cache selectors filter Jobs by `kuberay-operator` label | Kubernetes informer cache | Without `--watch-namespace`, operator watches all namespaces (cluster-wide) |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| Platform Gateway (RHOAI) | HTTPRoutes per-cluster in platform namespace | HTTPRoute path-based routing: `/ray/{namespace}/{cluster}/` |
| ReferenceGrant | One per namespace (shared by all auth-enabled clusters in namespace) | Reference-counted creation/deletion |
| Redis (GCS fault tolerance) | Namespace-scoped within Redis | `externalStorageNamespace` field in GcsFaultToleranceOptions |

## Admission Webhooks

This component defines 5 webhook(s) (2 mutating, 3 validating).

| Name | Type | Target Resources | Purpose |
|------|------|-----------------|---------|
| mraycluster.kb.io | mutating | rayclusters | Mutating webhook that defaults RayCluster resources on create/update. On OpenShift clusters (detected via Route API discovery), it enforces secure trusted networking by setting the EnableSecureTrustedNetwork annotation to true and forcibly disables EnableIngress to ensure all access goes through Gateway API authentication rather than direct Routes/Ingress. On non-OpenShift clusters it sets the annotation to false. |
| mraycluster.kb.io | mutating |  | Mutating webhook that defaults RayCluster resources on create/update. On OpenShift clusters (detected via Route API discovery), it enforces secure trusted networking by setting the EnableSecureTrustedNetwork annotation to true and forcibly disables EnableIngress to ensure all access goes through Gateway API authentication rather than direct Routes/Ingress. On non-OpenShift clusters it sets the annotation to false. |
| vraycluster.kb.io | validating |  | Validates RayCluster resources on create and update, ensuring the cluster metadata name is valid and that all worker group names within the spec are unique. |
| vrayjob.kb.io | validating |  | Validating admission webhook for RayJob resources that rejects create and update requests whose metadata (specifically the object name) fails RayJob-specific naming rules enforced by ValidateRayJobMetadata. Delete requests are always allowed without validation. |
| vrayservice.kb.io | validating |  | Validating admission webhook for RayService resources that rejects create and update requests whose metadata (specifically the object name) fails RayService-specific naming rules enforced by ValidateRayServiceMetadata. Delete requests are always allowed. |

### External Webhooks

The following webhooks from peer components intercept this component's resource types:

| Webhook | Defined By |
|---------|-----------|
| mraycluster.ray.openshift.ai | codeflare-operator |
| vraycluster.ray.openshift.ai | codeflare-operator |

## Data Flows

### Flow 1: RayCluster Authentication (OIDC via Gateway API)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | External Client | Platform Gateway | 443/TCP | HTTPS | TLS 1.2+ (Gateway-terminated) | None (at Gateway) |
| 2 | Platform Gateway | kube-rbac-proxy sidecar (head pod) | 8443/TCP | HTTPS | TLS | OIDC token (SubjectAccessReview) |
| 3 | kube-rbac-proxy | Ray Dashboard (localhost) | 8265/TCP | HTTP | None (loopback) | None (upstream proxied) |

### Flow 2: RayJob Submission (K8sJobMode)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | RayJob Controller | Kubernetes API | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token |
| 2 | RayJob Controller | RayCluster (creates) | N/A | N/A | N/A | N/A |
| 3 | RayJob Controller | Ray Dashboard API | 8265/TCP | HTTP | None | None |
| 4 | Submitter Job Pod | Ray Dashboard API | 8265/TCP | HTTP | None | None |

### Flow 3: RayService Incremental Upgrade (Gateway API Traffic Splitting)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | External Client | Gateway (created by RayService) | 443/TCP | HTTPS | TLS | None |
| 2 | Gateway | Active cluster serve-svc | 8000/TCP | HTTP | None | None |
| 3 | Gateway | Pending cluster serve-svc | 8000/TCP | HTTP | None | None |
| 4 | RayService Controller | HTTPRoute (weight update) | N/A | Kubernetes API | TLS 1.2+ | ServiceAccount token |

### Flow 4: mTLS Certificate Lifecycle

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | mTLS Controller | cert-manager API | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token |
| 2 | cert-manager | Kubernetes Secrets | N/A | Kubernetes API | TLS 1.2+ | ServiceAccount token |
| 3 | Ray Head Pod | Ray Worker Pods | Ray RPC ports | gRPC | mTLS (cert-manager issued) | Client certificate |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| Kubernetes API Server | REST API (controller-runtime) | 443/TCP | HTTPS | TLS 1.2+ | All resource CRUD, informer watches, leader election |
| Ray Dashboard API | REST API (HTTP client) | 8265/TCP | HTTP | None | Job submission (POST /api/jobs/), job status polling, serve config deployment, StopJob |
| cert-manager | CRD (Issuer, Certificate) | N/A | Kubernetes API | TLS 1.2+ | mTLS certificate lifecycle: create SelfSigned Issuer → CA Certificate → CA Issuer → leaf certs |
| Platform Gateway | CRD (Gateway, HTTPRoute, ReferenceGrant) | N/A | Kubernetes API | TLS 1.2+ | Create HTTPRoutes with cross-namespace backend refs for authenticated dashboard access |
| kube-rbac-proxy | Container sidecar | 8443/TCP | HTTPS | TLS | OIDC auth enforcement: SubjectAccessReview on head Service, upstream proxy to dashboard |
| OpenShift API (config.openshift.io) | REST API | 443/TCP | HTTPS | TLS 1.2+ | TLS security profile resolution (apiservers), auth mode detection (authentications, oauths) |
| OpenShift API (operator.openshift.io) | REST API | 443/TCP | HTTPS | TLS 1.2+ | KubeAPIServer configuration monitoring |
| OpenShift Routes | CRD (Route) | N/A | Kubernetes API | TLS 1.2+ | Legacy dashboard exposure (disabled when auth enabled) |
| Volcano scheduler | CRD (PodGroup) | N/A | Kubernetes API | TLS 1.2+ | Gang scheduling: create PodGroups for Ray cluster pods |
| Yunikorn scheduler | Pod annotations | N/A | N/A | N/A | Task group annotations for gang scheduling |
| Kai-scheduler | CRD (PodGroup) | N/A | Kubernetes API | TLS 1.2+ | GPU-sharing aware gang scheduling |
| scheduler-plugins | CRD (PodGroup) | N/A | Kubernetes API | TLS 1.2+ | Scheduler plugins integration for co-scheduling |
| Prometheus | Metrics scrape | 8080/TCP | HTTP | TLS (OpenShift TLS profile) | Cluster/job/service metrics: pod counts, resource usage, job durations |
| Kubernetes Batch API | CRD (Job) | N/A | Kubernetes API | TLS 1.2+ | RayJob submitter Jobs, GCS FT Redis cleanup Jobs |
| Redis (external) | TCP client | 6379/TCP | TCP | Configurable | GCS fault tolerance external storage |
| rhods-operator / opendatahub-operator | Kustomize deployment | N/A | N/A | N/A | Deploys kuberay-operator manifests from `config/openshift/` |
| Kubernetes NetworkPolicy API | CRD (NetworkPolicy) | N/A | Kubernetes API | TLS 1.2+ | Per-cluster head/worker NetworkPolicies for network isolation |

## Architectural Analysis

The KubeRay operator implements a hierarchical controller architecture where RayService owns RayClusters, and RayJob creates ephemeral RayClusters. This composition creates a clean ownership chain: `RayCronJob → RayJob → RayCluster → Pods/Services`. Each layer adds lifecycle management — RayCronJob adds scheduling, RayJob adds submission and cleanup, RayService adds serve application deployment and zero-downtime upgrades. The ownership model uses Kubernetes controller references for garbage collection, with finalizers for operations that require ordered cleanup (GCS Redis namespace cleanup, job stopping via dashboard API, authentication resource removal).

The RHOAI-specific authentication architecture is notable for its cross-namespace design. The authentication controller creates HTTPRoutes in the platform namespace (APPLICATION_NAMESPACE) that reference Services in user namespaces via Gateway API ReferenceGrants. This centralizes ingress through a single platform Gateway while maintaining per-cluster authentication via kube-rbac-proxy sidecars. The kube-rbac-proxy checks SubjectAccessReview against the head Service — effectively, access to the Ray dashboard requires Kubernetes RBAC permission to `get` the cluster's head Service. The ReferenceGrant is reference-counted (one per namespace, shared by all auth-enabled clusters) and cleaned up when the last authenticated cluster in a namespace is deleted. The mutating webhook enforces `enableIngress: false` on OpenShift to prevent unauthenticated Route creation, ensuring all external access flows through the authenticated Gateway API path.

The mTLS controller follows a cert-manager integration pattern: SelfSigned Issuer bootstraps a per-cluster CA, which then issues head and worker leaf certificates. Certificate names include a UID suffix to prevent conflicts during cluster recreation. The controller monitors certificate expiry and relies on cert-manager for automatic renewal. Pod IPs are included as SANs, requiring certificate updates when pods scale, which the controller handles by watching pod changes.

The operator's TLS configuration for its own metrics endpoint demonstrates defense-in-depth: it reads the OpenShift cluster-wide TLS security profile (`apiservers.config.openshift.io/cluster`), which can be Intermediate, Modern, Old, or Custom. This ensures the operator's TLS configuration matches the cluster's security posture. Non-OpenShift deployments get a hardened default (TLS 1.2+, ECDHE ciphers with ALPN).

The batch scheduler integration is pluggable via a manager interface, supporting Volcano, Yunikorn, Kai-scheduler, and scheduler-plugins. Each scheduler implementation modifies pod specs (annotations, labels) and optionally creates PodGroup CRDs for gang scheduling semantics. This ensures all Ray cluster pods are scheduled together, avoiding resource deadlock in shared clusters.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 4a6c1050 | 2026-07 | Release KubeRay 1.6.2 for ODH, restore test resources, add CI validation |
| c992b373 | 2026-07 | Integrate with cluster TLS security profile (RHOAIENG-61067) |
| a7e6a14b | 2026-07 | Tier 3 updates for Ray repos (RHOAIENG-61477) |
| d087660f | 2026-07 | Dockerfile digest updates |
| 9f2fd708 | 2026-07 | Add disconnected readiness check and testing updates |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| `ray-operator/main.go` | 1-388 | Architecture Components, APIs Exposed, Dependencies, Multi-Tenancy |
| `ray-operator/Dockerfile.konflux` | 1-48 | Metadata, Security (FIPS), Build Hermeticity |
| `ray-operator/Dockerfile.rhoai` | 1-34 | Security (FIPS) |
| `ray-operator/go.mod` | 1-112 | Dependencies, Metadata |
| `go.mod` | 1-142 | Dependencies |
| `ray-operator/apis/ray/v1/raycluster_types.go` | 1-400+ | APIs Exposed (CRDs), Multi-Tenancy |
| `ray-operator/apis/ray/v1/rayjob_types.go` | 1-300+ | APIs Exposed (CRDs) |
| `ray-operator/apis/ray/v1/rayservice_types.go` | 1-300+ | APIs Exposed (CRDs) |
| `ray-operator/apis/ray/v1/raycronjob_types.go` | 1-50+ | APIs Exposed (CRDs) |
| `ray-operator/apis/ray/v1/groupversion_info.go` | 1-20 | APIs Exposed (CRDs) |
| `ray-operator/apis/config/v1alpha1/configuration_types.go` | 1-80+ | APIs Exposed, Deployment Manifests |
| `ray-operator/apis/config/v1alpha1/defaults.go` | 1-40+ | APIs Exposed, Deployment Manifests |
| `ray-operator/apis/config/v1alpha1/config_utils.go` | 1-50+ | Architecture Components |
| `ray-operator/controllers/ray/raycluster_controller.go` | 1-2541 | Architecture Components, Network Architecture, RBAC, Data Flows |
| `ray-operator/controllers/ray/rayjob_controller.go` | 1-1606 | Architecture Components, Data Flows, Integration Points |
| `ray-operator/controllers/ray/rayservice_controller.go` | 1-2083 | Architecture Components, Data Flows, Integration Points |
| `ray-operator/controllers/ray/raycronjob_controller.go` | 1-195 | Architecture Components |
| `ray-operator/controllers/ray/raycluster_mtls_controller.go` | 1-895 | Security (Secrets), Data Flows, Integration Points |
| `ray-operator/controllers/ray/authentication_controller.go` | 1-1105 | Network Architecture (Ingress), Security (Auth), Data Flows, Multi-Tenancy |
| `ray-operator/controllers/ray/networkpolicy_controller.go` | 1-494 | Network Architecture, Security, Multi-Tenancy |
| `ray-operator/controllers/ray/utils/auth.go` | 1-50+ | Security (Auth) |
| `ray-operator/controllers/ray/utils/auth_sidecar.go` | 1-80+ | Security (Auth), Network Architecture |
| `ray-operator/controllers/ray/utils/constant.go` | 1-40 | Architecture Components |
| `ray-operator/controllers/ray/common/service.go` | 1-200+ | Network Architecture (Services) |
| `ray-operator/controllers/ray/common/route.go` | 1-50+ | Network Architecture (Ingress) |
| `ray-operator/controllers/ray/common/ingress.go` | 1-100+ | Network Architecture (Ingress) |
| `ray-operator/controllers/ray/common/rbac.go` | 1-100+ | RBAC |
| `ray-operator/controllers/ray/common/pod.go` | 1-400+ | Architecture Components, Network Architecture |
| `ray-operator/controllers/ray/common/job.go` | 1-50+ | Integration Points |
| `ray-operator/pkg/tls/tls.go` | 1-90 | Security (FIPS, TLS) |
| `ray-operator/pkg/features/features.go` | 1-50+ | Architecture Components |
| `ray-operator/pkg/webhooks/v1/raycluster_mutating_webhook.go` | 1-74 | Architecture Components, Security (Auth) |
| `ray-operator/pkg/webhooks/v1/raycluster_validating_webhook.go` | 1-89 | Architecture Components |
| `ray-operator/config/rbac/role.yaml` | 1-303 | RBAC |
| `ray-operator/config/manager/manager.yaml` | 1-91 | Deployment Manifests, APIs Exposed |
| `ray-operator/config/manager/service.yaml` | 1-18 | Network Architecture (Services) |
| `ray-operator/config/default/kustomization.yaml` | 1-28 | Deployment Manifests |
| `ray-operator/config/openshift/kustomization.yaml` | 1-60 | Deployment Manifests |
| `ray-operator/config/openshift/params.yaml` | 1-17 | Deployment Manifests |
| `ray-operator/config/openshift/gateway-env-patch.yaml` | 1-29 | Deployment Manifests, Network Architecture |
| `ray-operator/config/openshift/kuberay-operator-image-patch.yaml` | 1-10 | Deployment Manifests |
| `ray-operator/config/openshift/ray_operator_scc.yaml` | 1-19 | Security |
| `ray-operator/config/openshift/webhook-deployment-patch.yaml` | 1-27 | Deployment Manifests, APIs Exposed |
| `ray-operator/config/openshift/webhook.yaml` | 1-48 | APIs Exposed, Network Architecture |
| `ray-operator/config/webhook/manifests.yaml` | 1-93 | APIs Exposed |
| `ray-operator/config/component_metadata.yaml` | 1-4 | Metadata, Provenance |
| `.tekton/odh-kuberay-operator-controller-pull-request.yaml` | 1-30 | Metadata |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `sigs.k8s.io/controller-runtime` | `main.go`, `raycluster_controller.go`, `apis/config`, `apis/ray` | Architecture Components |
| `gateway.networking.k8s.io\|HTTPRoute\|GRPCRoute` | `authentication_controller.go`, `rayservice_controller.go`, `apis/ray/v1/` | Network Architecture (Ingress), Integration Points |
| `kube-rbac-proxy\|oauth-proxy` | `authentication_controller.go`, `raycluster_controller.go`, `config/openshift/` | Security (Auth), Network Architecture |
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` | `Dockerfile.konflux`, `Dockerfile.rhoai`, `Dockerfile` | Security (FIPS) |
| `tls\.Config\|CipherSuites\|MinVersion\|InsecureSkipVerify` | `pkg/tls/tls.go` | Security (FIPS, TLS) |
| `rpms.lock.yaml`, `go.sum`, lock files | `go.sum` (2 files), `yarn.lock`, `poetry.lock` | Build Hermeticity |
| `cachi2\|hermeto\|REMOTE_SOURCES` | (no matches) | Build Hermeticity |

### Summary

- **Total files read**: 45
- **Total lines referenced**: ~12,000+
- **Coverage**: All sections have direct source backing. CRD definitions from `apis/ray/v1/` types, controller behavior from `controllers/ray/` Go source, RBAC from generated `config/rbac/role.yaml`, deployment from `config/openshift/` kustomize overlays, FIPS from Dockerfile build flags and `pkg/tls/tls.go`, provenance from `git remote -v` and `config/component_metadata.yaml`.

---
*Generated in 6m 51s (412s total)*
