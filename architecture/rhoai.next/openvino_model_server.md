# Component: OpenVINO Model Server (OVMS)

## Metadata

- **Repository**: https://github.com/red-hat-data-services/openvino_model_server
- **Version**: aa7ea470 (main)
- **Distribution**: RHOAI
- **Languages**: C++, Python 3.12, Protocol Buffers
- **Deployment Type**: Service (inference server)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/openvinotoolkit/model_server | -- | -- | -- | local_analysis |
| Midstream | https://github.com/opendatahub-io/openvino_model_server | sync_workflow | releases/2026/2 | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/openvino_model_server | manual | main | -- | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: High-performance C++ inference server for serving AI models via OpenAI-compatible, KServe, and TensorFlow Serving APIs using Intel OpenVINO for optimized execution.

**Detailed**: OpenVINO Model Server (OVMS) is a production inference server implemented in C++ for scalability and optimized for deployment on Intel architectures. It provides model serving through multiple API protocols: OpenAI-compatible REST APIs (chat completions, embeddings, reranking, image generation, audio), KServe v2 inference protocol (both gRPC and REST), and TensorFlow Serving prediction API. The server uses Intel OpenVINO as its inference backend, supporting models in OpenVINO IR, ONNX, TensorFlow, PaddlePaddle, and PyTorch formats.

Within the RHOAI platform, OVMS operates as a KServe ServingRuntime, deployed by KServe's InferenceService controller as the inference container within model-serving pods. It supports both single-model serving (via KServe) and multi-model serving (via its own configuration file). The server features continuous batching for LLM workloads, MediaPipe graph-based pipelines for complex inference workflows (DAGs), model hot-reloading, and a C API for embedded use cases. It can pull models from local storage, S3, Google Cloud Storage, Azure Blob Storage, and HuggingFace Hub.

The Konflux build produces a single container image (`odh-openvino-model-server-rhel9`) based on UBI 9 Minimal, compiled from source with Link-Time Optimization (LTO) enabled and GPU driver support (Intel GPU via OpenCL). The build fetches OpenVINO, OpenVINO Tokenizers, and OpenVINO GenAI from forked `opendatahub-io` repositories on release branches.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| ovms | C++ Service (Bazel-built binary) | Main inference server binary serving gRPC and REST APIs |
| libovms_shared.so | C/C++ Shared Library | C API for embedding OVMS in other applications |
| gRPC Frontend | C++ Module (GRPCServerModule) | KServe v2 and TFS gRPC inference endpoints |
| HTTP/REST Frontend | C++ Module (HTTPServerModule, Drogon) | REST API handler for all HTTP endpoints |
| LLM Module | C++ MediaPipe Calculator | OpenAI-compatible chat/completions with continuous batching |
| Embeddings Module | C++ MediaPipe Calculator | OpenAI-compatible text embeddings endpoint |
| Rerank Module | C++ MediaPipe Calculator | Cohere-compatible document reranking endpoint |
| Image Generation Module | C++ MediaPipe Calculator | OpenAI-compatible image generation endpoint |
| Speech-to-Text Module | C++ MediaPipe Calculator | OpenAI-compatible audio transcription endpoint |
| Text-to-Speech Module | C++ MediaPipe Calculator | OpenAI-compatible speech synthesis endpoint |
| Metrics Module | C++ Module (MetricModule) | Prometheus-compatible metrics collection and exposition |
| Filesystem Backends | C++ Classes | Model storage on local, S3, GCS, Azure Blob |
| HuggingFace Pull Module | C++ Module | Download models from HuggingFace Hub via libgit2 |
| DAG Scheduler | C++ Pipeline Engine | Directed Acyclic Graph inference pipelines with custom nodes |
| Python Interpreter Module | C++ Module (pybind11) | Optional Python execution within DAG pipelines |
| MediaPipe Integration | C++ Calculator Framework | Graph-based orchestration for GenAI workloads |

## AIPCC Ecosystems Use

The Konflux Dockerfile (`Dockerfile.konflux`) installs Python packages via pip (numpy, Jinja2, MarkupSafe in the build stage; optimum-intel and nncf in the release stage via `base-requirements.txt`). However, the component does **not** use AIPCC base images.

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| CPU (default) | Dockerfile.konflux | registry.access.redhat.com/ubi9/ubi:9.8 (build) / ubi9/ubi-minimal:9.8 (release) | CPU | N/A | x86_64 | Active |
| GPU (Intel) | Dockerfile.konflux (GPU=1 in build-args.conf) | registry.access.redhat.com/ubi9/ubi:9.8 (build) / ubi9/ubi-minimal:9.8 (release) | Intel GPU (OpenCL) | Driver 25.05.32567 | x86_64 | Active |

The component uses UBI 9 base images directly rather than AIPCC Ecosystems base images. As a C++ binary with Python as an optional runtime dependency (not a primary Python service), migration to AIPCC base images would need to be evaluated for applicability. The Python packages installed are build utilities (Jinja2 for chat templates) and model conversion tools (optimum-intel, nncf), not ML framework dependencies that typically drive AIPCC adoption.

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | No |
| **pip.conf / uv.toml configured** | No |
| **Additional packages installed** | numpy<2.0.0, Jinja2==3.1.6, MarkupSafe==3.0.2 (build); optimum-intel, nncf>=2.11.0 (release) |
| **Source** | Dockerfile.konflux:149, Dockerfile.konflux:450 |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_OVMS does not define CRDs. It is deployed as a container within KServe InferenceService pods via ServingRuntime definitions._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/v1/models/{name}:predict` | POST | 8888/TCP | HTTP | None | API Key (optional) | TFS prediction |
| `/v1/models/{name}:classify` | POST | 8888/TCP | HTTP | None | API Key (optional) | TFS classification |
| `/v1/models/{name}:regress` | POST | 8888/TCP | HTTP | None | API Key (optional) | TFS regression |
| `/v1/models/{name}` | GET | 8888/TCP | HTTP | None | API Key (optional) | TFS model status |
| `/v1/models/{name}/metadata` | GET | 8888/TCP | HTTP | None | API Key (optional) | TFS model metadata |
| `/v1/config/reload` | POST | 8888/TCP | HTTP | None | API Key (optional) | Reload server configuration |
| `/v1/config` | GET | 8888/TCP | HTTP | None | API Key (optional) | Get configuration status |
| `/v2/models/{name}/infer` | POST | 8888/TCP | HTTP | None | API Key (optional) | KServe v2 inference |
| `/v2/models/{name}/ready` | GET | 8888/TCP | HTTP | None | None | KServe v2 model readiness |
| `/v2/models/{name}` | GET | 8888/TCP | HTTP | None | API Key (optional) | KServe v2 model metadata |
| `/v2/health/ready` | GET | 8888/TCP | HTTP | None | None | Server readiness probe |
| `/v2/health/live` | GET | 8888/TCP | HTTP | None | None | Server liveness probe |
| `/v2` | GET | 8888/TCP | HTTP | None | API Key (optional) | KServe v2 server metadata |
| `/v3/chat/completions` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI chat completions |
| `/v3/completions` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI legacy completions |
| `/v3/responses` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI responses API |
| `/v3/v1/chat/completions` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI chat (alternate path) |
| `/v3/v1/completions` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI completions (alternate) |
| `/v3/v1/embeddings` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI embeddings |
| `/v3/v1/images/generations` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI image generation |
| `/v3/v1/audio/transcriptions` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI speech-to-text |
| `/v3/v1/audio/speech` | POST | 8888/TCP | HTTP | None | API Key (optional) | OpenAI text-to-speech |
| `/v3/v1/models` | GET | 8888/TCP | HTTP | None | API Key (optional) | List models (OpenAI) |
| `/v3/v1/models/{name}` | GET | 8888/TCP | HTTP | None | API Key (optional) | Get model details (OpenAI) |
| `/v3/rerank` | POST | 8888/TCP | HTTP | None | API Key (optional) | Cohere reranking |
| `/v3/tokenize` | POST | 8888/TCP | HTTP | None | API Key (optional) | Tokenization |
| `/metrics` | GET | 8888/TCP | HTTP | None | None | Prometheus metrics |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| GRPCInferenceService.ServerLive | 8001/TCP | gRPC | None | None | Server liveness check |
| GRPCInferenceService.ServerReady | 8001/TCP | gRPC | None | None | Server readiness check |
| GRPCInferenceService.ModelReady | 8001/TCP | gRPC | None | None | Model readiness check |
| GRPCInferenceService.ServerMetadata | 8001/TCP | gRPC | None | None | Server metadata |
| GRPCInferenceService.ModelMetadata | 8001/TCP | gRPC | None | None | Model metadata (inputs/outputs) |
| GRPCInferenceService.ModelInfer | 8001/TCP | gRPC | None | None | Unary model inference |
| GRPCInferenceService.ModelStreamInfer | 8001/TCP | gRPC | None | None | Bidirectional streaming inference |
| tensorflow.serving.PredictionService.Predict | 8001/TCP | gRPC | None | None | TFS-compatible prediction |
| tensorflow.serving.PredictionService.GetModelMetadata | 8001/TCP | gRPC | None | None | TFS model metadata |
| tensorflow.serving.ModelService.GetModelStatus | 8001/TCP | gRPC | None | None | TFS model status |

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| OpenVINO Runtime | 2026.2 | Yes | Core inference engine for model execution |
| OpenVINO GenAI | 2026.2 | Yes | LLM, VLM, embeddings, image gen, speech pipelines |
| OpenVINO Tokenizers | 2026.2 | Yes | Tokenization for LLM serving |
| Bazel | 6.1.1 | Yes (build) | Build system |
| gRPC C++ | -- | Yes | gRPC server framework |
| Drogon | -- | Yes | HTTP/REST server framework |
| Prometheus C++ Client | -- | Yes | Metrics exposition |
| MediaPipe | -- | Yes | Graph-based inference orchestration |
| OpenCV | 4.13.0 | Yes | Image preprocessing |
| Azure Storage SDK | 7.5.0 | Yes | Azure Blob model storage |
| AWS SDK (S3) | -- | Yes | S3 model storage |
| GCS SDK | -- | Yes | Google Cloud Storage model storage |
| libgit2 | -- | Yes | HuggingFace Hub model pulling |
| Boost | 1.68.0 | Yes | Networking, filesystem, threading |
| RapidJSON | -- | Yes | JSON parsing |
| pugixml | 1.13 | Yes | XML parsing |
| cxxopts | -- | Yes | Command-line argument parsing |
| spdlog | -- | Yes | Logging framework |
| Protocol Buffers 3 | -- | Yes | API serialization |
| cURL | 8.20.0 | Yes | HTTP client for model downloads |
| OpenSSL | -- | Yes (via UBI) | Crypto (MD5 checksums for model caching) |
| Python 3.12 | 3.12 | Optional | Python DAG nodes, Jinja2 chat templates |
| numpy | <2.0.0 | Optional | Python numeric support |
| Jinja2 | 3.1.6 | Optional | Chat template rendering |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| KServe | ServingRuntime CRD | OVMS is deployed as a KServe ServingRuntime for model serving |
| kube-rbac-proxy | Sidecar (RHOAI 3.x) | Auth enforcement in front of OVMS when deployed via RHOAI |
| OpenShift AI Dashboard | UI integration | ServingRuntime registered for user selection |
| S3-compatible storage | Egress (data plane) | Loading models from object storage |
| HuggingFace Hub | Egress (data plane) | Downloading models via git clone or API |

## Deployment Manifests

_OVMS does not ship kustomize manifests within the repo. It is deployed via KServe ServingRuntime definitions._

### KServe ServingRuntime Definitions

| Manifest | Path | Purpose |
|----------|------|---------|
| KServe ClusterServingRuntime | extras/kserve/kserve-openvino.yaml | Upstream KServe integration with KServe v2 + gRPC-v2 protocols |
| RHOAI ServingRuntime | extras/openshift_AI/ServingRuntime.yaml | RHOAI-specific runtime with dashboard integration |

### RHOAI ServingRuntime Configuration

| Parameter | Value | Purpose |
|-----------|-------|---------|
| gRPC port | 8001 | gRPC inference endpoint |
| REST port | 8888 | REST inference endpoint |
| Metrics path | /metrics | Prometheus scraping |
| Metrics port | 8888 | Prometheus scraping port |
| Health probe | /v2/health/ready | Startup/readiness check |
| Supported formats | openvino_ir, onnx, tensorflow, paddle, pytorch, vLLM | Model format support |
| Runtime version | 2026.2 | OpenVINO version label |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| gRPC inference | ClusterIP (via KServe) | 8001/TCP | 8001 | gRPC | None (plaintext) | None (delegated to kube-rbac-proxy) | Internal |
| REST inference | ClusterIP (via KServe) | 8888/TCP | 8888 | HTTP | None (plaintext) | API Key (optional) | Internal |
| Prometheus metrics | ClusterIP (via KServe) | 8888/TCP | 8888 | HTTP | None | None | Internal |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

_OVMS does not manage its own ingress. In RHOAI, the platform operator (rhods-operator) creates HTTPRoute or Route resources pointing to the KServe-managed Service for the InferenceService._

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| S3-compatible storage | 443/TCP | HTTPS | TLS 1.2+ | AWS IAM credentials / access key | Load models from S3 buckets |
| Google Cloud Storage | 443/TCP | HTTPS | TLS 1.2+ | GCP service account credentials | Load models from GCS buckets |
| Azure Blob Storage | 443/TCP | HTTPS | TLS 1.2+ | Azure storage key / SAS token | Load models from Azure containers |
| HuggingFace Hub (github.com) | 443/TCP | HTTPS | TLS 1.2+ | HF_TOKEN (optional) | Clone models from HF repositories |
| Image URLs (configurable domains) | 443/TCP | HTTPS | TLS 1.2+ | None | Download images for VLM inference |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_OVMS does not define RBAC resources. It runs as a non-root user (UID 5000) within KServe-managed pods. RBAC is managed by KServe and the RHOAI platform operator._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|

_OVMS accesses cloud storage credentials via environment variables (AWS_ACCESS_KEY_ID, AZURE_STORAGE_CONNECTION_STRING, etc.) injected by KServe from Kubernetes Secrets. It does not define or manage Secrets directly._

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| All REST endpoints | GET, POST | API Key (Bearer token via Authorization header) | OVMS application code (http_rest_api_handler.cpp) | Optional; configured via --api_key flag |
| All REST endpoints | GET, POST | kube-rbac-proxy | Sidecar container (RHOAI 3.x deployment) | Platform-managed RBAC |
| /v2/health/ready | GET | None | N/A | Always unauthenticated (health check) |
| /v2/health/live | GET | None | N/A | Always unauthenticated (health check) |
| /metrics | GET | None | N/A | Unauthenticated metrics scraping |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A -- C++ binary, not Go. No GOEXPERIMENT or CGO flags. | Dockerfile.konflux (no FIPS flags present) |
| **Linking** | Dynamic (C++ with shared libraries: libopenvino, libovms_shared.so, etc.) | Dockerfile.konflux:314 (bazel build), create_package.sh |
| **OpenSSL in image** | Yes (via UBI 9 base; openssl-devel installed in build stage) | Dockerfile.konflux:46 |
| **OLM FIPS annotation** | Not applicable (not an OLM operator) | N/A |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No TLS termination -- gRPC uses InsecureServerCredentials(); HTTP via Drogon with no SSL config | grpcservermodule.cpp, http_server.cpp |
| **Crypto libraries** | OpenSSL MD5 for model file checksums; OpenSSL linked via UBI system libraries | src/filesystem/filesystem.hpp:29 |
| **Certificate handling** | libgit2 uses system trust store or GIT_SSL_CERT_LOCATIONS env var for HF Hub | src/pull_module/libgit2.cpp, src/pull_module/hf_pull_model_module.cpp |
| **Non-FIPS crypto risks** | Uses OpenSSL MD5 (via `openssl/md5.h`) for model file checksums -- MD5 is not FIPS-approved for integrity but is acceptable for non-security cache key generation | src/filesystem/filesystem.hpp:29 |

_OVMS is a C++ binary, not Go. check-payload primarily validates Go binaries. The C++ binary dynamically links against system OpenSSL from UBI 9, inheriting the platform's FIPS-validated crypto module. The server does not terminate TLS itself -- TLS is handled by the platform's ingress layer (Envoy/Gateway, kube-rbac-proxy sidecar, or OpenShift Route). The MD5 usage is for model file caching/identification, not for security-sensitive operations._

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (expected on downstream release branches) |
| **Language deps (Bazel)** | WORKSPACE (pinned URLs + hashes) | Yes | Bazel | WORKSPACE |
| **Language deps (JS)** | yarn.lock | Yes | yarn | yarn.lock |
| **Language deps (Python)** | not found | No | pip | N/A |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | not present | No | Hermeto (formerly cachi2) | N/A |

_The Bazel build system pins external dependencies via the WORKSPACE file with explicit URLs and SHA256 hashes, providing hermeticity at the C++ dependency layer. The Konflux build sets `KONFLUX=1` to skip Bazel's network fetch step (line 283), relying on pre-fetched sources. Python packages (numpy, Jinja2, MarkupSafe) are installed via pip without a lock file. The `main` branch does not have RPM lock files; downstream release branches (`rhoai-*`) likely add them via Konflux's build hardening._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-pod (single InferenceService per OVMS instance) | KServe ServingRuntime deployment model |
| **Deployment model** | single-tenant per pod | extras/openshift_AI/ServingRuntime.yaml:36 (multiModel: false) |
| **Tenant identifier** | N/A (tenancy delegated to Kubernetes namespace + KServe InferenceService) | Inferred from deployment model |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | kube-rbac-proxy sidecar (RHOAI 3.x) or API key (optional) | Platform / Application | API key is a shared secret, not per-user auth; in-pod auth delegated to sidecar |
| Data storage | Per-pod model storage; cloud storage credentials scoped by KServe Secret | Kubernetes / KServe | Multiple InferenceServices in the same namespace may share storage credentials |
| Network traffic | Kubernetes NetworkPolicy (if configured by platform operator) | Kubernetes / Platform operator | OVMS itself does not create NetworkPolicies |
| Compute & resources | Resource limits on container (CPU/memory via ServingRuntime) | Kubernetes | No internal request-level resource isolation between concurrent inference requests |
| Configuration & secrets | Per-pod environment variables for cloud storage access | Kubernetes | N/A |
| API scoping | Single model (or multi-model config) per pod instance | Application | Multi-model mode serves all models on same port without per-model auth |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|

_No shared services. Each OVMS instance runs as an isolated pod serving one InferenceService._

## Data Flows

### Flow 1: Model Inference (REST)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | Platform ingress (Gateway/Route) | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (OpenShift OAuth) |
| 2 | Platform ingress | kube-rbac-proxy sidecar | 8443/TCP | HTTPS | TLS | Kubernetes RBAC |
| 3 | kube-rbac-proxy | OVMS REST endpoint | 8888/TCP | HTTP | plaintext (localhost) | None |
| 4 | OVMS | OpenVINO Runtime | in-process | N/A | N/A | N/A |
| 5 | OVMS | Cloud storage (S3/GCS/Azure) | 443/TCP | HTTPS | TLS 1.2+ | Cloud credentials |

### Flow 2: Model Inference (gRPC)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | OVMS gRPC endpoint | 8001/TCP | gRPC | None (plaintext) | None |
| 2 | OVMS | OpenVINO Runtime | in-process | N/A | N/A | N/A |

### Flow 3: LLM Chat Completion (OpenAI API)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | OVMS REST /v3/chat/completions | 8888/TCP | HTTP | None | API Key (optional) |
| 2 | OVMS HttpLLMCalculator | GenAiServable (MediaPipe graph) | in-process | N/A | N/A | N/A |
| 3 | GenAiServable | OpenVINO GenAI ContinuousBatchingPipeline | in-process | N/A | N/A | N/A |
| 4 | OVMS | Client (SSE stream) | 8888/TCP | HTTP (chunked/SSE) | None | N/A |

### Flow 4: HuggingFace Model Pull

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | OVMS HfPullModelModule | github.com (HuggingFace Hub) | 443/TCP | HTTPS | TLS 1.2+ | HF_TOKEN (optional) |
| 2 | OVMS | Local filesystem (/mnt/models) | N/A | filesystem | N/A | N/A |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| KServe InferenceService | ServingRuntime container | 8001/TCP, 8888/TCP | gRPC, HTTP | None | OVMS is the inference container in KServe pods |
| kube-rbac-proxy | Sidecar proxy | 8443/TCP → 8888/TCP | HTTPS → HTTP | TLS (sidecar) | Auth enforcement in RHOAI 3.x |
| Prometheus | Scrape target | 8888/TCP | HTTP | None | Metrics collection at /metrics |
| S3-compatible storage | REST client | 443/TCP | HTTPS | TLS 1.2+ | Model loading from S3 buckets |
| Google Cloud Storage | REST client | 443/TCP | HTTPS | TLS 1.2+ | Model loading from GCS |
| Azure Blob Storage | REST client | 443/TCP | HTTPS | TLS 1.2+ | Model loading from Azure |
| HuggingFace Hub | Git clone (libgit2) | 443/TCP | HTTPS | TLS 1.2+ | Model downloading |
| OpenVINO Runtime | In-process library | N/A | N/A | N/A | Model compilation and inference execution |
| OpenVINO GenAI | In-process library | N/A | N/A | N/A | LLM/VLM/Embeddings/ImageGen/Audio pipelines |
| OpenVINO Tokenizers | In-process library | N/A | N/A | N/A | Text tokenization for LLM serving |
| MediaPipe | In-process framework | N/A | N/A | N/A | Graph-based inference orchestration |
| OpenShift AI Dashboard | UI metadata | N/A | N/A | N/A | ServingRuntime listed for user selection via labels |

## Architectural Analysis

OVMS is a monolithic C++ server with a modular internal architecture organized around a `Module` base class and a `Server` singleton that manages module lifecycle. The startup sequence is carefully ordered: the Metrics module initializes first to enable metric collection from the start, followed by the optional Python interpreter, then the Servable Manager (which loads models), the C API module, and finally the gRPC and HTTP server modules. This ordering ensures health probes respond correctly only after all infrastructure is ready. The server supports graceful shutdown with a 5-second deadline, using signal handlers for SIGINT/SIGTERM.

The GenAI serving stack (LLM, embeddings, reranking, image generation, audio) is built on MediaPipe's calculator framework rather than the traditional DAG scheduler. Each GenAI capability is implemented as a MediaPipe calculator (`HttpLLMCalculator`, `EmbeddingsCalculatorOv`, `RerankCalculatorOv`, etc.) configured via Protocol Buffer definitions. This allows complex inference graphs to be composed from reusable calculator nodes. The LLM module supports two execution modes: a legacy single-sequence pipeline (`LLMPipeline`) and a high-throughput continuous batching pipeline (`ContinuousBatchingPipeline`) from OpenVINO GenAI. The continuous batching mode is the production path for LLM serving, supporting multiple concurrent sequences with configurable KV cache, prefix caching, and speculative decoding.

The server exposes three API families on two ports: OpenAI-compatible REST endpoints under `/v3/`, KServe v2 endpoints under `/v2/`, and TensorFlow Serving endpoints under `/v1/`. The HTTP server uses Drogon (a high-performance C++ web framework) with configurable worker threads scaled to CPU cores and file descriptor limits. The gRPC server supports multiple instances via the `grpc_workers` setting, each with independent thread pools and resource quotas. Notably, neither the gRPC nor HTTP server implements TLS termination -- both use `InsecureServerCredentials` and plaintext HTTP respectively. TLS is expected to be handled by the platform's ingress layer (OpenShift Routes, Gateway API with Envoy, or kube-rbac-proxy sidecars).

The security model relies almost entirely on the deployment platform. OVMS itself provides only optional API key authentication (a single shared key, not per-user). In RHOAI deployments, the platform operator injects kube-rbac-proxy sidecars for proper authentication and authorization. The container runs as non-root user `ovms` (UID 5000) with restricted security context (no privilege escalation, all capabilities dropped). For image/media loading in VLM workloads, the server implements domain whitelisting (`allowed_media_domains`) and local path restrictions (`allowed_local_media_path`) to prevent SSRF and path traversal attacks.

The model storage abstraction supports four backends (local filesystem, S3, GCS, Azure Blob) with a common `FileSystem` interface. Model repositories can be polled for changes with configurable intervals (`file_system_poll_wait_seconds`), enabling model hot-reloading without server restart. The HuggingFace Pull module adds the ability to download models directly from HuggingFace Hub using libgit2, with support for authentication tokens and proxy configuration.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 2026.2 | 2026-07 | Sync stable to RHOAI for 2026.2 release; updated base images to UBI 9.8; GPU driver update to 25.05.32567 |
| 2026.2 | 2026-07 | Added symlink for libopenvino_genai.so.2612; updated dependent repo sources |
| 2025.4 | 2026-01 | Overlay of 2025.4 release; pinned tokenizer and genai; Konflux build fixes |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| README.md | 1-89 | Purpose, Metadata |
| Dockerfile.konflux | 1-467 | Architecture Components, AIPCC Ecosystems Use, Dependencies, Security (FIPS, Build Hermeticity) |
| .konflux/build-args.conf | 1-36 | Architecture Components, Dependencies, Deployment Manifests |
| versions.mk | 1-39 | Dependencies (External), Metadata (version) |
| src/main.cpp | 1-33 | Architecture Components (entry point) |
| src/server.hpp | 1-64 | Architecture Components (module system) |
| src/config.cpp | 1-100 | Network Architecture (port defaults, thread pools) |
| src/cli_parser.hpp | 1-57 | Architecture Components (CLI parsing, graph modes) |
| src/capi_frontend/server_settings.hpp | (via sub-agent) | Network Architecture, Authentication, Configuration |
| src/http_frontend/http_rest_api_handler.cpp | (via sub-agent) | APIs Exposed (HTTP endpoints), Authentication |
| src/kfserving_api/grpc_predict_v2.proto | (via sub-agent) | APIs Exposed (gRPC services), Data Flows |
| src/kfs_frontend/kfs_grpc_inference_service.cpp | (via sub-agent) | APIs Exposed (gRPC), Health checks |
| src/grpcservermodule.cpp | (via sub-agent) | Network Architecture, Security (FIPS -- InsecureServerCredentials) |
| src/metrics/metric_registry.hpp | 1-55 | Architecture Components (Prometheus metrics) |
| src/llm/llm_calculator.proto | (via sub-agent) | APIs Exposed (LLM configuration), Architecture Components |
| src/llm/servable.cpp | (via sub-agent) | APIs Exposed (endpoint routing) |
| src/llm/apis/openai_completions.hpp | (via sub-agent) | APIs Exposed (OpenAI-compatible parameters) |
| src/llm/apis/openai_api_handler.cpp | (via sub-agent) | Security (media domain whitelisting) |
| src/llm/language_model/continuous_batching/servable.hpp | (via sub-agent) | Architecture Components (continuous batching) |
| src/llm/language_model/legacy/servable.hpp | (via sub-agent) | Architecture Components (legacy pipeline) |
| src/embeddings/embeddings_calculator_ov.proto | (via sub-agent) | APIs Exposed (embeddings configuration) |
| src/rerank/rerank_calculator_ov.proto | (via sub-agent) | APIs Exposed (rerank configuration) |
| src/image_gen/image_gen_calculator.proto | (via sub-agent) | APIs Exposed (image generation configuration) |
| src/audio/speech_to_text/s2t_calculator.proto | (via sub-agent) | APIs Exposed (speech-to-text) |
| src/audio/text_to_speech/t2s_calculator.proto | (via sub-agent) | APIs Exposed (text-to-speech) |
| src/filesystem/filesystem.hpp | 1-50 | Security (FIPS -- OpenSSL MD5 usage), Dependencies |
| src/pull_module/libgit2.cpp | (via sub-agent) | Security (certificate handling), Egress |
| src/pull_module/hf_pull_model_module.cpp | (via sub-agent) | Integration Points (HuggingFace), Egress |
| src/network_utils.cpp | (via sub-agent) | Network Architecture (IPv6 support) |
| extras/kserve/kserve-openvino.yaml | 1-63 | Deployment Manifests, Integration Points |
| extras/openshift_AI/ServingRuntime.yaml | 1-61 | Deployment Manifests, Multi-Tenancy, Integration Points |
| demos/python_demos/base-requirements.txt | 1-3 | AIPCC Ecosystems Use, Dependencies |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `Dockerfile*konflux*` (find) | Dockerfile.konflux | Architecture Components, AIPCC Ecosystems Use |
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` | (none) | Security (FIPS -- absence documented) |
| `fips-compliant\|fips\.enabled\|FIPS` | (none) | Security (FIPS -- no annotations) |
| `boring\|boringcrypto\|openssl\|crypto/tls` | src/filesystem/filesystem.hpp, src/pull_module/libgit2.cpp | Security (FIPS crypto) |
| `pycryptodome\|pycrypto\|hashlib\.md5\|cryptography` | (none) | Security (FIPS -- no Python crypto) |
| `rpms.lock.yaml`, `go.sum`, lock files (find) | yarn.lock | Security (Build Hermeticity) |
| `cachi2\|hermeto\|REMOTE_SOURCES` | (none) | Security (Build Hermeticity) |
| `pip install\|pip3 install` | Dockerfile.konflux (3 matches) | AIPCC Ecosystems Use |
| `openssl` (in .cpp/.hpp) | src/filesystem/filesystem.hpp | Security (crypto analysis) |

### Summary

- **Total files read**: 34
- **Total lines referenced**: ~2,500+
- **Coverage**: All sections have direct source backing. Provenance lineage uses `local_analysis` (no component-map.json available). Multi-Tenancy model is inferred from KServe deployment model and ServingRuntime configuration. Ingress is inferred from RHOAI platform architecture (OVMS does not manage its own ingress).

---
*Generated in 7m 5s (425s total)*
