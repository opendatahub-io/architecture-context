# Component: ModelMesh Runtime Adapter

## Metadata

- **Repository**: https://github.com/red-hat-data-services/modelmesh-runtime-adapter
- **Version**: v1.20.0-11-rhods-578-gc57fd87 (branch: main)
- **Distribution**: RHOAI
- **Languages**: Go 1.23, Python 3.11
- **Deployment Type**: Sidecar container (multi-binary image)
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/kserve/modelmesh-runtime-adapter | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/modelmesh-runtime-adapter | manual | main | -- | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Sidecar container that bridges ModelMesh model lifecycle management with third-party ML inference runtime servers (Triton, MLServer, OVMS, TorchServe) and pulls model artifacts from cloud storage.

**Detailed**: ModelMesh Runtime Adapter is a multi-purpose sidecar image that runs inside every ModelMesh Serving model server pod. It contains five Go binaries — a model-serving puller and four runtime-specific adapters — all compiled into a single container image with no defined entrypoint; the consuming pod spec selects which binary to run.

The **puller** component implements the ModelRuntime gRPC interface on port 8084, intercepts model load/unload requests from ModelMesh, downloads model artifacts from cloud storage backends (S3, GCS, Azure Blob, HTTP, PVC) into a local directory, and then proxies the request to the downstream runtime adapter on port 8085. The **PullMan** library provides a plugin-based storage abstraction with client caching, credential management via mounted Kubernetes Secrets, and support for mTLS.

The four **runtime adapters** (Triton, MLServer, OVMS, TorchServe) each implement the same ModelRuntime gRPC interface on port 8085, adapting model layouts (symlinks, config files, directory structures) to each runtime's expected format before instructing the runtime to load models via its native API (gRPC for Triton/MLServer/TorchServe, REST for OVMS). The image also includes Python (3.11) and TensorFlow for Keras-to-SavedModel conversion used by the Triton adapter.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| model-serving-puller | Go Service (sidecar) | Downloads model artifacts from cloud storage and proxies ModelRuntime gRPC calls to downstream adapter |
| model-mesh-triton-adapter | Go Service (sidecar) | Adapts model layouts and manages model lifecycle on NVIDIA Triton Inference Server via gRPC |
| model-mesh-mlserver-adapter | Go Service (sidecar) | Adapts model layouts and manages model lifecycle on Seldon MLServer via gRPC |
| model-mesh-ovms-adapter | Go Service (sidecar) | Adapts model layouts and manages model lifecycle on OpenVINO Model Server via REST HTTP |
| model-mesh-torchserve-adapter | Go Service (sidecar) | Adapts model layouts and manages model lifecycle on PyTorch TorchServe via gRPC management API |
| pullman | Go Library | Plugin-based storage provider abstraction with client caching for S3, GCS, Azure, HTTP, PVC |
| tf_pb.py | Python Script | Converts Keras H5 models to TensorFlow SavedModel format (used by Triton adapter) |

## AIPCC Ecosystems Use

The Konflux Dockerfile installs Python packages via `pip install -r requirements.txt` (line 65-66) using a `ubi9/ubi-minimal` base image. It does **not** use AIPCC base images.

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| N/A | Dockerfile.konflux | registry.access.redhat.com/ubi9/ubi-minimal@sha256:463c... | N/A | N/A | x86_64, aarch64 | N/A |

The component uses a standard UBI 9 minimal base image and does not use AIPCC Ecosystems base images. The Python packages installed (TensorFlow, Keras, numpy, etc.) are for Keras-to-TF model conversion, not for inference. This component must be evaluated for migration to AIPCC base images to comply with Red Hat's product security requirements for secure Python package builds, though the sidecar nature and limited Python usage (conversion utility only) may justify the current approach.

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | No |
| **pip.conf / uv.toml configured** | No — uses default PyPI |
| **Additional packages installed** | tensorflow==2.19.0, keras==3.11.3, numpy==2.1.3, h5py==3.12.0, grpcio==1.73.0, protobuf==5.29.5, and 25+ transitive deps |
| **Source** | Dockerfile.konflux:63-66 |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|
| N/A | Component does not use AIPCC tooling | -- |

## Sub-Component Details

### model-serving-puller

**Dockerfile**: `Dockerfile.konflux` (shared multi-binary image)
**Intent**: Sidecar process that intercepts ModelRuntime gRPC calls from ModelMesh, downloads model artifacts from cloud storage to local filesystem, then proxies the request to the downstream runtime adapter. Always deployed alongside one of the four runtime adapters.
**Image**: odh-modelmesh-runtime-adapter
**Language**: Go
**Port**: 8084/TCP (gRPC)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Downstream runtime adapter | gRPC | 8085/TCP | None (loopback) | Proxy model load/unload/status after pulling artifacts |
| S3-compatible storage | HTTPS | 443/TCP | Access Key + Secret Key | Download model artifacts |
| Google Cloud Storage | HTTPS | 443/TCP | Service Account (private key + client email) | Download model artifacts |
| Azure Blob Storage | HTTPS | 443/TCP | Connection String or Service Principal | Download model artifacts |
| HTTP(S) model server | HTTP/HTTPS | configurable | Optional mTLS, custom headers | Download model artifacts |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| PORT | env | 8084 | gRPC server listen port |
| MODEL_SERVER_ENDPOINT | env | port:8085 | Downstream runtime adapter endpoint |
| LOG_LEVEL | env | debug | Logging verbosity |
| ROOT_MODEL_DIR | env | /models | Local directory for downloaded models |
| STORAGE_CONFIG_DIR | env | /storage-config | Directory with mounted storage credential secrets |

### model-mesh-triton-adapter

**Dockerfile**: `Dockerfile.konflux` (shared multi-binary image)
**Intent**: Runtime adapter for NVIDIA Triton Inference Server. Transforms model artifacts into Triton-expected directory layout, generates `config.pbtxt` from schema, and manages model lifecycle via Triton's gRPC repository API. Supports Keras-to-TF conversion using Python.
**Image**: odh-modelmesh-runtime-adapter
**Language**: Go
**Port**: 8085/TCP (gRPC)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Triton Inference Server | gRPC | 8001/TCP | None (loopback) | RepositoryModelLoad, RepositoryModelUnload, ServerReady, RepositoryIndex, ServerMetadata |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| ADAPTER_PORT | env | 8085 | gRPC server listen port |
| RUNTIME_PORT | env | 8001 | Triton gRPC port |
| CONTAINER_MEM_REQ_BYTES | env | -1 (required) | Total container memory allocation |
| MEM_BUFFER_BYTES | env | 268435456 (256 MiB) | Memory overhead buffer |
| LOADING_CONCURRENCY | env | 1 | Max concurrent model loads |
| LOADTIME_TIMEOUT | env | 30000 | Model loading timeout (ms) |
| DEFAULT_MODELSIZE | env | 1000000 | Default model size estimate (bytes) |
| MODELSIZE_MULTIPLIER | env | 1.25 | Size calculation multiplier |
| MAX_CONC_KERAS_CONV_PROCS | env | 2 | Max concurrent Keras→TF conversion processes |
| USE_EMBEDDED_PULLER | env | false | Enable embedded storage puller |
| ROOT_MODEL_DIR | env | /models | Root directory for adapted models |

### model-mesh-mlserver-adapter

**Dockerfile**: `Dockerfile.konflux` (shared multi-binary image)
**Intent**: Runtime adapter for Seldon MLServer. Creates model directory layout with `model-settings.json`, maps model types to MLServer implementation classes (LightGBM, sklearn, XGBoost, MLlib), and manages lifecycle via MLServer's gRPC repository API.
**Image**: odh-modelmesh-runtime-adapter
**Language**: Go
**Port**: 8085/TCP (gRPC)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| MLServer | gRPC | 8001/TCP | None (loopback) | RepositoryModelLoad, RepositoryModelUnload, ServerReady, RepositoryIndex, ServerMetadata |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| ADAPTER_PORT | env | 8085 | gRPC server listen port |
| RUNTIME_PORT | env | 8001 | MLServer gRPC port |
| CONTAINER_MEM_REQ_BYTES | env | -1 (required) | Total container memory allocation |
| MEM_BUFFER_BYTES | env | 268435456 (256 MiB) | Memory overhead buffer |
| LOADING_CONCURRENCY | env | 1 | Max concurrent model loads |
| LOADTIME_TIMEOUT | env | 30000 | Model loading timeout (ms) |
| DEFAULT_MODELSIZE | env | 1000000 | Default model size estimate (bytes) |
| MODELSIZE_MULTIPLIER | env | 1.25 | Size calculation multiplier |
| USE_EMBEDDED_PULLER | env | false | Enable embedded storage puller |
| ROOT_MODEL_DIR | env | /models | Root directory for adapted models |

### model-mesh-ovms-adapter

**Dockerfile**: `Dockerfile.konflux` (shared multi-binary image)
**Intent**: Runtime adapter for Intel OpenVINO Model Server (OVMS). Uses an actor-based batching pattern to aggregate model load/unload operations into batched config file updates, communicating with OVMS via its REST HTTP API rather than gRPC.
**Image**: odh-modelmesh-runtime-adapter
**Language**: Go
**Port**: 8085/TCP (gRPC)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| OpenVINO Model Server | HTTP | 8001/TCP | None (loopback) | GET /v1/config, POST /v1/config/reload |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| ADAPTER_PORT | env | 8085 | gRPC server listen port |
| RUNTIME_PORT | env | 8001 | OVMS HTTP port |
| MODEL_CONFIG_FILE | env | /models/model_config_list.json | OVMS multi-model config file path |
| BATCH_WAIT_TIME_MIN | env | 100ms | Minimum batch wait time for model operations |
| BATCH_WAIT_TIME_MAX | env | 3s | Maximum batch wait time for model operations |
| OVMS_RELOAD_TIMEOUT | env | 30s | Model config reload timeout |
| OVMS_FORCE_TARGET_DEVICE | env | CPU | Force model target device (CPU/GPU) |
| CONTAINER_MEM_REQ_BYTES | env | -1 (required) | Total container memory allocation |
| MEM_BUFFER_BYTES | env | 268435456 (256 MiB) | Memory overhead buffer |
| USE_EMBEDDED_PULLER | env | false | Enable embedded storage puller |
| ROOT_MODEL_DIR | env | /models | Root directory for adapted models |

### model-mesh-torchserve-adapter

**Dockerfile**: `Dockerfile.konflux` (shared multi-binary image)
**Intent**: Runtime adapter for PyTorch TorchServe. Manages MAR (Model Archive) files via symlinks in the model store, communicates with TorchServe via its gRPC management API for model registration/unregistration, and supports batch inference configuration.
**Image**: odh-modelmesh-runtime-adapter
**Language**: Go
**Port**: 8085/TCP (gRPC)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| TorchServe (management) | gRPC | 7071/TCP | None (loopback) | RegisterModel, UnregisterModel, DescribeModel, ListModels |
| TorchServe (inference) | gRPC | 7070/TCP | None (loopback) | Ping health check, inference readiness verification |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| ADAPTER_PORT | env | 8085 | gRPC server listen port |
| RUNTIME_PORT | env | 7071 | TorchServe management gRPC port |
| RUNTIME_DATA_ENDPOINT | env | port:7070 | TorchServe inference gRPC endpoint |
| REQUEST_BATCH_SIZE | env | 0 | Batch size for model inference (0 = disabled) |
| MAX_BATCH_DELAY_SECS | env | 0 | Maximum batch delay in seconds |
| CONTAINER_MEM_REQ_BYTES | env | -1 (required) | Total container memory allocation |
| MEM_BUFFER_BYTES | env | 268435456 (256 MiB) | Memory overhead buffer |
| MODELSIZE_MULTIPLIER | env | 2.75 | Size calculation multiplier (higher than other adapters due to TorchServe memory overhead) |
| USE_EMBEDDED_PULLER | env | false | Enable embedded storage puller |
| ROOT_MODEL_DIR | env | /models | Root directory for adapted models |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| mmesh.ModelRuntime (puller) | 8084/TCP | gRPC | plaintext | None | LoadModel, UnloadModel, PredictModelSize, ModelSize, RuntimeStatus — proxies to downstream adapter after pulling model artifacts |
| mmesh.ModelRuntime (adapter) | 8085/TCP | gRPC | plaintext | None | LoadModel, UnloadModel, RuntimeStatus — adapts model layout and manages lifecycle on target runtime |

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| cloud.google.com/go/storage | v1.28.1 | No | GCS model storage provider |
| github.com/Azure/azure-sdk-for-go/sdk/azidentity | v0.13.2 | No | Azure Blob Storage authentication |
| github.com/Azure/azure-sdk-for-go/sdk/storage/azblob | v0.3.0 | No | Azure Blob Storage model downloads |
| github.com/IBM/ibm-cos-sdk-go | v1.9.1 | No | S3-compatible model storage provider |
| google.golang.org/grpc | v1.56.3 | Yes | gRPC server and client framework |
| google.golang.org/protobuf | v1.33.0 | Yes | Protocol Buffer serialization |
| sigs.k8s.io/controller-runtime | v0.14.6 | Yes | Logging only (not used as operator framework) |
| tensorflow | 2.19.0 | No | Keras H5 to TF SavedModel conversion (Python) |
| keras | 3.11.3 | No | Keras model loading for conversion (Python) |
| github.com/cyphar/filepath-securejoin | v0.2.4 | Yes | Path traversal prevention |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| ModelMesh | gRPC client (inbound) | Sends LoadModel/UnloadModel/RuntimeStatus RPCs to the puller on port 8084 |
| modelmesh-serving (modelmesh-controller) | ConfigMap | Configures the runtime adapter image via `model-serving-config` ConfigMap |
| Kubernetes Secrets | Volume mount | Provides storage credentials at /storage-config |
| Triton Inference Server | gRPC (localhost) | Target runtime for triton-adapter |
| MLServer (Seldon) | gRPC (localhost) | Target runtime for mlserver-adapter |
| OpenVINO Model Server | REST HTTP (localhost) | Target runtime for ovms-adapter |
| TorchServe | gRPC (localhost) | Target runtime for torchserve-adapter |

## Deployment Manifests

_This component has no kustomize manifests. It is deployed as a sidecar container image referenced by the ModelMesh controller via the `model-serving-config` ConfigMap (`storageHelperImage` field)._

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| puller gRPC | N/A (localhost) | 8084/TCP | 8084 | gRPC | plaintext | None | Pod-internal (loopback) |
| adapter gRPC | N/A (localhost) | 8085/TCP | 8085 | gRPC | plaintext | None | Pod-internal (loopback) |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| S3-compatible storage endpoints | 443/TCP | HTTPS | TLS 1.2+ | Access Key + Secret Key | Download model artifacts from S3/MinIO/IBM COS |
| storage.googleapis.com | 443/TCP | HTTPS | TLS 1.2+ | Service Account credentials (private key + client email) | Download model artifacts from GCS |
| *.blob.core.windows.net | 443/TCP | HTTPS | TLS 1.2+ | Connection String or Service Principal (client_id + client_secret + tenant_id) | Download model artifacts from Azure Blob |
| Configurable HTTP(S) URL | configurable | HTTP/HTTPS | Optional TLS, optional mTLS | Optional custom headers, optional client certificate | Download model artifacts from HTTP sources |
| Triton Inference Server | 8001/TCP | gRPC | plaintext | None | Model lifecycle management (RepositoryModelLoad/Unload) |
| MLServer | 8001/TCP | gRPC | plaintext | None | Model lifecycle management (RepositoryModelLoad/Unload) |
| OpenVINO Model Server | 8001/TCP | HTTP | plaintext | None | Model config reload and status polling (/v1/config, /v1/config/reload) |
| TorchServe (management) | 7071/TCP | gRPC | plaintext | None | Model registration/unregistration (RegisterModel/UnregisterModel) |
| TorchServe (inference) | 7070/TCP | gRPC | plaintext | None | Health check (Ping) and inference readiness |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| Storage config secrets (mounted at /storage-config) | Opaque | Cloud storage credentials (S3 access keys, GCS service accounts, Azure connection strings, HTTP client certs) | User / modelmesh-serving controller | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| puller gRPC (8084/TCP) | LoadModel, UnloadModel, PredictModelSize, ModelSize, RuntimeStatus | None (plaintext loopback) | Pod-level network isolation | Accessible only within the pod |
| adapter gRPC (8085/TCP) | LoadModel, UnloadModel, RuntimeStatus | None (plaintext loopback) | Pod-level network isolation | Accessible only within the pod |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime | Dockerfile.konflux:29-35 |
| **Linking** | Dynamic (CGO_ENABLED=1) | Dockerfile.konflux:29 |
| **OpenSSL in image** | Yes (via UBI 9 minimal base image) | Dockerfile.konflux:42 |
| **OLM FIPS annotation** | N/A (not an operator — no CSV) | -- |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No custom TLS config; all inter-process gRPC uses insecure credentials (loopback only) | model-serving-puller/server/server.go:85 |
| **Crypto libraries** | stdlib crypto/tls (FIPS via OpenSSL at build time); S3/GCS/Azure SDKs inherit Go TLS defaults | go.mod:54 (golang.org/x/crypto v0.31.0) |
| **Certificate handling** | Optional custom CA cert for S3, optional CA cert + client cert/key for HTTP provider; GCS and Azure use SDK defaults with system trust store | pullman/storageproviders/s3/downloader.go:50-56, pullman/storageproviders/http/provider.go:72-110 |
| **Non-FIPS crypto risks** | None detected — no crypto/md5, crypto/rc4, or crypto/des imports | grep analysis |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | Yes | rpm-lockfile-prototype | rpms.lock.yaml |
| **Language deps (Go)** | go.sum | Yes | go mod | go.sum |
| **Language deps (Python)** | requirements.txt (pinned versions) | Yes | pip (pinned freeze output) | requirements.txt |
| **Artifacts** | not found | No | Hermeto | N/A |
| **Hermeto prefetch** | Not present in Dockerfile.konflux | No | Hermeto (formerly cachi2) | N/A |

_The Tekton pipeline configuration (`.tekton/`) references `prefetch-input` with `[{"type": "gomod"}, {"type": "rpm"}, {"type": "pip", ...}]` for Konflux hermetic build integration. RPM lock file (`rpms.lock.yaml`) is present alongside `rpms.in.yaml` and `ubi.repo`, indicating downstream-branch RPM hermeticity. Python dependencies are pinned via `requirements.txt` generated from `pip freeze`. The `rpms.in.yaml` specifies architectures `x86_64` and `aarch64`._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-pod | Inferred from sidecar deployment model — each model serving pod contains its own puller and adapter instances |
| **Deployment model** | single-tenant per pod | Each runtime pod manages models for its specific ServingRuntime; model isolation is at the pod level |
| **Tenant identifier** | N/A | No tenant concept within the adapter — ModelMesh handles multi-tenant model routing at the cluster level |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | No inter-process auth (loopback gRPC) | Pod-level isolation (Kubernetes) | Relies entirely on pod network boundary; no authentication between puller, adapter, and runtime |
| Data storage | Per-pod model directory (/models) | Kubernetes volume mounts | Models share local filesystem within the pod; no per-model access control |
| Network traffic | Loopback only (localhost) | Pod network namespace | All adapter ↔ runtime communication is pod-internal; no external exposure |
| Compute & resources | Pod-level resource limits | Kubernetes | CONTAINER_MEM_REQ_BYTES tracks capacity but does not enforce limits — relies on pod resource requests/limits |
| Configuration & secrets | Per-pod mounted secrets (/storage-config) | Kubernetes Secret volume mounts | All storage credentials accessible to all binaries in the pod |
| API scoping | Per-model request (modelId in LoadModelRequest) | ModelMesh (upstream) | Adapter processes any modelId sent by ModelMesh; no per-tenant filtering |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| Cloud storage (S3/GCS/Azure) | Per-storage-secret | Different storage configs can use different credentials; PullMan caches clients per credential hash |

## Data Flows

### Flow 1: Model Loading

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | ModelMesh | model-serving-puller | 8084/TCP | gRPC | plaintext | None |
| 2 | model-serving-puller | Cloud storage (S3/GCS/Azure/HTTP) | 443/TCP | HTTPS | TLS 1.2+ | Storage credentials |
| 3 | model-serving-puller | runtime adapter (triton/mlserver/ovms/torchserve) | 8085/TCP | gRPC | plaintext | None |
| 4 | runtime adapter | target runtime server | 8001/TCP (7071 for TorchServe) | gRPC or HTTP | plaintext | None |

### Flow 2: Model Unloading

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | ModelMesh | model-serving-puller | 8084/TCP | gRPC | plaintext | None |
| 2 | model-serving-puller | runtime adapter | 8085/TCP | gRPC | plaintext | None |
| 3 | runtime adapter | target runtime server | 8001/TCP (7071 for TorchServe) | gRPC or HTTP | plaintext | None |
| 4 | runtime adapter | local filesystem | -- | -- | -- | -- |

### Flow 3: Keras Model Conversion (Triton only)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | triton-adapter (Go) | Python subprocess (tf_pb.py) | -- | subprocess exec | -- | -- |
| 2 | tf_pb.py | local filesystem | -- | file I/O | -- | -- |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| ModelMesh | gRPC (inbound from MM) | 8084/TCP | gRPC | plaintext | Receives model lifecycle commands (LoadModel, UnloadModel, RuntimeStatus) |
| NVIDIA Triton Inference Server | gRPC (outbound to runtime) | 8001/TCP | gRPC | plaintext | Model repository load/unload, server readiness, model repository index |
| Seldon MLServer | gRPC (outbound to runtime) | 8001/TCP | gRPC | plaintext | Model repository load/unload, server readiness, model repository index |
| OpenVINO Model Server | REST HTTP (outbound to runtime) | 8001/TCP | HTTP | plaintext | Config reload (POST /v1/config/reload), config status (GET /v1/config) |
| TorchServe management API | gRPC (outbound to runtime) | 7071/TCP | gRPC | plaintext | RegisterModel, UnregisterModel, DescribeModel, ListModels |
| TorchServe inference API | gRPC (outbound to runtime) | 7070/TCP | gRPC | plaintext | Ping health check for readiness verification |
| AWS S3 / IBM COS / MinIO | HTTPS (outbound to storage) | 443/TCP | HTTPS | TLS 1.2+ | Model artifact download using IBM S3 SDK |
| Google Cloud Storage | HTTPS (outbound to storage) | 443/TCP | HTTPS | TLS 1.2+ | Model artifact download using GCS SDK |
| Azure Blob Storage | HTTPS (outbound to storage) | 443/TCP | HTTPS | TLS 1.2+ | Model artifact download using Azure SDK |
| HTTP/HTTPS model sources | HTTP/HTTPS (outbound to storage) | configurable | HTTP/HTTPS | Optional TLS/mTLS | Model artifact download via HTTP with optional client certs |
| Kubernetes PVC | Filesystem (local mount) | -- | -- | -- | Model artifact access via symlinks to /pvc_mounts/{pvcName} |
| Kubernetes Secrets | Volume mount | -- | -- | -- | Storage credentials mounted at /storage-config |
| model-serving-config ConfigMap | Read by modelmesh-controller | -- | -- | -- | Controls which runtime adapter image is used in pods |

## Architectural Analysis

The ModelMesh Runtime Adapter follows a **multi-binary sidecar pattern** where five Go binaries are compiled into a single container image. The image has no default entrypoint; instead, the consuming pod spec selects which binary to execute via its `command:` field. This design minimizes image count and registry footprint while supporting four distinct inference runtimes. The puller and one adapter run as separate processes within the same pod, communicating exclusively over loopback gRPC — the puller on port 8084 receiving requests from ModelMesh, and the selected adapter on port 8085 managing the target runtime.

The **PullMan library** (`pullman/`) implements a clean plugin architecture for storage providers. Each provider registers itself and implements the `StorageProvider` interface with `GetKey` (credential hashing), `NewRepository` (client construction), and download methods. A cache layer (`pullman/cache.go`) reuses storage clients by credential hash with a 1-hour TTL and 4-hour cleanup cycle, which avoids reconnecting to cloud storage for every model load. Storage credentials flow from Kubernetes Secrets mounted at `/storage-config`, with per-request `storage_params` in the `ModelKey` JSON able to override or supplement the mounted config. This credential layering (mounted secret → per-request override) gives flexibility without hardcoding storage details.

The **OVMS adapter stands apart architecturally** from the other three adapters. While Triton, MLServer, and TorchServe adapters interact with their runtimes via gRPC, the OVMS adapter uses REST HTTP to communicate with OpenVINO Model Server. More significantly, it implements an **actor-based batching pattern** (`modelmanager.go`) where model load and unload operations are queued and batched within a configurable time window (100ms–3s). The actor writes a JSON config file (`model_config_list.json`) containing all models and calls OVMS's `/v1/config/reload` endpoint, then polls `/v1/config` to verify each model reaches its expected state. This batching is necessary because OVMS loads its model configuration from a single file rather than individual repository operations.

All inter-process communication within the pod uses **plaintext gRPC with insecure credentials**. This is architecturally intentional — the puller, adapter, and runtime all share a pod network namespace, so loopback traffic never leaves the pod boundary. Security is enforced at the pod level by Kubernetes network isolation, not within the adapter. The only encrypted traffic is outbound to cloud storage, where the SDKs use TLS by default. The HTTP provider additionally supports mTLS with client certificates for private model repositories. The FIPS posture is strong at build time (CGO_ENABLED=1, GOEXPERIMENT=strictfipsruntime, -tags strictfipsruntime) with no application-level non-FIPS crypto detected.

The inclusion of **Python 3.11 and TensorFlow 2.19.0** in the runtime image is solely for the Triton adapter's Keras-to-SavedModel conversion feature (`tf_pb.py`). This adds significant image size but enables users to serve Keras H5 models via Triton without manual conversion. The conversion runs as a subprocess with a concurrency semaphore (`MAX_CONC_KERAS_CONV_PROCS=2`) to prevent OOM. The Python dependencies are pinned via `requirements.txt` from `pip freeze` output, and the Konflux build uses prefetch for hermetic builds. The TensorFlow dependency does not currently use AIPCC base images, which is worth tracking for future security hardening.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| c57fd87 | Recent | Remove open-data-hub-tenant PipelineRun manifests |
| 205f0e9 | Recent | Dockerfile digest updates (dependency maintenance) |
| 6c92b9b | Recent | Dockerfile digest updates (dependency maintenance) |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| Dockerfile.konflux | 1-96 | Metadata, Architecture Components, AIPCC Ecosystems Use, FIPS Compliance, Build Hermeticity, Sub-Component Details |
| Dockerfile | 1-202 | Architecture Components (upstream reference), Purpose |
| go.mod | 1-81 | Dependencies, Metadata (Go version), FIPS (crypto libs) |
| README.md | 1-229 | Purpose, Architecture Components, Integration Points |
| Makefile | 1-79 | Metadata (build system) |
| version | 1-4 | Metadata (upstream version v0.11.0) |
| requirements.txt | 1-43 | AIPCC Ecosystems Use, Dependencies (Python), Build Hermeticity |
| rpms.in.yaml | 1-15 | Build Hermeticity (RPM packages, architectures) |
| rpms.lock.yaml | (headers) | Build Hermeticity (RPM lock presence) |
| model-serving-puller/main.go | 15-46 | Sub-Component Details (puller), Configuration |
| model-serving-puller/server/server.go | 38-212 | gRPC Services, Network Architecture, Data Flows, Security (TLS) |
| model-serving-puller/server/config.go | 23-35 | Configuration (PORT, MODEL_SERVER_ENDPOINT) |
| model-serving-puller/server/modelstate.go | 29-219 | Architecture Components (model state management) |
| model-serving-puller/puller/config.go | 29-98 | Configuration, Secrets, Storage Providers |
| model-serving-puller/puller/puller.go | 29-303 | Data Flows, Storage Providers, Configuration |
| model-serving-puller/puller/dotpath.go | 22-71 | Configuration (parameter override logic) |
| model-mesh-triton-adapter/main.go | 41-50 | gRPC Services, Sub-Component Details |
| model-mesh-triton-adapter/server/server.go | 54-275 | gRPC Services, Network Architecture, Data Flows, Model Management |
| model-mesh-triton-adapter/server/config.go | 27-82 | Configuration (env vars) |
| model-mesh-triton-adapter/server/const.go | 17-23 | Architecture Components (service names, constants) |
| model-mesh-triton-adapter/server/schema.go | 29-76 | Architecture Components (schema to config conversion) |
| model-mesh-triton-adapter/server/adaptmodellayout.go | 59-411 | Model Management (directory layout, symlinks, config.pbtxt) |
| model-mesh-triton-adapter/server/utils.go | 35-97 | Architecture Components (Keras conversion, concurrency semaphore) |
| model-mesh-mlserver-adapter/main.go | 41-50 | gRPC Services, Sub-Component Details |
| model-mesh-mlserver-adapter/server/server.go | 41-559 | gRPC Services, Network Architecture, Model Management |
| model-mesh-mlserver-adapter/server/config.go | 27-82 | Configuration (env vars) |
| model-mesh-ovms-adapter/main.go | 40-49 | gRPC Services, Sub-Component Details |
| model-mesh-ovms-adapter/server/server.go | 32-236 | gRPC Services, Network Architecture, Data Flows |
| model-mesh-ovms-adapter/server/config.go | 28-106 | Configuration (env vars, OVMS-specific) |
| model-mesh-ovms-adapter/server/const.go | 17-22 | Architecture Components (service names) |
| model-mesh-ovms-adapter/server/adaptmodellayout.go | 30-148 | Model Management (directory layout for OVMS) |
| model-mesh-ovms-adapter/server/modelconfig.go | 17-94 | Architecture Components (OVMS config JSON structures) |
| model-mesh-ovms-adapter/server/modelmanager.go | 34-597 | Architectural Analysis (actor pattern, batching, HTTP client) |
| model-mesh-torchserve-adapter/main.go | 46-55 | gRPC Services, Sub-Component Details |
| model-mesh-torchserve-adapter/server/server.go | 46-488 | gRPC Services, Network Architecture, Model Management, Data Flows |
| model-mesh-torchserve-adapter/server/config.go | 27-97 | Configuration (env vars, TorchServe-specific) |
| internal/envconfig/envconfig.go | 15-102 | Configuration (env var helper functions) |
| internal/modelschema/modelschema.go | 15-71 | Architecture Components (model schema, data types) |
| internal/util/connect.go | 15-31 | Network Architecture (endpoint resolution) |
| internal/util/fileutil.go | 15-73 | Architecture Components (file utilities) |
| internal/util/join.go | 15-33 | Security (SecureJoin path traversal prevention) |
| internal/util/loadmodel.go | 15-93 | Architecture Components (model type extraction, size calculation) |
| internal/proto/mmesh/model-runtime.proto | 1-152 | gRPC Services (ModelRuntime: 5 RPCs) |
| internal/proto/mmesh/model-mesh.proto | 1-206 | Integration Points (ModelMesh: 7 RPCs) |
| internal/proto/kfserving-predict-v2/inference.proto | 1-298 | gRPC Services (KFServing v2: 6 RPCs) |
| internal/proto/triton/triton.proto | 1-1440 | gRPC Services (Triton: 14 RPCs) |
| internal/proto/triton/model_config.proto | 1-1637 | Architecture Components (Triton model config) |
| internal/proto/torchserve/inference.proto | 1-37 | gRPC Services (TorchServe inference: 2 RPCs) |
| internal/proto/torchserve/management.proto | 1-121 | gRPC Services (TorchServe management: 6 RPCs) |
| internal/proto/mlserver/dataplane/dataplane.proto | 1-454 | gRPC Services (MLServer dataplane: 9 RPCs) |
| model-mesh-triton-adapter/scripts/tf_pb.py | 1-35 | Architecture Components (Keras conversion script) |
| pullman/pullman.go | 26-125 | Storage Providers, Architecture Components |
| pullman/types.go | 23-54 | Storage Providers (interfaces) |
| pullman/config.go | 26-120 | Configuration (storage config) |
| pullman/cache.go | 24-147 | Architecture Components (client caching) |
| pullman/helpers.go | 25-60 | Architecture Components (hashing, file utils) |
| pullman/cmd/main.go | 32-104 | Architecture Components (CLI example) |
| pullman/storageproviders/s3/provider.go | 30-179 | Storage Providers (S3), Egress |
| pullman/storageproviders/s3/downloader.go | 15-151 | Storage Providers (S3), Security (TLS) |
| pullman/storageproviders/gcs/provider.go | 29-176 | Storage Providers (GCS), Egress |
| pullman/storageproviders/gcs/downloader.go | 32-182 | Storage Providers (GCS) |
| pullman/storageproviders/azure/provider.go | 29-195 | Storage Providers (Azure), Egress |
| pullman/storageproviders/azure/downloader.go | 28-137 | Storage Providers (Azure) |
| pullman/storageproviders/http/provider.go | 33-215 | Storage Providers (HTTP), Security (mTLS) |
| pullman/storageproviders/http/downloader.go | 30-90 | Storage Providers (HTTP) |
| pullman/storageproviders/pvc/provider.go | 29-129 | Storage Providers (PVC) |
| .tekton/odh-modelmesh-runtime-adapter-pull-request.yaml | 1-20 | Build Hermeticity (Konflux pipeline config) |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED | Dockerfile.konflux | FIPS Compliance |
| tls\.Config\|CipherSuites\|MinVersion\|InsecureSkipVerify | internal/proto/*.pb.go (false positives — protoimpl version checks only) | FIPS Compliance (Application-Level Crypto) |
| crypto/md5\|crypto/rc4\|crypto/des | (no matches) | FIPS Compliance (no non-FIPS crypto) |
| *Dockerfile*konflux* | Dockerfile.konflux | Architecture Components, AIPCC Ecosystems Use |
| rpms.lock.yaml, go.sum, requirements.txt | rpms.lock.yaml, go.sum, requirements.txt | Build Hermeticity |
| cachi2\|hermeto\|REMOTE_SOURCES | (no matches in Dockerfiles) | Build Hermeticity |
| *.proto | 8 proto files in internal/proto/ | gRPC Services |
| *.go (non-test) | 64 Go source files | All code-analysis sections |
| upstream\|fork\|sync in .github/workflows/ | create-tag-release.yml (weak match) | Provenance |

### Summary

- **Total files read**: 65
- **Total lines referenced**: ~8,500
- **Coverage**: All sections have direct source backing. Provenance based on local analysis (no component-map.json available). Multi-tenancy model inferred from sidecar deployment pattern and loopback-only communication.

---
*Generated in 6m 29s (389s total)*
