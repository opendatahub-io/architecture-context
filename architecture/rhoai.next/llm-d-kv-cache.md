# Component: llm-d-kv-cache

## Metadata

- **Repository**: https://github.com/red-hat-data-services/llm-d-kv-cache
- **Version**: v0.7.0-rc2-189-g16f44ed (main branch)
- **Distribution**: RHOAI
- **Languages**: Go 1.25, Python 3.12
- **Deployment Type**: Library / Sidecar Service
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/llm-d/llm-d-kv-cache | -- | -- | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/llm-d-kv-cache | manual | main | -- | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Go library and Python sidecar services for KV-cache-aware routing in distributed LLM inference, tracking block residency across a fleet of model-server pods and scoring requests by prefix cache hit ratio.

**Detailed**: The llm-d-kv-cache project provides the core KV-cache indexing infrastructure for the llm-d distributed inference platform. Its primary component is a Go library (`pkg/kvcache`) that maintains a near-real-time, globally consistent view of KV-cache block locality across a fleet of vLLM or SGLang model-server pods. The library is designed to be embedded in a host process -- llm-d's Envoy Processing Plugin (EPP) is the reference consumer -- rather than deployed as a standalone service.

The library operates on two data paths: a **Write Path** that ingests KV-block lifecycle events (stored, evicted, cleared) from model servers via ZeroMQ, and a **Read Path** that scores candidate pods for a given prompt by computing how much of the request's KV-cache prefix each pod already holds. Block keys are derived from token sequences using chained FNV-64a hashes over CBOR-encoded content, with support for multimodal content hashes, LoRA adapter differentiation, and CPU-offloaded blocks.

The project also ships supporting services: a Python gRPC **UDS Tokenizer** sidecar that tokenizes and renders prompts via vLLM's tokenization engine, a **llmd-fs-backend** Python connector for offloading KV-cache blocks to shared storage (POSIX/GDS/S3), and a **PVC Evictor** utility for managing disk space on PVC-backed KV-cache storage. The fs-backend has been upstreamed into vLLM as of v0.23 and is in its final release.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| `pkg/kvcache` | Go Library | KV-cache indexer orchestrating block-key computation, index lookup, and pod scoring via longest-prefix matching |
| `pkg/kvevents` | Go Library | Sharded ZMQ event processing pool consuming KV-block lifecycle events from model servers |
| `pkg/kvevents/engineadapter` | Go Library | Engine-specific msgpack event parsers for vLLM and SGLang wire formats |
| `pkg/kvcache/kvblock` | Go Library | Pluggable block index backends (in-memory LRU, cost-aware Ristretto, Redis/Valkey) and token-to-block-key hashing |
| `pkg/tokenization` | Go Library | gRPC client for external tokenization via UDS tokenizer sidecar |
| `pkg/telemetry` | Go Library | OpenTelemetry tracing initialization and span management |
| `services/uds_tokenizer` | Python gRPC Service | Sidecar tokenizer and chat template renderer using vLLM's tokenization engine |
| `kv_connectors/llmd_fs_backend` | Python Library | vLLM OffloadingConnector backend for GPU-to-shared-storage KV-cache block transfers |
| `kv_connectors/llmd_fs_backend/llmd_nixl` | Python Library | NIXL-based async transfer engine for S3/object storage KV-cache offloading |
| `kv_connectors/pvc_evictor` | Python CLI | Multi-process utility for managing PVC disk space by evicting stale KV-cache blocks |

## AIPCC Ecosystems Use

### Accelerator Build Variants

| Variant | Dockerfile | Base Image | Accelerator | Version | Architectures | Status |
|---------|-----------|------------|-------------|---------|---------------|--------|
| CPU | `services/uds_tokenizer/Dockerfile.konflux` | `quay.io/aipcc/base-images/cpu:3.4` | CPU | -- | aarch64, ppc64le, s390x, x86_64 | Active |

### AIPCC Package Index

| Scope | Details |
|-------|---------|
| **Uses RHEL AI PyPI** | Yes -- inherited from AIPCC CPU base image, plus explicit `--index-url` in `pyproject.toml` |
| **pip.conf / uv.toml configured** | Inherited from base image; `pyproject.toml` additionally configures `tool.uv.index-url` pointing to `https://packages.redhat.com/api/pypi/public-rhai/rhoai/3.4/cpu-ubi9/simple/` |
| **Additional packages installed** | vllm==0.18.0+rhaiv.4, pydantic, shortuuid, transformers, safetensors, Jinja2, huggingface-hub, aiohttp, protobuf, tiktoken, grpcio, grpcio-tools, grpcio-reflection |
| **Source** | `services/uds_tokenizer/Dockerfile.konflux:29`, `services/uds_tokenizer/pyproject.toml:31-48` |

### AIPCC Tooling

| Tool / Path | Purpose | Used By |
|-------------|---------|---------|
| _No AIPCC tooling used_ | The Dockerfile.konflux does not call `/usr/libexec/rhaipcc/dnf` or source `/etc/rhaipcc/env` | -- |

## Sub-Component Details

### uds-tokenizer

**Dockerfile**: `services/uds_tokenizer/Dockerfile.konflux`
**Intent**: Sidecar tokenizer service deployed alongside the KV-cache indexer (embedded in llm-d's EPP). Provides gRPC-based tokenization and chat template rendering using vLLM's tokenization engine, communicating over Unix domain sockets for low-latency IPC. Deployed when the host process needs external tokenization rather than in-process tokenization.
**Image**: `odh-llm-d-kv-cache-rhel9`
**Language**: Python 3.12
**Port**: 8082/TCP (health check), UDS `/tmp/tokenizer/tokenizer-uds.socket` (gRPC)

#### API Routes

| Path | Method | Upstream Service | Auth | Purpose |
|------|--------|-----------------|------|---------|
| `/healthz` | GET | -- | None | Liveness/readiness health check |

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| HuggingFace Hub | HTTPS | 443/TCP | HF Token (optional) | Download tokenizer model files |
| ModelScope | HTTPS | 443/TCP | None | Alternative model download source |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `LOG_LEVEL` | env | `INFO` | Logging verbosity |
| `PROBE_PORT` | env | `8082` | HTTP health check port |
| `GRPC_PORT` | env | -- | TCP gRPC port (testing only; production uses UDS) |
| `GRACE_PERIOD_SECONDS` | env | `30.0` | Shutdown grace period |
| `TOKENIZERS_DIR` | env | `services/uds_tokenizer/tokenizers` | Tokenizer cache directory |
| `USE_MODELSCOPE` | env | `false` | Use ModelScope instead of HuggingFace |
| `ENABLE_GRPC_REFLECTION` | env | -- | Enable gRPC reflection service |
| `THREAD_POOL_SIZE` | env | auto (2x CPU) | Thread pool size for CPU operations |
| `VLLM_TARGET_DEVICE` | env | `cpu` | vLLM device target (set in Dockerfile) |

### llmd-fs-backend

**Dockerfile**: `kv_connectors/llmd_fs_backend/Dockerfile.dev` (development only; no Konflux Dockerfile)
**Intent**: vLLM OffloadingConnector plugin that moves KV-cache blocks between GPU memory and shared storage (POSIX filesystem, GDS, or S3 via NIXL). Deployed as a vLLM plugin loaded at runtime via entry point. This connector is in its **final release** (v0.23) -- functionality has been upstreamed into vLLM's multi-tier offloading connector.
**Image**: N/A (Python wheel, not a standalone container)
**Language**: Python 3.12, C++/CUDA (native extension)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Shared filesystem | POSIX I/O | N/A | Filesystem permissions | KV-cache block storage |
| S3/Object Store | HTTPS | 443/TCP | AWS credentials | KV-cache block storage (OBJ backend) |
| ZMQ Subscriber | ZMQ PUB | 5559/TCP (configurable) | None | Publish BlockStored/BlockRemoved events |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `backend` | extra_config | `POSIX` | Storage backend type (POSIX or OBJ) |
| `shared_storage_path` | extra_config | `/tmp/shared-kv` | Root directory for block storage |
| `block_size` | extra_config | `256` | Storage block size |
| `threads_per_gpu` | extra_config | `64` | I/O threads per GPU |
| `gds_mode` | extra_config | `disabled` | GPU Direct Storage mode |
| `max_staging_memory_gb` | extra_config | `150` | CPU staging buffer limit (GB) |
| `enable_events` | extra_config | `false` | Enable ZMQ event publishing |
| `storage_events_endpoint` | extra_config | -- | ZMQ PUB endpoint for events |
| `STORAGE_LOG_LEVEL` | env | -- | Logging verbosity |

### pvc-evictor

**Dockerfile**: `kv_connectors/pvc_evictor/Dockerfile` (no Konflux Dockerfile)
**Intent**: Kubernetes-side utility deployed as a sidecar or standalone pod that monitors PVC disk usage and automatically evicts stale KV-cache block files when disk pressure exceeds a threshold. Uses a multi-process N+2 architecture (N crawlers + activator + deleter + optional folder cleaner) for high-throughput file discovery and deletion on multi-terabyte PVCs.
**Image**: `quay.io/pvc-evictor/pvc-evictor:latest` (configurable)
**Language**: Python 3.12

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `PVC_MOUNT_PATH` | env | `/kv-cache` | PVC mount point to monitor |
| `CLEANUP_THRESHOLD` | env | `85.0` | Disk usage % to trigger eviction |
| `TARGET_THRESHOLD` | env | `70.0` | Disk usage % to stop eviction |
| `CACHE_DIRECTORY` | env | `kv/model-cache/models` | Cache subdirectory relative to mount |
| `NUM_CRAWLER_PROCESSES` | env | `8` | Number of parallel crawler processes (power of 2) |
| `DELETION_BATCH_SIZE` | env | `100` | Files deleted per batch |
| `FILE_ACCESS_TIME_THRESHOLD_MINUTES` | env | `60` | Skip files accessed within this window |
| `ENABLE_DIR_CLEANUP` | env | `true` | Background empty directory cleanup |
| `DIR_CLEANUP_TTL_SECONDS` | env | `120` | Age guard for directory cleanup |
| `DRY_RUN` | env | `false` | Simulate deletion without removing files |
| `STORAGE_EVENTS_ENDPOINT` | env | -- | ZMQ endpoint for BlockRemoved events |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_No CRDs -- this is a library/service, not an operator._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| `/healthz` | GET | 8082/TCP | HTTP | None | None | UDS tokenizer health check |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| `TokenizationService.Tokenize` | UDS | gRPC | None (local IPC) | None | Tokenize text input to token IDs |
| `TokenizationService.RenderChatCompletion` | UDS | gRPC | None (local IPC) | None | Render OpenAI chat completion request to tokens + multimodal features |
| `TokenizationService.RenderCompletion` | UDS | gRPC | None (local IPC) | None | Render OpenAI completion request to tokens |
| `TokenizationService.InitializeTokenizer` | UDS | gRPC | None (local IPC) | None | Pre-load tokenizer for a model |
| `TokenizationService.RenderChatTemplate` | UDS | gRPC | None (local IPC) | None | Deprecated: apply chat template to messages |
| `IndexerService.GetPodScores` | 50051/TCP (configurable) | gRPC | None | None | Score pods for a prompt based on KV-cache prefix hits |

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| ZeroMQ (libzmq) | 4.x | Yes | Event transport from model servers to indexer |
| Redis / Valkey | 6.x+ | No | Optional external block index backend |
| vLLM | 0.18.0+rhaiv.4 | Yes (tokenizer) | Tokenization engine and chat template rendering |
| HuggingFace Hub | latest | No | Tokenizer model downloads |
| NIXL | latest | No | Async GPU-to-S3 block transfers (OBJ backend) |
| OpenTelemetry Collector | latest | No | Tracing data export |
| Prometheus | latest | No | Metrics scraping |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| llm-d-router (EPP) | Library embedding | Primary consumer -- embeds kvcache.Indexer for KV-aware routing |
| vLLM model server pods | ZMQ PUB/SUB | Source of KV-block lifecycle events |
| SGLang model server pods | ZMQ PUB/SUB | Alternative event source (SGLang adapter) |

## Deployment Manifests

### Kustomize Structure

| Base / Overlay | Path | Purpose |
|----------------|------|---------|
| base | `deploy/kustomization.yaml` | Core deployment resources (StatefulSet, Service, OpenShift Route, RBAC) |
| common | `deploy/common/` | StatefulSet and Service definitions with patches |
| openshift | `deploy/openshift/` | OpenShift Route configuration |
| rbac | `deploy/rbac/` | Exec RBAC Role and RoleBinding for pod exec access |

### Parameterization

| Parameter | Source | Default | Purpose |
|-----------|--------|---------|---------|
| `${NAMESPACE}` | envsubst | -- | Target namespace for all resources |
| `${PROJECT_NAME}` | envsubst | `llm-d-kv-cache` | Name prefix for all resources |
| `config` ConfigMap | configMapGenerator | -- | Application configuration (suffix hash disabled) |

### Distribution Variants

| Variant | Path | Differences |
|---------|------|-------------|
| Helm (vLLM setup) | `vllm-setup-helm/` | Full vLLM + KV-cache-manager + Redis deployment chart |
| Helm (PVC evictor) | `kv_connectors/pvc_evictor/helm/` | PVC evictor standalone deployment with security constraints |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| kv-cache-manager | ClusterIP | 5557/TCP | 5557 | ZMQ | None | None | Internal |
| kv-cache-manager-http | ClusterIP | 8080/TCP | 8080 | HTTP | None | None | Internal |
| uds-tokenizer | UDS | N/A | `/tmp/tokenizer/tokenizer-uds.socket` | gRPC | None | None | Pod-local |
| uds-tokenizer-probe | ClusterIP | 8082/TCP | 8082 | HTTP | None | None | Internal |
| redis-lookup-server | ClusterIP | 8100/TCP | 6379 | Redis wire | None | None | Internal |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|
| kv-cache-manager-route | Route (OpenShift) | -- | 443/TCP | HTTPS | TLS (edge) | edge | External |

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| vLLM pods (ZMQ) | 5557/TCP | ZMQ | None | None | Subscribe to KV-block events |
| Redis / Valkey | 6379/TCP | Redis wire | None (optional TLS) | None (optional password) | External block index backend |
| OTLP Collector | 4317/TCP | gRPC | None (configurable TLS) | None | Export OpenTelemetry traces |
| HuggingFace Hub | 443/TCP | HTTPS | TLS 1.2+ | Bearer token (optional) | Download tokenizer models |
| S3 / Object Store | 443/TCP | HTTPS | TLS 1.2+ | AWS IAM credentials | KV-cache block storage (OBJ backend) |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| kv-cache-manager (namespace-scoped) | `""` (core) | pods | get, list, watch |
| kv-cache-manager (cluster-wide) | `""` (core) | pods | get, list, watch |
| exec-rbac-role | `""` (core) | pods/exec | create |

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| exec-rbac-rolebinding | `${NAMESPACE}` | exec-rbac-role | `${PROJECT_NAME}` |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| vllm-p2p-secrets | Opaque | HuggingFace API token for model downloads | Manual | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| gRPC TokenizationService | all RPCs | None | N/A (pod-local UDS) | Pod-internal only, no network exposure |
| gRPC IndexerService | GetPodScores | None | N/A (library API) | Embedded in host process |
| `/healthz` | GET | None | N/A | Unauthenticated health check |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A -- no Go binary in Konflux build; Konflux Dockerfile builds Python-only UDS tokenizer | `services/uds_tokenizer/Dockerfile.konflux` |
| **Linking** | N/A (Python service) | `services/uds_tokenizer/Dockerfile.konflux` |
| **OpenSSL in image** | Yes -- inherited from AIPCC `cpu:3.4` base (RHEL 9-based UBI) | `services/uds_tokenizer/Dockerfile.konflux:16` |
| **OLM FIPS annotation** | Not present (not an OLM-managed operator) | N/A |

_Note: The upstream `Dockerfile` builds a Go binary using `quay.io/projectquay/golang:1.24` but has no FIPS build flags. This Dockerfile is not used by Konflux -- only `Dockerfile.konflux` is used for RHOAI builds._

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No custom TLS configuration in Go library code; gRPC connections use default Go crypto/tls | `pkg/tokenization/pool_uds.go`, `pkg/kvcache/kvblock/redis.go` |
| **Crypto libraries** | Go: stdlib `crypto/tls` (FIPS via OpenSSL when linked with boringcrypto); Python: `cryptography==46.0.7` (transitive via PyJWT/vLLM) | `go.mod`, `services/uds_tokenizer/requirements.txt` |
| **Certificate handling** | System trust store (default); Redis/Valkey supports custom TLS config | `pkg/kvcache/kvblock/redis.go` |
| **Non-FIPS crypto risks** | `hashlib.md5()` used in `llmd_nixl/obj_backend.py` for S3 key hashing (non-cryptographic use, bucket distribution only) | `kv_connectors/llmd_fs_backend/llmd_nixl/obj_backend.py` |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (may exist on downstream release branches) |
| **Language deps (Go)** | go.sum | Yes | go mod | `./go.sum` |
| **Language deps (Python - tokenizer)** | uv.lock | Yes | uv | `./services/uds_tokenizer/uv.lock` |
| **Language deps (Python - tokenizer)** | requirements.txt (pinned hashes) | Yes | uv export | `./services/uds_tokenizer/requirements.txt` |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | pip prefetch in Tekton PipelineRun | Yes | Hermeto (formerly cachi2) | `.tekton/odh-llm-d-kv-cache-pull-request.yaml:49` |

_The Tekton PipelineRun specifies `hermetic: true` and includes a `prefetch-input` parameter for pip dependencies, indicating Konflux hermetic build support. The `rpms.lock.yaml` is not present on the `main` branch -- downstream release branches likely add it._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-namespace (pod discovery) / per-model (event topics) | `pkg/kvevents/pool.go`, `docs/configuration.md` |
| **Deployment model** | Single-tenant library embedded per host process instance | `docs/architecture.md:1-7` |
| **Tenant identifier** | Model name (event topic filter) + pod label selector (namespace) | `pkg/kvevents/subscriber_manager.go`, `docs/configuration.md:370-398` |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | None -- library trusts host process | Host process (EPP) | No authentication on ZMQ event ingestion; malicious event publisher could poison the index |
| Data storage | Per-instance in-memory index; Redis/Valkey shared across replicas | Application | Redis/Valkey backend shares state across all replicas without tenant isolation |
| Network traffic | ZMQ PUB/SUB with topic-based filtering (`kv@<pod>:<port>@<model>`) | Application | No encryption on ZMQ transport; relies on network-level isolation |
| Compute & resources | Configurable concurrency (workers), memory-bounded index | Application | Cost-aware backend provides memory ceiling; in-memory LRU has entry-count limit |
| Configuration & secrets | Per-deployment configuration via JSON/env | Kubernetes | HuggingFace token shared across all models in a deployment |
| API scoping | Pod label selector + namespace scoping in pod discovery | Kubernetes RBAC | Cluster-wide pod watch when namespace is empty |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| Redis / Valkey (optional) | None -- all keys in single instance | No tenant isolation; all block index data in shared key space |
| ZMQ event bus | Per-pod topic | Topic filter prevents cross-model event mixing |

## Data Flows

### Flow 1: KV-Event Ingestion (Write Path)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | vLLM model server | ZMQ PUB socket | 5557/TCP | ZMQ (msgpack) | None | None |
| 2 | ZMQ SUB socket | kvevents.Pool | local | in-process | N/A | N/A |
| 3 | kvevents.Pool worker | EngineAdapter (vLLM/SGLang) | local | in-process | N/A | N/A |
| 4 | kvevents.Pool worker | kvblock.Index | local | in-process | N/A | N/A |
| 5 | kvblock.Index (Redis backend) | Redis/Valkey | 6379/TCP | Redis wire | None (optional TLS) | None (optional password) |

### Flow 2: Pod Scoring (Read Path)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Host process (EPP) | kvcache.Indexer.ScoreTokens | local | in-process | N/A | N/A |
| 2 | kvcache.Indexer | kvblock.TokenProcessor | local | in-process | N/A | N/A |
| 3 | kvcache.Indexer | kvblock.Index.Lookup | local | in-process (or Redis 6379/TCP) | N/A | N/A |
| 4 | kvcache.Indexer | kvblock.Scorer | local | in-process | N/A | N/A |

### Flow 3: Tokenization (UDS Sidecar)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Go gRPC client (pkg/tokenization) | UDS tokenizer | UDS `/tmp/tokenizer/tokenizer-uds.socket` | gRPC | None (local IPC) | None |
| 2 | UDS tokenizer | HuggingFace Hub | 443/TCP | HTTPS | TLS 1.2+ | Bearer (HF token) |

### Flow 4: KV-Cache Offloading (FS Backend)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | vLLM (GPU) | llmd-fs-backend | local | in-process | N/A | N/A |
| 2 | llmd-fs-backend | Shared filesystem / S3 | POSIX I/O / 443/TCP | NFS/POSIX / HTTPS | None / TLS | Filesystem perms / AWS IAM |
| 3 | llmd-fs-backend | ZMQ PUB | 5559/TCP | ZMQ (msgpack) | None | None |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| llm-d-router (EPP) | Library embedding | N/A | in-process | N/A | Primary consumer -- embeds Indexer for KV-aware routing decisions |
| vLLM model server | ZMQ PUB/SUB | 5557/TCP | ZMQ (msgpack) | None | KV-block lifecycle events (BlockStored, BlockRemoved, AllBlocksCleared) |
| SGLang model server | ZMQ PUB/SUB | 5557/TCP | ZMQ (msgpack) | None | Alternative KV-block event source |
| UDS tokenizer sidecar | gRPC over UDS | UDS | gRPC | None | Tokenize prompts and render chat templates for block-key computation |
| Redis / Valkey | TCP | 6379/TCP | Redis wire | Optional TLS | External block index backend (optional) |
| Kubernetes API server | REST (controller-runtime) | 443/TCP | HTTPS | mTLS (ServiceAccount) | Pod discovery for per-pod ZMQ subscriber management |
| OTLP Collector | gRPC | 4317/TCP | gRPC | Optional TLS | Export OpenTelemetry traces |
| Prometheus | HTTP scrape | varies | HTTP | None | Metrics scraping (kvcache_index_* metrics) |
| HuggingFace Hub | HTTPS | 443/TCP | HTTPS | TLS 1.2+ | Tokenizer model downloads (UDS tokenizer) |
| S3 / Object Store | HTTPS | 443/TCP | HTTPS | TLS 1.2+ | KV-cache block offloading (NIXL OBJ backend) |
| Shared filesystem (NFS/PVC) | POSIX I/O | N/A | NFS/local | None | KV-cache block offloading (POSIX backend) |

## Architectural Analysis

The llm-d-kv-cache project exhibits a clean separation between its library core and supporting services, following a **library-first architecture** where the primary value -- the KV-cache indexer -- is designed to be embedded rather than deployed independently. This pattern is unusual in the Kubernetes ecosystem, where most components are standalone services, but it's well-suited for the latency-sensitive scoring path where every network hop adds milliseconds to request routing decisions.

The **dual-key design** is the most architecturally significant pattern in the codebase. The indexer maintains two parallel key spaces -- engine keys (opaque hashes from vLLM/SGLang) and request keys (computed from tokens by the indexer itself) -- bridged by a mapping built during event ingestion. This decoupling means scoring never needs to round-trip to a model server, and the indexer's hash computation is independent of any engine-side hash seed. The downside is that eviction requires the reverse lookup (`engine_key → request_key`), which adds complexity to the index implementation and requires careful lifecycle management (e.g., keeping the mapping alive as long as any tier holds the block during CPU offloading).

The **sharded event processing pool** (`kvevents.Pool`) uses FNV-1a hashing on pod identifiers to route events to dedicated worker queues, guaranteeing per-pod ordering without global locks. This is critical for correctness: a CPU offload `BlockStored` event (which carries no tokens) depends on the preceding GPU `BlockStored` event (which does) having already been processed. The system degrades gracefully on message loss -- a CPU offload event for an unknown engine key is a no-op rather than a crash.

The **pluggable index backend** architecture (in-memory LRU, cost-aware Ristretto, Redis/Valkey) is well-designed but the in-memory backend dominates in practice. Redis/Valkey provides consistency across replicas but is rarely needed since each replica converges independently from the event stream. The Valkey RDMA transport support is experimental and signals a future direction for ultra-low-latency index lookups in HPC environments.

The **UDS tokenizer sidecar** pattern is notable for its use of Unix domain sockets rather than TCP. This avoids network overhead and kernel-level TCP processing for what is a pod-local communication path. The sidecar wraps vLLM's `OpenAIServingRender` to provide tokenization + multimodal feature extraction, which means the sidecar has a large dependency footprint (vLLM, PyTorch, transformers) relative to the simple tokenization task. The gRPC message size limits (100MB max) accommodate large multimodal inputs.

The **fs-backend upstreaming** (`llmd-fs-backend` → vLLM v0.23) is a significant architectural event: the connector that enables cross-node KV-cache sharing via shared storage is now part of vLLM itself. The version in this repo (v0.23) is the final release, and the NIXL-based object storage backend represents the last feature addition before the handoff. Future development continues in vLLM's `TieringOffloadingSpec`.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 16f44ed | 2026-07 | Remove open-data-hub-tenant PipelineRun manifests |
| 7bfda47 | 2026-07 | Support map-encoded vLLM KV events (vLLM #42892 compatibility) |
| 58978bf | 2026-07 | Migrate fs_backend kv_offload API to vllm 0.23.0 |
| 7be76e1 | 2026-07 | Invalidate KV index on AllBlocksCleared via eager delete |
| f453b6a | 2026-07 | Align kv-cache tracing with standard llm-d observability pattern |
| 9f03606 | 2026-07 | Add HMA (Hybrid Model Attention) support to FS connector |
| 2ffc3eb | 2026-07 | Track HMA group identity in KV cache index |
| 7566278 | 2026-07 | Implement background empty directory cleanup process in PVC evictor |
| 3eba513 | 2026-07 | Document fs-backend upstreaming into vLLM multi-tier offloading |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| `README.md` | 1-89 | Purpose, Architecture Components |
| `go.mod` | 1-134 | Metadata, Dependencies, External Dependencies |
| `Makefile` | 1-590 | Metadata, Deployment Manifests, Architecture Components |
| `Dockerfile` | 1-59 | Architecture Components, Security (FIPS) |
| `services/uds_tokenizer/Dockerfile.konflux` | 1-104 | AIPCC Ecosystems Use, Sub-Component Details, Security (FIPS) |
| `api/tokenizerpb/tokenizer.proto` | 1-206 | APIs Exposed (gRPC Services), Sub-Component Details |
| `api/indexerpb/indexer.proto` | 1-44 | APIs Exposed (gRPC Services), Integration Points |
| `docs/architecture.md` | 1-335 | Purpose, Architecture Components, Data Flows, Architectural Analysis |
| `docs/configuration.md` | 1-479 | Dependencies, Network Architecture, Multi-Tenancy, Configuration |
| `deploy/kustomization.yaml` | 1-37 | Deployment Manifests, Network Architecture |
| `services/uds_tokenizer/pyproject.toml` | 1-70 | AIPCC Ecosystems Use, Dependencies |
| `services/uds_tokenizer/requirements.txt` | 1-745 | Build Hermeticity, Dependencies, FIPS (crypto libraries) |
| `version/version.go` | 1-51 | Metadata |
| `kv_connectors/llmd_fs_backend/pyproject.toml` | 1-82 | Sub-Component Details, Architecture Components |
| `.tekton/odh-llm-d-kv-cache-pull-request.yaml` | 1-70 | Build Hermeticity, Metadata |
| `pkg/kvcache/backend.go` | all | Architecture Components, APIs Exposed |
| `pkg/kvcache/indexer.go` | all | Architecture Components, Data Flows, Integration Points |
| `pkg/kvcache/kvblock_scorer.go` | all | Architecture Components, Data Flows |
| `pkg/kvcache/types.go` | all | Architecture Components |
| `pkg/kvcache/metrics/collector.go` | all | Integration Points (Prometheus) |
| `pkg/kvcache/kvblock/index.go` | all | Architecture Components, Network Architecture |
| `pkg/kvcache/kvblock/in_memory.go` | all | Architecture Components |
| `pkg/kvcache/kvblock/cost_aware_memory.go` | all | Architecture Components |
| `pkg/kvcache/kvblock/redis.go` | all | Architecture Components, Network Architecture, Security |
| `pkg/kvcache/kvblock/token_processor.go` | all | Architecture Components, Data Flows |
| `pkg/kvcache/kvblock/extra_keys.go` | all | Architecture Components |
| `pkg/kvcache/kvblock/hma.go` | all | Architecture Components |
| `pkg/kvevents/pool.go` | all | Architecture Components, Data Flows, Multi-Tenancy |
| `pkg/kvevents/events.go` | all | Architecture Components, Data Flows |
| `pkg/kvevents/subscriber_manager.go` | all | Architecture Components, Integration Points, Multi-Tenancy |
| `pkg/kvevents/zmq_subscriber.go` | all | Architecture Components, Network Architecture |
| `pkg/kvevents/engineadapter/adapter.go` | all | Architecture Components |
| `pkg/kvevents/engineadapter/vllm_adapter.go` | all | Architecture Components, Data Flows |
| `pkg/kvevents/engineadapter/sglang_adapter.go` | all | Architecture Components |
| `pkg/telemetry/tracing.go` | all | Integration Points (OTLP) |
| `pkg/tokenization/pool.go` | all | Architecture Components |
| `pkg/tokenization/pool_uds.go` | all | Architecture Components, Integration Points, Security |
| `pkg/tokenization/uds_tokenizer.go` | all | Architecture Components, Integration Points |
| `pkg/tokenization/types/types.go` | all | Architecture Components |
| `services/uds_tokenizer/run_grpc_server.py` | all | Sub-Component Details, APIs Exposed, Configuration |
| `services/uds_tokenizer/tokenizer_grpc_service.py` | all | APIs Exposed, Sub-Component Details |
| `services/uds_tokenizer/tokenizer_service/tokenizer.py` | all | Sub-Component Details, Configuration |
| `services/uds_tokenizer/tokenizer_service/renderer.py` | all | Sub-Component Details |
| `services/uds_tokenizer/tokenizer_service/exceptions.py` | all | Sub-Component Details |
| `services/uds_tokenizer/utils/thread_pool_utils.py` | all | Sub-Component Details, Configuration |
| `kv_connectors/llmd_fs_backend/llmd_fs_backend/manager.py` | all | Sub-Component Details, Data Flows |
| `kv_connectors/llmd_fs_backend/llmd_fs_backend/worker.py` | all | Sub-Component Details, Configuration |
| `kv_connectors/llmd_fs_backend/llmd_fs_backend/spec.py` | all | Sub-Component Details, Configuration |
| `kv_connectors/llmd_fs_backend/llmd_fs_backend/event_publisher.py` | all | Sub-Component Details, Data Flows |
| `kv_connectors/llmd_fs_backend/llmd_fs_backend/file_mapper.py` | all | Sub-Component Details |
| `kv_connectors/llmd_fs_backend/llmd_fs_backend/metrics.py` | all | Integration Points (Prometheus) |
| `kv_connectors/llmd_fs_backend/llmd_nixl/obj_backend.py` | all | Sub-Component Details, Security (FIPS) |
| `kv_connectors/llmd_fs_backend/llmd_nixl/nixl_offload.py` | all | Sub-Component Details |
| `kv_connectors/llmd_fs_backend/llmd_nixl/manager.py` | all | Sub-Component Details |
| `kv_connectors/llmd_fs_backend/llmd_nixl/nixl_lookup.py` | all | Sub-Component Details |
| `kv_connectors/llmd_fs_backend/llmd_nixl/staged_backend.py` | all | Sub-Component Details |
| `kv_connectors/pvc_evictor/config.py` | all | Sub-Component Details, Configuration |
| `kv_connectors/pvc_evictor/evictor.py` | all | Sub-Component Details |
| `kv_connectors/pvc_evictor/processes/activator.py` | all | Sub-Component Details |
| `kv_connectors/pvc_evictor/processes/crawler.py` | all | Sub-Component Details |
| `kv_connectors/pvc_evictor/processes/deleter.py` | all | Sub-Component Details |
| `kv_connectors/pvc_evictor/processes/folder_cleaner.py` | all | Sub-Component Details |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `Dockerfile*konflux*` | 1 file | AIPCC Ecosystems Use, Build Hermeticity |
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` | 0 files | Security (FIPS) |
| `fips-compliant\|fips\.enabled\|FIPS` | 0 files | Security (FIPS) |
| `boring\|boringcrypto\|openssl\|crypto/tls` | 2 files (protobuf generated) | Security (FIPS) |
| `tls\.Config\|CipherSuites\|MinVersion\|InsecureSkipVerify` | 2 files (protobuf generated) | Security (FIPS) |
| `hashlib\.md5` | 1 file (`llmd_nixl/obj_backend.py`) | Security (FIPS - non-FIPS crypto risks) |
| `cryptography\|pyOpenSSL` | 2 files (requirements.txt) | Security (FIPS) |
| `cachi2\|hermeto\|REMOTE_SOURCES` | 0 files | Build Hermeticity |
| `go.sum\|uv.lock\|rpms.lock.yaml` | 2 files | Build Hermeticity |
| `sigs.k8s.io/controller-runtime` | go.mod | Architecture Components (Kubernetes integration) |
| `upstream\|fork\|sync` in workflows | workflow files | Provenance |

### Summary

- **Total files read**: 62
- **Total lines referenced**: ~8,500+
- **Coverage**: All sections have direct source backing. Provenance section uses `local_analysis` detection method (no `component-map.json` with provenance data found). The upstream `llm-d/llm-d-kv-cache` → downstream `red-hat-data-services/llm-d-kv-cache` relationship is inferred from the git remote URL and repository naming convention.

---
*Generated in 7m 24s (444s total)*
