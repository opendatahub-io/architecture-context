# Component: AI Gateway Payload Processing

## Metadata

- **Repository**: https://github.com/red-hat-data-services/ai-gateway-payload-processing
- **Version**: bf275fa (main)
- **Distribution**: RHOAI
- **Languages**: Go 1.25
- **Deployment Type**: Envoy External Processing Service (ext_proc) with Controller-Runtime Controllers
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Upstream | https://github.com/opendatahub-io/ai-gateway-payload-processing | -- | main | -- | local_analysis |
| Downstream | https://github.com/red-hat-data-services/ai-gateway-payload-processing | auto_merge | main | -- | local_analysis |

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Envoy external processing (ext_proc) service that enables RHOAI's AI Gateway to route inference requests to external LLM providers (OpenAI, Anthropic, Azure, AWS Bedrock, Google Vertex AI) with API format translation, credential injection, usage metering, and content guardrails.

**Detailed**: AI Gateway Payload Processing is a pluggable Inference Payload Processor (IPP) built on the [llm-d](https://github.com/llm-d/llm-d-inference-payload-processor) framework. It runs as an Envoy ext_proc filter attached to the AI Gateway's Istio data plane, intercepting inference requests and responses to perform mutations on headers and body content.

The service introduces two Custom Resource Definitions -- `ExternalProvider` and `ExternalModel` -- in the `inference.opendatahub.io` API group. `ExternalProvider` defines a connection to an external LLM provider (endpoint, credentials, auth type), while `ExternalModel` maps a client-facing model name to one or more providers with weighted traffic distribution, API format translation configuration, and path templating.

Three controller-runtime controllers run inside the same process: the ExternalProvider controller creates Kubernetes ExternalName Services, Istio ServiceEntries, and DestinationRules for TLS origination to each provider endpoint; the ExternalModel controller creates Gateway API HTTPRoutes that route client traffic through the gateway to the appropriate provider backend; and a legacy migration controller watches deprecated `maas.opendatahub.io/v1alpha1` ExternalModel CRs and automatically creates equivalent `inference.opendatahub.io/v1alpha1` resources. The plugin pipeline (maas-headers-guard → model-provider-resolver → stream-usage-enforcer → api-translation → apikey-injection) handles request/response processing at runtime, including multi-provider weighted routing, format translation between OpenAI, Anthropic Messages, and Vertex AI APIs, credential injection (API key, AWS SigV4, GCP OAuth2), optional usage metering via an external metering service, and NeMo Guardrails content safety checks.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| Payload Processing Service | Go ext_proc Service (controller-runtime) | Envoy external processor with pluggable IPP pipeline for request/response mutation |
| ExternalProvider Controller | Go Controller | Creates Service, ServiceEntry, DestinationRule for each external provider endpoint |
| ExternalModel Controller | Go Controller | Creates Gateway API HTTPRoutes for model-to-provider routing |
| Legacy Migration Controller | Go Controller | Migrates maas.opendatahub.io ExternalModel CRs to inference.opendatahub.io CRs |
| model-provider-resolver plugin | IPP Plugin | Resolves model names to providers via CRD watches, sets routing headers and CycleState |
| api-translation plugin | IPP Plugin | Translates request/response between OpenAI, Anthropic Messages, and Vertex AI formats |
| apikey-injection plugin | IPP Plugin | Injects API keys, AWS SigV4 signatures, or GCP OAuth2 tokens from Kubernetes Secrets |
| stream-usage-enforcer plugin | IPP Plugin | Forces stream_options.include_usage=true for streaming OpenAI requests |
| maas-headers-guard plugin | IPP Plugin | Captures and strips internal x-maas-* headers, stores in CycleState |
| external-metering plugin | IPP Plugin | Checks token balance and reports usage/error events to external metering service |
| nemo-request-guard plugin | IPP Plugin | Calls NeMo Guardrails to evaluate input content safety rails |
| nemo-response-guard plugin | IPP Plugin | Calls NeMo Guardrails to evaluate output content safety rails |
| E2E Test Image | Test Container | Ginkgo test binary + kubectl for RHOAI shift-left Jenkins pipeline |

## Sub-Component Details

### ai-gateway-payload-processing (primary)

**Dockerfile**: `Dockerfile.konflux`
**Intent**: Primary deployable service -- the ext_proc process that attaches to the AI Gateway's Envoy data plane via Istio EnvoyFilter. Deployed as a single-replica Deployment in the same namespace as the Gateway resource.
**Image**: `odh-ai-gateway-payload-processing-rhel9`
**Language**: Go 1.25
**Port**: 9004/TCP (ext_proc gRPC), 9005/TCP (health check)

#### Upstream Dependencies

| Service | Protocol | Port | Auth | Purpose |
|---------|----------|------|------|---------|
| Kubernetes API Server | HTTPS | 443/TCP | ServiceAccount Token | Watch ExternalModel, ExternalProvider, Secret CRs; create HTTPRoute, Service, ServiceEntry, DestinationRule |
| External LLM Providers | HTTPS | 443/TCP | API Key / SigV4 / OAuth2 | Forward translated inference requests |
| External Metering Service | HTTP | configurable | None | Check token balance and report usage events (optional) |
| NeMo Guardrails Service | HTTP | configurable | None | Content safety evaluation (optional) |

#### Configuration

| Name | Type | Default | Purpose |
|------|------|---------|---------|
| `GATEWAY_NAME` | env | `maas-default-gateway` | Gateway CR name for HTTPRoute parent refs |
| `GATEWAY_NAMESPACE` | env | `openshift-ingress` | Namespace of the Gateway CR |
| `-v` | flag | `3` | Log verbosity level |

### ai-gateway-payload-processing-e2e

**Dockerfile**: `Dockerfile.konflux.e2e`
**Intent**: E2E test container used by the RHOAI shift-left Jenkins pipeline. Contains a compiled Ginkgo test binary and FIPS-compliant kubectl from the OpenShift CLI image. Run via `podman run` with a mounted kubeconfig.
**Image**: `ai-gateway-payload-processing-e2e`
**Language**: Go 1.25

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|
| inference.opendatahub.io | v1alpha1 | ExternalModel | Namespaced | Maps client-facing model name to one or more external providers with weighted routing, API format, and path config |
| inference.opendatahub.io | v1alpha1 | ExternalProvider | Namespaced | Defines connection to an external LLM provider (endpoint + credentials + auth type) |

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| /healthz | GET | 9005/TCP | HTTP | None | None | Liveness probe |
| /readyz | GET | 9005/TCP | HTTP | None | None | Readiness probe |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| ext_proc (envoy.service.ext_proc.v3) | 9004/TCP | gRPC | plaintext (mesh-internal) | None (Istio mTLS) | Envoy external processing filter for request/response mutation |

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| llm-d-inference-payload-processor | v0.1.0-rc.4 | Yes | IPP framework providing ext_proc gRPC server, plugin registry, and CycleState |
| controller-runtime | v0.23.3 | Yes | Kubernetes controller framework for CRD reconciliation |
| gateway-api | v1.5.1 | Yes | Gateway API types for HTTPRoute creation |
| aws-sdk-go-v2 | v1.42.1 | Yes | AWS SigV4 request signing for Bedrock providers |
| golang.org/x/oauth2 | v0.36.0 | Yes | GCP OAuth2 token generation for Vertex AI providers |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| AI Gateway (Istio Gateway + EnvoyFilter) | EnvoyFilter ext_proc attachment | The payload processing ext_proc is wired into the Gateway's Envoy via Istio EnvoyFilter |
| Istio Service Mesh | ServiceEntry + DestinationRule CRDs | TLS origination to external provider endpoints |
| Gateway API (Gateway CR) | HTTPRoute parentRef | HTTPRoutes created per ExternalModel reference the platform Gateway |
| maas-controller | CRD status read | Reads ExternalModel.status.httpRouteName to attach policies |

## Deployment Manifests

### Helm Chart Structure

| Base / Overlay | Path | Purpose |
|----------------|------|---------|
| Chart | deploy/payload-processing/Chart.yaml | Helm chart wrapping upstream payload-processor chart |
| Values | deploy/payload-processing/values.yaml | Default configuration: image, ports, plugin pipeline, gateway ref |
| RBAC | deploy/payload-processing/templates/rbac.yaml | ClusterRole/RoleBinding for CRD access, Secret watching, networking resource creation |

### Parameterization

| Parameter | Source | Default | Purpose |
|-----------|--------|---------|---------|
| upstreamIpp.payloadProcessor.image.repository | values.yaml | odh-ai-gateway-payload-processing | Container image name |
| upstreamIpp.payloadProcessor.port | values.yaml | 9004 | ext_proc gRPC listen port |
| upstreamIpp.payloadProcessor.healthCheckPort | values.yaml | 9005 | Health check HTTP port |
| upstreamIpp.payloadProcessor.multiNamespace | values.yaml | true | Enable cross-namespace CRD watching (ClusterRole vs Role) |
| upstreamIpp.inferenceGateway.name | values.yaml | maas-default-gateway | Gateway CR name for HTTPRoute parent refs |
| upstreamIpp.payloadProcessor.customConfig.plugins | values.yaml | [plugin pipeline] | Ordered list of IPP plugins |

### Distribution Variants

| Variant | Path | Differences |
|---------|------|-------------|
| Default (RHOAI) | deploy/payload-processing/ | Multi-namespace mode with ClusterRole, Istio provider |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| payload-processing (ext_proc) | ClusterIP | 9004/TCP | 9004 | gRPC | plaintext (Istio mTLS) | None | Internal |
| payload-processing (health) | ClusterIP | 9005/TCP | 9005 | HTTP | None | None | Internal |
| {provider-name} (per ExternalProvider) | ExternalName | 443/TCP | 443 | HTTPS | TLS (via DestinationRule) | N/A | Internal (mesh egress) |

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|
| {model-name} HTTPRoute (per ExternalModel) | HTTPRoute (Gateway API) | /{namespace}/{model-name}/* | 443/TCP | HTTPS | TLS 1.2+ | SIMPLE | External (via Gateway) |

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| External LLM providers (api.openai.com, bedrock.*.amazonaws.com, etc.) | 443/TCP | HTTPS | TLS 1.2+ (SIMPLE mode via Istio DestinationRule) | API Key / SigV4 / OAuth2 (injected by apikey-injection plugin) | Forward inference requests to external providers |
| External metering service | configurable | HTTP/HTTPS | configurable | None | Token balance checks and usage event reporting |
| NeMo Guardrails service | configurable | HTTP/HTTPS | configurable | None | Content safety evaluation |
| Kubernetes API server | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Bearer Token | CRD watches, resource creation |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| {release}-externalmodels-reader | "" | secrets | get, list, watch |
| {release}-externalmodels-reader | maas.opendatahub.io | externalmodels | get, list, watch |
| {release}-externalmodels-reader | inference.opendatahub.io | externalproviders, externalmodels | get, list, watch, create, update |
| {release}-externalmodels-reader | inference.opendatahub.io | externalproviders/status, externalmodels/status | get, update, patch |
| {release}-externalmodels-reader | inference.opendatahub.io | externalproviders/finalizers, externalmodels/finalizers | update |
| {release}-externalmodels-reader | "" | services | get, list, watch, create, update, delete |
| {release}-externalmodels-reader | gateway.networking.k8s.io | httproutes | get, list, watch, create, update, delete |
| {release}-externalmodels-reader | networking.istio.io | serviceentries, destinationrules | get, list, watch, create, update, delete |

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| {release}-externalmodels-reader | cluster-wide (or release namespace) | {release}-externalmodels-reader | payload-processing-{release} |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| {provider-name}-secret (per ExternalProvider) | Opaque | Provider API keys (field: `api-key`), AWS credentials (`aws-access-key-id`, `aws-secret-access-key`, `aws-session-token`), or GCP service account JSON (`gcp-service-account-json`) | User (manual) | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| ext_proc gRPC (9004/TCP) | gRPC | Istio mTLS (PeerAuthentication) | Istio sidecar | Mesh-internal only |
| Kubernetes API | HTTPS | ServiceAccount Bearer Token | kube-apiserver | RBAC ClusterRole |
| External LLM provider APIs | POST | API Key header / AWS SigV4 / GCP OAuth2 Bearer | Provider API | Per-provider auth via apikey-injection plugin |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | `GOEXPERIMENT=strictfipsruntime`, `CGO_ENABLED=1` | Dockerfile.konflux:30-31 |
| **Linking** | Dynamic (CGO_ENABLED=1) | Dockerfile.konflux:10 (ARG default), line 30 (build) |
| **OpenSSL in image** | Yes (via UBI9 ubi-minimal base) | Dockerfile.konflux:34 |
| **OLM FIPS annotation** | Not applicable (not an OLM operator) | N/A |

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | No custom tls.Config -- TLS handled by Istio sidecar (mesh-internal) and DestinationRule (egress) | Controller code delegates TLS to Istio |
| **Crypto libraries** | crypto/sha256 (FIPS-safe via OpenSSL backend), golang.org/x/oauth2 (uses net/http default TLS) | sigv4_auth_generator.go:5, gcp_oauth2_generator.go |
| **Certificate handling** | System trust store via UBI9 base image, Istio handles mTLS certificates | Runtime environment |
| **Non-FIPS crypto risks** | None detected. SHA256 used for SigV4 body hashing and OAuth2 token cache key -- both route through OpenSSL via strictfipsruntime | sigv4_auth_generator.go:175, gcp_oauth2_generator.go:99 |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A (expected on downstream release branches) |
| **Language deps** | go.sum | Yes | go mod | go.sum |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | not present | No | Hermeto (formerly cachi2) | N/A (expected on downstream release branches) |

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | per-namespace | CRDs are Namespaced scope (config/crd/bases/*.yaml) |
| **Deployment model** | multi-tenant (single instance watches all namespaces) | values.yaml:8 (multiNamespace: true), ClusterRole in rbac.yaml |
| **Tenant identifier** | namespace name | HTTPRoute path prefix: /{namespace}/{model-name} (externalmodel/reconciler.go:279) |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | ClusterRole grants cross-namespace Secret read; Secrets filtered by label `inference.llm-d.ai/ipp-managed` | Kubernetes RBAC + label-filtered informer | Broad Secret read permission mitigated by label-filtered cache; rogue labeled Secret in any namespace could be cached |
| Data storage | Per-namespace Secrets for provider credentials | Kubernetes | No persistent data storage beyond in-memory caches |
| Network traffic | Per-ExternalModel HTTPRoutes with namespace-scoped path prefixes | Gateway API + Istio | HTTPRoutes from different namespaces share the same Gateway entry point |
| Compute & resources | Single shared Deployment (replicas=1 default) | Kubernetes | All tenants share the same ext_proc process; no per-tenant resource limits |
| Configuration & secrets | Per-namespace ExternalProvider/ExternalModel CRs and Secrets | Kubernetes | Config isolation via namespace; CRs are namespace-scoped |
| API scoping | Controller lists ExternalModels per namespace when mapping provider changes | Kubernetes (namespace-scoped list) | Cross-namespace reconciliation is by design for multi-namespace mode |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|
| In-memory infoStore (model/provider cache) | namespace/name key | Thread-safe map keyed by NamespacedName |
| In-memory secretStore (credential cache) | namespace/name key | Thread-safe nested map keyed by namespace then name |
| Gateway (Envoy data plane) | HTTPRoute path prefix per namespace | Istio EnvoyFilter routing by path and x-ipp-selected-provider header |

## Data Flows

### Flow 1: External Model Inference Request

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client | AI Gateway (Envoy) | 443/TCP | HTTPS | TLS 1.2+ | Bearer Token (via platform auth) |
| 2 | Envoy | Payload Processing ext_proc | 9004/TCP | gRPC | Istio mTLS | None |
| 3 | Payload Processing | Envoy (response) | 9004/TCP | gRPC | Istio mTLS | None |
| 4 | Envoy | External LLM Provider | 443/TCP | HTTPS | TLS 1.2+ (Istio DestinationRule SIMPLE) | API Key / SigV4 / OAuth2 (injected header) |

### Flow 2: ExternalProvider Reconciliation

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | ExternalProvider Controller | Kubernetes API | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Token |
| 2 | Controller | Kubernetes API (create Service) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Token |
| 3 | Controller | Kubernetes API (create ServiceEntry) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Token |
| 4 | Controller | Kubernetes API (create DestinationRule) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Token |

### Flow 3: ExternalModel Reconciliation

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | ExternalModel Controller | Kubernetes API (get ExternalProvider) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Token |
| 2 | Controller | Kubernetes API (create/update HTTPRoute) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount Token |

### Flow 4: External Metering (Optional)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | external-metering plugin | Metering Service (balance check) | configurable | HTTP/HTTPS | configurable | None |
| 2 | external-metering plugin | Metering Service (usage report) | configurable | HTTP/HTTPS | configurable | None |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| Istio Service Mesh | ServiceEntry + DestinationRule CRDs | 443/TCP | HTTPS | TLS (SIMPLE) | TLS origination for egress to external providers |
| Gateway API | HTTPRoute CRD creation | N/A | Kubernetes API | TLS 1.2+ | Per-model routing rules referencing platform Gateway |
| AI Gateway (Envoy) | Istio EnvoyFilter ext_proc attachment | 9004/TCP | gRPC | Istio mTLS | Request/response interception in Envoy filter chain |
| Kubernetes Secrets | Watch (label-filtered: `inference.llm-d.ai/ipp-managed`) | 443/TCP | HTTPS | TLS 1.2+ | Credential data for API key injection |
| maas.opendatahub.io ExternalModel (legacy) | CRD Watch | 443/TCP | HTTPS | TLS 1.2+ | Legacy migration: convert old CRs to new API group |
| inference.opendatahub.io ExternalProvider | CRD Watch + Status Update | 443/TCP | HTTPS | TLS 1.2+ | Provider endpoint and credential configuration |
| inference.opendatahub.io ExternalModel | CRD Watch + Status Update | 443/TCP | HTTPS | TLS 1.2+ | Model-to-provider mapping and routing config |
| maas-controller | CRD Status Read | N/A | Kubernetes API | TLS 1.2+ | Reads ExternalModel.status.httpRouteName to attach gateway policies |
| External Metering Service | REST API (balance check + usage report) | configurable | HTTP/HTTPS | configurable | Token budget enforcement and usage tracking (optional plugin) |
| NeMo Guardrails | REST API (/v1/guardrail/checks) | configurable | HTTP/HTTPS | configurable | Content safety evaluation for input/output rails (optional plugin) |
| OpenAI API | HTTPS (proxied) | 443/TCP | HTTPS | TLS 1.2+ | External LLM inference |
| Anthropic API | HTTPS (proxied) | 443/TCP | HTTPS | TLS 1.2+ | External LLM inference |
| AWS Bedrock | HTTPS (proxied + SigV4 signed) | 443/TCP | HTTPS | TLS 1.2+ | External LLM inference |
| Google Vertex AI | HTTPS (proxied + OAuth2) | 443/TCP | HTTPS | TLS 1.2+ | External LLM inference |
| Azure OpenAI | HTTPS (proxied) | 443/TCP | HTTPS | TLS 1.2+ | External LLM inference |

## Architectural Analysis

The AI Gateway Payload Processing service demonstrates a clean separation between the control plane (controller-runtime controllers managing Kubernetes resources) and the data plane (IPP plugin pipeline processing ext_proc gRPC calls). Both run in the same Go process, sharing CRD-backed state through in-memory stores. The `infoStore` in the model-provider-resolver plugin is populated by reconcilers watching ExternalModel and ExternalProvider CRDs, providing O(1) model name lookups at request time without Kubernetes API calls on the hot path. This design avoids latency spikes during inference but means the data plane operates on eventually-consistent state -- a provider deletion may take one reconcile cycle to propagate to the request path.

The plugin pipeline architecture (`maas-headers-guard → model-provider-resolver → stream-usage-enforcer → api-translation → apikey-injection`) uses a `CycleState` object (from the llm-d framework) as a per-request data bag that plugins write to and read from in sequence. This avoids direct plugin-to-plugin coupling: the model-provider-resolver writes the selected provider, auth type, endpoint, and credentials reference into CycleState; the api-translation plugin reads the input/output format pair to select the correct translator; and the apikey-injection plugin reads the Secret reference to look up credentials. The `external-metering` and `nemo-*-guard` plugins are optional additions to the pipeline, configured at the Helm values level rather than compiled in.

The multi-provider weighted routing design is noteworthy: an ExternalModel can reference multiple ExternalProviders with weights (0-100), and the model-provider-resolver plugin performs weighted random selection at request time. The selected provider name is written to the `x-ipp-selected-provider` header, and the ExternalModel controller creates per-provider HTTPRoute rules that match on this header, ensuring the Gateway routes to the correct backend Service. A fallback rule (no header match) routes to the first provider. This header-based routing means the ext_proc and the Gateway data plane must agree on the provider selection for each request -- a subtle correctness requirement.

The Istio networking integration follows a consistent pattern: each ExternalProvider creates three resources -- an ExternalName Service (for Kubernetes DNS resolution), a ServiceEntry (for Istio mesh-external resolution), and a DestinationRule (for TLS origination with SIMPLE mode). This triple ensures that traffic from the Envoy sidecar to external provider endpoints is properly encrypted without requiring the application to handle TLS directly. The `crypto/sha256` usage in the SigV4 auth generator and GCP OAuth2 token cache is FIPS-safe because the `strictfipsruntime` build experiment routes all crypto operations through OpenSSL.

The legacy migration controller is a pragmatic backward-compatibility mechanism: it watches the old `maas.opendatahub.io/v1alpha1` ExternalModel CRDs and creates corresponding `inference.opendatahub.io/v1alpha1` ExternalProvider + ExternalModel CRs, with owner references ensuring cleanup on legacy CR deletion. The migration controller gracefully skips registration if the legacy CRD is not installed on the cluster, making it safe to deploy on clusters that never had the old API group.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 6d2dd6b | 2026 | fix: ext_proc-driven multi-provider routing, weight=0, user-agent metering |
| 519c8b3 | 2026 | feat: add "none" auth type for hub-to-spoke mTLS routing |
| 68b2a2f | 2026 | fix: align ExternalModel BBR auth, body rewrite, and header match with /v1/models identity |
| efde850 | 2026 | feat: support {model} as reserved placeholder in path |
| b2afb1e | 2026 | feat: add LLMISvc support in model-provider-resolver |
| 7a5e31e | 2026 | feat: report provider error responses as metering events |
| a434264 | 2026 | feat: implement Vertex Anthropic translator for Claude on Vertex AI |
| 81b7bc3 | 2026 | feat: restrict Secret informer to bbr-managed labeled Secrets only |
| a20917b | 2026 | fix: recover usage from response payloads split across Envoy chunk boundaries |
| b3343c0 | 2026 | feat: streaming token metering via ChunkProcessor |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| README.md | 1-68 | Purpose, Deployment Manifests |
| go.mod | 1-128 | Metadata, Dependencies |
| Dockerfile.konflux | 1-50 | Architecture Components, FIPS Compliance, Sub-Component Details |
| Dockerfile.konflux.e2e | 1-53 | Sub-Component Details |
| Makefile | 1-246 | Metadata, FIPS Compliance, Build Hermeticity |
| cmd/main.go | 1-51 | Architecture Components, Purpose |
| cmd/controllers.go | 1-93 | Architecture Components, Integration Points |
| api/inference/v1alpha1/groupversion_info.go | 1-33 | APIs Exposed (CRDs) |
| api/inference/v1alpha1/externalmodel_types.go | 1-144 | APIs Exposed (CRDs), Multi-Tenancy |
| api/inference/v1alpha1/externalprovider_types.go | 1-93 | APIs Exposed (CRDs), Security (Secrets) |
| api/inference/v1alpha1/common_types.go | 1-43 | APIs Exposed (CRDs), Security |
| pkg/controller/externalmodel/reconciler.go | 1-397 | Architecture Components, Network Architecture, Data Flows, Multi-Tenancy |
| pkg/controller/externalprovider/reconciler.go | 1-319 | Architecture Components, Network Architecture, Egress, Integration Points |
| pkg/controller/legacymigration/reconciler.go | 1-223 | Architecture Components, Integration Points |
| pkg/controller/common/constants.go | 1-29 | Network Architecture, Configuration |
| pkg/controller/common/path.go | 1-55 | Architecture Components |
| pkg/plugins/plugins.go | 1-40 | Architecture Components |
| pkg/plugins/model-provider-resolver/plugin.go | 1-253 | Architecture Components, Data Flows |
| pkg/plugins/model-provider-resolver/store.go | 1-118 | Multi-Tenancy (Shared Services) |
| pkg/plugins/model-provider-resolver/external_model_reconciler.go | 1-148 | Architecture Components, Data Flows |
| pkg/plugins/model-provider-resolver/external_provider_reconciler.go | 1-101 | Architecture Components, Integration Points |
| pkg/plugins/api-translation/plugin.go | 1-240 | Architecture Components, Data Flows |
| pkg/plugins/api-translation/translator/translator.go | 1-42 | Architecture Components |
| pkg/plugins/apikey-injection/plugin.go | 1-235 | Architecture Components, Security, Multi-Tenancy |
| pkg/plugins/apikey-injection/reconciler.go | 1-74 | Security (RBAC, Secrets) |
| pkg/plugins/apikey-injection/store.go | 1-97 | Multi-Tenancy (Shared Services) |
| pkg/plugins/apikey-injection/auth-generator/auth_generator.go | 1-35 | Architecture Components |
| pkg/plugins/apikey-injection/auth-generator/apikey_auth_generator.go | 1-98 | Security (Auth), Data Flows |
| pkg/plugins/apikey-injection/auth-generator/sigv4_auth_generator.go | 1-179 | Security (Auth, FIPS), Egress, Dependencies |
| pkg/plugins/apikey-injection/auth-generator/gcp_oauth2_generator.go | 1-145 | Security (Auth, FIPS), Egress, Dependencies |
| pkg/plugins/stream-usage-enforcer/plugin.go | 1-83 | Architecture Components |
| pkg/plugins/maas-headers-guard/plugin.go | 1-77 | Architecture Components |
| pkg/plugins/external-metering/plugin.go | 1-608 | Architecture Components, Data Flows, Integration Points |
| pkg/plugins/external-metering/client.go | 1-109 | Egress, Integration Points |
| pkg/plugins/nemo/nemo_guard_base.go | 1-149 | Architecture Components, Integration Points |
| pkg/plugins/nemo/request_guard.go | 1-196 | Architecture Components |
| pkg/plugins/nemo/response_guard.go | 1-202 | Architecture Components |
| pkg/plugins/common/state/state-keys.go | 1-40 | Data Flows |
| pkg/plugins/common/apiformat/apiformat.go | 1-27 | Architecture Components |
| pkg/plugins/common/auth/auth.go | 1-32 | Security (Auth) |
| pkg/plugins/common/provider/provider.go | 1-30 | Integration Points |
| config/crd/bases/inference.opendatahub.io_externalmodels.yaml | 1-252 | APIs Exposed (CRDs) |
| config/crd/bases/inference.opendatahub.io_externalproviders.yaml | 1-192 | APIs Exposed (CRDs) |
| deploy/payload-processing/Chart.yaml | 1-16 | Deployment Manifests, Dependencies |
| deploy/payload-processing/values.yaml | 1-44 | Deployment Manifests, Configuration, Network Architecture |
| deploy/payload-processing/templates/rbac.yaml | 1-100 | Security (RBAC) |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `Dockerfile*konflux*` (find) | Dockerfile.konflux, Dockerfile.konflux.e2e | Architecture Components, FIPS Compliance |
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` | Dockerfile.konflux, Dockerfile.konflux.e2e, Makefile | FIPS Compliance |
| `rpms.lock.yaml` / `uv.lock` / lock files (find) | None found | Build Hermeticity |
| `cachi2\|hermeto\|REMOTE_SOURCES` | None found | Build Hermeticity |
| `*.yaml` (find, excluding .git/test/.tekton/.github) | 10 files | Deployment Manifests, Network Architecture |
| `go.mod` / `pyproject.toml` / `Chart.yaml` (find) | go.mod, go.sum, Chart.yaml | Metadata, Dependencies |
| `component-map.json` (path check) | Not found | Provenance |
| git metadata commands | N/A | Metadata, Provenance, Recent Changes |

### Summary

- **Total files read**: 44
- **Total lines referenced**: ~5,500
- **Coverage**: All sections have direct source backing. Provenance section based on local_analysis (no component-map.json available). Health check endpoints inferred from upstream llm-d framework defaults and values.yaml healthCheckPort configuration.

---
*Generated in 5m 6s (307s total)*
