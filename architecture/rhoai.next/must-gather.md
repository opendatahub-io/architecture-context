# Component: must-gather

## Metadata

- **Repository**: https://github.com/red-hat-data-services/must-gather
- **Version**: 9922e69 (main)
- **Distribution**: RHOAI
- **Languages**: Bash
- **Deployment Type**: Diagnostic Utility Image
- **Generated By**: Claude Opus 4.6 on 2026-07-22

## Provenance

### Repo Lineage

| Role | Repository | Sync Mechanism | Sync Branch | Sync Workflows | Detection Method |
|------|-----------|----------------|-------------|----------------|------------------|
| Downstream | https://github.com/red-hat-data-services/must-gather | -- | main | -- | local_analysis |

_No upstream or midstream repository detected. This is a downstream-only component with no external sync workflows. The repository is owned by the `red-hat-data-services` organization and does not fork from an upstream community project._

### Aliases

| Current Name | Previous Name | Type | Context |
|--------------|--------------|------|---------|

## Purpose

**Short**: Diagnostic data collection tool that gathers logs, resource definitions, and cluster state from RHOAI and RHAII (LLM-D) deployments for troubleshooting.

**Detailed**: The `must-gather` component is a container image used with `oc adm must-gather` (on OpenShift) or as a Kubernetes Job (on non-OpenShift platforms like AKS, EKS, CKS) to collect diagnostic information from Red Hat OpenShift AI clusters. It extends the standard OpenShift must-gather base image with RHOAI-specific collection scripts that know how to enumerate and capture resources for every RHOAI component -- dashboard, KServe, data science pipelines, KubeRay, Kueue, training operator, model registry, TrustyAI, Feast, LlamaStack, MLflow, Spark, AI Gateway, MCP Lifecycle Operator, and LLM-D.

The tool detects the Kubernetes distribution at runtime (OpenShift, AKS, EKS, CKS) and adjusts its collection strategy accordingly. On OpenShift it uses `oc adm inspect` for namespace-level data collection; on non-OpenShift platforms it implements a `kubectl`-based equivalent (`kubectl_inspect`) that auto-discovers all API resources, collects pod logs, and gathers cluster-scoped resources. Component-specific gathering can be invoked in isolation via the `COMPONENT` environment variable, or the default `all` mode runs all component gatherers in parallel. The tool also collects Helm release information, Gateway API resources, Istio/Sail operator state, cert-manager certificates, and observability stack configuration.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| odh-must-gather | Container Image | Diagnostic data collection from RHOAI/RHAII clusters using shell scripts |
| gather.sh | Shell Script (Entrypoint) | Main orchestrator that detects K8s distribution, sets up namespaces, and dispatches component-specific gatherers |
| common.sh | Shell Script (Library) | Shared functions for namespace inspection, resource collection, version detection, and Helm release gathering |
| xks_util.sh | Shell Script (Library) | Kubernetes distribution detection (OCP, AKS, EKS, CKS) and kubectl-based resource inspection for non-OpenShift platforms |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|

_No CRDs defined. This is a diagnostic utility, not an operator._

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|

_No HTTP endpoints exposed. The container runs as a batch job, not a service._

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| ose-must-gather-rhel9 | v4.20 | Yes | Base image providing `oc adm inspect` tooling and OpenShift must-gather framework |
| ose-cli-rhel9 | v4.20 | Yes | Provides `kubectl` binary (v1.33.3) via multi-stage build for xKS environments |
| helm | v4.1.4 | Yes | Helm CLI for collecting Helm release values and manifests; shipped as Git LFS artifact |
| oc CLI | (from base image) | Yes | OpenShift CLI used for `oc adm inspect` on OpenShift clusters |
| kubectl | v1.33.3 / v1.34.7 | Yes | Kubernetes CLI used on non-OpenShift (xKS) platforms |

### Internal Platform Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| rhods-operator | Resource collection (CRD reads) | Collects DSCInitialization, DataScienceCluster, Auth, Monitoring, GatewayConfig CRs |
| KServe | Resource collection (CRD reads) | Collects InferenceService, InferenceGraph, ServingRuntime, LLMInferenceService CRs |
| Data Science Pipelines | Resource collection (CRD reads) | Collects DataSciencePipelinesApplication, Argo Workflow CRs |
| KubeRay | Resource collection (CRD reads) | Collects RayCluster, RayJob, RayService CRs |
| Kueue | Resource collection (CRD reads) | Collects ClusterQueue, LocalQueue, Workload, ResourceFlavor CRs |
| Training Operator | Resource collection (CRD reads) | Collects PyTorchJob, TrainJob, TrainingRuntime CRs (v1 and v2) |
| Model Registry | Resource collection (CRD reads) | Collects ModelRegistry CRs |
| TrustyAI | Resource collection (CRD reads) | Collects LMEvalJob, TrustyAIService, GuardrailsOrchestrator, EvalHub CRs |
| Dashboard | Resource collection (CRD reads) | Collects ODHDashboardConfig, AcceleratorProfile, HardwareProfile CRs |
| Feast Operator | Resource collection (CRD reads) | Collects FeatureStore CRs |
| LlamaStack Operator | Resource collection (CRD reads) | Collects LlamaStackDistribution CRs |
| MLflow Operator | Resource collection (CRD reads) | Collects MLflow CRs |
| Spark Operator | Resource collection (CRD reads) | Collects SparkApplication, ScheduledSparkApplication, SparkConnect CRs |
| AI Gateway | Resource collection (CRD reads) | Collects AITenant, MaaSSubscription, ExternalModel, ExternalProvider, LLMBatchGateway CRs |
| MCP Lifecycle Operator | Resource collection (CRD reads) | Collects MCPServer CRs |
| Gateway API | Resource collection (CRD reads) | Collects Gateway, HTTPRoute, GRPCRoute, GatewayClass, ReferenceGrant CRs |
| Istio / Sail Operator | Resource collection (CRD reads) | Collects VirtualService, DestinationRule, EnvoyFilter, AuthorizationPolicy, PeerAuthentication CRs |
| cert-manager | Resource collection (CRD reads) | Collects Certificate, Issuer, ClusterIssuer CRs |
| LWS (LeaderWorkerSet) | Resource collection (CRD reads) | Collects LeaderWorkerSet, LeaderWorkerSetOperator CRs |
| Prometheus Operator | Resource collection (CRD reads) | Collects ServiceMonitor, PodMonitor, PrometheusRule, Alertmanager CRs |
| KEDA | Resource collection (CRD reads) | Collects ScaledObject, ScaledJob, TriggerAuthentication CRs (for WVA) |
| Authorino | Resource collection (CRD reads) | Collects AuthConfig, AuthPolicy CRs |
| Kuadrant | Resource collection (CRD reads) | Collects RateLimitPolicy, TokenRateLimitPolicy CRs |

## Deployment Manifests

_No kustomize manifests or Helm charts. The component is deployed as a container image consumed by `oc adm must-gather` or a Kubernetes Job. The Tekton pipeline in `.tekton/` handles CI builds via Konflux._

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|

_No services exposed. The must-gather container runs as a batch process, not a long-running service._

### Ingress

| Name | Type | Hosts | Port | Protocol | Encryption | TLS Mode | Exposure |
|------|------|-------|------|----------|------------|----------|----------|

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| Kubernetes API server | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token | All `oc`/`kubectl` commands to list/get resources, inspect namespaces, collect pod logs |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|

_The must-gather image relies on the RBAC context of `oc adm must-gather` (which uses a cluster-admin-equivalent ServiceAccount from the must-gather plugin). For xKS deployments, the README provides a ClusterRole `must-gather-reader` granting `get`, `list`, `watch` on all API groups and resources._

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| must-gather-reader-binding (xKS only) | cluster-scoped | must-gather-reader | must-gather-sa (in must-gather namespace) |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| redhat-pull-secret (xKS only) | kubernetes.io/dockerconfigjson | Pull must-gather image from registry.redhat.io | Manual (user-created) | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| Kubernetes API | GET, LIST | ServiceAccount bearer token | Kubernetes API server | ClusterRole with read-only access to all resources |

### FIPS Compliance

#### Build-Time FIPS (check-payload gate)

| Aspect | Value | Source |
|--------|-------|--------|
| **Build flags** | N/A -- no compiled binaries (shell scripts only) | Dockerfile.konflux |
| **Linking** | N/A -- no compiled binaries | Dockerfile.konflux |
| **OpenSSL in image** | Yes (via UBI/ose-must-gather-rhel9 base) | Dockerfile.konflux:3 |
| **OLM FIPS annotation** | N/A -- not an OLM-managed operator | -- |

_The must-gather image contains only shell scripts and pre-built CLI binaries (`oc`, `kubectl`, `helm`) from the base image and build stages. The Go binaries (`oc`, `kubectl`) are provided by Red Hat's ose-cli-rhel9 image, which is built with FIPS compliance by the OpenShift team. The `helm` binary is checked in via Git LFS and its FIPS compliance status depends on how it was built externally._

#### Application-Level Crypto

| Aspect | Value | Source |
|--------|-------|--------|
| **TLS configuration** | Inherited from `oc`/`kubectl` CLI tools | N/A (shell scripts only) |
| **Crypto libraries** | None -- shell scripts do not perform cryptographic operations | collection-scripts/*.sh |
| **Certificate handling** | Inherited from `oc`/`kubectl` (uses system trust store) | N/A |
| **Non-FIPS crypto risks** | None detected -- no direct crypto usage in shell scripts | collection-scripts/*.sh |

### Build Hermeticity

| Layer | Lock File | Present | Tool | Source |
|-------|-----------|---------|------|--------|
| **OS packages (RPM)** | rpms.lock.yaml | No | rpm-lockfile-prototype | N/A |
| **Language deps** | N/A | N/A | N/A | N/A -- no language-level dependencies (shell scripts only) |
| **Artifacts** | artifacts.lock.yaml | No | Hermeto | N/A |
| **Hermeto prefetch** | Not present | No | Hermeto (formerly cachi2) | Dockerfile.konflux |

_The Tekton pipeline declares `hermetic: true` (.tekton/odh-must-gather-pull-request.yaml:49), meaning Konflux builds run in a network-isolated environment. However, there are no lock files present on this branch. The `helm` binary is checked in via Git LFS rather than downloaded at build time, which supports hermeticity. The `kubectl` binary is copied from the ose-cli-rhel9 builder stage, also supporting hermeticity. Since no packages are downloaded at build time (the Konflux Dockerfile only COPYs files), the build is effectively hermetic despite having no explicit lock files._

## Multi-Tenancy

### Tenant Model

| Aspect | Value | Source |
|--------|-------|--------|
| **Tenant boundary** | N/A | Diagnostic utility, not a tenant-scoped service |
| **Deployment model** | Single-use batch job | README.md:34-38 |
| **Tenant identifier** | N/A | No tenant concept -- operates cluster-wide with admin privileges |

### Isolation Mechanisms

| Dimension | Mechanism | Enforced By | Gaps / Risks |
|-----------|-----------|-------------|--------------|
| Auth & AuthZ | Cluster-admin ServiceAccount (OpenShift) or cluster-wide ClusterRole (xKS) | Kubernetes RBAC | By design, must-gather requires read access to all cluster resources including potentially sensitive data |
| Data storage | Output written to local filesystem; copied out via `oc adm must-gather` or `kubectl cp` | Host filesystem / OpenShift must-gather plugin | Collected data may contain secrets or sensitive configuration from any namespace |
| Network traffic | N/A -- read-only Kubernetes API calls | Kubernetes API server | No network isolation constraints |
| Compute & resources | None -- runs as a Job with no resource limits defined | Kubernetes scheduler | Large clusters may cause high API server load during collection |
| Configuration & secrets | Namespace variables configurable via env vars | Bash script env vars | No default scoping -- collects from all namespaces by default |
| API scoping | Cluster-wide list/get on all resources | Kubernetes RBAC | Expected behavior for diagnostics; COMPONENT env var allows scoping to single component |

### Shared Services

| Shared Service | Tenant Boundary | Isolation Mechanism |
|----------------|----------------|---------------------|

## Data Flows

### Flow 1: OpenShift Must-Gather Collection

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Cluster admin | must-gather pod (via `oc adm must-gather`) | -- | oc CLI | TLS 1.2+ | kubeconfig |
| 2 | gather.sh | Kubernetes API server | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token |
| 3 | gather.sh (parallel) | Component gatherer scripts | -- | Local exec | -- | -- |
| 4 | Component scripts | Kubernetes API (via `oc adm inspect`) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token |
| 5 | must-gather pod | Local filesystem (`/must-gather/`) | -- | Filesystem | -- | -- |
| 6 | `oc adm must-gather` | User workstation (rsync) | -- | oc CLI | TLS 1.2+ | kubeconfig |

### Flow 2: xKS (Non-OpenShift) Must-Gather Collection

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Cluster admin | must-gather Job (via `kubectl apply`) | -- | kubectl CLI | TLS 1.2+ | kubeconfig |
| 2 | gather.sh | Kubernetes API server | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token |
| 3 | gather.sh | LLM-D gatherer + dependency scripts | -- | Local exec | -- | -- |
| 4 | xks_util.sh (kubectl_inspect) | Kubernetes API (via `kubectl`) | 443/TCP | HTTPS | TLS 1.2+ | ServiceAccount token |
| 5 | must-gather pod | Local filesystem (`/tmp/must-gather/`) | -- | Filesystem | -- | -- |
| 6 | `kubectl cp` | User workstation | -- | kubectl CLI | TLS 1.2+ | kubeconfig |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| Kubernetes API server | REST (list/get/watch) | 443/TCP | HTTPS | TLS 1.2+ | All resource enumeration and log collection |
| OpenShift API (infrastructures.config.openshift.io) | REST (get) | 443/TCP | HTTPS | TLS 1.2+ | Kubernetes distribution detection (OCP vs xKS) |
| OLM (clusterserviceversions) | REST (get) | 443/TCP | HTTPS | TLS 1.2+ | RHOAI version detection from rhods-operator CSV |
| Helm (Tiller-less) | CLI | -- | -- | -- | Collect Helm release values and manifests for RHOAI/RHAII |
| rhods-operator namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect operator pod logs, deployments, events |
| redhat-ods-applications namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect application pod logs, deployments, events |
| redhat-ods-monitoring namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect monitoring stack resources |
| rhods-notebooks namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect notebook pod logs and resources |
| rhoai-model-registries namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect model registry resources |
| openshift-ingress namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect Gateway/ingress controller resources |
| kuadrant-system namespace | `oc adm inspect` | 443/TCP | HTTPS | TLS 1.2+ | Collect Kuadrant rate limiting resources |
| rhai-gitops namespace | `oc adm inspect` / Helm | 443/TCP | HTTPS | TLS 1.2+ | Collect Helm chart namespace and release info |
| rhai-cloudmanager-system namespace | kubectl_inspect | 443/TCP | HTTPS | TLS 1.2+ | Collect cloud manager resources (xKS) |
| cert-manager namespace | kubectl_inspect | 443/TCP | HTTPS | TLS 1.2+ | Collect certificate management resources |
| istio-system namespace | kubectl_inspect | 443/TCP | HTTPS | TLS 1.2+ | Collect Istio/Sail operator resources |
| monitoring namespace | kubectl_inspect | 443/TCP | HTTPS | TLS 1.2+ | Collect Prometheus, Alertmanager resources (xKS self-hosted) |
| kube-system namespace (AKS) | kubectl_inspect | 443/TCP | HTTPS | TLS 1.2+ | Collect Azure Managed Prometheus (ama-metrics) pods and logs |
| openshift-lws-operator namespace | kubectl_inspect | 443/TCP | HTTPS | TLS 1.2+ | Collect LeaderWorkerSet operator resources |
| batch-gateway namespace | kubectl_inspect / Helm | 443/TCP | HTTPS | TLS 1.2+ | Collect batch gateway deployment and Helm release (optional) |

## Architectural Analysis

The must-gather component follows a hierarchical dispatch pattern where the main `gather.sh` script acts as an orchestrator. It first detects the Kubernetes distribution using node metadata heuristics (kernel version for CKS, provider ID prefix for AKS/EKS, OpenShift infrastructure API for OCP), then branches between OpenShift-specific and xKS-specific collection strategies. On OpenShift, it leverages the built-in `oc adm inspect` command which handles namespace-level resource serialization, pod log collection, and event gathering in a single operation. On non-OpenShift platforms, the `xks_util.sh` library implements an equivalent `kubectl_inspect` function that auto-discovers all API resource types via `kubectl api-resources` and iterates over them, collecting YAML representations and pod logs in parallel.

Component-specific collection is modular: each component has a dedicated `gather_*.sh` script that defines the CRD resource types it cares about, uses `get_all_namespace` to discover which namespaces contain those resources, and then calls `run_mustgather` (OpenShift) or `run_k8sgather` (xKS) to collect them. When no specific component is selected, all component gatherers run in parallel as background processes with PID tracking for error reporting. This parallel execution model significantly reduces collection time on large clusters. The `common.sh` library also collects a default set of Gateway API and Istio resources (`gateways`, `httproutes`, `grpcroutes`, `envoyfilters`, `destinationrules`) for every namespace inspected, reflecting the RHOAI 3.x ingress architecture.

The LLM-D collection path (`llm-d/gather_llmd.sh`) is notably more complex than other component gatherers. It runs dependency collection scripts (cert-manager, Sail/Istio, LeaderWorkerSet) in parallel, collects Gateway API Inference Extension resources (`inferencepools`, `inferenceobjectives`), and supports optional sub-components (WVA autoscaler via `ENABLE_WVA`, batch gateway via `ENABLE_BATCH_GATEWAY`). The observability collection (`gather_o11y.sh`) adapts to the monitoring stack -- Azure Managed Prometheus on AKS versus self-hosted kube-prometheus-stack elsewhere. This xKS awareness makes must-gather the most cross-platform component in the RHOAI ecosystem, supporting OpenShift, AKS, EKS, and CKS from a single codebase.

A significant design decision is the inclusion of `helm` and `kubectl` binaries directly in the image. The `helm` binary (v4.1.4) is checked into the repository via Git LFS, making the build hermetic but coupling the tool version to the repository. The `kubectl` binary (v1.33.3) is extracted from Red Hat's `ose-cli-rhel9` image in a multi-stage build for the Konflux Dockerfile, while the upstream Containerfile downloads it from `dl.k8s.io` at build time. The Tekton pipeline configuration builds multi-architecture images (x86_64, ppc64le, s390x, arm64), ensuring must-gather works across all supported RHOAI architectures.

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| 9922e69 | 2026-07-22 | Consolidated MaaS CRDs into AI Gateway must-gather collection |
| 90e3f61 | 2026-07-22 | Use full API group for resource references |
| 61ead8a | 2026-07-22 | Onboard MCP Lifecycle Operator |
| 32e6755 | 2026-07-22 | Add support for new AI Gateway with batch-gateway |
| 36dd46d | 2026-07-22 | Add KServes CRD to serving must-gather |
| 2742df8 | 2026-07-22 | Fix gather script rename -- oc adm must-gather expects /usr/bin/gather |
| 934829c | 2026-07-22 | Onboard llm-d batch gateway |
| 437940c | 2026-07-22 | Add EKS support for llm-d path |
| 09f951b | 2026-07-22 | Set rhoai as default Helm release name for OpenShift, rhaii for xKS |
| 13dca25 | 2026-07-22 | Add support to get Helm values and manifests |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| Dockerfile.konflux | 1-23 | Metadata, Architecture Components, Dependencies, Security (FIPS, Hermeticity) |
| Containerfile | 1-24 | Architecture Components, Dependencies |
| Makefile | 1-16 | Metadata |
| README.md | 1-340 | Purpose, Dependencies, Security (RBAC), Data Flows, Multi-Tenancy, Integration Points |
| collection-scripts/gather.sh | 1-247 | Architecture Components, Data Flows, Integration Points, Architectural Analysis |
| collection-scripts/common.sh | 1-140 | Architecture Components, Integration Points, Architectural Analysis |
| collection-scripts/gather_serving.sh | 1-19 | Integration Points (KServe, Authorino, NIM, LLM-D, Kuadrant) |
| collection-scripts/gather_dashboard.sh | 1-10 | Integration Points (Dashboard) |
| collection-scripts/gather_data_science_pipelines.sh | 1-15 | Integration Points (DSP, Argo) |
| collection-scripts/gather_kfto.sh | 1-14 | Integration Points (Training Operator) |
| collection-scripts/gather_kueue.sh | 1-10 | Integration Points (Kueue) |
| collection-scripts/gather_kuberay.sh | 1-10 | Integration Points (KubeRay) |
| collection-scripts/gather_notebooks.sh | 1-9 | Integration Points (Workbench) |
| collection-scripts/gather_mr.sh | 1-10 | Integration Points (Model Registry) |
| collection-scripts/gather_trustyai.sh | 1-10 | Integration Points (TrustyAI) |
| collection-scripts/gather_feastoperator.sh | 1-10 | Integration Points (Feast) |
| collection-scripts/gather_lls.sh | 1-10 | Integration Points (LlamaStack) |
| collection-scripts/gather_mlflow.sh | 1-10 | Integration Points (MLflow) |
| collection-scripts/gather_sparkoperator.sh | 1-10 | Integration Points (Spark) |
| collection-scripts/gather_aigateway.sh | 1-30 | Integration Points (AI Gateway, MaaS, Inference) |
| collection-scripts/gather_mcp_lifecycle_operator.sh | 1-10 | Integration Points (MCP Lifecycle Operator) |
| collection-scripts/llm-d/gather_llmd.sh | 1-98 | Architecture Components, Integration Points, Data Flows, Architectural Analysis |
| collection-scripts/llm-d/xks_util.sh | 1-180 | Architecture Components, Architectural Analysis |
| collection-scripts/llm-d/gather_cluster.sh | 1-35 | Integration Points |
| collection-scripts/llm-d/gather_o11y.sh | 1-79 | Integration Points (Prometheus, AKS Managed Prometheus) |
| collection-scripts/llm-d/gather_wva.sh | 1-39 | Integration Points (KEDA, WVA) |
| collection-scripts/llm-d/gather_batch_gateway.sh | 1-27 | Integration Points (batch-gateway) |
| collection-scripts/llm-d/dependency/cert-manager.sh | 1-33 | Integration Points (cert-manager) |
| collection-scripts/llm-d/dependency/lws.sh | 1-24 | Integration Points (LeaderWorkerSet) |
| collection-scripts/llm-d/dependency/sail.sh | 1-58 | Integration Points (Istio, Sail Operator) |
| .github/workflows/linter.yml | 1-34 | Metadata |
| .tekton/odh-must-gather-pull-request.yaml | 1-69 | Security (Build Hermeticity), Metadata |
| .tekton/README.md | 1-40 | Architectural Analysis |
| .gitignore | 1 | Metadata |
| bin/helm | 1-3 | Dependencies (Git LFS artifact) |
| component-architecture.json | 1-105 | Metadata, Dependencies |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| `GOEXPERIMENT\|strictfipsruntime\|CGO_ENABLED` in Dockerfile*/Containerfile*/Makefile*/*.sh | No matches | Security (FIPS Compliance) |
| `rpms.lock.yaml` / lock files / `cachi2\|hermeto` | No matches | Security (Build Hermeticity) |
| `Dockerfile*konflux*` / `Containerfile*konflux*` | Dockerfile.konflux | Architecture Components |

### Summary

- **Total files read**: 35
- **Total lines referenced**: ~1,400
- **Coverage**: All sections have direct source backing. Provenance section based on local_analysis (no component-map.json available). FIPS assessment for the `helm` binary is noted as externally-sourced and unverifiable from this repo alone.

---
*Generated in 4m 17s (258s total)*
