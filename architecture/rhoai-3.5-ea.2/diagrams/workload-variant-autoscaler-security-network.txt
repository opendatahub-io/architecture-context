┌──────────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted - Kubernetes Network)                            │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  WVA CONTROLLER POD                                                    │  │
│  │  Namespace: wva-system (k8s) / <operator-namespace> (OpenShift)       │  │
│  │  ServiceAccount: controller-manager                                    │  │
│  │  Image: ghcr.io/llm-d/llm-d-workload-variant-autoscaler:v0.7.0       │  │
│  │                                                                        │  │
│  │  Exposed Ports:                                                        │  │
│  │  ┌──────────────────────────────────────────────────────────────────┐  │  │
│  │  │  Port: 8443/TCP                                                  │  │  │
│  │  │  Protocol: HTTPS                                                 │  │  │
│  │  │  Encryption: TLS 1.2+ (self-signed certificate)                  │  │  │
│  │  │  Auth: Bearer Token (SA token via TokenReview)                   │  │  │
│  │  │  Purpose: Prometheus metrics endpoint (/metrics)                 │  │  │
│  │  │  Consumers: Prometheus/Thanos scraper                            │  │  │
│  │  └──────────────────────────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────────────────────────────┐  │  │
│  │  │  Port: 8081/TCP                                                  │  │  │
│  │  │  Protocol: HTTP                                                  │  │  │
│  │  │  Encryption: None                                                │  │  │
│  │  │  Auth: None                                                      │  │  │
│  │  │  Purpose: Health probes (/healthz, /readyz)                      │  │  │
│  │  │  Consumers: kubelet                                              │  │  │
│  │  └──────────────────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│         │                          │                           │             │
│         │ EGRESS                   │ EGRESS                    │ EGRESS      │
│         ▼                          ▼                           ▼             │
│  ┌─────────────────┐  ┌────────────────────────┐  ┌───────────────────────┐ │
│  │ Prometheus /     │  │ Kubernetes API Server   │  │ vLLM Pods (fallback) │ │
│  │ Thanos Querier   │  │                         │  │                       │ │
│  │                  │  │ Port: 6443/TCP           │  │ Port: 8200/TCP        │ │
│  │ Port: 9091/TCP   │  │ Protocol: HTTPS          │  │ Protocol: HTTP        │ │
│  │ (OpenShift) or   │  │ Encryption: TLS 1.2+     │  │ Encryption: None      │ │
│  │ 9090/TCP (k8s)   │  │ Auth: ServiceAccount     │  │ Auth: Bearer Token    │ │
│  │ Protocol: HTTPS   │  │       Token              │  │   (epp-metrics-token, │ │
│  │ Encryption:       │  │ Purpose: Resource CRUD,  │  │    optional)          │ │
│  │   TLS 1.2+        │  │   leader election,       │  │ Purpose: Direct EPP   │ │
│  │ Auth: Bearer      │  │   scale subresource      │  │   pod metrics scraping│ │
│  │   Token (SA)      │  │   updates                │  │   fallback            │ │
│  │ Purpose: Query    │  │                          │  │                       │ │
│  │   vLLM & EPP      │  └────────────────────────┘  └───────────────────────┘ │
│  │   metrics         │                                                        │
│  └─────────────────┘                                                         │
│         ▲                                                                     │
│         │ INGRESS (scraping)                                                  │
│         │                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │  METRICS CONSUMERS                                                      │  │
│  │                                                                          │  │
│  │  [Prometheus / Thanos] ──scrapes──▶ WVA /metrics (8443/TCP HTTPS)       │  │
│  │  [HPA / KEDA] ──reads external metric──▶ Prometheus ──▶ scale decision  │  │
│  │  [HPA / KEDA] ──PATCH scale──▶ Kubernetes API (6443/TCP)               │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │  METRICS SOURCES (scraped by Prometheus)                                │  │
│  │                                                                          │  │
│  │  [vLLM Servers] ─HTTP/8200─▶ Prometheus                                │  │
│  │    Metrics: KV cache utilization, queue depth, token counts,             │  │
│  │             latency histograms, throughput, cache config                 │  │
│  │                                                                          │  │
│  │  [EPP (Gateway API Inference Extension)] ─HTTP─▶ Prometheus             │  │
│  │    Metrics: Scheduler dispatch rate, flow control queue size/bytes       │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │  CRD WATCH SOURCES (Kubernetes API)                                     │  │
│  │                                                                          │  │
│  │  [HPA] ◄── annotation: llm-d.ai/managed=true                           │  │
│  │  [KEDA ScaledObject] ◄── annotation: llm-d.ai/managed=true (optional)  │  │
│  │  [InferencePool] ◄── endpoint pool data, scale-from-zero queue          │  │
│  │  [VariantAutoscaling CR] ◄── deprecated CRD                            │  │
│  │  [ConfigMaps] ◄── global defaults + namespace overrides                 │  │
│  │  [Nodes] ◄── GPU inventory (nvidia/amd/intel labels)                    │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────────┘

No external ingress (no Routes, Ingresses, or LoadBalancer services).

RBAC Summary:
─────────────
ClusterRole: manager-role
  ServiceAccount: controller-manager
  Permissions:
  - core: configmaps (get,update,list,watch), events (create,patch),
    namespaces (get,list,watch), nodes/nodes/status (get,list,watch,update,patch),
    pods (get,list,watch), secrets (get,list,watch), services (get,list,watch)
  - apps: deployments/deployments/scale (get,list,watch,update,patch),
    replicasets (get,list,watch), statefulsets (get,list,watch)
  - autoscaling: horizontalpodautoscalers (get,list,watch,patch,update)
  - keda.sh: scaledobjects (get,list,watch)
  - inference.networking: inferencepools (get,list,watch)
  - leaderworkerset.x-k8s.io: leaderworkersets/leaderworkersets/scale (get,list,watch,update,patch)
  - llmd.ai: variantautoscalings/status/finalizers (get,list,watch,create,update,patch,delete)
  - monitoring.coreos.com: servicemonitors (get,list,watch)

ClusterRole: metrics-reader (nonResourceURLs: /metrics → get)
ClusterRole: metrics-auth-role (tokenreviews, subjectaccessreviews → create)
ClusterRole: epp-metrics-reader-role (nonResourceURLs: /metrics, /debug/pprof/* → get)
ClusterRole: variantautoscaling-admin-role (llmd.ai variantautoscalings → *)
ClusterRole: variantautoscaling-editor-role (llmd.ai variantautoscalings → CRUD)
ClusterRole: variantautoscaling-viewer-role (llmd.ai variantautoscalings → read-only)
OpenShift: cluster-monitoring-view (Thanos Querier access)

Secrets:
────────
- epp-metrics-token (Opaque)
  Purpose: Bearer token for scraping EPP pod /metrics endpoints
  Provisioned: Manual / SA token
  Auto-Rotate: No

- controller-manager-token (kubernetes.io/service-account-token) [OpenShift only]
  Purpose: Long-lived SA token for user-workload monitoring Prometheus
  Provisioned: OpenShift
  Auto-Rotate: No

TLS Configuration:
──────────────────
- Metrics server: TLS 1.2+ minimum, self-signed certificate, HTTP/2 disabled
  (mitigates CVE-2023-44487, CVE-2023-39325)
- Prometheus client: Configurable CA bundle (PROMETHEUS_CA_CERT_PATH),
  optional mTLS (PROMETHEUS_CLIENT_CERT_PATH/KEY_PATH),
  system trust store fallback
- InsecureSkipVerify: Configurable (defaults true in dev, proper CA in OpenShift)
- FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, UBI9 base with OpenSSL

Network Policy (OpenShift overlay):
───────────────────────────────────
- NetworkPolicy for metrics traffic (allows Prometheus scraping on 8443/TCP)
- No other network policies defined in base manifests
