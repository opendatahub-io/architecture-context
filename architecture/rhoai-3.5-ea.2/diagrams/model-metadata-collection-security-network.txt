┌─────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME ENVIRONMENT (CI/CD Pipeline — Konflux Tekton)               │
│                                                                          │
│  [model-extractor (Go CLI)]                                              │
│  - No Kubernetes API access                                              │
│  - No RBAC required                                                      │
│  - No network listeners                                                  │
│  - Runs as build step, produces YAML files                               │
│         │                          │                                     │
│         │ Port: 443/TCP            │ Port: 443/TCP                       │
│         │ Protocol: HTTPS          │ Protocol: HTTPS                     │
│         │ Encryption: TLS 1.2+     │ Encryption: TLS 1.2+               │
│         │ Auth: Bearer Token       │ Auth: Anonymous                     │
│         │ (HF_TOKEN, optional)     │ (no credentials required)           │
│         ▼                          ▼                                     │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Build-Time Only)                                     │
│                                                                          │
│  [HuggingFace API — huggingface.co]                                      │
│  - Endpoints: /api/collections, /api/models/{id},                        │
│    /{model}/raw/main/README.md                                           │
│  - Purpose: Model collection discovery, detail fetching,                 │
│    README/frontmatter metadata extraction                                │
│  - Auth: Bearer Token (optional — unauthenticated may be rate-limited)  │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Red Hat Container Registry — registry.redhat.io]                       │
│  - Protocol: OCI Registry API (v2)                                       │
│  - Purpose: Fetch OCI manifests, image configs, layer blobs              │
│    for model card extraction and timestamp retrieval                     │
│  - Auth: Anonymous pulls (no credentials required)                       │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  BUILD ARTIFACTS (Output)                                                │
│                                                                          │
│  [Local Filesystem → Container Image]                                    │
│  - data/models-catalog.yaml                                              │
│  - data/models-index.yaml                                                │
│  - data/*-mcp-servers-catalog.yaml                                       │
│  - data/*-mcp-servers-index.yaml                                         │
│  - COPY'd into container image via Dockerfile.konflux                    │
│                                                                          │
├═════════════════════════════════════════════════════════════════════════┤
│                                                                          │
│         ▲ Image built by Konflux, pushed to container registry           │
│         │                                                                │
│         ▼                                                                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CONTAINER REGISTRY (Image Distribution)                                 │
│                                                                          │
│  [quay.io/opendatahub/odh-model-metadata-collection]                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Image pull secret                                               │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Auth: Image pull secret                                        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  RUNTIME ENVIRONMENT (Kubernetes Cluster)                                │
│                                                                          │
│  Namespace: redhat-ods-operator                                          │
│  ┌────────────────────────────────────────────────────────────┐          │
│  │  [rhods-operator / opendatahub-operator]                   │          │
│  │  - Deploys data container as init container or sidecar     │          │
│  │  - Provides catalog data to dashboard pods                 │          │
│  └──────────────────────────┬─────────────────────────────────┘          │
│                              │                                           │
│                              │ Deploys as init container/sidecar         │
│                              ▼                                           │
│  Namespace: redhat-ods-applications                                      │
│  ┌────────────────────────────────────────────────────────────┐          │
│  │  [odh-model-metadata-collection (Data Container)]          │          │
│  │  - Image: ubi9-minimal (no application code)               │          │
│  │  - CMD: sleep infinity                                      │          │
│  │  - VOLUME: /app/data                                        │          │
│  │  - Contains ONLY pre-generated YAML catalog files           │          │
│  │  - NO network services exposed                              │          │
│  │  - NO Kubernetes API access                                 │          │
│  │  - NO RBAC required                                         │          │
│  │  - NO egress traffic                                        │          │
│  │  - NO secrets at runtime                                    │          │
│  └──────────────────────────┬─────────────────────────────────┘          │
│                              │                                           │
│                              │ Volume mount: /app/data/*.yaml            │
│                              │ (Filesystem only — no network)            │
│                              ▼                                           │
│  ┌────────────────────────────────────────────────────────────┐          │
│  │  [RHOAI Dashboard]                                          │          │
│  │  - Reads models-catalog.yaml                                │          │
│  │  - Reads MCP server catalogs                                │          │
│  │  - Populates model registry UI                              │          │
│  └────────────────────────────────────────────────────────────┘          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No ClusterRoleBindings defined
- No RoleBindings defined
- The production container runs with NO Kubernetes API access
- No ServiceAccount with elevated permissions required

Secrets:
- HF_TOKEN (Environment variable, NOT a K8s Secret)
  - Purpose: HuggingFace API authentication for build-time processing only
  - Provisioned By: Manual developer .env file or CI/CD secret
  - Auto-Rotate: No
  - Scope: Build-time only — NOT present in production container

Authentication Summary:
- HuggingFace API: Bearer Token (HF_TOKEN) — optional, unauthenticated
  requests work but may be rate-limited
- Container Registry (build-time): Anonymous pulls (registry.redhat.io)
  or Docker config auth (~/.docker/config.json)
- Container Registry (runtime): Image pull secret for pulling the
  data container image

FIPS Compliance:
- Go binary: CGO_ENABLED=0 (static linking, non-FIPS crypto)
  → NOT relevant: Go binary is build-time only, never shipped
- Production image: ubi9-minimal base → inherits OS-level FIPS
  (OpenSSL in base image)
- Production container: Contains only YAML data files — no crypto
  operations at runtime

Network Security Profile:
- Attack surface: MINIMAL — data-only container with no network
  services, no listeners, no API endpoints
- Runtime egress: NONE — production container has zero network access
- Runtime ingress: NONE — no ports exposed, no Services defined
- Build-time egress: HuggingFace (443/TCP) + registry.redhat.io (443/TCP)
  — both HTTPS/TLS 1.2+
