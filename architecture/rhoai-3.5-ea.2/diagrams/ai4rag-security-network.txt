┌─────────────────────────────────────────────────────────────────────────┐
│  CONSUMING SERVICE (Trusted - same process)                              │
│                                                                          │
│  [Consuming Service]                                                     │
│  - Types: Kubeflow Pipeline Component, CLI Tool, Jupyter Notebook        │
│  - Integration: Python import (ai4rag library)                           │
│  - No network boundary crossed                                          │
│         │                                                                │
│         │ In-process Python call                                         │
│         │ Protocol: N/A (library import)                                 │
│         │ Encryption: N/A                                                │
│         │ Auth: N/A                                                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  AI4RAG LIBRARY (In-Process)                                             │
│                                                                          │
│  [AI4RAGExperiment]                                                      │
│  - Core orchestrator for RAG hyperparameter optimization                │
│  - No Kubernetes deployment (pure Python library)                        │
│  - No listening ports                                                    │
│                                                                          │
│  Internal Components (all in-process, no network):                       │
│  ┌────────────────────────────────────────────────────────────────┐      │
│  │ ModelsPreSelector    - Model pre-selection via lightweight eval│      │
│  │ GAMOptimizer         - HPO via Generalized Additive Models     │      │
│  │ RandomOptimizer      - Random search baseline                  │      │
│  │ SimpleRAGTemplate    - RAG pipeline template                   │      │
│  │ Retriever            - Vector similarity search                │      │
│  │ LangChainChunker     - Token-based text splitting (tiktoken)   │      │
│  │ DoclingChunker       - Structure-aware chunking                │      │
│  │ UnitxtEvaluator      - RAG quality metrics                     │      │
│  │ SearchSpace          - Parameter space with constraint rules   │      │
│  │ LocalEventHandler    - Results to disk (JSON)                  │      │
│  │ KFPEventHandler      - Results for Kubeflow Pipelines          │      │
│  └────────────────────────────────────────────────────────────────┘      │
│         │                                                                │
│         │ Outbound connections (egress only):                            │
│         │                                                                │
│         ├──────────────────────────────────────────────────────────┐     │
│         │                                                          │     │
│         ▼                                                          ▼     │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [OGX Server]                                                            │
│  - Purpose: Foundation models, embedding models, vector store backends   │
│  - Endpoints called:                                                     │
│    * POST /chat/completions     (foundation model text generation)       │
│    * POST /embeddings           (text embedding generation)              │
│    * POST /vector_stores/create (collection lifecycle)                   │
│    * POST /vector_stores/retrieve                                        │
│    * POST /vector_stores/delete                                          │
│    * POST /vector_io/insert     (document chunk indexing)                │
│    * POST /vector_io/query      (similarity search)                      │
│    * GET  /models/list          (model discovery)                        │
│  - Port: Configurable (set by consumer)                                  │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: API Key (Bearer Token) passed to OgxClient constructor          │
│  - Backends: Milvus, Qdrant (for vector stores)                         │
│                                                                          │
│  [ChromaDB - In-Memory]                                                  │
│  - Purpose: Lightweight vector store for MPS pre-selection, dev/test     │
│  - Port: N/A (in-process, no network)                                    │
│  - Protocol: In-process Python calls (langchain_chroma.Chroma)           │
│  - Encryption: N/A                                                       │
│  - Auth: None                                                            │
│  - Note: Ephemeral, no persistent storage                                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK SUMMARY:
================

Listening Ports: NONE (ai4rag is a library, not a service)
Ingress:         NONE
Egress:          OGX Server only (HTTPS, configurable port, TLS 1.2+, API Key)
In-Process:      ChromaDB, Unitxt, LangChain, Docling, pyGAM, scikit-learn, tiktoken

RBAC Summary:
=============
- No Kubernetes RBAC resources defined (library, not an operator)
- No ClusterRoles, Roles, or RoleBindings
- No ServiceAccounts

Service Mesh Configuration:
===========================
- N/A -- ai4rag is a library and does not deploy into a service mesh
- If consumed within a meshed pod, inherits the pod's mesh configuration

Secrets:
========
- OGX API Key (Opaque)
  - Purpose: Authentication to OGX Server
  - Provisioned: Manual / environment variable
  - Passed to: OgxClient constructor (api_key parameter)
  - Auto-Rotate: No
  - Note: Consumer is responsible for secret management

FIPS Compliance:
================
- No container images produced (pure Python library)
- hashlib.sha256 used for content deduplication IDs only (FIPS-compatible, not security-sensitive)
- TLS delegated to ogx-client (httpx) -- uses system OpenSSL
- No MD5, RC4, DES, or other non-FIPS crypto detected
- FIPS compliance depends on runtime environment (Python interpreter, OpenSSL version)

Build Hermeticity:
==================
- uv.lock present (4753 lines) -- hermetic Python dependency resolution
- No Konflux Dockerfiles (pure Python library)
- No RPM locks or Hermeto prefetch (not applicable)
- Distributed via PyPI
