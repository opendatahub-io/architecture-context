┌─────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (No External Ingress)                                 │
│                                                                         │
│  No ingress is configured — MLMD is an internal-only service.           │
│  It is accessed by ML Pipelines components within the same namespace.   │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE LAYER (Cluster Internal)                                       │
│                                                                         │
│  [ML Pipeline Controller / Data Science Pipelines]                      │
│  - Namespace: {dspa-namespace}                                          │
│  - Role: Primary consumer — records pipeline run metadata               │
│         │                                                               │
│         │ Port: 8080/TCP                                                │
│         │ Protocol: gRPC/HTTP2                                          │
│         │ Encryption: Optional TLS (SSLConfig)                          │
│         │ Auth: Optional mTLS (client_verify flag)                      │
│         ▼                                                               │
│  [MLMD gRPC Server (metadata_store_server)]                             │
│  - Service: metadata_store_server (ClusterIP)                           │
│  - Namespace: {dspa-namespace}                                          │
│  - Container: odh-mlmd-grpc-server (UBI9-based)                        │
│  - User: non-root (UID 65534)                                          │
│  - Port: 8080/TCP (gRPC/HTTP2)                                         │
│  - TLS: Optional — configurable via SSLConfig protobuf                  │
│    - Server key/cert for TLS termination                                │
│    - custom_ca for client CA verification                               │
│    - client_verify flag for mTLS enforcement                            │
│  - Crypto: BoringSSL (via gRPC) for transport TLS                       │
│         │                                                               │
│         ├──────────────────────────────┐                                 │
│         │                              │                                │
│         │ Port: 3306/TCP               │ Port: 5432/TCP                 │
│         │ Protocol: MySQL              │ Protocol: PostgreSQL            │
│         │ Encryption: Optional SSL     │ Encryption: Optional SSL       │
│         │   (SSLOptions: cert/key/     │   (SSLOptions: sslmode/        │
│         │    ca/capath/cipher/         │    sslcert/sslkey/             │
│         │    verify_server_cert)       │    sslrootcert)                │
│         │ Auth: Username/password      │ Auth: Username/password        │
│         ▼                              ▼                                │
├─────────────────────────────────────────────────────────────────────────┤
│  DATABASE LAYER                                                         │
│                                                                         │
│  [MySQL / MariaDB]                     [PostgreSQL]                      │
│  - Port: 3306/TCP                      - Port: 5432/TCP                 │
│  - Purpose: Persistent metadata        - Purpose: Alternative           │
│    storage backend                       persistent storage backend     │
│  - Auth: Username/password             - Auth: Username/password        │
│  - Encryption: Optional SSL            - Encryption: Optional SSL       │
│    (mysql_ssl_set with cipher            (sslmode: disable/allow/       │
│     preferences)                          prefer/require/verify-ca/     │
│                                           verify-full)                  │
│                                                                         │
│  [SQLite - In-Memory]                                                   │
│  - Purpose: Ephemeral/testing backend                                   │
│  - No network traffic (embedded)                                        │
│                                                                         │
│  Also accessed by:                                                      │
│  [Pipeline UI (Frontend)]                                               │
│  - Interaction: gRPC client (indirect via pipeline API server)          │
│  - Port: 8080/TCP                                                       │
│  - Protocol: gRPC/HTTP2                                                 │
│  - Purpose: Queries artifact lineage and execution history              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC resources defined (standalone gRPC service)
- No ClusterRoles or RoleBindings

Authentication & Authorization:
- gRPC Transport:
  - Optional mTLS via SSLConfig protobuf
  - If ssl_config is set with client_verify=true, client must present
    valid certificate signed by custom_ca
  - No application-level authentication beyond transport mTLS
- Database Connections:
  - MySQL: Username/password + SSLOptions (cert/key/ca/capath/cipher/verify_server_cert)
  - PostgreSQL: Username/password + SSLOptions (sslmode/sslcert/sslkey/sslrootcert)

Secrets:
- Database credentials (Opaque) - MySQL/PostgreSQL username/password
  - Provisioned by: Platform operator (DSP controller)
  - Auto-Rotate: No
- TLS certificates (kubernetes.io/tls) - Optional server key/cert and client CA
  - Provisioned by: Manual or cert-manager
  - Auto-Rotate: Depends on provisioner

FIPS Compliance Notes:
- C++ binary (not Go) — no GOEXPERIMENT/strictfipsruntime
- BoringSSL (via gRPC) for transport-layer TLS
- UBI9 base provides OpenSSL in FIPS mode on FIPS-enabled nodes
- No explicit cipher suite restrictions in gRPC server setup
- MySQL SSL passes cipher preferences without validation

Build Hermeticity:
- Tekton pipeline: hermetic: false (NOT hermetic)
- No RPM lock files for UBI9 base image packages
- Bazel WORKSPACE deps pinned by SHA256 hash
- Supply chain gap: non-hermetic build + no RPM lock files

Container Security:
- Base image: UBI9 (Red Hat Universal Base Image)
- Runs as non-root user (UID 65534)
- Dynamic linking (C++ binary linked against system libraries)
