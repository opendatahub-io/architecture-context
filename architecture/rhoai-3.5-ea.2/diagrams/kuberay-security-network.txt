┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [User / Data Scientist]                                                    │
│         │                                                                   │
│         │ Port: 6443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Bearer Token (OpenShift/OIDC)                               │
│         │                                                                   │
│         ├──────────────────────────────────────────────┐                     │
│         │                                              │                    │
│         ▼                                              ▼                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 6443/TCP                                                          │
│  - Encryption: TLS 1.2+                                                    │
│  - Auth: ServiceAccount Token, Bearer Token                                │
│         │                                                                   │
│         │ Admission webhooks (sync)                                         │
│         │ Port: 9443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (cert-manager provisioned)                        │
│         │ Auth: Kubernetes API server identity                              │
│         ▼                                                                   │
│  [Mutating Webhook - mraycluster.kb.io]                                    │
│  - Enforces odh.ray.io/secure-trusted-network annotation                   │
│  - Disables enableIngress on OpenShift                                     │
│         │                                                                   │
│         ▼                                                                   │
│  [Validating Webhook - vraycluster.kb.io]                                  │
│  - Validates cluster name (DNS-compatible)                                 │
│  - Validates worker group name uniqueness                                  │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (e.g., redhat-ods-applications)                        │
│                                                                             │
│  [KubeRay Operator Pod]                                                    │
│  - ServiceAccount: kuberay-operator                                        │
│  - Image: quay.io/opendatahub/kuberay-operator:v1.4.4                     │
│  - Ports:                                                                  │
│    - 8080/TCP (metrics, HTTP, no auth)                                     │
│    - 8081/TCP (health/ready, HTTP, no auth)                                │
│    - 9443/TCP (webhooks, HTTPS, TLS cert-manager)                          │
│         │                                                                   │
│         │ Port: 6443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount Token                                        │
│         │                                                                   │
│         ├──────────────────── Manages CRDs ──────────────────────┐         │
│         │                                                        │         │
│         ▼                                                        ▼         │
│  [Authentication Controller outputs]              [mTLS Controller outputs]│
│  - HTTPRoute (in platform NS)                     - Issuer (SelfSigned)    │
│  - ReferenceGrant (cross-NS)                      - CA Certificate         │
│  - ServiceAccount                                 - Head TLS Certificate   │
│  - ConfigMap (kube-rbac-proxy config)             - Worker TLS Certificate │
│  - kube-rbac-proxy sidecar injection                                       │
│                                                                             │
│  [NetworkPolicy Controller outputs]                                        │
│  - Head NetworkPolicy: allow intra-cluster,                                │
│    openshift-ingress NS, monitoring NS                                     │
│  - Worker NetworkPolicy: allow intra-cluster only                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                      │
│                                                                             │
│  [data-science-gateway (Gateway CR)]                                       │
│  - Port: 443/TCP (external)                                                │
│  - Encryption: TLS (Gateway-terminated)                                    │
│  - TLS Mode: SIMPLE                                                        │
│  - Path: /ray/{namespace}/{cluster-name}/*                                 │
│         │                                                                   │
│         │ Port: 8443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS                                                   │
│         │ Auth: OIDC Bearer Token (forwarded)                               │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  USER NAMESPACE (Ray Cluster)                                               │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────┐              │
│  │ [Ray Head Pod]                                            │              │
│  │  ServiceAccount: {cluster}-sa (auto-created)              │              │
│  │                                                           │              │
│  │  ┌─────────────────────────────────┐                      │              │
│  │  │ kube-rbac-proxy sidecar         │                      │              │
│  │  │ Port: 8443/TCP HTTPS TLS        │                      │              │
│  │  │ Auth: OIDC → TokenReview +      │                      │              │
│  │  │       SubjectAccessReview       │                      │              │
│  │  │ Image: ose-kube-rbac-proxy-rhel9│                      │              │
│  │  └──────────────┬──────────────────┘                      │              │
│  │                 │ Port: 8265/TCP                           │              │
│  │                 │ Protocol: HTTP                           │              │
│  │                 │ Encryption: None (localhost)             │              │
│  │                 │ Auth: Authenticated upstream             │              │
│  │                 ▼                                          │              │
│  │  ┌─────────────────────────────────┐                      │              │
│  │  │ Ray Dashboard                   │                      │              │
│  │  │ Port: 8265/TCP HTTP             │                      │              │
│  │  └─────────────────────────────────┘                      │              │
│  │                                                           │              │
│  │  Other ports:                                             │              │
│  │  - 6379/TCP (GCS/Redis, TCP, mTLS when enabled)          │              │
│  │  - 10001/TCP (Ray Client, TCP, mTLS when enabled)        │              │
│  │  - 8080/TCP (Metrics, HTTP)                              │              │
│  │  - 8000/TCP (Serve proxy, HTTP)                          │              │
│  └───────────────────────────────────────────────────────────┘              │
│         │                                                                   │
│         │ Port: 6379/TCP, 10001/TCP, worker ports                           │
│         │ Protocol: TCP                                                     │
│         │ Encryption: mTLS (cert-manager, when enabled)                     │
│         │ Auth: TLS client certificate                                      │
│         ▼                                                                   │
│  ┌───────────────────────────────────────────────────────────┐              │
│  │ [Ray Worker Pods]                                         │              │
│  │  - Worker group replicas                                  │              │
│  │  - mTLS certs mounted from ray-worker-secret-{cluster}   │              │
│  │  - NetworkPolicy: intra-cluster traffic only              │              │
│  └───────────────────────────────────────────────────────────┘              │
│                                                                             │
│  Services:                                                                  │
│  - {cluster}-head-svc (ClusterIP): 6379, 8265, 10001, 8080, 8000          │
│  - {cluster}-head-svc (Headless): All Ray ports (pod discovery)            │
│  - {cluster}-serve-svc (ClusterIP): 8000 (Serve traffic)                   │
│  - {cluster}-headless-worker-svc (Headless): Worker ports                  │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [cert-manager]                                                             │
│  - Purpose: TLS certificate provisioning (mTLS + webhook certs)            │
│  - Auth: ServiceAccount Token (CRD management)                             │
│  - Certs: CA (10-year), Head/Worker (90-day, renew at 75 days)             │
│                                                                             │
│  [External Redis] (optional, for GCS Fault Tolerance)                      │
│  - Port: 6379/TCP                                                          │
│  - Protocol: TCP                                                           │
│  - Encryption: None (user-configurable)                                    │
│  - Auth: Username/Password (optional)                                      │
│  - Purpose: GCS fault tolerance external storage                           │
│                                                                             │
│  [OpenShift OAuth / Authentication Config]                                  │
│  - Port: 6443/TCP (via K8s API)                                            │
│  - Protocol: HTTPS                                                         │
│  - API: config.openshift.io/authentications, oauths                        │
│  - Purpose: Auth mode detection for OIDC provider config                   │
│                                                                             │
│  [Prometheus / Monitoring]                                                  │
│  - Scrapes operator: 8080/TCP HTTP (no auth)                               │
│  - NetworkPolicy: monitoring NS pods allowed to head pod metrics           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: kuberay-operator
  - ray.io: rayclusters, rayjobs, rayservices (full CRUD + status + finalizers)
  - core: pods (full CRUD + deletecollection + proxy), services (full CRUD + proxy + status),
          configmaps, serviceaccounts, events (full CRUD), secrets (get, list, watch, delete),
          endpoints (get, list, watch)
  - batch: jobs (full CRUD)
  - apps: deployments (get, list, watch, update, patch)
  - cert-manager.io: certificates, issuers (full CRUD + status)
  - gateway.networking.k8s.io: gateways (read), httproutes, referencegrants (full CRUD)
  - networking.k8s.io: ingresses, networkpolicies (full CRUD), ingressclasses (read)
  - route.openshift.io: routes (full CRUD)
  - rbac.authorization.k8s.io: roles (CRUD), rolebindings (full CRUD),
                                clusterroles, clusterrolebindings (read)
  - authentication.k8s.io: tokenreviews (create)
  - authorization.k8s.io: subjectaccessreviews (create)
  - config.openshift.io: authentications, oauths (read + status)
  - operator.openshift.io: kubeapiservers (read + status)
  - coordination.k8s.io: leases (get, list, create, update)
  - extensions: ingresses (full CRUD)

- ClusterRoleBinding: kuberay-operator → kuberay-operator (ClusterRole)
- RoleBinding: kuberay-operator-leader-election → kuberay-leader-election-role (Role)
- ServiceAccount: kuberay-operator

Service Mesh / Network Policies:
- Head Pod NetworkPolicy:
  - Ingress: Allow from pods with cluster labels (intra-cluster)
  - Ingress: Allow from openshift-ingress namespace (Gateway traffic)
  - Ingress: Allow from monitoring namespace (Prometheus scraping)
- Worker Pod NetworkPolicy:
  - Ingress: Allow from pods with cluster labels only (intra-cluster)

Secrets:
- {cluster}-ca-secret-{uid} (kubernetes.io/tls) - CA cert, cert-manager, 10-year duration
- ray-head-secret-{cluster} (kubernetes.io/tls) - Head TLS cert, cert-manager, 90-day/renew@75d
- ray-worker-secret-{cluster} (kubernetes.io/tls) - Worker TLS cert, cert-manager, 90-day/renew@75d
- oauth-tls / oidc-tls (kubernetes.io/tls) - Auth proxy TLS, self-signed by controller, no rotation
- oauth-config / oidc-config (Opaque) - kube-rbac-proxy config, authentication controller, no rotation

Admission Webhooks:
- mraycluster.kb.io (Mutating): Enforces secure-trusted-network, disables Ingress/Route on OpenShift
- vraycluster.kb.io (Validating): DNS name validation, worker group uniqueness

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
- Runtime: OpenSSL via UBI 9 minimal base
- No custom tls.Config; mTLS via cert-manager (FIPS-compliant at runtime)
- No insecure crypto detected (no md5, rc4, InsecureSkipVerify)
