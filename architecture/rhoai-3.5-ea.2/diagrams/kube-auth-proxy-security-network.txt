┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL CLIENTS (Untrusted)                                                │
│                                                                              │
│  [Browser Client]                                                            │
│         │                                                                    │
│         │ Port: 4180/TCP (HTTP) or 443/TCP (HTTPS)                          │
│         │ Protocol: HTTP or HTTPS                                            │
│         │ Encryption: Configurable TLS 1.2+ (TLS 1.3 supported)             │
│         │ Auth: Session cookie (after OAuth flow) or Bearer token            │
│         ▼                                                                    │
│  [API / CLI Client]                                                          │
│         │                                                                    │
│         │ Port: 4180/TCP or 443/TCP                                          │
│         │ Protocol: HTTP or HTTPS                                            │
│         │ Encryption: Configurable TLS 1.2+                                  │
│         │ Auth: Authorization: Bearer <token>                                │
│         │       (OpenShift sha256~ token, JWT ID token, or K8s SA token)     │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  POD BOUNDARY (Sidecar)                                                      │
│                                                                              │
│  [Envoy Proxy] ─────────────────────────┐                                   │
│  - ext_authz subrequest                  │                                   │
│         │                                │                                   │
│         │ Port: 4180/TCP                 │                                   │
│         │ Protocol: HTTP                 │                                   │
│         │ Encryption: None (pod-local)   │                                   │
│         │ Auth: Forwarded original       │                                   │
│         │       request headers          │                                   │
│         ▼                                │                                   │
│  ┌──────────────────────────────────┐    │                                   │
│  │  kube-auth-proxy                  │    │                                   │
│  │                                   │    │                                   │
│  │  Listeners:                       │    │                                   │
│  │  - 4180/TCP (HTTP, default)       │    │                                   │
│  │  - 443/TCP (HTTPS, optional)      │    │                                   │
│  │    TLS 1.2+ (MinVersion enforced) │    │                                   │
│  │    TLS 1.3 supported              │    │                                   │
│  │    Cipher suites configurable     │    │                                   │
│  │                                   │    │                                   │
│  │  Auth Endpoints:                  │    │                                   │
│  │  - /oauth2/sign_in (no auth)      │    │                                   │
│  │  - /oauth2/sign_out (session)     │    │                                   │
│  │  - /oauth2/start (CSRF cookie)    │    │                                   │
│  │  - /oauth2/callback (CSRF)        │    │                                   │
│  │  - /oauth2/auth (ext_authz)       │    │                                   │
│  │  - /oauth2/userinfo (session)     │    │                                   │
│  │  - /* (reverse proxy, session)    │    │                                   │
│  │                                   │    │                                   │
│  │  Health:                          │    │                                   │
│  │  - /ping (liveness, no auth)      │    │                                   │
│  │  - /ready (readiness, no auth)    │    │                                   │
│  │                                   │    │                                   │
│  │  Metrics:                         │    │                                   │
│  │  - Configurable port              │    │                                   │
│  │  - Prometheus /metrics            │    │                                   │
│  │                                   │    │                                   │
│  │  FIPS: GOEXPERIMENT=strict        │    │                                   │
│  │        fipsruntime, CGO_ENABLED=1 │    │                                   │
│  │        OpenSSL via UBI9           │    │                                   │
│  └──────────────────────────────────┘    │                                   │
│         │                                │                                   │
│         │ Configurable port/protocol     │                                   │
│         │ HTTP / HTTPS / Unix socket     │                                   │
│         │ Encryption: Configurable       │                                   │
│         │ Auth: None (pre-authenticated) │                                   │
│         │ Headers: GAP-Auth,             │                                   │
│         │   X-Forwarded-User,            │                                   │
│         │   X-Forwarded-Email,           │                                   │
│         │   X-Forwarded-Groups,          │                                   │
│         │   X-Forwarded-Access-Token     │                                   │
│         ▼                                │                                   │
│  [Upstream Application Service] ◄────────┘                                   │
│  - Receives pre-authenticated requests                                       │
│  - Identity conveyed via headers                                             │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS TO IDENTITY PROVIDERS (Trusted, TLS-encrypted)                       │
│                                                                              │
│  [OIDC Identity Provider (Keycloak / DEX)]                                   │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: OAuth2 client credentials (client_id + client_secret)               │
│  - Endpoints: .well-known/openid-configuration (discovery),                  │
│               /authorize, /token, /jwks, /userinfo, /end_session             │
│  - PKCE: Supported (S256, plain)                                             │
│                                                                              │
│  [OpenShift OAuth Server]                                                    │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (custom CA via --openshift-ca-file or SA CA)         │
│  - Auth: OAuth2 client credentials                                           │
│  - Discovery: /.well-known/oauth-authorization-server on kubernetes.default   │
│  - Endpoints: /authorize, /token                                             │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS TO KUBERNETES/OPENSHIFT APIs (Trusted, TLS-encrypted)                │
│                                                                              │
│  [Kubernetes API Server (kubernetes.default.svc)]                            │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (in-cluster CA or kubeconfig)                        │
│  - Auth: Service account token (auto-mounted)                                │
│  - API: authentication.k8s.io/v1 TokenReview (POST)                         │
│  - Purpose: Validate K8s service account bearer tokens                       │
│                                                                              │
│  [OpenShift User API]                                                        │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (service account CA)                                 │
│  - Auth: Bearer token (user's access token, forwarded)                       │
│  - Endpoint: /apis/user.openshift.io/v1/users/~ (GET)                       │
│  - Purpose: Enrich session with username, email, groups                      │
│                                                                              │
│  [OpenShift OAuthAccessToken API]                                            │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Bearer token (user's access token)                                  │
│  - Endpoint: /apis/oauth.openshift.io/v1/oauthaccesstokens/{name} (DELETE)  │
│  - Purpose: Token cleanup on sign-out (sha256 hash derivation)               │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS TO SESSION STORE (Optional)                                          │
│                                                                              │
│  [Redis]                                                                     │
│  - Port: 6379/TCP                                                            │
│  - Protocol: Redis protocol                                                  │
│  - Encryption: Optional TLS (configurable via --redis-use-tls,               │
│                --redis-ca-path)                                               │
│  - Auth: Optional password + ACL                                             │
│  - Modes: Standalone, Sentinel, Cluster                                      │
│  - Purpose: Persistent session storage for multi-replica deployments         │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ServiceAccount: (consuming deployment's SA, not defined by kube-auth-proxy)
  - API Group: authentication.k8s.io
  - Resource: tokenreviews
  - Verbs: create
  - Purpose: Validate K8s service account tokens via TokenReview API
  - Note: Only required when --enable-k8s-token-validation is set

Secrets:
- OAuth client secret (Opaque) - OIDC/OpenShift client_secret, manual/operator-managed, no auto-rotate
- Cookie secret (Opaque) - HMAC key for session cookie encryption (32/64 bytes), manual, no auto-rotate
- TLS cert/key (kubernetes.io/tls) - Optional HTTPS termination, cert-manager or manual, depends on provisioner
- Service account token (kubernetes.io/service-account-token) - OpenShift OAuth auto-detect + K8s TokenReview, Kubernetes auto-mounted, auto-rotated by kubelet
- JWT signing key (Opaque) - Optional request signature, manual, no auto-rotate
- Redis password (Opaque) - Optional Redis auth, manual, no auto-rotate

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
- Linking: Dynamic (CGO_ENABLED=1, uses OpenSSL via UBI9)
- Base Image: registry.access.redhat.com/ubi9/ubi-minimal
- TLS: MinVersion TLS 1.2 enforced on server and client connections
- Risk: --ssl-insecure-skip-verify flag exists (disabled by default, #nosec G402)
- No MD5, RC4, or DES usage detected

Authentication Methods (simultaneous):
1. Browser OAuth2/OIDC: Cookie-based session after authorization code flow
2. Bearer Token (CLI/API): OpenShift sha256~ tokens, JWT ID tokens
3. K8s ServiceAccount Token: Validated via TokenReview API
4. Skip-auth: Configurable path/method regex allowlist
5. Trusted IPs: IP-based bypass (--trusted-ip)
