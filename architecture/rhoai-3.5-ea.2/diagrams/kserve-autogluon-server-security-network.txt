┌──────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                    │
│                                                                          │
│  [Inference Client]                                                      │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token / mTLS certificate                          │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) - Managed by KServe                                       │
│                                                                          │
│  [Istio Ingress Gateway]                                                 │
│  - Type: HTTPRoute or VirtualService                                     │
│  - Host: {isvc-name}.{namespace}.svc.cluster.local                      │
│  - Port: 443/TCP (external) → 8080/TCP (internal)                      │
│  - TLS Termination: TLS 1.2+ (SIMPLE mode)                              │
│  - AuthN: RequestAuthentication (validates JWT if present)              │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: mTLS (STRICT via Istio sidecar)                    │
│         │ Auth: PeerAuthentication (Service Identity)                    │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - mTLS STRICT)                                    │
│                                                                          │
│  [Istio Sidecar Proxy]                                                   │
│  - Port: 15006/TCP (inbound)                                             │
│  - Protocol: HTTP/2                                                      │
│  - mTLS: STRICT                                                          │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: localhost (no encryption needed)                    │
│         │ Auth: N/A (sidecar to app on same pod)                        │
│         ▼                                                                │
│  [AutoGluon Server Pod]                                                  │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: default (managed by KServe)                          │
│  - Image: odh-kserve-autogluon-server-rhel9                             │
│  - Ports:                                                                │
│    - 8080/TCP (HTTP REST v1/v2)                                          │
│    - 8081/TCP (gRPC Inference)                                           │
│  - Endpoints:                                                            │
│    - POST /v1/models/{name}:predict (tabular + time series)             │
│    - POST /v2/models/{name}/infer (tabular only)                        │
│    - GET  /v2/models/{name} (model metadata)                            │
│    - GET  /v1/models (list models)                                      │
│    - GET  /v2/health/ready (readiness probe)                            │
│    - GET  /v2/health/live (liveness probe)                              │
│                                                                          │
│  [Prometheus Scrape]                                                     │
│  - Source: Prometheus                                                    │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None (in-mesh)                                            │
│  - Purpose: Metrics collection                                           │
│         │                                                                │
│         │ EGRESS                                                         │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: AWS IAM / GCP Service Account / Azure AD / Bearer Token │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3-compatible Storage]                                                 │
│  - Purpose: Model artifact download at startup                          │
│  - Auth: AWS IAM credentials (via storage-credentials secret)           │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Google Cloud Storage]                                                  │
│  - Purpose: Model artifact download at startup                          │
│  - Auth: GCP Service Account credentials                                │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Azure Blob Storage]                                                    │
│  - Purpose: Model artifact download at startup                          │
│  - Auth: Azure AD credentials                                           │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Hugging Face Hub]                                                      │
│  - Purpose: Model artifact download (if storageUri uses HF)            │
│  - Auth: Bearer Token (optional)                                        │
│  - Encryption: TLS 1.2+                                                  │
└──────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- This component does NOT define RBAC resources.
- Runs as a user-space model server pod.
- RBAC for InferenceService lifecycle is managed by the KServe operator.

Service Mesh Configuration:
- PeerAuthentication: STRICT (mTLS enforced via Istio sidecar)
- Istio sidecar injected into pod (port 15006/TCP inbound)
- Traffic routing managed by KServe via HTTPRoute or VirtualService
- AuthN: RequestAuthentication at Istio Gateway level

Secrets:
- storage-credentials (Opaque) - S3/GCS/Azure credentials for model download
  - Provisioned by: User / platform operator
  - Auto-Rotate: No

FIPS Compliance:
- Base Image: AIPCC CPU base (RHEL 9.6) with FIPS-validated OpenSSL
- Python ssl module: Uses system OpenSSL (FIPS-mode aware)
- cryptography 48.0.0: Links against system OpenSSL
- No custom TLS configuration in application code
- No non-FIPS crypto risks detected

Container Security:
- Image: registry.redhat.io odh-kserve-autogluon-server-rhel9
- Architectures: aarch64, ppc64le, s390x, x86_64
- Python deps: SHA256 hash-pinned (autogluon-all-requirements.txt)
- Package source: RHEL AI PyPI (console.redhat.com)
