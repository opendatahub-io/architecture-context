┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.3                                                │
│         │ Auth: Bearer Token (platform auth policy)                          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  POD BOUNDARY (InferenceService Pod)                                         │
│                                                                              │
│  ┌───────────────────────────────────────────────────┐                       │
│  │  [kube-rbac-proxy Sidecar]                        │                       │
│  │  - Port: 8443/TCP (external-facing)               │                       │
│  │  - TLS Termination: TLS 1.3                       │                       │
│  │  - AuthN/AuthZ: Bearer Token validation           │                       │
│  │  - Proxies to localhost:8080 (plaintext)           │                       │
│  └───────────────────────┬───────────────────────────┘                       │
│                          │                                                    │
│                          │ Port: 8080/TCP                                     │
│                          │ Protocol: HTTP                                     │
│                          │ Encryption: None (localhost/plaintext)              │
│                          │ Auth: None (pre-authenticated by sidecar)          │
│                          ▼                                                    │
│  ┌───────────────────────────────────────────────────┐                       │
│  │  [autogluonserver Container]                      │                       │
│  │  - UID: 1001 (non-root)                           │                       │
│  │  - Ports: 8080/TCP (HTTP), 8081/TCP (gRPC)        │                       │
│  │  - Image: AIPCC CPU base (RHEL 9.6 UBI)           │                       │
│  │  - No Kubernetes API access                        │                       │
│  │  - No RBAC roles/bindings                          │                       │
│  │                                                    │                       │
│  │  HTTP Endpoints (8080/TCP):                        │                       │
│  │    POST /v1/models/{name}:predict   (v1 inference) │                       │
│  │    POST /v2/models/{name}/infer     (v2 inference) │                       │
│  │    GET  /v2/models/{name}           (v2 metadata)  │                       │
│  │    GET  /v1/models/{name}           (v1 metadata)  │                       │
│  │    GET  /v2/health/ready            (readiness)    │                       │
│  │    GET  /v2/health/live             (liveness)     │                       │
│  │    GET  /metrics                    (prometheus)   │                       │
│  │                                                    │                       │
│  │  gRPC Endpoint (8081/TCP):                         │                       │
│  │    GRPCInferenceService             (v2 inference) │                       │
│  └───────────────────────┬───────────────────────────┘                       │
│                          │                                                    │
├──────────────────────────┼───────────────────────────────────────────────────┤
│  EGRESS (Startup Only - Model Download)                                      │
│                          │                                                    │
│                          │ Port: 443/TCP                                      │
│                          │ Protocol: HTTPS                                    │
│                          │ Encryption: TLS 1.2+                               │
│                          │ Auth: Varies by backend (see below)                │
│                          ▼                                                    │
│  ┌───────────────────────────────────────────────────┐                       │
│  │  [S3-Compatible Storage - *.amazonaws.com]        │                       │
│  │  - Port: 443/TCP                                  │                       │
│  │  - Protocol: HTTPS                                │                       │
│  │  - Encryption: TLS 1.2+                           │                       │
│  │  - Auth: AWS IAM credentials (via boto3/env vars) │                       │
│  │  - Purpose: Model artifact download               │                       │
│  └───────────────────────────────────────────────────┘                       │
│                                                                              │
│  ┌───────────────────────────────────────────────────┐                       │
│  │  [Google Cloud Storage - storage.googleapis.com]  │                       │
│  │  - Port: 443/TCP                                  │                       │
│  │  - Protocol: HTTPS                                │                       │
│  │  - Encryption: TLS 1.2+                           │                       │
│  │  - Auth: GCP service account                      │                       │
│  │  - Purpose: Model artifact download               │                       │
│  └───────────────────────────────────────────────────┘                       │
│                                                                              │
│  ┌───────────────────────────────────────────────────┐                       │
│  │  [Azure Blob Storage - *.blob.core.windows.net]   │                       │
│  │  - Port: 443/TCP                                  │                       │
│  │  - Protocol: HTTPS                                │                       │
│  │  - Encryption: TLS 1.2+                           │                       │
│  │  - Auth: Azure identity / SAS token               │                       │
│  │  - Purpose: Model artifact download               │                       │
│  └───────────────────────────────────────────────────┘                       │
│                                                                              │
│  ┌───────────────────────────────────────────────────┐                       │
│  │  [HuggingFace Hub - huggingface.co]               │                       │
│  │  - Port: 443/TCP                                  │                       │
│  │  - Protocol: HTTPS                                │                       │
│  │  - Encryption: TLS 1.2+                           │                       │
│  │  - Auth: HF token (optional)                      │                       │
│  │  - Purpose: Model artifact download               │                       │
│  └───────────────────────────────────────────────────┘                       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  INTERNAL CLUSTER (Control Plane)                                            │
│                                                                              │
│  [KServe Controller]                                                         │
│  - Manages InferenceService CR lifecycle                                     │
│  - Creates/updates autogluon server pods                                     │
│  - Injects kube-rbac-proxy sidecar                                          │
│                                                                              │
│  [ClusterServingRuntime]                                                     │
│  - Defines container image, ports, args                                      │
│  - Maps modelFormat: autogluon to this server                                │
│                                                                              │
│  [Prometheus]                                                                │
│  - Scrapes GET /metrics on 8080/TCP HTTP                                     │
│  - No authentication (internal cluster network)                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC roles or bindings defined by this component
- Container runs as non-root UID 1001
- No Kubernetes API access from the container
- Auth is delegated entirely to kube-rbac-proxy sidecar (RHOAI 3.x)

Secrets:
- No secrets defined by this component
- Storage credentials injected by KServe controller via:
  - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, etc.)
  - Service account annotations (GCP workload identity, AWS IRSA)
  - Secret volume mounts (storage-config)

FIPS Compliance:
- AIPCC CPU base image: RHEL 9.6 UBI with system OpenSSL (FIPS-capable)
- cryptography >= 48.0.0: Uses OpenSSL backend (FIPS-capable on RHEL 9)
- No custom crypto, no md5/rc4/des usage detected
- Certificate trust: System trust store (ca-certificates RPM + certifi)

Build Hermeticity:
- Python deps: autogluon-all-requirements.txt (SHA256 hash-pinned, ~150 deps)
- Python index: RHEL AI PyPI (console.redhat.com/api/pypi/public-rhai/...)
- RPM layer: No lock file (relies on AIPCC base image stability)
- Go deps: go.sum present (not shipped in this image)

Network Trust Zones:
- EXTERNAL → kube-rbac-proxy: TLS 1.3, Bearer Token auth
- kube-rbac-proxy → autogluonserver: plaintext HTTP (localhost, same pod)
- autogluonserver → Cloud Storage: TLS 1.2+, IAM/SA/Token auth (startup only)
- Prometheus → autogluonserver: plaintext HTTP (internal cluster network)
