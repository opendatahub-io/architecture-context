┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client / Data Scientist]                                         │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (Reencrypt or Edge, via Route)                    │
│         │ Auth: Bearer Token (optional, per AuthPolicy)                     │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (OpenShift Routes / Gateway API)                                   │
│                                                                             │
│  [{isvc-name} Route] (dynamic, per InferenceService)                        │
│  - Port: 443/TCP (external) → predictor backend                            │
│  - TLS Termination: Reencrypt (auth enabled) or Edge                       │
│  - Created by: Route Reconciler                                             │
│                                                                             │
│  [{gateway-name} AuthPolicy] (dynamic, per Gateway)                         │
│  - Auth: KubernetesTokenReview + SubjectAccessReview                        │
│  - Enforced by: Kuadrant/Authorino                                          │
│                                                                             │
│  [{httproute-name} AuthPolicy] (dynamic, per LLMInferenceService)           │
│  - Auth: Anonymous or UserDefined (per annotation)                          │
│  - Enforced by: Kuadrant/Authorino                                          │
│         │                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted - ServiceAccount Auth)                           │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────┐      │
│  │  Namespace: opendatahub                                           │      │
│  │                                                                   │      │
│  │  [odh-model-controller]                                           │      │
│  │  - Deployment: single-replica, leader election                    │      │
│  │  - ServiceAccount: odh-model-controller                           │      │
│  │  - Ports:                                                         │      │
│  │    - 9443/TCP (Webhook, HTTPS, TLS via service-ca)                │      │
│  │    - 8080/TCP (Metrics, HTTP/HTTPS configurable)                  │      │
│  │    - 8081/TCP (Health probes, HTTP)                               │      │
│  │                                                                   │      │
│  │  [model-serving-api]                                              │      │
│  │  - Deployment: separate, own ServiceAccount                       │      │
│  │  - ServiceAccount: model-serving-api                              │      │
│  │  - Ports:                                                         │      │
│  │    - 8443/TCP (HTTPS API, TLS 1.2+ FIPS cipher suites)           │      │
│  │      Ciphers: ECDHE+AES-GCM only                                 │      │
│  │      Curves: P-256, P-384 (NIST)                                 │      │
│  │    - 8080/TCP (Metrics, HTTPS, TLS 1.2+)                         │      │
│  │                                                                   │      │
│  └───────────────────────────────────────────────────────────────────┘      │
│         │                                                                   │
│         │ Webhook Service:                                                  │
│         │   Port: 443/TCP → 9443/TCP                                        │
│         │   Protocol: HTTPS                                                 │
│         │   Encryption: TLS (OpenShift service-ca signed)                   │
│         │   Auth: Kubernetes API TLS client cert (AdmissionReview)           │
│         │                                                                   │
│         │ API Service:                                                       │
│         │   Port: 443/TCP → 8443/TCP                                        │
│         │   Protocol: HTTPS                                                 │
│         │   Encryption: TLS 1.2+ (FIPS cipher suites)                       │
│         │   Auth: Bearer Token + SelfSubjectAccessReview                    │
│         │                                                                   │
│         │ Metrics Service:                                                   │
│         │   Port: 8080/TCP                                                  │
│         │   Protocol: HTTP/HTTPS (configurable)                             │
│         │   Auth: TokenReview (when secure)                                 │
│         │                                                                   │
│  ┌───────────────────────────────────────────────────────────────────┐      │
│  │  [RHOAI Dashboard]                                                │      │
│  │         │                                                         │      │
│  │         │ Port: 8443/TCP                                          │      │
│  │         │ Protocol: HTTPS                                         │      │
│  │         │ Encryption: TLS 1.2+ (FIPS cipher suites)               │      │
│  │         │ Auth: Bearer Token (user's Kubernetes token)            │      │
│  │         ▼                                                         │      │
│  │  [model-serving-api /api/v1/gateways]                             │      │
│  └───────────────────────────────────────────────────────────────────┘      │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────┐      │
│  │  [Authorino gRPC Service]                                         │      │
│  │  - Namespace: kuadrant-system                                     │      │
│  │  - Port: 50051/TCP                                                │      │
│  │  - Protocol: gRPC                                                 │      │
│  │  - Encryption: TLS (service-ca signed)                            │      │
│  │  - Auth: mTLS via ServiceAccount CA                               │      │
│  │  - Accessed via: Istio EnvoyFilter cluster                        │      │
│  └───────────────────────────────────────────────────────────────────┘      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External Services)                                                 │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: ServiceAccount token (in-cluster)                                  │
│  - Purpose: CRUD on all managed resources, SelfSubjectAccessReview          │
│                                                                             │
│  [NVIDIA NGC Catalog API - api.ngc.nvidia.com]                              │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: API Key (header) / Bearer Token                                    │
│  - Purpose: Fetch NIM runtimes and model metadata                           │
│  - Note: Skipped when nim.airGapped=true in DSC                             │
│                                                                             │
│  [NVIDIA Registry - nvcr.io]                                                │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS (Docker Registry API)                                    │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: Bearer Token (OAuth)                                               │
│  - Purpose: Validate API keys via manifest pulls                            │
│  - Note: Skipped when nim.airGapped=true in DSC                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  ClusterRole: odh-model-controller-role
  - API Groups: core, serving.kserve.io, nim.opendatahub.io, gateway.networking.k8s.io,
                kuadrant.io, operator.authorino.kuadrant.io, networking.istio.io,
                route.openshift.io, rbac.authorization.k8s.io, networking.k8s.io,
                monitoring.coreos.com, keda.sh, metrics.k8s.io, config.openshift.io,
                datasciencecluster.opendatahub.io, dscinitialization.opendatahub.io,
                template.openshift.io, infrastructure.opendatahub.io
  - Key Resources: inferenceservices, llminferenceservices, servingruntimes,
                   inferencegraphs, accounts, gateways, authpolicies, envoyfilters,
                   routes, servicemonitors, podmonitors, triggerauthentications
  - Verbs: get, list, watch, create, update, patch, delete (varies per resource)

  ClusterRole: model-serving-api
  - API Groups: gateway.networking.k8s.io, core
  - Resources: gateways (list), namespaces (get)

  ClusterRole: proxy-role / metrics-auth-role
  - API Groups: authentication.k8s.io, authorization.k8s.io
  - Resources: tokenreviews, subjectaccessreviews (create)

Service Account Bindings:
  - odh-model-controller → odh-model-controller-role (ClusterRoleBinding)
  - odh-model-controller → leader-election-role (ClusterRoleBinding)
  - odh-model-controller → proxy-role (ClusterRoleBinding)
  - model-serving-api → model-serving-api (ClusterRoleBinding)
  - Dynamic: {namespace}-{sa}-auth-delegator → system:auth-delegator (per ISVC)

Secrets:
  - odh-model-controller-webhook-cert (kubernetes.io/tls)
    Provisioned: OpenShift service-ca (annotation-driven)
    Auto-Rotate: Yes
  - model-serving-api-tls (kubernetes.io/tls)
    Provisioned: OpenShift service-ca (annotation-driven)
    Auto-Rotate: Yes
  - ray-ca-tls (Opaque, CA cert)
    Provisioned: Controller (self-signed, 4096-bit RSA, 5-year validity)
    Auto-Rotate: No (manual)
  - ray-tls (Opaque, dynamic, per-pod)
    Provisioned: Controller (self-signed, 3-year validity)
    Auto-Rotate: No (recreated on pod IP change)
  - storage-config (Opaque, dynamic)
    Provisioned: Secret controller (aggregated from data connection secrets)
    Auto-Rotate: No (reconciled on change)
  - {account-name}-pull (kubernetes.io/dockerconfigjson, dynamic)
    Provisioned: NIM Account controller
    Auto-Rotate: No (reconciled on API key change)
  - inference-prometheus-auth (Opaque, dynamic)
    Provisioned: KEDA reconciler (ServiceAccount token)
    Auto-Rotate: Yes (Kubernetes SA token rotation)

FIPS Compliance:
  Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking, UBI9 base
  Runtime: FIPS-compliant cipher suites (ECDHE+AES-GCM), TLS 1.2+ min, NIST curves
  No non-FIPS crypto detected (no MD5, RC4, DES, no InsecureSkipVerify)

Authentication & Authorization:
  /api/v1/gateways → Bearer Token → SelfSubjectAccessReview (create llminferenceservices)
  Webhook endpoints → Kubernetes API server TLS client cert → AdmissionReview
  Gateway API listeners → KubernetesTokenReview + SubjectAccessReview → Kuadrant AuthPolicy
  HTTPRoute endpoints → AuthPolicy (Anonymous or UserDefined) → Kuadrant AuthPolicy

Multi-Tenancy:
  - Tenant boundary: per-namespace
  - Deployment model: single-instance multi-tenant
  - Controller watches all namespaces; API server serves all users
  - Data isolation: per-namespace Secrets, ConfigMaps
  - API scoping: RBAC-filtered gateway list via SelfSubjectAccessReview
  - Gap: ClusterRoleBindings (auth-delegator) are cluster-scoped
