┌─────────────────────────────────────────────────────────────────────────┐
│  CONSUMER PROCESS (User Notebook / Script / Pipeline)                    │
│                                                                          │
│  Training Hub is a Python library with NO deployment footprint.          │
│  All network activity originates from the consuming process.             │
│                                                                          │
│  [Training Hub Library]                                                  │
│  - Type: PyPI package (pip install training-hub)                         │
│  - Runtime: Python 3.13+ in consumer process                             │
│  - No Kubernetes manifests, no services, no CRDs                         │
│  - Security posture inherited from consuming environment                 │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  IN-PROCESS BACKENDS                                            │     │
│  │                                                                  │     │
│  │  [InstructLabTrainingSFTBackend]                                 │     │
│  │  - Engine: instructlab-training                                  │     │
│  │  - Training: torchrun distributed (NCCL/Gloo)                   │     │
│  │                                                                  │     │
│  │  [MiniTrainerOSFTBackend]                                        │     │
│  │  - Engine: rhai-innovation-mini-trainer                          │     │
│  │  - Training: torchrun distributed (NCCL/Gloo)                   │     │
│  │                                                                  │     │
│  │  [UnslothLoRABackend]                                            │     │
│  │  - Engine: Unsloth + HuggingFace SFTTrainer                     │     │
│  │  - Training: single-GPU or accelerate                            │     │
│  │                                                                  │     │
│  │  [GEPABackend / MLflowGEPABackend]                               │     │
│  │  - Engine: gepa / mlflow.genai                                   │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  SUBPROCESS-ISOLATED BACKENDS                                   │     │
│  │                                                                  │     │
│  │  [ARTLoRAGRPOBackend]                                            │     │
│  │  - Engine: OpenPipe ART (subprocess)                             │     │
│  │  - Launches vLLM inference server (local HTTP)                   │     │
│  │  - _force_exit_subprocess() for clean shutdown                   │     │
│  │         │                                                        │     │
│  │         │ Port: local (ephemeral)                                │     │
│  │         │ Protocol: HTTP                                         │     │
│  │         │ Encryption: NONE (localhost only)                      │     │
│  │         │ Auth: NONE (local process)                             │     │
│  │         ▼                                                        │     │
│  │  [vLLM Inference Server - subprocess]                            │     │
│  │  - Purpose: Rollout generation for GRPO                          │     │
│  │  - Lifetime: training duration only                              │     │
│  │                                                                  │     │
│  │  [VeRLLoRAGRPOBackend]                                           │     │
│  │  - Engine: verl (Volcano Engine RL, subprocess)                  │     │
│  │  - Training: FSDP multi-GPU via NCCL                             │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  DISTRIBUTED TRAINING (GPU-to-GPU)                              │     │
│  │                                                                  │     │
│  │  [torchrun / NCCL]                                               │     │
│  │  - Protocol: NCCL (GPU-direct) or Gloo (CPU fallback)           │     │
│  │  - Port: master_addr:master_port (ephemeral)                    │     │
│  │  - Encryption: NONE (intra-node GPU communication)              │     │
│  │  - Auth: NONE (process-level trust)                              │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│         │                                                                │
│         │                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  OUTBOUND CONNECTIONS (Egress from Consumer Process)                     │
│                                                                          │
│  Note: All egress is OPTIONAL and user-configured.                       │
│  Network policies are the responsibility of the platform layer.          │
│                                                                          │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (version depends on client library)            │
│         │ Auth: API key (environment variable)                           │
│         ▼                                                                │
│  [Weights & Biases - api.wandb.ai]                                       │
│  - Purpose: Experiment tracking (SFT, OSFT, LoRA, GRPO)                 │
│  - Auth: WANDB_API_KEY environment variable                              │
│  - Optional: only if wandb is installed and configured                   │
│                                                                          │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS                                                │
│         │ Auth: API key or token                                         │
│         ▼                                                                │
│  [MLflow Tracking Server]                                                │
│  - Purpose: Experiment tracking, GEPA prompt optimization                │
│  - Auth: MLFLOW_TRACKING_TOKEN or configured auth                        │
│  - Optional: only if mlflow is installed and configured                  │
│                                                                          │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS                                                │
│         │ Auth: HF_TOKEN environment variable (optional)                 │
│         ▼                                                                │
│  [HuggingFace Hub - huggingface.co]                                      │
│  - Purpose: Model config download (memory estimation)                    │
│  - Auth: HF_TOKEN (optional, for gated models)                          │
│  - Used by: estimate() for model metadata                                │
│                                                                          │
│         │ Port: varies                                                   │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS                                                │
│         │ Auth: API key (via litellm config)                             │
│         ▼                                                                │
│  [LLM API Providers - via litellm]                                       │
│  - Purpose: Reward model evaluation (GRPO), GEPA optimization           │
│  - Auth: Provider-specific API keys via environment                      │
│  - Optional: only for litellm-based reward functions                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- NONE -- Training Hub is a library, not a Kubernetes operator or service.
- No ClusterRoles, Roles, RoleBindings, or ServiceAccounts defined.
- RBAC requirements are inherited from the consuming environment
  (e.g., notebook ServiceAccount in RHOAI).

Secrets / Credentials:
- NONE managed by Training Hub directly.
- User-provided credentials via environment variables at runtime:
  - WANDB_API_KEY (optional) -- Weights & Biases experiment tracking
  - MLFLOW_TRACKING_TOKEN (optional) -- MLflow tracking server auth
  - HF_TOKEN (optional) -- HuggingFace Hub for gated model access
  - LLM provider API keys (optional) -- via litellm for reward models
- No Kubernetes Secrets created or managed.

Service Mesh / Network Policies:
- NOT APPLICABLE -- Training Hub has no Kubernetes Services or Pods.
- Network policies for egress are the responsibility of the platform
  layer (e.g., RHOAI namespace NetworkPolicy).

FIPS Compliance:
- Training Hub performs NO cryptographic operations directly.
- TLS for outbound HTTPS connections is handled by Python's ssl module
  and transitive dependencies (requests, urllib3, litellm, wandb).
- FIPS compliance is inherited from the host OS and Python runtime.
- No direct imports of hashlib, ssl, or crypto modules detected.

Supply Chain Notes:
- litellm dependency explicitly excludes v1.82.0 (supply chain issue)
- Floor version pins on critical deps (torch>=2.6.0, transformers>=4.51.0)
- No lock files present (expected: library, not container image)
