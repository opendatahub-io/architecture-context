┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client - UI BFF / CLI / SDK]                                           │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: OAuth2/OIDC (platform-level)                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ)                                                          │
│                                                                          │
│  [Istio IngressGateway]                                                  │
│  - Port: 443/TCP (external) → 8080/TCP (internal)                      │
│  - TLS Termination: TLS 1.2+                                            │
│  - AuthN: RequestAuthentication (validates JWT if present)              │
│  - AuthZ: AuthorizationPolicy (allows istio-ingressgateway SA)         │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: mTLS (ISTIO_MUTUAL)                                │
│         │ Auth: Istio service identity                                   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - mTLS ISTIO_MUTUAL)                             │
│                                                                          │
│  [model-registry proxy]                                                  │
│  - Namespace: {model-registry-namespace}                                 │
│  - ServiceAccount: model-registry                                        │
│  - Port: 8080/TCP (HTTP, mTLS via sidecar)                              │
│  - sidecar.istio.io/inject: "true"                                       │
│  - VirtualService: routes via kubeflow-gateway                           │
│  - DestinationRule: ISTIO_MUTUAL trafficPolicy                          │
│  - Image: odh-model-registry                                             │
│  - Build: Go, GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1             │
│  - TLS Config: MinVersion TLS 1.2, insecure ciphers rejected            │
│         │                                                                │
│         │ Port: 3306/TCP (MySQL) or 5432/TCP (PostgreSQL)               │
│         │ Protocol: MySQL/PostgreSQL protocol                            │
│         │ Encryption: Optional TLS (configurable)                        │
│         │ Auth: Username/password (via Secret)                           │
│         ▼                                                                │
│  [Database - MySQL 8.3+ / PostgreSQL 16+]                                │
│  - Namespace: {model-registry-namespace}                                 │
│  - Port: 3306/TCP (MySQL) or 5432/TCP (PostgreSQL)                      │
│  - Storage: PVC-backed                                                   │
│                                                                          │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│                                                                          │
│  [InferenceService Controller]                                           │
│  - ServiceAccount: controller-manager                                    │
│  - Metrics Port: 8443/TCP (HTTPS, TLS autogenerated)                    │
│  - Health Port: 8081/TCP (HTTP, liveness/readiness)                      │
│         │                                                                │
│         ├──── Port: 443/TCP ──── [Kubernetes API Server]                │
│         │     Protocol: HTTPS                                            │
│         │     Encryption: TLS                                            │
│         │     Auth: ServiceAccount token                                 │
│         │     Purpose: Watch InferenceService CRs, update labels         │
│         │                                                                │
│         └──── Port: 8080/TCP ──── [model-registry proxy]                │
│               Protocol: HTTP                                             │
│               Encryption: mTLS (via mesh)                                │
│               Auth: Bearer token                                         │
│               Purpose: Sync deployment metadata to registry              │
│               Note: SKIP_TLS_VERIFY option exists (default: false)       │
│                                                                          │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│                                                                          │
│  [mr-storage-initializer (CSI)]                                          │
│  - Runs as: KServe init container                                        │
│  - Purpose: Resolves model-registry:// URIs                              │
│         │                                                                │
│         ├──── Port: 8080/TCP ──── [model-registry proxy]                │
│         │     Protocol: HTTP                                             │
│         │     Encryption: mTLS (via mesh)                                │
│         │     Auth: None                                                 │
│         │     Purpose: Resolve artifact URI                              │
│         │                                                                │
│         └──── Port: 443/TCP ──── [Model artifact storage]               │
│               Protocol: HTTPS                                            │
│               Encryption: TLS 1.2+                                       │
│               Auth: Per-storage credentials                              │
│               Purpose: Download model artifacts                          │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  JOBS (One-shot workloads)                                               │
│                                                                          │
│  [async-upload Job]                                                      │
│  - Image: odh-model-registry-job-async-upload-rhel9                      │
│  - Base: quay.io/aipcc/base-images/cpu:3.5                              │
│  - Language: Python                                                      │
│         │                                                                │
│         ├──── Port: 443/TCP ──── [S3-compatible Storage]                │
│         │     Protocol: HTTPS                                            │
│         │     Encryption: TLS 1.2+                                       │
│         │     Auth: AWS IAM credentials                                  │
│         │     Purpose: Model download/upload                             │
│         │                                                                │
│         ├──── Port: 443/TCP ──── [OCI Container Registry]               │
│         │     Protocol: HTTPS                                            │
│         │     Encryption: TLS 1.2+                                       │
│         │     Auth: Docker config auth                                   │
│         │     Purpose: Model image push/pull                             │
│         │                                                                │
│         ├──── Port: 443/TCP ──── [HuggingFace Hub]                      │
│         │     Protocol: HTTPS                                            │
│         │     Encryption: TLS 1.2+                                       │
│         │     Auth: API key (optional)                                   │
│         │     Purpose: Model download                                    │
│         │                                                                │
│         ├──── Port: 443/TCP ──── [Sigstore (Fulcio/Rekor)]              │
│         │     Protocol: HTTPS                                            │
│         │     Encryption: TLS 1.2+                                       │
│         │     Auth: OIDC identity token                                  │
│         │     Purpose: Model and image signing                           │
│         │                                                                │
│         └──── Port: 443/TCP ──── [Model Registry API]                   │
│               Protocol: HTTPS                                            │
│               Encryption: TLS 1.2+                                       │
│               Auth: Bearer token / custom CA                             │
│               Purpose: Register models and artifacts                     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [MySQL 8.3+ / PostgreSQL 16+]                                           │
│  - Purpose: Metadata storage backend (one or the other)                  │
│  - Auth: Username/password credentials                                   │
│  - Encryption: Optional TLS (configurable ciphers, server cert verify)  │
│                                                                          │
│  [S3-compatible Storage - s3.amazonaws.com or equivalent]                │
│  - Purpose: Model artifact storage                                       │
│  - Auth: AWS IAM credentials                                             │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [OCI Container Registry]                                                │
│  - Purpose: Model image storage                                          │
│  - Auth: Docker config auth                                              │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Sigstore - Fulcio / Rekor]                                             │
│  - Purpose: Model signing infrastructure                                 │
│  - Auth: OIDC identity token                                             │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [HuggingFace Hub]                                                       │
│  - Purpose: Model download source                                        │
│  - Auth: API key (optional)                                              │
│  - Encryption: TLS 1.2+                                                  │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: model-registry-manager-role
  - API Groups: serving.kserve.io
  - Resources: inferenceservices (get, list, watch, update, patch)
  - Resources: inferenceservices/finalizers (get, list, watch, update, create, patch, delete)
  - API Groups: "" (core)
  - Resources: services (get, list, watch)

- Role: leader-election-role
  - API Groups: "" (core)
  - Resources: configmaps (get, list, watch, create, update, patch, delete)
  - API Groups: coordination.k8s.io
  - Resources: leases (get, list, watch, create, update, patch, delete)
  - API Groups: "" (core)
  - Resources: events (create, patch)

- ClusterRole: metrics-auth-role
  - API Groups: authentication.k8s.io
  - Resources: tokenreviews (create)
  - API Groups: authorization.k8s.io
  - Resources: subjectaccessreviews (create)

Service Mesh Configuration:
- VirtualService: model-registry
  - Gateway: kubeflow-gateway
  - Route: /* → model-registry-service:8080
- DestinationRule: model-registry
  - TrafficPolicy: tls.mode = ISTIO_MUTUAL
- AuthorizationPolicy: model-registry
  - Rule 1: Allow from istio-ingressgateway SA
  - Rule 2: Allow from kubeflow namespace with authorization header
    (excludes requests with kubeflow-userid header)

Secrets:
- mysql-secret (Opaque) - Database credentials (username, password), manual provisioning, no auto-rotation
- model-registry-db-secrets (Opaque) - Generated from secrets.env via kustomize secretGenerator, no auto-rotation

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
- Runtime: OpenSSL via UBI 9 minimal base
- TLS: MinVersion TLS 1.2, insecure cipher suites explicitly rejected
- Risk: SKIP_TLS_VERIFY option in controller (default false)

Multi-Tenancy:
- Boundary: Per-namespace (each namespace gets its own Model Registry instance + database)
- Isolation: Separate Deployment + database per tenant namespace
- Risk: No application-level auth; if Istio misconfigured, API is open
- Risk: AuthorizationPolicy allows any pod in kubeflow namespace with auth header
