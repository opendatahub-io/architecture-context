┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [Platform Admin / Helm Client]                                             │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: kubeconfig credentials (client cert or token)               │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP (external), 6443/TCP (internal)                           │
│  - Protocol: HTTPS                                                         │
│  - TLS Termination: TLS 1.2+                                               │
│  - AuthN: Client certificates, Bearer tokens, OIDC                        │
│  - AuthZ: RBAC                                                             │
│         │                                                                   │
│         ├──────────────────────────────────────┐                            │
│         │                                      │                            │
│         │ (OpenShift path)                      │ (xKS path)               │
│         ▼                                      ▼                            │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-operator / redhat-ods-applications)         │
│                                                                             │
│  [rhods-operator / rhai-operator]                                           │
│  - Namespace: redhat-ods-operator                                           │
│  - ServiceAccount: rhai-operator-controller-manager                        │
│  - ClusterRole: rhai-operator-role (broad cluster-wide permissions)         │
│         │                                                                   │
│         │ Port: 8443/TCP (metrics)                                          │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS                                                   │
│         │ Auth: ServiceAccount Bearer token (kube-rbac-proxy)              │
│         │                                                                   │
│         │ Port: 443/TCP → 9443/TCP (webhook)                               │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (cert-manager provisioned)                        │
│         │ Auth: Webhook certificate validation                              │
│         │                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  POST-INSTALL HOOK JOBS (xKS path only)                                     │
│                                                                             │
│  [post-install-crs Job]                                                     │
│  - Image: ose-cli                                                           │
│  - ServiceAccount: rhai-post-install-hook                                  │
│  - Creates: KubernetesEngine, Platform, Kserve, AIGateway CRs             │
│         │                                                                   │
│  [post-install-gateway Job]                                                 │
│  - Image: ose-cli                                                           │
│  - Creates: GatewayClass, Gateway, Certificate CRs                        │
│         │                                                                   │
│         │ Port: internal (K8s API)                                          │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+ (in-cluster)                                │
│         │ Auth: ServiceAccount token (RBAC)                                │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INFERENCE GATEWAY LAYER                                                    │
│                                                                             │
│  [Inference Gateway - openshift-ai-inference]                               │
│  - Port: 80/TCP (HTTP), 443/TCP (HTTPS)                                   │
│  - Protocol: HTTP / HTTPS                                                  │
│  - Encryption: TLS (cert-manager Certificate)                              │
│  - TLS Mode: Terminate                                                     │
│  - Auth: Backend auth (delegated)                                          │
│  - GatewayClass controller: Sail/Istio                                     │
│                                                                             │
│  [MaaS Gateway - maas-gateway-class]                                        │
│  - Port: 80/TCP (HTTP), 443/TCP (HTTPS)                                   │
│  - Protocol: HTTP / HTTPS                                                  │
│  - Encryption: TLS (cert-manager Certificate)                              │
│  - TLS Mode: Terminate                                                     │
│  - Auth: TLS + Authorino (Kuadrant AuthPolicy)                            │
│  - GatewayClass controller: Sail/Istio                                     │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  DEPENDENCY OPERATORS (installed by odh-gitops)                             │
│                                                                             │
│  [cert-manager] ─── TLS certificate provisioning                           │
│  [Kueue] ─── Job queue for distributed workloads                           │
│  [RHCL / Kuadrant] ─── API mgmt, AuthPolicy, rate limiting                │
│  [Authorino] ─── Auth/AuthZ for MaaS (CA trust patched by hook)           │
│  [Sail / Istio] ─── Service mesh, Gateway API, GatewayClass               │
│  [Leader Worker Set] ─── Distributed inference orchestration               │
│  [JobSet] ─── Job management for training                                  │
│  [NFD] ─── Node hardware feature discovery                                 │
│  [NVIDIA GPU Operator] ─── GPU driver and device plugin                    │
│  [Cluster Observability] ─── Monitoring and alerting                       │
│  [OpenTelemetry] ─── Distributed tracing instrumentation                   │
│  [Tempo] ─── Tracing backend                                              │
│  [KEDA] ─── Event-driven autoscaling                                      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [OLM Catalog Sources]                                                      │
│  - Port: 443/TCP                                                           │
│  - Protocol: HTTPS                                                         │
│  - Encryption: TLS 1.2+                                                    │
│  - Auth: None                                                              │
│  - Purpose: Operator package resolution (OpenShift chart only)             │
│                                                                             │
│  [GitHub - RHOAI-Build-Config]                                              │
│  - Port: 443/TCP                                                           │
│  - Protocol: HTTPS                                                         │
│  - Encryption: TLS 1.2+                                                    │
│  - Auth: GitHub App token                                                  │
│  - Purpose: Helm sync workflow pushes chart updates                        │
│                                                                             │
│  [GitHub - ODH-Build-Config]                                                │
│  - Port: 443/TCP                                                           │
│  - Protocol: HTTPS                                                         │
│  - Encryption: TLS 1.2+                                                    │
│  - Auth: GitHub App token                                                  │
│  - Purpose: Source of operator images for xKS chart                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  ClusterRole: rhai-operator-role
  - Bound to SA: rhai-operator-controller-manager (redhat-ods-operator)
  - Key permissions:
    - components.platform.opendatahub.io/* → CRUD all component CRs
    - datasciencecluster.opendatahub.io/* → CRUD DSC CRs
    - dscinitialization.opendatahub.io/* → CRUD DSCI CRs
    - config.opendatahub.io/platforms → get, list, watch, update, patch
    - gateway.networking.k8s.io/* → CRUD GatewayClass, Gateway, HTTPRoute
    - serving.kserve.io/* → CRUD InferenceServices, ServingRuntimes
    - cert-manager.io/* → CRUD Certificates, Issuers
    - kuadrant.io/* → CRUD AuthPolicies, RateLimitPolicies
    - rbac.authorization.k8s.io/* → * (WILDCARD on all RBAC resources)
    - admissionregistration.k8s.io/* → CRUD webhooks
    - apiextensions.k8s.io/crds → CRUD
    - core (configmaps, secrets, services, etc.) → CRUD
    - apps (deployments, daemonsets, statefulsets) → CRUD
    - monitoring.coreos.com + monitoring.rhobs → CRUD

  ⚠️  SECURITY NOTE: Wildcard (*) on rbac.authorization.k8s.io resources
      grants ability to create/modify any RBAC binding cluster-wide.

  ClusterRole: rhai-post-install-hook
  - Bound to SA: rhai-post-install-hook
  - Permissions:
    - gateway.networking.k8s.io (gatewayclasses, gateways) → create, get, list, watch
    - infrastructure.opendatahub.io (KubernetesEngine CRs) → create, get, list, watch
    - config.opendatahub.io (platforms) → create, get, list, watch
    - cert-manager.io (certificates) → create, get, list, watch

Secrets:

  | Secret                              | Type                    | Purpose                          | Provisioned By   | Rotation   |
  |-------------------------------------|-------------------------|----------------------------------|------------------|------------|
  | rhai-operator-controller-webhook-cert| kubernetes.io/tls       | Webhook TLS certificate          | cert-manager     | Yes (auto) |
  | rhai-pull-secret                    | dockerconfigjson         | Image pull secret                | User-provided    | No         |
  | rhai-ca                             | Opaque                  | Internal CA for cert-manager     | cert-manager     | No         |
  | inference-gateway-cert-secret       | kubernetes.io/tls       | Inference Gateway TLS            | cert-manager     | Yes (auto) |
  | maas-gateway-cert-secret            | kubernetes.io/tls       | MaaS Gateway TLS                 | cert-manager     | Yes (auto) |
  | maas-api-serving-cert               | kubernetes.io/tls       | MaaS API serving TLS             | cert-manager     | Yes (auto) |

Authentication & Authorization:

  | Endpoint              | Auth Mechanism                        | Enforcement Point                |
  |-----------------------|---------------------------------------|----------------------------------|
  | Inference Gateway     | TLS termination + backend auth        | Gateway API (Istio)              |
  | MaaS Gateway          | TLS + Authorino (AuthPolicy)          | Gateway API (Istio) + Kuadrant   |
  | Operator Webhook      | TLS mutual auth                       | Kubernetes API server            |
  | Operator Metrics      | ServiceAccount Bearer token           | kube-rbac-proxy                  |
