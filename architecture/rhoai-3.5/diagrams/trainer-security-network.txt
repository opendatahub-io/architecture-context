┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [User / SDK / kubectl]                                                     │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Bearer Token (kubeconfig)                                   │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP (external) → 6443/TCP (internal)                         │
│  - TLS: TLS 1.2+                                                           │
│  - AuthN: Client certificates, Bearer tokens, OIDC                        │
│  - AuthZ: RBAC                                                             │
│         │                                                                   │
│         │ Port: 9443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+ (OpenShift cluster profile)                 │
│         │ Auth: API Server TLS client certificate                          │
│         │ Failure Policy: Fail (fail-closed)                               │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (opendatahub)                                           │
│                                                                             │
│  [trainer-controller-manager]                                               │
│  - ServiceAccount: kubeflow-trainer-controller-manager                     │
│  - Image: quay.io/opendatahub/trainer:odh-3.5-ea1                         │
│  - FIPS: CGO_ENABLED=1 GOEXPERIMENT=strictfipsruntime                     │
│  - Build: Dynamic linking, UBI 9 minimal base                             │
│                                                                             │
│    ┌──────────────────────────────────┐                                     │
│    │ Webhook Server                   │                                     │
│    │ Port: 9443/TCP (HTTPS)           │                                     │
│    │ TLS: Cluster profile / Intermediate                                   │
│    │ Cert: /tmp/k8s-webhook-server/serving-certs                           │
│    │ CA: open-policy-agent/cert-controller (self-signed)                   │
│    │ Rotation: Automatic                                                    │
│    │                                                                        │
│    │ Endpoints:                                                             │
│    │  POST /validate-*-trainjob                                            │
│    │  POST /validate-*-trainingruntime                                     │
│    │  POST /validate-*-clustertrainingruntime                              │
│    └──────────────────────────────────┘                                     │
│                                                                             │
│    ┌──────────────────────────────────┐                                     │
│    │ Metrics Server                   │                                     │
│    │ Port: 8443/TCP (HTTPS)           │                                     │
│    │ TLS: Cluster profile / Intermediate                                   │
│    │ Auth: None (Prometheus scraping)                                      │
│    │ Endpoint: GET /metrics                                                │
│    └──────────────────────────────────┘                                     │
│                                                                             │
│    ┌──────────────────────────────────┐                                     │
│    │ Health Probes                    │                                     │
│    │ Port: 8081/TCP (HTTP)            │                                     │
│    │ Encryption: None                 │                                     │
│    │ GET /healthz (liveness)          │                                     │
│    │ GET /readyz (readiness)          │                                     │
│    └──────────────────────────────────┘                                     │
│         │                                                                   │
│         │ Egress                                                            │
│         ├──▶ Kubernetes API Server   443/TCP  HTTPS TLS 1.2+              │
│         │    Auth: ServiceAccount Token                                     │
│         │    Purpose: CRD CRUD, leader election, webhook cert mgmt         │
│         │                                                                   │
│         ├──▶ OpenShift APIServer CR  443/TCP  HTTPS TLS 1.2+              │
│         │    Auth: ServiceAccount Token                                     │
│         │    Purpose: Read cluster TLS security profile                    │
│         │                                                                   │
│         └──▶ Training Pod /metrics   28080/TCP  HTTP  No encryption       │
│              Auth: None (NetworkPolicy enforced)                           │
│              Purpose: RHAI progression tracking (loss, epoch, step)        │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  TRAINING NAMESPACE ({user-namespace})                                      │
│                                                                             │
│  [Training Pods] (created by JobSet)                                        │
│  - Per-TrainJob NetworkPolicy (RHAI)                                       │
│  - Ingress restricted to:                                                  │
│    • Same-JobSet peer pods (training traffic)                              │
│    • Controller pod (metrics scraping)                                     │
│                                                                             │
│    ┌──────────────────────────────────┐                                     │
│    │ PyTorch Distributed (torchrun)   │                                     │
│    │ Port: 29500/TCP (rendezvous)     │                                     │
│    │ Protocol: TCP                    │                                     │
│    │ Encryption: None                 │                                     │
│    │ Auth: None (NetworkPolicy)       │                                     │
│    └──────────────────────────────────┘                                     │
│                                                                             │
│    ┌──────────────────────────────────┐                                     │
│    │ MPI Training                     │                                     │
│    │ Port: 22/TCP (SSH)               │                                     │
│    │ Protocol: SSH                    │                                     │
│    │ Encryption: ECDSA key pair       │                                     │
│    │ Auth: SSH key from Secret        │                                     │
│    │ Secret: {trainjob}-mpi-ssh-auth  │                                     │
│    │ (immutable, per-TrainJob)        │                                     │
│    └──────────────────────────────────┘                                     │
│                                                                             │
│    ┌──────────────────────────────────┐                                     │
│    │ Metrics Endpoint                 │                                     │
│    │ Port: 28080/TCP (configurable)   │                                     │
│    │ Protocol: HTTP                   │                                     │
│    │ Encryption: None                 │                                     │
│    │ Auth: None (NetworkPolicy)       │                                     │
│    │ Purpose: RHAI progression        │                                     │
│    └──────────────────────────────────┘                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  ClusterRole: kubeflow-trainer-controller-manager
    - "" / configmaps, secrets: create, get, list, patch, update, watch
    - "" / events: create, patch, update, watch
    - "" / limitranges: get, list, watch
    - "" / pods: get, list
    - admissionregistration.k8s.io / validatingwebhookconfigurations: get, list, update, watch
    - config.openshift.io / apiservers: get
    - coordination.k8s.io / leases: create, get, list, update
    - jobset.x-k8s.io / jobsets: create, get, list, patch, update, watch
    - networking.k8s.io / networkpolicies: get, list, watch, create, update, patch
    - node.k8s.io / runtimeclasses: get, list, watch
    - scheduling.volcano.sh / podgroups: create, get, list, patch, update, watch
    - scheduling.x-k8s.io / podgroups: create, get, list, patch, update, watch
    - trainer.kubeflow.org / clustertrainingruntimes: get, list, patch, update, watch
    - trainer.kubeflow.org / clustertrainingruntimes/finalizers: get, patch, update
    - trainer.kubeflow.org / trainingruntimes: get, list, patch, update, watch
    - trainer.kubeflow.org / trainingruntimes/finalizers: get, patch, update
    - trainer.kubeflow.org / trainjobs: get, list, patch, update, watch
    - trainer.kubeflow.org / trainjobs/finalizers: get, patch, update
    - trainer.kubeflow.org / trainjobs/status: get, patch, update

  Aggregated ClusterRoles:
    - training-admin: full CRUD on trainjobs, trainingruntimes, clustertrainingruntimes
    - training-edit: full CRUD on trainjobs; read-only on runtimes
    - training-view: read-only on all trainer resources

Secrets:
  - kubeflow-trainer-webhook-cert (kubernetes.io/tls)
    Provisioned by: open-policy-agent/cert-controller (self-signed)
    Auto-rotate: Yes
    Purpose: Webhook server TLS certificate and CA

  - {trainjob-name}-mpi-ssh-auth (Opaque, immutable)
    Provisioned by: MPI plugin (auto-generated per TrainJob)
    Auto-rotate: No
    Purpose: ECDSA SSH key pair for MPI inter-node communication

TLS Configuration:
  - Dynamic TLS profile from OpenShift APIServer CR (config.openshift.io/v1)
  - Fallback: Intermediate profile (TLS 1.2+ with ECDHE cipher suites)
  - ALPN: h2, http/1.1
  - Applied to: webhook server (9443) and metrics server (8443)

FIPS Compliance:
  - Build flags: CGO_ENABLED=1 GOEXPERIMENT=strictfipsruntime
  - Dynamic linking with UBI 9 minimal base (OpenSSL at runtime)
  - No non-FIPS crypto risks detected
