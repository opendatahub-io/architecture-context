┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL PLATFORM (Trusted Cluster Network)                            │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: (orchestrator namespace)                                   │
│  - Role: Routes detection requests to downstream detectors               │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None                                                     │
│         │ Direction: Inbound to detector                                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE (Internal Only - No External Exposure)                         │
│                                                                          │
│  [Guardrails Regex Detector]                                             │
│  - Service Type: ClusterIP (inferred)                                    │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP (plaintext)                                            │
│  - Language: Rust (axum framework)                                       │
│  - Namespace: (deployment namespace)                                     │
│  - ServiceAccount: (default or assigned)                                 │
│  - Container User: root (no USER directive in Dockerfile)                │
│                                                                          │
│  Endpoints:                                                              │
│  - POST /api/v1/text/contents  (PII/regex detection)                    │
│  - GET  /health                (health check)                            │
│                                                                          │
│  No outbound connections (stateless, no egress)                          │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  (None - service has no egress dependencies)                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK EXPOSURE:
- Ingress: None (no external ingress configured)
- Egress: None (no outbound connections)
- Service Type: ClusterIP (internal only)
- External Access: Not exposed; accessed only by orchestrator within cluster

SECURITY POSTURE:
- TLS: None at application level
  * Service listens on plaintext HTTP only
  * No TLS configuration, no certificate handling
  * Network-level encryption (mTLS via service mesh) must be provided by platform
- Authentication: None
  * No auth mechanism at application level
  * Relies on network isolation and orchestrator-mediated access
- Authorization: None
  * All requests processed identically
  * No tenant scoping or access control
- Input Validation: Minimal
  * Checks for non-empty regex parameters only
  * Custom regex patterns accepted without sanitization

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount-specific permissions

Secrets:
- None consumed or referenced

FIPS Compliance:
- Application Level: Not applicable (no cryptographic operations)
- No TLS, no crypto crates, no certificate handling
- compat-openssl11 installed in runtime image but not used by application
- Build: Standard Rust compilation, no FIPS flags
- No Konflux build integration

Container Security Concerns:
- Container runs as root (no USER directive in Dockerfile)
- No NetworkPolicy defined at application level
- No resource limits defined in application code
- Base image: ubi9/ubi-minimal:latest (unpinned)

CRYPTO OPERATIONS:
- None detected
- Service performs regex matching only
- No TLS termination, no token validation, no hashing
