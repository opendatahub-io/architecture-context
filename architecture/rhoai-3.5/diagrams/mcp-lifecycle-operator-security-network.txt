┌─────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted)                                              │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  Namespace: mcp-lifecycle-operator-system                       │     │
│  │                                                                  │     │
│  │  [MCP Lifecycle Operator]                                        │     │
│  │  - Deployment: controller-manager (1 replica)                    │     │
│  │  - ServiceAccount: controller-manager                            │     │
│  │  - Image: controller:latest (overridden at deploy)               │     │
│  │  - Resources: 10m-500m CPU, 128Mi-512Mi memory                   │     │
│  │  - GOMEMLIMIT: 460MiB                                            │     │
│  │  - Pod Security: non-root, read-only rootfs, drop ALL caps,      │     │
│  │                  RuntimeDefault seccomp                           │     │
│  │  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1           │     │
│  │                                                                  │     │
│  │  Exposed Ports:                                                  │     │
│  │  ┌────────────────────────────────────────────────────────┐      │     │
│  │  │ Port: 8443/TCP                                         │      │     │
│  │  │ Protocol: HTTPS                                        │      │     │
│  │  │ Encryption: TLS (self-signed or cert-manager)          │      │     │
│  │  │ Auth: Bearer Token (TokenReview + SubjectAccessReview) │      │     │
│  │  │ Purpose: Prometheus metrics (/metrics)                 │      │     │
│  │  │ Service: controller-manager-metrics-service (ClusterIP)│      │     │
│  │  │ NetworkPolicy: allow-metrics-traffic                   │      │     │
│  │  │   - From: namespaces with label metrics=enabled        │      │     │
│  │  └────────────────────────────────────────────────────────┘      │     │
│  │  ┌────────────────────────────────────────────────────────┐      │     │
│  │  │ Port: 8081/TCP                                         │      │     │
│  │  │ Protocol: HTTP                                         │      │     │
│  │  │ Encryption: None                                       │      │     │
│  │  │ Auth: None                                             │      │     │
│  │  │ Purpose: Health probes (/healthz, /readyz)             │      │     │
│  │  │ NetworkPolicy: allow-metrics-traffic                   │      │     │
│  │  │   - From: namespaces with label metrics=enabled        │      │     │
│  │  └────────────────────────────────────────────────────────┘      │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│         │                              ▲                                 │
│         │                              │                                 │
│         │ Port: 443/TCP                │ Port: 8443/TCP                  │
│         │ Protocol: HTTPS              │ Protocol: HTTPS                 │
│         │ Encryption: TLS              │ Encryption: TLS                 │
│         │ Auth: ServiceAccount Token   │ Auth: Bearer Token              │
│         ▼                              │                                 │
│  ┌──────────────────────┐    ┌─────────────────────┐                    │
│  │ [Kubernetes API      │    │ [Prometheus]         │                    │
│  │  Server]             │    │ Namespace: varies    │                    │
│  │ Port: 443/TCP        │    │ Scrapes /metrics     │                    │
│  │ Protocol: HTTPS      │    │ via ServiceMonitor   │                    │
│  │ Auth: SA Token       │    └─────────────────────┘                    │
│  │                      │                                                │
│  │ Resources managed:   │                                                │
│  │ - Deployments        │                                                │
│  │ - Services           │                                                │
│  │ - NetworkPolicies    │                                                │
│  │ - ConfigMaps (read)  │                                                │
│  │ - Secrets (read)     │                                                │
│  │ - MCPServers         │                                                │
│  │ - Events             │                                                │
│  └──────────────────────┘                                                │
│         │                                                                │
│         │ Watch events (MCPServer, ConfigMap, Secret, etc.)              │
│         │                                                                │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  Namespace: {user-namespace} (per MCPServer CR)                 │     │
│  │                                                                  │     │
│  │  [MCP Server Pod]                                                │     │
│  │  - Deployment managed by operator                                │     │
│  │  - Image: user-specified                                         │     │
│  │  - Pod Security: non-root, read-only rootfs, drop ALL caps      │     │
│  │                                                                  │     │
│  │  ┌────────────────────────────────────────────────────────┐      │     │
│  │  │ Port: {spec.config.port}/TCP                           │      │     │
│  │  │ Protocol: HTTP (MCP Streamable HTTP)                   │      │     │
│  │  │ Encryption: None (in-cluster)                          │      │     │
│  │  │ Auth: None (operator treats 401/403 as reachable)      │      │     │
│  │  │ Purpose: MCP protocol endpoint                         │      │     │
│  │  │ Service: {mcpserver-name} (ClusterIP)                  │      │     │
│  │  │ NetworkPolicy: {mcpserver-name} (ingress-only)         │      │     │
│  │  │   - From: Any source (no namespace selector)           │      │     │
│  │  │   - Only port is restricted                            │      │     │
│  │  └────────────────────────────────────────────────────────┘      │     │
│  │                                                                  │     │
│  │  Operator ──► MCP Server Pod                                     │     │
│  │    Port: {spec.config.port}/TCP                                  │     │
│  │    Protocol: HTTP (MCP Streamable HTTP)                          │     │
│  │    Encryption: None                                              │     │
│  │    Auth: None                                                    │     │
│  │    Purpose: MCP protocol handshake (initialize)                  │     │
│  │    Retry: exponential backoff 10s→20s→40s→80s→120s cap, 10 max  │     │
│  │                                                                  │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│                                                                          │
│  No external egress from operator (all traffic is in-cluster)           │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: manager-role
  - API Groups: "" (core)
    - configmaps, secrets: get, list, watch
    - pods: get, list
    - services: create, get, list, update, watch
    - events: create, patch
  - API Groups: apps
    - deployments: create, get, list, update, watch
  - API Groups: mcp.x-k8s.io
    - mcpservers: get, list, patch, update, watch
    - mcpservers/finalizers: update
    - mcpservers/status: get, patch, update
  - API Groups: networking.k8s.io
    - networkpolicies: create, get, list, update, watch

- ClusterRole: mcpserver-admin-role
  - mcpservers: * (full access)
  - mcpservers/status: get

- ClusterRole: mcpserver-editor-role
  - mcpservers: create, delete, get, list, patch, update, watch
  - mcpservers/status: get

- ClusterRole: mcpserver-viewer-role
  - mcpservers: get, list, watch
  - mcpservers/status: get

- ClusterRole: metrics-auth-role
  - tokenreviews: create (authentication.k8s.io)
  - subjectaccessreviews: create (authorization.k8s.io)

Role Bindings:
- leader-election-rolebinding → leader-election-role (Role, ns: mcp-lifecycle-operator-system) → SA: controller-manager
- manager-rolebinding → manager-role (ClusterRole) → SA: controller-manager
- metrics-auth-rolebinding → metrics-auth-role (ClusterRole) → SA: controller-manager

NetworkPolicy Configuration:
- allow-metrics-traffic (operator namespace):
  - Ingress from: namespaces with label metrics=enabled
  - Ports: 8443/TCP, 8081/TCP
- {mcpserver-name} (per MCPServer CR, user namespace):
  - Ingress from: any source (no namespace selector)
  - Ports: {spec.config.port}/TCP only

TLS Configuration:
- Metrics endpoint (8443/TCP): TLS (self-signed or cert-manager)
  - Configurable via --metrics-cert-path flag
  - TLS_MIN_VERSION env var (defaults to Go stdlib)
  - TLS_CIPHER_SUITES env var (configurable)
  - HTTP/2 disabled (CVE-2023-44487 mitigation)
- Health probes (8081/TCP): No TLS (plaintext HTTP)
- MCP handshake: No TLS (plaintext HTTP, in-cluster)

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
- Base image: registry.redhat.io/ubi9/ubi-minimal (OpenSSL included)
- Crypto: stdlib crypto/tls via OpenSSL (FIPS mode)
- Config hash: SHA-256 (FIPS-approved)
- No non-FIPS crypto detected (no MD5/RC4/DES)

Secrets:
- User-referenced secrets via MCPServer.spec.config.storage/env (Opaque, user-provisioned)
- Metrics/webhook TLS certs via --metrics-cert-path / --webhook-cert-path (self-signed or cert-manager)
