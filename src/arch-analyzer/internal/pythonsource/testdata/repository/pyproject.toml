[project]
name = "widget-api"
version = "1.2.3"
description = "Widget API service"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "grpcio>=1.67",
  "httpx>=0.27",
]

[project.optional-dependencies]
storage = ["pydantic>=2.0"]
