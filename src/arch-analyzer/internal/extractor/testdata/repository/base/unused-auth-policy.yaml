apiVersion: kuadrant.io/v1
kind: AuthPolicy
metadata:
  name: unused-policy
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: controller
  rules:
    authentication:
      cluster-token:
        kubernetesTokenReview: {}
